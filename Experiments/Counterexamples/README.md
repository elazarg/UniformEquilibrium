# Counterexample programs

Give each search program a stable directory. Keep reusable code under `src/`
and immutable executions under `runs/<run-id>/`. A run manifest pins the claim,
revisions, parameters, seed, command, and exact evidence produced.
