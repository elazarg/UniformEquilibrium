import UniformEquilibrium.Quitting.Classification.LCP.MatrixClasses
import UniformEquilibrium.Quitting.Classification.LCP.NormalCore
import UniformEquilibrium.Quitting.Classification.LCP.StationaryExistence
import UniformEquilibrium.Quitting.Classification.LCP.StrategicTransport
import UniformEquilibrium.Quitting.Classification.TableExistenceBranches
import UniformEquilibrium.Quitting.Punishment.ApproximateCompletedCycle
import MathUE.CaristiFixedPoint
import MathUE.DivergentChargeRecurrence
import MathUE.Probability.FiniteClosedCoreReach
import MathUE.ProbabilityMassFunction.Simplex
import Mathlib.Analysis.Normed.Affine.AddTorsorBases
import Mathlib.LinearAlgebra.AffineSpace.FiniteDimensional

/-!
# Solan--Solan (2020) — paper-order audit

E. Solan and O. N. Solan, *Quitting Games and Linear Complementarity
Problems*, *Mathematics of Operations Research* 45(2), 434--454. The
accepted May 2018 manuscript is the authoritative complete text available
for this audit; it is used where arXiv:1707.02598v1 differs and supplies the
final theorem numbering.

One correction is mathematically material. The arXiv v1 building-block
condition (F.1) mentions only segments starting at `w`, although its final
construction uses a segment starting at `y`; the accepted manuscript states
the necessary disjunction. The paper's normal players are defined by their
min--max values. Its distinct-witness recursion occurs only in Section 5,
where it defines the different, generally smaller set of alpha players.

Public randomization is represented by an actual finite public-signal
stochastic game, and all payoffs and deviation payoffs below are induced by
behavior profiles. The stationary and LCP statements use the full payoff
table, including the nontermination payoff.
-/

namespace Literature.SolanAndSolan2020

open GameTheory StochasticGame QuittingLCPClassification
open Math.Probability
open Math.LinearProgramming
open Filter
open scoped BigOperators
open scoped Topology

noncomputable section

variable {ι : Type} [Fintype ι] [DecidableEq ι]

/-! ## Section 2.1 — model -/

/-! **Definition 2.1 (paper).** A quitting game is a finite player set
`I=[N]` and a payoff vector `r_S ∈ [-1,1]^N` for every `S ⊆ I`. At every
stage every player chooses Continue or Quit; the first nonempty quitter set
`S*` ends the game and pays `r_{S*}`, while never quitting pays `r_∅`.
The paper also defines pure strategies `N ∪ {∞}`, behavior strategies
`x_i=(x_i^t)_{t∈N}`, profiles `X_i`, `X`, the induced law `P_x`, expected
payoff `γ(x)`, and the undiscounted ε-equilibrium inequality.

The repository's `QuittingPayoffTable` is the faithful finite-player adapter:
`terminal` is `r_S` for nonempty `S`, and `never` is `r_∅`. Its
`terminalPayoff` supplies the induced expected payoff for a repository
behavior profile. The paper's pure-strategy presentation is equivalent to
the behavioral presentation but has no separate declaration here.
-/

/-! `Table` carries `(r_S)_{S ⊆ I}` together with `r_∅`. -/
abbrev Table (ι : Type) [Fintype ι] [DecidableEq ι] :=
  QuittingPayoffTable ι

/-! Paper ε-equilibrium adapter:
`γ_i(x) ≥ γ_i(x'_i,x_{-i}) - ε`. -/
def EpsilonEquilibrium
    (table : Table ι) (ε : ℝ)
    (profile : (quittingGame table.terminal).BehaviorProfile) : Prop :=
  (quittingGame table.terminal).IsεAsymptoticNash table.terminalPayoff ε profile

/-! `StationaryEpsilonEquilibria table` means stationary ε-equilibria
exist for every `ε>0`. -/
abbrev StationaryEpsilonEquilibria (table : Table ι) : Prop :=
  table.StationaryεEquilibriumExistence

/-! **Assumption 2.1.** The paper assumes `r_i^i = 0` for every player.
This is a standing condition, not a theorem. -/

def SoloExitNormalized (table : Table ι) : Prop :=
  ∀ i : ι, table.terminal (quittingProjectiveSingletonTerminal i) i = 0

/-! The source's global payoff normalization `r_S∈[-1,1]^N`. -/
def TablePayoffsBounded (table : Table ι) : Prop :=
  (∀ S who, |table.terminal S who| ≤ 1) ∧
    ∀ who, |table.never who| ≤ 1

/-! **Definition 2.3 (sunspot ε-equilibrium, paper).** At every stage the
players observe an independent uniform signal in `[0,1]`; a strategy is a
sequence of measurable functions from public signal histories to quit
probabilities. The local profile below packages the paper's induced payoff and
unilateral-deviation payoff functions; the probability construction is not
silently identified with the repository's terminal-only profile. -/

/-! The finite-support public randomizations used by the proof are exactly
coarsenings of the source's uniform `[0,1]` signal. We represent a coarsening
by a law on `ℕ`; its atoms are the publicly observed labels. -/

inductive PublicQuittingState (ι Signal : Type)
  | draw
  | active (signal : Signal)
  | absorbed (quitters : {S : Finset ι // S.Nonempty})
deriving Fintype

/-! The induced public-signal quitting game. The action-irrelevant `draw`
state samples a label before every live decision, including the first one.
At an `active` state a nonempty quitter set absorbs, while unanimous Continue
returns to `draw`. Thus no strategic decision is made before the first public
signal, exactly as in Definition 2.3. -/
def publicQuittingGame {Signal : Type} [Finite Signal]
    (table : Table ι)
    (signalLaw : PMF Signal) :
    StochasticGame ι where
  State := PublicQuittingState ι Signal
  Act := fun _ => Bool
  stagePayoff := fun state _ who =>
    match state with
    | .draw => table.never who
    | .active _ => table.never who
    | .absorbed quitters => table.terminal quitters who
  transition := fun state action =>
    match state with
    | .draw => signalLaw.map PublicQuittingState.active
    | .active _ =>
        if h : ({who | action who = true} : Finset ι).Nonempty then
          PMF.pure (.absorbed ⟨_, h⟩)
        else
          PMF.pure .draw
    | .absorbed quitters => PMF.pure (.absorbed quitters)
  discount := 0
  discount_nonneg := le_rfl
  discount_lt_one := zero_lt_one

@[simp] theorem publicQuittingGame_transition_draw
    {Signal : Type} [Finite Signal] (table : Table ι)
    (signalLaw : PMF Signal)
    (action : (publicQuittingGame table signalLaw).JointAct) :
    (publicQuittingGame table signalLaw).transition .draw action =
      signalLaw.map PublicQuittingState.active :=
  rfl

@[simp] theorem publicQuittingGame_transition_active
    {Signal : Type} [Finite Signal] (table : Table ι)
    (signalLaw : PMF Signal) (signal : Signal)
    (action : (publicQuittingGame table signalLaw).JointAct) :
    (publicQuittingGame table signalLaw).transition (.active signal) action =
      if h : (quittingQuitters action).Nonempty then
        PMF.pure (.absorbed ⟨quittingQuitters action, h⟩)
      else PMF.pure .draw :=
  by
    classical
    rfl

@[simp] theorem publicQuittingGame_transition_absorbed
    {Signal : Type} [Finite Signal] (table : Table ι)
    (signalLaw : PMF Signal)
    (quitters : {S : Finset ι // S.Nonempty})
    (action : (publicQuittingGame table signalLaw).JointAct) :
    (publicQuittingGame table signalLaw).transition (.absorbed quitters)
        action =
      PMF.pure (.absorbed quitters) :=
  rfl

private instance publicQuittingHistFinite
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) (t : ℕ) :
    Finite ((publicQuittingGame table signalLaw).Hist t) :=
  inferInstanceAs (Finite
    ((Fin t → PublicQuittingState ι Signal × (ι → Bool)) ×
      PublicQuittingState ι Signal))

private instance publicQuittingStateFinite
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) :
    Finite (publicQuittingGame table signalLaw).State :=
  inferInstanceAs (Finite (PublicQuittingState ι Signal))

private instance publicQuittingJointActFinite
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) :
    Finite (publicQuittingGame table signalLaw).JointAct :=
  inferInstanceAs (Finite (ι → Bool))

private instance publicQuittingActFinite
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) (who : ι) :
    Finite ((publicQuittingGame table signalLaw).Act who) :=
  inferInstanceAs (Finite Bool)

/-! Indicator and time-`t` mass of one absorbing quitter set. -/
def publicAbsorbedIndicator {Signal : Type} [Fintype Signal]
    (quitters : {S : Finset ι // S.Nonempty})
    (state : PublicQuittingState ι Signal) : ℝ := by
  classical
  exact if state = .absorbed quitters then 1 else 0

def publicAbsorbedMass
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (quitters : {S : Finset ι // S.Nonempty}) : ℝ :=
  (publicQuittingGame table signalLaw).expectedStateValue strategy
    .draw t (publicAbsorbedIndicator quitters)

/-! Limiting probability of absorption at one quitter set. -/
def publicAbsorbedMassLimit
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (quitters : {S : Finset ι // S.Nonempty}) : ℝ :=
  ⨆ t, publicAbsorbedMass table signalLaw strategy t quitters

theorem publicAbsorbedMass_succ_ge
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (quitters : {S : Finset ι // S.Nonempty}) :
    publicAbsorbedMass table signalLaw strategy t quitters ≤
      publicAbsorbedMass table signalLaw strategy (t + 1) quitters := by
  letI : Finite (publicQuittingGame table signalLaw).State :=
    inferInstanceAs (Finite (PublicQuittingState ι (Fin (signalCount + 1))))
  letI : ∀ i : ι, Finite ((publicQuittingGame table signalLaw).Act i) :=
    fun _ => inferInstanceAs (Finite Bool)
  rw [publicAbsorbedMass, publicAbsorbedMass,
    (publicQuittingGame table signalLaw).expectedStateValue_succ]
  unfold StochasticGame.expectedStateValue
  apply expect_mono
  intro history
  cases hstate : history.2 with
  | draw =>
      rw [show publicAbsorbedIndicator quitters .draw = 0 by
        simp [publicAbsorbedIndicator]]
      exact expect_nonneg _ _ fun action =>
        expect_nonneg _ _ fun state => by
          unfold publicAbsorbedIndicator
          split <;> norm_num
  | active signal =>
      rw [show publicAbsorbedIndicator quitters (.active signal) = 0 by
        simp [publicAbsorbedIndicator]]
      exact expect_nonneg _ _ fun action =>
        expect_nonneg _ _ fun state => by
          unfold publicAbsorbedIndicator
          split <;> norm_num
  | absorbed terminal =>
      simp [publicQuittingGame, publicAbsorbedIndicator]

theorem publicAbsorbedMass_le_one
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (quitters : {S : Finset ι // S.Nonempty}) :
    publicAbsorbedMass table signalLaw strategy t quitters ≤ 1 := by
  letI : Finite (publicQuittingGame table signalLaw).State :=
    inferInstanceAs (Finite (PublicQuittingState ι (Fin (signalCount + 1))))
  letI : ∀ i : ι, Finite ((publicQuittingGame table signalLaw).Act i) :=
    fun _ => inferInstanceAs (Finite Bool)
  unfold publicAbsorbedMass StochasticGame.expectedStateValue
  calc
    expect ((publicQuittingGame table signalLaw).histDist strategy
        .draw t)
        (fun history => publicAbsorbedIndicator quitters history.2) ≤
        expect ((publicQuittingGame table signalLaw).histDist strategy
          .draw t) (fun _ => 1) := by
      apply expect_mono
      intro history
      unfold publicAbsorbedIndicator
      split <;> norm_num
    _ = 1 := expect_const _ _

theorem tendsto_publicAbsorbedMass
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (quitters : {S : Finset ι // S.Nonempty}) :
    Tendsto (fun t => publicAbsorbedMass table signalLaw strategy t quitters)
      atTop (nhds (publicAbsorbedMassLimit table signalLaw strategy quitters)) := by
  apply tendsto_atTop_ciSup
  · exact monotone_nat_of_le_succ fun t =>
      publicAbsorbedMass_succ_ge table signalLaw strategy t quitters
  · refine ⟨1, ?_⟩
    rintro _ ⟨t, rfl⟩
    exact publicAbsorbedMass_le_one table signalLaw strategy t quitters

/-! The expected terminal payoff induced by a public profile. Absorption at
`S` changes the payoff from `r_∅` to `r_S`; the residual nonabsorption mass
therefore receives `r_∅` exactly as in the source. -/
def publicQuittingPayoff
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (who : ι) : ℝ :=
  table.never who +
    ∑ quitters, publicAbsorbedMassLimit table signalLaw strategy quitters *
      (table.terminal quitters who - table.never who)

/-! A sunspot profile is a public-signal law together with an actual behavior
profile of the induced quitting game. Its payoff is computed from play. -/
structure SunspotProfile (table : Table ι) where
  signalCount : ℕ
  signalLaw : PMF (Fin (signalCount + 1))
  strategy : (publicQuittingGame table signalLaw).BehaviorProfile

/-! The payoff generated by a sunspot profile. -/
def SunspotProfile.payoff {table : Table ι}
    (profile : SunspotProfile table) : Payoff ι :=
  publicQuittingPayoff table profile.signalLaw profile.strategy

/-! `ξ` is a sunspot ε-equilibrium iff
`γ_i(ξ) ≥ γ_i(ξ'_i,ξ_{-i})-ε` for every behavior deviation. -/
def SunspotEpsilonEquilibrium
    (table : Table ι) (ε : ℝ)
    (profile : SunspotProfile table) : Prop :=
  (publicQuittingGame table profile.signalLaw).IsεAsymptoticNash
    (publicQuittingPayoff table profile.signalLaw) ε profile.strategy

/-! The trivial public signal used to compare ordinary and sunspot play. -/
def trivialSignalLaw : PMF (Fin 1) :=
  PMF.pure 0

/-! A live-root sequence lifted through an arbitrary action-independent
public-signal law. At public time `2n+1`, the active decision uses root `n`;
actions at the intervening draw states are immaterial and fixed to Continue. -/
def rootSequencePublicStrategy {Signal : Type} [Finite Signal]
    (table : Table ι) (signalLaw : PMF Signal)
    (roots : ℕ → ι → PMF Bool) :
    (publicQuittingGame table signalLaw).BehaviorProfile :=
  fun who time history => match history.2 with
    | .active _ => roots (time / 2) who
    | _ => PMF.pure false

/-! The one-label specialization used to compare ordinary and sunspot play. -/
def rootSequenceSunspotStrategy (table : Table ι)
    (roots : ℕ → ι → PMF Bool) :
    (publicQuittingGame table trivialSignalLaw).BehaviorProfile :=
  rootSequencePublicStrategy table trivialSignalLaw roots

/-! The one-label sunspot profile induced by a sequence of ordinary live
roots. -/
def rootSequenceSunspotProfile (table : Table ι)
    (roots : ℕ → ι → PMF Bool) : SunspotProfile table where
  signalCount := 0
  signalLaw := trivialSignalLaw
  strategy := rootSequenceSunspotStrategy table roots

/-! At every live public history, at most one player assigns positive
probability to Quit. -/
def AtMostOneQuitter {table : Table ι}
    (profile : SunspotProfile table) : Prop :=
  ∀ t (history : (publicQuittingGame table profile.signalLaw).Hist t),
    match history.2 with
    | .draw => True
    | .active _ =>
        (Finset.filter (fun who =>
          0 < ((profile.strategy who t history) true).toReal)
          Finset.univ).card ≤ 1
    | .absorbed _ => True

/-! **Theorem 2.4 (paper).** Every quitting game admits a sunspot
ε-equilibrium for every `ε > 0`. -/

/-! Its checked declaration appears after Theorem 2.13, whose two branches
are used in the proof. -/

/-! ## Section 2.3 — normal and abnormal players -/

/-! **Definition 2.5 (paper).** Player `i` is normal when its min--max value
is nonpositive, and abnormal when that value is positive. -/
def IsNormalPlayer (table : Table ι) (who : ι) : Prop :=
  table.punishmentValue who ≤ 0

/-! `I*`, the finite set of normal players. -/
def NormalPlayers (table : Table ι) : Finset ι := by
  classical
  exact Finset.univ.filter (IsNormalPlayer table)

@[simp] theorem mem_normalPlayers_iff (table : Table ι) (who : ι) :
    who ∈ NormalPlayers table ↔ IsNormalPlayer table who := by
  classical
  simp [NormalPlayers]

/-! A normal player, used as the row and column type of the paper's matrix
`R̂`. -/
abbrev NormalPlayer (table : Table ι) :=
  {who : ι // who ∈ NormalPlayers table}

/-! `R̂`: singleton-quitting payoffs restricted in both coordinates to
normal players. -/
def NormalMatrix (table : Table ι) :
    NormalPlayer table → NormalPlayer table → ℝ :=
  fun who owner =>
    table.terminal (quittingProjectiveSingletonTerminal owner.1) who.1

/-! Under Assumption 2.1, the production normalization of the translated
zero-never table recovers the paper's raw singleton-payoff matrix. -/
theorem normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
    (table : Table ι) (hnormalized : SoloExitNormalized table) :
    normalizedSoloMatrix table.zeroNeverReward = table.singletonMatrix := by
  funext who owner
  simp only [normalizedSoloMatrix, QuittingPayoffTable.singletonMatrix,
    normalizedQuittingPayoffTable, QuittingPayoffTable.translate,
    repositoryQuittingPayoffTable,
    QuittingLCPClassification.quittingSoloBaseline,
    QuittingPayoffTable.zeroNeverReward]
  rw [hnormalized who]
  ring

def HasNormalPlayers (table : Table ι) : Prop :=
  (NormalPlayers table).Nonempty

def AllPlayersAbnormal (table : Table ι) : Prop :=
  NormalPlayers table = ∅

/-! **Lemma 2.6 (paper).** If `i` is abnormal and `j≠i`, then player `i`
strictly benefits when `j` quits alone: `rʲ_i>0`. -/
theorem lemma2_6
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table) {who owner : ι}
    (habnormal : ¬IsNormalPlayer table who) (hne : owner ≠ who) :
    0 < table.terminal (quittingProjectiveSingletonTerminal owner) who := by
  by_contra hpositive
  have howner :
      table.terminal (quittingProjectiveSingletonTerminal owner) who ≤ 0 :=
    le_of_not_gt hpositive
  apply habnormal
  unfold IsNormalPlayer
  refine le_of_forall_pos_le_add fun ε hε => ?_
  let p : ℝ := min (ε / 2) 1
  have hp : 0 < p := by
    change 0 < min (ε / 2) 1
    exact lt_min (half_pos hε) zero_lt_one
  have hp0 : 0 ≤ p := hp.le
  have hp1 : p ≤ 1 := min_le_right _ _
  have hpε : 2 * p ≤ ε := by
    have := min_le_left (ε / 2) 1
    linarith
  let hazard := quittingHazardCoin p hp0 hp1
  have hhazard : 0 < (hazard true).toReal := by
    simpa only [hazard, quittingHazardCoin_true_toReal] using hp
  have hcap := quittingPunishmentValue_le_stationaryUnilateralCap
    table.zeroNeverReward who (quittingSoloStationaryRoot owner hazard)
  rw [table.punishmentValue_eq_add_never]
  calc
    quittingPunishmentValue table.zeroNeverReward who + table.never who ≤
        quittingStationaryUnilateralCap table.zeroNeverReward
            (quittingSoloStationaryRoot owner hazard) who +
          table.never who := by linarith
    _ ≤ ε := by
      rw [quittingStationaryUnilateralCap_solo_other table.zeroNeverReward
          hne.symm hazard hhazard,
        quittingStationaryFixedOpponentsQuitValue_solo_other_eq_mix
          table.zeroNeverReward hne.symm hazard]
      simp only [hazard, quittingHazardCoin_false_toReal,
        quittingHazardCoin_true_toReal]
      unfold quittingSoloReward quittingSingletonCollisionReward
        QuittingPayoffTable.zeroNeverReward
      have hsolo : table.terminal ⟨{who}, by simp⟩ who = 0 := by
        simpa only [quittingProjectiveSingletonTerminal] using hnormalized who
      have howner' : table.terminal ⟨{owner}, by simp⟩ who ≤ 0 := by
        simpa only [quittingProjectiveSingletonTerminal] using howner
      rw [hsolo]
      have hcollision :
          table.terminal ⟨{owner, who}, by simp⟩ who ≤ 1 :=
        (le_abs_self _).trans (hbounded.1 _ who)
      rw [← max_add_add_right, max_le_iff]
      constructor <;> nlinarith
    _ = 0 + ε := by ring

/-! **Lemma 2.7 (paper).** If every player is abnormal, a stationary
`ε`-equilibrium exists for every positive `ε`. -/
theorem lemma2_7
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (habnormal : AllPlayersAbnormal table) :
    StationaryEpsilonEquilibria table := by
  have hlayer : normalLayer
      (normalizedSoloMatrix table.zeroNeverReward) 1 = ∅ := by
    ext who
    simp only [normalLayer, Finset.mem_filter, Finset.mem_univ, true_and]
    constructor
    · rintro ⟨owner, hne, hnonpositive⟩
      have hnotmem : who ∉ NormalPlayers table := by
        rw [habnormal]
        simp
      have habnormalWho : ¬IsNormalPlayer table who := by
        simpa only [mem_normalPlayers_iff] using hnotmem
      have hpositive := lemma2_6 table hnormalized hbounded
        habnormalWho hne
      have hmatrix := congr_fun
        (congr_fun
          (normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
            table hnormalized) who) owner
      rw [hmatrix] at hnonpositive
      exact False.elim ((not_lt_of_ge hnonpositive) hpositive)
    · simp
  obtain ⟨value, hvalue⟩ :=
    exists_stationaryUniformEquilibriumPayoff_of_normalLayer_one_eq_empty
      table.zeroNeverReward hlayer
  change table.StationaryεEquilibriumExistence
  rw [QuittingPayoffTable.stationaryεEquilibriumExistence_iff]
  exact hvalue.hasApproximateEquilibria

/-! The payoff to `who` from the paper's distribution over unilateral
quittings by normal players. -/
def NormalMixturePayoff (table : Table ι)
    (weight : stdSimplex ℝ (NormalPlayer table)) (who : ι) : ℝ :=
  wsum weight fun owner =>
    table.terminal (quittingProjectiveSingletonTerminal owner.1) who

/-! Zero-extension of a distribution on the paper's normal players to all
players. -/
def extendNormalPlayerWeight (table : Table ι)
    (weight : NormalPlayer table → ℝ) : ι → ℝ :=
  fun who => if hwho : who ∈ NormalPlayers table then
    weight ⟨who, hwho⟩ else 0

@[simp] theorem extendNormalPlayerWeight_of_mem
    (table : Table ι) (weight : NormalPlayer table → ℝ)
    {who : ι} (hwho : who ∈ NormalPlayers table) :
    extendNormalPlayerWeight table weight who = weight ⟨who, hwho⟩ := by
  simp [extendNormalPlayerWeight, hwho]

@[simp] theorem extendNormalPlayerWeight_of_notMem
    (table : Table ι) (weight : NormalPlayer table → ℝ)
    {who : ι} (hwho : who ∉ NormalPlayers table) :
    extendNormalPlayerWeight table weight who = 0 := by
  simp [extendNormalPlayerWeight, hwho]

/-! Zero-extension preserves nonnegativity and total mass one. -/
def extendNormalPlayerSimplex (table : Table ι)
    (weight : stdSimplex ℝ (NormalPlayer table)) : stdSimplex ℝ ι := by
  classical
  refine ⟨extendNormalPlayerWeight table weight.1, ?_, ?_⟩
  · intro who
    by_cases hwho : who ∈ NormalPlayers table
    · rw [extendNormalPlayerWeight_of_mem table weight.1 hwho]
      exact weight.2.1 ⟨who, hwho⟩
    · simp [extendNormalPlayerWeight, hwho]
  · calc
      (∑ who, extendNormalPlayerWeight table weight.1 who) =
          ∑ who ∈ NormalPlayers table,
            extendNormalPlayerWeight table weight.1 who := by
        symm
        apply Finset.sum_subset (Finset.subset_univ _)
        intro who _ hwho
        exact extendNormalPlayerWeight_of_notMem table weight.1 hwho
      _ = ∑ who : NormalPlayer table, weight.1 who := by
        calc
          (∑ who ∈ NormalPlayers table,
              extendNormalPlayerWeight table weight.1 who) =
              ∑ who : NormalPlayer table,
                extendNormalPlayerWeight table weight.1 who.1 :=
            Finset.sum_subtype (NormalPlayers table) (fun _ => Iff.rfl) _
          _ = ∑ who : NormalPlayer table, weight.1 who := by
            apply Finset.sum_congr rfl
            intro who _
            exact extendNormalPlayerWeight_of_mem table weight.1 who.2
      _ = 1 := weight.2.2

@[simp] theorem extendNormalPlayerSimplex_apply_of_mem
    (table : Table ι) (weight : stdSimplex ℝ (NormalPlayer table))
    {who : ι} (hwho : who ∈ NormalPlayers table) :
    (extendNormalPlayerSimplex table weight).1 who = weight.1 ⟨who, hwho⟩ := by
  simp [extendNormalPlayerSimplex, hwho]

@[simp] theorem extendNormalPlayerSimplex_apply_of_notMem
    (table : Table ι) (weight : stdSimplex ℝ (NormalPlayer table))
    {who : ι} (hwho : who ∉ NormalPlayers table) :
    (extendNormalPlayerSimplex table weight).1 who = 0 := by
  simp [extendNormalPlayerSimplex, hwho]

/-! On a normal coordinate, the full zero-extended singleton residual is
the mixture appearing in Lemma 2.8. -/
theorem singletonLCPResidual_extendNormalPlayerSimplex
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (weight : stdSimplex ℝ (NormalPlayer table))
    (who : NormalPlayer table) :
    singletonLCPResidual (normalizedSoloMatrix table.zeroNeverReward)
        (extendNormalPlayerSimplex table weight) who.1 =
      NormalMixturePayoff table weight who.1 := by
  classical
  unfold singletonLCPResidual NormalMixturePayoff wsum
  rw [normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
    table hnormalized]
  calc
    (∑ owner,
        (extendNormalPlayerSimplex table weight).1 owner *
          table.singletonMatrix who.1 owner) =
        ∑ owner ∈ NormalPlayers table,
          (extendNormalPlayerSimplex table weight).1 owner *
            table.singletonMatrix who.1 owner := by
      symm
      apply Finset.sum_subset (Finset.subset_univ _)
      intro owner _ howner
      rw [extendNormalPlayerSimplex_apply_of_notMem table weight howner,
        zero_mul]
    _ = ∑ owner : NormalPlayer table,
        weight.1 owner * table.singletonMatrix who.1 owner.1 := by
      calc
        (∑ owner ∈ NormalPlayers table,
            (extendNormalPlayerSimplex table weight).1 owner *
              table.singletonMatrix who.1 owner) =
            ∑ owner : NormalPlayer table,
              (extendNormalPlayerSimplex table weight).1 owner.1 *
                table.singletonMatrix who.1 owner.1 :=
          Finset.sum_subtype (NormalPlayers table) (fun _ => Iff.rfl) _
        _ = ∑ owner : NormalPlayer table,
            weight.1 owner * table.singletonMatrix who.1 owner.1 := by
          apply Finset.sum_congr rfl
          intro owner _
          rw [extendNormalPlayerSimplex_apply_of_mem
            table weight owner.2]
    _ = ∑ owner : NormalPlayer table,
        weight.1 owner *
          table.terminal (quittingProjectiveSingletonTerminal owner.1)
            who.1 := by
      apply Finset.sum_congr rfl
      intro owner _
      congr 1

/-! **Lemma 2.8 (paper).** A distribution over normal players whose mixture
payoff is nonnegative in every normal coordinate, and zero on every used
coordinate, yields stationary approximate equilibria. The displayed proof
misses the case where the distribution is concentrated on one player: then
that player's deviation to Continue leaves the game unabsorbed. The final
hypothesis is the exact missing condition in that case; it is vacuous for a
nonvertex distribution. -/
theorem lemma2_8
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (weight : stdSimplex ℝ (NormalPlayer table))
    (hnonnegative : ∀ who : NormalPlayer table,
      0 ≤ NormalMixturePayoff table weight who.1)
    (hcomplementary : ∀ who : NormalPlayer table,
      0 < weight.1 who → NormalMixturePayoff table weight who.1 = 0)
    (hvertexNever : ∀ owner : NormalPlayer table,
      weight.1 owner = 1 → table.never owner.1 ≤ 0) :
    StationaryEpsilonEquilibria table := by
  classical
  let fullWeight := extendNormalPlayerSimplex table weight
  have hfullResidual : ∀ who,
      0 ≤ singletonLCPResidual
        (normalizedSoloMatrix table.zeroNeverReward) fullWeight who := by
    intro who
    by_cases hwho : who ∈ NormalPlayers table
    · rw [singletonLCPResidual_extendNormalPlayerSimplex
        table hnormalized weight ⟨who, hwho⟩]
      exact hnonnegative ⟨who, hwho⟩
    · rw [normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
        table hnormalized]
      unfold singletonLCPResidual wsum
      apply Finset.sum_nonneg
      intro owner _
      by_cases howner : owner ∈ NormalPlayers table
      · have hne : owner ≠ who := fun heq => hwho (heq ▸ howner)
        exact mul_nonneg (fullWeight.2.1 owner)
          (lemma2_6 table hnormalized hbounded
            (by simpa only [mem_normalPlayers_iff] using hwho) hne).le
      · change 0 ≤ (extendNormalPlayerSimplex table weight).1 owner * _
        rw [extendNormalPlayerSimplex_apply_of_notMem
          table weight howner, zero_mul]
  have hfullComplementary : ∀ who,
      fullWeight.1 who * singletonLCPResidual
        (normalizedSoloMatrix table.zeroNeverReward) fullWeight who = 0 := by
    intro who
    by_cases hwho : who ∈ NormalPlayers table
    · rw [singletonLCPResidual_extendNormalPlayerSimplex
        table hnormalized weight ⟨who, hwho⟩,
      extendNormalPlayerSimplex_apply_of_mem table weight hwho]
      by_cases hweight : weight.1 ⟨who, hwho⟩ = 0
      · rw [hweight, zero_mul]
      · have hpositive : 0 < weight.1 ⟨who, hwho⟩ :=
          lt_of_le_of_ne (weight.2.1 _) (Ne.symm hweight)
        rw [hcomplementary ⟨who, hwho⟩ hpositive, mul_zero]
    · rw [extendNormalPlayerSimplex_apply_of_notMem
        table weight hwho, zero_mul]
  change table.StationaryεEquilibriumExistence
  rw [QuittingPayoffTable.stationaryεEquilibriumExistence_iff]
  by_cases hvertex : ∃ owner, fullWeight.1 owner = 1
  · obtain ⟨owner, howner⟩ := hvertex
    have hownerMem : owner ∈ NormalPlayers table := by
      by_contra hnot
      have hzero := extendNormalPlayerSimplex_apply_of_notMem
        table weight hnot
      rw [howner] at hzero
      norm_num at hzero
    have hownerWeight : weight.1 ⟨owner, hownerMem⟩ = 1 := by
      rw [← extendNormalPlayerSimplex_apply_of_mem
        table weight hownerMem]
      exact howner
    have hnever : table.never owner ≤ 0 :=
      hvertexNever ⟨owner, hownerMem⟩ hownerWeight
    have hcolumn : ∀ who,
        0 ≤ table.terminal (quittingProjectiveSingletonTerminal owner) who := by
      intro who
      have hresidual := hfullResidual who
      rw [singletonLCPResidual_eq_column_of_weight_eq_one
        (normalizedSoloMatrix table.zeroNeverReward) fullWeight howner who,
        normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
          table hnormalized] at hresidual
      exact hresidual
    intro ε hε
    let p : ℝ := min ε 1
    have hp : 0 < p := by
      change 0 < min ε 1
      exact lt_min hε zero_lt_one
    have hp1 : p ≤ 1 := min_le_right _ _
    have hpε : p ≤ ε := min_le_left _ _
    let hazard := quittingHazardCoin p hp.le hp1
    have hhazard : 0 < (hazard true).toReal := by
      simpa only [hazard, quittingHazardCoin_true_toReal] using hp
    refine ⟨quittingSoloStationaryRoot owner hazard, ?_⟩
    intro who deviation
    have hdeviation := quittingTerminalPayoff_update_stationary_le_cap
      table.zeroNeverReward (quittingSoloStationaryRoot owner hazard)
        who deviation
    rw [quittingTerminalPayoff_soloStationary table.zeroNeverReward
      owner who hazard hhazard]
    refine hdeviation.trans ?_
    by_cases hwho : who = owner
    · subst who
      rw [quittingStationaryUnilateralCap_solo_owner]
      unfold quittingSoloReward QuittingPayoffTable.zeroNeverReward
      have hsolo : table.terminal ⟨{owner}, by simp⟩ owner = 0 := by
        simpa only [quittingProjectiveSingletonTerminal] using
          hnormalized owner
      rw [hsolo]
      exact max_le (le_add_of_nonneg_right hε.le) (by linarith)
    · rw [quittingStationaryUnilateralCap_solo_other
          table.zeroNeverReward hwho hazard hhazard,
        quittingStationaryFixedOpponentsQuitValue_solo_other_eq_mix
          table.zeroNeverReward hwho hazard]
      simp only [hazard, quittingHazardCoin_false_toReal,
        quittingHazardCoin_true_toReal]
      unfold quittingSoloReward quittingSingletonCollisionReward
        QuittingPayoffTable.zeroNeverReward
      have hsolo : table.terminal ⟨{who}, by simp⟩ who = 0 := by
        simpa only [quittingProjectiveSingletonTerminal] using
          hnormalized who
      have hcollision : table.terminal ⟨{owner, who}, by simp⟩ who ≤ 1 :=
        (le_abs_self _).trans (hbounded.1 _ who)
      have hownerColumn : 0 ≤ table.terminal ⟨{owner}, by simp⟩ who := by
        simpa only [quittingProjectiveSingletonTerminal] using hcolumn who
      rw [hsolo, max_le_iff]
      constructor <;> nlinarith
  · have hnonvertex : ∀ who, fullWeight.1 who < 1 := by
      intro who
      have hle : fullWeight.1 who ≤ 1 := by
        rw [← fullWeight.2.2]
        exact Finset.single_le_sum
          (fun owner _ => fullWeight.2.1 owner) (Finset.mem_univ who)
      exact lt_of_le_of_ne hle (fun heq => hvertex ⟨who, heq⟩)
    exact
      (isQuittingStationaryUniformEquilibriumPayoff_of_nonvertexHomogeneousWitness
        table.zeroNeverReward fullWeight hfullResidual hfullComplementary
        hnonvertex).hasApproximateEquilibria

/-! Under the accepted manuscript's min--max definition of normality, the
singleton case in the printed proof of Lemma 2.8 need not be stationary.
The conclusion needed by Theorem 2.4 still follows: a rare solo exit makes
every outsider's joining gain small, while the owner's min--max inequality
supplies the punishment continuation. -/
private theorem exists_epsilonEquilibrium_of_normal_nonnegativeColumn
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table) (owner : NormalPlayer table)
    (hcolumn : ∀ who,
      0 ≤ table.terminal
        (quittingProjectiveSingletonTerminal owner.1) who) :
    ∀ ε : ℝ, 0 < ε →
      ∃ profile : (quittingGame table.terminal).BehaviorProfile,
        EpsilonEquilibrium table ε profile := by
  intro ε hε
  let p : ℝ := min (ε / 2) (1 / 2)
  have hp : 0 < p := by
    dsimp only [p]
    exact lt_min (half_pos hε) (by norm_num)
  have hp1 : p ≤ 1 := (min_le_right _ _).trans (by norm_num)
  have hpε : p < ε := by
    have := min_le_left (ε / 2) (1 / 2)
    linarith
  let hazard := quittingHazardCoin p hp.le hp1
  have hhazard : 0 < (hazard true).toReal := by
    simpa only [hazard, quittingHazardCoin_true_toReal] using hp
  have hcap : ∀ other, other ≠ owner.1 →
      quittingStationaryUnilateralCap table.zeroNeverReward
          (quittingSoloStationaryRoot owner.1 hazard) other ≤
        quittingSoloReward table.zeroNeverReward owner.1 other + p := by
    intro other hother
    rw [quittingStationaryUnilateralCap_solo_other
        table.zeroNeverReward hother hazard hhazard,
      quittingStationaryFixedOpponentsQuitValue_solo_other_eq_mix
        table.zeroNeverReward hother hazard]
    simp only [hazard, quittingHazardCoin_false_toReal,
      quittingHazardCoin_true_toReal]
    unfold quittingSoloReward quittingSingletonCollisionReward
      QuittingPayoffTable.zeroNeverReward
    have hsolo : table.terminal ⟨{other}, by simp⟩ other = 0 := by
      simpa only [quittingProjectiveSingletonTerminal] using
        hnormalized other
    have howner : 0 ≤ table.terminal ⟨{owner.1}, by simp⟩ other := by
      simpa only [quittingProjectiveSingletonTerminal] using hcolumn other
    have hcollision : table.terminal ⟨{owner.1, other}, by simp⟩ other ≤ 1 :=
      (le_abs_self _).trans (hbounded.1 _ other)
    rw [hsolo, max_le_iff]
    constructor <;> nlinarith
  have hpunishment : quittingPunishmentValue table.zeroNeverReward owner.1 ≤
      quittingSoloReward table.zeroNeverReward owner.1 owner.1 := by
    have hnormal := owner.2
    rw [mem_normalPlayers_iff] at hnormal
    unfold IsNormalPlayer at hnormal
    rw [table.punishmentValue_eq_add_never] at hnormal
    unfold quittingSoloReward
    have hsolo : table.terminal ⟨{owner.1}, by simp⟩ owner.1 = 0 := by
      simpa only [quittingProjectiveSingletonTerminal] using
        hnormalized owner.1
    change quittingPunishmentValue table.zeroNeverReward owner.1 ≤
      table.terminal ⟨{owner.1}, by simp⟩ owner.1 - table.never owner.1
    rw [hsolo]
    linarith
  obtain ⟨profile, hnash, _⟩ :=
    exists_isεAsymptoticNash_close_soloReward_of_cap_of_punishmentIR
      table.zeroNeverReward owner.1 hazard hhazard hp.le hpε hcap hpunishment
  refine ⟨profile, ?_⟩
  exact (table.isεAsymptoticNash_iff ε profile).2 hnash

/-! **Remark 2.9 (paper).** For an `n×n` matrix `R` and `q`, the
paper recalls the textbook LCP: find `w,z ≥ 0` with `w=q+Rz` and
`w_i z_i=0`. -/

/-! The paper's projective LCP `LCP(M,q)` is solvable. -/
abbrev LCP (M : ι → ι → ℝ) (q : ι → ℝ) : Prop :=
  HasProjectiveLCPSolution M q

/-! **Definition 2.10 (paper).** `R` is a Q-matrix when the paper's
projective/simplex LCP `LCP(R,q)` has a solution for every `q`. -/

/-! `QMatrix M ↔ ∀q, LCP(M,q)` is solvable. -/
abbrev QMatrix (M : ι → ι → ℝ) : Prop := IsProjectiveQMatrix M

/-! **Example 2.11 (paper).** For the displayed cyclic `3×3` sign pattern
with zero diagonal, Berman--Plemmons implies Q iff the determinant is
positive. This numerical example is retained here as paper text; no
repository matrix-example declaration is introduced. -/

/-! **Lemma 2.12 (paper, repaired vertex case).** If the projective LCP with
right-hand side zero has a solution with `z₀<1`, then a stationary
ε-equilibrium exists for every positive ε. As in Lemma 2.8, the manuscript's
one-vertex construction also needs the nontermination payoff of a normal
owner to be nonpositive. -/

/-! Lemma 2.12: a zero-LCP solution with cemetery weight `<1` yields
stationary ε-equilibria for every `ε>0`. -/
theorem lemma2_12
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (hnormalNever : ∀ owner : NormalPlayer table, table.never owner.1 ≤ 0)
    (h : HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table)) :
    StationaryEpsilonEquilibria table := by
  rw [hasNontrivialZeroProjectiveLCPSolution_iff_homogeneous] at h
  obtain ⟨weight, hnonnegative, hcomplementary⟩ := h
  apply lemma2_8 table hnormalized hbounded weight
  · intro who
    exact hnonnegative who
  · intro who hpositive
    have hproduct := hcomplementary who
    change weight.1 who * NormalMixturePayoff table weight who.1 = 0 at hproduct
    exact (mul_eq_zero.mp hproduct).resolve_left hpositive.ne'
  · intro owner _
    exact hnormalNever owner

/-! The zero-LCP branch needed by the main theorem.  Away from a simplex
vertex this is Lemma 2.8.  At a vertex, the accepted manuscript's min--max
normality supplies a punishment-completed, generally nonstationary profile. -/
private theorem epsilonEquilibria_of_nontrivialZeroProjectiveLCPSolution
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (h : HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table)) :
    ∀ ε : ℝ, 0 < ε →
      ∃ profile : (quittingGame table.terminal).BehaviorProfile,
        EpsilonEquilibrium table ε profile := by
  rw [hasNontrivialZeroProjectiveLCPSolution_iff_homogeneous] at h
  obtain ⟨weight, hnonnegative, hcomplementary⟩ := h
  by_cases hvertex : ∃ owner : NormalPlayer table, weight.1 owner = 1
  · obtain ⟨owner, howner⟩ := hvertex
    have hcolumn : ∀ who,
        0 ≤ table.terminal
          (quittingProjectiveSingletonTerminal owner.1) who := by
      intro who
      by_cases hwho : who ∈ NormalPlayers table
      · have hresidual := hnonnegative ⟨who, hwho⟩
        rw [singletonLCPResidual_eq_column_of_weight_eq_one
          (NormalMatrix table) weight howner ⟨who, hwho⟩] at hresidual
        exact hresidual
      · have habnormal : ¬IsNormalPlayer table who := by
          simpa only [mem_normalPlayers_iff] using hwho
        have hne : owner.1 ≠ who := fun heq => by
          apply hwho
          rw [← heq]
          exact owner.2
        exact (lemma2_6 table hnormalized hbounded habnormal hne).le
    exact exists_epsilonEquilibrium_of_normal_nonnegativeColumn
      table hnormalized hbounded owner hcolumn
  · have hstationary : StationaryEpsilonEquilibria table := by
      apply lemma2_8 table hnormalized hbounded weight
      · intro who
        exact hnonnegative who
      · intro who hpositive
        have hproduct := hcomplementary who
        change weight.1 who * NormalMixturePayoff table weight who.1 = 0
          at hproduct
        exact (mul_eq_zero.mp hproduct).resolve_left hpositive.ne'
      · intro owner howner
        exact False.elim (hvertex ⟨owner, howner⟩)
    intro ε hε
    obtain ⟨root, hnash⟩ := hstationary ε hε
    exact ⟨quittingStationaryProfile table.terminal root, hnash⟩

/-! Extend a right-hand side on the min--max normal players by the fixed
positive value one on abnormal coordinates, as in the proof of Theorem
2.13(1). -/
def extendNormalPlayerDirection (table : Table ι)
    (q : NormalPlayer table → ℝ) : ι → ℝ :=
  fun who => if hwho : who ∈ NormalPlayers table then q ⟨who, hwho⟩ else 1

@[simp] theorem extendNormalPlayerDirection_of_mem
    (table : Table ι) (q : NormalPlayer table → ℝ)
    {who : ι} (hwho : who ∈ NormalPlayers table) :
    extendNormalPlayerDirection table q who = q ⟨who, hwho⟩ := by
  simp [extendNormalPlayerDirection, hwho]

@[simp] theorem extendNormalPlayerDirection_of_notMem
    (table : Table ι) (q : NormalPlayer table → ℝ)
    {who : ι} (hwho : who ∉ NormalPlayers table) :
    extendNormalPlayerDirection table q who = 1 := by
  simp [extendNormalPlayerDirection, hwho]

/-! Restrict a full projective solution whose positive singleton support is
normal to the paper's principal normal-player matrix. -/
def restrictProjectiveLCPSolutionToNormalPlayers
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (q : NormalPlayer table → ℝ)
    (solution : ProjectiveLCPSolution
      (normalizedSoloMatrix table.zeroNeverReward)
      (extendNormalPlayerDirection table q))
    (hsupport : ∀ who, 0 < solution.singleton who →
      who ∈ NormalPlayers table) :
    ProjectiveLCPSolution (NormalMatrix table) q := by
  classical
  have hzeroOff : ∀ who, who ∉ NormalPlayers table →
      solution.singleton who = 0 := by
    intro who hwho
    by_contra hne
    have hpositive : 0 < solution.singleton who :=
      lt_of_le_of_ne (solution.singleton_nonneg who) (Ne.symm hne)
    exact hwho (hsupport who hpositive)
  have hsumRestrict (f : ι → ℝ)
      (hoff : ∀ who, who ∉ NormalPlayers table → f who = 0) :
      (∑ who, f who) = ∑ who : NormalPlayer table, f who.1 := by
    calc
      (∑ who, f who) = ∑ who ∈ NormalPlayers table, f who := by
        symm
        apply Finset.sum_subset (Finset.subset_univ _)
        intro who _ hwho
        exact hoff who hwho
      _ = ∑ who : NormalPlayer table, f who.1 :=
        Finset.sum_subtype (NormalPlayers table) (fun _ => Iff.rfl) f
  refine
    { cemetery := solution.cemetery
      singleton := fun who => solution.singleton who.1
      cemetery_nonneg := solution.cemetery_nonneg
      singleton_nonneg := fun who => solution.singleton_nonneg who.1
      total := ?_
      residual_nonneg := ?_
      complementary := ?_ }
  · calc
      solution.cemetery +
          ∑ who : NormalPlayer table, solution.singleton who.1 =
          solution.cemetery + ∑ who, solution.singleton who := by
        rw [hsumRestrict solution.singleton hzeroOff]
      _ = 1 := solution.total
  · intro who
    have h := solution.residual_nonneg who.1
    rw [extendNormalPlayerDirection_of_mem table q who.2] at h
    have hmatrix (row owner : ι) :
        normalizedSoloMatrix table.zeroNeverReward row owner =
          table.singletonMatrix row owner := by
      exact congr_fun (congr_fun
        (normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
          table hnormalized) row) owner
    simp_rw [hmatrix] at h
    change 0 ≤ solution.cemetery * q who +
      ∑ owner : NormalPlayer table,
        solution.singleton owner.1 *
          table.singletonMatrix who.1 owner.1
    calc
      0 ≤ solution.cemetery * q who +
          ∑ owner, solution.singleton owner *
            table.singletonMatrix who.1 owner := h
      _ = solution.cemetery * q who +
          ∑ owner : NormalPlayer table,
            solution.singleton owner.1 *
              table.singletonMatrix who.1 owner.1 := by
        congr 1
        exact hsumRestrict
          (fun owner => solution.singleton owner *
            table.singletonMatrix who.1 owner)
          (fun owner howner => by rw [hzeroOff owner howner, zero_mul])
  · intro who
    have h := solution.complementary who.1
    rw [extendNormalPlayerDirection_of_mem table q who.2] at h
    have hmatrix (row owner : ι) :
        normalizedSoloMatrix table.zeroNeverReward row owner =
          table.singletonMatrix row owner := by
      exact congr_fun (congr_fun
        (normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
          table hnormalized) row) owner
    simp_rw [hmatrix] at h
    change solution.singleton who.1 *
      (solution.cemetery * q who +
        ∑ owner : NormalPlayer table,
          solution.singleton owner.1 *
            table.singletonMatrix who.1 owner.1) = 0
    rw [← hsumRestrict
      (fun owner => solution.singleton owner *
        table.singletonMatrix who.1 owner)
      (fun owner howner => by rw [hzeroOff owner howner, zero_mul])]
    exact h

/-! **Theorem 2.13 (paper).** Assume the normal-player set is nonempty and
the zero-right-hand-side projective LCP has no nontrivial solution. If the
normal-player matrix is not Q, then ordinary ε-equilibria exist for every
positive ε. If it is Q, then a sunspot ε-equilibrium exists for every
positive ε in which at most one player quits with positive probability at
each stage. The paper-local public-signal relation is the
`SunspotProfile` relation above. -/

/-! Theorem 2.13(1): the non-Q branch yields ordinary ε-equilibria. The
paper does not claim stationarity here; that stronger conclusion belongs to
the alpha-player variant in Theorem 5.1. -/
theorem theorem2_13_nonQ
    (table : Table ι)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (_hnormal : HasNormalPlayers table)
    (_hzero : ¬HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table))
    (hnotQ : ¬QMatrix (NormalMatrix table)) :
    ∀ ε : ℝ, 0 < ε →
      ∃ profile : (quittingGame table.terminal).BehaviorProfile,
        EpsilonEquilibrium table ε profile := by
  classical
  let reward := table.zeroNeverReward
  obtain ⟨q, hno⟩ := exists_direction_without_projectiveLCPSolution
    (NormalMatrix table) hnotQ
  let qfull := extendNormalPlayerDirection table q
  obtain ⟨g⟩ := nonempty_analyticBellmanGerm_quittingGame
    (quittingRewardShift reward (fun who =>
      QuittingLCPClassification.quittingSoloBaseline reward who + qfull who))
  have hmassLe : quittingStationaryContinueMass
      (g.endpointProfile none) ≤ 1 :=
    quittingStationaryContinueMass_le_one (g.endpointProfile none)
  have hzeroGame : ∀ ε : ℝ, 0 < ε →
      ∃ profile : (quittingGame reward).BehaviorProfile,
        (quittingGame reward).IsεAsymptoticNash
          (quittingTerminalPayoff reward) ε profile := by
    intro ε hε
    rcases lt_or_eq_of_le hmassLe with habsorbs | hcontinue
    · rcases two_positive_or_isolated_of_continueMass_lt_one
          (g.endpointProfile none) habsorbs with htwo | hisolated
      · have hcontracts := fixedOpponents_contract_of_two_positive
          (g.endpointProfile none) htwo
        have hvalue :=
          isQuittingStationaryUniformEquilibriumPayoff_of_shiftedGerm_absorbingEndpoint
            reward (fun who =>
              QuittingLCPClassification.quittingSoloBaseline reward who +
                qfull who) g habsorbs hcontracts
        obtain ⟨root, hnash, _⟩ := hvalue ε hε
        exact ⟨quittingStationaryProfile reward root, hnash⟩
      · obtain ⟨owner, howner, hother⟩ := hisolated
        have hownerNormal : IsNormalPlayer table owner := by
          by_contra habnormal
          have hownerNotMem : owner ∉ NormalPlayers table := by
            simpa only [mem_normalPlayers_iff]
          have hqfull : 0 < qfull owner := by
            rw [show qfull owner = 1 by
              exact extendNormalPlayerDirection_of_notMem
                table q hownerNotMem]
            norm_num
          obtain ⟨blocker, hne, hentry⟩ :=
            exists_normalizedSoloMatrix_blocker_of_isolated_endpoint
              reward qfull g owner howner hother hqfull
          have hmatrix := congr_fun (congr_fun
            (normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
              table hnormalized) owner) blocker
          rw [show reward = table.zeroNeverReward by rfl, hmatrix] at hentry
          have hpositive := lemma2_6 table hnormalized hbounded
            habnormal hne
          exact (not_lt_of_ge hentry) hpositive
        let root := g.endpointProfile none
        let hazard := root owner
        have hroot : root = quittingSoloStationaryRoot owner hazard :=
          eq_quittingSoloStationaryRoot_of_others_continue hother
        let target := quittingPayoffUnshift (fun who =>
          QuittingLCPClassification.quittingSoloBaseline reward who +
            qfull who) (quittingGermValue g 0)
        have hfixedShift := quittingGerm_endpoint_fixedPoint g
        have hfixed : target =
            quittingRootSuccessorPayoff reward target root :=
          quittingRootFixedPoint_unshift reward (fun who =>
            QuittingLCPClassification.quittingSoloBaseline reward who +
              qfull who)
            (quittingGermValue g 0) root hfixedShift
        have hnashEndpoint : IsεQuittingRootEndpointNash
            reward target 0 root :=
          (isεQuittingRootEndpointNash_zero_shift_iff reward (fun who =>
            QuittingLCPClassification.quittingSoloBaseline reward who +
              qfull who)
            (quittingGermValue g 0) root).mp
              (quittingGerm_endpoint_endpointNash g)
        have hnashRoot : IsεQuittingRootNash reward target 0 root :=
          (isZeroQuittingRootEndpointNash_iff_isZeroQuittingRootNash
            reward target root).mp hnashEndpoint
        let cycle : Fin 1 → ι → PMF Bool := fun _ => root
        let value : Fin 1 → Payoff ι := fun _ => target
        have hpolicy : ∀ phase : Fin 1,
            value phase = quittingRootSuccessorPayoff reward
              (value (finRotate 1 phase)) (cycle phase) := by
          intro phase
          simpa only [value, cycle] using hfixed
        have hnash : ∀ phase : Fin 1,
            IsεQuittingRootNash reward
              (value (finRotate 1 phase)) 0 (cycle phase) := by
          intro phase
          simpa only [value, cycle] using hnashRoot
        have hcycleAbsorbs :
            (∏ phase : Fin 1,
              quittingStationaryContinueMass (cycle phase)) < 1 := by
          simpa only [cycle, Fin.prod_univ_one] using habsorbs
        have hadmissible :
            IsQuittingCyclePunishmentAdmissible reward cycle := by
          intro who
          unfold IsQuittingCyclePunishmentAdmissibleAt
          by_cases hwho : who = owner
          · subst who
            right
            have hpunishment := hownerNormal
            unfold IsNormalPlayer at hpunishment
            rw [table.punishmentValue_eq_add_never] at hpunishment
            change quittingPunishmentValue table.zeroNeverReward owner ≤
              table.zeroNeverReward
                (quittingSingletonTerminal owner) owner
            unfold QuittingPayoffTable.zeroNeverReward
            have hsolo : table.terminal
                (quittingSingletonTerminal owner) owner = 0 := by
              simpa [quittingSingletonTerminal,
                quittingProjectiveSingletonTerminal] using hnormalized owner
            rw [hsolo]
            change quittingPunishmentValue
                (fun S who => table.terminal S who - table.never who)
                owner + table.never owner ≤ 0 at hpunishment
            linarith
          · left
            have hpositive : 0 < (hazard true).toReal := howner
            have hcontract :=
              quittingStationaryFixedOpponentsContinueMass_solo_other_lt_one
                hwho hazard hpositive
            rw [← hroot] at hcontract
            simpa only [cycle, Fin.prod_univ_one] using hcontract
        obtain ⟨profile, hnashProfile, _⟩ :=
          exists_isεAsymptoticNash_close_of_punishmentAdmissibleCycle
            reward cycle value 0 hpolicy hnash hcycleAbsorbs
              hadmissible hε
        exact ⟨profile, hnashProfile⟩
    · obtain ⟨normalOwner, hqnegative⟩ :=
        exists_negative_of_no_projectiveLCPSolution
          (NormalMatrix table) q hno
      have hnegative : qfull normalOwner.1 < 0 := by
        simpa only [qfull, extendNormalPlayerDirection_of_mem
          table q normalOwner.2] using hqnegative
      have hnotZero : ¬IsQuittingZeroSolo
          (quittingRewardShift reward (fun who =>
            QuittingLCPClassification.quittingSoloBaseline reward who +
              qfull who)) :=
        not_isQuittingZeroSolo_baselineShift_of_negative
          reward qfull hnegative
      rcases quittingGerm_allContinue_zeroSolo_or_supportedProjectivePacket
          (quittingRewardShift reward (fun who =>
            QuittingLCPClassification.quittingSoloBaseline reward who +
              qfull who)) g hcontinue with
        hzeroSolo | hsupported
      · exact False.elim (hnotZero hzeroSolo)
      · let solution : ProjectiveLCPSolution
            (normalizedSoloMatrix reward) qfull :=
          projectiveLCPSolutionOfBaselineShiftedPacket
            reward qfull hsupported.some.packet
        have hsupport : ∀ who, 0 < solution.singleton who →
            who ∈ NormalPlayers table := by
          intro who hpositive
          by_contra hnotNormal
          have habnormal : ¬IsNormalPlayer table who := by
            simpa only [mem_normalPlayers_iff] using hnotNormal
          have hactive : QuittingGermEventuallyActive g who :=
            hsupported.some.positive_eventuallyActive who (by
              simpa only [solution,
                projectiveLCPSolutionOfBaselineShiftedPacket_singleton]
                using hpositive)
          have hfixed : quittingStationaryFixedOpponentsContinueMass
              (g.endpointProfile none) who = 1 := by
            have hlower :=
              quittingStationaryContinueMass_le_fixedOpponentsContinueMass
                (g.endpointProfile none) who
            have hupper := quittingStationaryContinueMass_le_one
              (Function.update (g.endpointProfile none) who
                (PMF.pure false))
            change quittingStationaryFixedOpponentsContinueMass
              (g.endpointProfile none) who ≤ 1 at hupper
            linarith
          obtain ⟨data⟩ :=
            nonempty_quittingGermOpponentLeadingData g who
          have hqfull : 0 < qfull who := by
            rw [show qfull who = 1 by
              exact extendNormalPlayerDirection_of_notMem
                table q hnotNormal]
            norm_num
          obtain ⟨blocker, hweight, hentry⟩ :=
            exists_weighted_nonpositive_normalizedSoloMatrix_of_eventuallyActive
              reward qfull g who hfixed hactive data hqfull
          have hne : blocker ≠ who := by
            intro heq
            subst blocker
            rw [data.singletonWeight_self] at hweight
            exact (lt_irrefl 0) hweight
          have hmatrix := congr_fun (congr_fun
            (normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
              table hnormalized) who) blocker
          rw [show reward = table.zeroNeverReward by rfl, hmatrix] at hentry
          have hpositiveEntry := lemma2_6 table hnormalized hbounded
            habnormal hne
          exact (not_lt_of_ge hentry) hpositiveEntry
        have hrestricted := restrictProjectiveLCPSolutionToNormalPlayers
          table hnormalized q solution (by
            simpa only [reward] using hsupport)
        exact False.elim (hno ⟨hrestricted⟩)
  intro ε hε
  obtain ⟨profile, hnash⟩ := hzeroGame ε hε
  refine ⟨profile, ?_⟩
  exact (table.isεAsymptoticNash_iff ε profile).2 hnash

/-! Theorem 2.13(2): the Q branch yields a unilateral-quitting sunspot
ε-equilibrium for every `ε>0`. Its checked declaration follows the
Section 3 compiler and estimates on which its proof depends. -/

/-! ## Section 2.5 — example -/

/-! The paper gives a four-player example with
`r¹=(0,4,-1,-1)`, `r²=(4,0,-1,-1)`, `r³=(-1,-1,0,4)`, and
`r⁴=(-1,-1,4,0)`, then describes two public-signal constructions, with
identities (5)--(8), and concludes that the first construction is a sunspot
`ε`-equilibrium and the second a sunspot `5ε`-equilibrium. These are
construction-specific probability statements, so they remain paper notes
rather than generic repository declarations. -/

/-! ## Section 3 — proof of Theorem 2.13 -/

/-! **Section 3.1 (paper definitions).** The paper defines the
`λ`-discounted quitting game, its payoff `γ^λ`, stationary payoff formula (9),
λ-discounted equilibrium, and invokes Fink/Takahashi and Bewley--Kohlberg to
choose a semi-algebraic stationary equilibrium branch `λ ↦ x^λ` with a
limit. The checked carrier below uses the repository's analytic Bellman germ
after playerwise normalization of the nontermination payoff. -/

/-! Subtracting `r^∅` identifies the paper's discounted game with the
repository quitting game `quittingGame table.zeroNeverReward`, up to the
playerwise payoff constant `r^∅`. -/
abbrev DiscountedGame (table : Table ι) :=
  quittingGame table.zeroNeverReward

/-! A semi-algebraic stationary equilibrium branch on `(0,1)` with a limit
as `λ→0+`. -/
abbrev SemiAlgebraicSelection (table : Table ι) :=
  (DiscountedGame table).AnalyticBellmanGerm

/-! Existence of the paper's discounted-equilibrium selection. -/
abbrev HasSemiAlgebraicSelection (table : Table ι) : Prop :=
  Nonempty ((DiscountedGame table).AnalyticBellmanGerm)

/-! Fink/Takahashi plus Bewley--Kohlberg selection claim. -/
theorem discountedSelection
    (table : Table ι) : HasSemiAlgebraicSelection table :=
  nonempty_analyticBellmanGerm_quittingGame table.zeroNeverReward

/-! The displayed stationary formula (9) is the stationary specialization of
the Bellman values on this germ. The formal carrier uses the reusable checked
stochastic-game semantics rather than an unconstrained payoff field. -/

/-! **Lemma 3.2 (published paper).** For
`D = conv(r̂¹,…,r̂ⁿ) ∩ ℝⁿ_≥0`, if `q̂` is in the column hull but not
the nonnegative orthant, every solution `(w,z)` of `LCP(R̂,q̂)` has
`w ∈ D₀`. -/

/-! `ColumnConvexHull M = conv{M_{·i}:i∈I}`. -/
def ColumnConvexHull (M : ι → ι → ℝ) : Set (ι → ℝ) :=
  convexHull ℝ (Set.range fun owner => fun who => M who owner)

/-! `D = conv{r̂¹,…,r̂ⁿ} ∩ ℝ^n_≥0`. -/
def D (M : ι → ι → ℝ) : Set (ι → ℝ) :=
  ColumnConvexHull M ∩ {w | ∀ who, 0 ≤ w who}

/-! The source normalizes every payoff coordinate to `[-1,1]`. -/
def MatrixPayoffsBounded (M : ι → ι → ℝ) : Prop :=
  ∀ who owner, |M who owner| ≤ 1

theorem normalMatrix_payoffsBounded
    (table : Table ι) (hbounded : TablePayoffsBounded table) :
    MatrixPayoffsBounded (NormalMatrix table) := by
  intro who owner
  exact hbounded.1 _ _

theorem normalMatrix_diagonal_zero
    (table : Table ι) (hnormalized : SoloExitNormalized table) :
    ∀ who, NormalMatrix table who who = 0 := by
  intro who
  exact hnormalized who.1

/-! `D₀` consists of the points of `D` with a zero coordinate. The paper
only observes that this set lies in the relative boundary of `D`; replacing
it by the ambient frontier would be vacuous because `n` columns in `ℝⁿ`
have lower-dimensional convex hull. -/
def DZero (M : ι → ι → ℝ) (w : ι → ℝ) : Prop :=
  w ∈ D M ∧ ∃ who, w who = 0

omit [Fintype ι] [DecidableEq ι] in
theorem abs_le_one_of_mem_D
    {M : ι → ι → ℝ} (hbound : MatrixPayoffsBounded M)
    {value : ι → ℝ} (hvalue : value ∈ D M) (who : ι) :
    |value who| ≤ 1 := by
  let cube : Set (ι → ℝ) :=
    Set.univ.pi (fun _ => Set.Icc (-1 : ℝ) 1)
  have hcubeConvex : Convex ℝ cube := by
    exact convex_pi fun _ _ => convex_Icc (-1 : ℝ) 1
  have hcolumns : Set.range (fun owner => fun row => M row owner) ⊆ cube := by
    rintro value ⟨owner, rfl⟩ row _
    exact abs_le.mp (hbound row owner)
  have hcube : value ∈ cube :=
    convexHull_min hcolumns hcubeConvex hvalue.1
  exact abs_le.mpr (hcube who (Set.mem_univ who))

/-! The coordinatewise nonnegative orthant. -/
def NonnegativeOrthant (ι : Type) :=
  {w : ι → ℝ | ∀ who, 0 ≤ w who}

omit [DecidableEq ι] in
theorem isClosed_D (M : ι → ι → ℝ) : IsClosed (D M) := by
  apply IsClosed.inter
  · exact (Set.finite_range (fun owner => fun who => M who owner)).isClosed_convexHull ℝ
  · rw [show {w : ι → ℝ | ∀ who, 0 ≤ w who} =
        ⋂ who, {w | 0 ≤ w who} by ext; simp]
    exact isClosed_iInter fun who => isClosed_le
      (continuous_const : Continuous (fun _ : (ι → ℝ) => (0 : ℝ)))
      (continuous_apply who)

/-! The corrected boundary carrier `D₀` is closed. Finiteness is essential:
the zero-coordinate condition is a finite union of closed hyperplanes. -/
omit [DecidableEq ι] in
theorem isClosed_DZero (M : ι → ι → ℝ) :
    IsClosed {w | DZero M w} := by
  rw [show {w | DZero M w} =
      ⋃ who, D M ∩ {w | w who = 0} by
    ext w
    simp only [DZero, Set.mem_setOf_eq, Set.mem_iUnion, Set.mem_inter_iff]
    aesop]
  exact isClosed_iUnion_of_finite fun who =>
    (isClosed_D M).inter <| isClosed_eq
      (continuous_apply who) continuous_const

omit [Fintype ι] [DecidableEq ι] in
theorem convex_D (M : ι → ι → ℝ) : Convex ℝ (D M) := by
  apply (convex_convexHull ℝ _).inter
  rw [show {w : ι → ℝ | ∀ who, 0 ≤ w who} =
      ⋂ who, (LinearMap.proj (R := ℝ) who) ⁻¹' Set.Ici 0 by ext; simp]
  apply convex_iInter
  intro who
  exact (convex_Ici (𝕜 := ℝ) (0 : ℝ)).linear_preimage
    (LinearMap.proj (R := ℝ) (φ := fun _ : ι => ℝ) who)

omit [Fintype ι] [DecidableEq ι] in
theorem mem_D_of_DZero {M : ι → ι → ℝ} {y : ι → ℝ}
    (hy : DZero M y) : y ∈ D M :=
  hy.1

omit [DecidableEq ι] in
private theorem frontier_nonnegativeOrthant_has_zero
    {w : ι → ℝ} (hw : w ∈ frontier (NonnegativeOrthant ι)) :
    w ∈ NonnegativeOrthant ι ∧ ∃ who, w who = 0 := by
  have hclosed : IsClosed (NonnegativeOrthant ι) := by
    rw [NonnegativeOrthant, show {x : ι → ℝ | ∀ who, 0 ≤ x who} =
      ⋂ who, {x | 0 ≤ x who} by ext; simp]
    exact isClosed_iInter fun who => isClosed_le
      (continuous_const : Continuous (fun _ : (ι → ℝ) => (0 : ℝ)))
      (continuous_apply who)
  have hwmem : w ∈ NonnegativeOrthant ι := hclosed.frontier_subset hw
  refine ⟨hwmem, ?_⟩
  by_contra hzero
  push Not at hzero
  have hpos : ∀ who, 0 < w who := fun who =>
    lt_of_le_of_ne (hwmem who) (Ne.symm (hzero who))
  have hwInterior : w ∈ interior (NonnegativeOrthant ι) := by
    have heq : {x : ι → ℝ | ∀ who, 0 ≤ x who} =
        Set.univ.pi (fun _ => Set.Ici 0) := by
      ext x
      constructor
      · intro hx who _
        exact hx who
      · intro hx who
        exact hx who (Set.mem_univ who)
    rw [NonnegativeOrthant, heq, interior_pi_set Set.finite_univ,
      Set.mem_pi]
    intro who _
    simpa only [interior_Ici, Set.mem_Ioi] using hpos who
  exact (mem_frontier_iff_notMem_interior hwmem).1 hw hwInterior

private theorem exists_frontier_on_segment_of_mem_notMem
    {E : Type*} [NormedAddCommGroup E] [NormedSpace ℝ E]
    {s : Set E} {u v : E} (hu : u ∈ s) (hv : v ∉ s) :
    ∃ w ∈ segment ℝ u v, w ∈ frontier s := by
  by_contra hnone
  push Not at hnone
  have havoid : segment ℝ u v ⊆ (frontier s)ᶜ := fun w hw => hnone w hw
  rw [compl_frontier_eq_union_interior] at havoid
  have huInterior : u ∈ interior s := by
    by_contra huNot
    exact hnone u (left_mem_segment ℝ u v)
      ((mem_frontier_iff_notMem_interior hu).2 huNot)
  have hvInterior : v ∈ interior sᶜ := by
    by_contra hvNot
    exact hnone v (right_mem_segment ℝ u v) <| by
      rw [← frontier_compl]
      exact (mem_frontier_iff_notMem_interior (show v ∈ sᶜ from hv)).2 hvNot
  have hdisjoint : Disjoint (interior s) (interior sᶜ) :=
    (Set.disjoint_compl_right_iff_subset.mpr fun _ hx => hx).mono
      interior_subset interior_subset
  have hsubset :=
    (convex_segment u v).isPreconnected.subset_left_of_subset_union
      isOpen_interior isOpen_interior hdisjoint havoid
      ⟨u, left_mem_segment ℝ u v, huInterior⟩
  exact hv (interior_subset (hsubset (right_mem_segment ℝ u v)))

private theorem endpoint_not_mem_tail_segment
    {E : Type*} [AddCommGroup E] [Module ℝ E] {y p c : E}
    (hp : p ∈ segment ℝ y c) (hpy : p ≠ y) (hcy : c ≠ y) :
    y ∉ segment ℝ p c := by
  rw [segment_eq_image_lineMap] at hp ⊢
  obtain ⟨t, ht, rfl⟩ := hp
  rintro ⟨s, hs, heq⟩
  have htpos : 0 < t := by
    apply lt_of_le_of_ne ht.1
    intro htzero
    apply hpy
    rw [← htzero]
    simp [AffineMap.lineMap_apply_module]
  have hcoeff : 0 < s + (1 - s) * t := by
    have hs0 := hs.1
    have hs1 := hs.2
    by_cases hspos : 0 < s
    · positivity
    · have hs0eq : s = 0 := le_antisymm (le_of_not_gt hspos) hs0
      rw [hs0eq]
      simpa using htpos
  have hv : (s + (1 - s) * t) • (c - y) = 0 := by
    rw [AffineMap.lineMap_apply_module, AffineMap.lineMap_apply_module] at heq
    linear_combination (norm := module) heq
  have hcsub : c - y = 0 :=
    (smul_eq_zero.mp hv).resolve_left (ne_of_gt hcoeff)
  exact hcy (sub_eq_zero.mp hcsub)

/-! **Lemma 3.1 (paper).** If the homogeneous projective LCP has no
nontrivial solution, no singleton column lies in the nonnegative orthant. -/
omit [DecidableEq ι] in
theorem lemma3_1
    (M : ι → ι → ℝ) (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M) (owner : ι) :
    (fun who => M who owner) ∉ NonnegativeOrthant ι := by
  intro hcolumn
  apply hzero
  rw [hasNontrivialZeroProjectiveLCPSolution_iff_homogeneous]
  exact singletonLCPFeasible_of_diag_eq_zero owner (hdiag owner) hcolumn

/-! Lemma 3.2: `q∈conv{r̂ᶦ} ∖ ℝⁿ_≥0` forces every LCP residual into
`D₀`. -/
omit [DecidableEq ι] in
theorem lemma3_2
    (M : ι → ι → ℝ) (q : ι → ℝ)
    (hq : q ∈ ColumnConvexHull M)
    (hqNegative : q ∉ NonnegativeOrthant ι)
    (solution : ProjectiveLCPSolution M q) :
    DZero M (fun who => solution.cemetery * q who +
      ∑ owner, solution.singleton owner * M who owner) := by
  let weight : Option ι → ℝ
    | none => solution.cemetery
    | some i => solution.singleton i
  let point : Option ι → (ι → ℝ)
    | none => q
    | some i => fun who => M who i
  have hweight_nonneg (a : Option ι) : 0 ≤ weight a := by
    cases a with
    | none => exact solution.cemetery_nonneg
    | some i => exact solution.singleton_nonneg i
  have hweight_total : ∑ a : Option ι, weight a = 1 := by
    simp [weight, solution.total]
  have hpoint (a : Option ι) : point a ∈ ColumnConvexHull M := by
    cases a with
    | none => exact hq
    | some i => exact subset_convexHull ℝ _ ⟨i, rfl⟩
  have hconvex : (∑ a : Option ι, weight a • point a) ∈
      ColumnConvexHull M := by
    exact (convex_convexHull ℝ _).sum_mem
      (fun a _ => hweight_nonneg a) hweight_total (fun a _ => hpoint a)
  have hresidual : (fun who => solution.cemetery * q who +
      ∑ owner, solution.singleton owner * M who owner) ∈
      ColumnConvexHull M := by
    convert hconvex using 1
    funext who
    simp [weight, point, smul_eq_mul]
  have hwD : (fun who => solution.cemetery * q who +
      ∑ owner, solution.singleton owner * M who owner) ∈ D M :=
    ⟨hresidual, solution.residual_nonneg⟩
  have hsumNonneg : 0 ≤ ∑ owner, solution.singleton owner :=
    Finset.sum_nonneg fun owner _ => solution.singleton_nonneg owner
  have hcemeteryLe : solution.cemetery ≤ 1 := by
    linarith [solution.total]
  have hcemeteryLt : solution.cemetery < 1 := by
    apply lt_of_le_of_ne hcemeteryLe
    intro hcemetery
    have hsumZero : ∑ owner, solution.singleton owner = 0 := by
      linarith [solution.total]
    have hsingletonZero (owner : ι) : solution.singleton owner = 0 :=
      (Finset.sum_eq_zero_iff_of_nonneg
        (fun i _ => solution.singleton_nonneg i)).mp hsumZero owner
        (Finset.mem_univ owner)
    apply hqNegative
    intro who
    simpa only [hcemetery, one_mul, hsingletonZero, zero_mul,
      Finset.sum_const_zero, add_zero] using solution.residual_nonneg who
  have hsumPositive : 0 < ∑ owner, solution.singleton owner := by
    linarith [solution.total]
  obtain ⟨who, _, hwho⟩ :=
    (Finset.sum_pos_iff_of_nonneg
      (fun i _ => solution.singleton_nonneg i)).mp hsumPositive
  refine ⟨hwD, who, ?_⟩
  exact (mul_eq_zero.mp (solution.complementary who)).resolve_left
    (ne_of_gt hwho)

/-! Under the Q and no-homogeneous-solution hypotheses, `D₀` is nonempty:
solve the projective LCP in the direction of any (necessarily nonpositive)
matrix column and apply Lemmas 3.1--3.2. -/
omit [DecidableEq ι] in
theorem exists_mem_DZero [Nonempty ι]
    (M : ι → ι → ℝ) (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (hQ : QMatrix M) : ∃ y, DZero M y := by
  let owner : ι := Classical.arbitrary ι
  let q : ι → ℝ := fun who => M who owner
  have hqHull : q ∈ ColumnConvexHull M :=
    subset_convexHull ℝ _ ⟨owner, rfl⟩
  have hqNegative : q ∉ NonnegativeOrthant ι :=
    lemma3_1 M hdiag hzero owner
  obtain ⟨solution⟩ := hQ q
  exact ⟨fun who => solution.cemetery * q who +
      ∑ other, solution.singleton other * M who other,
    lemma3_2 M q hqHull hqNegative solution⟩

/-! Finitely many strictly negative column witnesses admit one uniform
positive margin, as used in Section 3.3.5. -/
omit [DecidableEq ι] in
theorem exists_uniformNegativeMargin [Nonempty ι]
    (M : ι → ι → ℝ) (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M) :
    ∃ margin : ℝ, 0 < margin ∧
      ∀ owner, ∃ who, M who owner < -margin := by
  classical
  rw [hasNontrivialZeroProjectiveLCPSolution_iff_homogeneous] at hzero
  choose negative hnegative using
    fun owner => exists_negative_entry_in_column_of_noHomogeneous
      M hdiag hzero owner
  let magnitudes : Finset ℝ :=
    Finset.univ.image fun owner => -M (negative owner) owner
  have hmagnitudes : magnitudes.Nonempty := by
    exact Finset.image_nonempty.mpr Finset.univ_nonempty
  let minimum := magnitudes.min' hmagnitudes
  have hminimumPos : 0 < minimum := by
    have hmember : minimum ∈ magnitudes := magnitudes.min'_mem hmagnitudes
    obtain ⟨owner, _, howner⟩ := Finset.mem_image.mp hmember
    rw [← howner]
    linarith [hnegative owner]
  refine ⟨minimum / 2, half_pos hminimumPos, ?_⟩
  intro owner
  refine ⟨negative owner, ?_⟩
  have hminimumLe : minimum ≤ -M (negative owner) owner :=
    magnitudes.min'_le _ <| Finset.mem_image.mpr
      ⟨owner, Finset.mem_univ owner, rfl⟩
  linarith

/-! **Theorem 3.3 (published paper).** For every `y∈D₀` and `ε>0`,
there are `w∈D₀`, vectors `w¹,…,wⁿ`, and simplex weights `z`
satisfying (F.1)--(F.5).
The following paper-local structures spell out those five conditions. -/

/-! `Segment a b x` means `x∈conv{a,b}`. -/
def Segment (a b x : ι → ℝ) : Prop :=
  ∃ weight : ℝ, 0 ≤ weight ∧ weight ≤ 1 ∧
    ∀ who, x who = weight * a who + (1 - weight) * b who

omit [Fintype ι] [DecidableEq ι] in
private theorem segment_of_mem_segment {a b x : ι → ℝ}
    (hx : x ∈ segment ℝ a b) : Segment a b x := by
  rw [segment_eq_image_lineMap] at hx
  obtain ⟨t, ht, rfl⟩ := hx
  refine ⟨1 - t, sub_nonneg.mpr ht.2, by linarith [ht.1], ?_⟩
  intro who
  simp only [AffineMap.lineMap_apply_module, Pi.add_apply, Pi.smul_apply,
    smul_eq_mul]
  ring

omit [Fintype ι] [DecidableEq ι] in
private theorem mem_segment_of_Segment {a b x : ι → ℝ}
    (hx : Segment a b x) : x ∈ segment ℝ a b := by
  obtain ⟨weight, hweight0, hweight1, hx⟩ := hx
  rw [segment_eq_image_lineMap]
  refine ⟨1 - weight, ⟨by linarith, by linarith⟩, ?_⟩
  ext who
  rw [hx]
  simp only [AffineMap.lineMap_apply_module, Pi.add_apply, Pi.smul_apply,
    smul_eq_mul]
  ring

/-! `J_y={i:y_i=0}`. -/
def ZeroCoordinates (y : ι → ℝ) : Finset ι :=
  Finset.univ.filter (fun i => y i = 0)

/-! `SubfamilyHull M J = conv{M_{·i}:i∈J}`. -/
def SubfamilyHull (M : ι → ι → ℝ) (J : Finset ι) : Set (ι → ℝ) :=
  convexHull ℝ (Set.range fun i : J => fun who => M who i)

/-! Simplex weights `z=(z₀,zᵢ)` satisfy `z₀+Σᵢzᵢ=1`. -/
structure SimplexWeights (ι : Type) [Fintype ι] [DecidableEq ι] where
  cemetery : ℝ
  singleton : ι → ℝ
  cemetery_nonneg : 0 ≤ cemetery
  singleton_nonneg : ∀ i, 0 ≤ singleton i
  total : cemetery + ∑ i, singleton i = 1

/-! The lottery on `{0}∪I` represented by the paper's vector
`z=(z₀,(zᵢ)ᵢ)`. -/
def SimplexWeights.toStdSimplex (weight : SimplexWeights ι) :
    stdSimplex ℝ (Option ι) := by
  refine ⟨fun choice => match choice with
    | none => weight.cemetery
    | some owner => weight.singleton owner, ?_, ?_⟩
  · intro choice
    cases choice with
    | none => exact weight.cemetery_nonneg
    | some owner => exact weight.singleton_nonneg owner
  · rw [Fintype.sum_option]
    exact weight.total

/-! The corresponding finite public lottery. -/
noncomputable def SimplexWeights.toPMF (weight : SimplexWeights ι) :
    PMF (Option ι) :=
  Math.ProbabilityMassFunction.ofVector
    weight.toStdSimplex.1 weight.toStdSimplex.2

@[simp] theorem SimplexWeights.toPMF_none_toReal
    (weight : SimplexWeights ι) :
    (weight.toPMF none).toReal = weight.cemetery := by
  change ((Math.ProbabilityMassFunction.ofVector
    weight.toStdSimplex.1 weight.toStdSimplex.2) none).toReal = _
  rw [Math.ProbabilityMassFunction.ofVector_toReal]
  rfl

@[simp] theorem SimplexWeights.toPMF_some_toReal
    (weight : SimplexWeights ι) (owner : ι) :
    (weight.toPMF (some owner)).toReal = weight.singleton owner := by
  change ((Math.ProbabilityMassFunction.ofVector
    weight.toStdSimplex.1 weight.toStdSimplex.2) (some owner)).toReal = _
  rw [Math.ProbabilityMassFunction.ofVector_toReal]
  rfl

/-! Conditions (F.1)--(F.5) of the published Theorem 3.3 for `(M,y,ε)`.
The published condition (F.1) allows the segment containing `wⁱ` to start
at either `w` or `y`. This disjunction is essential in the second case of the
proof; the arXiv v1 statement omitted it even though its printed construction
uses the segment starting at `y`. -/
structure BuildingBlock (M : ι → ι → ℝ) (y : ι → ℝ) (ε : ℝ) where
  w : ι → ℝ
  wi : ι → ι → ℝ
  z : SimplexWeights ι
  y_boundary : DZero M y
  w_boundary : DZero M w
  approach : ∀ i,
    (Segment w (fun who => M who i) (wi i) ∧ wi i ≠ w) ∨
      (Segment y (fun who => M who i) (wi i) ∧ wi i ≠ y)
  lower : ∀ i who, -ε ≤ wi i who
  balance : ∀ who, w who = z.cemetery * y who +
    ∑ i, z.singleton i * wi i who
  complementary : ∀ i, z.singleton i > 0 → wi i i = 0
  nontrivial : 0 < ∑ i, z.singleton i

theorem BuildingBlock.wi_abs_le_one
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε) (hbound : MatrixPayoffsBounded M)
    (owner who : ι) :
    |block.wi owner who| ≤ 1 := by
  rcases block.approach owner with
    ⟨⟨weight, hweight0, hweight1, hvalue⟩, _⟩ |
      ⟨⟨weight, hweight0, hweight1, hvalue⟩, _⟩
  · have hbase := abs_le_one_of_mem_D hbound block.w_boundary.1 who
    have hcolumn := hbound who owner
    rw [hvalue who]
    calc
      |weight * block.w who + (1 - weight) * M who owner| ≤
          |weight * block.w who| + |(1 - weight) * M who owner| :=
        abs_add_le _ _
      _ = weight * |block.w who| + (1 - weight) * |M who owner| := by
        rw [abs_mul, abs_mul, abs_of_nonneg hweight0,
          abs_of_nonneg (sub_nonneg.mpr hweight1)]
      _ ≤ weight * 1 + (1 - weight) * 1 := by
        exact add_le_add
          (mul_le_mul_of_nonneg_left hbase hweight0)
          (mul_le_mul_of_nonneg_left hcolumn
            (sub_nonneg.mpr hweight1))
      _ = 1 := by ring
  · have hbase := abs_le_one_of_mem_D hbound block.y_boundary.1 who
    have hcolumn := hbound who owner
    rw [hvalue who]
    calc
      |weight * y who + (1 - weight) * M who owner| ≤
          |weight * y who| + |(1 - weight) * M who owner| :=
        abs_add_le _ _
      _ = weight * |y who| + (1 - weight) * |M who owner| := by
        rw [abs_mul, abs_mul, abs_of_nonneg hweight0,
          abs_of_nonneg (sub_nonneg.mpr hweight1)]
      _ ≤ weight * 1 + (1 - weight) * 1 := by
        exact add_le_add
          (mul_le_mul_of_nonneg_left hbase hweight0)
          (mul_le_mul_of_nonneg_left hcolumn
            (sub_nonneg.mpr hweight1))
      _ = 1 := by ring

/-! Equation (14): `‖w(y)-y‖∞ ≤ 2 ∑ᵢ zᵢ(y)`. -/
theorem BuildingBlock.displacement_le_twice_singletonMass
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε) (hbound : MatrixPayoffsBounded M) :
    dist y block.w ≤ 2 * ∑ owner, block.z.singleton owner := by
  have hmass : 0 ≤ ∑ owner, block.z.singleton owner :=
    Finset.sum_nonneg fun owner _ => block.z.singleton_nonneg owner
  rw [dist_eq_norm]
  rw [pi_norm_le_iff_of_nonneg
    (show 0 ≤ (2 : ℝ) * ∑ owner, block.z.singleton owner by
      exact mul_nonneg (by norm_num) hmass)]
  intro who
  rw [Pi.sub_apply, Real.norm_eq_abs]
  have hidentity : y who - block.w who =
      ∑ owner, block.z.singleton owner *
        (y who - block.wi owner who) := by
    rw [block.balance who]
    have htotal := block.z.total
    calc
      y who - (block.z.cemetery * y who +
          ∑ owner, block.z.singleton owner * block.wi owner who) =
          (1 - block.z.cemetery) * y who -
            ∑ owner, block.z.singleton owner * block.wi owner who := by
          ring
      _ = (∑ owner, block.z.singleton owner) * y who -
            ∑ owner, block.z.singleton owner * block.wi owner who := by
          congr 2
          linarith
      _ = ∑ owner, block.z.singleton owner *
            (y who - block.wi owner who) := by
          rw [Finset.sum_mul, ← Finset.sum_sub_distrib]
          apply Finset.sum_congr rfl
          intro owner _
          ring
  rw [hidentity]
  calc
    |∑ owner, block.z.singleton owner *
        (y who - block.wi owner who)| ≤
        ∑ owner, |block.z.singleton owner *
          (y who - block.wi owner who)| :=
      Finset.abs_sum_le_sum_abs _ _
    _ ≤ ∑ owner, block.z.singleton owner * 2 := by
      apply Finset.sum_le_sum
      intro owner _
      rw [abs_mul, abs_of_nonneg (block.z.singleton_nonneg owner)]
      apply mul_le_mul_of_nonneg_left _ (block.z.singleton_nonneg owner)
      apply (abs_sub (y who) (block.wi owner who)).trans
      have hadd := add_le_add
        (abs_le_one_of_mem_D hbound block.y_boundary.1 who)
        (block.wi_abs_le_one hbound owner who)
      norm_num at hadd
      exact hadd
    _ = 2 * ∑ owner, block.z.singleton owner := by
      rw [← Finset.sum_mul]
      ring

/-! The value assigned to a public type draw: type zero advances directly
to `y`, while type `i` runs the corresponding attempt with value `wⁱ`. -/
def BuildingBlock.choiceValue
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε) (choice : Option ι) (who : ι) : ℝ :=
  match choice with
  | none => y who
  | some owner => block.wi owner who

/-! Condition (F.4) is exactly the harmonicity of the public type draw. -/
theorem BuildingBlock.expect_choiceValue
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε) (who : ι) :
    expect block.z.toPMF (fun choice => block.choiceValue choice who) =
      block.w who := by
  rw [expect_eq_sum, Fintype.sum_option]
  simp only [BuildingBlock.choiceValue,
    SimplexWeights.toPMF_none_toReal,
    SimplexWeights.toPMF_some_toReal]
  rw [block.balance who]

/-! A type-`i` block either returns to the current auxiliary-game root `w`
after no quit, or exits the auxiliary game to its continuation target `y`.
This is the operational distinction in the two branches of (F.1). -/
inductive AttemptContinuation
  | restart
  | advance
deriving DecidableEq

/-! The branch-sensitive data of one type-`i` attempt. The strict upper
bound on `quitWeight` is what permits the total quit probability to be spread
over an arbitrarily long finite block. -/
structure BuildingAttempt (M : ι → ι → ℝ) (y : ι → ℝ)
    (ε : ℝ) (block : BuildingBlock M y ε) (owner : ι) where
  continuation : AttemptContinuation
  quitWeight : ℝ
  quitWeight_pos : 0 < quitWeight
  quitWeight_lt_one : quitWeight < 1
  payoff : ∀ who,
    block.wi owner who =
      quitWeight * M who owner + (1 - quitWeight) *
        match continuation with
        | .restart => block.w who
        | .advance => y who

/-! Extract the operational branch and its quit weight from (F.1). The
negative coordinate required in Section 3.3.5 rules out quit weight one;
(F.2) would otherwise be violated at that coordinate. -/
theorem BuildingBlock.exists_attempt
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε) (owner : ι)
    (hnegative : ∃ who, M who owner < -ε) :
    Nonempty (BuildingAttempt M y ε block owner) := by
  classical
  rcases block.approach owner with hrestart | hadvance
  · obtain ⟨⟨weight, hweight0, hweight1, hpayoff⟩, hne⟩ := hrestart
    obtain ⟨negative, hnegative⟩ := hnegative
    have hweightPos : 0 < weight := by
      apply lt_of_le_of_ne hweight0
      intro hweight
      have hlow := block.lower owner negative
      have hcolumn := hpayoff negative
      rw [← hweight] at hcolumn
      norm_num at hcolumn
      linarith
    have hweightLt : weight < 1 := by
      apply lt_of_le_of_ne hweight1
      intro hweight
      apply hne
      funext who
      rw [hpayoff who, hweight]
      ring
    exact ⟨
      { continuation := .restart
        quitWeight := 1 - weight
        quitWeight_pos := sub_pos.mpr hweightLt
        quitWeight_lt_one := by linarith
        payoff := by
          intro who
          rw [hpayoff who]
          ring }⟩
  · obtain ⟨⟨weight, hweight0, hweight1, hpayoff⟩, hne⟩ := hadvance
    obtain ⟨negative, hnegative⟩ := hnegative
    have hweightPos : 0 < weight := by
      apply lt_of_le_of_ne hweight0
      intro hweight
      have hlow := block.lower owner negative
      have hcolumn := hpayoff negative
      rw [← hweight] at hcolumn
      norm_num at hcolumn
      linarith
    have hweightLt : weight < 1 := by
      apply lt_of_le_of_ne hweight1
      intro hweight
      apply hne
      funext who
      rw [hpayoff who, hweight]
      ring
    exact ⟨
      { continuation := .advance
        quitWeight := 1 - weight
        quitWeight_pos := sub_pos.mpr hweightLt
        quitWeight_lt_one := by linarith
        payoff := by
          intro who
          rw [hpayoff who]
          ring }⟩

noncomputable def BuildingBlock.attempt
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε) (owner : ι)
    (hnegative : ∃ who, M who owner < -ε) :
    BuildingAttempt M y ε block owner :=
  Classical.choice (block.exists_attempt owner hnegative)

/-! Under the negative-column margin used in Section 3.3.5, a building
block cannot return its input boundary point. Otherwise the effective quit
weights in its balance equation give a nontrivial homogeneous LCP solution. -/
theorem BuildingBlock.w_ne_y_of_column_negative
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    (block : BuildingBlock M y ε)
    (hdiag : ∀ who, M who who = 0)
    (hnegative : ∀ owner, ∃ who, M who owner < -ε)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M) :
    block.w ≠ y := by
  intro hwy
  apply hzero
  rw [hasNontrivialZeroProjectiveLCPSolution_iff_homogeneous]
  let attempt : ∀ owner, BuildingAttempt M y ε block owner :=
    fun owner => block.attempt owner (hnegative owner)
  let mass : ι → ℝ := fun owner =>
    block.z.singleton owner * (attempt owner).quitWeight
  let totalMass : ℝ := ∑ owner, mass owner
  have hmassNonneg (owner : ι) : 0 ≤ mass owner := by
    exact mul_nonneg (block.z.singleton_nonneg owner)
      (attempt owner).quitWeight_pos.le
  have htotalMassPos : 0 < totalMass := by
    have hsingletonPos : 0 < ∑ owner, block.z.singleton owner :=
      block.nontrivial
    obtain ⟨owner, _, howner⟩ :=
      (Finset.sum_pos_iff_of_nonneg
        (fun i _ => block.z.singleton_nonneg i)).mp hsingletonPos
    apply Finset.sum_pos'
    · exact fun i _ => hmassNonneg i
    · exact ⟨owner, Finset.mem_univ owner,
        mul_pos howner (attempt owner).quitWeight_pos⟩
  let weight : stdSimplex ℝ ι :=
    ⟨fun owner => mass owner * totalMass⁻¹,
      fun owner => mul_nonneg (hmassNonneg owner)
        (inv_nonneg.mpr htotalMassPos.le), by
        rw [← Finset.sum_mul]
        exact mul_inv_cancel₀ (ne_of_gt htotalMassPos)⟩
  have hbalance (who : ι) :
      ∑ owner, mass owner * M who owner = totalMass * y who := by
    have hblock := block.balance who
    rw [hwy] at hblock
    have htotal := block.z.total
    have hattempt (owner : ι) :
        block.wi owner who =
          (attempt owner).quitWeight * M who owner +
            (1 - (attempt owner).quitWeight) * y who := by
      have hpayoff := (attempt owner).payoff who
      cases hcontinuation : (attempt owner).continuation <;>
        simp only [hcontinuation, hwy] at hpayoff ⊢ <;>
        exact hpayoff
    calc
      ∑ owner, mass owner * M who owner =
          ∑ owner, block.z.singleton owner *
            ((attempt owner).quitWeight * M who owner) := by
        apply Finset.sum_congr rfl
        intro owner _
        simp only [mass]
        ring
      _ = ∑ owner, block.z.singleton owner *
            (block.wi owner who -
              (1 - (attempt owner).quitWeight) * y who) := by
        apply Finset.sum_congr rfl
        intro owner _
        rw [hattempt owner]
        ring
      _ = totalMass * y who := by
        simp only [totalMass, mass]
        have hmassExpand :
            ∑ owner,
                block.z.singleton owner * (attempt owner).quitWeight =
              ∑ owner, block.z.singleton owner -
                ∑ owner, block.z.singleton owner *
                  (1 - (attempt owner).quitWeight) := by
          rw [← Finset.sum_sub_distrib]
          apply Finset.sum_congr rfl
          intro owner _
          ring
        rw [hmassExpand]
        calc
          ∑ owner, block.z.singleton owner *
                (block.wi owner who -
                  (1 - (attempt owner).quitWeight) * y who) =
              ∑ owner, block.z.singleton owner * block.wi owner who -
                (∑ owner, block.z.singleton owner *
                  (1 - (attempt owner).quitWeight)) * y who := by
            rw [Finset.sum_mul, ← Finset.sum_sub_distrib]
            apply Finset.sum_congr rfl
            intro owner _
            ring
          _ = (1 - block.z.cemetery) * y who -
                (∑ owner, block.z.singleton owner *
                  (1 - (attempt owner).quitWeight)) * y who := by
            congr 2
            linarith
          _ = (∑ owner, block.z.singleton owner -
                ∑ owner, block.z.singleton owner *
                  (1 - (attempt owner).quitWeight)) * y who := by
            rw [show 1 - block.z.cemetery =
                ∑ owner, block.z.singleton owner by linarith]
            ring
          _ = _ := by ring
  refine ⟨weight, ?_, ?_⟩
  · intro who
    change 0 ≤ ∑ owner,
      (mass owner * totalMass⁻¹) * M who owner
    calc
      0 ≤ totalMass⁻¹ * (totalMass * y who) := by
        exact mul_nonneg (inv_nonneg.mpr htotalMassPos.le)
          (mul_nonneg htotalMassPos.le (block.y_boundary.1.2 who))
      _ = ∑ owner,
          (mass owner * totalMass⁻¹) * M who owner := by
        rw [← hbalance who, Finset.mul_sum]
        apply Finset.sum_congr rfl
        intro owner _
        ring
  · intro owner
    change (mass owner * totalMass⁻¹) *
      (∑ other, (mass other * totalMass⁻¹) * M owner other) = 0
    by_cases hsingleton : block.z.singleton owner = 0
    · simp [mass, hsingleton]
    · have hsingletonPos : 0 < block.z.singleton owner :=
        lt_of_le_of_ne (block.z.singleton_nonneg owner)
          (Ne.symm hsingleton)
      have hwiZero := block.complementary owner hsingletonPos
      have hattempt : block.wi owner owner =
          (1 - (attempt owner).quitWeight) * y owner := by
        have hpayoff := (attempt owner).payoff owner
        rw [hdiag owner, mul_zero, zero_add] at hpayoff
        cases hcontinuation : (attempt owner).continuation <;>
          simp only [hcontinuation, hwy] at hpayoff ⊢ <;>
          exact hpayoff
      have hyZero : y owner = 0 := by
        rw [hattempt] at hwiZero
        exact (mul_eq_zero.mp hwiZero).resolve_left
          (sub_ne_zero.mpr (ne_of_gt (attempt owner).quitWeight_lt_one))
      have hresidualZero :
          ∑ other, (mass other * totalMass⁻¹) * M owner other = 0 := by
        calc
          ∑ other, (mass other * totalMass⁻¹) * M owner other =
              totalMass⁻¹ * (∑ other, mass other * M owner other) := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro other _
            ring
          _ = totalMass⁻¹ * (totalMass * y owner) := by
            rw [hbalance owner]
          _ = 0 := by rw [hyZero, mul_zero, mul_zero]
      rw [hresidualZero, mul_zero]

/-! Any interior total quit weight can be spread over a sufficiently long
block so that the per-stage quit probability is below a prescribed positive
accuracy, while preserving the total survival probability exactly. -/
theorem BuildingAttempt.exists_mesh
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    {block : BuildingBlock M y ε} {owner : ι}
    (attempt : BuildingAttempt M y ε block owner)
    {η : ℝ} (hη : 0 < η) :
    ∃ mesh : ℕ, 0 < mesh ∧
      quittingMeshHazard attempt.quitWeight mesh < η ∧
      (1 - quittingMeshHazard attempt.quitWeight mesh) ^ mesh =
        1 - attempt.quitWeight := by
  obtain ⟨mesh, hmesh⟩ := exists_nat_gt
    (max 1 (quittingMeshIntensity attempt.quitWeight / η))
  have hmeshOne : 1 < (mesh : ℝ) :=
    (le_max_left 1 _).trans_lt hmesh
  have hmeshPosReal : 0 < (mesh : ℝ) := zero_lt_one.trans hmeshOne
  have hmeshPos : 0 < mesh := by exact_mod_cast hmeshPosReal
  have hratio : quittingMeshIntensity attempt.quitWeight / (mesh : ℝ) < η := by
    have hquotient :
        quittingMeshIntensity attempt.quitWeight / η < (mesh : ℝ) :=
      (le_max_right 1 _).trans_lt hmesh
    have hintermediate :
        quittingMeshIntensity attempt.quitWeight < (mesh : ℝ) * η :=
      (div_lt_iff₀ hη).mp hquotient
    exact (div_lt_iff₀ hmeshPosReal).mpr (by
      simpa only [mul_comm] using hintermediate)
  refine ⟨mesh, hmeshPos, ?_, ?_⟩
  · exact (quittingMeshHazard_le_intensity_div
      attempt.quitWeight_lt_one).trans_lt hratio
  · exact one_sub_quittingMeshHazard_pow
      attempt.quitWeight_lt_one.le hmeshPos

/-! The continuation value with `remaining` independent stages left in a
mesh implementation of an attempt. Survival leads to the branch endpoint;
the first Quit leads to the owner's singleton column. -/
def BuildingAttempt.remainingValue
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    {block : BuildingBlock M y ε} {owner : ι}
    (attempt : BuildingAttempt M y ε block owner)
    (mesh remaining : ℕ) (who : ι) : ℝ :=
  let survival :=
    (1 - quittingMeshHazard attempt.quitWeight mesh) ^ remaining
  survival *
      (match attempt.continuation with
      | .restart => block.w who
      | .advance => y who) +
    (1 - survival) * M who owner

@[simp]
theorem BuildingAttempt.remainingValue_zero
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    {block : BuildingBlock M y ε} {owner : ι}
    (attempt : BuildingAttempt M y ε block owner)
    (mesh : ℕ) (who : ι) :
    attempt.remainingValue mesh 0 who =
      match attempt.continuation with
      | .restart => block.w who
      | .advance => y who := by
  simp [BuildingAttempt.remainingValue]

theorem BuildingAttempt.remainingValue_succ
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    {block : BuildingBlock M y ε} {owner : ι}
    (attempt : BuildingAttempt M y ε block owner)
    (mesh remaining : ℕ) (who : ι) :
    attempt.remainingValue mesh (remaining + 1) who =
      quittingMeshHazard attempt.quitWeight mesh * M who owner +
        (1 - quittingMeshHazard attempt.quitWeight mesh) *
          attempt.remainingValue mesh remaining who := by
  simp only [BuildingAttempt.remainingValue, pow_succ]
  ring

theorem BuildingAttempt.remainingValue_mesh
    {M : ι → ι → ℝ} {y : ι → ℝ} {ε : ℝ}
    {block : BuildingBlock M y ε} {owner : ι}
    (attempt : BuildingAttempt M y ε block owner)
    (mesh : ℕ)
    (hsurvival :
      (1 - quittingMeshHazard attempt.quitWeight mesh) ^ mesh =
        1 - attempt.quitWeight)
    (who : ι) :
    attempt.remainingValue mesh mesh who = block.wi owner who := by
  rw [BuildingAttempt.remainingValue, hsurvival, attempt.payoff who]
  ring

/-! The existential conclusion of Theorem 3.3. -/
def Theorem33Conclusion (M : ι → ι → ℝ) (y : ι → ℝ) (ε : ℝ) : Prop :=
  Nonempty (BuildingBlock M y ε)

private theorem subfamilyHull_weights
    (M : ι → ι → ℝ) (J : Finset ι) {y : ι → ℝ}
    (hy : y ∈ SubfamilyHull M J) :
    ∃ weight : ι → ℝ, (∀ i, 0 ≤ weight i) ∧
      (∑ i, weight i) = 1 ∧
      (∀ i, 0 < weight i → i ∈ J) ∧
      (∀ who, ∑ i, weight i * M who i = y who) := by
  rw [SubfamilyHull, mem_convexHull_iff_exists_fintype] at hy
  obtain ⟨κ, hκ, a, point, ha, hatotal, hpoint, hsum⟩ := hy
  letI : Fintype κ := hκ
  choose owner howner using hpoint
  let weight : ι → ℝ := fun i =>
    ∑ k, if (owner k : ι) = i then a k else 0
  refine ⟨weight, ?_, ?_, ?_, ?_⟩
  · intro i
    exact Finset.sum_nonneg fun k _ => by split <;> simp_all
  · simp only [weight]
    calc
      ∑ i, ∑ k, (if (owner k : ι) = i then a k else 0) =
          ∑ k, ∑ i, (if (owner k : ι) = i then a k else 0) := Finset.sum_comm
      _ = ∑ k, a k := by
        apply Finset.sum_congr rfl
        intro k _
        exact Fintype.sum_ite_eq (owner k : ι) (fun _ => a k)
      _ = 1 := hatotal
  · intro i hi
    by_contra hnot
    have hne (k : κ) : (owner k : ι) ≠ i := fun heq =>
      hnot (heq ▸ (owner k).property)
    simp [weight, hne] at hi
  · intro who
    have happly := congr_fun hsum who
    simp only [Finset.sum_apply, Pi.smul_apply, smul_eq_mul] at happly
    simp only [weight, Finset.sum_mul]
    calc
      ∑ i, ∑ k, (if (owner k : ι) = i then a k else 0) * M who i =
          ∑ k, ∑ i, (if (owner k : ι) = i then a k else 0) * M who i :=
        Finset.sum_comm
      _ = ∑ k, a k * M who (owner k) := by
        apply Finset.sum_congr rfl
        intro k _
        simpa only [ite_mul, zero_mul] using
          (Fintype.sum_ite_eq (owner k : ι) (fun i => a k * M who i))
      _ = y who := by
        rw [← happly]
        apply Finset.sum_congr rfl
        intro k _
        rw [← howner k]

private theorem column_ne_of_no_nontrivial_zero_solution
    (M : ι → ι → ℝ) {y : ι → ℝ} (hy : y ∈ D M)
    (hdiag : ∀ i, M i i = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M) (owner : ι) :
    (fun who => M who owner) ≠ y := by
  intro heq
  apply hzero
  let solution : ProjectiveLCPSolution M (0 : ι → ℝ) :=
    { cemetery := 0
      singleton := fun i => if i = owner then 1 else 0
      cemetery_nonneg := le_rfl
      singleton_nonneg := fun i => by split <;> simp_all
      total := by simp
      residual_nonneg := fun who => by
        simp only [zero_mul, zero_add, ite_mul, one_mul, zero_mul,
          Fintype.sum_ite_eq']
        rw [congr_fun heq who]
        exact hy.2 who
      complementary := fun who => by
        by_cases hwho : who = owner
        · subst who
          simp [hdiag]
        · simp [hwho] }
  exact ⟨solution, by simp [solution]⟩

private theorem column_not_mem_D_of_no_nontrivial_zero_solution
    (M : ι → ι → ℝ) (hdiag : ∀ i, M i i = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M) (owner : ι) :
    (fun who => M who owner) ∉ D M := by
  intro hcolumn
  apply hzero
  let solution : ProjectiveLCPSolution M (0 : ι → ℝ) :=
    { cemetery := 0
      singleton := fun i => if i = owner then 1 else 0
      cemetery_nonneg := le_rfl
      singleton_nonneg := fun i => by split <;> simp_all
      total := by simp
      residual_nonneg := fun who => by
        simp only [zero_mul, zero_add, ite_mul, one_mul, zero_mul,
          Fintype.sum_ite_eq']
        exact hcolumn.2 who
      complementary := fun who => by
        by_cases hwho : who = owner
        · subst who
          simp [hdiag]
        · simp [hwho] }
  exact ⟨solution, by simp [solution]⟩

/-! The first case in the proof of Theorem 3.3: `y` already lies in the
convex hull of the columns whose own coordinate vanishes. -/
theorem theorem33Conclusion_of_mem_subfamilyHull
    (M : ι → ι → ℝ) {y : ι → ℝ} (hy : DZero M y)
    (hbound : MatrixPayoffsBounded M) (hdiag : ∀ i, M i i = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (hmember : y ∈ SubfamilyHull M (ZeroCoordinates y))
    {ε : ℝ} (hε : 0 < ε) : Theorem33Conclusion M y ε := by
  let δ := min ε 1
  have hδpos : 0 < δ := lt_min hε zero_lt_one
  have hδnonneg : 0 ≤ δ := hδpos.le
  have hδε : δ ≤ ε := min_le_left _ _
  have hδone : δ ≤ 1 := min_le_right _ _
  have hyD : y ∈ D M := mem_D_of_DZero hy
  obtain ⟨weight, hweight, htotal, hsupport, haverage⟩ :=
    subfamilyHull_weights M (ZeroCoordinates y) hmember
  let wi : ι → ι → ℝ := fun i who =>
    (1 - δ) * y who + δ * M who i
  let z : SimplexWeights ι :=
    { cemetery := 0
      singleton := weight
      cemetery_nonneg := le_rfl
      singleton_nonneg := hweight
      total := by simpa using htotal }
  refine ⟨{
    w := y
    wi := wi
    z := z
    y_boundary := hy
    w_boundary := hy
    approach := ?_
    lower := ?_
    balance := ?_
    complementary := ?_
    nontrivial := ?_ }⟩
  · intro i
    left
    constructor
    · refine ⟨1 - δ, sub_nonneg.mpr hδone, by linarith, ?_⟩
      intro who
      simp only [wi]
      ring
    · intro hwi
      apply column_ne_of_no_nontrivial_zero_solution M hyD hdiag hzero i
      funext who
      have hcoordinate := congr_fun hwi who
      simp only [wi] at hcoordinate
      nlinarith
  · intro i who
    have hMlower : -1 ≤ M who i := (abs_le.mp (hbound who i)).1
    have hscaled : -δ ≤ δ * M who i := by
      nlinarith [mul_nonneg hδnonneg (show 0 ≤ M who i + 1 by linarith)]
    have hy_nonneg := hyD.2 who
    have hfactor : 0 ≤ 1 - δ := sub_nonneg.mpr hδone
    dsimp only [wi]
    nlinarith [mul_nonneg hfactor hy_nonneg]
  · intro who
    simp only [z, zero_mul, zero_add, wi]
    symm
    calc
      ∑ i, weight i * ((1 - δ) * y who + δ * M who i) =
          (1 - δ) * y who * (∑ i, weight i) +
            δ * (∑ i, weight i * M who i) := by
        simp_rw [mul_add]
        rw [Finset.sum_add_distrib]
        congr 1
        · rw [← Finset.sum_mul]
          ring
        · rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro i _
          ring
      _ = y who := by rw [htotal, haverage]; ring
  · intro i hi
    have hmem := hsupport i hi
    rw [ZeroCoordinates, Finset.mem_filter] at hmem
    simp only [wi, hmem.2, hdiag i]
    ring
  · simpa only [z, htotal] using (zero_lt_one : (0 : ℝ) < 1)

/-! The first subcase of Lemma 3.4: a segment from `y` to a zero-coordinate
column reaches a second boundary point of `D`. -/
theorem theorem33Conclusion_of_segment_boundary
    (M : ι → ι → ℝ) {y w : ι → ℝ}
    (hy : DZero M y)
    (hbound : MatrixPayoffsBounded M) (hdiag : ∀ i, M i i = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (i : ι) (hyi : y i = 0) (hw : DZero M w)
    (hsegment : Segment y (fun who => M who i) w) (hwy : w ≠ y)
    {ε : ℝ} (hε : 0 < ε) : Theorem33Conclusion M y ε := by
  obtain ⟨a, ha0, ha1, haw⟩ := hsegment
  let δ := 1 - a
  have hδ0 : 0 ≤ δ := sub_nonneg.mpr ha1
  have hδpos : 0 < δ := by
    apply lt_of_le_of_ne hδ0
    intro hzeroδ
    have haeq : a = 1 := by
      dsimp only [δ] at hzeroδ
      linarith
    apply hwy
    funext who
    rw [haw, haeq]
    ring
  have hδone : δ ≤ 1 := by dsimp only [δ]; linarith
  have hwD : w ∈ D M := mem_D_of_DZero hw
  have hcolumn_ne : (fun who => M who i) ≠ w := by
    exact column_ne_of_no_nontrivial_zero_solution M hwD hdiag hzero i
  have hδlt : δ < 1 := by
    apply lt_of_le_of_ne hδone
    intro hδeq
    apply hcolumn_ne
    have haeq : a = 0 := by
      dsimp only [δ] at hδeq
      linarith
    funext who
    rw [haw, haeq]
    ring
  have hw_affine (who : ι) :
      w who = (1 - δ) * y who + δ * M who i := by
    have := haw who
    dsimp only [δ]
    nlinarith
  let η := min ε 1
  have hηpos : 0 < η := lt_min hε zero_lt_one
  have hη0 : 0 ≤ η := hηpos.le
  have hηε : η ≤ ε := min_le_left _ _
  have hηone : η ≤ 1 := min_le_right _ _
  let denominator := η * (1 - δ) + δ
  have hdenominator : 0 < denominator := by
    dsimp only [denominator]
    have : 0 ≤ η * (1 - δ) :=
      mul_nonneg hη0 (sub_nonneg.mpr hδone)
    linarith
  let wi : ι → ι → ℝ := fun owner who =>
    (1 - η) * w who + η * M who owner
  let z : SimplexWeights ι :=
    { cemetery := η * (1 - δ) / denominator
      singleton := fun owner => if owner = i then δ / denominator else 0
      cemetery_nonneg := div_nonneg
        (mul_nonneg hη0 (sub_nonneg.mpr hδone)) hdenominator.le
      singleton_nonneg := fun owner => by
        split
        · exact div_nonneg hδ0 hdenominator.le
        · exact le_rfl
      total := by
        simp only [Fintype.sum_ite_eq', denominator]
        field_simp [show η * (1 - δ) + δ ≠ 0 by positivity] }
  refine ⟨{
    w := w
    wi := wi
    z := z
    y_boundary := hy
    w_boundary := hw
    approach := ?_
    lower := ?_
    balance := ?_
    complementary := ?_
    nontrivial := ?_ }⟩
  · intro owner
    left
    constructor
    · refine ⟨1 - η, sub_nonneg.mpr hηone, by linarith, ?_⟩
      intro who
      simp only [wi]
      ring
    · intro hwi
      apply column_ne_of_no_nontrivial_zero_solution M hwD hdiag hzero owner
      funext who
      have hcoordinate := congr_fun hwi who
      simp only [wi] at hcoordinate
      nlinarith
  · intro owner who
    have hMlower : -1 ≤ M who owner :=
      (abs_le.mp (hbound who owner)).1
    have hscaled : -η ≤ η * M who owner := by
      nlinarith [mul_nonneg hη0 (show 0 ≤ M who owner + 1 by linarith)]
    have hw_nonneg := hwD.2 who
    have hfactor : 0 ≤ 1 - η := sub_nonneg.mpr hηone
    dsimp only [wi]
    nlinarith [mul_nonneg hfactor hw_nonneg]
  · intro who
    change w who = η * (1 - δ) / denominator * y who +
      ∑ owner, (if owner = i then δ / denominator else 0) * wi owner who
    have hsum :
        (∑ owner, (if owner = i then δ / denominator else 0) * wi owner who) =
          δ / denominator * wi i who := by
      simpa only [ite_mul, zero_mul] using
        (Fintype.sum_ite_eq' i fun owner => δ / denominator * wi owner who)
    rw [hsum]
    simp only [wi]
    rw [hw_affine]
    field_simp [ne_of_gt hdenominator]
    dsimp only [denominator]
    ring
  · intro owner howner
    by_cases hoi : owner = i
    · subst owner
      have hwi : w i = 0 := by rw [hw_affine, hyi, hdiag]; ring
      simp only [wi, hwi, hdiag]
      ring
    · simp [z, hoi] at howner
  · simp only [z, Fintype.sum_ite_eq', denominator]
    exact div_pos hδpos hdenominator

/-! The published second subcase of Lemma 3.5. Here `w` is a boundary
point of the small slice `(1-η)y+ηS(J_y)`. Its extreme points approach
the singleton columns from `y`, which is the second alternative in the
published condition (F.1). -/
theorem theorem33Conclusion_of_slice_boundary
    (M : ι → ι → ℝ) {y w : ι → ℝ}
    (hy : DZero M y) (hw : DZero M w)
    (hbound : MatrixPayoffsBounded M) (hdiag : ∀ i, M i i = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    {η ε : ℝ} (hηpos : 0 < η) (hηone : η ≤ 1) (hηε : η ≤ ε)
    (hwSlice : w ∈ SubfamilyHull
      (fun who owner => (1 - η) * y who + η * M who owner)
      (ZeroCoordinates y)) :
    Theorem33Conclusion M y ε := by
  have hyD : y ∈ D M := mem_D_of_DZero hy
  obtain ⟨weight, hweight, htotal, hsupport, haverage⟩ :=
    subfamilyHull_weights
      (fun who owner => (1 - η) * y who + η * M who owner)
      (ZeroCoordinates y) hwSlice
  let wi : ι → ι → ℝ := fun owner who =>
    (1 - η) * y who + η * M who owner
  let z : SimplexWeights ι :=
    { cemetery := 0
      singleton := weight
      cemetery_nonneg := le_rfl
      singleton_nonneg := hweight
      total := by simpa using htotal }
  refine ⟨{
    w := w
    wi := wi
    z := z
    y_boundary := hy
    w_boundary := hw
    approach := ?_
    lower := ?_
    balance := ?_
    complementary := ?_
    nontrivial := ?_ }⟩
  · intro owner
    right
    constructor
    · refine ⟨1 - η, sub_nonneg.mpr hηone, by linarith, ?_⟩
      intro who
      simp only [wi]
      ring
    · intro hwi
      apply column_ne_of_no_nontrivial_zero_solution M hyD hdiag hzero owner
      funext who
      have hcoordinate := congr_fun hwi who
      simp only [wi] at hcoordinate
      nlinarith
  · intro owner who
    have hMlower : -1 ≤ M who owner :=
      (abs_le.mp (hbound who owner)).1
    have hscaled : -η ≤ η * M who owner := by
      nlinarith [mul_nonneg hηpos.le (show 0 ≤ M who owner + 1 by linarith)]
    have hy_nonneg := hyD.2 who
    have hfactor : 0 ≤ 1 - η := sub_nonneg.mpr hηone
    dsimp only [wi]
    nlinarith [mul_nonneg hfactor hy_nonneg]
  · intro who
    simp only [z, zero_mul, zero_add, wi]
    exact (haverage who).symm
  · intro owner howner
    have hmem := hsupport owner howner
    rw [ZeroCoordinates, Finset.mem_filter] at hmem
    simp only [wi, hmem.2, hdiag owner]
    ring
  · simpa only [z, htotal] using (zero_lt_one : (0 : ℝ) < 1)

/-! Rescaling a nontrivial projective complementarity packet gives the
first construction in the published Lemma 3.4. -/
theorem theorem33Conclusion_of_projective_packet
    (M : ι → ι → ℝ) {y w : ι → ℝ}
    (hy : DZero M y) (hw : DZero M w)
    (hbound : MatrixPayoffsBounded M)
    (hdiag : ∀ i, M i i = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (cemetery : ℝ) (singleton : ι → ℝ)
    (hcemetery : 0 ≤ cemetery) (hsingleton : ∀ i, 0 ≤ singleton i)
    (htotal : cemetery + ∑ i, singleton i = 1)
    (hbalance : ∀ who, w who = cemetery * y who +
      ∑ i, singleton i * M who i)
    (hcomplementary : ∀ i, singleton i > 0 → w i = 0)
    (hnontrivial : 0 < ∑ i, singleton i)
    {ε : ℝ} (hε : 0 < ε) : Theorem33Conclusion M y ε := by
  let η := min ε 1
  have hηpos : 0 < η := lt_min hε zero_lt_one
  have hηnonneg : 0 ≤ η := hηpos.le
  have hηε : η ≤ ε := min_le_left _ _
  have hηone : η ≤ 1 := min_le_right _ _
  let mass := ∑ i, singleton i
  let denominator := η * cemetery + mass
  have hdenominator : 0 < denominator := by
    dsimp only [denominator]
    exact add_pos_of_nonneg_of_pos (mul_nonneg hηnonneg hcemetery) hnontrivial
  let wi : ι → ι → ℝ := fun owner who =>
    (1 - η) * w who + η * M who owner
  let z : SimplexWeights ι :=
    { cemetery := η * cemetery / denominator
      singleton := fun owner => singleton owner / denominator
      cemetery_nonneg := div_nonneg (mul_nonneg hηnonneg hcemetery)
        hdenominator.le
      singleton_nonneg := fun owner =>
        div_nonneg (hsingleton owner) hdenominator.le
      total := by
        change η * cemetery / denominator +
          ∑ owner, singleton owner / denominator = 1
        rw [← Finset.sum_div]
        field_simp [ne_of_gt hdenominator]
        rfl }
  refine ⟨{
    w := w
    wi := wi
    z := z
    y_boundary := hy
    w_boundary := hw
    approach := ?_
    lower := ?_
    balance := ?_
    complementary := ?_
    nontrivial := ?_ }⟩
  · intro owner
    left
    constructor
    · refine ⟨1 - η, sub_nonneg.mpr hηone, by linarith, ?_⟩
      intro who
      simp only [wi]
      ring
    · intro hwi
      have hcolumn : (fun who => M who owner) = w := by
        funext who
        have hcoordinate := congr_fun hwi who
        simp only [wi] at hcoordinate
        nlinarith
      exact column_not_mem_D_of_no_nontrivial_zero_solution M hdiag
        hzero owner (hcolumn ▸ hw.1)
  · intro owner who
    have hMlower : -1 ≤ M who owner :=
      (abs_le.mp (hbound who owner)).1
    have hscaled : -η ≤ η * M who owner := by
      nlinarith [mul_nonneg hηnonneg (show 0 ≤ M who owner + 1 by linarith)]
    have hwNonnegative := hw.1.2 who
    have hfactor : 0 ≤ 1 - η := sub_nonneg.mpr hηone
    dsimp only [wi]
    nlinarith [mul_nonneg hfactor hwNonnegative]
  · intro who
    change w who = η * cemetery / denominator * y who +
      ∑ owner, singleton owner / denominator * wi owner who
    simp_rw [div_mul_eq_mul_div]
    rw [← Finset.sum_div]
    simp_rw [wi, mul_add]
    rw [Finset.sum_add_distrib, ← Finset.sum_mul]
    have hweighted : ∑ owner, singleton owner * (η * M who owner) =
        η * (∑ owner, singleton owner * M who owner) := by
      rw [Finset.mul_sum]
      apply Finset.sum_congr rfl
      intro owner _
      ring
    rw [hweighted]
    field_simp [ne_of_gt hdenominator]
    rw [hbalance]
    dsimp only [denominator, mass]
    have hmass : ∑ i, singleton i = 1 - cemetery := by
      linarith [htotal]
    rw [hmass]
    ring
  · intro owner howner
    have hsinglePos : 0 < singleton owner := by
      rcases div_pos_iff.mp howner with hpositive | hnegative
      · exact hpositive.1
      · exact False.elim ((not_lt_of_ge hdenominator.le) hnegative.2)
    have hwzero := hcomplementary owner hsinglePos
    simp only [wi, hwzero, hdiag owner]
    ring
  · change 0 < ∑ owner, singleton owner / denominator
    rw [← Finset.sum_div]
    exact div_pos hnontrivial hdenominator

/-! **Lemmas 3.3 and 3.4 (paper).** Put `J_y={i:y_i=0}` and
`S(J_y)=conv{r̂^i:i∈J_y}`. In the two geometric cases
`conv(S(J_y),y)∩D={y}` and `conv(S(J_y),y)∩D ⊋ {y}`, respectively, the
paper proves the same `Theorem33Conclusion` represented above. -/

/-! `conv(S(J),y)`, the convex hull after adjoining `y`. -/
def AugmentedHull (M : ι → ι → ℝ) (J : Finset ι)
    (y : ι → ℝ) : Set (ι → ℝ) :=
  convexHull ℝ (SubfamilyHull M J ∪ {y})

omit [Fintype ι] [DecidableEq ι] in
private theorem affine_slice_mem_subfamilyHull
    (M : ι → ι → ℝ) (J : Finset ι) {y s : ι → ℝ}
    (η : ℝ) (hs : s ∈ SubfamilyHull M J) :
    (fun who => (1 - η) * y who + η * s who) ∈
      SubfamilyHull
        (fun who owner => (1 - η) * y who + η * M who owner) J := by
  let slice : (ι → ℝ) →ᵃ[ℝ] (ι → ℝ) :=
    AffineMap.mk (fun x => (1 - η) • y + η • x)
      (η • LinearMap.id)
      (by
        intro p v
        ext who
        simp
        ring)
  have himage : slice ''
      (Set.range fun i : J => fun who => M who i) =
      Set.range (fun i : J =>
        fun who => (1 - η) * y who + η * M who i) := by
    ext x
    constructor
    · rintro ⟨_, ⟨i, rfl⟩, rfl⟩
      refine ⟨i, ?_⟩
      ext who
      simp [slice, smul_eq_mul]
    · rintro ⟨i, rfl⟩
      refine ⟨fun who => M who i, ⟨i, rfl⟩, ?_⟩
      ext who
      simp [slice, smul_eq_mul]
  have hsImage : slice s ∈ slice ''
      convexHull ℝ (Set.range fun i : J => fun who => M who i) :=
    ⟨s, hs, rfl⟩
  rw [slice.image_convexHull, himage] at hsImage
  change (fun who => (1 - η) * y who + η * s who) ∈
    convexHull ℝ (Set.range fun i : J =>
      fun who => (1 - η) * y who + η * M who i)
  convert hsImage using 1
  ext who
  simp [slice, smul_eq_mul]

omit [Fintype ι] [DecidableEq ι] in
private theorem mem_augmentedHull
    (M : ι → ι → ℝ) (J : Finset ι) (hJ : J.Nonempty)
    {y q : ι → ℝ} (hq : q ∈ AugmentedHull M J y) :
    ∃ s ∈ SubfamilyHull M J, q ∈ segment ℝ y s := by
  have hS : (SubfamilyHull M J).Nonempty := by
    obtain ⟨i, hi⟩ := hJ
    exact ⟨fun who => M who i, subset_convexHull ℝ _ ⟨⟨i, hi⟩, rfl⟩⟩
  have hconvexS : Convex ℝ (SubfamilyHull M J) :=
    convex_convexHull ℝ _
  have heq : convexHull ℝ (SubfamilyHull M J ∪ {y}) =
      convexJoin ℝ (SubfamilyHull M J) {y} :=
    hconvexS.convexHull_union (convex_singleton y)
      hS (Set.singleton_nonempty y)
  rw [AugmentedHull, heq, mem_convexJoin] at hq
  obtain ⟨s, hs, b, hb, hsegment⟩ := hq
  have hb' : b = y := Set.mem_singleton_iff.mp hb
  subst b
  exact ⟨s, hs, segment_symm ℝ s y ▸ hsegment⟩

omit [Fintype ι] [DecidableEq ι] in
private theorem endpoint_not_mem_augmentedHull
    {C : Set (ι → ℝ)} (hC : Convex ℝ C) (hCnonempty : C.Nonempty)
    {y s q : ι → ℝ} (hy : y ∉ C) (hs : s ∈ C)
    (hq : Segment y s q) (hqy : q ≠ y) :
    y ∉ convexHull ℝ (C ∪ {q}) := by
  intro hyHull
  have heq : convexHull ℝ (C ∪ {q}) = convexJoin ℝ C {q} :=
    hC.convexHull_union (convex_singleton q) hCnonempty
      (Set.singleton_nonempty q)
  rw [heq, mem_convexJoin] at hyHull
  obtain ⟨c, hc, q', hq', hySegment⟩ := hyHull
  have hq'eq : q' = q := Set.mem_singleton_iff.mp hq'
  subst q'
  obtain ⟨b, hb0, hb1, hqb⟩ := hq
  have hbne : b ≠ 1 := by
    intro hb
    apply hqy
    funext who
    rw [hqb who, hb]
    ring
  obtain ⟨a, ha0, ha1, hya⟩ :=
    segment_of_mem_segment hySegment
  let denominator := a + (1 - a) * (1 - b)
  have hdenominator : 0 < denominator := by
    dsimp only [denominator]
    have hbone : 0 < 1 - b := lt_of_le_of_ne
      (sub_nonneg.mpr hb1) (Ne.symm (sub_ne_zero.mpr hbne.symm))
    by_cases hapos : 0 < a
    · positivity
    · have haeq : a = 0 := le_antisymm (le_of_not_gt hapos) ha0
      rw [haeq]
      simpa using hbone
  have hySegmentCS : Segment c s y := by
    refine ⟨a / denominator, div_nonneg ha0 hdenominator.le, ?_, ?_⟩
    · rw [div_le_one hdenominator]
      dsimp only [denominator]
      exact le_add_of_nonneg_right
        (mul_nonneg (sub_nonneg.mpr ha1) (sub_nonneg.mpr hb1))
    · intro who
      field_simp [ne_of_gt hdenominator]
      dsimp only [denominator]
      linear_combination hya who + (1 - a) * (hqb who)
  exact hy <| hC.segment_subset hc hs (mem_segment_of_Segment hySegmentCS)

/-! Lemma 3.4: the singleton-intersection case yields (F.1)--(F.5). -/
theorem lemma3_4
    (M : ι → ι → ℝ) {y : ι → ℝ} (hy : DZero M y)
    (hbound : MatrixPayoffsBounded M) (hdiag : ∀ i, M i i = 0) (hzero :
      ¬HasNontrivialZeroProjectiveLCPSolution M) (hQ : QMatrix M)
    (hnot : y ∉ SubfamilyHull M (ZeroCoordinates y))
    (hpoint : AugmentedHull M (ZeroCoordinates y) y ∩
      D M = {y}) {ε : ℝ} (hε : 0 < ε) :
    Theorem33Conclusion M y ε := by
  let J := ZeroCoordinates y
  let C := SubfamilyHull M J
  obtain ⟨pivot, hpivotZero⟩ := hy.2
  have hpivot : pivot ∈ J := by
    simp only [J, ZeroCoordinates, Finset.mem_filter, Finset.mem_univ,
      true_and]
    exact hpivotZero
  have hCconvex : Convex ℝ C := convex_convexHull ℝ _
  have hCnonempty : C.Nonempty := by
    exact ⟨fun who => M who pivot,
      subset_convexHull ℝ _ ⟨⟨pivot, hpivot⟩, rfl⟩⟩
  let δ : ℕ → ℝ := fun n => 1 / ((n : ℝ) + 1)
  let q : ℕ → ι → ℝ := fun n who =>
    y who + δ n * (M who pivot - y who)
  have hδpos (n : ℕ) : 0 < δ n := by
    dsimp only [δ]
    positivity
  have hδone (n : ℕ) : δ n ≤ 1 := by
    dsimp only [δ]
    rw [div_le_one (by positivity : (0 : ℝ) < (n : ℝ) + 1)]
    norm_num
  have hqSegment (n : ℕ) :
      Segment y (fun who => M who pivot) (q n) := by
    refine ⟨1 - δ n, sub_nonneg.mpr (hδone n), by linarith [hδpos n], ?_⟩
    intro who
    simp only [q]
    ring
  have hqNe (n : ℕ) : q n ≠ y := by
    intro hqy
    have hcolumnNe := column_ne_of_no_nontrivial_zero_solution
      M hy.1 hdiag hzero pivot
    apply hcolumnNe
    funext who
    have hcoordinate := congr_fun hqy who
    simp only [q] at hcoordinate
    nlinarith [hδpos n]
  have hqHull (n : ℕ) : q n ∈ ColumnConvexHull M :=
    (convex_convexHull ℝ _).segment_subset hy.1.1
      (subset_convexHull ℝ _ ⟨pivot, rfl⟩)
      (mem_segment_of_Segment (hqSegment n))
  have hqAugmented (n : ℕ) : q n ∈ AugmentedHull M J y := by
    have hcolumnC : (fun who => M who pivot) ∈ C := by
      exact subset_convexHull ℝ _ ⟨⟨pivot, hpivot⟩, rfl⟩
    apply (convex_convexHull ℝ _).segment_subset
      (subset_convexHull ℝ _ <| Set.mem_union_right _ (Set.mem_singleton y))
      (subset_convexHull ℝ _ <| Set.mem_union_left _ <|
        (show (fun who => M who pivot) ∈ SubfamilyHull M J from hcolumnC))
    exact mem_segment_of_Segment (hqSegment n)
  have hqNegative (n : ℕ) : q n ∉ NonnegativeOrthant ι := by
    intro hnonnegative
    have hqD : q n ∈ D M := ⟨hqHull n, hnonnegative⟩
    have hsingleton : q n ∈ ({y} : Set (ι → ℝ)) := by
      rw [← hpoint]
      exact ⟨by simpa only [J] using hqAugmented n, hqD⟩
    exact hqNe n (Set.mem_singleton_iff.mp hsingleton)
  let solution (n : ℕ) : ProjectiveLCPSolution M (q n) :=
    Classical.choice (hQ (q n))
  let residual (n : ℕ) : ι → ℝ := fun who =>
    (solution n).cemetery * q n who +
      ∑ owner, (solution n).singleton owner * M who owner
  have hresidualDZero (n : ℕ) : DZero M (residual n) :=
    lemma3_2 M (q n) (hqHull n) (hqNegative n) (solution n)
  let coefficient (n : ℕ) : Option ι → ℝ
    | none => (solution n).cemetery
    | some owner => (solution n).singleton owner
  have hcoefficient (n : ℕ) : coefficient n ∈
      Set.univ.pi (fun _ : Option ι => Set.Icc (0 : ℝ) 1) := by
    intro index _
    constructor
    · cases index with
      | none => exact (solution n).cemetery_nonneg
      | some owner => exact (solution n).singleton_nonneg owner
    · cases index with
      | none =>
          have hsum : 0 ≤ ∑ owner, (solution n).singleton owner :=
            Finset.sum_nonneg fun owner _ =>
              (solution n).singleton_nonneg owner
          linarith [(solution n).total]
      | some owner =>
          have hsingleSum : (solution n).singleton owner ≤
              ∑ i, (solution n).singleton i :=
            Finset.single_le_sum
              (fun i _ => (solution n).singleton_nonneg i)
              (Finset.mem_univ owner)
          have hsum : ∑ i, (solution n).singleton i ≤ 1 := by
            linarith [(solution n).total, (solution n).cemetery_nonneg]
          exact hsingleSum.trans hsum
  have hcubeCompact : IsCompact
      (Set.univ.pi (fun _ : Option ι => Set.Icc (0 : ℝ) 1)) :=
    isCompact_univ_pi fun _ => isCompact_Icc
  obtain ⟨limit, hlimitCube, subseq, hsubseq, hlimit⟩ :=
    hcubeCompact.tendsto_subseq hcoefficient
  let cemetery := limit none
  let singleton : ι → ℝ := fun owner => limit (some owner)
  have hcoefficientLimit (index : Option ι) :
      Tendsto (fun n => coefficient (subseq n) index) atTop
        (nhds (limit index)) :=
    ((continuous_apply index).tendsto limit).comp hlimit
  have hδTendsto : Tendsto δ atTop (nhds 0) := by
    simpa only [δ, one_div] using
      (tendsto_one_div_add_atTop_nhds_zero_nat (𝕜 := ℝ))
  have hqTendsto : Tendsto q atTop (nhds y) := by
    rw [tendsto_pi_nhds]
    intro who
    simpa only [q, zero_mul, add_zero] using
      tendsto_const_nhds.add (hδTendsto.mul_const (M who pivot - y who))
  have hqSubseq : Tendsto (q ∘ subseq) atTop (nhds y) :=
    hqTendsto.comp hsubseq.tendsto_atTop
  let w : ι → ℝ := fun who => cemetery * y who +
    ∑ owner, singleton owner * M who owner
  have hresidualTendsto : Tendsto (residual ∘ subseq) atTop (nhds w) := by
    rw [tendsto_pi_nhds]
    intro who
    have hcemetery := hcoefficientLimit none
    have hqWho : Tendsto (fun n => q (subseq n) who) atTop (nhds (y who)) :=
      (continuous_apply who).tendsto y |>.comp hqSubseq
    have hsum := tendsto_finsetSum Finset.univ fun owner _ =>
      (hcoefficientLimit (some owner)).mul_const (M who owner)
    simpa only [residual, Function.comp_apply, coefficient, w, cemetery,
      singleton] using hcemetery.mul hqWho |>.add hsum
  have hcemeteryNonneg : 0 ≤ cemetery := hlimitCube none (Set.mem_univ _)|>.1
  have hsingletonNonneg (owner : ι) : 0 ≤ singleton owner :=
    hlimitCube (some owner) (Set.mem_univ _)|>.1
  have htotal : cemetery + ∑ owner, singleton owner = 1 := by
    have htendsto : Tendsto
        (fun n => coefficient (subseq n) none +
          ∑ owner, coefficient (subseq n) (some owner)) atTop
        (nhds (cemetery + ∑ owner, singleton owner)) := by
      exact (hcoefficientLimit none).add <|
        tendsto_finsetSum Finset.univ fun owner _ =>
          hcoefficientLimit (some owner)
    have hone : Tendsto
        (fun n => coefficient (subseq n) none +
          ∑ owner, coefficient (subseq n) (some owner)) atTop
        (nhds 1) := by
      simpa only [coefficient, (solution _).total] using
        (tendsto_const_nhds : Tendsto (fun _ : ℕ => (1 : ℝ)) atTop (nhds 1))
    exact tendsto_nhds_unique htendsto hone
  have hcomplementary (owner : ι) :
      singleton owner * w owner = 0 := by
    have htendsto := (hcoefficientLimit (some owner)).mul <|
      (continuous_apply owner).tendsto w |>.comp hresidualTendsto
    have hzero : Tendsto
        (fun n => coefficient (subseq n) (some owner) *
          residual (subseq n) owner) atTop (nhds 0) := by
      have heq : (fun n => coefficient (subseq n) (some owner) *
          residual (subseq n) owner) = fun _ : ℕ => (0 : ℝ) := by
        funext n
        exact (solution (subseq n)).complementary owner
      rw [heq]
      exact tendsto_const_nhds
    exact tendsto_nhds_unique htendsto hzero
  have hwD : w ∈ D M := (isClosed_D M).mem_of_tendsto
    hresidualTendsto (Filter.Eventually.of_forall fun n =>
      (hresidualDZero (subseq n)).1)
  have hwZero : ∃ owner, w owner = 0 := by
    have hprodTendsto := tendsto_finsetProd Finset.univ fun owner _ =>
      (continuous_apply owner).tendsto w |>.comp hresidualTendsto
    have hprodZero : Tendsto
        (fun n => ∏ owner, residual (subseq n) owner) atTop (nhds 0) := by
      have heq : (fun n => ∏ owner, residual (subseq n) owner) =
          fun _ : ℕ => (0 : ℝ) := by
        funext n
        obtain ⟨owner, howner⟩ := (hresidualDZero (subseq n)).2
        exact Finset.prod_eq_zero (Finset.mem_univ owner) howner
      rw [heq]
      exact tendsto_const_nhds
    have hprod : ∏ owner, w owner = 0 :=
      tendsto_nhds_unique hprodTendsto hprodZero
    obtain ⟨owner, _, howner⟩ := Finset.prod_eq_zero_iff.mp hprod
    exact ⟨owner, howner⟩
  have hw : DZero M w := ⟨hwD, hwZero⟩
  have hwy : w ≠ y := by
    intro hwyeq
    have heventuallyPositive : ∀ᶠ n in atTop,
        ∀ owner ∈ Finset.univ, owner ∉ J →
          0 < residual (subseq n) owner := by
      rw [Finset.eventually_all]
      intro owner _
      by_cases howner : owner ∈ J
      · exact Filter.Eventually.of_forall fun _ hnotOwner =>
          False.elim (hnotOwner howner)
      · have hyowner : 0 < y owner := by
          have hyNonnegative := hy.1.2 owner
          have hyne : y owner ≠ 0 := by
            simpa only [J, ZeroCoordinates, Finset.mem_filter,
              Finset.mem_univ, true_and] using howner
          exact lt_of_le_of_ne hyNonnegative hyne.symm
        have htendsto : Tendsto (fun n => residual (subseq n) owner)
            atTop (nhds (y owner)) := by
          have ht :=
            (continuous_apply owner).tendsto w |>.comp hresidualTendsto
          change Tendsto (fun n => residual (subseq n) owner) atTop
            (nhds (w owner)) at ht
          simpa only [hwyeq] using ht
        filter_upwards [htendsto.eventually_const_lt hyowner] with n hn
        exact fun _ => hn
    obtain ⟨n, hn⟩ := heventuallyPositive.exists
    have hsingleOutside (owner : ι) (howner : owner ∉ J) :
        (solution (subseq n)).singleton owner = 0 := by
      have hcomp := (solution (subseq n)).complementary owner
      change (solution (subseq n)).singleton owner *
        residual (subseq n) owner = 0 at hcomp
      exact (mul_eq_zero.mp hcomp).resolve_right
        (ne_of_gt (hn owner (Finset.mem_univ owner) howner))
    let point : Option ι → (ι → ℝ)
      | none => q (subseq n)
      | some owner => if owner ∈ J then (fun who => M who owner)
        else q (subseq n)
    have hpointMem (index : Option ι) : point index ∈
        C ∪ {q (subseq n)} := by
      cases index with
      | none => exact Set.mem_union_right _ (Set.mem_singleton _)
      | some owner =>
          by_cases howner : owner ∈ J
          · change (if owner ∈ J then (fun who => M who owner)
                else q (subseq n)) ∈ C ∪ {q (subseq n)}
            rw [if_pos howner]
            apply Set.mem_union_left
            exact subset_convexHull ℝ _ ⟨⟨owner, howner⟩, rfl⟩
          · exact Set.mem_union_right _ (by simp [point, howner])
    have hpacketHull : residual (subseq n) ∈
        convexHull ℝ (C ∪ {q (subseq n)}) := by
      have hsum := (convex_convexHull ℝ _).sum_mem
        (t := Finset.univ) (w := fun index => coefficient (subseq n) index)
        (z := point)
        (fun index _ => by
          cases index with
          | none => exact (solution (subseq n)).cemetery_nonneg
          | some owner => exact (solution (subseq n)).singleton_nonneg owner)
        (by simpa only [coefficient, Fintype.sum_option] using
          (solution (subseq n)).total)
        (fun index _ => subset_convexHull ℝ _ (hpointMem index))
      convert hsum using 1
      funext who
      simp only [residual]
      rw [Fintype.sum_option]
      simp only [coefficient, point]
      congr 1
      rw [Finset.sum_apply]
      simp only [Pi.smul_apply, smul_eq_mul]
      apply Finset.sum_congr rfl
      intro owner _
      by_cases howner : owner ∈ J
      · simp [howner]
      · simp [howner, hsingleOutside owner howner]
    have hnotEndpoint := endpoint_not_mem_augmentedHull hCconvex
      hCnonempty (by simpa only [C, J] using hnot)
      (show (fun who => M who pivot) ∈ C from
        subset_convexHull ℝ _ ⟨⟨pivot, hpivot⟩, rfl⟩)
      (hqSegment (subseq n)) (hqNe (subseq n))
    have hpacketAugmented : residual (subseq n) ∈
        AugmentedHull M J y := by
      apply (convexHull_min ?_ (convex_convexHull ℝ _)) hpacketHull
      intro x hx
      rcases hx with hx | hx
      · exact subset_convexHull ℝ _ (Set.mem_union_left _ hx)
      · rw [Set.mem_singleton_iff] at hx
        subst x
        exact hqAugmented (subseq n)
    have hresidualEq : residual (subseq n) = y := by
      apply Set.mem_singleton_iff.mp
      rw [← hpoint]
      exact ⟨by simpa only [J] using hpacketAugmented,
        (hresidualDZero (subseq n)).1⟩
    exact hnotEndpoint (hresidualEq ▸ hpacketHull)
  have hnontrivial : 0 < ∑ owner, singleton owner := by
    have hsumNonnegative : 0 ≤ ∑ owner, singleton owner :=
      Finset.sum_nonneg fun owner _ => hsingletonNonneg owner
    apply lt_of_le_of_ne hsumNonnegative
    intro hsumZero
    have hcemeteryOne : cemetery = 1 := by linarith [htotal]
    have hsingleZero (owner : ι) : singleton owner = 0 :=
      (Finset.sum_eq_zero_iff_of_nonneg (s := Finset.univ)
        (fun i _ => hsingletonNonneg i)).mp hsumZero.symm owner
        (Finset.mem_univ owner)
    apply hwy
    funext who
    simp [w, hcemeteryOne, hsingleZero]
  apply theorem33Conclusion_of_projective_packet M hy hw hbound hdiag hzero
    cemetery singleton hcemeteryNonneg hsingletonNonneg htotal
  · exact fun who => rfl
  · intro owner howner
    exact (mul_eq_zero.mp (hcomplementary owner)).resolve_left
      (ne_of_gt howner)
  · exact hnontrivial
  · exact hε

/-! Lemma 3.5: the strict-intersection case yields (F.1)--(F.5). -/
theorem lemma3_5
    (M : ι → ι → ℝ) {y : ι → ℝ} (hy : DZero M y)
    (hbound : MatrixPayoffsBounded M) (hdiag : ∀ i, M i i = 0) (hzero :
      ¬HasNontrivialZeroProjectiveLCPSolution M) (_hQ : QMatrix M)
    (_hnot : y ∉ SubfamilyHull M (ZeroCoordinates y))
    (hstrict : AugmentedHull M (ZeroCoordinates y) y ∩
      D M ≠ {y}) {ε : ℝ} (hε : 0 < ε) :
    Theorem33Conclusion M y ε := by
  have hyD : y ∈ D M := mem_D_of_DZero hy
  have hyAugmented : y ∈ AugmentedHull M (ZeroCoordinates y) y := by
    apply subset_convexHull ℝ
    exact Set.mem_union_right _ (Set.mem_singleton y)
  have hyIntersection : y ∈
      AugmentedHull M (ZeroCoordinates y) y ∩ D M :=
    ⟨hyAugmented, hyD⟩
  obtain ⟨q, hqIntersection, hqy⟩ :
      ∃ q ∈ AugmentedHull M (ZeroCoordinates y) y ∩ D M, q ≠ y := by
    by_contra hnone
    push Not at hnone
    apply hstrict
    apply Set.Subset.antisymm
    · intro x hx
      rw [Set.mem_singleton_iff]
      exact hnone x hx
    · intro x hx
      rw [Set.mem_singleton_iff] at hx
      simpa only [hx] using hyIntersection
  have hJ : (ZeroCoordinates y).Nonempty := by
    by_contra hempty
    rw [Finset.not_nonempty_iff_eq_empty] at hempty
    letI : IsEmpty (ZeroCoordinates y : Type) :=
      ⟨fun owner => by simpa [hempty] using owner.property⟩
    have hrange : (Set.range fun i : ZeroCoordinates y =>
        fun who => M who i) = ∅ :=
      Set.range_eq_empty_iff.mpr inferInstance
    have hSempty : SubfamilyHull M (ZeroCoordinates y) = ∅ := by
      simp only [SubfamilyHull, hrange, convexHull_empty]
    have hqy' : q = y := by
      have := hqIntersection.1
      simp [AugmentedHull, hSempty] at this
      exact this
    exact hqy hqy'
  obtain ⟨s, hs, hqSegment⟩ :=
    mem_augmentedHull M (ZeroCoordinates y) hJ hqIntersection.1
  by_cases hsegmentCase :
      ∃ owner ∈ ZeroCoordinates y,
        ∃ p ∈ segment ℝ y (fun who => M who owner), p ∈ D M ∧ p ≠ y
  · obtain ⟨owner, howner, p, hpSegment, hpD, hpy⟩ := hsegmentCase
    have hcolumnNotD :=
      column_not_mem_D_of_no_nontrivial_zero_solution M hdiag hzero owner
    have hcolumnHull : (fun who => M who owner) ∈ ColumnConvexHull M :=
      subset_convexHull ℝ _ ⟨owner, rfl⟩
    have hcolumnNotOrthant :
        (fun who => M who owner) ∉ NonnegativeOrthant ι := by
      intro hnonnegative
      exact hcolumnNotD ⟨hcolumnHull, hnonnegative⟩
    obtain ⟨w, hwTail, hwFrontier⟩ :=
      exists_frontier_on_segment_of_mem_notMem hpD.2 hcolumnNotOrthant
    obtain ⟨hwNonnegative, hwZero⟩ :=
      frontier_nonnegativeOrthant_has_zero hwFrontier
    have hwHull : w ∈ ColumnConvexHull M :=
      (convex_convexHull ℝ _).segment_subset hpD.1 hcolumnHull hwTail
    have hwDZero : DZero M w := ⟨⟨hwHull, hwNonnegative⟩, hwZero⟩
    have hwFull : w ∈ segment ℝ y (fun who => M who owner) :=
      (convex_segment y (fun who => M who owner)).segment_subset
        hpSegment (right_mem_segment ℝ y (fun who => M who owner)) hwTail
    have hcolumnNe : (fun who => M who owner) ≠ y := by
      intro heq
      exact hcolumnNotD (heq ▸ hyD)
    have hwy : w ≠ y := by
      intro hwy
      exact endpoint_not_mem_tail_segment hpSegment hpy hcolumnNe (hwy ▸ hwTail)
    have hyowner : y owner = 0 := by
      simpa [ZeroCoordinates] using howner
    exact theorem33Conclusion_of_segment_boundary M hy hbound hdiag hzero
      owner hyowner hwDZero (segment_of_mem_segment hwFull) hwy hε
  · push Not at hsegmentCase
    obtain ⟨a, ha0, ha1, hq⟩ := segment_of_mem_segment hqSegment
    let ρ := 1 - a
    have hρpos : 0 < ρ := by
      have hane : a ≠ 1 := by
        intro ha
        apply hqy
        funext who
        rw [hq who, ha]
        ring
      dsimp only [ρ]
      exact lt_of_le_of_ne (sub_nonneg.mpr ha1)
        (Ne.symm (sub_ne_zero.mpr hane.symm))
    have hqAffine (who : ι) :
        q who = (1 - ρ) * y who + ρ * s who := by
      rw [hq who]
      dsimp only [ρ]
      ring
    let η := min ε (min ρ 1)
    have hηpos : 0 < η := lt_min hε (lt_min hρpos zero_lt_one)
    have hηε : η ≤ ε := min_le_left _ _
    have hηρ : η ≤ ρ := (min_le_right _ _).trans (min_le_left _ _)
    have hηone : η ≤ 1 := (min_le_right _ _).trans (min_le_right _ _)
    let inside : ι → ℝ := fun who =>
      (1 - η) * y who + η * s who
    have hinsideSegment : inside ∈ segment ℝ y q := by
      rw [segment_eq_image_lineMap]
      refine ⟨η / ρ, ⟨div_nonneg hηpos.le hρpos.le,
        (div_le_one hρpos).mpr hηρ⟩, ?_⟩
      ext who
      simp only [inside, AffineMap.lineMap_apply_module, Pi.add_apply,
        Pi.smul_apply, smul_eq_mul]
      rw [hqAffine]
      field_simp [ne_of_gt hρpos]
      ring
    have hinsideD : inside ∈ D M :=
      (convex_D M).segment_subset hyD hqIntersection.2 hinsideSegment
    obtain ⟨owner, howner⟩ := hJ
    let extreme : ι → ℝ := fun who =>
      (1 - η) * y who + η * M who owner
    have hextremeSegment : extreme ∈
        segment ℝ y (fun who => M who owner) := by
      apply mem_segment_of_Segment
      refine ⟨1 - η, sub_nonneg.mpr hηone, by linarith, ?_⟩
      intro who
      simp only [extreme]
      ring
    have hextremeNe : extreme ≠ y := by
      intro heq
      have hcolumnNe := column_ne_of_no_nontrivial_zero_solution
        M hyD hdiag hzero owner
      apply hcolumnNe
      funext who
      have hcoordinate := congr_fun heq who
      simp only [extreme] at hcoordinate
      nlinarith
    have hextremeNotD : extreme ∉ D M := by
      intro hextremeD
      exact hextremeNe <|
        hsegmentCase owner howner extreme hextremeSegment hextremeD
    have hextremeHull : extreme ∈ ColumnConvexHull M :=
      (convex_convexHull ℝ _).segment_subset hyD.1
        (subset_convexHull ℝ _ ⟨owner, rfl⟩) hextremeSegment
    have hextremeNotOrthant : extreme ∉ NonnegativeOrthant ι := by
      intro hnonnegative
      exact hextremeNotD ⟨hextremeHull, hnonnegative⟩
    obtain ⟨w, hwSegment, hwFrontier⟩ :=
      exists_frontier_on_segment_of_mem_notMem hinsideD.2
        hextremeNotOrthant
    obtain ⟨hwNonnegative, hwZero⟩ :=
      frontier_nonnegativeOrthant_has_zero hwFrontier
    have hwHull : w ∈ ColumnConvexHull M :=
      (convex_convexHull ℝ _).segment_subset hinsideD.1
        hextremeHull hwSegment
    have hwDZero : DZero M w := ⟨⟨hwHull, hwNonnegative⟩, hwZero⟩
    have hinsideSlice : inside ∈ SubfamilyHull
        (fun who owner => (1 - η) * y who + η * M who owner)
        (ZeroCoordinates y) :=
      affine_slice_mem_subfamilyHull M (ZeroCoordinates y) η hs
    have hextremeSlice : extreme ∈ SubfamilyHull
        (fun who owner => (1 - η) * y who + η * M who owner)
        (ZeroCoordinates y) := by
      apply subset_convexHull ℝ
      exact ⟨⟨owner, howner⟩, rfl⟩
    have hwSlice : w ∈ SubfamilyHull
        (fun who owner => (1 - η) * y who + η * M who owner)
        (ZeroCoordinates y) :=
      (convex_convexHull ℝ _).segment_subset
        hinsideSlice hextremeSlice hwSegment
    exact theorem33Conclusion_of_slice_boundary M hy hwDZero hbound
      hdiag hzero hηpos hηone hηε hwSlice

/-! Theorem 3.3, under Theorem 2.13(2)'s standing hypotheses: the derived
matrix has zero diagonal, is Q, and its homogeneous projective problem has no
nontrivial solution. Every `y∈D₀` and `ε>0` then admits a building block. -/
theorem theorem3_3
    (M : ι → ι → ℝ) {y : ι → ℝ} (hy : DZero M y)
    (hbound : MatrixPayoffsBounded M) (hdiag : ∀ i, M i i = 0) (hzero :
      ¬HasNontrivialZeroProjectiveLCPSolution M) (hQ : QMatrix M)
    {ε : ℝ} (hε : 0 < ε) : Theorem33Conclusion M y ε := by
  by_cases hmember : y ∈ SubfamilyHull M (ZeroCoordinates y)
  · exact theorem33Conclusion_of_mem_subfamilyHull M hy hbound hdiag
      hzero hmember hε
  · by_cases hintersection :
        AugmentedHull M (ZeroCoordinates y) y ∩ D M = {y}
    · exact lemma3_4 M hy hbound hdiag hzero hQ hmember hintersection hε
    · exact lemma3_5 M hy hbound hdiag hzero hQ hmember hintersection hε

/-! The source chooses one Theorem 3.3 block at every point of `D₀` and
then applies Theorem 3.6 to the resulting endpoint map `y ↦ w(y)`. -/
noncomputable def selectedBuildingBlock
    (M : ι → ι → ℝ) (hbound : MatrixPayoffsBounded M)
    (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (hQ : QMatrix M) {ε : ℝ} (hε : 0 < ε)
    (y : {value // DZero M value}) : BuildingBlock M y.1 ε :=
  Classical.choice <| theorem3_3 M y.2 hbound hdiag hzero hQ hε

/-! The selected endpoint map `w : D₀ → D₀`. -/
noncomputable def buildingBlockMap
    (M : ι → ι → ℝ) (hbound : MatrixPayoffsBounded M)
    (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (hQ : QMatrix M) {ε : ℝ} (hε : 0 < ε) :
    {value // DZero M value} → {value // DZero M value} :=
  fun y =>
    ⟨(selectedBuildingBlock M hbound hdiag hzero hQ hε y).w,
      (selectedBuildingBlock M hbound hdiag hzero hQ hε y).w_boundary⟩

theorem buildingBlockMap_ne
    (M : ι → ι → ℝ) (hbound : MatrixPayoffsBounded M)
    (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (hQ : QMatrix M) {ε : ℝ} (hε : 0 < ε)
    (hnegative : ∀ owner, ∃ who, M who owner < -ε)
    (y : {value // DZero M value}) :
    buildingBlockMap M hbound hdiag hzero hQ hε y ≠ y := by
  intro heq
  have hvalue := congrArg Subtype.val heq
  exact (selectedBuildingBlock M hbound hdiag hzero hQ hε y).w_ne_y_of_column_negative
    hdiag hnegative hzero hvalue

/-! **Theorem 3.6 (paper, corrected endpoint).** If `(X,d)` is a nonempty
complete metric space and `f:X→X` has no fixed point, then for every `c>0`
and `C≥0` there are `K` and `x¹,…,xᴷ` with
`Σ_k d(xᵏ,f(xᵏ))>C` and `Σ_{k<K}d(xᵏ⁺¹,f(xᵏ))<c`. The paper proof uses
transfinite recursion. The printed `c≥0` cannot include `c=0`, because the
second conclusion is then a strict inequality between a nonnegative sum and
zero. -/

/-! The displacement along a finite list `x¹,…,xᵏ`. -/
def PathDisplacement {X : Type*} [PseudoMetricSpace X]
    (f : X → X) : List X → ℝ
  | [] => 0
  | x :: xs => dist x (f x) + PathDisplacement f xs

/-! The tracking error of a finite list whose first point is intended to
follow `anchor`; subsequent points are intended to follow their predecessor
under `f`. -/
def PathTrackingFrom {X : Type*} [PseudoMetricSpace X]
    (f : X → X) (anchor : X) : List X → ℝ
  | [] => 0
  | x :: xs => dist x anchor + PathTrackingFrom f (f x) xs

/-! Turn the head and tail of a finite path into its canonical finite-index
presentation. -/
def pathPoint {X : Type*} (x : X) (xs : List X) :
    Fin (xs.length + 1) → X :=
  Fin.cases x xs.get

theorem pathPoint_castSucc_eq_get_cons
    {X : Type*} (x : X) (xs : List X) (k : Fin xs.length) :
    pathPoint x xs ⟨k.1, by omega⟩ =
      (x :: xs).get ⟨k.1, by simp only [List.length_cons]; omega⟩ := by
  let k' : Fin (xs.length + 1) := ⟨k.1, by omega⟩
  change Fin.cases x xs.get k' = (x :: xs).get k'
  refine Fin.cases ?_ (fun j => ?_) k'
  · simp
  · rw [Fin.cases_succ]
    symm
    exact List.get_cons_succ

private theorem pathDisplacement_eq_sum_get
    {X : Type*} [PseudoMetricSpace X] (f : X → X) (xs : List X) :
    PathDisplacement f xs =
      ∑ k, dist (xs.get k) (f (xs.get k)) := by
  induction xs with
  | nil => simp [PathDisplacement]
  | cons y ys ih =>
      simp only [List.length_cons]
      rw [PathDisplacement, Fin.sum_univ_succ]
      simp only [List.get_cons_zero]
      rw [ih]
      congr 1

theorem pathDisplacement_eq_sum_pathPoint
    {X : Type*} [PseudoMetricSpace X] (f : X → X)
    (x : X) (xs : List X) :
    dist x (f x) + PathDisplacement f xs =
      ∑ k, dist (pathPoint x xs k) (f (pathPoint x xs k)) := by
  rw [Fin.sum_univ_succ]
  simp only [pathPoint, Fin.cases_zero, Fin.cases_succ]
  rw [← pathDisplacement_eq_sum_get]

theorem pathTrackingFrom_eq_sum_pathPoint
    {X : Type*} [PseudoMetricSpace X] (f : X → X)
    (x : X) (xs : List X) :
    PathTrackingFrom f (f x) xs =
      ∑ k : Fin xs.length,
        dist (pathPoint x xs ⟨k.1 + 1, by omega⟩)
          (f (pathPoint x xs ⟨k.1, by omega⟩)) := by
  induction xs generalizing x with
  | nil => simp [PathTrackingFrom]
  | cons y ys ih =>
      rw [PathTrackingFrom, ih]
      simp only [List.length_cons]
      rw [Fin.sum_univ_succ]
      congr 1
      simp [pathPoint]
      apply Finset.sum_congr rfl
      intro k _
      congr 2
      exact pathPoint_castSucc_eq_get_cons y ys k

theorem pathTrackingFrom_nonneg {X : Type*} [PseudoMetricSpace X]
    (f : X → X) (anchor : X) (xs : List X) :
    0 ≤ PathTrackingFrom f anchor xs := by
  induction xs generalizing anchor with
  | nil => simp [PathTrackingFrom]
  | cons x xs ih =>
      simp only [PathTrackingFrom]
      exact add_nonneg dist_nonneg (ih (f x))

/-! The finite-sequence conclusion of Theorem 3.6. The head is `x¹`; the
list contains `x²,…,xᵏ`, so its tracking error starts at `f(x¹)`. -/
def ApproximationWitness {X : Type*} [PseudoMetricSpace X]
    (f : X → X) (c C : ℝ) : Prop :=
  ∃ x : X, ∃ xs : List X,
    C < dist x (f x) + PathDisplacement f xs ∧
    PathTrackingFrom f (f x) xs < c

/-! Supremum of the displacement attainable from `anchor` while spending
less than half of the tracking budget. This is the Caristi potential in the
short proof of Theorem 3.6. -/
def RemainingDisplacement {X : Type*} [PseudoMetricSpace X]
    (f : X → X) (c : ℝ) (anchor : X) : ℝ :=
  sSup {value | ∃ xs : List X,
    PathTrackingFrom f anchor xs < c / 2 ∧
    value = PathDisplacement f xs}

/-! Theorem 3.6: a fixed-point-free map admits large displacement with small
tracking error on a finite sequence. -/
theorem theorem3_6 {X : Type*} [MetricSpace X] [Nonempty X]
    [CompleteSpace X] (f : X → X) (hfixed : ∀ x, f x ≠ x)
    (c C : ℝ) (hc : 0 < c) (hC : 0 ≤ C) :
    ApproximationWitness f c C := by
  by_contra hno
  have hpathBound {x : X} {xs : List X}
      (htracking : PathTrackingFrom f (f x) xs < c) :
      dist x (f x) + PathDisplacement f xs ≤ C := by
    by_contra hnot
    exact hno ⟨x, xs, lt_of_not_ge hnot, htracking⟩
  have hset_nonempty (anchor : X) :
      {value | ∃ xs : List X,
        PathTrackingFrom f anchor xs < c / 2 ∧
        value = PathDisplacement f xs}.Nonempty := by
    refine ⟨0, [], ?_, rfl⟩
    simp [PathTrackingFrom, hc]
  have hset_bdd (anchor : X) : BddAbove
      {value | ∃ xs : List X,
        PathTrackingFrom f anchor xs < c / 2 ∧
        value = PathDisplacement f xs} := by
    refine ⟨C, ?_⟩
    rintro value ⟨xs, htracking, rfl⟩
    cases xs with
    | nil => simpa [PathDisplacement] using hC
    | cons x xs =>
        apply hpathBound
        have htail := pathTrackingFrom_nonneg f (f x) xs
        have hdistance : 0 ≤ dist x anchor := dist_nonneg
        simp only [PathTrackingFrom] at htracking
        linarith
  let φ : X → ℝ := RemainingDisplacement f c
  have hφ_nonneg (anchor : X) : 0 ≤ φ anchor := by
    apply le_csSup (hset_bdd anchor)
    exact ⟨[], by simp [PathTrackingFrom, hc], rfl⟩
  have hφ_bdd : BddBelow (Set.range φ) := by
    exact ⟨0, by rintro _ ⟨anchor, rfl⟩; exact hφ_nonneg anchor⟩
  have hφ_lsc : LowerSemicontinuous φ := by
    rw [lowerSemicontinuous_iff]
    intro anchor y hy
    dsimp [φ, RemainingDisplacement] at hy ⊢
    obtain ⟨value, hvalue, hyvalue⟩ := exists_lt_of_lt_csSup
      (hset_nonempty anchor) hy
    obtain ⟨xs, htracking, rfl⟩ := hvalue
    have hcontinuous : ContinuousAt
        (fun nextAnchor => PathTrackingFrom f nextAnchor xs) anchor := by
      cases xs with
      | nil => simpa [PathTrackingFrom] using continuousAt_const
      | cons x xs =>
          simp only [PathTrackingFrom]
          fun_prop
    filter_upwards [hcontinuous.eventually (gt_mem_nhds htracking)]
      with nextAnchor hnext
    exact hyvalue.trans_le
      (le_csSup (hset_bdd nextAnchor) ⟨xs, hnext, rfl⟩)
  have hcaristi (x : X) : dist x (f x) ≤ φ x - φ (f x) := by
    have hsuple : φ (f x) ≤ φ x - dist x (f x) := by
      apply csSup_le (hset_nonempty (f x))
      intro value hvalue
      obtain ⟨xs, htracking, rfl⟩ := hvalue
      have hmem : dist x (f x) + PathDisplacement f xs ∈
          {value | ∃ ys : List X,
            PathTrackingFrom f x ys < c / 2 ∧
            value = PathDisplacement f ys} := by
        refine ⟨x :: xs, ?_, ?_⟩
        · simpa [PathTrackingFrom] using htracking
        · simp [PathDisplacement]
      have hupper := le_csSup (hset_bdd x) hmem
      dsimp [φ, RemainingDisplacement]
      linarith
    linarith
  obtain ⟨x, hx⟩ := MathUE.exists_fixedPoint_of_caristi
    f φ hφ_lsc hφ_bdd hcaristi
  exact hfixed x hx

/-! Applying Theorem 3.6 to the selected endpoint map gives the finite
boundary-point chain used to assemble the paper's kiloblocks. -/
theorem exists_buildingBlock_approximationWitness [Nonempty ι]
    (M : ι → ι → ℝ) (hbound : MatrixPayoffsBounded M)
    (hdiag : ∀ who, M who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution M)
    (hQ : QMatrix M) {ε : ℝ} (hε : 0 < ε)
    (hnegative : ∀ owner, ∃ who, M who owner < -ε)
    (c C : ℝ) (hc : 0 < c) (hC : 0 ≤ C) :
    ApproximationWitness
      (buildingBlockMap M hbound hdiag hzero hQ hε) c C := by
  letI : Nonempty {value // DZero M value} := by
    obtain ⟨value, hvalue⟩ := exists_mem_DZero M hdiag hzero hQ
    exact ⟨⟨value, hvalue⟩⟩
  letI : CompleteSpace {value // DZero M value} :=
    (isClosed_DZero M).completeSpace_coe
  exact theorem3_6
    (buildingBlockMap M hbound hdiag hzero hQ hε)
    (buildingBlockMap_ne M hbound hdiag hzero hQ hε hnegative)
    c C hc hC

/-! **Lemmas 3.8--3.10 (paper).** For the kiloblock strategy `ξ*` built from
Theorem 3.3 and Theorem 3.6, the paper proves (i) its payoff is within
`2ε` of `w(yᴷ)` for normal players, (ii) continuation by any normal player
still terminates with probability at least `1-ε` before the final kiloblock,
and (iii) every pure deviation gains at most `5ε`. -/

/-! A kiloblock construction carries the actual public-signal behavior
profile, its finite chain of Theorem 3.3 blocks, and the history event that the
final kiloblock has not started. It carries no asserted payoff or deviation
bound. -/

/-! The control state between live decisions. `choose k` means that the next
signal selects a type in kiloblock `k`; `resume` ignores the next signal and
continues a previously selected type. The source's order is reversed: the
construction starts at the last index and ends after index zero. -/
inductive KiloblockPhase (table : Table ι) (lastIndex : ℕ)
  | choose (k : Fin (lastIndex + 1))
  | resume (k : Fin (lastIndex + 1))
      (choice : Option (NormalPlayer table)) (remaining : ℕ)
  | final

/-! The schedule state at an actual history. Draw and active states are
separated because the public-signal adapter draws before every decision. -/
inductive KiloblockMode (table : Table ι) (lastIndex : ℕ)
  | draw (phase : KiloblockPhase table lastIndex)
  | active (k : Fin (lastIndex + 1))
      (choice : Option (NormalPlayer table)) (remaining : ℕ)
  | finalActive
  | absorbed (origin : Option (Fin (lastIndex + 1)))

/-! Move from original index `k` to `k-1`, or to the final infinite tail
when `k=0`. -/
def precedingKiloblockPhase (table : Table ι) {lastIndex : ℕ}
    (k : Fin (lastIndex + 1)) : KiloblockPhase table lastIndex :=
  if hk : k.1 = 0 then .final else .choose ⟨k.1 - 1, by omega⟩

/-! A type-zero draw always advances. A failed type-`i` attempt restarts the
same auxiliary game in the `w` branch of (F.1), but advances in its `y`
branch. The latter exit is implicit in Figures 1--2 and is omitted from the
later prose partition; recording it here is the necessary source correction. -/
def phaseAfterAttempt (table : Table ι) {lastIndex : ℕ}
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (k : Fin (lastIndex + 1)) (choice : Option (NormalPlayer table)) :
    KiloblockPhase table lastIndex :=
  match choice with
  | none => precedingKiloblockPhase table k
  | some owner =>
      match continuation k owner with
      | .restart => .choose k
      | .advance => precedingKiloblockPhase table k

/-! The schedule mode agrees with the actual public-game state. -/
def KiloblockMode.MatchesState
    {table : Table ι} {Signal : Type} [Fintype Signal] {lastIndex : ℕ}
    (mode : KiloblockMode table lastIndex)
    (state : PublicQuittingState ι Signal) : Prop :=
  match mode, state with
  | .draw _, .draw => True
  | .active _ _ _, .active _ => True
  | .finalActive, .active _ => True
  | .absorbed _, .absorbed _ => True
  | _, _ => False

/-! Exact one-step automaton of the accepted manuscript's kiloblock
schedule. A selected player is kept for `mesh k` active decisions. Failure
then follows the branch-sensitive exit rule above; type zero advances after
an all-Continue block of the same length. -/
def KiloblockModeStep
    {table : Table ι} {Signal : Type} [Fintype Signal] {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Signal →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (mode : KiloblockMode table lastIndex) (action : ι → Bool)
    (nextState : PublicQuittingState ι Signal)
    (nextMode : KiloblockMode table lastIndex) : Prop := by
  classical
  exact match mode with
  | .draw (.choose k) =>
      match nextState with
      | .active signal =>
          nextMode = .active k (selector k signal) (mesh k)
      | _ => False
  | .draw (.resume k choice remaining) =>
      match nextState with
      | .active _ => nextMode = .active k choice remaining
      | _ => False
  | .draw .final =>
      match nextState with
      | .active _ => nextMode = .finalActive
      | _ => False
  | .active k choice remaining =>
      if hquit : ({who | action who = true} : Finset ι).Nonempty then
        nextState = .absorbed ⟨_, hquit⟩ ∧ nextMode = .absorbed (some k)
      else if remaining ≤ 1 then
        nextState = .draw ∧
          nextMode = .draw
            (phaseAfterAttempt table continuation k choice)
      else
        nextState = .draw ∧
          nextMode = .draw (.resume k choice (remaining - 1))
  | .finalActive =>
      if hquit : ({who | action who = true} : Finset ι).Nonempty then
        nextState = .absorbed ⟨_, hquit⟩ ∧ nextMode = .absorbed none
      else nextState = .draw ∧ nextMode = .draw .final
  | .absorbed origin =>
      match nextState with
      | .absorbed _ => nextMode = .absorbed origin
      | _ => False

/-! The total displacement in (A.1''). -/
def KiloblockDisplacement {table : Table ι} {ε : ℝ} {lastIndex : ℕ}
    (point : Fin (lastIndex + 1) → NormalPlayer table → ℝ)
    (block : ∀ k, BuildingBlock (NormalMatrix table) (point k) ε) : ℝ :=
  ∑ k, dist (point k) (block k).w

/-! The adjacent mismatch in (A.2''):
`Σ_{k<K} ‖y^{k+1}-w(y^k)‖∞`. -/
def KiloblockTracking {table : Table ι} {ε : ℝ} {lastIndex : ℕ}
    (point : Fin (lastIndex + 1) → NormalPlayer table → ℝ)
    (block : ∀ k, BuildingBlock (NormalMatrix table) (point k) ε) : ℝ :=
  ∑ k : Fin lastIndex,
    dist (point ⟨k.1 + 1, by omega⟩) (block ⟨k.1, by omega⟩).w

/-! The displacement threshold `C` chosen in Section 3.3.5. -/
def KiloblockDisplacementThreshold (table : Table ι) (ε : ℝ) : ℝ :=
  ((Nat.choose (Fintype.card (NormalPlayer table)) 2 : ℕ) : ℝ) *
    (2 * (1 + ε)) / ε ^ 2

/-! The finite sequence furnished by Theorems 3.3 and 3.6, with the two
quantitative estimates (A.1'') and (A.2'') used by the kiloblock schedule. -/
structure KiloblockChain (table : Table ι) (ε : ℝ) where
  blockCount : ℕ
  point : Fin (blockCount + 1) → NormalPlayer table → ℝ
  point_boundary : ∀ k, DZero (NormalMatrix table) (point k)
  buildingBlock : ∀ k,
    BuildingBlock (NormalMatrix table) (point k) ε
  displacement_large : KiloblockDisplacementThreshold table ε <
    KiloblockDisplacement point buildingBlock
  tracking_small : KiloblockTracking point buildingBlock < ε

/-! Choose the paper's finite kiloblock chain from its fixed-point-free
building-block map. -/
theorem exists_kiloblockChain
    (table : Table ι) [Nonempty (NormalPlayer table)]
    (hbound : MatrixPayoffsBounded (NormalMatrix table))
    (hdiag : ∀ who, NormalMatrix table who who = 0)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table))
    (hQ : QMatrix (NormalMatrix table)) {ε : ℝ} (hε : 0 < ε)
    (hnegative : ∀ owner, ∃ who,
      NormalMatrix table who owner < -ε) :
    Nonempty (KiloblockChain table ε) := by
  let M := NormalMatrix table
  let f := buildingBlockMap M hbound hdiag hzero hQ hε
  have hthreshold : 0 ≤ KiloblockDisplacementThreshold table ε := by
    unfold KiloblockDisplacementThreshold
    positivity
  obtain ⟨x, xs, hdisplacement, htracking⟩ :=
    exists_buildingBlock_approximationWitness M hbound hdiag hzero hQ
      hε hnegative ε (KiloblockDisplacementThreshold table ε)
      hε hthreshold
  let boundaryPoint : Fin (xs.length + 1) →
      {value // DZero M value} := pathPoint x xs
  let point : Fin (xs.length + 1) → NormalPlayer table → ℝ :=
    fun k => (boundaryPoint k).1
  let block : ∀ k, BuildingBlock M (point k) ε :=
    fun k => selectedBuildingBlock M hbound hdiag hzero hQ hε
      (boundaryPoint k)
  refine ⟨{
    blockCount := xs.length
    point := point
    point_boundary := fun k => (boundaryPoint k).2
    buildingBlock := block
    displacement_large := ?_
    tracking_small := ?_ }⟩
  · rw [pathDisplacement_eq_sum_pathPoint] at hdisplacement
    simpa only [KiloblockDisplacement, point, block, boundaryPoint, f,
      buildingBlockMap, Subtype.dist_eq] using hdisplacement
  · rw [pathTrackingFrom_eq_sum_pathPoint] at htracking
    simpa only [KiloblockTracking, point, block, boundaryPoint, f,
      buildingBlockMap, Subtype.dist_eq] using htracking

/-! The internal accuracy is chosen below the requested `ε/7`, below one,
and below the dimension-scaled negative-column margin. -/
theorem exists_kiloblockInternalAccuracy
    (table : Table ι) [Nonempty (NormalPlayer table)]
    {requested margin : ℝ} (hrequested : 0 < requested)
    (hmargin : 0 < margin) :
    ∃ ε : ℝ, 0 < ε ∧ ε < 1 ∧
      ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ) * ε <
        margin ∧
      7 * ε ≤ requested := by
  let factor : ℝ :=
    ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ)
  have hfactor : 0 < factor := by
    dsimp only [factor]
    positivity
  let ε := min (requested / 7) (min (margin / (2 * factor)) (1 / 2))
  have hε : 0 < ε := by
    dsimp only [ε]
    exact lt_min (by positivity) (lt_min (by positivity) (by norm_num))
  have hrequestedBound : ε ≤ requested / 7 := by
    exact min_le_left _ _
  have hmarginBound : ε ≤ margin / (2 * factor) := by
    exact (min_le_right _ _).trans (min_le_left _ _)
  have hhalf : ε ≤ 1 / 2 := by
    exact (min_le_right _ _).trans (min_le_right _ _)
  have hscaled : factor * ε < margin := by
    have htwice : 2 * factor * ε ≤ margin := by
      have hbound := (le_div_iff₀
        (show 0 < 2 * factor by positivity)).mp hmarginBound
      nlinarith
    nlinarith
  refine ⟨ε, hε, by linarith, ?_, by linarith⟩
  simpa only [factor] using hscaled

/-! The quantitative data fixed before compiling the public-signal history
automaton: a chain, its attempts, and one sufficiently fine mesh per block. -/
structure KiloblockBlueprint (table : Table ι) (ε : ℝ) where
  soloExitNormalized : SoloExitNormalized table
  payoffsBounded : TablePayoffsBounded table
  chain : KiloblockChain table ε
  epsilon_pos : 0 < ε
  epsilon_lt_one : ε < 1
  negativeMargin : ℝ
  negativeMargin_pos : 0 < negativeMargin
  accuracy_below_margin :
    ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ) * ε <
      negativeMargin
  column_negative : ∀ owner,
    ∃ who, NormalMatrix table who owner < -negativeMargin
  attempt : ∀ k owner,
    BuildingAttempt (NormalMatrix table) (chain.point k) ε
      (chain.buildingBlock k) owner
  mesh : Fin (chain.blockCount + 1) → ℕ
  mesh_pos : ∀ k, 0 < mesh k
  mesh_hazard_small : ∀ k owner,
    quittingMeshHazard (attempt k owner).quitWeight (mesh k) < ε
  mesh_survival : ∀ k owner,
    (1 - quittingMeshHazard (attempt k owner).quitWeight (mesh k)) ^
        mesh k =
      1 - (attempt k owner).quitWeight

/-! Assemble all quantitative kiloblock data at an internal accuracy whose
eventual `7ε` equilibrium error is below the requested tolerance. -/
theorem exists_kiloblockBlueprint
    (table : Table ι)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (hnormal : HasNormalPlayers table)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table))
    (hQ : QMatrix (NormalMatrix table))
    {requested : ℝ} (hrequested : 0 < requested) :
    ∃ ε : ℝ, 7 * ε ≤ requested ∧
      Nonempty (KiloblockBlueprint table ε) := by
  letI : Nonempty (NormalPlayer table) := hnormal.to_subtype
  let M := NormalMatrix table
  have hmatrixBound : MatrixPayoffsBounded M :=
    normalMatrix_payoffsBounded table hbounded
  have hdiag : ∀ who, M who who = 0 :=
    normalMatrix_diagonal_zero table hnormalized
  obtain ⟨margin, hmargin, hcolumn⟩ :=
    exists_uniformNegativeMargin M hdiag hzero
  obtain ⟨ε, hε, hεone, hscaled, hrequestedBound⟩ :=
    exists_kiloblockInternalAccuracy table hrequested hmargin
  have hεmargin : ε < margin := by
    have hfactor : 1 ≤
        ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ) := by
      norm_num
    nlinarith [mul_le_mul_of_nonneg_right hfactor hε.le]
  have hcolumnε : ∀ owner, ∃ who, M who owner < -ε := by
    intro owner
    obtain ⟨who, hwho⟩ := hcolumn owner
    exact ⟨who, by linarith⟩
  obtain ⟨chain⟩ := exists_kiloblockChain table hmatrixBound hdiag
    hzero hQ hε hcolumnε
  let attempt : ∀ k owner,
      BuildingAttempt M (chain.point k) ε
        (chain.buildingBlock k) owner :=
    fun k owner => (chain.buildingBlock k).attempt owner (hcolumnε owner)
  let intensityBound : Fin (chain.blockCount + 1) → ℝ :=
    fun k => ∑ owner,
      quittingMeshIntensity (attempt k owner).quitWeight
  choose mesh hmesh using fun k : Fin (chain.blockCount + 1) =>
    exists_nat_gt (max 1 (intensityBound k / ε))
  have hmeshPos (k) : 0 < mesh k := by
    have hone : 1 < (mesh k : ℝ) :=
      (le_max_left 1 _).trans_lt (hmesh k)
    exact_mod_cast (zero_lt_one.trans hone)
  have hintensity (k) (owner) :
      quittingMeshIntensity (attempt k owner).quitWeight ≤
        intensityBound k := by
    dsimp only [intensityBound]
    exact Finset.single_le_sum
      (s := Finset.univ)
      (f := fun other =>
        quittingMeshIntensity (attempt k other).quitWeight)
      (fun other _ => quittingMeshIntensity_nonneg
        (attempt k other).quitWeight_pos.le
        (attempt k other).quitWeight_lt_one.le)
      (Finset.mem_univ owner)
  have hratio (k) : intensityBound k / (mesh k : ℝ) < ε := by
    have hmeshReal : 0 < (mesh k : ℝ) := by
      exact_mod_cast hmeshPos k
    have hquotient : intensityBound k / ε < (mesh k : ℝ) :=
      (le_max_right 1 _).trans_lt (hmesh k)
    have hintermediate : intensityBound k < (mesh k : ℝ) * ε :=
      (div_lt_iff₀ hε).mp hquotient
    exact (div_lt_iff₀ hmeshReal).mpr (by
      simpa only [mul_comm] using hintermediate)
  refine ⟨ε, hrequestedBound, ⟨{
    soloExitNormalized := hnormalized
    payoffsBounded := hbounded
    chain := chain
    epsilon_pos := hε
    epsilon_lt_one := hεone
    negativeMargin := margin
    negativeMargin_pos := hmargin
    accuracy_below_margin := hscaled
    column_negative := hcolumn
    attempt := attempt
    mesh := mesh
    mesh_pos := hmeshPos
    mesh_hazard_small := ?_
    mesh_survival := ?_ }⟩⟩
  · intro k owner
    exact (quittingMeshHazard_le_intensity_div
      (attempt k owner).quitWeight_lt_one).trans_lt <|
        (div_le_div_of_nonneg_right (hintensity k owner)
          (Nat.cast_nonneg (mesh k))).trans_lt (hratio k)
  · intro k owner
    exact one_sub_quittingMeshHazard_pow
      (attempt k owner).quitWeight_lt_one.le (hmeshPos k)

/-! A state-matching fallback only defines the compiler on histories that
cannot arise from the public game. It never changes an on-support transition. -/
def fallbackKiloblockMode
    {table : Table ι} {Signal : Type} {lastIndex : ℕ}
    (state : PublicQuittingState ι Signal) :
    KiloblockMode table lastIndex :=
  match state with
  | .draw => .draw .final
  | .active _ => .finalActive
  | .absorbed _ => .absorbed none

/-! Deterministic update of the kiloblock control state. -/
def nextKiloblockMode
    {table : Table ι} {Signal : Type} [Fintype Signal]
    {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Signal →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (mode : KiloblockMode table lastIndex) (action : ι → Bool)
    (nextState : PublicQuittingState ι Signal) :
    KiloblockMode table lastIndex := by
  classical
  exact match mode with
  | .draw (.choose k) =>
      match nextState with
      | .active signal => .active k (selector k signal) (mesh k)
      | state => fallbackKiloblockMode state
  | .draw (.resume k choice remaining) =>
      match nextState with
      | .active _ => .active k choice remaining
      | state => fallbackKiloblockMode state
  | .draw .final =>
      match nextState with
      | .active _ => .finalActive
      | state => fallbackKiloblockMode state
  | .active k choice remaining =>
      if hquit : ({who | action who = true} : Finset ι).Nonempty then
        match nextState with
        | .absorbed _ => .absorbed (some k)
        | state => fallbackKiloblockMode state
      else if remaining ≤ 1 then
        match nextState with
        | .draw => .draw (phaseAfterAttempt table continuation k choice)
        | state => fallbackKiloblockMode state
      else
        match nextState with
        | .draw => .draw (.resume k choice (remaining - 1))
        | state => fallbackKiloblockMode state
  | .finalActive =>
      if hquit : ({who | action who = true} : Finset ι).Nonempty then
        match nextState with
        | .absorbed _ => .absorbed none
        | state => fallbackKiloblockMode state
      else
        match nextState with
        | .draw => .draw .final
        | state => fallbackKiloblockMode state
  | .absorbed origin =>
      match nextState with
      | .absorbed _ => .absorbed origin
      | state => fallbackKiloblockMode state

theorem nextKiloblockMode_matchesState
    {table : Table ι} {Signal : Type} [Fintype Signal]
    {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Signal →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (mode : KiloblockMode table lastIndex) (action : ι → Bool)
    (nextState : PublicQuittingState ι Signal) :
    (nextKiloblockMode mesh selector continuation mode action nextState).MatchesState
      nextState := by
  classical
  cases mode with
  | draw phase =>
      cases phase <;> cases nextState <;>
        simp [nextKiloblockMode, fallbackKiloblockMode,
          KiloblockMode.MatchesState]
  | active k choice remaining =>
      by_cases hquit : ({who | action who = true} : Finset ι).Nonempty
      · cases nextState <;>
          simp [nextKiloblockMode, fallbackKiloblockMode,
            KiloblockMode.MatchesState, hquit]
      · by_cases hremaining : remaining ≤ 1
        · cases nextState <;>
            simp [nextKiloblockMode, fallbackKiloblockMode,
              KiloblockMode.MatchesState, hquit, hremaining]
        · cases nextState <;>
            simp [nextKiloblockMode, fallbackKiloblockMode,
              KiloblockMode.MatchesState, hquit, hremaining]
  | finalActive =>
      by_cases hquit : ({who | action who = true} : Finset ι).Nonempty
      · cases nextState <;>
          simp [nextKiloblockMode, fallbackKiloblockMode,
            KiloblockMode.MatchesState, hquit]
      · cases nextState <;>
          simp [nextKiloblockMode, fallbackKiloblockMode,
            KiloblockMode.MatchesState, hquit]
  | absorbed =>
      cases nextState <;>
        simp [nextKiloblockMode, fallbackKiloblockMode,
          KiloblockMode.MatchesState]

/-! Every active or pending-resume counter is positive and bounded by its
block's common mesh. -/
def KiloblockMode.ValidRemaining
    {table : Table ι} {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (mode : KiloblockMode table lastIndex) : Prop :=
  match mode with
  | .active k _ remaining => 0 < remaining ∧ remaining ≤ mesh k
  | .draw (.resume k _ remaining) =>
      0 < remaining ∧ remaining ≤ mesh k
  | _ => True

theorem precedingKiloblockPhase_validRemaining
    {table : Table ι} {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (k : Fin (lastIndex + 1)) :
    (KiloblockMode.draw
      (precedingKiloblockPhase table k)).ValidRemaining mesh := by
  unfold precedingKiloblockPhase
  split <;> trivial

theorem nextKiloblockMode_validRemaining
    {table : Table ι} {Signal : Type} [Fintype Signal]
    {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (hmesh : ∀ k, 0 < mesh k)
    (selector : Fin (lastIndex + 1) → Signal →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (mode : KiloblockMode table lastIndex) (action : ι → Bool)
    (nextState : PublicQuittingState ι Signal)
    (hvalid : mode.ValidRemaining mesh) :
    (nextKiloblockMode mesh selector continuation mode action nextState).ValidRemaining
      mesh := by
  classical
  cases mode with
  | draw phase =>
      cases phase <;> cases nextState <;>
        simp_all [KiloblockMode.ValidRemaining, nextKiloblockMode,
          fallbackKiloblockMode]
  | active k choice remaining =>
      by_cases hquit : ({who | action who = true} : Finset ι).Nonempty
      · cases nextState <;>
          simp [KiloblockMode.ValidRemaining, nextKiloblockMode,
            fallbackKiloblockMode, hquit]
      · by_cases hremaining : remaining ≤ 1
        · cases nextState with
          | draw =>
              simp only [nextKiloblockMode, hquit, ↓reduceDIte,
                hremaining, KiloblockMode.ValidRemaining]
              cases choice with
              | none =>
                  exact precedingKiloblockPhase_validRemaining
                    (table := table) mesh k
              | some owner =>
                  cases hcontinuation : continuation k owner with
                  | restart =>
                      simp [phaseAfterAttempt, hcontinuation]
                  | advance =>
                      simp only [if_pos True.intro, phaseAfterAttempt,
                        hcontinuation]
                      change (KiloblockMode.draw
                        (precedingKiloblockPhase table k)).ValidRemaining mesh
                      exact precedingKiloblockPhase_validRemaining mesh k
          | active =>
              simp [nextKiloblockMode, fallbackKiloblockMode, hquit,
                hremaining, KiloblockMode.ValidRemaining]
          | absorbed =>
              simp [nextKiloblockMode, fallbackKiloblockMode, hquit,
                KiloblockMode.ValidRemaining]
        · cases nextState with
          | draw =>
              simp only [nextKiloblockMode, hquit, ↓reduceDIte,
                hremaining, KiloblockMode.ValidRemaining]
              exact ⟨by omega, (Nat.sub_le remaining 1).trans hvalid.2⟩
          | active =>
              simp [nextKiloblockMode, fallbackKiloblockMode, hquit,
                hremaining, KiloblockMode.ValidRemaining]
          | absorbed =>
              simp [nextKiloblockMode, fallbackKiloblockMode, hquit,
                KiloblockMode.ValidRemaining]
  | finalActive =>
      by_cases hquit : ({who | action who = true} : Finset ι).Nonempty <;>
        cases nextState <;>
        simp_all [KiloblockMode.ValidRemaining, nextKiloblockMode,
          fallbackKiloblockMode]
  | absorbed =>
      cases nextState <;>
        simp [KiloblockMode.ValidRemaining, nextKiloblockMode,
          fallbackKiloblockMode]

theorem nextKiloblockMode_step
    {table : Table ι} {Signal : Type} [Fintype Signal]
    {lastIndex : ℕ}
    (signalLaw : PMF Signal)
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Signal →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (mode : KiloblockMode table lastIndex)
    (state : PublicQuittingState ι Signal) (action : ι → Bool)
    (nextState : PublicQuittingState ι Signal)
    (hmatch : mode.MatchesState state)
    (hsupport : nextState ∈
      ((publicQuittingGame table signalLaw).transition state action).support) :
    KiloblockModeStep mesh selector continuation mode action nextState
      (nextKiloblockMode mesh selector continuation mode action nextState) := by
  classical
  cases mode with
  | draw phase =>
      cases state with
      | draw =>
          simp only [publicQuittingGame, PMF.support_map] at hsupport
          obtain ⟨signal, _, rfl⟩ := hsupport
          cases phase <;>
            simp [KiloblockModeStep, nextKiloblockMode]
      | active => simp [KiloblockMode.MatchesState] at hmatch
      | absorbed => simp [KiloblockMode.MatchesState] at hmatch
  | active k choice remaining =>
      cases state with
      | draw => simp [KiloblockMode.MatchesState] at hmatch
      | active signal =>
          by_cases hquit :
              ({who | action who = true} : Finset ι).Nonempty
          · have hnext : nextState = .absorbed ⟨_, hquit⟩ := by
              simp only [publicQuittingGame, dif_pos hquit,
                PMF.support_pure] at hsupport
              exact Set.mem_singleton_iff.mp hsupport
            subst nextState
            simp [KiloblockModeStep, nextKiloblockMode, hquit]
          · by_cases hremaining : remaining ≤ 1
            · have hnext : nextState = .draw := by
                simp only [publicQuittingGame, dif_neg hquit,
                  PMF.support_pure] at hsupport
                exact Set.mem_singleton_iff.mp hsupport
              subst nextState
              simp [KiloblockModeStep, nextKiloblockMode, hquit,
                hremaining]
            · have hnext : nextState = .draw := by
                simp only [publicQuittingGame, dif_neg hquit,
                  PMF.support_pure] at hsupport
                exact Set.mem_singleton_iff.mp hsupport
              subst nextState
              simp [KiloblockModeStep, nextKiloblockMode, hquit,
                hremaining]
      | absorbed => simp [KiloblockMode.MatchesState] at hmatch
  | finalActive =>
      cases state with
      | draw => simp [KiloblockMode.MatchesState] at hmatch
      | active signal =>
          by_cases hquit :
              ({who | action who = true} : Finset ι).Nonempty
          · have hnext : nextState = .absorbed ⟨_, hquit⟩ := by
              simp only [publicQuittingGame, dif_pos hquit,
                PMF.support_pure] at hsupport
              exact Set.mem_singleton_iff.mp hsupport
            subst nextState
            simp [KiloblockModeStep, nextKiloblockMode, hquit]
          · have hnext : nextState = .draw := by
              simp only [publicQuittingGame, dif_neg hquit,
                PMF.support_pure] at hsupport
              exact Set.mem_singleton_iff.mp hsupport
            subst nextState
            simp [KiloblockModeStep, nextKiloblockMode, hquit]
      | absorbed => simp [KiloblockMode.MatchesState] at hmatch
  | absorbed origin =>
      cases state with
      | draw => simp [KiloblockMode.MatchesState] at hmatch
      | active => simp [KiloblockMode.MatchesState] at hmatch
      | absorbed quitters =>
          have hnext : nextState = .absorbed quitters := by
            simp only [publicQuittingGame, PMF.support_pure] at hsupport
            exact Set.mem_singleton_iff.mp hsupport
          subst nextState
          simp [KiloblockModeStep, nextKiloblockMode]

/-! Remove the last transition record from a nonempty stochastic-game
history; its recorded source state becomes the preceding terminal state. -/
def publicHistoryInit
    {table : Table ι} {Signal : Type} [Finite Signal]
    (signalLaw : PMF Signal) {t : ℕ}
    (history : (publicQuittingGame table signalLaw).Hist (t + 1)) :
    (publicQuittingGame table signalLaw).Hist t :=
  ⟨Fin.init history.1, (history.1 (Fin.last t)).1⟩

@[simp] theorem publicHistoryInit_snoc
    {table : Table ι} {Signal : Type} [Finite Signal]
    (signalLaw : PMF Signal) {t : ℕ}
    (history : (publicQuittingGame table signalLaw).Hist t)
    (action : ι → Bool) (nextState : PublicQuittingState ι Signal) :
    publicHistoryInit signalLaw
      ⟨Fin.snoc history.1 (history.2, action), nextState⟩ = history := by
  simp [publicHistoryInit]

def initialKiloblockMode
    {table : Table ι} {Signal : Type} {lastIndex : ℕ}
    (state : PublicQuittingState ι Signal) :
    KiloblockMode table lastIndex :=
  match state with
  | .draw => .draw (.choose (Fin.last lastIndex))
  | .active _ => .finalActive
  | .absorbed _ => .absorbed none

theorem initialKiloblockMode_validRemaining
    {table : Table ι} {Signal : Type} {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (state : PublicQuittingState ι Signal) :
    (initialKiloblockMode (table := table) (lastIndex := lastIndex)
      state).ValidRemaining mesh := by
  cases state <;>
    simp [initialKiloblockMode, KiloblockMode.ValidRemaining]

/-! The schedule mode is reconstructed from every literal public history.
This makes the compiler a genuine behavior strategy rather than an external
state machine whose relation to histories is postulated. -/
def compiledKiloblockMode
    {table : Table ι} {signalCount lastIndex : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Fin (signalCount + 1) →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation) :
    ∀ t, (publicQuittingGame table signalLaw).Hist t →
      KiloblockMode table lastIndex
  | 0, history => initialKiloblockMode history.2
  | t + 1, history =>
      nextKiloblockMode mesh selector continuation
        (compiledKiloblockMode signalLaw mesh selector continuation t
          (publicHistoryInit signalLaw history))
        (history.1 (Fin.last t)).2 history.2

theorem compiledKiloblockMode_matchesState
    {table : Table ι} {signalCount lastIndex : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Fin (signalCount + 1) →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (t : ℕ) (history : (publicQuittingGame table signalLaw).Hist t) :
    (compiledKiloblockMode signalLaw mesh selector continuation t history).MatchesState
      history.2 := by
  cases t with
  | zero =>
      cases hstate : history.2 <;>
        simp [compiledKiloblockMode, initialKiloblockMode,
          KiloblockMode.MatchesState, hstate]
  | succ t =>
      exact nextKiloblockMode_matchesState mesh selector continuation _ _ _

theorem compiledKiloblockMode_validRemaining
    {table : Table ι} {signalCount lastIndex : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (mesh : Fin (lastIndex + 1) → ℕ)
    (hmesh : ∀ k, 0 < mesh k)
    (selector : Fin (lastIndex + 1) → Fin (signalCount + 1) →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation) :
    ∀ t (history : (publicQuittingGame table signalLaw).Hist t),
      (compiledKiloblockMode signalLaw mesh selector continuation
        t history).ValidRemaining mesh := by
  intro t
  induction t with
  | zero =>
      intro history
      exact initialKiloblockMode_validRemaining mesh history.2
  | succ t ih =>
      intro history
      exact nextKiloblockMode_validRemaining mesh hmesh selector continuation
        (compiledKiloblockMode signalLaw mesh selector continuation t
          (publicHistoryInit signalLaw history))
        (history.1 (Fin.last t)).2 history.2
        (ih (publicHistoryInit signalLaw history))

theorem compiledKiloblockMode_remaining_bounds
    {table : Table ι} {signalCount lastIndex : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (mesh : Fin (lastIndex + 1) → ℕ)
    (hmesh : ∀ k, 0 < mesh k)
    (selector : Fin (lastIndex + 1) → Fin (signalCount + 1) →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (t : ℕ) (history : (publicQuittingGame table signalLaw).Hist t)
    (k : Fin (lastIndex + 1)) (choice : Option (NormalPlayer table))
    (remaining : ℕ)
    (hmode : compiledKiloblockMode signalLaw mesh selector continuation
          t history = .active k choice remaining ∨
        compiledKiloblockMode signalLaw mesh selector continuation
          t history = .draw (.resume k choice remaining)) :
    0 < remaining ∧ remaining ≤ mesh k := by
  have hvalid := compiledKiloblockMode_validRemaining
    signalLaw mesh hmesh selector continuation t history
  rcases hmode with hmode | hmode <;> rw [hmode] at hvalid <;> exact hvalid

theorem compiledKiloblockMode_step
    {table : Table ι} {signalCount lastIndex : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Fin (signalCount + 1) →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (t : ℕ) (history : (publicQuittingGame table signalLaw).Hist t)
    (action : ι → Bool)
    (nextState : PublicQuittingState ι (Fin (signalCount + 1)))
    (hsupport : nextState ∈
      ((publicQuittingGame table signalLaw).transition
        history.2 action).support) :
    KiloblockModeStep mesh selector continuation
      (compiledKiloblockMode signalLaw mesh selector continuation t history)
      action nextState
      (compiledKiloblockMode signalLaw mesh selector continuation (t + 1)
        ⟨Fin.snoc history.1 (history.2, action), nextState⟩) := by
  rw [compiledKiloblockMode, publicHistoryInit_snoc]
  simpa using nextKiloblockMode_step signalLaw mesh selector continuation
    (compiledKiloblockMode signalLaw mesh selector continuation t history)
    history.2 action nextState
    (compiledKiloblockMode_matchesState signalLaw mesh selector continuation
      t history) hsupport

structure KiloblockConstruction
    (table : Table ι) (ε : ℝ) where
  soloExitNormalized : SoloExitNormalized table
  payoffsBounded : TablePayoffsBounded table
  profile : SunspotProfile table
  blockCount : ℕ
  point : Fin (blockCount + 1) → NormalPlayer table → ℝ
  point_boundary : ∀ k, DZero (NormalMatrix table) (point k)
  buildingBlock : ∀ k,
    BuildingBlock (NormalMatrix table) (point k) ε
  epsilon_pos : 0 < ε
  epsilon_lt_one : ε < 1
  negativeMargin : ℝ
  negativeMargin_pos : 0 < negativeMargin
  accuracy_below_margin :
    ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ) * ε <
      negativeMargin
  displacement_large : KiloblockDisplacementThreshold table ε <
    KiloblockDisplacement point buildingBlock
  tracking_small : KiloblockTracking point buildingBlock < ε
  column_negative : ∀ owner,
    ∃ who, NormalMatrix table who owner <
      -negativeMargin
  attempt : ∀ k owner,
    BuildingAttempt (NormalMatrix table) (point k) ε
      (buildingBlock k) owner
  mesh : Fin (blockCount + 1) → ℕ
  mesh_pos : ∀ k, 0 < mesh k
  mesh_hazard_small : ∀ k owner,
    quittingMeshHazard (attempt k owner).quitWeight (mesh k) < ε
  mesh_survival : ∀ k owner,
    (1 - quittingMeshHazard (attempt k owner).quitWeight (mesh k)) ^
        mesh k =
      1 - (attempt k owner).quitWeight
  signalSelector : Fin (blockCount + 1) →
    Fin (profile.signalCount + 1) → Option (NormalPlayer table)
  signalSelector_law : ∀ k,
    profile.signalLaw.map (signalSelector k) = (buildingBlock k).z.toPMF
  mode : ∀ t,
    (publicQuittingGame table profile.signalLaw).Hist t →
      KiloblockMode table blockCount
  mode_initial : mode 0
    ((publicQuittingGame table profile.signalLaw).emptyHist
      (PublicQuittingState.draw)) =
      .draw (.choose (Fin.last blockCount))
  mode_state : ∀ t history, (mode t history).MatchesState history.2
  mode_remaining_pos : ∀ t history k choice remaining,
    mode t history = .active k choice remaining ∨
      mode t history = .draw (.resume k choice remaining) →
        0 < remaining
  mode_remaining_le_mesh : ∀ t history k choice remaining,
    mode t history = .active k choice remaining ∨
      mode t history = .draw (.resume k choice remaining) →
        remaining ≤ mesh k
  mode_step : ∀ t history action nextState,
    nextState ∈ ((publicQuittingGame table profile.signalLaw).transition
      history.2 action).support →
    KiloblockModeStep mesh signalSelector
      (fun k owner => (attempt k owner).continuation)
      (mode t history) action nextState
      (mode (t + 1) (Fin.snoc history.1 (history.2, action), nextState))
  strategy_eq : ∀ who t history,
    profile.strategy who t history =
      match mode t history with
      | .active k (some owner) _ =>
          if _hwho : who = owner.1 then
            quittingMeshHazardCoin (attempt k owner).quitWeight (mesh k)
              (attempt k owner).quitWeight_pos.le
              (attempt k owner).quitWeight_lt_one
          else PMF.pure false
      | _ => PMF.pure false

def KiloblockBlueprint.continuation
    {table : Table ι} {ε : ℝ}
    (blueprint : KiloblockBlueprint table ε) :
    Fin (blueprint.chain.blockCount + 1) → NormalPlayer table →
      AttemptContinuation :=
  fun k owner => (blueprint.attempt k owner).continuation

noncomputable def KiloblockBlueprint.signalData
    {table : Table ι} {ε : ℝ}
    (blueprint : KiloblockBlueprint table ε) :
    Math.PMFProduct.CommonFiniteSignal
      (Fin (blueprint.chain.blockCount + 1))
      (Option (NormalPlayer table))
      (fun k => (blueprint.chain.buildingBlock k).z.toPMF) :=
  Math.PMFProduct.commonFiniteSignal _ _ _

noncomputable def KiloblockBlueprint.mode
    {table : Table ι} {ε : ℝ}
    (blueprint : KiloblockBlueprint table ε) :
    ∀ t,
      (publicQuittingGame table blueprint.signalData.law).Hist t →
        KiloblockMode table blueprint.chain.blockCount :=
  compiledKiloblockMode blueprint.signalData.law blueprint.mesh
    blueprint.signalData.selector blueprint.continuation

noncomputable def KiloblockBlueprint.strategy
    {table : Table ι} {ε : ℝ}
    (blueprint : KiloblockBlueprint table ε) :
    (publicQuittingGame table blueprint.signalData.law).BehaviorProfile :=
  fun who t history =>
    match blueprint.mode t history with
    | .active k (some owner) _ =>
        if _hwho : who = owner.1 then
          quittingMeshHazardCoin
            (blueprint.attempt k owner).quitWeight (blueprint.mesh k)
            (blueprint.attempt k owner).quitWeight_pos.le
            (blueprint.attempt k owner).quitWeight_lt_one
        else PMF.pure false
    | _ => PMF.pure false

noncomputable def KiloblockBlueprint.profile
    {table : Table ι} {ε : ℝ}
    (blueprint : KiloblockBlueprint table ε) : SunspotProfile table :=
  { signalCount := blueprint.signalData.signalCount
    signalLaw := blueprint.signalData.law
    strategy := blueprint.strategy }

/-! Compile the quantitative blueprint into the literal public-signal
behavior profile and history automaton consumed by Lemmas 3.8--3.10. -/
noncomputable def KiloblockBlueprint.toConstruction
    {table : Table ι} {ε : ℝ}
    (blueprint : KiloblockBlueprint table ε) :
    KiloblockConstruction table ε := by
  let profile := blueprint.profile
  refine {
    soloExitNormalized := blueprint.soloExitNormalized
    payoffsBounded := blueprint.payoffsBounded
    profile := profile
    blockCount := blueprint.chain.blockCount
    point := blueprint.chain.point
    point_boundary := blueprint.chain.point_boundary
    buildingBlock := blueprint.chain.buildingBlock
    epsilon_pos := blueprint.epsilon_pos
    epsilon_lt_one := blueprint.epsilon_lt_one
    negativeMargin := blueprint.negativeMargin
    negativeMargin_pos := blueprint.negativeMargin_pos
    accuracy_below_margin := blueprint.accuracy_below_margin
    displacement_large := blueprint.chain.displacement_large
    tracking_small := blueprint.chain.tracking_small
    column_negative := blueprint.column_negative
    attempt := blueprint.attempt
    mesh := blueprint.mesh
    mesh_pos := blueprint.mesh_pos
    mesh_hazard_small := blueprint.mesh_hazard_small
    mesh_survival := blueprint.mesh_survival
    signalSelector := blueprint.signalData.selector
    signalSelector_law := blueprint.signalData.selector_law
    mode := blueprint.mode
    mode_initial := ?_
    mode_state := ?_
    mode_remaining_pos := ?_
    mode_remaining_le_mesh := ?_
    mode_step := ?_
    strategy_eq := ?_ }
  · rfl
  · intro t history
    exact compiledKiloblockMode_matchesState
      blueprint.signalData.law blueprint.mesh blueprint.signalData.selector
      blueprint.continuation t history
  · intro t history k choice remaining hmode
    exact (compiledKiloblockMode_remaining_bounds
      blueprint.signalData.law blueprint.mesh blueprint.mesh_pos
      blueprint.signalData.selector blueprint.continuation t history
      k choice remaining hmode).1
  · intro t history k choice remaining hmode
    exact (compiledKiloblockMode_remaining_bounds
      blueprint.signalData.law blueprint.mesh blueprint.mesh_pos
      blueprint.signalData.selector blueprint.continuation t history
      k choice remaining hmode).2
  · intro t history action nextState hsupport
    exact compiledKiloblockMode_step
      blueprint.signalData.law blueprint.mesh blueprint.signalData.selector
      blueprint.continuation t history action nextState hsupport
  · intro who t history
    change blueprint.strategy who t history = _
    cases hmode : blueprint.mode t history with
    | draw =>
        simp [KiloblockBlueprint.strategy, hmode]
        rfl
    | active k choice remaining =>
        cases choice with
        | none =>
            simp [KiloblockBlueprint.strategy, hmode]
            rfl
        | some owner =>
            by_cases hwho : who = owner.1
            · simp [KiloblockBlueprint.strategy, hmode, hwho]
            · simp [KiloblockBlueprint.strategy, hmode, hwho]
              rfl
    | finalActive =>
        simp [KiloblockBlueprint.strategy, hmode]
        rfl
    | absorbed =>
        simp [KiloblockBlueprint.strategy, hmode]
        rfl

theorem exists_kiloblockConstruction
    (table : Table ι)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (hnormal : HasNormalPlayers table)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table))
    (hQ : QMatrix (NormalMatrix table))
    {requested : ℝ} (hrequested : 0 < requested) :
    ∃ ε : ℝ, 7 * ε ≤ requested ∧
      Nonempty (KiloblockConstruction table ε) := by
  obtain ⟨ε, hε, ⟨blueprint⟩⟩ := exists_kiloblockBlueprint
    table hnormalized hbounded hnormal hzero hQ hrequested
  exact ⟨ε, hε, ⟨blueprint.toConstruction⟩⟩

/-! A finite schedule-state shape. Normal-player values and remaining
counters are represented by finite indices. -/
inductive FiniteKiloblockMode
    (blockCount ownerCount totalMesh : ℕ)
  | drawChoose (k : Fin (blockCount + 1))
  | drawResume (k : Fin (blockCount + 1))
      (choice : Option (Fin ownerCount))
      (remaining : Fin (totalMesh + 1))
  | drawFinal
  | active (k : Fin (blockCount + 1))
      (choice : Option (Fin ownerCount))
      (remaining : Fin (totalMesh + 1))
  | finalActive
  | absorbed (origin : Option (Fin (blockCount + 1)))
deriving Fintype, DecidableEq

def KiloblockConstruction.totalMesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) : ℕ :=
  ∑ k, construction.mesh k

abbrev KiloblockFiniteMode
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :=
  FiniteKiloblockMode construction.blockCount
    (Fintype.card (NormalPlayer table)) construction.totalMesh

def KiloblockConstruction.ownerCode
    {table : Table ι} {ε : ℝ}
    (_construction : KiloblockConstruction table ε)
    (owner : NormalPlayer table) :
    Fin (Fintype.card (NormalPlayer table)) :=
  Fintype.equivFin (NormalPlayer table) owner

def KiloblockConstruction.ownerOfCode
    {table : Table ι} {ε : ℝ}
    (_construction : KiloblockConstruction table ε)
    (owner : Fin (Fintype.card (NormalPlayer table))) :
    NormalPlayer table :=
  (Fintype.equivFin (NormalPlayer table)).symm owner

@[simp]
theorem KiloblockConstruction.ownerCode_ownerOfCode
    {table : Table ι} {ε : ℝ}
  (construction : KiloblockConstruction table ε)
    (owner : Fin (Fintype.card (NormalPlayer table))) :
    construction.ownerCode (construction.ownerOfCode owner) = owner :=
  (Fintype.equivFin (NormalPlayer table)).apply_symm_apply owner

@[simp]
theorem KiloblockConstruction.ownerOfCode_ownerCode
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (owner : NormalPlayer table) :
    construction.ownerOfCode (construction.ownerCode owner) = owner :=
  (Fintype.equivFin (NormalPlayer table)).symm_apply_apply owner

def KiloblockConstruction.finiteMode
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockMode table construction.blockCount) :
    KiloblockFiniteMode construction :=
  match mode with
  | .draw (.choose k) => .drawChoose k
  | .draw (.resume k choice remaining) =>
      .drawResume k (choice.map construction.ownerCode)
        ⟨min remaining construction.totalMesh, by omega⟩
  | .draw .final => .drawFinal
  | .active k choice remaining =>
      .active k (choice.map construction.ownerCode)
        ⟨min remaining construction.totalMesh, by omega⟩
  | .finalActive => .finalActive
  | .absorbed origin => .absorbed origin

def KiloblockConstruction.modeOfFinite
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    KiloblockMode table construction.blockCount :=
  match mode with
  | .drawChoose k => .draw (.choose k)
  | .drawResume k choice remaining =>
      .draw (.resume k (choice.map construction.ownerOfCode) remaining.1)
  | .drawFinal => .draw .final
  | .active k choice remaining =>
      .active k (choice.map construction.ownerOfCode) remaining.1
  | .finalActive => .finalActive
  | .absorbed origin => .absorbed origin

theorem KiloblockConstruction.mesh_le_totalMesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.mesh k ≤ construction.totalMesh := by
  unfold KiloblockConstruction.totalMesh
  exact Finset.single_le_sum
    (fun other _ => Nat.zero_le (construction.mesh other))
    (Finset.mem_univ k)

@[simp]
theorem KiloblockConstruction.finiteMode_modeOfFinite
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    construction.finiteMode (construction.modeOfFinite mode) = mode := by
  cases mode with
  | drawChoose k => rfl
  | drawFinal => rfl
  | finalActive => rfl
  | absorbed origin => rfl
  | drawResume k choice remaining =>
      have hremaining : remaining.1 ≤ construction.totalMesh := by omega
      cases choice <;>
        simp [KiloblockConstruction.finiteMode,
          KiloblockConstruction.modeOfFinite, hremaining]
  | active k choice remaining =>
      have hremaining : remaining.1 ≤ construction.totalMesh := by omega
      cases choice <;>
        simp [KiloblockConstruction.finiteMode,
          KiloblockConstruction.modeOfFinite, hremaining]

theorem KiloblockConstruction.modeOfFinite_finiteMode_of_le_totalMesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockMode table construction.blockCount)
    (hremaining : ∀ k choice remaining,
      mode = .active k choice remaining ∨
        mode = .draw (.resume k choice remaining) →
          remaining ≤ construction.totalMesh) :
    construction.modeOfFinite (construction.finiteMode mode) = mode := by
  cases mode with
  | draw phase =>
      cases phase with
      | choose k => rfl
      | final => rfl
      | resume k choice remaining =>
          have hle := hremaining k choice remaining (Or.inr rfl)
          cases choice <;>
            simp [KiloblockConstruction.finiteMode,
              KiloblockConstruction.modeOfFinite, min_eq_left hle]
  | active k choice remaining =>
      have hle := hremaining k choice remaining (Or.inl rfl)
      cases choice <;>
        simp [KiloblockConstruction.finiteMode,
          KiloblockConstruction.modeOfFinite, min_eq_left hle]
  | finalActive => rfl
  | absorbed origin => rfl

theorem KiloblockConstruction.modeOfFinite_finiteMode
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockMode table construction.blockCount)
    (hremaining : ∀ k choice remaining,
      mode = .active k choice remaining ∨
        mode = .draw (.resume k choice remaining) →
          remaining ≤ construction.mesh k) :
    construction.modeOfFinite (construction.finiteMode mode) = mode := by
  apply construction.modeOfFinite_finiteMode_of_le_totalMesh
  intro k choice remaining hmode
  exact (hremaining k choice remaining hmode).trans
    (construction.mesh_le_totalMesh k)

theorem KiloblockConstruction.normalMatrix_bounded
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    MatrixPayoffsBounded (NormalMatrix table) := by
  intro who owner
  exact construction.payoffsBounded.1
    (quittingProjectiveSingletonTerminal owner.1) who.1

/-! Total type-`i` mass across the finite kiloblock chain. -/
def KiloblockConstruction.singletonMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) : ℝ :=
  ∑ k, ∑ owner, (construction.buildingBlock k).z.singleton owner

theorem KiloblockConstruction.displacement_le_twice_singletonMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockDisplacement construction.point construction.buildingBlock ≤
      2 * construction.singletonMass := by
  unfold KiloblockDisplacement KiloblockConstruction.singletonMass
  calc
    ∑ k, dist (construction.point k)
        (construction.buildingBlock k).w ≤
        ∑ k, 2 * ∑ owner,
          (construction.buildingBlock k).z.singleton owner := by
      apply Finset.sum_le_sum
      intro k _
      exact BuildingBlock.displacement_le_twice_singletonMass
        (construction.buildingBlock k) construction.normalMatrix_bounded
    _ = 2 * ∑ k, ∑ owner,
        (construction.buildingBlock k).z.singleton owner := by
      rw [Finset.mul_sum]

theorem KiloblockConstruction.half_displacementThreshold_lt_singletonMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockDisplacementThreshold table ε / 2 <
      construction.singletonMass := by
  have hle := construction.displacement_le_twice_singletonMass
  have hlarge := construction.displacement_large
  linarith

/-! Unnormalized mass with which a type draw leaves the restart loop of one
kiloblock. A restart attempt leaves only when its owner quits; an advance
attempt leaves whether or not its owner quits. -/
def KiloblockConstruction.attemptExitWeight
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) : ℝ :=
  match (construction.attempt k owner).continuation with
  | .restart => (construction.attempt k owner).quitWeight
  | .advance => 1

def KiloblockConstruction.exitMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) : ℝ :=
  (construction.buildingBlock k).z.cemetery +
    ∑ owner, (construction.buildingBlock k).z.singleton owner *
      construction.attemptExitWeight k owner

def KiloblockConstruction.absorbMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) : ℝ :=
  (construction.buildingBlock k).z.singleton owner *
    (construction.attempt k owner).quitWeight

def KiloblockConstruction.attemptAdvanceWeight
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) : ℝ :=
  match (construction.attempt k owner).continuation with
  | .restart => 0
  | .advance => 1 - (construction.attempt k owner).quitWeight

def KiloblockConstruction.advanceMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) : ℝ :=
  (construction.buildingBlock k).z.cemetery +
    ∑ owner, (construction.buildingBlock k).z.singleton owner *
      construction.attemptAdvanceWeight k owner

theorem KiloblockConstruction.exitMass_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.exitMass k = construction.advanceMass k +
      ∑ owner, construction.absorbMass k owner := by
  unfold KiloblockConstruction.exitMass
    KiloblockConstruction.advanceMass
    KiloblockConstruction.absorbMass
    KiloblockConstruction.attemptAdvanceWeight
    KiloblockConstruction.attemptExitWeight
  rw [add_assoc, ← Finset.sum_add_distrib]
  congr 1
  apply Finset.sum_congr rfl
  intro owner _
  cases (construction.attempt k owner).continuation <;> ring

theorem KiloblockConstruction.exitMass_pos
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    0 < construction.exitMass k := by
  obtain ⟨owner, _, howner⟩ :=
    (Finset.sum_pos_iff_of_nonneg (fun owner _ =>
      (construction.buildingBlock k).z.singleton_nonneg owner)).mp
      (construction.buildingBlock k).nontrivial
  have habsorb : 0 < construction.absorbMass k owner :=
    mul_pos howner (construction.attempt k owner).quitWeight_pos
  rw [construction.exitMass_eq k]
  have hadvance : 0 ≤ construction.advanceMass k := by
    unfold KiloblockConstruction.advanceMass
    apply add_nonneg (construction.buildingBlock k).z.cemetery_nonneg
    apply Finset.sum_nonneg
    intro other _
    apply mul_nonneg
    · exact (construction.buildingBlock k).z.singleton_nonneg other
    · unfold KiloblockConstruction.attemptAdvanceWeight
      split
      · exact le_rfl
      · exact sub_nonneg.mpr
          (construction.attempt k other).quitWeight_lt_one.le
  have hsumAbsorb : 0 < ∑ other, construction.absorbMass k other :=
    (Finset.sum_pos_iff_of_nonneg (fun other _ =>
      mul_nonneg ((construction.buildingBlock k).z.singleton_nonneg other)
        (construction.attempt k other).quitWeight_pos.le)).mpr
      ⟨owner, Finset.mem_univ owner, habsorb⟩
  exact add_pos_of_nonneg_of_pos hadvance hsumAbsorb

def KiloblockConstruction.macroAbsorbProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) : ℝ :=
  construction.absorbMass k owner / construction.exitMass k

def KiloblockConstruction.macroAdvanceProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) : ℝ :=
  construction.advanceMass k / construction.exitMass k

theorem KiloblockConstruction.macroProbability_total
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.macroAdvanceProbability k +
      ∑ owner, construction.macroAbsorbProbability k owner = 1 := by
  unfold KiloblockConstruction.macroAdvanceProbability
    KiloblockConstruction.macroAbsorbProbability
  rw [← Finset.sum_div, ← add_div, ← construction.exitMass_eq k]
  exact div_self (ne_of_gt (construction.exitMass_pos k))

private theorem KiloblockConstruction.attempt_account
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner who : NormalPlayer table) :
    construction.attemptExitWeight k owner *
          (construction.buildingBlock k).w who -
        construction.attemptAdvanceWeight k owner *
          construction.point k who -
        (construction.attempt k owner).quitWeight *
          NormalMatrix table who owner =
      (construction.buildingBlock k).w who -
        (construction.buildingBlock k).wi owner who := by
  unfold KiloblockConstruction.attemptExitWeight
    KiloblockConstruction.attemptAdvanceWeight
  cases hbranch : (construction.attempt k owner).continuation with
  | restart =>
      rw [(construction.attempt k owner).payoff who]
      simp only [hbranch]
      ring
  | advance =>
      rw [(construction.attempt k owner).payoff who]
      simp only [hbranch]
      ring

/-! Collapsing all restart attempts in one kiloblock gives the exact macro
balance between singleton absorption and advancement to `y`. -/
theorem KiloblockConstruction.exitMass_mul_w
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (who : NormalPlayer table) :
    construction.exitMass k * (construction.buildingBlock k).w who =
      construction.advanceMass k * construction.point k who +
        ∑ owner, construction.absorbMass k owner *
          NormalMatrix table who owner := by
  have htotal := (construction.buildingBlock k).z.total
  have hbalance := (construction.buildingBlock k).balance who
  have haccount :
      ∑ owner, (construction.buildingBlock k).z.singleton owner *
          (construction.attemptExitWeight k owner *
              (construction.buildingBlock k).w who -
            construction.attemptAdvanceWeight k owner *
              construction.point k who -
            (construction.attempt k owner).quitWeight *
              NormalMatrix table who owner) =
        ∑ owner, (construction.buildingBlock k).z.singleton owner *
          ((construction.buildingBlock k).w who -
            (construction.buildingBlock k).wi owner who) := by
    apply Finset.sum_congr rfl
    intro owner _
    rw [construction.attempt_account k owner who]
  unfold KiloblockConstruction.exitMass
    KiloblockConstruction.advanceMass
    KiloblockConstruction.absorbMass
  have haccount' := haccount
  simp only [mul_sub] at haccount'
  rw [Finset.sum_sub_distrib, Finset.sum_sub_distrib] at haccount'
  rw [Finset.sum_sub_distrib] at haccount'
  rw [add_mul, add_mul, Finset.sum_mul, Finset.sum_mul]
  simp_rw [mul_assoc]
  have hsingletonMul :
      ∑ owner, (construction.buildingBlock k).z.singleton owner *
          (construction.buildingBlock k).w who =
        (∑ owner, (construction.buildingBlock k).z.singleton owner) *
          (construction.buildingBlock k).w who := by
    rw [Finset.sum_mul]
  linear_combination haccount' +
    ((construction.buildingBlock k).w who) * htotal + hbalance + hsingletonMul

theorem KiloblockConstruction.macroAbsorbProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) :
    0 ≤ construction.macroAbsorbProbability k owner := by
  exact div_nonneg
    (mul_nonneg ((construction.buildingBlock k).z.singleton_nonneg owner)
      (construction.attempt k owner).quitWeight_pos.le)
    (construction.exitMass_pos k).le

theorem KiloblockConstruction.macroAdvanceProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    0 ≤ construction.macroAdvanceProbability k := by
  unfold KiloblockConstruction.macroAdvanceProbability
  apply div_nonneg _ (construction.exitMass_pos k).le
  unfold KiloblockConstruction.advanceMass
  apply add_nonneg (construction.buildingBlock k).z.cemetery_nonneg
  apply Finset.sum_nonneg
  intro owner _
  apply mul_nonneg ((construction.buildingBlock k).z.singleton_nonneg owner)
  unfold KiloblockConstruction.attemptAdvanceWeight
  split
  · exact le_rfl
  · exact sub_nonneg.mpr (construction.attempt k owner).quitWeight_lt_one.le

/-! Exact normalized form of the collapsed kiloblock balance. -/
theorem KiloblockConstruction.macro_balance
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (who : NormalPlayer table) :
    (construction.buildingBlock k).w who =
      construction.macroAdvanceProbability k * construction.point k who +
        ∑ owner, construction.macroAbsorbProbability k owner *
          NormalMatrix table who owner := by
  have hbalance := construction.exitMass_mul_w k who
  have hne : construction.exitMass k ≠ 0 := ne_of_gt (construction.exitMass_pos k)
  unfold KiloblockConstruction.macroAdvanceProbability
    KiloblockConstruction.macroAbsorbProbability
  simp_rw [div_mul_eq_mul_div]
  rw [← Finset.sum_div, ← add_div]
  apply (eq_div_iff hne).2
  simpa only [mul_comm] using hbalance

/-! The collapsed version of Eq. (14): displacement is bounded by twice
the actual probability of absorption while crossing the kiloblock. Unlike
the printed `z`-bound, this remains valid for both branches of (F.1). -/
theorem KiloblockConstruction.blockDisplacement_le_two_mul_macroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    dist (construction.point k) (construction.buildingBlock k).w ≤
      2 * ∑ owner, construction.macroAbsorbProbability k owner := by
  rw [dist_eq_norm]
  have hright : 0 ≤ 2 *
      ∑ owner, construction.macroAbsorbProbability k owner :=
    mul_nonneg (by norm_num) (Finset.sum_nonneg fun owner _ =>
      construction.macroAbsorbProbability_nonneg k owner)
  apply (pi_norm_le_iff_of_nonneg hright).2
  intro who
  rw [Real.norm_eq_abs, Pi.sub_apply]
  have htotal := construction.macroProbability_total k
  have hbalance := construction.macro_balance k who
  have hidentity : construction.point k who -
      (construction.buildingBlock k).w who =
        ∑ owner, construction.macroAbsorbProbability k owner *
          (construction.point k who - NormalMatrix table who owner) := by
    calc
      construction.point k who - (construction.buildingBlock k).w who =
          construction.point k who -
            (construction.macroAdvanceProbability k * construction.point k who +
              ∑ owner, construction.macroAbsorbProbability k owner *
                NormalMatrix table who owner) := by rw [hbalance]
      _ = (∑ owner, construction.macroAbsorbProbability k owner) *
            construction.point k who -
          ∑ owner, construction.macroAbsorbProbability k owner *
            NormalMatrix table who owner := by
          have hcoefficient :
              1 - construction.macroAdvanceProbability k =
                ∑ owner, construction.macroAbsorbProbability k owner := by
            linarith
          rw [← hcoefficient]
          ring
      _ = _ := by
        rw [Finset.sum_mul, ← Finset.sum_sub_distrib]
        apply Finset.sum_congr rfl
        intro owner _
        ring
  rw [hidentity]
  calc
    |∑ owner, construction.macroAbsorbProbability k owner *
        (construction.point k who - NormalMatrix table who owner)| ≤
        ∑ owner, |construction.macroAbsorbProbability k owner *
          (construction.point k who - NormalMatrix table who owner)| :=
      Finset.abs_sum_le_sum_abs _ _
    _ ≤ ∑ owner, construction.macroAbsorbProbability k owner * 2 := by
      apply Finset.sum_le_sum
      intro owner _
      rw [abs_mul, abs_of_nonneg
        (construction.macroAbsorbProbability_nonneg k owner)]
      apply mul_le_mul_of_nonneg_left _
        (construction.macroAbsorbProbability_nonneg k owner)
      apply (abs_sub _ _).trans
      have hpoint := abs_le_one_of_mem_D construction.normalMatrix_bounded
        (construction.point_boundary k).1 who
      have hmatrix := construction.normalMatrix_bounded who owner
      linarith
    _ = 2 * ∑ owner, construction.macroAbsorbProbability k owner := by
      rw [← Finset.sum_mul]
      ring

def KiloblockConstruction.totalMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) : ℝ :=
  ∑ k, ∑ owner, construction.macroAbsorbProbability k owner

theorem KiloblockConstruction.half_displacementThreshold_lt_totalMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockDisplacementThreshold table ε / 2 <
      construction.totalMacroAbsorption := by
  have hbound : KiloblockDisplacement construction.point
      construction.buildingBlock ≤ 2 * construction.totalMacroAbsorption := by
    unfold KiloblockDisplacement KiloblockConstruction.totalMacroAbsorption
    calc
      ∑ k, dist (construction.point k) (construction.buildingBlock k).w ≤
          ∑ k, 2 * ∑ owner,
            construction.macroAbsorbProbability k owner := by
        apply Finset.sum_le_sum
        intro k _
        exact construction.blockDisplacement_le_two_mul_macroAbsorption k
      _ = 2 * ∑ k, ∑ owner,
          construction.macroAbsorbProbability k owner := by
        rw [Finset.mul_sum]
  linarith [construction.displacement_large]

def KiloblockConstruction.macroAbsorptionProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) : ℝ :=
  ∑ owner, construction.macroAbsorbProbability k owner

theorem KiloblockConstruction.macroAbsorptionProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    0 ≤ construction.macroAbsorptionProbability k :=
  Finset.sum_nonneg fun owner _ =>
    construction.macroAbsorbProbability_nonneg k owner

theorem KiloblockConstruction.macroAbsorptionProbability_le_one
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.macroAbsorptionProbability k ≤ 1 := by
  have htotal := construction.macroProbability_total k
  have hadvance := construction.macroAdvanceProbability_nonneg k
  unfold KiloblockConstruction.macroAbsorptionProbability
  linarith

theorem KiloblockConstruction.macroAdvanceProbability_eq_one_sub
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.macroAdvanceProbability k =
      1 - construction.macroAbsorptionProbability k := by
  have htotal := construction.macroProbability_total k
  unfold KiloblockConstruction.macroAbsorptionProbability
  linarith

def KiloblockConstruction.macroSurvivalProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) : ℝ :=
  ∏ k, construction.macroAdvanceProbability k

theorem KiloblockConstruction.two_le_normalPlayer_card
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    2 ≤ Fintype.card (NormalPlayer table) := by
  let k : Fin (construction.blockCount + 1) := ⟨0, by omega⟩
  obtain ⟨owner, _, howner⟩ :=
    (Finset.sum_pos_iff_of_nonneg (fun owner _ =>
      (construction.buildingBlock k).z.singleton_nonneg owner)).mp
      (construction.buildingBlock k).nontrivial
  obtain ⟨who, hnegative⟩ := construction.column_negative owner
  have hne : who ≠ owner := by
    intro heq
    subst who
    have hzero : NormalMatrix table owner owner = 0 :=
      construction.soloExitNormalized owner.1
    linarith [construction.negativeMargin_pos]
  by_contra hcard
  have hcardOne : Fintype.card (NormalPlayer table) ≤ 1 := by omega
  exact hne ((Fintype.card_le_one_iff.mp hcardOne) who owner)

theorem KiloblockConstruction.one_le_choose_normalPlayer_two
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    1 ≤ Nat.choose (Fintype.card (NormalPlayer table)) 2 := by
  have hpositive := Nat.choose_pos construction.two_le_normalPlayer_card
  omega

theorem KiloblockConstruction.inv_epsilon_lt_totalMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    ε⁻¹ < construction.totalMacroAbsorption := by
  have hlarge := construction.half_displacementThreshold_lt_totalMacroAbsorption
  have hchoose : (1 : ℝ) ≤
      (Nat.choose (Fintype.card (NormalPlayer table)) 2 : ℝ) := by
    exact_mod_cast construction.one_le_choose_normalPlayer_two
  have hε := construction.epsilon_pos
  have hthreshold : ε⁻¹ < KiloblockDisplacementThreshold table ε / 2 := by
    unfold KiloblockDisplacementThreshold
    rw [inv_eq_one_div]
    apply (div_lt_iff₀ hε).2
    have hεsq : 0 < ε ^ 2 := sq_pos_of_pos hε
    field_simp [ne_of_gt hε]
    nlinarith [mul_pos hε hε]
  exact hthreshold.trans hlarge

theorem KiloblockConstruction.macroSurvival_mul_one_add_total_le_one
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.macroSurvivalProbability *
      (1 + construction.totalMacroAbsorption) ≤ 1 := by
  let charge : ℕ → ℝ := fun j =>
    if hj : j < construction.blockCount + 1 then
      construction.macroAbsorptionProbability ⟨j, hj⟩
    else 0
  have hcharge0 : ∀ j, 0 ≤ charge j := by
    intro j
    simp only [charge]
    split
    · exact construction.macroAbsorptionProbability_nonneg _
    · exact le_rfl
  have hcharge1 : ∀ j, charge j ≤ 1 := by
    intro j
    simp only [charge]
    split
    · exact construction.macroAbsorptionProbability_le_one _
    · exact zero_le_one
  have hmain := Math.prod_one_sub_mul_one_add_sum_range_le_one
    charge hcharge0 hcharge1 0 (construction.blockCount + 1)
  have hsum : (∑ j ∈ Finset.range (construction.blockCount + 1), charge j) =
      construction.totalMacroAbsorption := by
    rw [← Fin.sum_univ_eq_sum_range]
    unfold KiloblockConstruction.totalMacroAbsorption
    apply Finset.sum_congr rfl
    intro k _
    rw [show charge k.1 = construction.macroAbsorptionProbability k by
      simp only [charge, dif_pos k.isLt]]
    rfl
  have hprod : (∏ j ∈ Finset.range (construction.blockCount + 1),
      (1 - charge j)) = construction.macroSurvivalProbability := by
    rw [← Fin.prod_univ_eq_prod_range]
    unfold KiloblockConstruction.macroSurvivalProbability
    apply Finset.prod_congr rfl
    intro k _
    simp only [charge, dif_pos k.isLt]
    exact (construction.macroAdvanceProbability_eq_one_sub k).symm
  simp only [zero_add] at hmain
  rw [hsum, hprod] at hmain
  exact hmain

theorem KiloblockConstruction.inv_epsilon_sq_lt_totalMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    (ε ^ 2)⁻¹ < construction.totalMacroAbsorption := by
  have hlarge := construction.half_displacementThreshold_lt_totalMacroAbsorption
  have hchoose : (1 : ℝ) ≤
      (Nat.choose (Fintype.card (NormalPlayer table)) 2 : ℝ) := by
    exact_mod_cast construction.one_le_choose_normalPlayer_two
  have hεsq : 0 < ε ^ 2 := sq_pos_of_pos construction.epsilon_pos
  have hnumerator : (1 : ℝ) <
      (Nat.choose (Fintype.card (NormalPlayer table)) 2 : ℝ) * (1 + ε) := by
    nlinarith [construction.epsilon_pos]
  have hthreshold : (ε ^ 2)⁻¹ <
      KiloblockDisplacementThreshold table ε / 2 := by
    calc
      (ε ^ 2)⁻¹ = (1 : ℝ) / (ε ^ 2) := by simp
      _ < (Nat.choose (Fintype.card (NormalPlayer table)) 2 : ℝ) *
          (1 + ε) / (ε ^ 2) :=
        (div_lt_div_iff_of_pos_right hεsq).2 hnumerator
      _ = KiloblockDisplacementThreshold table ε / 2 := by
        unfold KiloblockDisplacementThreshold
        ring
  exact hthreshold.trans hlarge

/-! The corrected global estimate. The source applies (14) to the raw type
mass `z`; the branch-sensitive construction instead applies the collapsed
identity above to actual macro hazards. The original `1/ε²` displacement
budget actually gives the stronger survival bound `ε²`. -/
theorem KiloblockConstruction.macroSurvivalProbability_lt_epsilon_sq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.macroSurvivalProbability < ε ^ 2 := by
  have hmain := construction.macroSurvival_mul_one_add_total_le_one
  have hfactor : 0 < 1 + construction.totalMacroAbsorption := by
    have hinv : 0 < (ε ^ 2)⁻¹ := inv_pos.mpr
      (sq_pos_of_pos construction.epsilon_pos)
    linarith [construction.inv_epsilon_sq_lt_totalMacroAbsorption]
  have hepsilonFactor :
      1 < ε ^ 2 * (1 + construction.totalMacroAbsorption) := by
    have hmul : 1 < ε ^ 2 * construction.totalMacroAbsorption := by
      rw [← inv_mul_cancel₀ (ne_of_gt (sq_pos_of_pos construction.epsilon_pos))]
      simpa only [mul_comm] using
        mul_lt_mul_of_pos_left
          construction.inv_epsilon_sq_lt_totalMacroAbsorption
          (sq_pos_of_pos construction.epsilon_pos)
    nlinarith [sq_pos_of_pos construction.epsilon_pos]
  by_contra hnot
  have hεle : ε ^ 2 ≤ construction.macroSurvivalProbability := le_of_not_gt hnot
  have := mul_le_mul_of_nonneg_right hεle hfactor.le
  linarith

theorem KiloblockConstruction.macroSurvivalProbability_lt_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.macroSurvivalProbability < ε := by
  exact construction.macroSurvivalProbability_lt_epsilon_sq.trans_le
    (by nlinarith [construction.epsilon_pos, construction.epsilon_lt_one])

def KiloblockConstruction.macroAdvanceAt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ) : ℝ :=
  if hj : j < construction.blockCount + 1 then
    construction.macroAdvanceProbability ⟨j, hj⟩
  else 1

def KiloblockConstruction.macroAbsorbAt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ)
    (owner : NormalPlayer table) : ℝ :=
  if hj : j < construction.blockCount + 1 then
    construction.macroAbsorbProbability ⟨j, hj⟩ owner
  else 0

/-! Survival after crossing blocks `fuel-1,...,0` in the source's reverse
order. -/
def KiloblockConstruction.macroSurvivalFuel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) : ℕ → ℝ
  | 0 => 1
  | fuel + 1 => construction.macroAdvanceAt fuel *
      construction.macroSurvivalFuel fuel

/-! Total probability that `owner` absorbs while crossing the first `fuel`
blocks, with block `fuel-1` crossed first. -/
def KiloblockConstruction.macroOwnerMassFuel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (owner : NormalPlayer table) : ℕ → ℝ
  | 0 => 0
  | fuel + 1 => construction.macroAbsorbAt fuel owner +
      construction.macroAdvanceAt fuel *
        construction.macroOwnerMassFuel owner fuel

def KiloblockConstruction.macroOtherMassFuel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) : ℕ → ℝ
  | 0 => 0
  | fuel + 1 =>
      (∑ owner ∈ Finset.univ.erase excluded,
        construction.macroAbsorbAt fuel owner) +
      construction.macroAdvanceAt fuel *
        construction.macroOtherMassFuel excluded fuel

theorem KiloblockConstruction.macroAdvanceAt_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ) :
    0 ≤ construction.macroAdvanceAt j := by
  unfold KiloblockConstruction.macroAdvanceAt
  split
  · exact construction.macroAdvanceProbability_nonneg _
  · exact zero_le_one

theorem KiloblockConstruction.macroAbsorbAt_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ)
    (owner : NormalPlayer table) :
    0 ≤ construction.macroAbsorbAt j owner := by
  unfold KiloblockConstruction.macroAbsorbAt
  split
  · exact construction.macroAbsorbProbability_nonneg _ owner
  · exact le_rfl

theorem KiloblockConstruction.macroSurvivalFuel_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    ∀ fuel, 0 ≤ construction.macroSurvivalFuel fuel
  | 0 => by simp [KiloblockConstruction.macroSurvivalFuel]
  | fuel + 1 => by
      rw [KiloblockConstruction.macroSurvivalFuel]
      exact mul_nonneg (construction.macroAdvanceAt_nonneg fuel)
        (construction.macroSurvivalFuel_nonneg fuel)

theorem KiloblockConstruction.macroOwnerMassFuel_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (owner : NormalPlayer table) :
    ∀ fuel, 0 ≤ construction.macroOwnerMassFuel owner fuel
  | 0 => by simp [KiloblockConstruction.macroOwnerMassFuel]
  | fuel + 1 => by
      rw [KiloblockConstruction.macroOwnerMassFuel]
      exact add_nonneg (construction.macroAbsorbAt_nonneg fuel owner)
        (mul_nonneg (construction.macroAdvanceAt_nonneg fuel)
          (construction.macroOwnerMassFuel_nonneg owner fuel))

theorem KiloblockConstruction.macroOtherMassFuel_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ∀ fuel, 0 ≤ construction.macroOtherMassFuel excluded fuel
  | 0 => by simp [KiloblockConstruction.macroOtherMassFuel]
  | fuel + 1 => by
      rw [KiloblockConstruction.macroOtherMassFuel]
      exact add_nonneg
        (Finset.sum_nonneg fun owner _ =>
          construction.macroAbsorbAt_nonneg fuel owner)
        (mul_nonneg (construction.macroAdvanceAt_nonneg fuel)
          (construction.macroOtherMassFuel_nonneg excluded fuel))

theorem KiloblockConstruction.macroAt_total
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (j : ℕ) (hj : j < construction.blockCount + 1) :
    construction.macroAdvanceAt j +
      ∑ owner, construction.macroAbsorbAt j owner = 1 := by
  simp only [KiloblockConstruction.macroAdvanceAt,
    KiloblockConstruction.macroAbsorbAt, dif_pos hj]
  exact construction.macroProbability_total ⟨j, hj⟩

theorem KiloblockConstruction.macroMassFuel_total
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    ∀ fuel, fuel ≤ construction.blockCount + 1 →
      construction.macroSurvivalFuel fuel +
        ∑ owner, construction.macroOwnerMassFuel owner fuel = 1
  | 0, _ => by simp [KiloblockConstruction.macroSurvivalFuel,
      KiloblockConstruction.macroOwnerMassFuel]
  | fuel + 1, hfuel => by
      have hprevious := construction.macroMassFuel_total fuel (by omega)
      have hlocal := construction.macroAt_total fuel (by omega)
      rw [KiloblockConstruction.macroSurvivalFuel]
      simp_rw [KiloblockConstruction.macroOwnerMassFuel]
      rw [Finset.sum_add_distrib, ← Finset.mul_sum]
      calc
        construction.macroAdvanceAt fuel * construction.macroSurvivalFuel fuel +
              ((∑ owner, construction.macroAbsorbAt fuel owner) +
                construction.macroAdvanceAt fuel *
                  ∑ owner, construction.macroOwnerMassFuel owner fuel) =
            construction.macroAdvanceAt fuel *
                (construction.macroSurvivalFuel fuel +
                  ∑ owner, construction.macroOwnerMassFuel owner fuel) +
              ∑ owner, construction.macroAbsorbAt fuel owner := by ring
        _ = construction.macroAdvanceAt fuel +
            ∑ owner, construction.macroAbsorbAt fuel owner := by
          rw [hprevious, mul_one]
        _ = 1 := hlocal

theorem KiloblockConstruction.macroOwner_add_otherMassFuel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ∀ fuel, construction.macroOwnerMassFuel excluded fuel +
        construction.macroOtherMassFuel excluded fuel =
      ∑ owner, construction.macroOwnerMassFuel owner fuel
  | 0 => by simp [KiloblockConstruction.macroOwnerMassFuel,
      KiloblockConstruction.macroOtherMassFuel]
  | fuel + 1 => by
      rw [KiloblockConstruction.macroOwnerMassFuel,
        KiloblockConstruction.macroOtherMassFuel]
      have hdecompose :
          construction.macroAbsorbAt fuel excluded +
              ∑ owner ∈ Finset.univ.erase excluded,
                construction.macroAbsorbAt fuel owner =
            ∑ owner, construction.macroAbsorbAt fuel owner := by
        rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
        ring
      simp_rw [KiloblockConstruction.macroOwnerMassFuel]
      rw [Finset.sum_add_distrib, ← Finset.mul_sum]
      rw [← construction.macroOwner_add_otherMassFuel excluded fuel]
      linear_combination hdecompose

theorem KiloblockConstruction.macroSurvivalFuel_full
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.macroSurvivalFuel (construction.blockCount + 1) =
      construction.macroSurvivalProbability := by
  have hprefix : ∀ fuel, fuel ≤ construction.blockCount + 1 →
      construction.macroSurvivalFuel fuel =
        ∏ j ∈ Finset.range fuel, construction.macroAdvanceAt j := by
    intro fuel hfuel
    induction fuel with
    | zero => simp [KiloblockConstruction.macroSurvivalFuel]
    | succ fuel ih =>
        rw [KiloblockConstruction.macroSurvivalFuel, Finset.prod_range_succ]
        rw [ih (by omega)]
        ring
  calc
    construction.macroSurvivalFuel (construction.blockCount + 1) =
        ∏ j ∈ Finset.range (construction.blockCount + 1),
          construction.macroAdvanceAt j := hprefix _ le_rfl
    _ = ∏ k : Fin (construction.blockCount + 1),
        construction.macroAdvanceAt k.1 := by
      rw [Fin.prod_univ_eq_prod_range]
    _ = construction.macroSurvivalProbability := by
      unfold KiloblockConstruction.macroSurvivalProbability
      apply Finset.prod_congr rfl
      intro k _
      rw [show construction.macroAdvanceAt k.1 =
          construction.macroAdvanceProbability k by
        simp only [KiloblockConstruction.macroAdvanceAt, dif_pos k.isLt]]

/-! The coordinate error in (A.2'') between the endpoint of block `j` and
the next point in the chain. It is zero outside the actual chain, which lets
the cumulative correction below be indexed by an ordinary natural number. -/
def KiloblockConstruction.trackingIncrement
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ)
    (who : NormalPlayer table) : ℝ :=
  if hj : j < construction.blockCount then
    (construction.buildingBlock ⟨j, by omega⟩).w who -
      construction.point ⟨j + 1, by omega⟩ who
  else 0

def KiloblockConstruction.trackingDistanceIncrement
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ) : ℝ :=
  if hj : j < construction.blockCount then
    dist (construction.point ⟨j + 1, by omega⟩)
      (construction.buildingBlock ⟨j, by omega⟩).w
  else 0

/-! The accumulated (A.2'') correction below point `yᵏ`. Adding this
correction makes the prescribed value telescope exactly when the schedule
advances from kiloblock `k` to `k-1`. -/
def KiloblockConstruction.trackingCorrection
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (who : NormalPlayer table) : ℝ :=
  ∑ j ∈ Finset.range k.1, construction.trackingIncrement j who

@[simp]
theorem KiloblockConstruction.trackingCorrection_zero
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    construction.trackingCorrection ⟨0, by omega⟩ who = 0 := by
  simp [KiloblockConstruction.trackingCorrection]

theorem KiloblockConstruction.trackingCorrection_succ
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (j : ℕ) (hj : j < construction.blockCount)
    (who : NormalPlayer table) :
    construction.trackingCorrection ⟨j + 1, by omega⟩ who =
      construction.trackingCorrection ⟨j, by omega⟩ who +
        (construction.buildingBlock ⟨j, by omega⟩).w who -
          construction.point ⟨j + 1, by omega⟩ who := by
  simp [KiloblockConstruction.trackingCorrection,
    Finset.sum_range_succ, KiloblockConstruction.trackingIncrement, hj]
  ring

/-! Exact telescope at an advance from index `j+1` to index `j`. -/
theorem KiloblockConstruction.point_add_trackingCorrection_succ
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (j : ℕ) (hj : j < construction.blockCount)
    (who : NormalPlayer table) :
    construction.point ⟨j + 1, by omega⟩ who +
        construction.trackingCorrection ⟨j + 1, by omega⟩ who =
      (construction.buildingBlock ⟨j, by omega⟩).w who +
        construction.trackingCorrection ⟨j, by omega⟩ who := by
  rw [construction.trackingCorrection_succ j hj who]
  ring

theorem KiloblockConstruction.abs_trackingCorrection_lt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (who : NormalPlayer table) :
    |construction.trackingCorrection k who| < ε := by
  calc
    |construction.trackingCorrection k who| ≤
        ∑ j ∈ Finset.range k.1,
          |construction.trackingIncrement j who| := by
      exact Finset.abs_sum_le_sum_abs _ _
    _ ≤ ∑ j ∈ Finset.range k.1,
        construction.trackingDistanceIncrement j := by
      apply Finset.sum_le_sum
      intro j hj
      have hjk : j < k.1 := Finset.mem_range.mp hj
      have hjcount : j < construction.blockCount := by omega
      simp only [KiloblockConstruction.trackingIncrement,
        KiloblockConstruction.trackingDistanceIncrement, hjcount,
        dif_pos]
      rw [dist_eq_norm]
      simpa only [Pi.sub_apply, Real.norm_eq_abs, abs_sub_comm] using
        norm_le_pi_norm
          (construction.point ⟨j + 1, by omega⟩ -
            (construction.buildingBlock ⟨j, by omega⟩).w) who
    _ ≤ ∑ j ∈ Finset.range construction.blockCount,
        construction.trackingDistanceIncrement j := by
      apply Finset.sum_le_sum_of_subset_of_nonneg
      · intro j hj
        simp only [Finset.mem_range] at hj ⊢
        omega
      · intro j hj _
        simp only [KiloblockConstruction.trackingDistanceIncrement]
        split <;> positivity
    _ = KiloblockTracking construction.point construction.buildingBlock := by
      rw [← Fin.sum_univ_eq_sum_range]
      apply Finset.sum_congr rfl
      intro j _
      simp [KiloblockConstruction.trackingDistanceIncrement]
    _ < ε := construction.tracking_small

/-! Value of the currently selected type with `remaining` active decisions
left. Type zero has no Quit hazard and keeps the point value until it
advances; type `i` uses the exact mesh continuation value above. -/
def KiloblockConstruction.selectedValue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (who : NormalPlayer table) : ℝ :=
  match choice with
  | none => construction.point k who
  | some owner =>
      (construction.attempt k owner).remainingValue
        (construction.mesh k) remaining who

theorem KiloblockConstruction.selectedValue_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table))
    (who : NormalPlayer table) :
    construction.selectedValue k choice (construction.mesh k) who =
      (construction.buildingBlock k).choiceValue choice who := by
  cases choice with
  | none => rfl
  | some owner =>
      exact (construction.attempt k owner).remainingValue_mesh
        (construction.mesh k) (construction.mesh_survival k owner) who

theorem KiloblockConstruction.selectedValue_some_succ
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (who : NormalPlayer table) :
    construction.selectedValue k (some owner) (remaining + 1) who =
      quittingMeshHazard (construction.attempt k owner).quitWeight
          (construction.mesh k) * NormalMatrix table who owner +
        (1 - quittingMeshHazard (construction.attempt k owner).quitWeight
          (construction.mesh k)) *
          construction.selectedValue k (some owner) remaining who := by
  exact (construction.attempt k owner).remainingValue_succ
    (construction.mesh k) remaining who

/-! The actual public signal realizes the type lottery `z`, so a fresh draw
has exactly value `w(yᵏ)`. -/
theorem KiloblockConstruction.expect_selectedValue_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (who : NormalPlayer table) :
    expect construction.profile.signalLaw (fun signal =>
      construction.selectedValue k (construction.signalSelector k signal)
        (construction.mesh k) who) =
      (construction.buildingBlock k).w who := by
  simp_rw [construction.selectedValue_mesh k]
  calc
    expect construction.profile.signalLaw (fun signal =>
        (construction.buildingBlock k).choiceValue
          (construction.signalSelector k signal) who) =
        expect (construction.profile.signalLaw.map
          (construction.signalSelector k))
          (fun choice => (construction.buildingBlock k).choiceValue
            choice who) := by
      symm
      exact Math.Probability.expect_map _ _ _
    _ = _ := by
      rw [construction.signalSelector_law k]
      exact (construction.buildingBlock k).expect_choiceValue who

/-! `w(yᴷ)`, in the normal coordinates where the paper defines it. -/
def KiloblockConstruction.normalTarget
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    NormalPlayer table → ℝ :=
  (construction.buildingBlock (Fin.last construction.blockCount)).w

def KiloblockConstruction.macroTrackingAt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (j : ℕ)
    (who : NormalPlayer table) : ℝ :=
  if hj : j < construction.blockCount + 1 then
    construction.trackingCorrection ⟨j, hj⟩ who
  else 0

/-! Expected corrected normal payoff after crossing blocks
`fuel-1,...,0`, with the final endpoint `y⁰` if all blocks advance. -/
def KiloblockConstruction.macroCorrectedValueFuel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) : ℕ → ℝ
  | 0 => construction.point ⟨0, by omega⟩ who
  | fuel + 1 =>
      construction.macroAdvanceAt fuel *
          construction.macroCorrectedValueFuel who fuel +
        ∑ owner, construction.macroAbsorbAt fuel owner *
          (NormalMatrix table who owner +
            construction.macroTrackingAt fuel who)

private theorem KiloblockConstruction.macroCorrectedValueFuel_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) (j : ℕ)
    (hj : j < construction.blockCount + 1)
    (hprevious : construction.macroCorrectedValueFuel who j =
      construction.point ⟨j, hj⟩ who +
        construction.trackingCorrection ⟨j, hj⟩ who) :
    construction.macroCorrectedValueFuel who (j + 1) =
      (construction.buildingBlock ⟨j, hj⟩).w who +
        construction.trackingCorrection ⟨j, hj⟩ who := by
  rw [KiloblockConstruction.macroCorrectedValueFuel, hprevious]
  have hadvance : construction.macroAdvanceAt j =
      construction.macroAdvanceProbability ⟨j, hj⟩ := by
    simp only [KiloblockConstruction.macroAdvanceAt, dif_pos hj]
  have habsorb : ∀ owner, construction.macroAbsorbAt j owner =
      construction.macroAbsorbProbability ⟨j, hj⟩ owner := by
    intro owner
    simp only [KiloblockConstruction.macroAbsorbAt, dif_pos hj]
  have htracking : construction.macroTrackingAt j who =
      construction.trackingCorrection ⟨j, hj⟩ who := by
    simp only [KiloblockConstruction.macroTrackingAt, dif_pos hj]
  rw [hadvance]
  simp_rw [habsorb, htracking, mul_add]
  rw [Finset.sum_add_distrib, ← Finset.sum_mul]
  have hbalance := construction.macro_balance ⟨j, hj⟩ who
  have htotal := construction.macroProbability_total ⟨j, hj⟩
  linear_combination -hbalance +
    htotal * construction.trackingCorrection ⟨j, hj⟩ who

theorem KiloblockConstruction.macroCorrectedValueFuel_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) (j : ℕ)
    (hj : j < construction.blockCount + 1) :
    construction.macroCorrectedValueFuel who (j + 1) =
      (construction.buildingBlock ⟨j, hj⟩).w who +
        construction.trackingCorrection ⟨j, hj⟩ who := by
  induction j with
  | zero =>
      apply construction.macroCorrectedValueFuel_step who 0 hj
      rw [KiloblockConstruction.macroCorrectedValueFuel,
        construction.trackingCorrection_zero]
      simp
  | succ j ih =>
      apply construction.macroCorrectedValueFuel_step who (j + 1) hj
      have hjcount : j < construction.blockCount := by omega
      have hvalue := ih (by omega)
      have htelescope := construction.point_add_trackingCorrection_succ
        j hjcount who
      exact hvalue.trans htelescope.symm

theorem KiloblockConstruction.macroCorrectedValueFuel_full
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    construction.macroCorrectedValueFuel who
        (construction.blockCount + 1) =
      construction.normalTarget who +
        construction.trackingCorrection
          (Fin.last construction.blockCount) who := by
  unfold KiloblockConstruction.normalTarget
  let last : Fin (construction.blockCount + 1) :=
    ⟨construction.blockCount, by omega⟩
  have hlast : Fin.last construction.blockCount = last := by
    apply Fin.ext
    rfl
  rw [hlast]
  exact construction.macroCorrectedValueFuel_eq who construction.blockCount
    (by omega)

theorem KiloblockConstruction.macroTrackingAt_abs_lt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (j : ℕ) (hj : j < construction.blockCount + 1)
    (who : NormalPlayer table) :
    |construction.macroTrackingAt j who| < ε := by
  rw [show construction.macroTrackingAt j who =
      construction.trackingCorrection ⟨j, hj⟩ who by
    simp only [KiloblockConstruction.macroTrackingAt, dif_pos hj]]
  exact construction.abs_trackingCorrection_lt ⟨j, hj⟩ who

private theorem KiloblockConstruction.macro_terminal_sum_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded negative : NormalPlayer table)
    (hnegative : NormalMatrix table negative excluded <
      -construction.negativeMargin)
    (j : ℕ) (hj : j < construction.blockCount + 1) :
    (∑ owner, construction.macroAbsorbAt j owner *
        (NormalMatrix table negative owner +
          construction.macroTrackingAt j negative)) ≤
      construction.macroAbsorbAt j excluded *
          (-construction.negativeMargin + ε) +
        (∑ owner ∈ Finset.univ.erase excluded,
          construction.macroAbsorbAt j owner) * (1 + ε) := by
  have htracking := construction.macroTrackingAt_abs_lt j hj negative
  calc
    (∑ owner, construction.macroAbsorbAt j owner *
        (NormalMatrix table negative owner +
          construction.macroTrackingAt j negative)) =
        construction.macroAbsorbAt j excluded *
            (NormalMatrix table negative excluded +
              construction.macroTrackingAt j negative) +
          ∑ owner ∈ Finset.univ.erase excluded,
            construction.macroAbsorbAt j owner *
              (NormalMatrix table negative owner +
                construction.macroTrackingAt j negative) := by
      rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
      ring
    _ ≤ construction.macroAbsorbAt j excluded *
          (-construction.negativeMargin + ε) +
        ∑ owner ∈ Finset.univ.erase excluded,
          construction.macroAbsorbAt j owner * (1 + ε) := by
      apply add_le_add
      · apply mul_le_mul_of_nonneg_left _
          (construction.macroAbsorbAt_nonneg j excluded)
        linarith [(abs_lt.mp htracking).2]
      · apply Finset.sum_le_sum
        intro owner _
        apply mul_le_mul_of_nonneg_left _
          (construction.macroAbsorbAt_nonneg j owner)
        have hmatrix := (abs_le.mp
          (construction.normalMatrix_bounded negative owner)).2
        linarith [(abs_lt.mp htracking).2]
    _ = construction.macroAbsorbAt j excluded *
          (-construction.negativeMargin + ε) +
        (∑ owner ∈ Finset.univ.erase excluded,
          construction.macroAbsorbAt j owner) * (1 + ε) := by
      congr 1
      rw [Finset.sum_mul]

private theorem KiloblockConstruction.macroCorrectedValueFuel_upper
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded negative : NormalPlayer table)
    (hnegative : NormalMatrix table negative excluded <
      -construction.negativeMargin) :
    ∀ fuel, fuel ≤ construction.blockCount + 1 →
      construction.macroCorrectedValueFuel negative fuel ≤
        (-construction.negativeMargin + ε) *
            construction.macroOwnerMassFuel excluded fuel +
          (1 + ε) * construction.macroOtherMassFuel excluded fuel +
          construction.macroSurvivalFuel fuel
  | 0, _ => by
      simp only [KiloblockConstruction.macroCorrectedValueFuel,
        KiloblockConstruction.macroOwnerMassFuel,
        KiloblockConstruction.macroOtherMassFuel,
        KiloblockConstruction.macroSurvivalFuel, mul_zero, add_zero]
      simpa only [zero_add] using
        (abs_le.mp (abs_le_one_of_mem_D construction.normalMatrix_bounded
          (construction.point_boundary ⟨0, by omega⟩).1 negative)).2
  | fuel + 1, hfuel => by
      have hfuelLt : fuel < construction.blockCount + 1 := by omega
      have hprevious := construction.macroCorrectedValueFuel_upper
        excluded negative hnegative fuel (by omega)
      have hterminal := construction.macro_terminal_sum_le
        excluded negative hnegative fuel hfuelLt
      rw [KiloblockConstruction.macroCorrectedValueFuel]
      calc
        construction.macroAdvanceAt fuel *
              construction.macroCorrectedValueFuel negative fuel +
            ∑ owner, construction.macroAbsorbAt fuel owner *
              (NormalMatrix table negative owner +
                construction.macroTrackingAt fuel negative) ≤
            construction.macroAdvanceAt fuel *
                ((-construction.negativeMargin + ε) *
                    construction.macroOwnerMassFuel excluded fuel +
                  (1 + ε) *
                    construction.macroOtherMassFuel excluded fuel +
                  construction.macroSurvivalFuel fuel) +
              (construction.macroAbsorbAt fuel excluded *
                  (-construction.negativeMargin + ε) +
                (∑ owner ∈ Finset.univ.erase excluded,
                  construction.macroAbsorbAt fuel owner) * (1 + ε)) := by
          exact add_le_add
            (mul_le_mul_of_nonneg_left hprevious
              (construction.macroAdvanceAt_nonneg fuel))
            hterminal
        _ = (-construction.negativeMargin + ε) *
                construction.macroOwnerMassFuel excluded (fuel + 1) +
              (1 + ε) *
                construction.macroOtherMassFuel excluded (fuel + 1) +
              construction.macroSurvivalFuel (fuel + 1) := by
          rw [KiloblockConstruction.macroOwnerMassFuel,
            KiloblockConstruction.macroOtherMassFuel,
            KiloblockConstruction.macroSurvivalFuel]
          ring

theorem KiloblockConstruction.negativeMargin_lt_one
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.negativeMargin < 1 := by
  let k : Fin (construction.blockCount + 1) := ⟨0, by omega⟩
  obtain ⟨owner, _, _⟩ :=
    (Finset.sum_pos_iff_of_nonneg (fun owner _ =>
      (construction.buildingBlock k).z.singleton_nonneg owner)).mp
      (construction.buildingBlock k).nontrivial
  obtain ⟨negative, hnegative⟩ := construction.column_negative owner
  have hlower := (abs_le.mp
    (construction.normalMatrix_bounded negative owner)).1
  linarith

theorem KiloblockConstruction.epsilon_lt_macroOther_add_survival
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ε < construction.macroOtherMassFuel excluded
          (construction.blockCount + 1) +
        construction.macroSurvivalFuel (construction.blockCount + 1) := by
  obtain ⟨negative, hnegative⟩ := construction.column_negative excluded
  let fuel := construction.blockCount + 1
  have hupper := construction.macroCorrectedValueFuel_upper
    excluded negative hnegative fuel le_rfl
  have hvalue := construction.macroCorrectedValueFuel_full negative
  rw [hvalue] at hupper
  have htarget : 0 ≤ construction.normalTarget negative :=
    (construction.buildingBlock (Fin.last construction.blockCount)).w_boundary.1.2
      negative
  have hcorrection := construction.abs_trackingCorrection_lt
    (Fin.last construction.blockCount) negative
  have hlower : -ε < construction.normalTarget negative +
      construction.trackingCorrection (Fin.last construction.blockCount) negative := by
    linarith [(abs_lt.mp hcorrection).1]
  have howner := construction.macroOwnerMassFuel_nonneg excluded fuel
  have hother := construction.macroOtherMassFuel_nonneg excluded fuel
  have hsurvival := construction.macroSurvivalFuel_nonneg fuel
  have htotal := construction.macroMassFuel_total fuel le_rfl
  rw [← construction.macroOwner_add_otherMassFuel excluded fuel] at htotal
  have hmarginOne := construction.negativeMargin_lt_one
  have hcoefficient : (5 : ℝ) ≤
      ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ) := by
    have hcard := construction.two_le_normalPlayer_card
    exact_mod_cast (show 5 ≤ 2 * Fintype.card (NormalPlayer table) + 1 by omega)
  have hmarginFive : 5 * ε < construction.negativeMargin := by
    have hmul := mul_le_mul_of_nonneg_right hcoefficient
      construction.epsilon_pos.le
    exact lt_of_le_of_lt hmul construction.accuracy_below_margin
  have hownerCoefficient :
      (construction.negativeMargin + 1) *
          construction.macroOtherMassFuel excluded fuel ≤
        2 * construction.macroOtherMassFuel excluded fuel := by
    exact mul_le_mul_of_nonneg_right (by linarith) hother
  have hsurvivalCoefficient :
      (construction.negativeMargin - ε + 1) *
          construction.macroSurvivalFuel fuel ≤
        2 * construction.macroSurvivalFuel fuel := by
    exact mul_le_mul_of_nonneg_right (by linarith [construction.epsilon_pos])
      hsurvival
  nlinarith

def KiloblockConstruction.continueAdvanceProbabilityWeight
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  construction.macroAdvanceProbability k +
    if (construction.attempt k excluded).continuation = .advance then
      construction.macroAbsorbProbability k excluded
    else 0

def KiloblockConstruction.continueExitProbabilityWeight
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  construction.continueAdvanceProbabilityWeight k excluded +
    ∑ owner ∈ Finset.univ.erase excluded,
      construction.macroAbsorbProbability k owner

theorem KiloblockConstruction.continueAdvanceProbabilityWeight_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    0 ≤ construction.continueAdvanceProbabilityWeight k excluded := by
  unfold KiloblockConstruction.continueAdvanceProbabilityWeight
  apply add_nonneg (construction.macroAdvanceProbability_nonneg k)
  split
  · exact construction.macroAbsorbProbability_nonneg k excluded
  · exact le_rfl

theorem KiloblockConstruction.continueExitProbabilityWeight_pos
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    0 < construction.continueExitProbabilityWeight k excluded := by
  unfold KiloblockConstruction.continueExitProbabilityWeight
  have hadvance := construction.continueAdvanceProbabilityWeight_nonneg k excluded
  have hother : 0 ≤ ∑ owner ∈ Finset.univ.erase excluded,
      construction.macroAbsorbProbability k owner :=
    Finset.sum_nonneg fun owner _ =>
      construction.macroAbsorbProbability_nonneg k owner
  apply lt_of_le_of_ne (add_nonneg hadvance hother)
  intro hzero
  have hadvanceZero : construction.continueAdvanceProbabilityWeight k excluded = 0 := by
    nlinarith
  have hotherZero : ∑ owner ∈ Finset.univ.erase excluded,
      construction.macroAbsorbProbability k owner = 0 := by
    nlinarith
  cases hbranch : (construction.attempt k excluded).continuation with
  | advance =>
      have htotal := construction.macroProbability_total k
      have hexcluded := construction.macroAbsorbProbability_nonneg k excluded
      have hadvanceBase := construction.macroAdvanceProbability_nonneg k
      unfold KiloblockConstruction.continueAdvanceProbabilityWeight at hadvanceZero
      simp only [hbranch, if_pos] at hadvanceZero
      have hall : ∑ owner, construction.macroAbsorbProbability k owner =
          construction.macroAbsorbProbability k excluded +
            ∑ owner ∈ Finset.univ.erase excluded,
              construction.macroAbsorbProbability k owner := by
        rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
        ring
      rw [hall, hotherZero] at htotal
      nlinarith
  | restart =>
      have htotal := construction.macroProbability_total k
      unfold KiloblockConstruction.continueAdvanceProbabilityWeight at hadvanceZero
      simp only [hbranch, reduceCtorEq, if_false, add_zero] at hadvanceZero
      have hall : ∑ owner, construction.macroAbsorbProbability k owner =
          construction.macroAbsorbProbability k excluded +
            ∑ owner ∈ Finset.univ.erase excluded,
              construction.macroAbsorbProbability k owner := by
        rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
        ring
      rw [hall, hotherZero, hadvanceZero, zero_add, add_zero] at htotal
      obtain ⟨negative, hnegative⟩ := construction.column_negative excluded
      have hbalance := construction.macro_balance k negative
      rw [hadvanceZero] at hbalance
      have hsum : (∑ owner, construction.macroAbsorbProbability k owner *
          NormalMatrix table negative owner) =
          construction.macroAbsorbProbability k excluded *
            NormalMatrix table negative excluded := by
        rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
        have hzeroOther : ∀ owner ∈ Finset.univ.erase excluded,
            construction.macroAbsorbProbability k owner = 0 := by
          intro owner howner
          exact (Finset.sum_eq_zero_iff_of_nonneg (fun other _ =>
            construction.macroAbsorbProbability_nonneg k other)).mp
              hotherZero owner howner
        rw [Finset.sum_eq_zero fun owner howner => by
          rw [hzeroOther owner howner, zero_mul], zero_add]
      rw [hsum, htotal, one_mul] at hbalance
      have hwNonneg := (construction.buildingBlock k).w_boundary.1.2 negative
      linarith [construction.negativeMargin_pos]

def KiloblockConstruction.continueMacroAdvanceProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  construction.continueAdvanceProbabilityWeight k excluded /
    construction.continueExitProbabilityWeight k excluded

def KiloblockConstruction.continueMacroAbsorbProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded owner : NormalPlayer table) : ℝ :=
  if owner = excluded then 0 else
    construction.macroAbsorbProbability k owner /
      construction.continueExitProbabilityWeight k excluded

theorem KiloblockConstruction.continueMacroProbability_total
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueMacroAdvanceProbability k excluded +
      ∑ owner, construction.continueMacroAbsorbProbability k excluded owner = 1 := by
  unfold KiloblockConstruction.continueMacroAdvanceProbability
    KiloblockConstruction.continueMacroAbsorbProbability
  have hsum : (∑ owner,
      if owner = excluded then 0 else
        construction.macroAbsorbProbability k owner /
          construction.continueExitProbabilityWeight k excluded) =
      ∑ owner ∈ Finset.univ.erase excluded,
        construction.macroAbsorbProbability k owner /
          construction.continueExitProbabilityWeight k excluded := by
    let summand := fun owner : NormalPlayer table =>
      if owner = excluded then 0 else
        construction.macroAbsorbProbability k owner /
          construction.continueExitProbabilityWeight k excluded
    calc
      (∑ owner, summand owner) =
          ∑ owner ∈ Finset.univ.erase excluded, summand owner :=
        (Finset.sum_erase (s := Finset.univ) (f := summand) (by
          simp [summand])).symm
      _ = _ := by
        apply Finset.sum_congr rfl
        intro owner howner
        simp only [summand, if_neg (Finset.ne_of_mem_erase howner)]
  rw [hsum, ← Finset.sum_div, ← add_div]
  exact div_self (ne_of_gt
    (construction.continueExitProbabilityWeight_pos k excluded))

def KiloblockConstruction.macroOtherAbsorptionProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  ∑ owner ∈ Finset.univ.erase excluded,
    construction.macroAbsorbProbability k owner

theorem KiloblockConstruction.negative_owner_charge_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded negative : NormalPlayer table)
    (hnegative : NormalMatrix table negative excluded <
      -construction.negativeMargin) :
    construction.negativeMargin *
        construction.macroAbsorbProbability k excluded ≤
      construction.point k negative -
          (construction.buildingBlock k).w negative +
        construction.macroOtherAbsorptionProbability k excluded := by
  let p := construction.macroAbsorbProbability k
  let y := construction.point k negative
  have htotal := construction.macroProbability_total k
  have hbalance := construction.macro_balance k negative
  have hsum : (∑ owner, p owner * NormalMatrix table negative owner) =
      p excluded * NormalMatrix table negative excluded +
        ∑ owner ∈ Finset.univ.erase excluded,
          p owner * NormalMatrix table negative owner := by
    rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
    ring
  have hprobability : (∑ owner, p owner) = p excluded +
      ∑ owner ∈ Finset.univ.erase excluded, p owner := by
    rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
    ring
  have hidentity : y - (construction.buildingBlock k).w negative =
      p excluded * (y - NormalMatrix table negative excluded) +
        ∑ owner ∈ Finset.univ.erase excluded,
          p owner * (y - NormalMatrix table negative owner) := by
    dsimp only [p, y] at htotal hbalance hsum hprobability ⊢
    rw [hsum] at hbalance
    simp_rw [mul_sub]
    rw [Finset.sum_sub_distrib, ← Finset.sum_mul]
    rw [hprobability] at htotal
    linear_combination -hbalance - htotal * construction.point k negative
  have hexcluded : construction.negativeMargin * p excluded ≤
      p excluded * (y - NormalMatrix table negative excluded) := by
    rw [mul_comm construction.negativeMargin (p excluded)]
    apply mul_le_mul_of_nonneg_left _
      (construction.macroAbsorbProbability_nonneg k excluded)
    have hyNonneg := (construction.point_boundary k).1.2 negative
    dsimp only [y]
    linarith
  have hother : -(∑ owner ∈ Finset.univ.erase excluded, p owner) ≤
      ∑ owner ∈ Finset.univ.erase excluded,
        p owner * (y - NormalMatrix table negative owner) := by
    rw [← Finset.sum_neg_distrib]
    apply Finset.sum_le_sum
    intro owner _
    have hp := construction.macroAbsorbProbability_nonneg k owner
    have hyNonneg := (construction.point_boundary k).1.2 negative
    have hmatrix := (abs_le.mp
      (construction.normalMatrix_bounded negative owner)).2
    dsimp only [p, y]
    nlinarith
  rw [hidentity]
  unfold KiloblockConstruction.macroOtherAbsorptionProbability
  dsimp only [p] at hexcluded hother ⊢
  linarith

def KiloblockConstruction.totalMacroOtherAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) : ℝ :=
  ∑ k, construction.macroOtherAbsorptionProbability k excluded

def KiloblockConstruction.totalMacroOwnerAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (owner : NormalPlayer table) : ℝ :=
  ∑ k, construction.macroAbsorbProbability k owner

theorem KiloblockConstruction.totalMacroAbsorption_decompose
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    construction.totalMacroAbsorption =
      construction.totalMacroOwnerAbsorption excluded +
        construction.totalMacroOtherAbsorption excluded := by
  unfold KiloblockConstruction.totalMacroAbsorption
    KiloblockConstruction.totalMacroOwnerAbsorption
    KiloblockConstruction.totalMacroOtherAbsorption
    KiloblockConstruction.macroOtherAbsorptionProbability
  rw [← Finset.sum_add_distrib]
  apply Finset.sum_congr rfl
  intro k _
  rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
  ring

theorem KiloblockConstruction.sum_point_sub_w_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    (∑ k, (construction.point k who -
        (construction.buildingBlock k).w who)) =
      construction.point ⟨0, by omega⟩ who -
        (construction.buildingBlock (Fin.last construction.blockCount)).w who -
        construction.trackingCorrection
          (Fin.last construction.blockCount) who := by
  let y : ℕ → ℝ := fun j =>
    if hj : j < construction.blockCount + 1 then
      construction.point ⟨j, hj⟩ who
    else 0
  let w : ℕ → ℝ := fun j =>
    if hj : j < construction.blockCount + 1 then
      (construction.buildingBlock ⟨j, hj⟩).w who
    else 0
  have hgap : (∑ k, (construction.point k who -
      (construction.buildingBlock k).w who)) =
      ∑ j ∈ Finset.range (construction.blockCount + 1),
        (y j - w j) := by
    rw [← Fin.sum_univ_eq_sum_range]
    apply Finset.sum_congr rfl
    intro k _
    simp only [y, w, dif_pos k.isLt]
  have htracking : construction.trackingCorrection
      (Fin.last construction.blockCount) who =
      ∑ j ∈ Finset.range construction.blockCount,
        (w j - y (j + 1)) := by
    unfold KiloblockConstruction.trackingCorrection
    change (∑ j ∈ Finset.range construction.blockCount,
      construction.trackingIncrement j who) = _
    apply Finset.sum_congr rfl
    intro j hj
    have hjcount := Finset.mem_range.mp hj
    simp only [KiloblockConstruction.trackingIncrement, hjcount,
      dif_pos, y, w]
    rw [dif_pos (by omega), dif_pos (by omega)]
  have hfinite :
      (∑ j ∈ Finset.range (construction.blockCount + 1), (y j - w j)) +
          ∑ j ∈ Finset.range construction.blockCount, (w j - y (j + 1)) =
        y 0 - w construction.blockCount := by
    rw [Finset.sum_range_succ]
    have hterm : (∑ j ∈ Finset.range construction.blockCount,
        ((y j - w j) + (w j - y (j + 1)))) =
        ∑ j ∈ Finset.range construction.blockCount,
          (y j - y (j + 1)) := by
      apply Finset.sum_congr rfl
      intro j _
      ring
    calc
      ((∑ j ∈ Finset.range construction.blockCount, (y j - w j)) +
            (y construction.blockCount - w construction.blockCount)) +
          ∑ j ∈ Finset.range construction.blockCount,
            (w j - y (j + 1)) =
          ((∑ j ∈ Finset.range construction.blockCount, (y j - w j)) +
            ∑ j ∈ Finset.range construction.blockCount,
              (w j - y (j + 1))) +
            (y construction.blockCount - w construction.blockCount) := by
        ring
      _ = (∑ j ∈ Finset.range construction.blockCount,
            ((y j - w j) + (w j - y (j + 1)))) +
          (y construction.blockCount - w construction.blockCount) := by
        rw [Finset.sum_add_distrib]
      _ = (∑ j ∈ Finset.range construction.blockCount,
            (y j - y (j + 1))) +
          (y construction.blockCount - w construction.blockCount) := by
        rw [hterm]
      _ = y 0 - w construction.blockCount := by
        rw [Finset.sum_range_sub']
        ring
  rw [hgap, htracking]
  have hyzero : y 0 = construction.point ⟨0, by omega⟩ who := by
    simp [y]
  have hwlast : w construction.blockCount =
      (construction.buildingBlock (Fin.last construction.blockCount)).w who := by
    dsimp only [w]
    rw [dif_pos (by omega)]
    congr 2
  rw [hyzero, hwlast] at hfinite
  linarith

theorem KiloblockConstruction.sum_point_sub_w_lt_one_add_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    (∑ k, (construction.point k who -
      (construction.buildingBlock k).w who)) < 1 + ε := by
  rw [construction.sum_point_sub_w_eq who]
  have hpoint := (abs_le.mp (abs_le_one_of_mem_D
    construction.normalMatrix_bounded
    (construction.point_boundary ⟨0, by omega⟩).1 who)).2
  have hw := (construction.buildingBlock
    (Fin.last construction.blockCount)).w_boundary.1.2 who
  have htracking := construction.abs_trackingCorrection_lt
    (Fin.last construction.blockCount) who
  linarith [(abs_lt.mp htracking).1]

theorem KiloblockConstruction.five_mul_epsilon_lt_negativeMargin
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    5 * ε < construction.negativeMargin := by
  have hcard := construction.two_le_normalPlayer_card
  have hcoefficient : (5 : ℝ) ≤
      ((2 * Fintype.card (NormalPlayer table) + 1 : ℕ) : ℝ) := by
    exact_mod_cast (show 5 ≤ 2 * Fintype.card (NormalPlayer table) + 1 by omega)
  have hmul := mul_le_mul_of_nonneg_right hcoefficient
    construction.epsilon_pos.le
  exact lt_of_le_of_lt hmul construction.accuracy_below_margin

theorem KiloblockConstruction.ownerCharge_le_gap_add_other
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded negative : NormalPlayer table)
    (hnegative : NormalMatrix table negative excluded <
      -construction.negativeMargin) :
    construction.negativeMargin *
        construction.totalMacroOwnerAbsorption excluded ≤
      (∑ k, (construction.point k negative -
        (construction.buildingBlock k).w negative)) +
        construction.totalMacroOtherAbsorption excluded := by
  have hsum := Finset.sum_le_sum (s := Finset.univ) fun k _ =>
    construction.negative_owner_charge_le k excluded negative hnegative
  change construction.negativeMargin *
      (∑ k, construction.macroAbsorbProbability k excluded) ≤
    (∑ k, (construction.point k negative -
      (construction.buildingBlock k).w negative)) +
      ∑ k, construction.macroOtherAbsorptionProbability k excluded
  calc
    construction.negativeMargin *
        (∑ k, construction.macroAbsorbProbability k excluded) =
        ∑ k, construction.negativeMargin *
          construction.macroAbsorbProbability k excluded := by
      rw [Finset.mul_sum]
    _ ≤ ∑ k, (construction.point k negative -
        (construction.buildingBlock k).w negative +
          construction.macroOtherAbsorptionProbability k excluded) := hsum
    _ = (∑ k, (construction.point k negative -
        (construction.buildingBlock k).w negative)) +
          ∑ k, construction.macroOtherAbsorptionProbability k excluded := by
      rw [Finset.sum_add_distrib]

theorem KiloblockConstruction.inv_epsilon_lt_totalMacroOtherAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ε⁻¹ < construction.totalMacroOtherAbsorption excluded := by
  obtain ⟨negative, hnegative⟩ := construction.column_negative excluded
  have hcharge := construction.ownerCharge_le_gap_add_other
    excluded negative hnegative
  have hgap := construction.sum_point_sub_w_lt_one_add_epsilon negative
  have htotal := construction.inv_epsilon_sq_lt_totalMacroAbsorption
  rw [construction.totalMacroAbsorption_decompose excluded] at htotal
  have hmargin := construction.five_mul_epsilon_lt_negativeMargin
  have hmarginOne := construction.negativeMargin_lt_one
  let r := ε⁻¹
  have hrpos : 0 < r := inv_pos.mpr construction.epsilon_pos
  have her : ε * r = 1 := by
    dsimp only [r]
    simpa only [mul_comm] using
      inv_mul_cancel₀ (ne_of_gt construction.epsilon_pos)
  have hrsq : (ε ^ 2)⁻¹ = r ^ 2 := by
    dsimp only [r]
    rw [inv_pow]
  rw [hrsq] at htotal
  by_contra hnot
  have hother : construction.totalMacroOtherAbsorption excluded ≤ r :=
    le_of_not_gt hnot
  have howner : r ^ 2 - r <
      construction.totalMacroOwnerAbsorption excluded := by
    linarith
  have hrFive : 5 < r := by
    have hεfifth : ε < (1 : ℝ) / 5 := by linarith
    have := one_div_lt_one_div_of_lt construction.epsilon_pos hεfifth
    norm_num at this ⊢
    simpa only [r, one_div] using this
  have hrgap : 0 < r ^ 2 - r := by nlinarith
  have hownerPos : 0 < construction.totalMacroOwnerAbsorption excluded :=
    hrgap.trans howner
  have hproduct : (5 * ε) * (r ^ 2 - r) <
      construction.negativeMargin *
        construction.totalMacroOwnerAbsorption excluded := by
    exact mul_lt_mul_of_pos hmargin howner
      (mul_pos (by norm_num) construction.epsilon_pos) hownerPos
  have hlower : 1 + ε + r < (5 * ε) * (r ^ 2 - r) := by
    nlinarith
  linarith

theorem KiloblockConstruction.continueExitProbabilityWeight_le_one
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueExitProbabilityWeight k excluded ≤ 1 := by
  have htotal := construction.macroProbability_total k
  have hall : (∑ owner, construction.macroAbsorbProbability k owner) =
      construction.macroAbsorbProbability k excluded +
        ∑ owner ∈ Finset.univ.erase excluded,
          construction.macroAbsorbProbability k owner := by
    rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
    ring
  rw [hall] at htotal
  unfold KiloblockConstruction.continueExitProbabilityWeight
    KiloblockConstruction.continueAdvanceProbabilityWeight
  split
  · linarith
  · linarith [construction.macroAbsorbProbability_nonneg k excluded]

theorem KiloblockConstruction.continueMacroAdvanceProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    0 ≤ construction.continueMacroAdvanceProbability k excluded := by
  exact div_nonneg
    (construction.continueAdvanceProbabilityWeight_nonneg k excluded)
    (construction.continueExitProbabilityWeight_pos k excluded).le

theorem KiloblockConstruction.continueMacroAbsorbProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded owner : NormalPlayer table) :
    0 ≤ construction.continueMacroAbsorbProbability k excluded owner := by
  unfold KiloblockConstruction.continueMacroAbsorbProbability
  split
  · exact le_rfl
  · exact div_nonneg (construction.macroAbsorbProbability_nonneg k owner)
      (construction.continueExitProbabilityWeight_pos k excluded).le

def KiloblockConstruction.continueMacroAbsorptionProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  ∑ owner, construction.continueMacroAbsorbProbability k excluded owner

theorem KiloblockConstruction.continueMacroAbsorptionProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    0 ≤ construction.continueMacroAbsorptionProbability k excluded :=
  Finset.sum_nonneg fun owner _ =>
    construction.continueMacroAbsorbProbability_nonneg k excluded owner

theorem KiloblockConstruction.continueMacroAbsorptionProbability_le_one
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueMacroAbsorptionProbability k excluded ≤ 1 := by
  have htotal := construction.continueMacroProbability_total k excluded
  have hadvance := construction.continueMacroAdvanceProbability_nonneg k excluded
  unfold KiloblockConstruction.continueMacroAbsorptionProbability
  linarith

theorem KiloblockConstruction.macroOther_le_continueMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.macroOtherAbsorptionProbability k excluded ≤
      construction.continueMacroAbsorptionProbability k excluded := by
  have hsum : construction.continueMacroAbsorptionProbability k excluded =
      construction.macroOtherAbsorptionProbability k excluded /
        construction.continueExitProbabilityWeight k excluded := by
    unfold KiloblockConstruction.continueMacroAbsorptionProbability
      KiloblockConstruction.continueMacroAbsorbProbability
      KiloblockConstruction.macroOtherAbsorptionProbability
    let summand := fun owner : NormalPlayer table =>
      if owner = excluded then 0 else
        construction.macroAbsorbProbability k owner /
          construction.continueExitProbabilityWeight k excluded
    have herase : (∑ owner, summand owner) =
        ∑ owner ∈ Finset.univ.erase excluded,
          construction.macroAbsorbProbability k owner /
            construction.continueExitProbabilityWeight k excluded := by
      calc
        (∑ owner, summand owner) =
            ∑ owner ∈ Finset.univ.erase excluded, summand owner :=
          (Finset.sum_erase (s := Finset.univ) (f := summand) (by
            simp [summand])).symm
        _ = _ := by
          apply Finset.sum_congr rfl
          intro owner howner
          simp only [summand, if_neg (Finset.ne_of_mem_erase howner)]
    rw [herase, ← Finset.sum_div]
  rw [hsum]
  exact (le_div_iff₀
    (construction.continueExitProbabilityWeight_pos k excluded)).2
      (mul_le_of_le_one_right
        (Finset.sum_nonneg fun owner _ =>
          construction.macroAbsorbProbability_nonneg k owner)
        (construction.continueExitProbabilityWeight_le_one k excluded))

def KiloblockConstruction.totalContinueMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) : ℝ :=
  ∑ k, construction.continueMacroAbsorptionProbability k excluded

theorem KiloblockConstruction.inv_epsilon_lt_totalContinueMacroAbsorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ε⁻¹ < construction.totalContinueMacroAbsorption excluded := by
  exact (construction.inv_epsilon_lt_totalMacroOtherAbsorption excluded).trans_le
    (Finset.sum_le_sum fun k _ =>
      construction.macroOther_le_continueMacroAbsorption k excluded)

def KiloblockConstruction.continueMacroSurvivalProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) : ℝ :=
  ∏ k, construction.continueMacroAdvanceProbability k excluded

theorem KiloblockConstruction.continueMacroAdvance_eq_one_sub_absorption
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueMacroAdvanceProbability k excluded =
      1 - construction.continueMacroAbsorptionProbability k excluded := by
  have htotal := construction.continueMacroProbability_total k excluded
  unfold KiloblockConstruction.continueMacroAbsorptionProbability
  linarith

theorem KiloblockConstruction.continueMacroSurvivalProbability_lt_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    construction.continueMacroSurvivalProbability excluded < ε := by
  let charge : ℕ → ℝ := fun j =>
    if hj : j < construction.blockCount + 1 then
      construction.continueMacroAbsorptionProbability ⟨j, hj⟩ excluded
    else 0
  have hcharge0 : ∀ j, 0 ≤ charge j := by
    intro j
    simp only [charge]
    split
    · exact construction.continueMacroAbsorptionProbability_nonneg _ excluded
    · exact le_rfl
  have hcharge1 : ∀ j, charge j ≤ 1 := by
    intro j
    simp only [charge]
    split
    · exact construction.continueMacroAbsorptionProbability_le_one _ excluded
    · exact zero_le_one
  have hmain := Math.prod_one_sub_mul_one_add_sum_range_le_one
    charge hcharge0 hcharge1 0 (construction.blockCount + 1)
  have hsum : (∑ j ∈ Finset.range (construction.blockCount + 1), charge j) =
      construction.totalContinueMacroAbsorption excluded := by
    rw [← Fin.sum_univ_eq_sum_range]
    unfold KiloblockConstruction.totalContinueMacroAbsorption
    apply Finset.sum_congr rfl
    intro k _
    rw [show charge k.1 =
        construction.continueMacroAbsorptionProbability k excluded by
      simp only [charge, dif_pos k.isLt]]
  have hprod : (∏ j ∈ Finset.range (construction.blockCount + 1),
      (1 - charge j)) =
      construction.continueMacroSurvivalProbability excluded := by
    rw [← Fin.prod_univ_eq_prod_range]
    unfold KiloblockConstruction.continueMacroSurvivalProbability
    apply Finset.prod_congr rfl
    intro k _
    simp only [charge, dif_pos k.isLt]
    exact (construction.continueMacroAdvance_eq_one_sub_absorption
      k excluded).symm
  simp only [zero_add] at hmain
  rw [hsum, hprod] at hmain
  have hfactor : 0 < 1 + construction.totalContinueMacroAbsorption excluded := by
    have hinv : 0 < ε⁻¹ := inv_pos.mpr construction.epsilon_pos
    linarith [construction.inv_epsilon_lt_totalContinueMacroAbsorption excluded]
  have hepsilonFactor : 1 < ε *
      (1 + construction.totalContinueMacroAbsorption excluded) := by
    have hmul : 1 < ε *
        construction.totalContinueMacroAbsorption excluded := by
      rw [← inv_mul_cancel₀ (ne_of_gt construction.epsilon_pos)]
      simpa only [mul_comm] using mul_lt_mul_of_pos_left
        (construction.inv_epsilon_lt_totalContinueMacroAbsorption excluded)
        construction.epsilon_pos
    nlinarith [construction.epsilon_pos]
  by_contra hnot
  have hεle : ε ≤ construction.continueMacroSurvivalProbability excluded :=
    le_of_not_gt hnot
  have := mul_le_mul_of_nonneg_right hεle hfactor.le
  linarith

/-! The actual one-stage kernel on the finite schedule state after a normal
player is forced to Continue. This is derived from the strategy and the
schedule automaton; it is not an additional semantic field of the
construction. -/
def KiloblockConstruction.continueModeKernel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    KiloblockMode table construction.blockCount →
      PMF (KiloblockMode table construction.blockCount)
  | .draw (.choose k) =>
      construction.profile.signalLaw.map fun signal =>
        .active k (construction.signalSelector k signal)
          (construction.mesh k)
  | .draw (.resume k choice remaining) =>
      PMF.pure (.active k choice remaining)
  | .draw .final => PMF.pure .finalActive
  | .active k choice remaining =>
      let after := if remaining ≤ 1 then
          .draw (phaseAfterAttempt table
            (fun block owner =>
              (construction.attempt block owner).continuation)
            k choice)
        else .draw (.resume k choice (remaining - 1))
      match choice with
      | none => PMF.pure after
      | some owner =>
          if owner = excluded then PMF.pure after else
            (quittingMeshHazardCoin
                (construction.attempt k owner).quitWeight
                (construction.mesh k)
                (construction.attempt k owner).quitWeight_pos.le
                (construction.attempt k owner).quitWeight_lt_one).bind
              fun quits => if quits then
                PMF.pure (.absorbed (some k))
              else PMF.pure after
  | .finalActive => PMF.pure (.draw .final)
  | .absorbed origin => PMF.pure (.absorbed origin)

def KiloblockConstruction.continueProfile
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile :=
  Function.update construction.profile.strategy excluded.1
    (fun _ _ => PMF.pure false)

/-! Conditional law of the next schedule state at an actual history. -/
def KiloblockConstruction.historyModeStepDist
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    PMF (KiloblockMode table construction.blockCount) :=
  ((publicQuittingGame table
      construction.profile.signalLaw).stageActionDist strategy history).bind
    fun action =>
      ((publicQuittingGame table construction.profile.signalLaw).transition
          history.2 action).map fun nextState =>
        construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)

def KiloblockConstruction.continueFiniteModeKernel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    KiloblockFiniteMode construction →
      PMF (KiloblockFiniteMode construction) :=
  fun mode =>
    (construction.continueModeKernel excluded
      (construction.modeOfFinite mode)).map construction.finiteMode

/-! The one-stage kernel of the prescribed profile itself.  This differs
from `continueModeKernel` only when the selected owner is the player forced
to Continue there: under the prescribed profile every selected owner uses
the block's mesh coin. -/
def KiloblockConstruction.profileModeKernel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockMode table construction.blockCount →
      PMF (KiloblockMode table construction.blockCount)
  | .draw (.choose k) =>
      construction.profile.signalLaw.map fun signal =>
        .active k (construction.signalSelector k signal)
          (construction.mesh k)
  | .draw (.resume k choice remaining) =>
      PMF.pure (.active k choice remaining)
  | .draw .final => PMF.pure .finalActive
  | .active k choice remaining =>
      let after := if remaining ≤ 1 then
          .draw (phaseAfterAttempt table
            (fun block owner =>
              (construction.attempt block owner).continuation)
            k choice)
        else .draw (.resume k choice (remaining - 1))
      match choice with
      | none => PMF.pure after
      | some owner =>
          (quittingMeshHazardCoin
              (construction.attempt k owner).quitWeight
              (construction.mesh k)
              (construction.attempt k owner).quitWeight_pos.le
              (construction.attempt k owner).quitWeight_lt_one).bind
            fun quits => if quits then
              PMF.pure (.absorbed (some k))
            else PMF.pure after
  | .finalActive => PMF.pure (.draw .final)
  | .absorbed origin => PMF.pure (.absorbed origin)

def KiloblockConstruction.profileFiniteModeKernel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode construction →
      PMF (KiloblockFiniteMode construction) :=
  fun mode =>
    (construction.profileModeKernel
      (construction.modeOfFinite mode)).map construction.finiteMode

/-! The time-`t` marginal of the actual schedule state. -/
def KiloblockConstruction.modeDist
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (t : ℕ) : PMF (KiloblockFiniteMode construction) :=
  ((publicQuittingGame table construction.profile.signalLaw).histDist
      strategy .draw t).map fun history =>
    construction.finiteMode (construction.mode t history)

def publicAllContinueAction
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) :
    (publicQuittingGame table signalLaw).JointAct := fun _ => false

def publicSingleQuitAction
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal)
    (owner : ι) (quits : Bool) :
    (publicQuittingGame table signalLaw).JointAct :=
  Function.update (fun _ => false) owner quits

def rawSingleQuitAction (owner : ι) (quits : Bool) : ι → Bool :=
  Function.update (fun _ => false) owner quits

theorem publicSingleQuitAction_apply_eq_raw
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal)
    (owner : ι) (quits : Bool) (who : ι) :
    publicSingleQuitAction table signalLaw owner quits who =
      rawSingleQuitAction owner quits who := by
  rfl

omit [Fintype ι] in
@[simp]
theorem rawSingleQuitAction_false (owner who : ι) :
    rawSingleQuitAction owner false who = false := by
  by_cases hwho : who = owner
  · subst who
    simp [rawSingleQuitAction]
  · rw [rawSingleQuitAction, Function.update_of_ne hwho]

theorem rawSingleQuitAction_true_quitters (owner : ι) :
    ({who | rawSingleQuitAction owner true who = true} : Finset ι) =
      {owner} := by
  ext who
  by_cases hwho : who = owner
  · subst who
    simp [rawSingleQuitAction]
  · simp only [Finset.mem_filter, Finset.mem_univ, true_and,
      Finset.mem_singleton]
    rw [rawSingleQuitAction, Function.update_of_ne hwho]
    simp [hwho]

theorem publicQuittingGame_transition_publicSingleQuitAction
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) (signal : Signal)
    (owner : ι) :
    (publicQuittingGame table signalLaw).transition (.active signal)
        (publicSingleQuitAction table signalLaw owner true) =
      PMF.pure (.absorbed (quittingProjectiveSingletonTerminal owner)) := by
  change (publicQuittingGame table signalLaw).transition (.active signal)
      (rawSingleQuitAction owner true) = _
  simp only [publicQuittingGame, rawSingleQuitAction_true_quitters]
  split
  · congr
  · rename_i h
    exact False.elim (h (by simp))

theorem publicQuittingGame_transition_publicSingleQuitAction_false
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) (signal : Signal)
    (owner : ι) :
    (publicQuittingGame table signalLaw).transition (.active signal)
        (publicSingleQuitAction table signalLaw owner false) =
      PMF.pure .draw := by
  change (publicQuittingGame table signalLaw).transition (.active signal)
      (rawSingleQuitAction owner false) = _
  simp [publicQuittingGame, rawSingleQuitAction_false]

private theorem KiloblockConstruction.strategy_eq_pure_continue_of_no_owner
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : ∀ k owner remaining,
      construction.mode t history ≠ .active k (some owner) remaining)
    (who : ι) :
    construction.profile.strategy who t history = PMF.pure false := by
  rw [construction.strategy_eq]
  cases hm : construction.mode t history with
  | draw phase => rfl
  | finalActive => rfl
  | absorbed origin => rfl
  | active k choice remaining =>
      cases choice with
      | none => rfl
      | some owner => exact False.elim (hmode k owner remaining hm)

private theorem KiloblockConstruction.stageActionDist_continue_all
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : ∀ k owner remaining,
      construction.mode t history ≠ .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (construction.continueProfile excluded) history =
      PMF.pure (publicAllContinueAction table
        construction.profile.signalLaw) := by
  unfold StochasticGame.stageActionDist KiloblockConstruction.continueProfile
  rw [show (fun who => Function.update construction.profile.strategy excluded.1
      (fun _ _ => PMF.pure false) who t history) =
      fun _ => PMF.pure false by
    funext who
    by_cases hwho : who = excluded.1
    · subst who
      rw [Function.update_self]
      simp only [publicQuittingGame]
    · rw [Function.update_of_ne hwho]
      exact construction.strategy_eq_pure_continue_of_no_owner
        history hmode who]
  exact Math.PMFProduct.pmfPi_pure
    (publicAllContinueAction table construction.profile.signalLaw)

private theorem KiloblockConstruction.stageActionDist_profile_continue_all
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : ∀ k owner remaining,
      construction.mode t history ≠ .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history =
      PMF.pure (publicAllContinueAction table
        construction.profile.signalLaw) := by
  unfold StochasticGame.stageActionDist
  rw [show (fun who => construction.profile.strategy who t history) =
      fun _ => PMF.pure false by
    funext who
    exact construction.strategy_eq_pure_continue_of_no_owner
      history hmode who]
  exact Math.PMFProduct.pmfPi_pure
    (publicAllContinueAction table construction.profile.signalLaw)

private theorem KiloblockConstruction.stageActionDist_continue_selected_self
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some excluded) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (construction.continueProfile excluded) history =
      PMF.pure (publicAllContinueAction table
        construction.profile.signalLaw) := by
  unfold StochasticGame.stageActionDist KiloblockConstruction.continueProfile
  rw [show (fun who => Function.update construction.profile.strategy excluded.1
      (fun _ _ => PMF.pure false) who t history) =
      fun _ => PMF.pure false by
    funext who
    by_cases hwho : who = excluded.1
    · subst who
      rw [Function.update_self]
      rfl
    · rw [Function.update_of_ne hwho, construction.strategy_eq]
      simp [hmode, hwho]
      rfl]
  exact Math.PMFProduct.pmfPi_pure
    (publicAllContinueAction table construction.profile.signalLaw)

private theorem KiloblockConstruction.stageActionDist_continue_selected_other
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded owner : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (howner : owner ≠ excluded)
    (hmode : construction.mode t history =
      .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (construction.continueProfile excluded) history =
      (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history := by
  apply congrArg Math.PMFProduct.pmfPi
  funext who
  unfold KiloblockConstruction.continueProfile
  by_cases hwho : who = excluded.1
  · subst who
    have hne : excluded.1 ≠ owner.1 := fun heq =>
      howner (Subtype.ext heq.symm)
    rw [Function.update_self, construction.strategy_eq]
    simp [hmode, hne]
  · rw [Function.update_of_ne hwho]

private theorem KiloblockConstruction.stageActionDist_active_some_core
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history =
      (quittingMeshHazardCoin (construction.attempt k owner).quitWeight
          (construction.mesh k)
          (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one).bind
        (fun quits => PMF.pure
          (publicSingleQuitAction table construction.profile.signalLaw
            owner.1 quits)) := by
  classical
  let coin := quittingMeshHazardCoin
    (construction.attempt k owner).quitWeight (construction.mesh k)
    (construction.attempt k owner).quitWeight_pos.le
    (construction.attempt k owner).quitWeight_lt_one
  let base : ∀ who,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act who) :=
    fun _ => PMF.pure false
  have hfamily :
      (fun who => construction.profile.strategy who t history) =
        Function.update base owner.1 coin := by
    funext who
    by_cases hwho : who = owner.1
    · subst who
      simp [construction.strategy_eq, hmode, coin, base]
    · rw [construction.strategy_eq]
      simp [hmode, hwho, base]
  unfold StochasticGame.stageActionDist
  rw [hfamily]
  calc
    Math.PMFProduct.pmfPi (Function.update base owner.1 coin) =
        coin.bind (fun quits => Math.PMFProduct.pmfPi
          (Function.update base owner.1 (PMF.pure quits))) :=
      Math.PMFProduct.pmfPi_update_bind base owner.1 coin
    _ = coin.bind (fun quits => PMF.pure
        (publicSingleQuitAction table construction.profile.signalLaw
          owner.1 quits)) := by
      apply congrArg (PMF.bind coin)
      funext quits
      rw [show Function.update base owner.1 (PMF.pure quits) =
          fun who => PMF.pure
            (publicSingleQuitAction table construction.profile.signalLaw
              owner.1 quits who) by
        funext who
        by_cases hwho : who = owner.1
        · subst who
          rw [Function.update_self]
          simp [publicSingleQuitAction]
        · rw [Function.update_of_ne hwho]
          simp [base, publicSingleQuitAction_apply_eq_raw,
            rawSingleQuitAction, hwho]]
      exact Math.PMFProduct.pmfPi_pure _

private theorem KiloblockConstruction.historyModeStepDist_continue_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    construction.historyModeStepDist
        (construction.continueProfile excluded) history =
      construction.continueModeKernel excluded
        (construction.mode t history) := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases hmode : construction.mode t (past, state) with
  | draw phase =>
      cases state with
      | active signal =>
          simp [KiloblockMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [KiloblockMode.MatchesState, hmode] at hmatches
      | draw =>
          have haction := construction.stageActionDist_continue_all excluded
            (past, PublicQuittingState.draw) (by
              intro k owner remaining hselected
              simp [hmode] at hselected)
          unfold KiloblockConstruction.historyModeStepDist
          rw [haction, PMF.pure_bind]
          cases phase with
          | resume k choice remaining =>
              have hnext : ∀ signal,
                  signal ∈ construction.profile.signalLaw.support →
                  construction.mode (t + 1)
                    (Fin.snoc past
                        (PublicQuittingState.draw,
                          publicAllContinueAction table
                            construction.profile.signalLaw),
                      PublicQuittingState.active signal) =
                    .active k choice remaining := by
                intro signal hsignal
                have hsupport : PublicQuittingState.active signal ∈
                    ((publicQuittingGame table
                      construction.profile.signalLaw).transition
                        PublicQuittingState.draw
                          (publicAllContinueAction table
                            construction.profile.signalLaw)).support := by
                  change PublicQuittingState.active signal ∈
                    (construction.profile.signalLaw.map
                      PublicQuittingState.active).support
                  rw [PMF.mem_support_map_iff]
                  exact ⟨signal, hsignal, rfl⟩
                have hstep := construction.mode_step t
                  (past, PublicQuittingState.draw)
                  (publicAllContinueAction table construction.profile.signalLaw)
                  (PublicQuittingState.active signal) hsupport
                simpa [KiloblockModeStep, hmode] using hstep
              simp only [publicQuittingGame,
                KiloblockConstruction.continueModeKernel]
              rw [PMF.map_comp]
              rw [← PMF.bind_pure_comp]
              calc
                construction.profile.signalLaw.bind _ =
                    construction.profile.signalLaw.bind
                      (fun _ => PMF.pure (.active k choice remaining)) :=
                  Math.ProbabilityMassFunction.bind_congr_on_support
                    construction.profile.signalLaw _ _ (by
                      intro signal hsignal
                      change PMF.pure (construction.mode (t + 1)
                        (Fin.snoc past
                            (PublicQuittingState.draw,
                              publicAllContinueAction table
                                construction.profile.signalLaw),
                          PublicQuittingState.active signal)) = _
                      rw [hnext signal hsignal])
                _ = _ := PMF.bind_const _ _
          | final =>
              have hnext : ∀ signal,
                  signal ∈ construction.profile.signalLaw.support →
                  construction.mode (t + 1)
                    (Fin.snoc past
                        (PublicQuittingState.draw,
                          publicAllContinueAction table
                            construction.profile.signalLaw),
                      PublicQuittingState.active signal) =
                    .finalActive := by
                intro signal hsignal
                have hsupport : PublicQuittingState.active signal ∈
                    ((publicQuittingGame table
                      construction.profile.signalLaw).transition
                        PublicQuittingState.draw
                          (publicAllContinueAction table
                            construction.profile.signalLaw)).support := by
                  change PublicQuittingState.active signal ∈
                    (construction.profile.signalLaw.map
                      PublicQuittingState.active).support
                  rw [PMF.mem_support_map_iff]
                  exact ⟨signal, hsignal, rfl⟩
                have hstep := construction.mode_step t
                  (past, PublicQuittingState.draw)
                  (publicAllContinueAction table construction.profile.signalLaw)
                  (PublicQuittingState.active signal) hsupport
                simpa [KiloblockModeStep, hmode] using hstep
              simp only [publicQuittingGame,
                KiloblockConstruction.continueModeKernel]
              rw [PMF.map_comp]
              rw [← PMF.bind_pure_comp]
              calc
                construction.profile.signalLaw.bind _ =
                    construction.profile.signalLaw.bind
                      (fun _ => PMF.pure .finalActive) :=
                  Math.ProbabilityMassFunction.bind_congr_on_support
                    construction.profile.signalLaw _ _ (by
                      intro signal hsignal
                      change PMF.pure (construction.mode (t + 1)
                        (Fin.snoc past
                            (PublicQuittingState.draw,
                              publicAllContinueAction table
                                construction.profile.signalLaw),
                          PublicQuittingState.active signal)) = _
                      rw [hnext signal hsignal])
                _ = _ := PMF.bind_const _ _
          | choose k =>
              simp only [publicQuittingGame,
                KiloblockConstruction.continueModeKernel]
              rw [PMF.map_comp]
              rw [← PMF.bind_pure_comp, ← PMF.bind_pure_comp]
              apply Math.ProbabilityMassFunction.bind_congr_on_support
              intro signal hsignal
              have hsupport : PublicQuittingState.active signal ∈
                  ((publicQuittingGame table
                    construction.profile.signalLaw).transition
                      PublicQuittingState.draw
                        (publicAllContinueAction table
                          construction.profile.signalLaw)).support := by
                change PublicQuittingState.active signal ∈
                  (construction.profile.signalLaw.map
                    PublicQuittingState.active).support
                rw [PMF.mem_support_map_iff]
                exact ⟨signal, hsignal, rfl⟩
              have hstep := construction.mode_step t
                (past, PublicQuittingState.draw)
                (publicAllContinueAction table construction.profile.signalLaw)
                (PublicQuittingState.active signal) hsupport
              change PMF.pure (construction.mode (t + 1)
                (Fin.snoc past
                    (PublicQuittingState.draw,
                      publicAllContinueAction table
                        construction.profile.signalLaw),
                  PublicQuittingState.active signal)) = _
              congr 1
              simpa [KiloblockModeStep, hmode] using hstep
  | finalActive =>
      cases state with
      | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [KiloblockMode.MatchesState, hmode] at hmatches
      | active signal =>
          have haction := construction.stageActionDist_continue_all excluded
            (past, PublicQuittingState.active signal) (by
              intro k owner remaining hselected
              simp [hmode] at hselected)
          unfold KiloblockConstruction.historyModeStepDist
          rw [haction, PMF.pure_bind]
          have htransition :
              (publicQuittingGame table
                construction.profile.signalLaw).transition
                  (PublicQuittingState.active signal)
                    (publicAllContinueAction table
                      construction.profile.signalLaw) =
                PMF.pure .draw := by
            simp [publicQuittingGame, publicAllContinueAction]
          rw [htransition, PMF.pure_map]
          have hsupport : PublicQuittingState.draw ∈
              ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (PublicQuittingState.active signal)
                    (publicAllContinueAction table
                      construction.profile.signalLaw)).support := by
            rw [htransition]
            exact (PMF.mem_support_pure_iff
              (PublicQuittingState.draw :
                (publicQuittingGame table
                  construction.profile.signalLaw).State) _).2 rfl
          have hstep := construction.mode_step t
            (past, PublicQuittingState.active signal)
            (publicAllContinueAction table construction.profile.signalLaw)
            PublicQuittingState.draw hsupport
          have hnext : construction.mode (t + 1)
              (Fin.snoc past
                  (PublicQuittingState.active signal,
                    publicAllContinueAction table
                      construction.profile.signalLaw),
                PublicQuittingState.draw) = .draw .final := by
            simpa [KiloblockModeStep, hmode,
              publicAllContinueAction] using hstep
          simp [KiloblockConstruction.continueModeKernel, hnext]
  | absorbed origin =>
      cases state with
      | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
      | active signal =>
          simp [KiloblockMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          have haction := construction.stageActionDist_continue_all excluded
            (past, PublicQuittingState.absorbed quitters) (by
              intro k owner remaining hselected
              simp [hmode] at hselected)
          unfold KiloblockConstruction.historyModeStepDist
          rw [haction, PMF.pure_bind]
          simp only [publicQuittingGame, PMF.pure_map]
          have hsupport : PublicQuittingState.absorbed quitters ∈
              ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (PublicQuittingState.absorbed quitters)
                    (publicAllContinueAction table
                      construction.profile.signalLaw)).support := by
            exact (PMF.mem_support_pure_iff
              (PublicQuittingState.absorbed quitters :
                (publicQuittingGame table
                  construction.profile.signalLaw).State) _).2 rfl
          have hstep := construction.mode_step t
            (past, PublicQuittingState.absorbed quitters)
            (publicAllContinueAction table construction.profile.signalLaw)
            (PublicQuittingState.absorbed quitters) hsupport
          have hnext : construction.mode (t + 1)
              (Fin.snoc past
                  (PublicQuittingState.absorbed quitters,
                    publicAllContinueAction table
                      construction.profile.signalLaw),
                PublicQuittingState.absorbed quitters) = .absorbed origin := by
            simpa [KiloblockModeStep, hmode] using hstep
          unfold KiloblockConstruction.continueModeKernel
          exact congrArg PMF.pure hnext
  | active k choice remaining =>
      cases state with
      | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [KiloblockMode.MatchesState, hmode] at hmatches
      | active signal =>
          cases choice with
          | none =>
              have haction := construction.stageActionDist_continue_all
                excluded (past, PublicQuittingState.active signal) (by
                  intro other owner rest hselected
                  simp [hmode] at hselected)
              unfold KiloblockConstruction.historyModeStepDist
              rw [haction, PMF.pure_bind]
              have htransition :
                  (publicQuittingGame table
                    construction.profile.signalLaw).transition
                      (PublicQuittingState.active signal)
                        (publicAllContinueAction table
                          construction.profile.signalLaw) =
                    PMF.pure .draw := by
                simp [publicQuittingGame, publicAllContinueAction]
              rw [htransition, PMF.pure_map]
              have hsupport : PublicQuittingState.draw ∈
                  ((publicQuittingGame table
                    construction.profile.signalLaw).transition
                      (PublicQuittingState.active signal)
                        (publicAllContinueAction table
                          construction.profile.signalLaw)).support := by
                rw [htransition]
                exact (PMF.mem_support_pure_iff
                  (PublicQuittingState.draw :
                    (publicQuittingGame table
                      construction.profile.signalLaw).State) _).2 rfl
              have hstep := construction.mode_step t
                (past, PublicQuittingState.active signal)
                (publicAllContinueAction table
                  construction.profile.signalLaw)
                PublicQuittingState.draw hsupport
              have hnext : construction.mode (t + 1)
                  (Fin.snoc past
                      (PublicQuittingState.active signal,
                        publicAllContinueAction table
                          construction.profile.signalLaw),
                    PublicQuittingState.draw) =
                  if remaining ≤ 1 then
                    .draw (phaseAfterAttempt table
                      (fun block owner =>
                        (construction.attempt block owner).continuation)
                      k none)
                  else .draw (.resume k none (remaining - 1)) := by
                by_cases hremaining : remaining ≤ 1
                · simpa [KiloblockModeStep, hmode,
                    publicAllContinueAction, hremaining] using hstep
                · simpa [KiloblockModeStep, hmode,
                    publicAllContinueAction, hremaining] using hstep
              unfold KiloblockConstruction.continueModeKernel
              exact congrArg PMF.pure hnext
          | some owner =>
              by_cases howner : owner = excluded
              · subst owner
                have haction :=
                  construction.stageActionDist_continue_selected_self
                    excluded (past, PublicQuittingState.active signal)
                    k remaining hmode
                unfold KiloblockConstruction.historyModeStepDist
                rw [haction, PMF.pure_bind]
                have htransition :
                    (publicQuittingGame table
                      construction.profile.signalLaw).transition
                        (PublicQuittingState.active signal)
                          (publicAllContinueAction table
                            construction.profile.signalLaw) =
                      PMF.pure .draw := by
                  simp [publicQuittingGame, publicAllContinueAction]
                rw [htransition, PMF.pure_map]
                have hsupport : PublicQuittingState.draw ∈
                    ((publicQuittingGame table
                      construction.profile.signalLaw).transition
                        (PublicQuittingState.active signal)
                          (publicAllContinueAction table
                            construction.profile.signalLaw)).support := by
                  rw [htransition]
                  exact (PMF.mem_support_pure_iff
                    (PublicQuittingState.draw :
                      (publicQuittingGame table
                        construction.profile.signalLaw).State) _).2 rfl
                have hstep := construction.mode_step t
                  (past, PublicQuittingState.active signal)
                  (publicAllContinueAction table
                    construction.profile.signalLaw)
                  PublicQuittingState.draw hsupport
                have hnext : construction.mode (t + 1)
                    (Fin.snoc past
                        (PublicQuittingState.active signal,
                          publicAllContinueAction table
                            construction.profile.signalLaw),
                      PublicQuittingState.draw) =
                    if remaining ≤ 1 then
                      .draw (phaseAfterAttempt table
                        (fun block owner =>
                          (construction.attempt block owner).continuation)
                        k (some excluded))
                    else .draw
                      (.resume k (some excluded) (remaining - 1)) := by
                  by_cases hremaining : remaining ≤ 1
                  · simpa [KiloblockModeStep, hmode,
                      publicAllContinueAction, hremaining] using hstep
                  · simpa [KiloblockModeStep, hmode,
                      publicAllContinueAction, hremaining] using hstep
                unfold KiloblockConstruction.continueModeKernel
                simp only [ite_true]
                exact congrArg PMF.pure hnext
              · have hsame :=
                  construction.stageActionDist_continue_selected_other
                    excluded owner (past, PublicQuittingState.active signal)
                    k remaining howner hmode
                have hselected := construction.stageActionDist_active_some_core
                  (past, PublicQuittingState.active signal)
                  k owner remaining hmode
                have haction := hsame.trans hselected
                unfold KiloblockConstruction.historyModeStepDist
                rw [haction]
                unfold KiloblockConstruction.continueModeKernel
                simp only [howner, ite_false]
                rw [PMF.bind_bind]
                simp_rw [PMF.pure_bind]
                apply Math.ProbabilityMassFunction.bind_congr_on_support
                intro quits _
                cases quits with
                | false =>
                    have htransition :=
                      publicQuittingGame_transition_publicSingleQuitAction_false
                        table construction.profile.signalLaw signal owner.1
                    rw [htransition, PMF.pure_map]
                    have hsupport : PublicQuittingState.draw ∈
                        ((publicQuittingGame table
                          construction.profile.signalLaw).transition
                            (PublicQuittingState.active signal)
                            (publicSingleQuitAction table
                              construction.profile.signalLaw
                              owner.1 false)).support := by
                      rw [htransition]
                      exact (PMF.mem_support_pure_iff
                        (PublicQuittingState.draw :
                          (publicQuittingGame table
                            construction.profile.signalLaw).State) _).2 rfl
                    have hstep := construction.mode_step t
                      (past, PublicQuittingState.active signal)
                      (publicSingleQuitAction table
                        construction.profile.signalLaw owner.1 false)
                      PublicQuittingState.draw hsupport
                    have hnext : construction.mode (t + 1)
                        (Fin.snoc past
                            (PublicQuittingState.active signal,
                              publicSingleQuitAction table
                                construction.profile.signalLaw owner.1 false),
                          PublicQuittingState.draw) =
                        if remaining ≤ 1 then
                          .draw (phaseAfterAttempt table
                            (fun block selected =>
                              (construction.attempt block selected).continuation)
                            k (some owner))
                        else .draw
                          (.resume k (some owner) (remaining - 1)) := by
                      have hnoquit :
                          ¬({who | publicSingleQuitAction table
                            construction.profile.signalLaw owner.1 false who =
                              true} : Finset ι).Nonempty := by
                        simp_rw [publicSingleQuitAction_apply_eq_raw]
                        simp
                      by_cases hremaining : remaining ≤ 1
                      · simpa [KiloblockModeStep, hmode,
                          publicSingleQuitAction_apply_eq_raw,
                          rawSingleQuitAction_false, hnoquit,
                          hremaining] using hstep
                      · simpa [KiloblockModeStep, hmode,
                          publicSingleQuitAction_apply_eq_raw,
                          rawSingleQuitAction_false, hnoquit,
                          hremaining] using hstep
                    exact congrArg PMF.pure hnext
                | true =>
                    have htransition :=
                      publicQuittingGame_transition_publicSingleQuitAction
                        table construction.profile.signalLaw signal owner.1
                    rw [htransition, PMF.pure_map]
                    let quitState : (publicQuittingGame table
                        construction.profile.signalLaw).State :=
                      PublicQuittingState.absorbed
                        (quittingProjectiveSingletonTerminal owner.1)
                    have hsupport : quitState ∈
                        ((publicQuittingGame table
                          construction.profile.signalLaw).transition
                            (PublicQuittingState.active signal)
                            (publicSingleQuitAction table
                              construction.profile.signalLaw
                              owner.1 true)).support := by
                      rw [htransition]
                      change quitState ∈ (PMF.pure quitState).support
                      exact (PMF.mem_support_pure_iff _ _).2 rfl
                    have hstep := construction.mode_step t
                      (past, PublicQuittingState.active signal)
                      (publicSingleQuitAction table
                        construction.profile.signalLaw owner.1 true)
                      quitState hsupport
                    have hnext : construction.mode (t + 1)
                        (Fin.snoc past
                            (PublicQuittingState.active signal,
                              publicSingleQuitAction table
                                construction.profile.signalLaw owner.1 true),
                          quitState) =
                        .absorbed (some k) := by
                      simp only [KiloblockModeStep, hmode] at hstep
                      split at hstep
                      · exact hstep.2
                      · rename_i hnoquit
                        exact False.elim (hnoquit (by
                          refine ⟨owner.1, ?_⟩
                          simp only [Finset.mem_filter, Finset.mem_univ,
                            true_and]
                          rw [publicSingleQuitAction_apply_eq_raw]
                          simp [rawSingleQuitAction]))
                    simpa [quitState] using congrArg PMF.pure hnext

private theorem KiloblockConstruction.historyModeStepDist_profile_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    construction.historyModeStepDist construction.profile.strategy history =
      construction.profileModeKernel (construction.mode t history) := by
  classical
  have hnonempty : Nonempty (NormalPlayer table) := by
    rw [← Fintype.card_pos_iff]
    exact lt_of_lt_of_le Nat.zero_lt_two
      construction.two_le_normalPlayer_card
  let excluded : NormalPlayer table := Classical.choice hnonempty
  cases hmode : construction.mode t history with
  | draw phase =>
      have hnone : ∀ k owner remaining,
          construction.mode t history ≠ .active k (some owner) remaining := by
        intro k owner remaining hselected
        simp [hmode] at hselected
      have hprofile := construction.stageActionDist_profile_continue_all
        history hnone
      have hcontinue := construction.stageActionDist_continue_all
        excluded history hnone
      calc
        construction.historyModeStepDist construction.profile.strategy history =
            construction.historyModeStepDist
              (construction.continueProfile excluded) history := by
          unfold KiloblockConstruction.historyModeStepDist
          rw [hprofile, hcontinue]
        _ = construction.continueModeKernel excluded (.draw phase) := by
          simpa only [hmode] using
            construction.historyModeStepDist_continue_eq excluded history
        _ = construction.profileModeKernel (.draw phase) := by
          cases phase <;> rfl
  | finalActive =>
      have hnone : ∀ k owner remaining,
          construction.mode t history ≠ .active k (some owner) remaining := by
        intro k owner remaining hselected
        simp [hmode] at hselected
      have hprofile := construction.stageActionDist_profile_continue_all
        history hnone
      have hcontinue := construction.stageActionDist_continue_all
        excluded history hnone
      calc
        construction.historyModeStepDist construction.profile.strategy history =
            construction.historyModeStepDist
              (construction.continueProfile excluded) history := by
          unfold KiloblockConstruction.historyModeStepDist
          rw [hprofile, hcontinue]
        _ = construction.continueModeKernel excluded .finalActive := by
          simpa only [hmode] using
            construction.historyModeStepDist_continue_eq excluded history
        _ = construction.profileModeKernel .finalActive := by
          rfl
  | absorbed origin =>
      have hnone : ∀ k owner remaining,
          construction.mode t history ≠ .active k (some owner) remaining := by
        intro k owner remaining hselected
        simp [hmode] at hselected
      have hprofile := construction.stageActionDist_profile_continue_all
        history hnone
      have hcontinue := construction.stageActionDist_continue_all
        excluded history hnone
      calc
        construction.historyModeStepDist construction.profile.strategy history =
            construction.historyModeStepDist
              (construction.continueProfile excluded) history := by
          unfold KiloblockConstruction.historyModeStepDist
          rw [hprofile, hcontinue]
        _ = construction.continueModeKernel excluded (.absorbed origin) := by
          simpa only [hmode] using
            construction.historyModeStepDist_continue_eq excluded history
        _ = construction.profileModeKernel (.absorbed origin) := by
          rfl
  | active k choice remaining =>
      cases choice with
      | none =>
          have hnone : ∀ other owner rest,
              construction.mode t history ≠
                .active other (some owner) rest := by
            intro other owner rest hselected
            simp [hmode] at hselected
          have hprofile := construction.stageActionDist_profile_continue_all
            history hnone
          have hcontinue := construction.stageActionDist_continue_all
            excluded history hnone
          calc
            construction.historyModeStepDist
                construction.profile.strategy history =
                construction.historyModeStepDist
                  (construction.continueProfile excluded) history := by
              unfold KiloblockConstruction.historyModeStepDist
              rw [hprofile, hcontinue]
            _ = construction.continueModeKernel excluded
                (.active k none remaining) := by
              simpa only [hmode] using
                construction.historyModeStepDist_continue_eq excluded history
            _ = construction.profileModeKernel
                (.active k none remaining) := by
              rfl
      | some owner =>
          obtain ⟨other, hother⟩ : ∃ other : NormalPlayer table,
              other ≠ owner := by
            by_contra hnot
            push Not at hnot
            have hcard : Fintype.card (NormalPlayer table) ≤ 1 := by
              rw [Fintype.card_le_one_iff]
              intro left right
              exact (hnot left).trans (hnot right).symm
            exact (not_lt_of_ge hcard)
              construction.two_le_normalPlayer_card
          have hsame := construction.stageActionDist_continue_selected_other
            other owner history k remaining hother.symm hmode
          calc
            construction.historyModeStepDist
                construction.profile.strategy history =
                construction.historyModeStepDist
                  (construction.continueProfile other) history := by
              unfold KiloblockConstruction.historyModeStepDist
              rw [hsame]
            _ = construction.continueModeKernel other
                (.active k (some owner) remaining) := by
              simpa only [hmode] using
                construction.historyModeStepDist_continue_eq other history
            _ = construction.profileModeKernel
                (.active k (some owner) remaining) := by
              simp [KiloblockConstruction.continueModeKernel,
                KiloblockConstruction.profileModeKernel, hother.symm]

theorem KiloblockConstruction.finiteHistoryModeStepDist_continue_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (construction.historyModeStepDist
        (construction.continueProfile excluded) history).map
          construction.finiteMode =
      construction.continueFiniteModeKernel excluded
        (construction.finiteMode (construction.mode t history)) := by
  rw [construction.historyModeStepDist_continue_eq excluded history]
  unfold KiloblockConstruction.continueFiniteModeKernel
  rw [construction.modeOfFinite_finiteMode]
  intro k choice remaining hmode
  exact construction.mode_remaining_le_mesh t history k choice remaining hmode

theorem KiloblockConstruction.modeDist_succ_continue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (t : ℕ) :
    construction.modeDist (construction.continueProfile excluded) (t + 1) =
      (construction.modeDist (construction.continueProfile excluded) t).bind
        (construction.continueFiniteModeKernel excluded) := by
  let game := publicQuittingGame table construction.profile.signalLaw
  unfold KiloblockConstruction.modeDist
  rw [game.histDist_succ, PMF.map_bind, PMF.bind_map]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro history _
  simp only [Function.comp_apply]
  rw [← construction.finiteHistoryModeStepDist_continue_eq
    excluded history]
  unfold KiloblockConstruction.historyModeStepDist
  rw [PMF.map_bind, PMF.map_bind]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro action _
  rw [PMF.map_comp, PMF.map_bind]
  simp_rw [PMF.pure_map]
  rw [← PMF.bind_pure_comp]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro nextState _
  rfl

theorem KiloblockConstruction.modeDist_continue_eq_iter
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (t : ℕ) :
    construction.modeDist (construction.continueProfile excluded) t =
      Math.PMFIter.iter (construction.continueFiniteModeKernel excluded) t
        (.drawChoose (Fin.last construction.blockCount)) := by
  induction t with
  | zero =>
      unfold KiloblockConstruction.modeDist
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
        PMF.pure_map, Math.PMFIter.iter_zero]
      simp [construction.mode_initial,
        KiloblockConstruction.finiteMode]
  | succ t ih =>
      rw [construction.modeDist_succ_continue excluded t, ih,
        Math.PMFIter.iter_succ']

theorem KiloblockConstruction.finiteHistoryModeStepDist_profile_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (construction.historyModeStepDist
        construction.profile.strategy history).map construction.finiteMode =
      construction.profileFiniteModeKernel
        (construction.finiteMode (construction.mode t history)) := by
  rw [construction.historyModeStepDist_profile_eq history]
  unfold KiloblockConstruction.profileFiniteModeKernel
  rw [construction.modeOfFinite_finiteMode]
  intro k choice remaining hmode
  exact construction.mode_remaining_le_mesh t history k choice remaining hmode

theorem KiloblockConstruction.modeDist_succ_profile
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (t : ℕ) :
    construction.modeDist construction.profile.strategy (t + 1) =
      (construction.modeDist construction.profile.strategy t).bind
        construction.profileFiniteModeKernel := by
  let game := publicQuittingGame table construction.profile.signalLaw
  unfold KiloblockConstruction.modeDist
  rw [game.histDist_succ, PMF.map_bind, PMF.bind_map]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro history _
  simp only [Function.comp_apply]
  rw [← construction.finiteHistoryModeStepDist_profile_eq history]
  unfold KiloblockConstruction.historyModeStepDist
  rw [PMF.map_bind, PMF.map_bind]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro action _
  rw [PMF.map_comp, PMF.map_bind]
  simp_rw [PMF.pure_map]
  rw [← PMF.bind_pure_comp]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro nextState _
  rfl

theorem KiloblockConstruction.modeDist_profile_eq_iter
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (t : ℕ) :
    construction.modeDist construction.profile.strategy t =
      Math.PMFIter.iter construction.profileFiniteModeKernel t
        (.drawChoose (Fin.last construction.blockCount)) := by
  induction t with
  | zero =>
      unfold KiloblockConstruction.modeDist
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
        PMF.pure_map, Math.PMFIter.iter_zero]
      simp [construction.mode_initial,
        KiloblockConstruction.finiteMode]
  | succ t ih =>
      rw [construction.modeDist_succ_profile t, ih,
        Math.PMFIter.iter_succ']

def KiloblockConstruction.exitCapableChoice
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : Option (NormalPlayer table) → Prop
  | none => True
  | some owner =>
      (construction.attempt k owner).continuation = .advance ∨
        owner ≠ excluded

private theorem KiloblockConstruction.exists_exitCapableChoice
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    ∃ choice, 0 < (match choice with
        | none => (construction.buildingBlock k).z.cemetery
        | some owner => (construction.buildingBlock k).z.singleton owner) ∧
      construction.exitCapableChoice k excluded choice := by
  by_contra hnot
  push Not at hnot
  have hcemetery : (construction.buildingBlock k).z.cemetery = 0 := by
    have hnonneg := (construction.buildingBlock k).z.cemetery_nonneg
    by_contra hne
    have hpos : 0 < (construction.buildingBlock k).z.cemetery :=
      lt_of_le_of_ne hnonneg (Ne.symm hne)
    exact (hnot none hpos) trivial
  have hother : ∀ owner : NormalPlayer table, owner ≠ excluded →
      (construction.buildingBlock k).z.singleton owner = 0 := by
    intro owner howner
    have hnonneg := (construction.buildingBlock k).z.singleton_nonneg owner
    by_contra hne
    have hpos : 0 < (construction.buildingBlock k).z.singleton owner :=
      lt_of_le_of_ne hnonneg (Ne.symm hne)
    exact (hnot (some owner) hpos) (Or.inr howner)
  obtain ⟨owner, _, hownerPos⟩ :=
    (Finset.sum_pos_iff_of_nonneg (fun other _ =>
      (construction.buildingBlock k).z.singleton_nonneg other)).mp
      (construction.buildingBlock k).nontrivial
  have howner : owner = excluded := by
    by_contra hne
    rw [hother owner hne] at hownerPos
    linarith
  subst owner
  have hbranch : (construction.attempt k excluded).continuation = .restart := by
    cases hcontinuation : (construction.attempt k excluded).continuation with
    | restart => rfl
    | advance =>
        exact False.elim ((hnot (some excluded) hownerPos) (Or.inl hcontinuation))
  have hadvanceMass : construction.advanceMass k = 0 := by
    unfold KiloblockConstruction.advanceMass
      KiloblockConstruction.attemptAdvanceWeight
    rw [hcemetery, zero_add]
    apply Finset.sum_eq_zero
    intro other _
    by_cases hotherExcluded : other = excluded
    · subst other
      simp [hbranch]
    · rw [hother other hotherExcluded, zero_mul]
  have hadvance : construction.macroAdvanceProbability k = 0 := by
    simp [KiloblockConstruction.macroAdvanceProbability, hadvanceMass]
  have habsorbOther : ∀ other ∈ Finset.univ.erase excluded,
      construction.macroAbsorbProbability k other = 0 := by
    intro other hmem
    have hne := Finset.ne_of_mem_erase hmem
    unfold KiloblockConstruction.macroAbsorbProbability
      KiloblockConstruction.absorbMass
    rw [hother other hne, zero_mul, zero_div]
  have hzero : construction.continueExitProbabilityWeight k excluded = 0 := by
    unfold KiloblockConstruction.continueExitProbabilityWeight
      KiloblockConstruction.continueAdvanceProbabilityWeight
    rw [hadvance]
    simp only [hbranch, reduceCtorEq, if_false, zero_add]
    rw [Finset.sum_eq_zero habsorbOther]
  linarith [construction.continueExitProbabilityWeight_pos k excluded]

private theorem KiloblockConstruction.exists_signal_of_choice_weight_pos
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table))
    (hchoice : 0 < match choice with
      | none => (construction.buildingBlock k).z.cemetery
      | some owner => (construction.buildingBlock k).z.singleton owner) :
    ∃ signal ∈ construction.profile.signalLaw.support,
      construction.signalSelector k signal = choice := by
  have hmass : (construction.buildingBlock k).z.toPMF choice ≠ 0 := by
    intro hzero
    have hreal : ((construction.buildingBlock k).z.toPMF choice).toReal = 0 := by
      simp [hzero]
    cases choice with
    | none =>
        rw [SimplexWeights.toPMF_none_toReal] at hreal
        linarith
    | some owner =>
        rw [SimplexWeights.toPMF_some_toReal] at hreal
        linarith
  have hsupport : choice ∈
      (construction.profile.signalLaw.map
        (construction.signalSelector k)).support := by
    rw [construction.signalSelector_law k]
    rw [PMF.mem_support_iff]
    exact hmass
  rw [PMF.mem_support_map_iff] at hsupport
  exact hsupport

private theorem KiloblockConstruction.continueKernel_drawChoose_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (signal : Fin (construction.profile.signalCount + 1))
    (hsignal : signal ∈ construction.profile.signalLaw.support) :
    Math.Probability.PMFSupportStep
      (construction.continueFiniteModeKernel excluded)
      (.drawChoose k)
      (construction.finiteMode (.active k
        (construction.signalSelector k signal) (construction.mesh k))) := by
  rw [Math.Probability.PMFSupportStep, ← PMF.mem_support_iff]
  unfold KiloblockConstruction.continueFiniteModeKernel
    KiloblockConstruction.modeOfFinite
    KiloblockConstruction.continueModeKernel
  rw [PMF.mem_support_map_iff]
  refine ⟨.active k (construction.signalSelector k signal)
    (construction.mesh k), ?_, rfl⟩
  rw [PMF.mem_support_map_iff]
  exact ⟨signal, hsignal, rfl⟩

private theorem KiloblockConstruction.continueKernel_drawResume_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    Math.Probability.PMFSupportStep
      (construction.continueFiniteModeKernel excluded)
      (construction.finiteMode (.draw (.resume k choice remaining)))
      (construction.finiteMode (.active k choice remaining)) := by
  rw [Math.Probability.PMFSupportStep, ← PMF.mem_support_iff]
  unfold KiloblockConstruction.continueFiniteModeKernel
  rw [construction.modeOfFinite_finiteMode_of_le_totalMesh]
  · unfold KiloblockConstruction.continueModeKernel
    rw [PMF.mem_support_map_iff]
    exact ⟨.active k choice remaining,
      (PMF.mem_support_pure_iff _ _).2 rfl, rfl⟩
  · intro other otherChoice otherRemaining hmode
    rcases hmode with hmode | hmode
    · cases hmode
    · cases hmode
      exact hremaining

private theorem KiloblockConstruction.meshCoin_false_mem_support
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) :
    false ∈ (quittingMeshHazardCoin
      (construction.attempt k owner).quitWeight (construction.mesh k)
      (construction.attempt k owner).quitWeight_pos.le
      (construction.attempt k owner).quitWeight_lt_one).support := by
  rw [PMF.mem_support_iff]
  intro hzero
  have hreal : ((quittingMeshHazardCoin
      (construction.attempt k owner).quitWeight (construction.mesh k)
      (construction.attempt k owner).quitWeight_pos.le
      (construction.attempt k owner).quitWeight_lt_one) false).toReal = 0 := by
    simp [hzero]
  rw [quittingMeshHazardCoin_false_toReal] at hreal
  have hlt := quittingMeshHazard_lt_one
    (construction.attempt k owner).quitWeight_lt_one (construction.mesh k)
  linarith

private theorem KiloblockConstruction.continueKernel_active_continue_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    Math.Probability.PMFSupportStep
      (construction.continueFiniteModeKernel excluded)
      (construction.finiteMode (.active k choice remaining))
      (construction.finiteMode
        (if remaining ≤ 1 then
          .draw (phaseAfterAttempt table
            (fun block owner =>
              (construction.attempt block owner).continuation)
            k choice)
        else .draw (.resume k choice (remaining - 1)))) := by
  rw [Math.Probability.PMFSupportStep, ← PMF.mem_support_iff]
  unfold KiloblockConstruction.continueFiniteModeKernel
  rw [construction.modeOfFinite_finiteMode_of_le_totalMesh]
  · unfold KiloblockConstruction.continueModeKernel
    cases choice with
    | none =>
        rw [PMF.mem_support_map_iff]
        exact ⟨_, (PMF.mem_support_pure_iff _ _).2 rfl, rfl⟩
    | some owner =>
        by_cases howner : owner = excluded
        · subst owner
          simp only [ite_true]
          rw [PMF.mem_support_map_iff]
          exact ⟨_, (PMF.mem_support_pure_iff _ _).2 rfl, rfl⟩
        · simp only [howner, ite_false]
          rw [PMF.mem_support_map_iff]
          refine ⟨_, ?_, rfl⟩
          rw [PMF.mem_support_bind_iff]
          refine ⟨false, construction.meshCoin_false_mem_support k owner, ?_⟩
          exact (PMF.mem_support_pure_iff _ _).2 rfl
  · intro other otherChoice otherRemaining hmode
    rcases hmode with hmode | hmode
    · cases hmode
      exact hremaining
    · cases hmode

private theorem KiloblockConstruction.meshCoin_true_mem_support
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) :
    true ∈ (quittingMeshHazardCoin
      (construction.attempt k owner).quitWeight (construction.mesh k)
      (construction.attempt k owner).quitWeight_pos.le
      (construction.attempt k owner).quitWeight_lt_one).support := by
  rw [PMF.mem_support_iff]
  intro hzero
  have hreal : ((quittingMeshHazardCoin
      (construction.attempt k owner).quitWeight (construction.mesh k)
      (construction.attempt k owner).quitWeight_pos.le
      (construction.attempt k owner).quitWeight_lt_one) true).toReal = 0 := by
    simp [hzero]
  rw [quittingMeshHazardCoin_true_toReal] at hreal
  have hpow : (1 - (construction.attempt k owner).quitWeight) ^
      (((construction.mesh k : ℕ) : ℝ)⁻¹ : ℝ) < 1 := by
    apply Real.rpow_lt_one
    · exact sub_nonneg.mpr
        (construction.attempt k owner).quitWeight_lt_one.le
    · linarith [(construction.attempt k owner).quitWeight_pos]
    · exact inv_pos.mpr (Nat.cast_pos.mpr (construction.mesh_pos k))
  unfold quittingMeshHazard at hreal
  linarith

private theorem KiloblockConstruction.continueKernel_active_quit_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded owner : NormalPlayer table)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh)
    (howner : owner ≠ excluded) :
    Math.Probability.PMFSupportStep
      (construction.continueFiniteModeKernel excluded)
      (construction.finiteMode (.active k (some owner) remaining))
      (.absorbed (some k)) := by
  rw [Math.Probability.PMFSupportStep, ← PMF.mem_support_iff]
  unfold KiloblockConstruction.continueFiniteModeKernel
  rw [construction.modeOfFinite_finiteMode_of_le_totalMesh]
  · unfold KiloblockConstruction.continueModeKernel
    simp only [howner, ite_false]
    rw [PMF.mem_support_map_iff]
    refine ⟨.absorbed (some k), ?_, rfl⟩
    rw [PMF.mem_support_bind_iff]
    refine ⟨true, construction.meshCoin_true_mem_support k owner, ?_⟩
    exact (PMF.mem_support_pure_iff _ _).2 rfl
  · intro other otherChoice otherRemaining hmode
    rcases hmode with hmode | hmode
    · cases hmode
      exact hremaining
    · cases hmode

private theorem KiloblockConstruction.continueKernel_active_reaches_after
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    Math.Probability.PMFReachable
      (construction.continueFiniteModeKernel excluded)
      (construction.finiteMode (.active k choice remaining))
      (construction.finiteMode (.draw
        (phaseAfterAttempt table
          (fun block owner =>
            (construction.attempt block owner).continuation)
          k choice))) := by
  induction remaining using Nat.strong_induction_on with
  | h remaining ih =>
      by_cases hsmall : remaining ≤ 1
      · have hstep := construction.continueKernel_active_continue_step
          excluded k choice remaining hremaining
        simpa only [Math.Probability.PMFReachable, if_pos hsmall] using
          (Relation.ReflTransGen.single hstep)
      · have hremainingPred : remaining - 1 ≤ construction.totalMesh := by
          omega
        have hactive := construction.continueKernel_active_continue_step
          excluded k choice remaining hremaining
        have hdraw := construction.continueKernel_drawResume_step
          excluded k choice (remaining - 1) hremainingPred
        have htail := ih (remaining - 1) (by omega) hremainingPred
        simp only [if_neg hsmall] at hactive
        exact Relation.ReflTransGen.head hactive
          (Relation.ReflTransGen.head hdraw htail)

private theorem KiloblockConstruction.continueKernel_drawResume_reaches_after
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    Math.Probability.PMFReachable
      (construction.continueFiniteModeKernel excluded)
      (construction.finiteMode (.draw (.resume k choice remaining)))
      (construction.finiteMode (.draw
        (phaseAfterAttempt table
          (fun block owner =>
            (construction.attempt block owner).continuation)
          k choice))) := by
  exact Relation.ReflTransGen.head
    (construction.continueKernel_drawResume_step
      excluded k choice remaining hremaining)
    (construction.continueKernel_active_reaches_after
      excluded k choice remaining hremaining)

/-! Final-tail and absorbed modes form the closed boundary of the finite
schedule chain. -/
def KiloblockConstruction.continueBoundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    Set (KiloblockFiniteMode construction)
  | .drawFinal => True
  | .finalActive => True
  | .absorbed _ => True
  | _ => False

theorem KiloblockConstruction.continueBoundary_closed
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    Math.Probability.IsClosedCore
      (construction.continueFiniteModeKernel excluded)
      construction.continueBoundary := by
  intro source hsource destination hdestination
  cases source with
  | drawChoose k => contradiction
  | drawResume k choice remaining => contradiction
  | active k choice remaining => contradiction
  | drawFinal =>
      unfold KiloblockConstruction.continueFiniteModeKernel
        KiloblockConstruction.modeOfFinite
        KiloblockConstruction.continueModeKernel at hdestination
      have heq : destination = .finalActive := by
        simpa [KiloblockConstruction.finiteMode] using hdestination
      subst destination
      trivial
  | finalActive =>
      unfold KiloblockConstruction.continueFiniteModeKernel
        KiloblockConstruction.modeOfFinite
        KiloblockConstruction.continueModeKernel at hdestination
      have heq : destination = .drawFinal := by
        simpa [KiloblockConstruction.finiteMode] using hdestination
      subst destination
      trivial
  | absorbed origin =>
      unfold KiloblockConstruction.continueFiniteModeKernel
        KiloblockConstruction.modeOfFinite
        KiloblockConstruction.continueModeKernel at hdestination
      have heq : destination = .absorbed origin := by
        simpa [KiloblockConstruction.finiteMode] using hdestination
      subst destination
      trivial

private theorem KiloblockConstruction.exitChoice_reaches_exit
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table))
    (hchoice : construction.exitCapableChoice k excluded choice) :
    ∃ target,
      (target = construction.finiteMode (.draw
          (precedingKiloblockPhase table k)) ∨
        target = .absorbed (some k)) ∧
      Math.Probability.PMFReachable
        (construction.continueFiniteModeKernel excluded)
        (construction.finiteMode
          (.active k choice (construction.mesh k))) target := by
  cases choice with
  | none =>
      refine ⟨construction.finiteMode
        (.draw (precedingKiloblockPhase table k)), Or.inl rfl, ?_⟩
      simpa [phaseAfterAttempt] using
        construction.continueKernel_active_reaches_after excluded k none
          (construction.mesh k) (construction.mesh_le_totalMesh k)
  | some owner =>
      rcases hchoice with hadvance | howner
      · refine ⟨construction.finiteMode
          (.draw (precedingKiloblockPhase table k)), Or.inl rfl, ?_⟩
        simpa [phaseAfterAttempt, hadvance] using
          construction.continueKernel_active_reaches_after excluded k
            (some owner) (construction.mesh k)
            (construction.mesh_le_totalMesh k)
      · refine ⟨.absorbed (some k), Or.inr rfl, ?_⟩
        exact Relation.ReflTransGen.single
          (construction.continueKernel_active_quit_step excluded owner k
            (construction.mesh k) (construction.mesh_le_totalMesh k) howner)

private theorem KiloblockConstruction.continueKernel_drawChoose_reaches_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1)) :
    ∃ target, target ∈ construction.continueBoundary ∧
      Math.Probability.PMFReachable
        (construction.continueFiniteModeKernel excluded)
        (.drawChoose k) target := by
  induction k using Fin.induction with
  | zero =>
      obtain ⟨choice, hweight, hcapable⟩ :=
        construction.exists_exitCapableChoice 0 excluded
      obtain ⟨signal, hsignal, hselect⟩ :=
        construction.exists_signal_of_choice_weight_pos 0 choice hweight
      obtain ⟨target, htarget, hreach⟩ :=
        construction.exitChoice_reaches_exit excluded 0 choice hcapable
      have hchoose := construction.continueKernel_drawChoose_step
        excluded 0 signal hsignal
      rw [hselect] at hchoose
      rcases htarget with hpreceding | habsorbed
      · have hfinal : target = .drawFinal := by
          simpa [precedingKiloblockPhase,
            KiloblockConstruction.finiteMode] using hpreceding
        subst target
        exact ⟨.drawFinal, trivial,
          Relation.ReflTransGen.head hchoose hreach⟩
      · subst target
        exact ⟨.absorbed (some 0), trivial,
          Relation.ReflTransGen.head hchoose hreach⟩
  | succ index ih =>
      obtain ⟨choice, hweight, hcapable⟩ :=
        construction.exists_exitCapableChoice index.succ excluded
      obtain ⟨signal, hsignal, hselect⟩ :=
        construction.exists_signal_of_choice_weight_pos
          index.succ choice hweight
      obtain ⟨target, htarget, hreach⟩ :=
        construction.exitChoice_reaches_exit
          excluded index.succ choice hcapable
      have hchoose := construction.continueKernel_drawChoose_step
        excluded index.succ signal hsignal
      rw [hselect] at hchoose
      rcases htarget with hpreceding | habsorbed
      · obtain ⟨boundary, hboundary, htail⟩ := ih
        have hindex :
            (⟨index.1, by omega⟩ :
              Fin (construction.blockCount + 1)) = index.castSucc := by
          apply Fin.ext
          rfl
        have hprevious : target = .drawChoose index.castSucc := by
          simpa [precedingKiloblockPhase,
            KiloblockConstruction.finiteMode, hindex] using hpreceding
        subst target
        exact ⟨boundary, hboundary,
          Relation.ReflTransGen.head hchoose (hreach.trans htail)⟩
      · subst target
        exact ⟨.absorbed (some index.succ), trivial,
          Relation.ReflTransGen.head hchoose hreach⟩

private theorem KiloblockConstruction.continueKernel_preceding_reaches_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1)) :
    ∃ target, target ∈ construction.continueBoundary ∧
      Math.Probability.PMFReachable
        (construction.continueFiniteModeKernel excluded)
        (construction.finiteMode
          (.draw (precedingKiloblockPhase table k))) target := by
  induction k using Fin.induction with
  | zero =>
      refine ⟨.drawFinal, trivial, ?_⟩
      simpa [precedingKiloblockPhase,
        KiloblockConstruction.finiteMode,
        Math.Probability.PMFReachable] using
          (Relation.ReflTransGen.refl :
            Math.Probability.PMFReachable
              (construction.continueFiniteModeKernel excluded)
              (.drawFinal) (.drawFinal))
  | succ index ih =>
      obtain ⟨target, htarget, hreach⟩ :=
        construction.continueKernel_drawChoose_reaches_boundary
          excluded index.castSucc
      refine ⟨target, htarget, ?_⟩
      have hindex :
          (⟨index.1, by omega⟩ :
            Fin (construction.blockCount + 1)) = index.castSucc := by
        apply Fin.ext
        rfl
      simpa [precedingKiloblockPhase,
        KiloblockConstruction.finiteMode, hindex] using hreach

private theorem KiloblockConstruction.continueKernel_after_reaches_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) :
    ∃ target, target ∈ construction.continueBoundary ∧
      Math.Probability.PMFReachable
        (construction.continueFiniteModeKernel excluded)
        (construction.finiteMode (.draw
          (phaseAfterAttempt table
            (fun block owner =>
              (construction.attempt block owner).continuation)
            k choice))) target := by
  cases choice with
  | none =>
      simpa [phaseAfterAttempt] using
        construction.continueKernel_preceding_reaches_boundary excluded k
  | some owner =>
      cases hcontinuation : (construction.attempt k owner).continuation with
      | restart =>
          simpa [phaseAfterAttempt, hcontinuation,
            KiloblockConstruction.finiteMode] using
              construction.continueKernel_drawChoose_reaches_boundary
                excluded k
      | advance =>
          simpa [phaseAfterAttempt, hcontinuation] using
            construction.continueKernel_preceding_reaches_boundary excluded k

theorem KiloblockConstruction.continueKernel_reaches_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ∀ source, ∃ target, target ∈ construction.continueBoundary ∧
      Math.Probability.PMFReachable
        (construction.continueFiniteModeKernel excluded) source target := by
  intro source
  cases source with
  | drawChoose k =>
      exact construction.continueKernel_drawChoose_reaches_boundary excluded k
  | drawFinal =>
      exact ⟨.drawFinal, trivial, Relation.ReflTransGen.refl⟩
  | finalActive =>
      exact ⟨.finalActive, trivial, Relation.ReflTransGen.refl⟩
  | absorbed origin =>
      exact ⟨.absorbed origin, trivial, Relation.ReflTransGen.refl⟩
  | drawResume k choice remaining =>
      let rawChoice := choice.map construction.ownerOfCode
      obtain ⟨target, htarget, htail⟩ :=
        construction.continueKernel_after_reaches_boundary
          excluded k rawChoice
      refine ⟨target, htarget, ?_⟩
      have hhead := construction.continueKernel_drawResume_reaches_after
        excluded k rawChoice remaining.1 (by omega)
      have hsource : construction.finiteMode
          (.draw (.resume k rawChoice remaining.1)) =
          .drawResume k choice remaining := by
        change construction.finiteMode
          (construction.modeOfFinite (.drawResume k choice remaining)) = _
        exact construction.finiteMode_modeOfFinite _
      rw [hsource] at hhead
      exact hhead.trans htail
  | active k choice remaining =>
      let rawChoice := choice.map construction.ownerOfCode
      obtain ⟨target, htarget, htail⟩ :=
        construction.continueKernel_after_reaches_boundary
          excluded k rawChoice
      refine ⟨target, htarget, ?_⟩
      have hhead := construction.continueKernel_active_reaches_after
        excluded k rawChoice remaining.1 (by omega)
      have hsource : construction.finiteMode
          (.active k rawChoice remaining.1) =
          .active k choice remaining := by
        change construction.finiteMode
          (construction.modeOfFinite (.active k choice remaining)) = _
        exact construction.finiteMode_modeOfFinite _
      rw [hsource] at hhead
      exact hhead.trans htail

/-! The explicit finite-mode kernel is transient off its final/absorbed
boundary. This compiles the source's informal "the block eventually ends"
argument into a quantitative finite-chain certificate. -/
theorem KiloblockConstruction.continueKernel_transienceCertificate
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    Nonempty (Math.Probability.ClosedCoreTransienceCertificate
      (construction.continueFiniteModeKernel excluded)
      construction.continueBoundary) := by
  letI : Nonempty (KiloblockFiniteMode construction) :=
    ⟨FiniteKiloblockMode.drawFinal⟩
  exact Math.Probability.exists_closedCoreTransienceCertificate
    (construction.continueFiniteModeKernel excluded)
    construction.continueBoundary
    (construction.continueBoundary_closed excluded)
    (construction.continueKernel_reaches_boundary excluded)

private theorem KiloblockConstruction.continueModeKernel_support_subset_profile
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (mode : KiloblockMode table construction.blockCount) :
    (construction.continueModeKernel excluded mode).support ⊆
      (construction.profileModeKernel mode).support := by
  intro destination hdestination
  cases mode with
  | draw phase =>
      cases phase <;> simpa [KiloblockConstruction.continueModeKernel,
        KiloblockConstruction.profileModeKernel] using hdestination
  | finalActive =>
      simpa [KiloblockConstruction.continueModeKernel,
        KiloblockConstruction.profileModeKernel] using hdestination
  | absorbed origin =>
      simpa [KiloblockConstruction.continueModeKernel,
        KiloblockConstruction.profileModeKernel] using hdestination
  | active k choice remaining =>
      cases choice with
      | none =>
          simpa [KiloblockConstruction.continueModeKernel,
            KiloblockConstruction.profileModeKernel] using hdestination
      | some owner =>
          by_cases howner : owner = excluded
          · subst owner
            simp only [KiloblockConstruction.continueModeKernel,
              ite_true] at hdestination
            have hdestination' : destination =
                (if remaining ≤ 1 then
                  .draw (phaseAfterAttempt table
                    (fun block selected =>
                      (construction.attempt block selected).continuation)
                    k (some excluded))
                else .draw (.resume k (some excluded) (remaining - 1))) :=
              (PMF.mem_support_pure_iff _ _).mp hdestination
            subst destination
            simp only [KiloblockConstruction.profileModeKernel]
            rw [PMF.mem_support_bind_iff]
            refine ⟨false,
              construction.meshCoin_false_mem_support k excluded, ?_⟩
            exact (PMF.mem_support_pure_iff _ _).2 rfl
          · simpa [KiloblockConstruction.continueModeKernel,
              KiloblockConstruction.profileModeKernel, howner] using
              hdestination

private theorem
    KiloblockConstruction.continueFiniteModeKernel_support_subset_profile
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (source : KiloblockFiniteMode construction) :
    (construction.continueFiniteModeKernel excluded source).support ⊆
      (construction.profileFiniteModeKernel source).support := by
  intro destination hdestination
  unfold KiloblockConstruction.continueFiniteModeKernel at hdestination
  unfold KiloblockConstruction.profileFiniteModeKernel
  rw [PMF.mem_support_map_iff] at hdestination ⊢
  obtain ⟨raw, hraw, rfl⟩ := hdestination
  exact ⟨raw,
    construction.continueModeKernel_support_subset_profile excluded
      (construction.modeOfFinite source) hraw,
    rfl⟩

theorem KiloblockConstruction.profileBoundary_closed
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    Math.Probability.IsClosedCore
      construction.profileFiniteModeKernel
      construction.continueBoundary := by
  intro source hsource destination hdestination
  cases source with
  | drawChoose k => contradiction
  | drawResume k choice remaining => contradiction
  | active k choice remaining => contradiction
  | drawFinal =>
      unfold KiloblockConstruction.profileFiniteModeKernel
        KiloblockConstruction.modeOfFinite
        KiloblockConstruction.profileModeKernel at hdestination
      have heq : destination = .finalActive := by
        simpa [KiloblockConstruction.finiteMode] using hdestination
      subst destination
      trivial
  | finalActive =>
      unfold KiloblockConstruction.profileFiniteModeKernel
        KiloblockConstruction.modeOfFinite
        KiloblockConstruction.profileModeKernel at hdestination
      have heq : destination = .drawFinal := by
        simpa [KiloblockConstruction.finiteMode] using hdestination
      subst destination
      trivial
  | absorbed origin =>
      unfold KiloblockConstruction.profileFiniteModeKernel
        KiloblockConstruction.modeOfFinite
        KiloblockConstruction.profileModeKernel at hdestination
      have heq : destination = .absorbed origin := by
        simpa [KiloblockConstruction.finiteMode] using hdestination
      subst destination
      trivial

theorem KiloblockConstruction.profileKernel_reaches_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    ∀ source, ∃ target, target ∈ construction.continueBoundary ∧
      Math.Probability.PMFReachable
        construction.profileFiniteModeKernel source target := by
  have hnonempty : Nonempty (NormalPlayer table) := by
    rw [← Fintype.card_pos_iff]
    exact lt_of_lt_of_le Nat.zero_lt_two
      construction.two_le_normalPlayer_card
  let excluded : NormalPlayer table := Classical.choice hnonempty
  intro source
  obtain ⟨target, htarget, hreach⟩ :=
    construction.continueKernel_reaches_boundary excluded source
  have hmono : Math.Probability.PMFSupportStep
      (construction.continueFiniteModeKernel excluded) ≤
      Math.Probability.PMFSupportStep
        construction.profileFiniteModeKernel := by
    intro left right hstep
    exact construction.continueFiniteModeKernel_support_subset_profile
      excluded left hstep
  exact ⟨target, htarget,
    (Relation.ReflTransGen.mono hmono) source target hreach⟩

theorem KiloblockConstruction.profileKernel_transienceCertificate
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    Nonempty (Math.Probability.ClosedCoreTransienceCertificate
      construction.profileFiniteModeKernel
      construction.continueBoundary) := by
  letI : Nonempty (KiloblockFiniteMode construction) :=
    ⟨FiniteKiloblockMode.drawFinal⟩
  exact Math.Probability.exists_closedCoreTransienceCertificate
    construction.profileFiniteModeKernel
    construction.continueBoundary
    construction.profileBoundary_closed
    construction.profileKernel_reaches_boundary

/-! Probability of reaching the final tail from a selected prescribed
attempt, conditional on the downstream final-tail probability. -/
def KiloblockConstruction.profileSelectedSurvival
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table))
    (remaining : ℕ) (downstream : ℝ) : ℝ :=
  match choice with
  | none => downstream
  | some owner =>
      let endpoint :=
        match (construction.attempt k owner).continuation with
        | .restart =>
            construction.macroAdvanceProbability k * downstream
        | .advance => downstream
      (1 - quittingMeshHazard
        (construction.attempt k owner).quitWeight
        (construction.mesh k)) ^ (max remaining 1) * endpoint

def KiloblockConstruction.profileChoiceAdvanceProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    Option (NormalPlayer table) → ℝ
  | none => 1
  | some owner => construction.attemptAdvanceWeight k owner

def KiloblockConstruction.profileChoiceAbsorbProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    Option (NormalPlayer table) → ℝ
  | none => 0
  | some owner => (construction.attempt k owner).quitWeight

theorem KiloblockConstruction.profileSelectedSurvival_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (downstream : ℝ) :
    construction.profileSelectedSurvival k choice
        (construction.mesh k) downstream =
      construction.profileChoiceAdvanceProbability k choice * downstream +
        (1 - (construction.profileChoiceAdvanceProbability k choice +
          construction.profileChoiceAbsorbProbability k choice)) *
          (construction.macroAdvanceProbability k * downstream) := by
  cases choice with
  | none =>
      simp [KiloblockConstruction.profileSelectedSurvival,
        KiloblockConstruction.profileChoiceAdvanceProbability,
        KiloblockConstruction.profileChoiceAbsorbProbability]
  | some owner =>
      cases hcontinuation : (construction.attempt k owner).continuation <;>
        rw [KiloblockConstruction.profileSelectedSurvival,
          KiloblockConstruction.profileChoiceAdvanceProbability,
          KiloblockConstruction.profileChoiceAbsorbProbability,
          KiloblockConstruction.attemptAdvanceWeight, hcontinuation,
          max_eq_left (construction.mesh_pos k),
          construction.mesh_survival k owner]
      <;> ring

theorem KiloblockConstruction.expect_profileChoiceAdvance_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    expect (construction.buildingBlock k).z.toPMF
        (construction.profileChoiceAdvanceProbability k) =
      construction.advanceMass k := by
  rw [expect_eq_sum, Fintype.sum_option]
  simp only [SimplexWeights.toPMF_none_toReal,
    SimplexWeights.toPMF_some_toReal,
    KiloblockConstruction.profileChoiceAdvanceProbability, mul_one]
  rfl

theorem KiloblockConstruction.expect_profileChoiceAbsorb_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    expect (construction.buildingBlock k).z.toPMF
        (construction.profileChoiceAbsorbProbability k) =
      ∑ owner, construction.absorbMass k owner := by
  rw [expect_eq_sum, Fintype.sum_option]
  simp only [SimplexWeights.toPMF_none_toReal,
    SimplexWeights.toPMF_some_toReal,
    KiloblockConstruction.profileChoiceAbsorbProbability, mul_zero,
    zero_add, KiloblockConstruction.absorbMass]

theorem KiloblockConstruction.expect_profileSelectedSurvival_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) (downstream : ℝ) :
    expect (construction.buildingBlock k).z.toPMF (fun choice =>
        construction.profileSelectedSurvival k choice
          (construction.mesh k) downstream) =
      construction.macroAdvanceProbability k * downstream := by
  let law := (construction.buildingBlock k).z.toPMF
  let advance := construction.profileChoiceAdvanceProbability k
  let absorb := construction.profileChoiceAbsorbProbability k
  let entry := construction.macroAdvanceProbability k * downstream
  have hunfold : expect law (fun choice =>
      construction.profileSelectedSurvival k choice
        (construction.mesh k) downstream) =
      expect law (fun choice =>
        advance choice * downstream +
          (1 - (advance choice + absorb choice)) * entry) := by
    apply Math.ProbabilityMassFunction.expect_congr_on_support
    intro choice _
    exact construction.profileSelectedSurvival_mesh k choice downstream
  have hadvance : expect law advance = construction.advanceMass k :=
    construction.expect_profileChoiceAdvance_eq k
  have habsorb : expect law absorb =
      ∑ owner, construction.absorbMass k owner :=
    construction.expect_profileChoiceAbsorb_eq k
  have hfirst : expect law (fun choice => advance choice * downstream) =
      construction.advanceMass k * downstream := by
    calc
      expect law (fun choice => advance choice * downstream) =
          downstream * expect law advance := by
        simpa only [mul_comm] using
          Math.Probability.expect_const_mul law downstream advance
      _ = _ := by rw [hadvance]; ring
  have hsecond : expect law (fun choice =>
      (1 - (advance choice + absorb choice)) * entry) =
      (1 - construction.exitMass k) * entry := by
    calc
      expect law (fun choice =>
          (1 - (advance choice + absorb choice)) * entry) =
          entry * expect law (fun choice =>
            1 - (advance choice + absorb choice)) := by
        simpa only [mul_comm] using
          Math.Probability.expect_const_mul law entry
            (fun choice => 1 - (advance choice + absorb choice))
      _ = entry * (1 - (construction.advanceMass k +
          ∑ owner, construction.absorbMass k owner)) := by
        rw [Math.Probability.expect_sub, Math.Probability.expect_const,
          Math.Probability.expect_add, hadvance, habsorb]
      _ = (1 - construction.exitMass k) * entry := by
        rw [construction.exitMass_eq]
        ring
  rw [hunfold, Math.Probability.expect_add, hfirst, hsecond]
  dsimp only [entry]
  unfold KiloblockConstruction.macroAdvanceProbability
  field_simp [ne_of_gt (construction.exitMass_pos k)]
  ring

theorem KiloblockConstruction.expect_signal_profileSelectedSurvival_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) (downstream : ℝ) :
    expect construction.profile.signalLaw (fun signal =>
        construction.profileSelectedSurvival k
          (construction.signalSelector k signal) (construction.mesh k)
          downstream) =
      construction.macroAdvanceProbability k * downstream := by
  calc
    expect construction.profile.signalLaw (fun signal =>
        construction.profileSelectedSurvival k
          (construction.signalSelector k signal) (construction.mesh k)
          downstream) =
        expect (construction.profile.signalLaw.map
          (construction.signalSelector k)) (fun choice =>
            construction.profileSelectedSurvival k choice
              (construction.mesh k) downstream) := by
      symm
      exact Math.Probability.expect_map _ _ _
    _ = _ := by
      rw [construction.signalSelector_law k]
      exact construction.expect_profileSelectedSurvival_mesh k downstream

theorem KiloblockConstruction.macroSurvivalFuel_succ
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.macroSurvivalFuel (k.1 + 1) =
      construction.macroAdvanceProbability k *
        construction.macroSurvivalFuel k.1 := by
  rw [KiloblockConstruction.macroSurvivalFuel]
  have hk : construction.macroAdvanceAt k.1 =
      construction.macroAdvanceProbability k := by
    simp only [KiloblockConstruction.macroAdvanceAt, dif_pos k.isLt]
  rw [hk]

/-! Hitting probability of the final all-Continue tail under the prescribed
finite schedule kernel. -/
def KiloblockConstruction.profileFinalPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode construction → ℝ
  | .drawChoose k => construction.macroSurvivalFuel (k.1 + 1)
  | .drawResume k choice remaining =>
      construction.profileSelectedSurvival k
        (choice.map construction.ownerOfCode) remaining.1
        (construction.macroSurvivalFuel k.1)
  | .drawFinal => 1
  | .active k choice remaining =>
      construction.profileSelectedSurvival k
        (choice.map construction.ownerOfCode) remaining.1
        (construction.macroSurvivalFuel k.1)
  | .finalActive => 1
  | .absorbed _ => 0

@[simp]
theorem KiloblockConstruction.profileFinalPotential_finiteMode_active
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    construction.profileFinalPotential
        (construction.finiteMode (.active k choice remaining)) =
      construction.profileSelectedSurvival k choice remaining
        (construction.macroSurvivalFuel k.1) := by
  cases choice <;>
    simp [KiloblockConstruction.profileFinalPotential,
      KiloblockConstruction.finiteMode, min_eq_left hremaining]

@[simp]
theorem KiloblockConstruction.profileFinalPotential_finiteMode_drawResume
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    construction.profileFinalPotential
        (construction.finiteMode (.draw (.resume k choice remaining))) =
      construction.profileSelectedSurvival k choice remaining
        (construction.macroSurvivalFuel k.1) := by
  cases choice <;>
    simp [KiloblockConstruction.profileFinalPotential,
      KiloblockConstruction.finiteMode, min_eq_left hremaining]

theorem KiloblockConstruction.profileFinalPotential_preceding
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1)) :
    construction.profileFinalPotential
        (construction.finiteMode
          (.draw (precedingKiloblockPhase table k))) =
      construction.macroSurvivalFuel k.1 := by
  induction k using Fin.induction with
  | zero =>
      simp [precedingKiloblockPhase,
        KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.finiteMode,
        KiloblockConstruction.macroSurvivalFuel]
  | succ index ih =>
      have hindex :
          (⟨index.1, by omega⟩ :
            Fin (construction.blockCount + 1)) = index.castSucc := by
        apply Fin.ext
        rfl
      simp [precedingKiloblockPhase,
        KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.finiteMode, hindex]

theorem KiloblockConstruction.profileFinalPotential_after
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) :
    construction.profileFinalPotential
        (construction.finiteMode (.draw
          (phaseAfterAttempt table
            (fun block owner =>
              (construction.attempt block owner).continuation)
            k choice))) =
      match choice with
      | none => construction.macroSurvivalFuel k.1
      | some owner =>
          match (construction.attempt k owner).continuation with
          | .restart =>
              construction.macroAdvanceProbability k *
                construction.macroSurvivalFuel k.1
          | .advance => construction.macroSurvivalFuel k.1 := by
  cases choice with
  | none =>
      simpa [phaseAfterAttempt] using
        construction.profileFinalPotential_preceding k
  | some owner =>
      cases hcontinuation : (construction.attempt k owner).continuation with
      | restart =>
          simpa [phaseAfterAttempt, hcontinuation,
            KiloblockConstruction.profileFinalPotential,
            KiloblockConstruction.finiteMode] using
              construction.macroSurvivalFuel_succ k
      | advance =>
          simpa [phaseAfterAttempt, hcontinuation] using
            construction.profileFinalPotential_preceding k

private theorem KiloblockConstruction.profileSelectedSurvival_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ) (downstream : ℝ) :
    construction.profileSelectedSurvival k (some owner)
        remaining downstream =
      (1 - quittingMeshHazard
        (construction.attempt k owner).quitWeight (construction.mesh k)) *
        if remaining ≤ 1 then
          match (construction.attempt k owner).continuation with
          | .restart => construction.macroAdvanceProbability k * downstream
          | .advance => downstream
        else construction.profileSelectedSurvival k (some owner)
          (remaining - 1) downstream := by
  by_cases hsmall : remaining ≤ 1
  · cases hcontinuation : (construction.attempt k owner).continuation <;>
      simp [KiloblockConstruction.profileSelectedSurvival, hsmall,
        hcontinuation]
  · have hmax : max remaining 1 = remaining := by omega
    have hmaxPred : max (remaining - 1) 1 = remaining - 1 := by omega
    have hremaining : remaining = remaining - 1 + 1 := by omega
    have hsub : remaining - 1 + 1 - 1 = remaining - 1 := by omega
    cases hcontinuation : (construction.attempt k owner).continuation <;>
      simp only [KiloblockConstruction.profileSelectedSurvival,
        hsmall, if_false, hmax, hmaxPred, hcontinuation]
    <;> rw [hremaining, pow_succ, hsub]
    <;> ring

private theorem KiloblockConstruction.expect_profileMeshCoin_zero_else
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (value : ℝ) :
    expect (quittingMeshHazardCoin
        (construction.attempt k owner).quitWeight (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one)
      (fun quits => if quits then 0 else value) =
    (1 - quittingMeshHazard
      (construction.attempt k owner).quitWeight (construction.mesh k)) *
        value := by
  rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
  simp [quittingMeshHazardCoin_false_toReal,
    quittingMeshHazardCoin_true_toReal]

private theorem
    KiloblockConstruction.profileModeOfFinite_remaining_le_totalMesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    ∀ k choice remaining,
      construction.modeOfFinite mode = .active k choice remaining ∨
        construction.modeOfFinite mode =
          .draw (.resume k choice remaining) →
            remaining ≤ construction.totalMesh := by
  intro k choice remaining hmode
  cases mode <;> simp only [KiloblockConstruction.modeOfFinite] at hmode
  · rcases hmode with hmode | hmode <;> cases hmode
  · rcases hmode with hmode | hmode
    · cases hmode
    · cases hmode
      omega
  · rcases hmode with hmode | hmode <;> cases hmode
  · rcases hmode with hmode | hmode
    · cases hmode
      omega
    · cases hmode
  · rcases hmode with hmode | hmode <;> cases hmode
  · rcases hmode with hmode | hmode <;> cases hmode

private theorem KiloblockConstruction.profileRawFinalPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockMode table construction.blockCount)
    (hremaining : ∀ k choice remaining,
      mode = .active k choice remaining ∨
        mode = .draw (.resume k choice remaining) →
          remaining ≤ construction.totalMesh) :
    expect (construction.profileModeKernel mode) (fun next =>
        construction.profileFinalPotential
          (construction.finiteMode next)) =
      construction.profileFinalPotential
        (construction.finiteMode mode) := by
  cases mode with
  | draw phase =>
      cases phase with
      | choose k =>
          unfold KiloblockConstruction.profileModeKernel
          rw [Math.Probability.expect_map]
          simp_rw [construction.profileFinalPotential_finiteMode_active
            k _ (construction.mesh k)
            (construction.mesh_le_totalMesh k)]
          rw [construction.expect_signal_profileSelectedSurvival_mesh
            k (construction.macroSurvivalFuel k.1)]
          simpa [KiloblockConstruction.finiteMode,
            KiloblockConstruction.profileFinalPotential] using
              (construction.macroSurvivalFuel_succ k).symm
      | resume k choice remaining =>
          have hle := hremaining k choice remaining (Or.inr rfl)
          unfold KiloblockConstruction.profileModeKernel
          rw [Math.Probability.expect_pure,
            construction.profileFinalPotential_finiteMode_active
              k choice remaining hle,
            construction.profileFinalPotential_finiteMode_drawResume
              k choice remaining hle]
      | final =>
          simp [KiloblockConstruction.profileModeKernel,
            KiloblockConstruction.profileFinalPotential,
            KiloblockConstruction.finiteMode]
  | finalActive =>
      simp [KiloblockConstruction.profileModeKernel,
        KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.finiteMode]
  | absorbed origin =>
      simp [KiloblockConstruction.profileModeKernel,
        KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.finiteMode]
  | active k choice remaining =>
      have hle := hremaining k choice remaining (Or.inl rfl)
      have hcurrent := construction.profileFinalPotential_finiteMode_active
        k choice remaining hle
      let downstream := construction.macroSurvivalFuel k.1
      cases choice with
      | none =>
          by_cases hsmall : remaining ≤ 1
          · simp only [KiloblockConstruction.profileModeKernel,
              if_pos hsmall]
            rw [Math.Probability.expect_pure,
              construction.profileFinalPotential_after k none,
              hcurrent]
            rfl
          · have hpred : remaining - 1 ≤ construction.totalMesh := by
              omega
            simp only [KiloblockConstruction.profileModeKernel,
              if_neg hsmall]
            rw [Math.Probability.expect_pure,
              construction.profileFinalPotential_finiteMode_drawResume
                k none (remaining - 1) hpred,
              hcurrent]
            rfl
      | some owner =>
          by_cases hsmall : remaining ≤ 1
          · simp only [KiloblockConstruction.profileModeKernel,
              if_pos hsmall]
            let coin := quittingMeshHazardCoin
              (construction.attempt k owner).quitWeight
              (construction.mesh k)
              (construction.attempt k owner).quitWeight_pos.le
              (construction.attempt k owner).quitWeight_lt_one
            let afterMode : KiloblockMode table construction.blockCount :=
              .draw (phaseAfterAttempt table
                (fun block selected =>
                  (construction.attempt block selected).continuation)
                k (some owner))
            change expect (coin.bind fun quits => if quits = true then
                PMF.pure (KiloblockMode.absorbed (some k))
              else PMF.pure afterMode) (fun next =>
                construction.profileFinalPotential
                  (construction.finiteMode next)) = _
            have hkernel : (coin.bind fun quits => if quits = true then
                  PMF.pure (KiloblockMode.absorbed (some k))
                else PMF.pure afterMode) =
                coin.map fun quits => if quits = true then
                  KiloblockMode.absorbed (some k) else afterMode := by
              rw [← PMF.bind_pure_comp]
              congr 1
              funext quits
              cases quits <;> rfl
            rw [hkernel, Math.Probability.expect_map]
            rw [show (fun quits => construction.profileFinalPotential
                  (construction.finiteMode
                    (if quits = true then
                      KiloblockMode.absorbed (some k) else afterMode))) =
                fun quits => if quits = true then 0 else
                  match (construction.attempt k owner).continuation with
                  | .restart =>
                      construction.macroAdvanceProbability k * downstream
                  | .advance => downstream by
              funext quits
              cases quits with
              | false =>
                  dsimp only [afterMode]
                  exact construction.profileFinalPotential_after
                    k (some owner)
              | true => rfl]
            change expect coin _ = _
            rw [construction.expect_profileMeshCoin_zero_else]
            rw [hcurrent]
            dsimp only [downstream]
            simpa only [if_pos hsmall] using
              (construction.profileSelectedSurvival_step
                k owner remaining
                  (construction.macroSurvivalFuel k.1)).symm
          · have hpred : remaining - 1 ≤
                construction.totalMesh := by omega
            simp only [KiloblockConstruction.profileModeKernel,
              if_neg hsmall]
            let coin := quittingMeshHazardCoin
              (construction.attempt k owner).quitWeight
              (construction.mesh k)
              (construction.attempt k owner).quitWeight_pos.le
              (construction.attempt k owner).quitWeight_lt_one
            let afterMode : KiloblockMode table construction.blockCount :=
              .draw (.resume k (some owner) (remaining - 1))
            change expect (coin.bind fun quits => if quits = true then
                PMF.pure (KiloblockMode.absorbed (some k))
              else PMF.pure afterMode) (fun next =>
                construction.profileFinalPotential
                  (construction.finiteMode next)) = _
            have hkernel : (coin.bind fun quits => if quits = true then
                  PMF.pure (KiloblockMode.absorbed (some k))
                else PMF.pure afterMode) =
                coin.map fun quits => if quits = true then
                  KiloblockMode.absorbed (some k) else afterMode := by
              rw [← PMF.bind_pure_comp]
              congr 1
              funext quits
              cases quits <;> rfl
            rw [hkernel, Math.Probability.expect_map]
            rw [show (fun quits => construction.profileFinalPotential
                  (construction.finiteMode
                    (if quits = true then
                      KiloblockMode.absorbed (some k) else afterMode))) =
                fun quits => if quits = true then 0 else
                  construction.profileSelectedSurvival k (some owner)
                    (remaining - 1) downstream by
              funext quits
              cases quits with
              | false =>
                  dsimp only [afterMode, downstream]
                  exact
                    construction.profileFinalPotential_finiteMode_drawResume
                      k (some owner) (remaining - 1) hpred
              | true => rfl]
            change expect coin _ = _
            rw [construction.expect_profileMeshCoin_zero_else]
            rw [hcurrent]
            dsimp only [downstream]
            simpa only [if_neg hsmall] using
              (construction.profileSelectedSurvival_step
                k owner remaining
                  (construction.macroSurvivalFuel k.1)).symm

theorem KiloblockConstruction.profileFinalPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    expect (construction.profileFiniteModeKernel mode)
        construction.profileFinalPotential =
      construction.profileFinalPotential mode := by
  unfold KiloblockConstruction.profileFiniteModeKernel
  rw [Math.Probability.expect_map]
  rw [construction.profileRawFinalPotential_harmonic
    (construction.modeOfFinite mode)
    (construction.profileModeOfFinite_remaining_le_totalMesh mode)]
  rw [construction.finiteMode_modeOfFinite]

theorem KiloblockConstruction.profileFinalPotential_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.profileFinalPotential
        (.drawChoose (Fin.last construction.blockCount)) =
      construction.macroSurvivalProbability := by
  change construction.macroSurvivalFuel
      (construction.blockCount + 1) = _
  exact construction.macroSurvivalFuel_full

/-! One fresh type draw under the forced-Continue profile may advance,
absorb at another player, or restart. These are its unnormalized masses. -/
def KiloblockConstruction.continueRawAdvanceMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  construction.advanceMass k +
    if (construction.attempt k excluded).continuation = .advance then
      construction.absorbMass k excluded
    else 0

def KiloblockConstruction.continueRawOtherAbsorbMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  ∑ owner ∈ Finset.univ.erase excluded,
    construction.absorbMass k owner

def KiloblockConstruction.continueRawExitMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) : ℝ :=
  construction.continueRawAdvanceMass k excluded +
    construction.continueRawOtherAbsorbMass k excluded

theorem KiloblockConstruction.continueRawAdvanceMass_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueRawAdvanceMass k excluded =
      construction.exitMass k *
        construction.continueAdvanceProbabilityWeight k excluded := by
  unfold KiloblockConstruction.continueRawAdvanceMass
    KiloblockConstruction.continueAdvanceProbabilityWeight
    KiloblockConstruction.macroAdvanceProbability
    KiloblockConstruction.macroAbsorbProbability
  have hexit : construction.exitMass k ≠ 0 :=
    ne_of_gt (construction.exitMass_pos k)
  split
  · field_simp
  · field_simp
    ring

theorem KiloblockConstruction.continueRawOtherAbsorbMass_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueRawOtherAbsorbMass k excluded =
      construction.exitMass k *
        ∑ owner ∈ Finset.univ.erase excluded,
          construction.macroAbsorbProbability k owner := by
  unfold KiloblockConstruction.continueRawOtherAbsorbMass
    KiloblockConstruction.macroAbsorbProbability
  rw [Finset.mul_sum]
  apply Finset.sum_congr rfl
  intro owner _
  field_simp [ne_of_gt (construction.exitMass_pos k)]

theorem KiloblockConstruction.continueRawExitMass_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueRawExitMass k excluded =
      construction.exitMass k *
        construction.continueExitProbabilityWeight k excluded := by
  rw [KiloblockConstruction.continueRawExitMass,
    construction.continueRawAdvanceMass_eq,
    construction.continueRawOtherAbsorbMass_eq]
  unfold KiloblockConstruction.continueExitProbabilityWeight
  ring

theorem KiloblockConstruction.continueRawExitMass_pos
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    0 < construction.continueRawExitMass k excluded := by
  rw [construction.continueRawExitMass_eq]
  exact mul_pos (construction.exitMass_pos k)
    (construction.continueExitProbabilityWeight_pos k excluded)

theorem KiloblockConstruction.continueMacroAdvance_eq_raw
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    construction.continueMacroAdvanceProbability k excluded =
      construction.continueRawAdvanceMass k excluded /
        construction.continueRawExitMass k excluded := by
  rw [construction.continueRawAdvanceMass_eq,
    construction.continueRawExitMass_eq]
  unfold KiloblockConstruction.continueMacroAdvanceProbability
  field_simp [ne_of_gt (construction.exitMass_pos k),
    ne_of_gt (construction.continueExitProbabilityWeight_pos k excluded)]

/-! Probability of eventually reaching a supplied downstream event from a
selected attempt with `remaining` active decisions left. -/
def KiloblockConstruction.continueSelectedSurvival
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table)
    (choice : Option (NormalPlayer table))
    (remaining : ℕ) (downstream : ℝ) : ℝ :=
  match choice with
  | none => downstream
  | some owner =>
      let endpoint :=
        match (construction.attempt k owner).continuation with
        | .restart =>
            construction.continueMacroAdvanceProbability k excluded *
              downstream
        | .advance => downstream
      if owner = excluded then endpoint else
        (1 - quittingMeshHazard
          (construction.attempt k owner).quitWeight
          (construction.mesh k)) ^ (max remaining 1) * endpoint

def KiloblockConstruction.continueChoiceAdvanceProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    Option (NormalPlayer table) → ℝ
  | none => 1
  | some owner =>
      match (construction.attempt k owner).continuation with
      | .restart => 0
      | .advance =>
          if owner = excluded then 1 else
            1 - (construction.attempt k owner).quitWeight

def KiloblockConstruction.continueChoiceAbsorbProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    Option (NormalPlayer table) → ℝ
  | none => 0
  | some owner =>
      if owner = excluded then 0 else
        (construction.attempt k owner).quitWeight

theorem KiloblockConstruction.continueSelectedSurvival_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table)
    (choice : Option (NormalPlayer table)) (downstream : ℝ) :
    construction.continueSelectedSurvival k excluded choice
        (construction.mesh k) downstream =
      construction.continueChoiceAdvanceProbability k excluded choice *
          downstream +
        (1 - (construction.continueChoiceAdvanceProbability k excluded choice +
          construction.continueChoiceAbsorbProbability k excluded choice)) *
          (construction.continueMacroAdvanceProbability k excluded *
            downstream) := by
  cases choice with
  | none =>
      simp [KiloblockConstruction.continueSelectedSurvival,
        KiloblockConstruction.continueChoiceAdvanceProbability,
        KiloblockConstruction.continueChoiceAbsorbProbability]
  | some owner =>
      by_cases howner : owner = excluded
      · subst owner
        cases hcontinuation :
            (construction.attempt k excluded).continuation <;>
          simp [KiloblockConstruction.continueSelectedSurvival,
            KiloblockConstruction.continueChoiceAdvanceProbability,
            KiloblockConstruction.continueChoiceAbsorbProbability,
            hcontinuation]
      · cases hcontinuation :
            (construction.attempt k owner).continuation
        · rw [KiloblockConstruction.continueSelectedSurvival,
            KiloblockConstruction.continueChoiceAdvanceProbability,
            KiloblockConstruction.continueChoiceAbsorbProbability,
            hcontinuation, if_neg howner,
            max_eq_left (construction.mesh_pos k),
            construction.mesh_survival k owner]
          simp only [if_neg howner]
          ring
        · rw [KiloblockConstruction.continueSelectedSurvival,
            KiloblockConstruction.continueChoiceAdvanceProbability,
            KiloblockConstruction.continueChoiceAbsorbProbability,
            hcontinuation, if_neg howner,
            max_eq_left (construction.mesh_pos k),
            construction.mesh_survival k owner]
          simp only [if_neg howner]
          ring

private theorem KiloblockConstruction.continueChoiceAdvance_other
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded owner : NormalPlayer table) (howner : owner ≠ excluded) :
    construction.continueChoiceAdvanceProbability k excluded (some owner) =
      construction.attemptAdvanceWeight k owner := by
  cases hcontinuation : (construction.attempt k owner).continuation <;>
    simp [KiloblockConstruction.continueChoiceAdvanceProbability,
      KiloblockConstruction.attemptAdvanceWeight, hcontinuation, howner]

private theorem KiloblockConstruction.continueChoiceAdvance_excluded_account
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    (construction.buildingBlock k).z.singleton excluded *
        construction.continueChoiceAdvanceProbability k excluded
          (some excluded) =
      (construction.buildingBlock k).z.singleton excluded *
          construction.attemptAdvanceWeight k excluded +
        if (construction.attempt k excluded).continuation = .advance then
          construction.absorbMass k excluded
        else 0 := by
  cases hcontinuation :
      (construction.attempt k excluded).continuation
  · simp [KiloblockConstruction.continueChoiceAdvanceProbability,
      KiloblockConstruction.attemptAdvanceWeight,
      hcontinuation]
  · simp [KiloblockConstruction.continueChoiceAdvanceProbability,
      KiloblockConstruction.attemptAdvanceWeight,
      KiloblockConstruction.absorbMass, hcontinuation]
    ring

theorem KiloblockConstruction.expect_continueChoiceAdvance_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    expect (construction.buildingBlock k).z.toPMF
        (construction.continueChoiceAdvanceProbability k excluded) =
      construction.continueRawAdvanceMass k excluded := by
  rw [expect_eq_sum, Fintype.sum_option]
  simp only [SimplexWeights.toPMF_none_toReal,
    SimplexWeights.toPMF_some_toReal]
  rw [show construction.continueChoiceAdvanceProbability k excluded none = 1
    from rfl, mul_one]
  have hother :
      (∑ owner ∈ Finset.univ.erase excluded,
          (construction.buildingBlock k).z.singleton owner *
            construction.continueChoiceAdvanceProbability k excluded
              (some owner)) =
        ∑ owner ∈ Finset.univ.erase excluded,
          (construction.buildingBlock k).z.singleton owner *
            construction.attemptAdvanceWeight k owner := by
    apply Finset.sum_congr rfl
    intro owner howner
    rw [construction.continueChoiceAdvance_other k excluded owner
      (Finset.ne_of_mem_erase howner)]
  rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded),
    add_comm]
  rw [construction.continueChoiceAdvance_excluded_account k excluded]
  rw [add_assoc, hother]
  unfold KiloblockConstruction.continueRawAdvanceMass
    KiloblockConstruction.advanceMass
  rw [← Finset.sum_erase_add _ _ (Finset.mem_univ excluded)]
  ring

theorem KiloblockConstruction.expect_continueChoiceAbsorb_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) :
    expect (construction.buildingBlock k).z.toPMF
        (construction.continueChoiceAbsorbProbability k excluded) =
      construction.continueRawOtherAbsorbMass k excluded := by
  rw [expect_eq_sum, Fintype.sum_option]
  simp only [SimplexWeights.toPMF_none_toReal,
    SimplexWeights.toPMF_some_toReal,
    KiloblockConstruction.continueChoiceAbsorbProbability, mul_zero,
    zero_add]
  rw [← Finset.sum_subset (Finset.erase_subset excluded Finset.univ)]
  · unfold KiloblockConstruction.continueRawOtherAbsorbMass
      KiloblockConstruction.absorbMass
    apply Finset.sum_congr rfl
    intro owner howner
    simp only [if_neg (Finset.ne_of_mem_erase howner)]
  · intro owner _ howner
    have heq : owner = excluded := by
      simpa using howner
    subst owner
    simp

theorem KiloblockConstruction.expect_continueSelectedSurvival_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) (downstream : ℝ) :
    expect (construction.buildingBlock k).z.toPMF (fun choice =>
        construction.continueSelectedSurvival k excluded choice
          (construction.mesh k) downstream) =
      construction.continueMacroAdvanceProbability k excluded *
        downstream := by
  let law := (construction.buildingBlock k).z.toPMF
  let advance := construction.continueChoiceAdvanceProbability k excluded
  let absorb := construction.continueChoiceAbsorbProbability k excluded
  let entry := construction.continueMacroAdvanceProbability k excluded *
    downstream
  have hunfold : expect law (fun choice =>
      construction.continueSelectedSurvival k excluded choice
        (construction.mesh k) downstream) =
      expect law (fun choice =>
        advance choice * downstream +
          (1 - (advance choice + absorb choice)) * entry) := by
    apply Math.ProbabilityMassFunction.expect_congr_on_support
    intro choice _
    exact construction.continueSelectedSurvival_mesh
      k excluded choice downstream
  have hadvance : expect law advance =
      construction.continueRawAdvanceMass k excluded := by
    exact construction.expect_continueChoiceAdvance_eq k excluded
  have habsorb : expect law absorb =
      construction.continueRawOtherAbsorbMass k excluded := by
    exact construction.expect_continueChoiceAbsorb_eq k excluded
  have hfirst : expect law (fun choice => advance choice * downstream) =
      construction.continueRawAdvanceMass k excluded * downstream := by
    calc
      expect law (fun choice => advance choice * downstream) =
          downstream * expect law advance := by
        simpa only [mul_comm] using
          Math.Probability.expect_const_mul law downstream advance
      _ = _ := by rw [hadvance]; ring
  have hsecond : expect law (fun choice =>
      (1 - (advance choice + absorb choice)) * entry) =
      (1 - construction.continueRawExitMass k excluded) * entry := by
    calc
      expect law (fun choice =>
          (1 - (advance choice + absorb choice)) * entry) =
          entry * expect law (fun choice =>
            1 - (advance choice + absorb choice)) := by
        simpa only [mul_comm] using
          Math.Probability.expect_const_mul law entry
            (fun choice => 1 - (advance choice + absorb choice))
      _ = entry * (1 -
          (construction.continueRawAdvanceMass k excluded +
            construction.continueRawOtherAbsorbMass k excluded)) := by
        rw [Math.Probability.expect_sub, Math.Probability.expect_const,
          Math.Probability.expect_add, hadvance, habsorb]
      _ = (1 - construction.continueRawExitMass k excluded) * entry := by
        unfold KiloblockConstruction.continueRawExitMass
        ring
  rw [hunfold, Math.Probability.expect_add, hfirst, hsecond]
  dsimp only [entry]
  rw [construction.continueMacroAdvance_eq_raw k excluded]
  field_simp [ne_of_gt
    (construction.continueRawExitMass_pos k excluded)]
  ring

theorem KiloblockConstruction.expect_signal_continueSelectedSurvival_mesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table) (downstream : ℝ) :
    expect construction.profile.signalLaw (fun signal =>
        construction.continueSelectedSurvival k excluded
          (construction.signalSelector k signal) (construction.mesh k)
          downstream) =
      construction.continueMacroAdvanceProbability k excluded *
        downstream := by
  calc
    expect construction.profile.signalLaw (fun signal =>
        construction.continueSelectedSurvival k excluded
          (construction.signalSelector k signal) (construction.mesh k)
          downstream) =
        expect (construction.profile.signalLaw.map
          (construction.signalSelector k)) (fun choice =>
            construction.continueSelectedSurvival k excluded choice
              (construction.mesh k) downstream) := by
      symm
      exact Math.Probability.expect_map _ _ _
    _ = _ := by
      rw [construction.signalSelector_law k]
      exact construction.expect_continueSelectedSurvival_mesh
        k excluded downstream

def KiloblockConstruction.continueMacroAdvanceAt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (j : ℕ) : ℝ :=
  if hj : j < construction.blockCount + 1 then
    construction.continueMacroAdvanceProbability ⟨j, hj⟩ excluded
  else 1

def KiloblockConstruction.continueMacroSurvivalFuel
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) : ℕ → ℝ
  | 0 => 1
  | fuel + 1 => construction.continueMacroAdvanceAt excluded fuel *
      construction.continueMacroSurvivalFuel excluded fuel

theorem KiloblockConstruction.continueMacroSurvivalFuel_succ
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (k : Fin (construction.blockCount + 1)) :
    construction.continueMacroSurvivalFuel excluded (k.1 + 1) =
      construction.continueMacroAdvanceProbability k excluded *
        construction.continueMacroSurvivalFuel excluded k.1 := by
  rw [KiloblockConstruction.continueMacroSurvivalFuel]
  have hk : construction.continueMacroAdvanceAt excluded k.1 =
      construction.continueMacroAdvanceProbability k excluded := by
    simp only [KiloblockConstruction.continueMacroAdvanceAt, dif_pos k.isLt]
  rw [hk]

theorem KiloblockConstruction.continueMacroSurvivalFuel_full
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    construction.continueMacroSurvivalFuel excluded
        (construction.blockCount + 1) =
      construction.continueMacroSurvivalProbability excluded := by
  have hprefix : ∀ fuel, fuel ≤ construction.blockCount + 1 →
      construction.continueMacroSurvivalFuel excluded fuel =
        ∏ j ∈ Finset.range fuel,
          construction.continueMacroAdvanceAt excluded j := by
    intro fuel hfuel
    induction fuel with
    | zero =>
        simp [KiloblockConstruction.continueMacroSurvivalFuel]
    | succ fuel ih =>
        rw [KiloblockConstruction.continueMacroSurvivalFuel,
          Finset.prod_range_succ, ih (by omega)]
        ring
  calc
    construction.continueMacroSurvivalFuel excluded
        (construction.blockCount + 1) =
        ∏ j ∈ Finset.range (construction.blockCount + 1),
          construction.continueMacroAdvanceAt excluded j :=
      hprefix _ le_rfl
    _ = ∏ k : Fin (construction.blockCount + 1),
        construction.continueMacroAdvanceAt excluded k.1 := by
      rw [Fin.prod_univ_eq_prod_range]
    _ = construction.continueMacroSurvivalProbability excluded := by
      unfold KiloblockConstruction.continueMacroSurvivalProbability
      apply Finset.prod_congr rfl
      intro k _
      simp only [KiloblockConstruction.continueMacroAdvanceAt,
        dif_pos k.isLt]

/-! Hitting potential of the final infinite tail for the explicit finite
kernel. Absorbed modes have value zero; final-tail modes have value one. -/
def KiloblockConstruction.continueFinalPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    KiloblockFiniteMode construction → ℝ
  | .drawChoose k =>
      construction.continueMacroSurvivalFuel excluded (k.1 + 1)
  | .drawResume k choice remaining =>
      construction.continueSelectedSurvival k excluded
        (choice.map construction.ownerOfCode) remaining.1
        (construction.continueMacroSurvivalFuel excluded k.1)
  | .drawFinal => 1
  | .active k choice remaining =>
      construction.continueSelectedSurvival k excluded
        (choice.map construction.ownerOfCode) remaining.1
        (construction.continueMacroSurvivalFuel excluded k.1)
  | .finalActive => 1
  | .absorbed origin => if origin.isNone then 1 else 0

theorem KiloblockConstruction.continueMacroSurvivalFuel_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) : ∀ fuel,
    0 ≤ construction.continueMacroSurvivalFuel excluded fuel := by
  intro fuel
  induction fuel with
  | zero => simp [KiloblockConstruction.continueMacroSurvivalFuel]
  | succ fuel ih =>
      rw [KiloblockConstruction.continueMacroSurvivalFuel]
      apply mul_nonneg _ ih
      unfold KiloblockConstruction.continueMacroAdvanceAt
      split
      · exact construction.continueMacroAdvanceProbability_nonneg _ excluded
      · exact zero_le_one

theorem KiloblockConstruction.continueSelectedSurvival_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded : NormalPlayer table)
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    {downstream : ℝ} (hdownstream : 0 ≤ downstream) :
    0 ≤ construction.continueSelectedSurvival k excluded choice remaining
      downstream := by
  cases choice with
  | none =>
      simpa [KiloblockConstruction.continueSelectedSurvival] using
        hdownstream
  | some owner =>
      have hendpoint : 0 ≤
          match (construction.attempt k owner).continuation with
          | .restart =>
              construction.continueMacroAdvanceProbability k excluded *
                downstream
          | .advance => downstream := by
        cases (construction.attempt k owner).continuation
        · exact mul_nonneg
            (construction.continueMacroAdvanceProbability_nonneg k excluded)
            hdownstream
        · exact hdownstream
      by_cases howner : owner = excluded
      · simpa [KiloblockConstruction.continueSelectedSurvival, howner]
          using hendpoint
      · simp only [KiloblockConstruction.continueSelectedSurvival,
          howner, if_false]
        apply mul_nonneg (pow_nonneg _ _) hendpoint
        have hhazard := quittingMeshHazard_le_one
          (construction.mesh k)
          (construction.attempt k owner).quitWeight_lt_one.le
        linarith

theorem KiloblockConstruction.continueFinalPotential_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (mode : KiloblockFiniteMode construction) :
    0 ≤ construction.continueFinalPotential excluded mode := by
  cases mode with
  | drawChoose k =>
      exact construction.continueMacroSurvivalFuel_nonneg excluded (k.1 + 1)
  | drawResume k choice remaining =>
      exact construction.continueSelectedSurvival_nonneg k excluded
        (choice.map construction.ownerOfCode) remaining.1
        (construction.continueMacroSurvivalFuel_nonneg excluded k.1)
  | drawFinal => exact zero_le_one
  | active k choice remaining =>
      exact construction.continueSelectedSurvival_nonneg k excluded
        (choice.map construction.ownerOfCode) remaining.1
        (construction.continueMacroSurvivalFuel_nonneg excluded k.1)
  | finalActive => exact zero_le_one
  | absorbed origin =>
      cases origin <;>
        simp [KiloblockConstruction.continueFinalPotential]

@[simp]
theorem KiloblockConstruction.continueFinalPotential_finiteMode_active
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    construction.continueFinalPotential excluded
        (construction.finiteMode (.active k choice remaining)) =
      construction.continueSelectedSurvival k excluded choice remaining
        (construction.continueMacroSurvivalFuel excluded k.1) := by
  cases choice <;>
    simp [KiloblockConstruction.continueFinalPotential,
      KiloblockConstruction.finiteMode, min_eq_left hremaining]

@[simp]
theorem KiloblockConstruction.continueFinalPotential_finiteMode_drawResume
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.totalMesh) :
    construction.continueFinalPotential excluded
        (construction.finiteMode (.draw (.resume k choice remaining))) =
      construction.continueSelectedSurvival k excluded choice remaining
        (construction.continueMacroSurvivalFuel excluded k.1) := by
  cases choice <;>
    simp [KiloblockConstruction.continueFinalPotential,
      KiloblockConstruction.finiteMode, min_eq_left hremaining]

theorem KiloblockConstruction.continueFinalPotential_preceding
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1)) :
    construction.continueFinalPotential excluded
        (construction.finiteMode
          (.draw (precedingKiloblockPhase table k))) =
      construction.continueMacroSurvivalFuel excluded k.1 := by
  induction k using Fin.induction with
  | zero =>
      simp [precedingKiloblockPhase,
        KiloblockConstruction.continueFinalPotential,
        KiloblockConstruction.finiteMode,
        KiloblockConstruction.continueMacroSurvivalFuel]
  | succ index ih =>
      have hindex :
          (⟨index.1, by omega⟩ :
            Fin (construction.blockCount + 1)) = index.castSucc := by
        apply Fin.ext
        rfl
      simp [precedingKiloblockPhase,
        KiloblockConstruction.continueFinalPotential,
        KiloblockConstruction.finiteMode, hindex]

theorem KiloblockConstruction.continueFinalPotential_after
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) :
    construction.continueFinalPotential excluded
        (construction.finiteMode (.draw
          (phaseAfterAttempt table
            (fun block owner =>
              (construction.attempt block owner).continuation)
            k choice))) =
      match choice with
      | none => construction.continueMacroSurvivalFuel excluded k.1
      | some owner =>
          match (construction.attempt k owner).continuation with
          | .restart =>
              construction.continueMacroAdvanceProbability k excluded *
                construction.continueMacroSurvivalFuel excluded k.1
          | .advance =>
              construction.continueMacroSurvivalFuel excluded k.1 := by
  cases choice with
  | none =>
      simpa [phaseAfterAttempt] using
        construction.continueFinalPotential_preceding excluded k
  | some owner =>
      cases hcontinuation : (construction.attempt k owner).continuation with
      | restart =>
          simpa [phaseAfterAttempt, hcontinuation,
            KiloblockConstruction.continueFinalPotential,
            KiloblockConstruction.finiteMode] using
              construction.continueMacroSurvivalFuel_succ excluded k
      | advance =>
          simpa [phaseAfterAttempt, hcontinuation] using
            construction.continueFinalPotential_preceding excluded k

private theorem KiloblockConstruction.expect_meshCoin_zero_else
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (value : ℝ) :
    expect (quittingMeshHazardCoin
        (construction.attempt k owner).quitWeight (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one)
      (fun quits => if quits then 0 else value) =
    (1 - quittingMeshHazard
      (construction.attempt k owner).quitWeight (construction.mesh k)) *
        value := by
  rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
  simp [quittingMeshHazardCoin_false_toReal,
    quittingMeshHazardCoin_true_toReal]

private theorem KiloblockConstruction.continueSelectedSurvival_other_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (excluded owner : NormalPlayer table) (howner : owner ≠ excluded)
    (remaining : ℕ) (downstream : ℝ) :
    construction.continueSelectedSurvival k excluded (some owner)
        remaining downstream =
      (1 - quittingMeshHazard
        (construction.attempt k owner).quitWeight (construction.mesh k)) *
        if remaining ≤ 1 then
          match (construction.attempt k owner).continuation with
          | .restart =>
              construction.continueMacroAdvanceProbability k excluded *
                downstream
          | .advance => downstream
        else construction.continueSelectedSurvival k excluded (some owner)
          (remaining - 1) downstream := by
  by_cases hsmall : remaining ≤ 1
  · cases hcontinuation : (construction.attempt k owner).continuation <;>
      simp [KiloblockConstruction.continueSelectedSurvival, howner,
        hsmall, hcontinuation]
  · have hmax : max remaining 1 = remaining := by omega
    have hmaxPred : max (remaining - 1) 1 = remaining - 1 := by omega
    have hremaining : remaining = remaining - 1 + 1 := by omega
    have hsub : remaining - 1 + 1 - 1 = remaining - 1 := by omega
    cases hcontinuation : (construction.attempt k owner).continuation <;>
      simp only [KiloblockConstruction.continueSelectedSurvival, howner,
        if_false, hsmall, hmax, hmaxPred, hcontinuation]
    · rw [hremaining, pow_succ, hsub]
      ring
    · rw [hremaining, pow_succ, hsub]
      ring

private theorem KiloblockConstruction.continueRawFinalPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (mode : KiloblockMode table construction.blockCount)
    (hremaining : ∀ k choice remaining,
      mode = .active k choice remaining ∨
        mode = .draw (.resume k choice remaining) →
          remaining ≤ construction.totalMesh) :
    expect (construction.continueModeKernel excluded mode) (fun next =>
        construction.continueFinalPotential excluded
          (construction.finiteMode next)) =
      construction.continueFinalPotential excluded
        (construction.finiteMode mode) := by
  cases mode with
  | draw phase =>
      cases phase with
      | choose k =>
          unfold KiloblockConstruction.continueModeKernel
          rw [Math.Probability.expect_map]
          simp_rw [construction.continueFinalPotential_finiteMode_active
            excluded k _ (construction.mesh k)
            (construction.mesh_le_totalMesh k)]
          rw [construction.expect_signal_continueSelectedSurvival_mesh
            k excluded
            (construction.continueMacroSurvivalFuel excluded k.1)]
          simpa [KiloblockConstruction.finiteMode,
            KiloblockConstruction.continueFinalPotential] using
              (construction.continueMacroSurvivalFuel_succ excluded k).symm
      | resume k choice remaining =>
          have hle := hremaining k choice remaining (Or.inr rfl)
          unfold KiloblockConstruction.continueModeKernel
          rw [Math.Probability.expect_pure,
            construction.continueFinalPotential_finiteMode_active
              excluded k choice remaining hle,
            construction.continueFinalPotential_finiteMode_drawResume
              excluded k choice remaining hle]
      | final =>
          simp [KiloblockConstruction.continueModeKernel,
            KiloblockConstruction.continueFinalPotential,
            KiloblockConstruction.finiteMode]
  | finalActive =>
      simp [KiloblockConstruction.continueModeKernel,
        KiloblockConstruction.continueFinalPotential,
        KiloblockConstruction.finiteMode]
  | absorbed origin =>
      simp [KiloblockConstruction.continueModeKernel,
        KiloblockConstruction.continueFinalPotential,
        KiloblockConstruction.finiteMode]
  | active k choice remaining =>
      have hle := hremaining k choice remaining (Or.inl rfl)
      have hcurrent := construction.continueFinalPotential_finiteMode_active
        excluded k choice remaining hle
      let downstream :=
        construction.continueMacroSurvivalFuel excluded k.1
      cases choice with
      | none =>
          by_cases hsmall : remaining ≤ 1
          · simp only [KiloblockConstruction.continueModeKernel,
              if_pos hsmall]
            rw [Math.Probability.expect_pure,
              construction.continueFinalPotential_after excluded k none,
              hcurrent]
            rfl
          · have hpred : remaining - 1 ≤ construction.totalMesh := by omega
            simp only [KiloblockConstruction.continueModeKernel,
              if_neg hsmall]
            rw [Math.Probability.expect_pure,
              construction.continueFinalPotential_finiteMode_drawResume
                excluded k none (remaining - 1) hpred,
              hcurrent]
            rfl
      | some owner =>
          by_cases howner : owner = excluded
          · subst owner
            by_cases hsmall : remaining ≤ 1
            · simp only [KiloblockConstruction.continueModeKernel,
                if_pos hsmall, ite_true,
                Math.Probability.expect_pure]
              rw [construction.continueFinalPotential_after
                excluded k (some excluded), hcurrent]
              cases hcontinuation :
                  (construction.attempt k excluded).continuation <;>
                simp [KiloblockConstruction.continueSelectedSurvival,
                  hcontinuation]
            · have hpred : remaining - 1 ≤
                  construction.totalMesh := by omega
              simp only [KiloblockConstruction.continueModeKernel,
                if_neg hsmall, ite_true,
                Math.Probability.expect_pure]
              rw [construction.continueFinalPotential_finiteMode_drawResume
                excluded k (some excluded) (remaining - 1) hpred,
                hcurrent]
              cases hcontinuation :
                  (construction.attempt k excluded).continuation <;>
                simp [KiloblockConstruction.continueSelectedSurvival,
                  hcontinuation]
          · by_cases hsmall : remaining ≤ 1
            · simp only [KiloblockConstruction.continueModeKernel,
                if_pos hsmall, howner, ite_false]
              let coin := quittingMeshHazardCoin
                (construction.attempt k owner).quitWeight
                (construction.mesh k)
                (construction.attempt k owner).quitWeight_pos.le
                (construction.attempt k owner).quitWeight_lt_one
              let afterMode : KiloblockMode table construction.blockCount :=
                .draw (phaseAfterAttempt table
                  (fun block selected =>
                    (construction.attempt block selected).continuation)
                  k (some owner))
              change expect (coin.bind fun quits => if quits = true then
                  PMF.pure (KiloblockMode.absorbed (some k))
                else PMF.pure afterMode) (fun next =>
                  construction.continueFinalPotential excluded
                    (construction.finiteMode next)) = _
              have hkernel : (coin.bind fun quits => if quits = true then
                    PMF.pure (KiloblockMode.absorbed (some k))
                  else PMF.pure afterMode) =
                  coin.map fun quits => if quits = true then
                    KiloblockMode.absorbed (some k) else afterMode := by
                rw [← PMF.bind_pure_comp]
                congr 1
                funext quits
                cases quits <;> rfl
              rw [hkernel, Math.Probability.expect_map]
              rw [show (fun quits => construction.continueFinalPotential
                    excluded (construction.finiteMode
                      (if quits = true then
                        KiloblockMode.absorbed (some k) else afterMode))) =
                  fun quits => if quits = true then 0 else
                    match (construction.attempt k owner).continuation with
                    | .restart =>
                        construction.continueMacroAdvanceProbability
                          k excluded * downstream
                    | .advance => downstream by
                funext quits
                cases quits with
                | false =>
                    dsimp only [afterMode]
                    exact construction.continueFinalPotential_after
                      excluded k (some owner)
                | true => rfl]
              change expect coin _ = _
              rw [construction.expect_meshCoin_zero_else]
              rw [hcurrent]
              dsimp only [downstream]
              simpa only [if_pos hsmall] using
                (construction.continueSelectedSurvival_other_step
                  k excluded owner howner remaining
                    (construction.continueMacroSurvivalFuel
                      excluded k.1)).symm
            · have hpred : remaining - 1 ≤
                  construction.totalMesh := by omega
              simp only [KiloblockConstruction.continueModeKernel,
                if_neg hsmall, howner, ite_false]
              let coin := quittingMeshHazardCoin
                (construction.attempt k owner).quitWeight
                (construction.mesh k)
                (construction.attempt k owner).quitWeight_pos.le
                (construction.attempt k owner).quitWeight_lt_one
              let afterMode : KiloblockMode table construction.blockCount :=
                .draw (.resume k (some owner) (remaining - 1))
              change expect (coin.bind fun quits => if quits = true then
                  PMF.pure (KiloblockMode.absorbed (some k))
                else PMF.pure afterMode) (fun next =>
                  construction.continueFinalPotential excluded
                    (construction.finiteMode next)) = _
              have hkernel : (coin.bind fun quits => if quits = true then
                    PMF.pure (KiloblockMode.absorbed (some k))
                  else PMF.pure afterMode) =
                  coin.map fun quits => if quits = true then
                    KiloblockMode.absorbed (some k) else afterMode := by
                rw [← PMF.bind_pure_comp]
                congr 1
                funext quits
                cases quits <;> rfl
              rw [hkernel, Math.Probability.expect_map]
              rw [show (fun quits => construction.continueFinalPotential
                    excluded (construction.finiteMode
                      (if quits = true then
                        KiloblockMode.absorbed (some k) else afterMode))) =
                  fun quits => if quits = true then 0 else
                    construction.continueSelectedSurvival k excluded
                      (some owner) (remaining - 1) downstream by
                funext quits
                cases quits with
                | false =>
                    dsimp only [afterMode, downstream]
                    exact construction.continueFinalPotential_finiteMode_drawResume
                      excluded k (some owner) (remaining - 1) hpred
                | true => rfl]
              change expect coin _ = _
              rw [construction.expect_meshCoin_zero_else]
              rw [hcurrent]
              dsimp only [downstream]
              simpa only [if_neg hsmall] using
                (construction.continueSelectedSurvival_other_step
                  k excluded owner howner remaining
                    (construction.continueMacroSurvivalFuel
                      excluded k.1)).symm

private theorem KiloblockConstruction.modeOfFinite_remaining_le_totalMesh
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    ∀ k choice remaining,
      construction.modeOfFinite mode = .active k choice remaining ∨
        construction.modeOfFinite mode =
          .draw (.resume k choice remaining) →
            remaining ≤ construction.totalMesh := by
  intro k choice remaining hmode
  cases mode <;> simp only [KiloblockConstruction.modeOfFinite] at hmode
  · rcases hmode with hmode | hmode <;> cases hmode
  · rcases hmode with hmode | hmode
    · cases hmode
    · cases hmode
      omega
  · rcases hmode with hmode | hmode <;> cases hmode
  · rcases hmode with hmode | hmode
    · cases hmode
      omega
    · cases hmode
  · rcases hmode with hmode | hmode <;> cases hmode
  · rcases hmode with hmode | hmode <;> cases hmode

theorem KiloblockConstruction.continueFinalPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (mode : KiloblockFiniteMode construction) :
    expect (construction.continueFiniteModeKernel excluded mode)
        (construction.continueFinalPotential excluded) =
      construction.continueFinalPotential excluded mode := by
  unfold KiloblockConstruction.continueFiniteModeKernel
  rw [Math.Probability.expect_map]
  rw [construction.continueRawFinalPotential_harmonic excluded
    (construction.modeOfFinite mode)
    (construction.modeOfFinite_remaining_le_totalMesh mode)]
  rw [construction.finiteMode_modeOfFinite]

theorem KiloblockConstruction.continueFinalPotential_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    construction.continueFinalPotential excluded
        (.drawChoose (Fin.last construction.blockCount)) =
      construction.continueMacroSurvivalProbability excluded := by
  change construction.continueMacroSurvivalFuel excluded
      (construction.blockCount + 1) = _
  exact construction.continueMacroSurvivalFuel_full excluded

def KiloblockConstruction.continueAbsorbedIndicator
    {table : Table ι} {ε : ℝ}
    (_construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode _construction → ℝ
  | .absorbed (some _) => 1
  | _ => 0

def KiloblockConstruction.continuePastFinalIndicator
    {table : Table ι} {ε : ℝ}
    (_construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode _construction → ℝ
  | .drawFinal => 1
  | .finalActive => 1
  | .absorbed none => 1
  | _ => 0

theorem KiloblockConstruction.continuePastFinalIndicator_le_finalPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (mode : KiloblockFiniteMode construction) :
    construction.continuePastFinalIndicator mode ≤
      construction.continueFinalPotential excluded mode := by
  cases mode with
  | drawChoose k =>
      exact construction.continueFinalPotential_nonneg excluded _
  | drawResume k choice remaining =>
      exact construction.continueFinalPotential_nonneg excluded _
  | active k choice remaining =>
      exact construction.continueFinalPotential_nonneg excluded _
  | drawFinal => exact le_rfl
  | finalActive => exact le_rfl
  | absorbed origin =>
      cases origin <;> exact le_rfl

theorem KiloblockConstruction.continueIndicators_partition
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    construction.continueAbsorbedIndicator mode +
        construction.continuePastFinalIndicator mode +
        Math.Probability.transientCharge
          construction.continueBoundary mode = 1 := by
  cases mode with
  | drawChoose k =>
      have hnot : (.drawChoose k : KiloblockFiniteMode construction) ∉
          construction.continueBoundary := by
        intro hmode
        exact hmode
      rw [Math.Probability.transientCharge_of_not_mem hnot]
      simp [KiloblockConstruction.continueAbsorbedIndicator,
        KiloblockConstruction.continuePastFinalIndicator]
  | drawResume k choice remaining =>
      have hnot : (.drawResume k choice remaining :
          KiloblockFiniteMode construction) ∉
          construction.continueBoundary := by
        intro hmode
        exact hmode
      rw [Math.Probability.transientCharge_of_not_mem hnot]
      simp [KiloblockConstruction.continueAbsorbedIndicator,
        KiloblockConstruction.continuePastFinalIndicator]
  | drawFinal =>
      rw [Math.Probability.transientCharge_of_mem
        (show (.drawFinal : KiloblockFiniteMode construction) ∈
          construction.continueBoundary from trivial)]
      simp [KiloblockConstruction.continueAbsorbedIndicator,
        KiloblockConstruction.continuePastFinalIndicator]
  | active k choice remaining =>
      have hnot : (.active k choice remaining :
          KiloblockFiniteMode construction) ∉
          construction.continueBoundary := by
        intro hmode
        exact hmode
      rw [Math.Probability.transientCharge_of_not_mem hnot]
      simp [KiloblockConstruction.continueAbsorbedIndicator,
        KiloblockConstruction.continuePastFinalIndicator]
  | finalActive =>
      rw [Math.Probability.transientCharge_of_mem
        (show (.finalActive : KiloblockFiniteMode construction) ∈
          construction.continueBoundary from trivial)]
      simp [KiloblockConstruction.continueAbsorbedIndicator,
        KiloblockConstruction.continuePastFinalIndicator]
  | absorbed origin =>
      cases origin with
      | none =>
          rw [Math.Probability.transientCharge_of_mem
            (show (.absorbed none : KiloblockFiniteMode construction) ∈
              construction.continueBoundary from trivial)]
          simp [KiloblockConstruction.continueAbsorbedIndicator,
            KiloblockConstruction.continuePastFinalIndicator]
      | some k =>
          rw [Math.Probability.transientCharge_of_mem
            (show (.absorbed (some k) :
              KiloblockFiniteMode construction) ∈
                construction.continueBoundary from trivial)]
          simp [KiloblockConstruction.continueAbsorbedIndicator,
            KiloblockConstruction.continuePastFinalIndicator]

theorem KiloblockConstruction.continueFinalPotential_eq_on_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {mode : KiloblockFiniteMode construction}
    (hmode : mode ∈ construction.continueBoundary) :
    construction.continueFinalPotential excluded mode =
      construction.continuePastFinalIndicator mode := by
  cases mode with
  | drawChoose k => contradiction
  | drawResume k choice remaining => contradiction
  | drawFinal => rfl
  | active k choice remaining => contradiction
  | finalActive => rfl
  | absorbed origin => cases origin <;> rfl

theorem KiloblockConstruction.expect_iter_continueFinalPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (steps : ℕ)
    (initial : KiloblockFiniteMode construction) :
    expect (Math.PMFIter.iter
        (construction.continueFiniteModeKernel excluded) steps initial)
        (construction.continueFinalPotential excluded) =
      construction.continueFinalPotential excluded initial := by
  induction steps with
  | zero =>
      simp [Math.PMFIter.iter_zero]
  | succ steps ih =>
      rw [Math.PMFIter.iter_succ', Math.Probability.expect_bind]
      simp_rw [construction.continueFinalPotential_harmonic excluded]
      exact ih

theorem KiloblockConstruction.abs_expect_finalPotential_sub_pastFinal_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (distribution : PMF (KiloblockFiniteMode construction)) :
    |expect distribution (construction.continueFinalPotential excluded) -
        expect distribution construction.continuePastFinalIndicator| ≤
      ‖construction.continueFinalPotential excluded‖ *
        expect distribution (Math.Probability.transientCharge
          construction.continueBoundary) := by
  let potential := construction.continueFinalPotential excluded
  let pastFinal := construction.continuePastFinalIndicator
  let transient := Math.Probability.transientCharge
    construction.continueBoundary
  let bound := ‖potential‖
  have hbound : 0 ≤ bound := norm_nonneg _
  have hpoint : ∀ mode, |potential mode - pastFinal mode| ≤
      bound * transient mode := by
    intro mode
    by_cases hmode : mode ∈ construction.continueBoundary
    · have heq := construction.continueFinalPotential_eq_on_boundary
        excluded hmode
      simp [potential, pastFinal, transient, heq,
        Math.Probability.transientCharge_of_mem hmode]
    · have hpast : pastFinal mode = 0 := by
        cases mode with
        | drawChoose k => rfl
        | drawResume k choice remaining => rfl
        | drawFinal => exact False.elim (hmode trivial)
        | active k choice remaining => rfl
        | finalActive => exact False.elim (hmode trivial)
        | absorbed origin =>
            cases origin with
            | none => exact False.elim (hmode trivial)
            | some k => rfl
      rw [hpast, sub_zero]
      dsimp only [transient]
      rw [Math.Probability.transientCharge_of_not_mem hmode, mul_one]
      exact norm_le_pi_norm potential mode
  have hupper := Math.Probability.expect_mono distribution
    (fun mode => potential mode - pastFinal mode)
    (fun mode => bound * transient mode)
    (fun mode => (le_abs_self _).trans (hpoint mode))
  have hlower := Math.Probability.expect_mono distribution
    (fun mode => -(bound * transient mode))
    (fun mode => potential mode - pastFinal mode)
    (fun mode => (neg_le_of_abs_le (hpoint mode)))
  have hdiff : expect distribution (fun mode =>
      potential mode - pastFinal mode) =
      expect distribution potential - expect distribution pastFinal :=
    Math.Probability.expect_sub distribution potential pastFinal
  have hscale : expect distribution (fun mode => bound * transient mode) =
      bound * expect distribution transient :=
    Math.Probability.expect_const_mul distribution bound transient
  have hnegScale : expect distribution (fun mode =>
      -(bound * transient mode)) =
      -(bound * expect distribution transient) := by
    rw [Math.Probability.expect_neg, hscale]
  rw [hdiff, hscale] at hupper
  rw [hdiff, hnegScale] at hlower
  exact abs_le.mpr ⟨hlower, hupper⟩

theorem KiloblockConstruction.exists_iter_transientCharge_lt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {threshold : ℝ}
    (hthreshold : 0 < threshold)
    (initial : KiloblockFiniteMode construction) :
    ∃ steps, expect (Math.PMFIter.iter
        (construction.continueFiniteModeKernel excluded) steps initial)
        (Math.Probability.transientCharge construction.continueBoundary) <
      threshold := by
  let certificate := Classical.choice
    (construction.continueKernel_transienceCertificate excluded)
  let occupationBound :=
    (certificate.horizon : ℝ) / certificate.minorization
  obtain ⟨horizon, hhorizon⟩ :=
    exists_nat_gt (occupationBound / threshold)
  have hlarge : occupationBound < (horizon : ℝ) * threshold := by
    exact (div_lt_iff₀ hthreshold).mp hhorizon
  by_contra hnot
  push Not at hnot
  have hlower : (horizon : ℝ) * threshold ≤
      ∑ time ∈ Finset.range horizon,
        expect (Math.PMFIter.iter
          (construction.continueFiniteModeKernel excluded) time initial)
          (Math.Probability.transientCharge
            construction.continueBoundary) := by
    calc
      (horizon : ℝ) * threshold =
          ∑ _time ∈ Finset.range horizon, threshold := by simp
      _ ≤ _ := Finset.sum_le_sum fun time _ => hnot time
  have hupper := certificate.total_transientOccupation_le horizon initial
  exact (not_lt_of_ge (hlower.trans hupper)) hlarge

theorem KiloblockConstruction.exists_iter_continueAbsorbedIndicator_gt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    ∃ steps, 1 - ε <
      expect (Math.PMFIter.iter
        (construction.continueFiniteModeKernel excluded) steps
        (.drawChoose (Fin.last construction.blockCount)))
        construction.continueAbsorbedIndicator := by
  let initial : KiloblockFiniteMode construction :=
    .drawChoose (Fin.last construction.blockCount)
  let survival := construction.continueMacroSurvivalProbability excluded
  let potential := construction.continueFinalPotential excluded
  let pastFinal := construction.continuePastFinalIndicator
  let absorbed := construction.continueAbsorbedIndicator
  let transient := Math.Probability.transientCharge
    construction.continueBoundary
  let bound := ‖potential‖
  have hbound : 0 ≤ bound := norm_nonneg _
  have hgap : 0 < ε - survival := by
    dsimp only [survival]
    linarith [construction.continueMacroSurvivalProbability_lt_epsilon
      excluded]
  let threshold := (ε - survival) / (2 * (bound + 1))
  have hdenominator : 0 < 2 * (bound + 1) := by positivity
  have hthreshold : 0 < threshold :=
    div_pos hgap hdenominator
  obtain ⟨steps, htransient⟩ :=
    construction.exists_iter_transientCharge_lt excluded hthreshold initial
  let distribution := Math.PMFIter.iter
    (construction.continueFiniteModeKernel excluded) steps initial
  have hpartition :
      expect distribution absorbed + expect distribution pastFinal +
          expect distribution transient = 1 := by
    calc
      expect distribution absorbed + expect distribution pastFinal +
          expect distribution transient =
          expect distribution (fun mode =>
            absorbed mode + pastFinal mode + transient mode) := by
        rw [Math.Probability.expect_add, Math.Probability.expect_add]
      _ = expect distribution (fun _ => (1 : ℝ)) := by
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro mode _
        exact construction.continueIndicators_partition mode
      _ = 1 := Math.Probability.expect_const distribution 1
  have hpotential : expect distribution potential = survival := by
    dsimp only [distribution, potential, initial, survival]
    rw [construction.expect_iter_continueFinalPotential]
    exact construction.continueFinalPotential_initial excluded
  have herror := construction.abs_expect_finalPotential_sub_pastFinal_le
    excluded distribution
  rw [hpotential] at herror
  change |survival - expect distribution pastFinal| ≤
    bound * expect distribution transient at herror
  have hpastUpper : expect distribution pastFinal ≤
      survival + bound * expect distribution transient := by
    have hupper := (neg_le_abs (survival -
      expect distribution pastFinal)).trans herror
    linarith
  have hscaled : (bound + 1) * expect distribution transient <
      (ε - survival) / 2 := by
    have hmul := mul_lt_mul_of_pos_left htransient (by positivity :
      0 < bound + 1)
    dsimp only [threshold] at hmul
    calc
      (bound + 1) * expect distribution transient <
          (bound + 1) *
            ((ε - survival) / (2 * (bound + 1))) := hmul
      _ = (ε - survival) / 2 := by
        field_simp [ne_of_gt (show 0 < bound + 1 by positivity)]
  refine ⟨steps, ?_⟩
  dsimp only [distribution, absorbed, initial] at hpartition ⊢
  dsimp only [pastFinal, transient] at hpartition hpastUpper hscaled
  linarith

/-! The operational schedule itself implies the unilateral-quitting clause:
at an active history either one selected owner uses the mesh coin or everyone
continues. -/
theorem KiloblockConstruction.atMostOneQuitter
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    AtMostOneQuitter construction.profile := by
  classical
  intro t history
  cases hstate : history.2 with
  | draw => trivial
  | absorbed quitters => trivial
  | active signal =>
      have hmatches := construction.mode_state t history
      rw [hstate] at hmatches
      cases hmode : construction.mode t history with
      | draw phase => simp [KiloblockMode.MatchesState, hmode] at hmatches
      | absorbed origin =>
          simp [KiloblockMode.MatchesState, hmode] at hmatches
      | finalActive =>
          simp [construction.strategy_eq, hmode]
      | active k choice remaining =>
          cases choice with
          | none =>
              simp [construction.strategy_eq, hmode]
          | some owner =>
              calc
                (Finset.filter (fun who =>
                    0 < ((construction.profile.strategy who t history)
                      true).toReal) Finset.univ).card ≤
                    ({owner.1} : Finset ι).card := by
                  apply Finset.card_le_card
                  intro who hwho
                  simp only [Finset.mem_filter, Finset.mem_univ,
                    true_and] at hwho
                  simp only [Finset.mem_singleton]
                  by_contra hne
                  have : ¬0 < ((construction.profile.strategy who t history)
                      true).toReal := by
                    simp [construction.strategy_eq, hmode, hne]
                  exact this hwho
                _ = 1 := Finset.card_singleton owner.1

/-! At a selected type-`i` history, the joint action law is exactly one
Bernoulli coin at coordinate `i`; every other coordinate is Continue. -/
theorem KiloblockConstruction.stageActionDist_active_some
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history =
      (quittingMeshHazardCoin (construction.attempt k owner).quitWeight
          (construction.mesh k)
          (construction.attempt k owner).quitWeight_pos.le
          (construction.attempt k owner).quitWeight_lt_one).bind
        (fun quits => PMF.pure
          (Function.update (fun _ : ι => false) owner.1 quits)) := by
  exact construction.stageActionDist_active_some_core
    history k owner remaining hmode

def singleQuitAction (owner : ι) (quits : Bool) : ι → Bool :=
  Function.update (fun _ => false) owner quits

omit [Fintype ι] in
@[simp]
theorem singleQuitAction_false (owner who : ι) :
    singleQuitAction owner false who = false := by
  by_cases hwho : who = owner
  · subst who
    simp [singleQuitAction]
  · rw [singleQuitAction, Function.update_of_ne hwho]

theorem singleQuitAction_true_quitters (owner : ι) :
    ({who | singleQuitAction owner true who = true} : Finset ι) =
      {owner} := by
  ext who
  by_cases hwho : who = owner
  · subst who
    simp [singleQuitAction]
  · simp only [Finset.mem_filter, Finset.mem_univ, true_and,
      Finset.mem_singleton]
    rw [singleQuitAction, Function.update_of_ne hwho]
    simp [hwho]

theorem publicQuittingGame_transition_singleQuitAction
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) (signal : Signal)
    (owner : ι) :
    (publicQuittingGame table signalLaw).transition (.active signal)
        (singleQuitAction owner true) =
      PMF.pure (.absorbed (quittingProjectiveSingletonTerminal owner)) := by
  simp only [publicQuittingGame, singleQuitAction_true_quitters]
  split
  · congr
  · rename_i h
    exact False.elim (h (by simp))

theorem publicQuittingGame_transition_singleQuitAction_false
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal) (signal : Signal)
    (owner : ι) :
    (publicQuittingGame table signalLaw).transition (.active signal)
        (singleQuitAction owner false) = PMF.pure .draw := by
  simp [publicQuittingGame, singleQuitAction_false]

def KiloblockConstruction.trackingCorrectionAtOrigin
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (origin : Option (Fin (construction.blockCount + 1)))
    (who : NormalPlayer table) : ℝ :=
  origin.elim 0 (fun k => construction.trackingCorrection k who)

/-! The history potential used in Lemma 3.8. It is the exact remaining block
value plus the signed (A.2'') telescope. After pre-final absorption it is the
real terminal payoff plus the same correction. The final all-Continue tail is
assigned zero, as in the accepted manuscript's proof. -/
def KiloblockConstruction.normalPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history =>
    match construction.mode t history with
    | .draw (.choose k) =>
        (construction.buildingBlock k).w who +
          construction.trackingCorrection k who
    | .draw (.resume k choice remaining) =>
        construction.selectedValue k choice remaining who +
          construction.trackingCorrection k who
    | .draw .final => 0
    | .active k choice remaining =>
        construction.selectedValue k choice remaining who +
          construction.trackingCorrection k who
    | .finalActive => 0
    | .absorbed origin =>
        (match history.2 with
        | .absorbed quitters => table.terminal quitters who.1
        | _ => 0) + construction.trackingCorrectionAtOrigin origin who

/-! The same block-value potential with the genuine prescribed endpoint
`y⁰_i` on the final all-Continue tail.  This is the endpoint hidden by the
zero convention in the manuscript's displayed process. -/
def KiloblockConstruction.normalEndpointPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history =>
    match construction.mode t history with
    | .draw .final => construction.point ⟨0, by omega⟩ who
    | .finalActive => construction.point ⟨0, by omega⟩ who
    | _ => construction.normalPotential who t history

private theorem historyContinuationEU_congr_next
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (left right : (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential)
    (heq : ∀ action,
      action ∈ ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
          construction.profile.strategy history).support →
      ∀ nextState,
        nextState ∈ ((publicQuittingGame table
          construction.profile.signalLaw).transition
            history.2 action).support →
        left (t + 1)
            (Fin.snoc history.1 (history.2, action), nextState) =
          right (t + 1)
            (Fin.snoc history.1 (history.2, action), nextState)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy left history =
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          construction.profile.strategy right history := by
  unfold StochasticGame.historyContinuationEU
  apply Math.ProbabilityMassFunction.expect_congr_on_support
  intro action haction
  apply Math.ProbabilityMassFunction.expect_congr_on_support
  intro nextState hnext
  exact heq action haction nextState hnext

private theorem historyContinuationEU_add_const_mul
    {table : Table ι} {Signal : Type} [Fintype Signal]
    (signalLaw : PMF Signal)
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (left right : (publicQuittingGame table signalLaw).HistoryPotential)
    (constant : ℝ) {t : ℕ}
    (history : (publicQuittingGame table signalLaw).Hist t) :
    (publicQuittingGame table signalLaw).historyContinuationEU strategy
        (fun nextTime nextHistory => left nextTime nextHistory +
          constant * right nextTime nextHistory) history =
      (publicQuittingGame table signalLaw).historyContinuationEU
          strategy left history +
        constant * (publicQuittingGame table
          signalLaw).historyContinuationEU strategy right history := by
  unfold StochasticGame.historyContinuationEU
  calc
    expect ((publicQuittingGame table signalLaw).stageActionDist
        strategy history)
        (fun action => expect
          ((publicQuittingGame table signalLaw).transition history.2 action)
          (fun nextState => left (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState) +
            constant * right (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))) =
        expect ((publicQuittingGame table signalLaw).stageActionDist
          strategy history) (fun action =>
            expect ((publicQuittingGame table signalLaw).transition
              history.2 action) (fun nextState => left (t + 1)
                (Fin.snoc history.1 (history.2, action), nextState)) +
            constant * expect ((publicQuittingGame table
              signalLaw).transition history.2 action) (fun nextState =>
                right (t + 1)
                  (Fin.snoc history.1 (history.2, action), nextState))) := by
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro action _
      rw [Math.Probability.expect_add,
        Math.Probability.expect_const_mul]
    _ = _ := by
      rw [Math.Probability.expect_add,
        Math.Probability.expect_const_mul]

private theorem KiloblockConstruction.normalEndpointPotential_draw_final
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : construction.mode t history = .draw .final) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  rw [KiloblockConstruction.normalEndpointPotential, hmode]
  unfold StochasticGame.historyContinuationEU
  calc
    expect
        ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy history)
        (fun action =>
          expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.normalEndpointPotential who (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))) =
        expect
          ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy history)
          (fun _ => construction.point ⟨0, by omega⟩ who) := by
      apply congrArg (expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
          construction.profile.strategy history))
      funext action
      rw [hstate]
      simp only [publicQuittingGame]
      rw [Math.Probability.expect_map]
      rw [← Math.Probability.expect_const
        construction.profile.signalLaw
        (construction.point ⟨0, by omega⟩ who)]
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro signal hsignal
      have hsupp : PublicQuittingState.active signal ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              history.2 action).support := by
        rw [hstate]
        simp only [publicQuittingGame]
        exact (PMF.mem_support_map_iff _ _ _).2
          ⟨signal, hsignal, rfl⟩
      have hstep := construction.mode_step t history action
        (PublicQuittingState.active signal) hsupp
      have hm : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, action),
            PublicQuittingState.active signal) = .finalActive := by
        simpa [KiloblockModeStep, hmode] using hstep
      have hvalue : construction.normalEndpointPotential who (t + 1)
          (Fin.snoc history.1 (history.2, action),
            PublicQuittingState.active signal) =
          construction.point ⟨0, by omega⟩ who := by
        rw [KiloblockConstruction.normalEndpointPotential, hm]
      rw [hstate] at hvalue
      exact hvalue
    _ = _ := Math.Probability.expect_const _ _

private theorem KiloblockConstruction.normalEndpointPotential_finalActive
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : construction.mode t history = .finalActive) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      let continueAction : (publicQuittingGame table
        construction.profile.signalLaw).JointAct := fun _ => false
      let drawState : (publicQuittingGame table
        construction.profile.signalLaw).State := PublicQuittingState.draw
      let base : ∀ player, PMF ((publicQuittingGame table
          construction.profile.signalLaw).Act player) :=
        fun _ => PMF.pure false
      have hfamily :
          (fun player => construction.profile.strategy player t
            (past, PublicQuittingState.active signal)) = base := by
        funext player
        rw [construction.strategy_eq]
        simp [hmode]
        rfl
      have hproduct : Math.PMFProduct.pmfPi base =
          PMF.pure continueAction :=
        Math.PMFProduct.pmfPi_pure continueAction
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal) continueAction =
          PMF.pure drawState := by
        simp [publicQuittingGame, continueAction, drawState]
      have hsupp : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
            (PublicQuittingState.active signal) continueAction).support := by
        rw [htransition, PMF.mem_support_iff]
        simp
      have hstep := construction.mode_step t
        (past, PublicQuittingState.active signal) continueAction
        drawState hsupp
      have hm : construction.mode (t + 1)
          (Fin.snoc past
              (PublicQuittingState.active signal, continueAction),
            drawState) = .draw .final := by
        simpa [KiloblockModeStep, hmode, drawState] using hstep
      rw [KiloblockConstruction.normalEndpointPotential, hmode]
      unfold StochasticGame.historyContinuationEU
      unfold StochasticGame.stageActionDist
      rw [hfamily, hproduct, Math.Probability.expect_pure,
        htransition, Math.Probability.expect_pure]
      rw [KiloblockConstruction.normalEndpointPotential, hm]

private theorem KiloblockConstruction.endpointPotential_active_some_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some owner) remaining)
    (potential : (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential)
    (quitValue continuation : ℝ)
    (hquit : ∀ signal,
      history.2 = PublicQuittingState.active signal →
      potential (t + 1)
        (Fin.snoc history.1
            (history.2, singleQuitAction owner.1 true),
          PublicQuittingState.absorbed
            (quittingProjectiveSingletonTerminal owner.1)) = quitValue)
    (hcontinue : ∀ signal,
      history.2 = PublicQuittingState.active signal →
      potential (t + 1)
        (Fin.snoc history.1
            (history.2, singleQuitAction owner.1 false),
          PublicQuittingState.draw) = continuation) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy potential history =
      quittingMeshHazard (construction.attempt k owner).quitWeight
          (construction.mesh k) * quitValue +
        (1 - quittingMeshHazard (construction.attempt k owner).quitWeight
          (construction.mesh k)) * continuation := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      have htransitionQuit :=
        publicQuittingGame_transition_singleQuitAction table
          construction.profile.signalLaw signal owner.1
      have htransitionContinue :=
        publicQuittingGame_transition_singleQuitAction_false table
          construction.profile.signalLaw signal owner.1
      have hquitValue := hquit signal rfl
      have hcontinueValue := hcontinue signal rfl
      let coin := quittingMeshHazardCoin
        (construction.attempt k owner).quitWeight (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one
      let actionLaw : PMF ((publicQuittingGame table
          construction.profile.signalLaw).JointAct) :=
        coin.bind (fun quits => PMF.pure
          (singleQuitAction owner.1 quits))
      have hactionLaw : (publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
          construction.profile.strategy
          (past, PublicQuittingState.active signal) =
          actionLaw := by
        unfold actionLaw coin singleQuitAction
        exact construction.stageActionDist_active_some
          (past, PublicQuittingState.active signal) k owner remaining hmode
      letI : Finite ((publicQuittingGame table
          construction.profile.signalLaw).JointAct) :=
        inferInstanceAs (Finite (ι → Bool))
      unfold StochasticGame.historyContinuationEU
      rw [hactionLaw]
      unfold actionLaw
      rw [Math.Probability.expect_bind]
      simp_rw [Math.Probability.expect_pure]
      rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
      rw [htransitionContinue, Math.Probability.expect_pure,
        hcontinueValue, htransitionQuit, Math.Probability.expect_pure,
        hquitValue]
      unfold coin
      simp only [quittingMeshHazardCoin_false_toReal,
        quittingMeshHazardCoin_true_toReal]

private theorem KiloblockConstruction.normalEndpointPotential_active_some
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some owner) remaining) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  have hremaining : 0 < remaining :=
    construction.mode_remaining_pos t history k (some owner) remaining
      (Or.inl hmode)
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      obtain ⟨tail, rfl⟩ := Nat.exists_eq_succ_of_ne_zero
        (Nat.ne_of_gt hremaining)
      let drawState : (publicQuittingGame table
          construction.profile.signalLaw).State := PublicQuittingState.draw
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal)
            (singleQuitAction owner.1 false) = PMF.pure drawState := by
        exact publicQuittingGame_transition_singleQuitAction_false table
          construction.profile.signalLaw signal owner.1
      have hsupp : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              (PublicQuittingState.active signal)
              (singleQuitAction owner.1 false)).support := by
        rw [htransition, PMF.mem_support_iff]
        simp
      have hstep := construction.mode_step t
        (past, PublicQuittingState.active signal)
        (singleQuitAction owner.1 false) drawState hsupp
      let continuation := construction.selectedValue k (some owner) tail who +
        construction.trackingCorrection k who
      have hcontinueValue : construction.normalEndpointPotential who (t + 1)
          (Fin.snoc past
              (PublicQuittingState.active signal,
                singleQuitAction owner.1 false), drawState) =
          continuation := by
        cases tail with
        | succ nextTail =>
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                    (PublicQuittingState.active signal,
                      singleQuitAction owner.1 false), drawState) =
                .draw (.resume k (some owner) (nextTail + 1)) := by
              simpa [KiloblockModeStep, hmode, drawState,
                singleQuitAction_false] using hstep
            rw [KiloblockConstruction.normalEndpointPotential, hm,
              KiloblockConstruction.normalPotential, hm]
        | zero =>
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                    (PublicQuittingState.active signal,
                      singleQuitAction owner.1 false), drawState) =
                .draw (phaseAfterAttempt table
                  (fun block selected =>
                    (construction.attempt block selected).continuation)
                  k (some owner)) := by
              simpa [KiloblockModeStep, hmode, drawState,
                singleQuitAction_false] using hstep
            cases hbranch : (construction.attempt k owner).continuation with
            | restart =>
                have hm' : construction.mode (t + 1)
                    (Fin.snoc past
                        (PublicQuittingState.active signal,
                          singleQuitAction owner.1 false), drawState) =
                    .draw (.choose k) := by
                  simpa [phaseAfterAttempt, hbranch] using hm
                rw [KiloblockConstruction.normalEndpointPotential, hm',
                  KiloblockConstruction.normalPotential, hm']
                simp [continuation, KiloblockConstruction.selectedValue,
                  BuildingAttempt.remainingValue_zero, hbranch]
            | advance =>
                by_cases hk : k.1 = 0
                · have hkEq : k = ⟨0, by omega⟩ := Fin.ext hk
                  have hm' : construction.mode (t + 1)
                      (Fin.snoc past
                          (PublicQuittingState.active signal,
                            singleQuitAction owner.1 false), drawState) =
                      .draw .final := by
                    simpa [phaseAfterAttempt, precedingKiloblockPhase,
                      hbranch, hk] using hm
                  rw [KiloblockConstruction.normalEndpointPotential, hm']
                  simp [continuation, KiloblockConstruction.selectedValue,
                    BuildingAttempt.remainingValue_zero, hbranch, hkEq]
                  exact construction.trackingCorrection_zero who
                · have hkpos : 0 < k.1 := Nat.pos_of_ne_zero hk
                  let previous : Fin (construction.blockCount + 1) :=
                    ⟨k.1 - 1, by omega⟩
                  have hprevious : previous.1 < construction.blockCount := by
                    dsimp [previous]
                    omega
                  have hkEq : k = ⟨previous.1 + 1, by omega⟩ := by
                    apply Fin.ext
                    dsimp [previous]
                    omega
                  have hm' : construction.mode (t + 1)
                      (Fin.snoc past
                          (PublicQuittingState.active signal,
                            singleQuitAction owner.1 false), drawState) =
                      .draw (.choose previous) := by
                    simpa [phaseAfterAttempt, precedingKiloblockPhase,
                      hbranch, hk, previous] using hm
                  have htelescope :=
                    construction.point_add_trackingCorrection_succ
                      previous.1 hprevious who
                  rw [← hkEq] at htelescope
                  have htelescope' : construction.point k who +
                      construction.trackingCorrection k who =
                        (construction.buildingBlock previous).w who +
                          construction.trackingCorrection previous who := by
                    convert htelescope using 1
                  rw [KiloblockConstruction.normalEndpointPotential, hm',
                    KiloblockConstruction.normalPotential, hm']
                  change (construction.buildingBlock previous).w who +
                      construction.trackingCorrection previous who = continuation
                  rw [← htelescope']
                  simp [continuation, KiloblockConstruction.selectedValue,
                    BuildingAttempt.remainingValue_zero, hbranch]
      have hquitValue : ∀ selected,
          (PublicQuittingState.active signal :
            (publicQuittingGame table
              construction.profile.signalLaw).State) =
              PublicQuittingState.active selected →
          construction.normalEndpointPotential who (t + 1)
              (Fin.snoc past
                (PublicQuittingState.active signal,
                  singleQuitAction owner.1 true),
                PublicQuittingState.absorbed
                  (quittingProjectiveSingletonTerminal owner.1)) =
            NormalMatrix table who owner +
              construction.trackingCorrection k who := by
        intro selected hselected
        cases PublicQuittingState.active.inj hselected
        have htransitionQuit :=
          publicQuittingGame_transition_singleQuitAction table
            construction.profile.signalLaw signal owner.1
        have hquitSupport :
            (PublicQuittingState.absorbed
              (quittingProjectiveSingletonTerminal owner.1) :
              (publicQuittingGame table
                construction.profile.signalLaw).State) ∈
            ((publicQuittingGame table
              construction.profile.signalLaw).transition
                (PublicQuittingState.active signal)
                (singleQuitAction owner.1 true)).support := by
          rw [htransitionQuit]
          exact (PMF.mem_support_pure_iff _ _).2 rfl
        have hquitStep := construction.mode_step t
          (past, PublicQuittingState.active signal)
          (singleQuitAction owner.1 true)
          (PublicQuittingState.absorbed
            (quittingProjectiveSingletonTerminal owner.1)) hquitSupport
        have hquitMode : construction.mode (t + 1)
            (Fin.snoc past
              (PublicQuittingState.active signal,
                singleQuitAction owner.1 true),
              PublicQuittingState.absorbed
                (quittingProjectiveSingletonTerminal owner.1)) =
            .absorbed (some k) := by
          simp [KiloblockModeStep, hmode,
            singleQuitAction_true_quitters] at hquitStep
          exact hquitStep.2
        rw [KiloblockConstruction.normalEndpointPotential, hquitMode,
          KiloblockConstruction.normalPotential, hquitMode]
        simp [KiloblockConstruction.trackingCorrectionAtOrigin,
          NormalMatrix]
      have hlocal := construction.endpointPotential_active_some_step
        (past, PublicQuittingState.active signal) k owner (tail + 1) hmode
        (construction.normalEndpointPotential who)
        (NormalMatrix table who owner +
          construction.trackingCorrection k who)
        continuation hquitValue (fun selected hselected => by
          cases PublicQuittingState.active.inj hselected
          simpa [drawState] using hcontinueValue)
      have hcurrent : construction.normalEndpointPotential who t
          (past, PublicQuittingState.active signal) =
            construction.selectedValue k (some owner) (tail + 1) who +
              construction.trackingCorrection k who := by
        rw [KiloblockConstruction.normalEndpointPotential, hmode,
          KiloblockConstruction.normalPotential, hmode]
      rw [hlocal, hcurrent]
      rw [construction.selectedValue_some_succ k owner tail who]
      dsimp [continuation]
      ring

private theorem KiloblockConstruction.normalEndpointPotential_active_none
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k none remaining) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  have hremaining : 0 < remaining :=
    construction.mode_remaining_pos t history k none remaining
      (Or.inl hmode)
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      obtain ⟨tail, rfl⟩ := Nat.exists_eq_succ_of_ne_zero
        (Nat.ne_of_gt hremaining)
      let continueAction : (publicQuittingGame table
          construction.profile.signalLaw).JointAct := fun _ => false
      let base : ∀ player, PMF ((publicQuittingGame table
          construction.profile.signalLaw).Act player) :=
        fun _ => PMF.pure false
      let drawState : (publicQuittingGame table
          construction.profile.signalLaw).State := PublicQuittingState.draw
      have hfamily :
          (fun player => construction.profile.strategy player t
            (past, PublicQuittingState.active signal)) = base := by
        funext player
        rw [construction.strategy_eq]
        simp [hmode, base]
      have hproduct : Math.PMFProduct.pmfPi base =
          PMF.pure continueAction :=
        Math.PMFProduct.pmfPi_pure continueAction
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal) continueAction =
          PMF.pure drawState := by
        simp [publicQuittingGame, continueAction, drawState]
      have hsupp : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              (PublicQuittingState.active signal) continueAction).support := by
        rw [htransition, PMF.mem_support_iff]
        simp
      have hstep := construction.mode_step t
        (past, PublicQuittingState.active signal) continueAction drawState hsupp
      have hnextValue : construction.normalEndpointPotential who (t + 1)
          (Fin.snoc past
            (PublicQuittingState.active signal, continueAction), drawState) =
          construction.point k who +
            construction.trackingCorrection k who := by
        cases tail with
        | succ nextTail =>
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                  (PublicQuittingState.active signal, continueAction),
                    drawState) =
                .draw (.resume k none (nextTail + 1)) := by
              simpa [KiloblockModeStep, hmode, continueAction, drawState]
                using hstep
            rw [KiloblockConstruction.normalEndpointPotential, hm,
              KiloblockConstruction.normalPotential, hm]
            rfl
        | zero =>
            by_cases hk : k.1 = 0
            · have hkEq : k = ⟨0, by omega⟩ := Fin.ext hk
              have hm : construction.mode (t + 1)
                  (Fin.snoc past
                    (PublicQuittingState.active signal, continueAction),
                      drawState) = .draw .final := by
                simpa [KiloblockModeStep, hmode, continueAction, drawState,
                  phaseAfterAttempt, precedingKiloblockPhase, hk]
                  using hstep
              rw [KiloblockConstruction.normalEndpointPotential, hm, hkEq]
              simp
              exact construction.trackingCorrection_zero who
            · have hkpos : 0 < k.1 := Nat.pos_of_ne_zero hk
              let previous : Fin (construction.blockCount + 1) :=
                ⟨k.1 - 1, by omega⟩
              have hprevious : previous.1 < construction.blockCount := by
                dsimp [previous]
                omega
              have hkEq : k = ⟨previous.1 + 1, by omega⟩ := by
                apply Fin.ext
                dsimp [previous]
                omega
              have hm : construction.mode (t + 1)
                  (Fin.snoc past
                    (PublicQuittingState.active signal, continueAction),
                      drawState) = .draw (.choose previous) := by
                simpa [KiloblockModeStep, hmode, continueAction, drawState,
                  phaseAfterAttempt, precedingKiloblockPhase, hk, previous]
                  using hstep
              have htelescope :=
                construction.point_add_trackingCorrection_succ
                  previous.1 hprevious who
              rw [← hkEq] at htelescope
              rw [KiloblockConstruction.normalEndpointPotential, hm,
                KiloblockConstruction.normalPotential, hm]
              exact htelescope.symm
      have hcurrent : construction.normalEndpointPotential who t
          (past, PublicQuittingState.active signal) =
          construction.point k who +
            construction.trackingCorrection k who := by
        rw [KiloblockConstruction.normalEndpointPotential, hmode,
          KiloblockConstruction.normalPotential, hmode]
        rfl
      rw [hcurrent]
      unfold StochasticGame.historyContinuationEU
      unfold StochasticGame.stageActionDist
      rw [hfamily, hproduct, Math.Probability.expect_pure,
        htransition, Math.Probability.expect_pure]
      exact hnextValue

private theorem KiloblockConstruction.normalEndpointPotential_draw_choose_direct
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (hmode : construction.mode t history = .draw (.choose k)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  have hnext (action : (publicQuittingGame table
      construction.profile.signalLaw).JointAct)
      (signal : Fin (construction.profile.signalCount + 1))
      (hsignal : signal ∈ construction.profile.signalLaw.support) :
      construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, action),
            PublicQuittingState.active signal) =
        .active k (construction.signalSelector k signal)
          (construction.mesh k) := by
    have hsupp : PublicQuittingState.active signal ∈
        ((publicQuittingGame table construction.profile.signalLaw).transition
          history.2 action).support := by
      rw [hstate]
      simp only [publicQuittingGame]
      exact (PMF.mem_support_map_iff _ _ _).2 ⟨signal, hsignal, rfl⟩
    have hstep := construction.mode_step t history action
      (PublicQuittingState.active signal) hsupp
    simpa [KiloblockModeStep, hmode] using hstep
  rw [KiloblockConstruction.normalEndpointPotential, hmode,
    KiloblockConstruction.normalPotential, hmode]
  unfold StochasticGame.historyContinuationEU
  calc
    expect
        ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy history)
        (fun action =>
          expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.normalEndpointPotential who (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))) =
        expect
          ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy history)
          (fun _ => (construction.buildingBlock k).w who +
            construction.trackingCorrection k who) := by
      apply congrArg (expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
          construction.profile.strategy history))
      funext action
      rw [hstate]
      simp only [publicQuittingGame, Math.Probability.expect_map]
      calc
        expect construction.profile.signalLaw (fun signal =>
            construction.normalEndpointPotential who (t + 1)
              (Fin.snoc history.1 (PublicQuittingState.draw, action),
                PublicQuittingState.active signal)) =
            expect construction.profile.signalLaw (fun signal =>
              construction.selectedValue k
                  (construction.signalSelector k signal)
                  (construction.mesh k) who +
                construction.trackingCorrection k who) := by
          apply Math.ProbabilityMassFunction.expect_congr_on_support
          intro signal hsignal
          have hm := hnext action signal hsignal
          rw [hstate] at hm
          rw [KiloblockConstruction.normalEndpointPotential, hm,
            KiloblockConstruction.normalPotential, hm]
        _ = (construction.buildingBlock k).w who +
            construction.trackingCorrection k who := by
          rw [Math.Probability.expect_add,
            construction.expect_selectedValue_mesh k who,
            Math.Probability.expect_const]
    _ = _ := Math.Probability.expect_const _ _

private theorem KiloblockConstruction.normalEndpointPotential_draw_resume_direct
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history =
      .draw (.resume k choice remaining)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  let value := construction.selectedValue k choice remaining who +
    construction.trackingCorrection k who
  have hcurrent : construction.normalEndpointPotential who t history =
      value := by
    simp [KiloblockConstruction.normalEndpointPotential,
      KiloblockConstruction.normalPotential, hmode, value]
  rw [hcurrent]
  unfold StochasticGame.historyContinuationEU
  calc
    expect
        ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy history)
        (fun action =>
          expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.normalEndpointPotential who (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))) =
        expect
          ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy history)
          (fun _ => value) := by
      apply congrArg (expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
          construction.profile.strategy history))
      funext action
      rw [hstate]
      simp only [publicQuittingGame, Math.Probability.expect_map]
      rw [← Math.Probability.expect_const construction.profile.signalLaw value]
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro signal hsignal
      have hsupp : PublicQuittingState.active signal ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              history.2 action).support := by
        rw [hstate]
        simp only [publicQuittingGame]
        exact (PMF.mem_support_map_iff _ _ _).2 ⟨signal, hsignal, rfl⟩
      have hstep := construction.mode_step t history action
        (PublicQuittingState.active signal) hsupp
      have hm : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, action),
            PublicQuittingState.active signal) =
          .active k choice remaining := by
        simpa [KiloblockModeStep, hmode] using hstep
      rw [hstate] at hm
      change construction.normalEndpointPotential who (t + 1)
          (Fin.snoc history.1 (PublicQuittingState.draw, action),
            PublicQuittingState.active signal) = value
      rw [KiloblockConstruction.normalEndpointPotential, hm,
        KiloblockConstruction.normalPotential, hm]
    _ = _ := Math.Probability.expect_const _ _

private theorem KiloblockConstruction.normalEndpointPotential_absorbed_direct
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (origin : Option (Fin (construction.blockCount + 1)))
    (hmode : construction.mode t history = .absorbed origin) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      let value := table.terminal quitters who.1 +
        construction.trackingCorrectionAtOrigin origin who
      have hcurrent : construction.normalEndpointPotential who t
          (past, PublicQuittingState.absorbed quitters) = value := by
        simp [KiloblockConstruction.normalEndpointPotential,
          KiloblockConstruction.normalPotential, hmode, value]
      rw [hcurrent]
      unfold StochasticGame.historyContinuationEU
      calc
        expect
            ((publicQuittingGame table
              construction.profile.signalLaw).stageActionDist
                construction.profile.strategy (past, .absorbed quitters))
            (fun action =>
              expect ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (.absorbed quitters) action)
                (fun nextState =>
                  construction.normalEndpointPotential who (t + 1)
                    (Fin.snoc past (.absorbed quitters, action), nextState))) =
            expect
              ((publicQuittingGame table
                construction.profile.signalLaw).stageActionDist
                  construction.profile.strategy (past, .absorbed quitters))
              (fun _ => value) := by
          apply congrArg (expect ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy (past, .absorbed quitters)))
          funext action
          simp only [publicQuittingGame, Math.Probability.expect_pure]
          have hsupp : PublicQuittingState.absorbed quitters ∈
              ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (.absorbed quitters) action).support := by
            change PublicQuittingState.absorbed quitters ∈
              (PMF.pure (PublicQuittingState.absorbed quitters)).support
            exact (PMF.mem_support_pure_iff _ _).2 rfl
          have hstep := construction.mode_step t
            (past, PublicQuittingState.absorbed quitters) action
            (PublicQuittingState.absorbed quitters) hsupp
          have hm : construction.mode (t + 1)
              (Fin.snoc past (PublicQuittingState.absorbed quitters, action),
                PublicQuittingState.absorbed quitters) =
              .absorbed origin := by
            simpa [KiloblockModeStep, hmode] using hstep
          change construction.normalEndpointPotential who (t + 1)
              (Fin.snoc past
                (PublicQuittingState.absorbed quitters, action),
                PublicQuittingState.absorbed quitters) = value
          rw [KiloblockConstruction.normalEndpointPotential, hm,
            KiloblockConstruction.normalPotential, hm]
        _ = _ := Math.Probability.expect_const _ _

/-! The assembled potential and boundary-accounting lemmas. -/

theorem KiloblockConstruction.normalEndpointPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential who) history =
      construction.normalEndpointPotential who t history := by
  cases hmode : construction.mode t history with
  | draw phase =>
      cases phase with
      | choose k =>
          exact construction.normalEndpointPotential_draw_choose_direct
            who history k hmode
      | resume k choice remaining =>
          exact construction.normalEndpointPotential_draw_resume_direct
            who history k choice remaining hmode
      | final =>
          exact construction.normalEndpointPotential_draw_final
            who history hmode
  | active k choice remaining =>
      cases choice with
      | none =>
          exact construction.normalEndpointPotential_active_none
            who history k remaining hmode
      | some owner =>
          exact construction.normalEndpointPotential_active_some
            who history k owner remaining hmode
  | finalActive =>
      exact construction.normalEndpointPotential_finalActive
        who history hmode
  | absorbed origin =>
      exact construction.normalEndpointPotential_absorbed_direct
        who history origin hmode

def KiloblockConstruction.profileFinalHistoryPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => construction.profileFinalPotential
    (construction.finiteMode (construction.mode t history))

theorem KiloblockConstruction.profileFinalHistoryPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        construction.profileFinalHistoryPotential history =
      construction.profileFinalHistoryPotential t history := by
  unfold StochasticGame.historyContinuationEU
    KiloblockConstruction.profileFinalHistoryPotential
  calc
    expect
        ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy history)
        (fun action => expect
          ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
          (fun nextState => construction.profileFinalPotential
            (construction.finiteMode (construction.mode (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))))) =
        expect ((construction.historyModeStepDist
          construction.profile.strategy history).map construction.finiteMode)
          construction.profileFinalPotential := by
      unfold KiloblockConstruction.historyModeStepDist
      rw [PMF.map_bind, Math.Probability.expect_bind]
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro action _
      rw [PMF.map_comp, Math.Probability.expect_map]
      rfl
    _ = expect
        (construction.profileFiniteModeKernel
          (construction.finiteMode (construction.mode t history)))
        construction.profileFinalPotential := by
      rw [construction.finiteHistoryModeStepDist_profile_eq history]
    _ = _ := by
      exact construction.profileFinalPotential_harmonic
        (construction.finiteMode (construction.mode t history))

/-! The fully semantic normal-player potential: the endpoint process is
corrected by the probability of the final tail times `r_∅-y⁰`. -/
def KiloblockConstruction.normalPayoffPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => construction.normalEndpointPotential who t history +
    (table.never who.1 - construction.point ⟨0, by omega⟩ who) *
      construction.profileFinalHistoryPotential t history

theorem KiloblockConstruction.normalPayoffPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalPayoffPotential who) history =
      construction.normalPayoffPotential who t history := by
  let endpoint := construction.normalEndpointPotential who
  let final := construction.profileFinalHistoryPotential
  let correction := table.never who.1 -
    construction.point ⟨0, by omega⟩ who
  have hsplit :
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          construction.profile.strategy
          (construction.normalPayoffPotential who) history =
        (publicQuittingGame table
          construction.profile.signalLaw).historyContinuationEU
            construction.profile.strategy endpoint history +
          correction *
            (publicQuittingGame table
              construction.profile.signalLaw).historyContinuationEU
                construction.profile.strategy final history := by
    change (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (fun nextTime nextHistory => endpoint nextTime nextHistory +
          correction * final nextTime nextHistory) history = _
    exact historyContinuationEU_add_const_mul construction.profile.signalLaw
      construction.profile.strategy endpoint final correction history
  rw [hsplit,
    construction.normalEndpointPotential_harmonic who history,
    construction.profileFinalHistoryPotential_harmonic history]
  unfold KiloblockConstruction.normalPayoffPotential
  dsimp only [endpoint, final, correction]

theorem KiloblockConstruction.expect_normalPayoffPotential_eq_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) : ∀ t,
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (construction.normalPayoffPotential who t) =
    construction.normalPayoffPotential who 0
      ((publicQuittingGame table
        construction.profile.signalLaw).emptyHist .draw)
  | 0 => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
        Math.Probability.expect_pure]
  | t + 1 => by
      change (publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue
          construction.profile.strategy .draw
          (construction.normalPayoffPotential who) (t + 1) = _
      rw [(publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue_succ]
      simp_rw [construction.normalPayoffPotential_harmonic who]
      exact construction.expect_normalPayoffPotential_eq_initial who t

theorem KiloblockConstruction.normalPayoffPotential_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    construction.normalPayoffPotential who 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw) =
      construction.normalTarget who +
        construction.trackingCorrection
          (Fin.last construction.blockCount) who +
        construction.macroSurvivalProbability *
          (table.never who.1 - construction.point ⟨0, by omega⟩ who) := by
  unfold KiloblockConstruction.normalPayoffPotential
    KiloblockConstruction.normalEndpointPotential
    KiloblockConstruction.profileFinalHistoryPotential
  rw [construction.mode_initial]
  change construction.normalPotential who 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw) +
      (table.never who.1 - construction.point ⟨0, by omega⟩ who) *
        construction.profileFinalPotential
          (.drawChoose (Fin.last construction.blockCount)) = _
  rw [construction.profileFinalPotential_initial]
  simp [KiloblockConstruction.normalPotential,
    construction.mode_initial, KiloblockConstruction.normalTarget]
  ring

theorem KiloblockConstruction.abs_trackingCorrection_sub_last_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (who : NormalPlayer table) :
    |construction.trackingCorrection k who -
        construction.trackingCorrection
          (Fin.last construction.blockCount) who| ≤
      KiloblockTracking construction.point construction.buildingBlock := by
  have hk : k.1 ≤ construction.blockCount := by omega
  let increment := fun j => construction.trackingIncrement j who
  have hsplit :
      (∑ j ∈ Finset.range k.1, increment j) +
          ∑ j ∈ Finset.Ico k.1 construction.blockCount, increment j =
        ∑ j ∈ Finset.range construction.blockCount, increment j := by
    exact Finset.sum_range_add_sum_Ico increment hk
  have hdifference : construction.trackingCorrection k who -
      construction.trackingCorrection
        (Fin.last construction.blockCount) who =
      -∑ j ∈ Finset.Ico k.1 construction.blockCount, increment j := by
    unfold KiloblockConstruction.trackingCorrection
    change (∑ j ∈ Finset.range k.1, increment j) -
        (∑ j ∈ Finset.range construction.blockCount, increment j) = _
    linarith
  rw [hdifference, abs_neg]
  calc
    |∑ j ∈ Finset.Ico k.1 construction.blockCount, increment j| ≤
        ∑ j ∈ Finset.Ico k.1 construction.blockCount,
          |increment j| := Finset.abs_sum_le_sum_abs _ _
    _ ≤ ∑ j ∈ Finset.Ico k.1 construction.blockCount,
        construction.trackingDistanceIncrement j := by
      apply Finset.sum_le_sum
      intro j hj
      have hjcount : j < construction.blockCount :=
        (Finset.mem_Ico.mp hj).2
      simp only [increment, KiloblockConstruction.trackingIncrement,
        KiloblockConstruction.trackingDistanceIncrement, hjcount, dif_pos]
      rw [dist_eq_norm]
      simpa only [Pi.sub_apply, Real.norm_eq_abs, abs_sub_comm] using
        norm_le_pi_norm
          (construction.point ⟨j + 1, by omega⟩ -
            (construction.buildingBlock ⟨j, by omega⟩).w) who
    _ ≤ ∑ j ∈ Finset.range construction.blockCount,
        construction.trackingDistanceIncrement j := by
      apply Finset.sum_le_sum_of_subset_of_nonneg
      · intro j hj
        exact Finset.mem_range.mpr (Finset.mem_Ico.mp hj).2
      · intro j hj _
        simp only [KiloblockConstruction.trackingDistanceIncrement]
        split <;> positivity
    _ = KiloblockTracking construction.point construction.buildingBlock := by
      rw [← Fin.sum_univ_eq_sum_range]
      apply Finset.sum_congr rfl
      intro j _
      simp [KiloblockConstruction.trackingDistanceIncrement]

/-! The finite-time terminal-payoff expectation, written through the
absorbed masses so its limit is definitionally the paper payoff. -/
def publicFinitePayoffByMass
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (who : ι) : ℝ :=
  table.never who +
    ∑ quitters, publicAbsorbedMass table signalLaw strategy t quitters *
      (table.terminal quitters who - table.never who)

/-! The actual finite-horizon expected payoff at the current public state. -/
def publicFinitePayoff
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (who : ι) : ℝ :=
  expect ((publicQuittingGame table signalLaw).histDist strategy .draw t)
    (fun history => match history.2 with
      | .absorbed quitters => table.terminal quitters who
      | _ => table.never who)

theorem publicFinitePayoff_eq_byMass
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (who : ι) :
    publicFinitePayoff table signalLaw strategy t who =
      publicFinitePayoffByMass table signalLaw strategy t who := by
  unfold publicFinitePayoff
  classical
  let distribution :=
    (publicQuittingGame table signalLaw).histDist strategy .draw t
  have hpoint : ∀ history : (publicQuittingGame table signalLaw).Hist t,
      (match history.2 with
        | .absorbed quitters => table.terminal quitters who
        | _ => table.never who) =
      table.never who + ∑ quitters,
        publicAbsorbedIndicator quitters history.2 *
          (table.terminal quitters who - table.never who) := by
    intro history
    rcases history with ⟨past, state⟩
    cases state <;> simp [publicAbsorbedIndicator]
  calc
    expect distribution
      (fun history => match history.2 with
        | .absorbed quitters => table.terminal quitters who
        | _ => table.never who) =
        expect distribution (fun history => table.never who +
          ∑ quitters, publicAbsorbedIndicator quitters history.2 *
            (table.terminal quitters who - table.never who)) := by
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro history _
      exact hpoint history
    _ = table.never who + ∑ quitters,
        expect distribution (fun history =>
          publicAbsorbedIndicator quitters history.2 *
            (table.terminal quitters who - table.never who)) := by
      rw [Math.Probability.expect_add, Math.Probability.expect_const]
      congr 1
      exact (Math.Probability.expect_sum_comm distribution
        (fun quitters history =>
          publicAbsorbedIndicator quitters history.2 *
            (table.terminal quitters who - table.never who))).symm
    _ = publicFinitePayoffByMass table signalLaw strategy t who := by
      unfold publicFinitePayoffByMass publicAbsorbedMass
        StochasticGame.expectedStateValue
      congr 1
      apply Finset.sum_congr rfl
      intro quitters _
      let indicator : (publicQuittingGame table signalLaw).Hist t → ℝ :=
        fun history => publicAbsorbedIndicator quitters history.2
      change expect distribution (fun history => indicator history *
          (table.terminal quitters who - table.never who)) =
        expect distribution indicator *
          (table.terminal quitters who - table.never who)
      rw [show (fun history => indicator history *
          (table.terminal quitters who - table.never who)) =
        fun history => (table.terminal quitters who - table.never who) *
          indicator history by
        funext history
        ring]
      rw [Math.Probability.expect_const_mul]
      ring

theorem expect_publicHistoryPayoff_eq_publicFinitePayoff
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (t : ℕ) (who : ι) :
    expect ((publicQuittingGame table signalLaw).histDist strategy .draw t)
      (fun history => match history.2 with
        | .absorbed quitters => table.terminal quitters who
        | _ => table.never who) =
      publicFinitePayoff table signalLaw strategy t who := rfl

theorem tendsto_publicFinitePayoff
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (who : ι) :
    Tendsto (fun t => publicFinitePayoff table signalLaw strategy t who)
      atTop (nhds (publicQuittingPayoff table signalLaw strategy who)) := by
  have hmass : Tendsto
      (fun t => publicFinitePayoffByMass table signalLaw strategy t who)
      atTop (nhds (publicQuittingPayoff table signalLaw strategy who)) := by
    unfold publicFinitePayoffByMass publicQuittingPayoff
    apply Tendsto.add tendsto_const_nhds
    apply tendsto_finsetSum
    intro quitters _
    exact (tendsto_publicAbsorbedMass table signalLaw strategy
      quitters).mul_const
      (table.terminal quitters who - table.never who)
  apply hmass.congr'
  exact Filter.Eventually.of_forall fun t =>
    (publicFinitePayoff_eq_byMass table signalLaw strategy t who).symm

theorem tendsto_publicHistoryPayoff
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (who : ι) :
    Tendsto (fun t => expect
        ((publicQuittingGame table signalLaw).histDist strategy .draw t)
        (fun history => match history.2 with
          | .absorbed quitters => table.terminal quitters who
          | _ => table.never who))
      atTop (nhds (publicQuittingPayoff table signalLaw strategy who)) := by
  have h := tendsto_publicFinitePayoff table signalLaw strategy who
  with_unfolding_all exact h

def KiloblockConstruction.trackingBoundaryCorrection
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    KiloblockFiniteMode construction → ℝ
  | .absorbed (some k) => construction.trackingCorrection k who
  | .absorbed none => 0
  | _ => construction.trackingCorrection
      (Fin.last construction.blockCount) who

def KiloblockConstruction.normalPayoffModeValue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    KiloblockFiniteMode construction → ℝ
  | mode@(.drawChoose k) =>
      (construction.buildingBlock k).w who +
        construction.trackingCorrection k who +
        construction.profileFinalPotential mode *
          (table.never who.1 - construction.point ⟨0, by omega⟩ who)
  | mode@(.drawResume k choice remaining) =>
      construction.selectedValue k (choice.map construction.ownerOfCode)
          remaining.1 who + construction.trackingCorrection k who +
        construction.profileFinalPotential mode *
          (table.never who.1 - construction.point ⟨0, by omega⟩ who)
  | .drawFinal => table.never who.1
  | mode@(.active k choice remaining) =>
      construction.selectedValue k (choice.map construction.ownerOfCode)
          remaining.1 who + construction.trackingCorrection k who +
        construction.profileFinalPotential mode *
          (table.never who.1 - construction.point ⟨0, by omega⟩ who)
  | .finalActive => table.never who.1
  | .absorbed _ => 0

def KiloblockConstruction.profileAccountingResidual
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    KiloblockFiniteMode construction → ℝ
  | .absorbed _ => 0
  | mode => construction.normalPayoffModeValue who mode -
      (table.never who.1 + construction.trackingBoundaryCorrection who mode -
        construction.trackingCorrection
            (Fin.last construction.blockCount) who *
          construction.profileFinalPotential mode)

theorem KiloblockConstruction.normalPayoffPotential_account
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    construction.normalPayoffPotential who t history -
        ((match history.2 with
          | .absorbed quitters => table.terminal quitters who.1
          | _ => table.never who.1) +
          construction.trackingBoundaryCorrection who
            (construction.finiteMode (construction.mode t history)) -
          construction.trackingCorrection
              (Fin.last construction.blockCount) who *
            construction.profileFinalHistoryPotential t history) =
      construction.profileAccountingResidual who
        (construction.finiteMode (construction.mode t history)) := by
  have hmatches := construction.mode_state t history
  rcases history with ⟨past, state⟩
  cases hmode : construction.mode t (past, state) <;>
    cases state <;>
    simp [KiloblockMode.MatchesState, hmode,
      KiloblockConstruction.normalPayoffPotential,
      KiloblockConstruction.normalEndpointPotential,
      KiloblockConstruction.normalPotential,
      KiloblockConstruction.profileFinalHistoryPotential,
      KiloblockConstruction.normalPayoffModeValue,
      KiloblockConstruction.profileAccountingResidual,
      KiloblockConstruction.trackingBoundaryCorrection,
      KiloblockConstruction.finiteMode] at hmatches ⊢
  case draw.draw phase =>
    cases phase with
    | choose k =>
        ring
    | resume k choice remaining =>
        have hremaining := construction.mode_remaining_le_mesh t
          (past, PublicQuittingState.draw) k choice remaining (Or.inr hmode)
        have htotal := hremaining.trans (construction.mesh_le_totalMesh k)
        cases choice <;>
          simp [min_eq_left htotal,
            construction.ownerOfCode_ownerCode] <;> ring
    | final =>
        simp [KiloblockConstruction.profileFinalPotential]
  case active.active k choice remaining signal =>
    have hremaining := construction.mode_remaining_le_mesh t
      (past, PublicQuittingState.active signal) k choice remaining
        (Or.inl hmode)
    have htotal := hremaining.trans (construction.mesh_le_totalMesh k)
    cases choice <;>
      simp [min_eq_left htotal,
        construction.ownerOfCode_ownerCode] <;> ring
  case finalActive.active =>
    simp [KiloblockConstruction.profileFinalPotential]
  case absorbed.absorbed modeOrigin quitters =>
    cases modeOrigin <;>
      simp [KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.trackingCorrectionAtOrigin]

theorem KiloblockConstruction.profileAccountingResidual_eq_zero_on_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {mode : KiloblockFiniteMode construction}
    (hmode : mode ∈ construction.continueBoundary) :
    construction.profileAccountingResidual who mode = 0 := by
  cases mode with
  | drawChoose k => contradiction
  | drawResume k choice remaining => contradiction
  | active k choice remaining => contradiction
  | drawFinal =>
      simp [KiloblockConstruction.profileAccountingResidual,
        KiloblockConstruction.normalPayoffModeValue,
        KiloblockConstruction.trackingBoundaryCorrection,
        KiloblockConstruction.profileFinalPotential]
  | finalActive =>
      simp [KiloblockConstruction.profileAccountingResidual,
        KiloblockConstruction.normalPayoffModeValue,
        KiloblockConstruction.trackingBoundaryCorrection,
        KiloblockConstruction.profileFinalPotential]
  | absorbed origin =>
      simp [KiloblockConstruction.profileAccountingResidual]

theorem KiloblockConstruction.trackingBoundaryCorrection_sub_last_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) (mode : KiloblockFiniteMode construction) :
    |construction.trackingBoundaryCorrection who mode -
        construction.trackingCorrection
          (Fin.last construction.blockCount) who| ≤
      KiloblockTracking construction.point construction.buildingBlock := by
  have htracking : 0 ≤
      KiloblockTracking construction.point construction.buildingBlock := by
    unfold KiloblockTracking
    exact Finset.sum_nonneg fun _ _ => dist_nonneg
  cases mode with
  | absorbed origin =>
      cases origin with
      | none =>
          have hzero := construction.abs_trackingCorrection_sub_last_le
            ⟨0, by omega⟩ who
          simpa only [KiloblockConstruction.trackingBoundaryCorrection,
            construction.trackingCorrection_zero, zero_sub, abs_neg] using hzero
      | some k =>
          exact construction.abs_trackingCorrection_sub_last_le k who
  | drawChoose k =>
      simpa [KiloblockConstruction.trackingBoundaryCorrection] using htracking
  | drawResume k choice remaining =>
      simpa [KiloblockConstruction.trackingBoundaryCorrection] using htracking
  | drawFinal =>
      simpa [KiloblockConstruction.trackingBoundaryCorrection] using htracking
  | active k choice remaining =>
      simpa [KiloblockConstruction.trackingBoundaryCorrection] using htracking
  | finalActive =>
      simpa [KiloblockConstruction.trackingBoundaryCorrection] using htracking

theorem KiloblockConstruction.abs_expect_profileAccountingResidual_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table)
    (distribution : PMF (KiloblockFiniteMode construction)) :
    |expect distribution (construction.profileAccountingResidual who)| ≤
      ‖construction.profileAccountingResidual who‖ *
        expect distribution
          (Math.Probability.transientCharge construction.continueBoundary) := by
  let residual : KiloblockFiniteMode construction → ℝ :=
    construction.profileAccountingResidual who
  let transient := Math.Probability.transientCharge
    construction.continueBoundary
  let bound := ‖residual‖
  have hpoint (mode : KiloblockFiniteMode construction) :
      |residual mode| ≤ bound * transient mode := by
    by_cases hmode : mode ∈ construction.continueBoundary
    · have hzero :=
        construction.profileAccountingResidual_eq_zero_on_boundary who hmode
      rw [show residual mode =
        construction.profileAccountingResidual who mode by rfl, hzero]
      simp [transient, Math.Probability.transientCharge_of_mem hmode]
    · rw [show transient mode = 1 by
          simp [transient,
            Math.Probability.transientCharge_of_not_mem hmode], mul_one]
      simpa only [Real.norm_eq_abs] using norm_le_pi_norm residual mode
  have hupper := Math.Probability.expect_mono distribution residual
    (fun mode => bound * transient mode)
    (fun mode => (le_abs_self _).trans (hpoint mode))
  have hlower := Math.Probability.expect_mono distribution
    (fun mode => -(bound * transient mode)) residual
    (fun mode => neg_le_of_abs_le (hpoint mode))
  have hscale : expect distribution (fun mode => bound * transient mode) =
      bound * expect distribution transient :=
    Math.Probability.expect_const_mul distribution bound transient
  have hnegScale : expect distribution (fun mode =>
      -(bound * transient mode)) =
      -(bound * expect distribution transient) := by
    rw [Math.Probability.expect_neg, hscale]
  rw [hscale] at hupper
  rw [hnegScale] at hlower
  exact abs_le.mpr ⟨hlower, hupper⟩

theorem KiloblockConstruction.exists_iter_profile_transientCharge_lt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {threshold : ℝ} (hthreshold : 0 < threshold)
    (initial : KiloblockFiniteMode construction) :
    ∃ steps, expect (Math.PMFIter.iter
        construction.profileFiniteModeKernel steps initial)
        (Math.Probability.transientCharge construction.continueBoundary) <
      threshold := by
  let certificate := Classical.choice
    construction.profileKernel_transienceCertificate
  let occupationBound :=
    (certificate.horizon : ℝ) / certificate.minorization
  obtain ⟨horizon, hhorizon⟩ :=
    exists_nat_gt (occupationBound / threshold)
  have hlarge : occupationBound < (horizon : ℝ) * threshold := by
    exact (div_lt_iff₀ hthreshold).mp hhorizon
  by_contra hnot
  push Not at hnot
  have hlower : (horizon : ℝ) * threshold ≤
      ∑ time ∈ Finset.range horizon,
        expect (Math.PMFIter.iter
          construction.profileFiniteModeKernel time initial)
          (Math.Probability.transientCharge
            construction.continueBoundary) := by
    calc
      (horizon : ℝ) * threshold =
          ∑ _time ∈ Finset.range horizon, threshold := by simp
      _ ≤ _ := Finset.sum_le_sum fun time _ => hnot time
  have hupper := certificate.total_transientOccupation_le horizon initial
  exact (not_lt_of_ge (hlower.trans hupper)) hlarge

theorem KiloblockConstruction.expect_profileFinalHistoryPotential_eq_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) : ∀ t,
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (construction.profileFinalHistoryPotential t) =
    construction.profileFinalHistoryPotential 0
      ((publicQuittingGame table
        construction.profile.signalLaw).emptyHist .draw)
  | 0 => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
        Math.Probability.expect_pure]
  | t + 1 => by
      change (publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue
          construction.profile.strategy .draw
          construction.profileFinalHistoryPotential (t + 1) = _
      rw [(publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue_succ]
      simp_rw [construction.profileFinalHistoryPotential_harmonic]
      exact construction.expect_profileFinalHistoryPotential_eq_initial t

theorem KiloblockConstruction.expect_profileFinalHistoryPotential_eq_survival
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (construction.profileFinalHistoryPotential t) =
    construction.macroSurvivalProbability := by
  rw [construction.expect_profileFinalHistoryPotential_eq_initial t]
  unfold KiloblockConstruction.profileFinalHistoryPotential
  rw [construction.mode_initial]
  change construction.profileFinalPotential
    (.drawChoose (Fin.last construction.blockCount)) = _
  exact construction.profileFinalPotential_initial

theorem KiloblockConstruction.expect_normalPayoffPotential_account
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (construction.normalPayoffPotential who t) =
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (fun history => match history.2 with
        | .absorbed quitters => table.terminal quitters who.1
        | _ => table.never who.1) +
      expect (construction.modeDist construction.profile.strategy t)
        (construction.trackingBoundaryCorrection who) -
      construction.trackingCorrection
          (Fin.last construction.blockCount) who *
        construction.macroSurvivalProbability +
      expect (construction.modeDist construction.profile.strategy t)
        (construction.profileAccountingResidual who) := by
  have hmodeExpect (f : KiloblockFiniteMode construction → ℝ) :
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t) (fun history =>
          f (construction.finiteMode (construction.mode t history))) =
        expect (construction.modeDist construction.profile.strategy t) f := by
    unfold KiloblockConstruction.modeDist
    rw [Math.Probability.expect_map]
  calc
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
        (construction.normalPayoffPotential who t) =
        expect ((publicQuittingGame table
            construction.profile.signalLaw).histDist
          construction.profile.strategy .draw t) (fun history =>
          (match history.2 with
            | .absorbed quitters => table.terminal quitters who.1
            | _ => table.never who.1) +
          construction.trackingBoundaryCorrection who
            (construction.finiteMode (construction.mode t history)) -
          construction.trackingCorrection
              (Fin.last construction.blockCount) who *
            construction.profileFinalHistoryPotential t history +
          construction.profileAccountingResidual who
            (construction.finiteMode (construction.mode t history))) := by
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro history _
      have haccount := construction.normalPayoffPotential_account who history
      linarith
    _ = expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t)
          (fun history => match history.2 with
            | .absorbed quitters => table.terminal quitters who.1
            | _ => table.never who.1) +
          expect ((publicQuittingGame table
              construction.profile.signalLaw).histDist
            construction.profile.strategy .draw t) (fun history =>
            construction.trackingBoundaryCorrection who
              (construction.finiteMode (construction.mode t history))) -
          construction.trackingCorrection
              (Fin.last construction.blockCount) who *
            expect ((publicQuittingGame table
                construction.profile.signalLaw).histDist
              construction.profile.strategy .draw t)
              (construction.profileFinalHistoryPotential t) +
          expect ((publicQuittingGame table
              construction.profile.signalLaw).histDist
            construction.profile.strategy .draw t) (fun history =>
            construction.profileAccountingResidual who
              (construction.finiteMode (construction.mode t history))) := by
      rw [Math.Probability.expect_add,
        Math.Probability.expect_sub,
        Math.Probability.expect_add,
        Math.Probability.expect_const_mul]
    _ = _ := by
      rw [hmodeExpect (construction.trackingBoundaryCorrection who),
        construction.expect_profileFinalHistoryPotential_eq_survival t,
        hmodeExpect (construction.profileAccountingResidual who)]

theorem KiloblockConstruction.macroSurvivalProbability_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    0 ≤ construction.macroSurvivalProbability := by
  unfold KiloblockConstruction.macroSurvivalProbability
  exact Finset.prod_nonneg fun k _ =>
    construction.macroAdvanceProbability_nonneg k

theorem KiloblockConstruction.abs_survival_endpointCorrection_lt_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    |construction.macroSurvivalProbability *
        (construction.trackingCorrection
            (Fin.last construction.blockCount) who +
          table.never who.1 - construction.point ⟨0, by omega⟩ who)| < ε := by
  let survival := construction.macroSurvivalProbability
  let correction := construction.trackingCorrection
    (Fin.last construction.blockCount) who
  let endpoint := construction.point ⟨0, by omega⟩ who
  have hsurvival0 : 0 ≤ survival :=
    construction.macroSurvivalProbability_nonneg
  have hsurvival : survival < ε ^ 2 :=
    construction.macroSurvivalProbability_lt_epsilon_sq
  have hcorrection := construction.abs_trackingCorrection_lt
    (Fin.last construction.blockCount) who
  have hepsilonFifth : ε < 1 / 5 := by
    have hfive := construction.five_mul_epsilon_lt_negativeMargin
    have hmargin := construction.negativeMargin_lt_one
    linarith
  have hnever := construction.payoffsBounded.2 who.1
  have hendpoint := abs_le_one_of_mem_D construction.normalMatrix_bounded
    (construction.point_boundary ⟨0, by omega⟩).1 who
  have hfactor : |correction + table.never who.1 - endpoint| < 3 := by
    have hsum := abs_add_le (correction + table.never who.1) (-endpoint)
    have hfront := abs_add_le correction (table.never who.1)
    rw [abs_neg] at hsum
    dsimp only [correction, endpoint] at hcorrection hsum hfront ⊢
    rw [sub_eq_add_neg]
    calc
      |construction.trackingCorrection
          (Fin.last construction.blockCount) who + table.never who.1 +
          -construction.point ⟨0, by omega⟩ who| ≤
          |construction.trackingCorrection
              (Fin.last construction.blockCount) who + table.never who.1| +
            |construction.point ⟨0, by omega⟩ who| := hsum
      _ ≤ |construction.trackingCorrection
              (Fin.last construction.blockCount) who| +
            |table.never who.1| +
          |construction.point ⟨0, by omega⟩ who| :=
        add_le_add hfront le_rfl
      _ < 3 := by linarith [construction.epsilon_lt_one]
  rw [abs_mul, abs_of_nonneg hsurvival0]
  calc
    survival * |correction + table.never who.1 - endpoint| ≤
        survival * 3 :=
      mul_le_mul_of_nonneg_left hfactor.le hsurvival0
    _ < ε ^ 2 * 3 := mul_lt_mul_of_pos_right hsurvival (by norm_num)
    _ < ε := by nlinarith [construction.epsilon_pos]

theorem KiloblockConstruction.abs_expect_trackingBoundaryCorrection_sub_last_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table)
    (distribution : PMF (KiloblockFiniteMode construction)) :
    |expect distribution (construction.trackingBoundaryCorrection who) -
        construction.trackingCorrection
          (Fin.last construction.blockCount) who| ≤
      KiloblockTracking construction.point construction.buildingBlock := by
  let lastCorrection := construction.trackingCorrection
    (Fin.last construction.blockCount) who
  have hbound := Math.Probability.abs_expect_le_of_abs_le distribution
    (fun mode => construction.trackingBoundaryCorrection who mode -
      lastCorrection)
    (fun mode => construction.trackingBoundaryCorrection_sub_last_le who mode)
  rw [Math.Probability.expect_sub,
    Math.Probability.expect_const] at hbound
  exact hbound

theorem KiloblockConstruction.normalPotential_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    construction.normalPotential who 0
        ((publicQuittingGame table construction.profile.signalLaw).emptyHist
          PublicQuittingState.draw) =
      construction.normalTarget who +
        construction.trackingCorrection
          (Fin.last construction.blockCount) who := by
  simp [KiloblockConstruction.normalPotential,
    construction.mode_initial, KiloblockConstruction.normalTarget]

theorem KiloblockConstruction.normalPotential_draw_choose
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (hmode : construction.mode t history = .draw (.choose k)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  have hnext (action : (publicQuittingGame table
      construction.profile.signalLaw).JointAct)
      (signal : Fin (construction.profile.signalCount + 1))
      (hsignal : signal ∈ construction.profile.signalLaw.support) :
      construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, action),
            PublicQuittingState.active signal) =
        .active k (construction.signalSelector k signal)
          (construction.mesh k) := by
    have hsupp : PublicQuittingState.active signal ∈
        ((publicQuittingGame table construction.profile.signalLaw).transition
    history.2 action).support := by
      rw [hstate]
      simp only [publicQuittingGame]
      exact (PMF.mem_support_map_iff _ _ _).mpr
        ⟨signal, hsignal, rfl⟩
    have hstep := construction.mode_step t history action
      (PublicQuittingState.active signal) hsupp
    simpa [KiloblockModeStep, hmode] using hstep
  rw [KiloblockConstruction.normalPotential, hmode]
  unfold StochasticGame.historyContinuationEU
  calc
    expect
        ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy history)
        (fun action =>
          expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.normalPotential who (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))) =
        expect
          ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy history)
          (fun _ => (construction.buildingBlock k).w who +
            construction.trackingCorrection k who) := by
      apply congrArg (expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
          construction.profile.strategy history))
      funext action
      rw [hstate]
      simp only [publicQuittingGame]
      rw [Math.Probability.expect_map]
      calc
        expect construction.profile.signalLaw (fun signal =>
            construction.normalPotential who (t + 1)
              (Fin.snoc history.1 (PublicQuittingState.draw, action),
                PublicQuittingState.active signal)) =
            expect construction.profile.signalLaw (fun signal =>
              construction.selectedValue k
                  (construction.signalSelector k signal)
                  (construction.mesh k) who +
                construction.trackingCorrection k who) := by
          apply Math.ProbabilityMassFunction.expect_congr_on_support
          intro signal hsignal
          have hm := hnext action signal hsignal
          rw [hstate] at hm
          rw [KiloblockConstruction.normalPotential, hm]
        _ = (construction.buildingBlock k).w who +
            construction.trackingCorrection k who := by
          rw [Math.Probability.expect_add,
            construction.expect_selectedValue_mesh k who,
            Math.Probability.expect_const]
    _ = _ := expect_const _ _

private theorem KiloblockConstruction.normalPotential_draw_constant
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (value : ℝ)
    (hstate : history.2 = PublicQuittingState.draw)
    (hnext : ∀ action signal,
      signal ∈ construction.profile.signalLaw.support →
      construction.normalPotential who (t + 1)
        (Fin.snoc history.1 (history.2, action),
          PublicQuittingState.active signal) = value) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history = value := by
  classical
  unfold StochasticGame.historyContinuationEU
  calc
    expect
        ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy history)
        (fun action =>
          expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.normalPotential who (t + 1)
              (Fin.snoc history.1 (history.2, action), nextState))) =
        expect
          ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy history)
          (fun _ => value) := by
      apply congrArg (expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
          construction.profile.strategy history))
      funext action
      rw [hstate]
      simp only [publicQuittingGame]
      rw [Math.Probability.expect_map]
      rw [← Math.Probability.expect_const
        construction.profile.signalLaw value]
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro signal hsignal
      have := hnext action signal hsignal
      rw [hstate] at this
      exact this
    _ = value := expect_const _ _

theorem KiloblockConstruction.normalPotential_draw_resume
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history =
      .draw (.resume k choice remaining)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  rw [KiloblockConstruction.normalPotential, hmode]
  apply construction.normalPotential_draw_constant who history
    (construction.selectedValue k choice remaining who +
      construction.trackingCorrection k who) hstate
  intro action signal hsignal
  have hsupp : PublicQuittingState.active signal ∈
      ((publicQuittingGame table construction.profile.signalLaw).transition
        history.2 action).support := by
    rw [hstate]
    simp only [publicQuittingGame]
    exact (PMF.mem_support_map_iff _ _ _).mpr
      ⟨signal, hsignal, rfl⟩
  have hstep := construction.mode_step t history action
    (PublicQuittingState.active signal) hsupp
  have hm : construction.mode (t + 1)
      (Fin.snoc history.1 (history.2, action),
        PublicQuittingState.active signal) =
      .active k choice remaining := by
    simpa [KiloblockModeStep, hmode] using hstep
  rw [KiloblockConstruction.normalPotential, hm]

theorem KiloblockConstruction.normalPotential_draw_final
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : construction.mode t history = .draw .final) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  rw [KiloblockConstruction.normalPotential, hmode]
  apply construction.normalPotential_draw_constant who history 0 hstate
  intro action signal hsignal
  have hsupp : PublicQuittingState.active signal ∈
      ((publicQuittingGame table construction.profile.signalLaw).transition
        history.2 action).support := by
    rw [hstate]
    simp only [publicQuittingGame]
    exact (PMF.mem_support_map_iff _ _ _).mpr
      ⟨signal, hsignal, rfl⟩
  have hstep := construction.mode_step t history action
    (PublicQuittingState.active signal) hsupp
  have hm : construction.mode (t + 1)
      (Fin.snoc history.1 (history.2, action),
        PublicQuittingState.active signal) = .finalActive := by
    simpa [KiloblockModeStep, hmode] using hstep
  rw [KiloblockConstruction.normalPotential, hm]

theorem KiloblockConstruction.normalPotential_absorbed
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (origin : Option (Fin (construction.blockCount + 1)))
    (hmode : construction.mode t history = .absorbed origin) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      let value := table.terminal quitters who.1 +
        construction.trackingCorrectionAtOrigin origin who
      have hcurrent : construction.normalPotential who t
          (past, PublicQuittingState.absorbed quitters) = value := by
        simp only [KiloblockConstruction.normalPotential, hmode]
        rfl
      rw [hcurrent]
      unfold StochasticGame.historyContinuationEU
      calc
        expect
            ((publicQuittingGame table
              construction.profile.signalLaw).stageActionDist
                construction.profile.strategy (past, .absorbed quitters))
            (fun action =>
              expect ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (.absorbed quitters) action)
                (fun nextState => construction.normalPotential who (t + 1)
                  (Fin.snoc past (.absorbed quitters, action), nextState))) =
            expect
              ((publicQuittingGame table
                construction.profile.signalLaw).stageActionDist
                  construction.profile.strategy (past, .absorbed quitters))
              (fun _ => value) := by
          apply congrArg (expect ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy (past, .absorbed quitters)))
          funext action
          simp only [publicQuittingGame, Math.Probability.expect_pure]
          have hsupp : PublicQuittingState.absorbed quitters ∈
              ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (.absorbed quitters)
                  action).support := by
            change PublicQuittingState.absorbed quitters ∈
              (PMF.pure (PublicQuittingState.absorbed quitters)).support
            rw [PMF.mem_support_iff]
            simp
          have hstep := construction.mode_step t
            (past, PublicQuittingState.absorbed quitters) action
            (PublicQuittingState.absorbed quitters) hsupp
          have hm : construction.mode (t + 1)
              (Fin.snoc past (PublicQuittingState.absorbed quitters, action),
                PublicQuittingState.absorbed quitters) =
              .absorbed origin := by
            simpa [KiloblockModeStep, hmode] using hstep
          change construction.normalPotential who (t + 1)
            (Fin.snoc past
                (PublicQuittingState.absorbed quitters, action),
              PublicQuittingState.absorbed quitters) = value
          rw [KiloblockConstruction.normalPotential, hm]
        _ = _ := expect_const _ _

theorem KiloblockConstruction.normalPotential_finalActive
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : construction.mode t history = .finalActive) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      let continueAction : (publicQuittingGame table
        construction.profile.signalLaw).JointAct := fun _ => false
      let drawState : (publicQuittingGame table
        construction.profile.signalLaw).State := PublicQuittingState.draw
      let base : ∀ player, PMF ((publicQuittingGame table
          construction.profile.signalLaw).Act player) :=
        fun _ => PMF.pure false
      have hfamily :
          (fun player => construction.profile.strategy player t
            (past, PublicQuittingState.active signal)) =
            base := by
        funext player
        rw [construction.strategy_eq]
        simp [hmode]
        rfl
      have hproduct : Math.PMFProduct.pmfPi base =
          PMF.pure continueAction := by
        exact Math.PMFProduct.pmfPi_pure continueAction
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal) continueAction =
          PMF.pure drawState := by
        simp [publicQuittingGame, continueAction, drawState]
      have hsupp : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
            (PublicQuittingState.active signal) continueAction).support := by
        rw [htransition]
        rw [PMF.mem_support_iff]
        simp
      have hstep := construction.mode_step t
        (past, PublicQuittingState.active signal) continueAction
        drawState hsupp
      have hm : construction.mode (t + 1)
          (Fin.snoc past
              (PublicQuittingState.active signal, continueAction),
            drawState) = .draw .final := by
        simpa [KiloblockModeStep, hmode, drawState] using hstep
      have hcurrent : construction.normalPotential who t
          (past, PublicQuittingState.active signal) = 0 := by
        rw [KiloblockConstruction.normalPotential, hmode]
      rw [hcurrent]
      unfold StochasticGame.historyContinuationEU
      unfold StochasticGame.stageActionDist
      rw [hfamily, hproduct, Math.Probability.expect_pure,
        htransition, Math.Probability.expect_pure]
      rw [KiloblockConstruction.normalPotential, hm]

private theorem KiloblockConstruction.normalPotential_active_some_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some owner) remaining)
    (continuation : ℝ)
    (hcontinue : ∀ signal,
      history.2 = PublicQuittingState.active signal →
      construction.normalPotential who (t + 1)
        (Fin.snoc history.1
            (history.2, singleQuitAction owner.1 false),
          PublicQuittingState.draw) = continuation) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
      history =
      quittingMeshHazard (construction.attempt k owner).quitWeight
          (construction.mesh k) *
            (NormalMatrix table who owner +
              construction.trackingCorrection k who) +
        (1 - quittingMeshHazard (construction.attempt k owner).quitWeight
          (construction.mesh k)) * continuation := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      let quitState : (publicQuittingGame table
          construction.profile.signalLaw).State :=
        PublicQuittingState.absorbed
          (quittingProjectiveSingletonTerminal owner.1)
      let drawState : (publicQuittingGame table
          construction.profile.signalLaw).State := PublicQuittingState.draw
      have htransitionQuit : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal)
            (singleQuitAction owner.1 true) = PMF.pure quitState := by
        simpa [quitState] using
          publicQuittingGame_transition_singleQuitAction table
            construction.profile.signalLaw signal owner.1
      have htransitionContinue : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal)
            (singleQuitAction owner.1 false) = PMF.pure drawState := by
        exact publicQuittingGame_transition_singleQuitAction_false table
          construction.profile.signalLaw signal owner.1
      have hquitSupport : quitState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              (PublicQuittingState.active signal)
              (singleQuitAction owner.1 true)).support := by
        rw [htransitionQuit, PMF.mem_support_iff]
        simp
      have hquitStep := construction.mode_step t
        (past, PublicQuittingState.active signal)
        (singleQuitAction owner.1 true) quitState hquitSupport
      have hquitMode : construction.mode (t + 1)
          (Fin.snoc past
            (PublicQuittingState.active signal,
                singleQuitAction owner.1 true), quitState) =
          .absorbed (some k) := by
        simp [KiloblockModeStep, hmode,
          singleQuitAction_true_quitters] at hquitStep
        exact hquitStep.2
      have hquitValue : construction.normalPotential who (t + 1)
          (Fin.snoc past
              (PublicQuittingState.active signal,
                singleQuitAction owner.1 true), quitState) =
          NormalMatrix table who owner +
            construction.trackingCorrection k who := by
        rw [KiloblockConstruction.normalPotential, hquitMode]
        simp [quitState,
          KiloblockConstruction.trackingCorrectionAtOrigin, NormalMatrix]
      have hcontinueValue : construction.normalPotential who (t + 1)
          (Fin.snoc past
              (PublicQuittingState.active signal,
                singleQuitAction owner.1 false), drawState) =
          continuation := by
        simpa [drawState] using hcontinue signal rfl
      let coin := quittingMeshHazardCoin
        (construction.attempt k owner).quitWeight (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one
      let actionLaw : PMF ((publicQuittingGame table
          construction.profile.signalLaw).JointAct) :=
        coin.bind (fun quits => PMF.pure
          (singleQuitAction owner.1 quits))
      have hactionLaw : (publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
          construction.profile.strategy
          (past, PublicQuittingState.active signal) = actionLaw := by
        unfold actionLaw coin singleQuitAction
        exact construction.stageActionDist_active_some
          (past, PublicQuittingState.active signal) k owner remaining hmode
      letI : Finite ((publicQuittingGame table
          construction.profile.signalLaw).JointAct) :=
        inferInstanceAs (Finite (ι → Bool))
      unfold StochasticGame.historyContinuationEU
      rw [hactionLaw]
      unfold actionLaw
      rw [Math.Probability.expect_bind]
      simp_rw [Math.Probability.expect_pure]
      rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
      rw [htransitionContinue, Math.Probability.expect_pure,
        hcontinueValue, htransitionQuit, Math.Probability.expect_pure,
        hquitValue]
      unfold coin
      simp only [quittingMeshHazardCoin_false_toReal,
        quittingMeshHazardCoin_true_toReal]

theorem KiloblockConstruction.normalPotential_active_some_beforeFinal
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some owner) remaining)
    (hnotFinal : (construction.attempt k owner).continuation =
        .restart ∨ k.1 ≠ 0) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  have hremaining : 0 < remaining :=
    construction.mode_remaining_pos t history k (some owner) remaining
      (Or.inl hmode)
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      obtain ⟨tail, rfl⟩ := Nat.exists_eq_succ_of_ne_zero
        (Nat.ne_of_gt hremaining)
      let drawState : (publicQuittingGame table
          construction.profile.signalLaw).State := PublicQuittingState.draw
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal)
            (singleQuitAction owner.1 false) = PMF.pure drawState := by
        exact publicQuittingGame_transition_singleQuitAction_false table
          construction.profile.signalLaw signal owner.1
      have hsupp : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              (PublicQuittingState.active signal)
              (singleQuitAction owner.1 false)).support := by
        rw [htransition, PMF.mem_support_iff]
        simp
      have hstep := construction.mode_step t
        (past, PublicQuittingState.active signal)
        (singleQuitAction owner.1 false) drawState hsupp
      let continuation := construction.selectedValue k (some owner) tail who +
        construction.trackingCorrection k who
      have hcontinueValue : construction.normalPotential who (t + 1)
          (Fin.snoc past
              (PublicQuittingState.active signal,
                singleQuitAction owner.1 false), drawState) =
          continuation := by
        cases tail with
        | succ nextTail =>
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                    (PublicQuittingState.active signal,
                      singleQuitAction owner.1 false), drawState) =
                .draw (.resume k (some owner) (nextTail + 1)) := by
              simpa [KiloblockModeStep, hmode, drawState,
                singleQuitAction_false] using hstep
            rw [KiloblockConstruction.normalPotential, hm]
        | zero =>
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                    (PublicQuittingState.active signal,
                      singleQuitAction owner.1 false), drawState) =
                .draw (phaseAfterAttempt table
                  (fun k owner => (construction.attempt k owner).continuation)
                  k (some owner)) := by
              simpa [KiloblockModeStep, hmode, drawState,
                singleQuitAction_false] using hstep
            cases hbranch : (construction.attempt k owner).continuation with
            | restart =>
              have hm' : construction.mode (t + 1)
                  (Fin.snoc past
                      (PublicQuittingState.active signal,
                        singleQuitAction owner.1 false), drawState) =
                    .draw (.choose k) := by
                simpa [phaseAfterAttempt, hbranch] using hm
              rw [KiloblockConstruction.normalPotential, hm']
              simp [continuation, KiloblockConstruction.selectedValue,
                BuildingAttempt.remainingValue_zero, hbranch]
            | advance =>
              have hk : k.1 ≠ 0 := hnotFinal.resolve_left (by
                rw [hbranch]
                exact AttemptContinuation.noConfusion)
              have hkpos : 0 < k.1 := Nat.pos_of_ne_zero hk
              let previous : Fin (construction.blockCount + 1) :=
                ⟨k.1 - 1, by omega⟩
              have hprevious : previous.1 < construction.blockCount := by
                dsimp [previous]
                omega
              have hkEq : k = ⟨previous.1 + 1, by omega⟩ := by
                apply Fin.ext
                dsimp [previous]
                omega
              have hm' : construction.mode (t + 1)
                  (Fin.snoc past
                      (PublicQuittingState.active signal,
                        singleQuitAction owner.1 false), drawState) =
                    .draw (.choose previous) := by
                simpa [phaseAfterAttempt, precedingKiloblockPhase,
                  hbranch, hk, previous] using hm
              have htelescope :=
                construction.point_add_trackingCorrection_succ
                  previous.1 hprevious who
              rw [← hkEq] at htelescope
              have htelescope' : construction.point k who +
                  construction.trackingCorrection k who =
                    (construction.buildingBlock previous).w who +
                      construction.trackingCorrection previous who := by
                convert htelescope using 1
              have hpotential : construction.normalPotential who (t + 1)
                  (Fin.snoc past
                      (PublicQuittingState.active signal,
                        singleQuitAction owner.1 false), drawState) =
                    (construction.buildingBlock previous).w who +
                      construction.trackingCorrection previous who := by
                rw [KiloblockConstruction.normalPotential, hm']
              rw [hpotential, ← htelescope']
              simp [continuation, KiloblockConstruction.selectedValue,
                BuildingAttempt.remainingValue_zero, hbranch]
      have hlocal := construction.normalPotential_active_some_step who
        (past, PublicQuittingState.active signal) k owner (tail + 1) hmode
        continuation (fun selected hselected => by
          cases PublicQuittingState.active.inj hselected
          simpa [drawState] using hcontinueValue)
      have hcurrent : construction.normalPotential who t
          (past, PublicQuittingState.active signal) =
            construction.selectedValue k (some owner) (tail + 1) who +
              construction.trackingCorrection k who := by
        rw [KiloblockConstruction.normalPotential, hmode]
      rw [hlocal, hcurrent]
      rw [construction.selectedValue_some_succ k owner tail who]
      dsimp [continuation]
      ring

theorem KiloblockConstruction.normalPotential_active_none_beforeFinal
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k none remaining)
    (hnotFinal : remaining ≠ 1 ∨ k.1 ≠ 0) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy (construction.normalPotential who)
        history =
      construction.normalPotential who t history := by
  classical
  have hremaining : 0 < remaining :=
    construction.mode_remaining_pos t history k none remaining
      (Or.inl hmode)
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw => simp [KiloblockMode.MatchesState, hmode] at hmatches
  | absorbed quitters =>
      simp [KiloblockMode.MatchesState, hmode] at hmatches
  | active signal =>
      obtain ⟨tail, rfl⟩ := Nat.exists_eq_succ_of_ne_zero
        (Nat.ne_of_gt hremaining)
      let continueAction : (publicQuittingGame table
          construction.profile.signalLaw).JointAct := fun _ => false
      let base : ∀ player, PMF ((publicQuittingGame table
          construction.profile.signalLaw).Act player) :=
        fun _ => PMF.pure false
      let drawState : (publicQuittingGame table
          construction.profile.signalLaw).State := PublicQuittingState.draw
      have hfamily :
          (fun player => construction.profile.strategy player t
            (past, PublicQuittingState.active signal)) = base := by
        funext player
        rw [construction.strategy_eq]
        simp [hmode, base]
      have hproduct : Math.PMFProduct.pmfPi base =
          PMF.pure continueAction := by
        exact Math.PMFProduct.pmfPi_pure continueAction
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
          (PublicQuittingState.active signal) continueAction =
          PMF.pure drawState := by
        simp [publicQuittingGame, continueAction, drawState]
      have hsupp : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              (PublicQuittingState.active signal) continueAction).support := by
        rw [htransition, PMF.mem_support_iff]
        simp
      have hstep := construction.mode_step t
        (past, PublicQuittingState.active signal) continueAction drawState hsupp
      have hnextValue : construction.normalPotential who (t + 1)
          (Fin.snoc past
            (PublicQuittingState.active signal, continueAction), drawState) =
          construction.point k who +
            construction.trackingCorrection k who := by
        cases tail with
        | succ nextTail =>
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                  (PublicQuittingState.active signal, continueAction),
                    drawState) =
                .draw (.resume k none (nextTail + 1)) := by
              simpa [KiloblockModeStep, hmode, continueAction, drawState]
                using hstep
            rw [KiloblockConstruction.normalPotential, hm]
            rfl
        | zero =>
            have hk : k.1 ≠ 0 := hnotFinal.resolve_left (by simp)
            have hkpos : 0 < k.1 := Nat.pos_of_ne_zero hk
            let previous : Fin (construction.blockCount + 1) :=
              ⟨k.1 - 1, by omega⟩
            have hprevious : previous.1 < construction.blockCount := by
              dsimp [previous]
              omega
            have hkEq : k = ⟨previous.1 + 1, by omega⟩ := by
              apply Fin.ext
              dsimp [previous]
              omega
            have hm : construction.mode (t + 1)
                (Fin.snoc past
                  (PublicQuittingState.active signal, continueAction),
                    drawState) = .draw (.choose previous) := by
              simpa [KiloblockModeStep, hmode, continueAction, drawState,
                phaseAfterAttempt, precedingKiloblockPhase, hk, previous]
                using hstep
            have htelescope :=
              construction.point_add_trackingCorrection_succ
                previous.1 hprevious who
            rw [← hkEq] at htelescope
            have htelescope' : construction.point k who +
                construction.trackingCorrection k who =
                  (construction.buildingBlock previous).w who +
                    construction.trackingCorrection previous who := by
              convert htelescope using 1
            rw [KiloblockConstruction.normalPotential, hm]
            exact htelescope'.symm
      have hcurrent : construction.normalPotential who t
          (past, PublicQuittingState.active signal) =
          construction.point k who +
            construction.trackingCorrection k who := by
        rw [KiloblockConstruction.normalPotential, hmode]
        rfl
      rw [hcurrent]
      unfold StochasticGame.historyContinuationEU
      unfold StochasticGame.stageActionDist
      rw [hfamily, hproduct, Math.Probability.expect_pure,
        htransition, Math.Probability.expect_pure]
      exact hnextValue

/-! Whether the schedule has not yet entered its final infinite tail. The
boolean stored after absorption remembers on which side of that boundary
absorption occurred. -/
def KiloblockConstruction.beforeFinal
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (t : ℕ)
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) : Prop :=
  match construction.mode t history with
  | .draw .final => False
  | .finalActive => False
  | .absorbed origin => origin.isSome
  | _ => True

/-! Indicator that absorption has occurred while the construction's history
marker still lies before the final kiloblock. -/
def absorbedBeforeFinalIndicator
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (t : ℕ)
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) : ℝ := by
  classical
  exact match history.2 with
    | .draw => 0
    | .active _ => 0
    | .absorbed _ => if construction.beforeFinal t history then 1 else 0

theorem KiloblockConstruction.absorbedBeforeFinalIndicator_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    absorbedBeforeFinalIndicator construction t history =
      construction.continueAbsorbedIndicator
        (construction.finiteMode (construction.mode t history)) := by
  rcases history with ⟨past, state⟩
  have hmatches := construction.mode_state t (past, state)
  cases state with
  | draw =>
      cases hmode : construction.mode t (past, PublicQuittingState.draw) <;>
        simp [KiloblockMode.MatchesState, hmode,
          absorbedBeforeFinalIndicator,
          KiloblockConstruction.continueAbsorbedIndicator,
          KiloblockConstruction.finiteMode] at hmatches ⊢
      rename_i phase
      cases phase <;> rfl
  | active signal =>
      cases hmode : construction.mode t
          (past, PublicQuittingState.active signal) <;>
        simp [KiloblockMode.MatchesState, hmode,
          absorbedBeforeFinalIndicator,
          KiloblockConstruction.continueAbsorbedIndicator,
          KiloblockConstruction.finiteMode] at hmatches ⊢
  | absorbed quitters =>
      cases hmode : construction.mode t
          (past, PublicQuittingState.absorbed quitters) <;>
        simp [KiloblockMode.MatchesState, hmode,
          absorbedBeforeFinalIndicator,
          KiloblockConstruction.beforeFinal,
          KiloblockConstruction.continueAbsorbedIndicator,
          KiloblockConstruction.finiteMode] at hmatches ⊢
      rename_i origin
      cases origin <;> simp

theorem KiloblockConstruction.expect_absorbedBeforeFinalIndicator_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile) (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist strategy .draw t)
        (absorbedBeforeFinalIndicator construction t) =
      expect (construction.modeDist strategy t)
        construction.continueAbsorbedIndicator := by
  unfold KiloblockConstruction.modeDist
  rw [Math.Probability.expect_map]
  apply Math.ProbabilityMassFunction.expect_congr_on_support
  intro history _
  exact construction.absorbedBeforeFinalIndicator_eq history

theorem KiloblockConstruction.expect_absorbedBeforeFinalIndicator_le_one
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile) (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist strategy .draw t)
        (absorbedBeforeFinalIndicator construction t) ≤ 1 := by
  rw [construction.expect_absorbedBeforeFinalIndicator_eq strategy t]
  have hmono := Math.Probability.expect_mono
    (construction.modeDist strategy t)
    construction.continueAbsorbedIndicator (fun _ => 1) (by
      intro mode
      cases mode <;>
        simp [KiloblockConstruction.continueAbsorbedIndicator]
      split <;> simp_all)
  simpa using hmono

/-! Probability of absorption before the final kiloblock under a supplied
public behavior profile, represented as the supremum of its finite-horizon
event masses. -/
def absorptionBeforeFinalProbability
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile) : ℝ :=
  ⨆ t, expect
    ((publicQuittingGame table construction.profile.signalLaw).histDist
      strategy .draw t)
    (absorbedBeforeFinalIndicator construction t)

/-! Lemma 3.9's induced probability after player `who` is forced to Continue
at every public history. -/
def continueTerminationBeforeFinal
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (who : ι) : ℝ :=
  absorptionBeforeFinalProbability construction
    (Function.update construction.profile.strategy who
      (fun _ _ => PMF.pure false))

def KiloblockConstruction.continueFinalHistoryPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => construction.continueFinalPotential excluded
    (construction.finiteMode (construction.mode t history))

theorem KiloblockConstruction.continueFinalHistoryPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.continueFinalHistoryPotential excluded) history =
      construction.continueFinalHistoryPotential excluded t history := by
  unfold StochasticGame.historyContinuationEU
    KiloblockConstruction.continueFinalHistoryPotential
  rw [← construction.continueFinalPotential_harmonic excluded
    (construction.finiteMode (construction.mode t history))]
  rw [← construction.finiteHistoryModeStepDist_continue_eq excluded history]
  unfold KiloblockConstruction.historyModeStepDist
  rw [PMF.map_bind, Math.Probability.expect_bind]
  apply congrArg (expect ((publicQuittingGame table
    construction.profile.signalLaw).stageActionDist
      (construction.continueProfile excluded) history))
  funext action
  rw [PMF.map_comp, Math.Probability.expect_map]
  rfl

private theorem historyContinuationEU_update_eq_expect_pure
    {Signal : Type} [Fintype Signal]
    (table : Table ι) (signalLaw : PMF Signal)
    (profile : (publicQuittingGame table signalLaw).BehaviorProfile)
    (who : ι)
    (deviation : (publicQuittingGame table signalLaw).BehaviorStrategy who)
    (potential : (publicQuittingGame table signalLaw).HistoryPotential)
    {t : ℕ}
    (history : (publicQuittingGame table signalLaw).Hist t) :
    (publicQuittingGame table signalLaw).historyContinuationEU
        (Function.update profile who deviation) potential history =
      expect (deviation t history) (fun action =>
        (publicQuittingGame table signalLaw).historyContinuationEU
          (Function.update profile who (fun _ _ => PMF.pure action))
          potential history) := by
  let actions : ∀ player,
      PMF ((publicQuittingGame table signalLaw).Act player) :=
    fun player => profile player t history
  have hactionDist :
      (publicQuittingGame table signalLaw).stageActionDist
          (Function.update profile who deviation) history =
        Math.PMFProduct.pmfPi
          (Function.update actions who (deviation t history)) := by
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = who
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]

  unfold StochasticGame.historyContinuationEU
  rw [hactionDist, Math.PMFProduct.pmfPi_update_bind,
    Math.Probability.expect_bind]
  apply congrArg (expect (deviation t history))
  funext action
  unfold StochasticGame.stageActionDist
  rw [show Function.update actions who (PMF.pure action) =
      (fun player => Function.update profile who
        (fun _ _ => PMF.pure action) player t history) by
    funext player
    by_cases hplayer : player = who
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]]

private theorem KiloblockConstruction.continueFinal_pureQuit_le_continue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true))
        (construction.continueFinalHistoryPotential excluded) history ≤
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          (construction.continueProfile excluded)
          (construction.continueFinalHistoryPotential excluded) history := by
  classical
  let actions : ∀ player,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act player) :=
    fun player => construction.profile.strategy player t history
  have hquitDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true)) history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded.1 (PMF.pure true)) := by
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = excluded.1
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]

  have hcontinueDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        (construction.continueProfile excluded) history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded.1 (PMF.pure false)) := by
    unfold StochasticGame.stageActionDist
      KiloblockConstruction.continueProfile
    congr 1
    funext player
    by_cases hplayer : player = excluded.1
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  unfold StochasticGame.historyContinuationEU
  rw [hquitDist, hcontinueDist,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded.1 true,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded.1 false,
    Math.Probability.expect_bind, Math.Probability.expect_bind]
  simp_rw [Math.Probability.expect_pure]
  apply Math.Probability.expect_mono
  intro action
  let quitAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded.1 true
  let continueAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded.1 false
  cases hmode : construction.mode t history with
  | active k choice remaining =>
      have hquitValue : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.continueFinalHistoryPotential
            excluded (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        have hnextMode : construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, quitAction), nextState) =
              .absorbed (some k) := by
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep
          case isTrue => exact hstep.2
          case isFalse hfalse =>
            exfalso
            apply hfalse
            refine ⟨excluded.1, ?_⟩
            simp [quitAction]
        unfold KiloblockConstruction.continueFinalHistoryPotential
        rw [hnextMode]
        rfl
      rw [hquitValue]
      exact expect_nonneg _ _ fun nextState =>
        construction.continueFinalPotential_nonneg excluded
          (construction.finiteMode (construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState)))
  | finalActive =>
      have hleft : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.continueFinalHistoryPotential
            excluded (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 1 := by
        rw [← Math.Probability.expect_const _ 1]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        unfold KiloblockConstruction.continueFinalHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep
        · rw [hstep.2]
          rfl
        · rw [hstep.2]
          rfl
      have hright : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 continueAction)
          (fun nextState => construction.continueFinalHistoryPotential
            excluded (t + 1)
              (Fin.snoc history.1 (history.2, continueAction), nextState)) =
          1 := by
        rw [← Math.Probability.expect_const _ 1]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history continueAction
          nextState hnext
        unfold KiloblockConstruction.continueFinalHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep
        · rw [hstep.2]
          rfl
        · rw [hstep.2]
          rfl
      rw [hleft, hright]
  | draw phase =>
      have hstate : history.2 = PublicQuittingState.draw := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
      rw [hstate]
      apply le_of_eq
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro nextState hnext
      have hquitStep := construction.mode_step t history quitAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hcontinueStep := construction.mode_step t history continueAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hm : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState) := by
        cases phase <;> cases hsnext : nextState <;>
          simp [KiloblockModeStep, hmode, hsnext] at hquitStep hcontinueStep
        all_goals rw [hquitStep, hcontinueStep]
      unfold KiloblockConstruction.continueFinalHistoryPotential
      have hm' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.draw, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1
              (PublicQuittingState.draw, continueAction), nextState) := by
        simpa only [hstate] using hm
      rw [hm']
  | absorbed origin =>
      have hstate : ∃ quitters, history.2 =
          PublicQuittingState.absorbed quitters := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
        exact ⟨_, rfl⟩
      obtain ⟨quitters, hstate⟩ := hstate
      rw [hstate]
      simp only [publicQuittingGame, Math.Probability.expect_pure]
      have hquitStep := construction.mode_step t history quitAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      have hcontinueStep := construction.mode_step t history continueAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      simp only [KiloblockModeStep, hmode] at hquitStep hcontinueStep
      unfold KiloblockConstruction.continueFinalHistoryPotential
      have hquitStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, quitAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hquitStep
      have hcontinueStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, continueAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hcontinueStep
      change construction.continueFinalPotential excluded
          (construction.finiteMode
            (construction.mode (t + 1)
              (Fin.snoc history.1
                (PublicQuittingState.absorbed quitters, quitAction),
                  PublicQuittingState.absorbed quitters))) ≤
        construction.continueFinalPotential excluded
          (construction.finiteMode
            (construction.mode (t + 1)
              (Fin.snoc history.1
                (PublicQuittingState.absorbed quitters, continueAction),
                  PublicQuittingState.absorbed quitters)))
      rw [hquitStep', hcontinueStep']

/-! The finite Bellman value against fixed opponent roots: the player may
quit now or continue to the next active public decision, and the value after
the displayed horizon is zero. This proof-local recursion makes the
action-irrelevant draw stages explicit. -/
private def finiteStoppingBest
    (reward : {S : Finset ι // S.Nonempty} → Payoff ι)
    (roots : ℕ → ι → PMF Bool) (who : ι) : ℕ → ℕ → ℝ
  | _, 0 => 0
  | start, fuel + 1 =>
      max (quittingFixedOpponentsQuitValue reward roots who start)
        (quittingFixedOpponentsContinueReward reward roots who start +
          quittingFixedOpponentsContinueMass roots who start *
            finiteStoppingBest reward roots who (start + 1) fuel)

/-! Finite public-history Bellman potential. Draw time `t` and the following
active time `t+1` carry the same value; after an active time, the root index
advances by one. Absorbed histories carry their realized zero-never reward. -/
private def rootSequenceFinitePotential (table : Table ι)
    (roots : ℕ → ι → PMF Bool) (who : ι) (horizon : ℕ) :
    (publicQuittingGame table trivialSignalLaw).HistoryPotential :=
  fun time history => match history.2 with
    | .draw => finiteStoppingBest table.zeroNeverReward roots who
        ((time + 1) / 2) (horizon - (time + 1) / 2)
    | .active _ => finiteStoppingBest table.zeroNeverReward roots who
        (time / 2) (horizon - time / 2)
    | .absorbed quitters => table.zeroNeverReward quitters who

private theorem quittingFinitePureTimePayoff_le_finiteStoppingBest
    (reward : {S : Finset ι // S.Nonempty} → Payoff ι)
    (roots : ℕ → ι → PMF Bool) (who : ι) :
    ∀ (start fuel : ℕ) (choice : Fin (fuel + 1)),
      quittingFinitePureTimePayoff reward roots who start fuel choice ≤
        finiteStoppingBest reward roots who start fuel := by
  intro start fuel
  induction fuel generalizing start with
  | zero =>
      intro choice
      simp [quittingFinitePureTimePayoff, quittingFinitePureTimeValue,
        finiteStoppingBest]
  | succ fuel ih =>
      intro choice
      refine Fin.cases ?_ (fun later => ?_) choice
      · simp only [quittingFinitePureTimePayoff,
          quittingFinitePureTimeValue, finiteStoppingBest, Fin.cases_zero]
        exact le_max_left _ _
      · simp only [quittingFinitePureTimePayoff,
          quittingFinitePureTimeValue, finiteStoppingBest, Fin.cases_succ]
        apply le_trans _ (le_max_right _ _)
        change quittingFixedOpponentsContinueReward reward roots who start +
            quittingFixedOpponentsContinueMass roots who start *
              quittingFinitePureTimePayoff reward roots who (start + 1)
                fuel later ≤ _
        have hmass : 0 ≤
            quittingFixedOpponentsContinueMass roots who start := by
          unfold quittingFixedOpponentsContinueMass
          exact quittingStationaryContinueMass_nonneg _
        exact add_le_add le_rfl
          (mul_le_mul_of_nonneg_left (ih (start + 1) later) hmass)

private theorem exists_quittingFinitePureTimePayoff_eq_finiteStoppingBest
    (reward : {S : Finset ι // S.Nonempty} → Payoff ι)
    (roots : ℕ → ι → PMF Bool) (who : ι) :
    ∀ (start fuel : ℕ), ∃ choice : Fin (fuel + 1),
      quittingFinitePureTimePayoff reward roots who start fuel choice =
        finiteStoppingBest reward roots who start fuel := by
  intro start fuel
  induction fuel generalizing start with
  | zero =>
      exact ⟨0, by
        simp [quittingFinitePureTimePayoff, quittingFinitePureTimeValue,
          finiteStoppingBest]⟩
  | succ fuel ih =>
      let quitValue := quittingFixedOpponentsQuitValue reward roots who start
      let continueValue := quittingFixedOpponentsContinueReward reward roots
          who start + quittingFixedOpponentsContinueMass roots who start *
            finiteStoppingBest reward roots who (start + 1) fuel
      by_cases hquit : continueValue ≤ quitValue
      · refine ⟨0, ?_⟩
        simp only [quittingFinitePureTimePayoff,
          quittingFinitePureTimeValue, finiteStoppingBest, Fin.cases_zero]
        exact (max_eq_left hquit).symm
      · obtain ⟨later, hlater⟩ := ih (start + 1)
        refine ⟨later.succ, ?_⟩
        simp only [quittingFinitePureTimePayoff,
          quittingFinitePureTimeValue, finiteStoppingBest, Fin.cases_succ]
        change quittingFixedOpponentsContinueReward reward roots who start +
            quittingFixedOpponentsContinueMass roots who start *
              quittingFinitePureTimePayoff reward roots who (start + 1)
                fuel later = _
        rw [hlater]
        exact (max_eq_right (le_of_not_ge hquit)).symm

@[simp] private theorem rootSequenceFinitePotential_initial
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) :
    rootSequenceFinitePotential table roots who horizon 0
        ((publicQuittingGame table trivialSignalLaw).emptyHist .draw) =
      finiteStoppingBest table.zeroNeverReward roots who 0 horizon := by
  change finiteStoppingBest table.zeroNeverReward roots who 0 horizon = _
  rfl

@[simp] private theorem rootSequenceFinitePotential_evenHorizon
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (history : (publicQuittingGame table trivialSignalLaw).Hist
      (2 * horizon)) :
    rootSequenceFinitePotential table roots who horizon (2 * horizon)
        history =
      (match history.2 with
        | .absorbed quitters => table.zeroNeverReward quitters who
        | _ => 0) := by
  rcases history with ⟨past, state⟩
  cases state with
  | draw =>
      change finiteStoppingBest table.zeroNeverReward roots who
          ((2 * horizon + 1) / 2)
          (horizon - (2 * horizon + 1) / 2) = 0
      rw [show (2 * horizon + 1) / 2 = horizon by omega]
      simp [finiteStoppingBest]
  | active signal =>
      change finiteStoppingBest table.zeroNeverReward roots who
          ((2 * horizon) / 2) (horizon - (2 * horizon) / 2) = 0
      simp [finiteStoppingBest]
  | absorbed quitters =>
      rfl

private theorem rootSequenceFinitePotential_draw_harmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (strategy : (publicQuittingGame table trivialSignalLaw).BehaviorProfile)
    {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (hstate : history.2 = .draw) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU strategy
        (rootSequenceFinitePotential table roots who horizon) history =
      rootSequenceFinitePotential table roots who horizon time history := by
  unfold StochasticGame.historyContinuationEU
  let activeLaw : PMF
      (publicQuittingGame table trivialSignalLaw).State :=
    trivialSignalLaw.map PublicQuittingState.active
  have htransition (action :
      (publicQuittingGame table trivialSignalLaw).JointAct) :
      (publicQuittingGame table trivialSignalLaw).transition history.2
          action =
        activeLaw := by
    rw [hstate]
    exact publicQuittingGame_transition_draw table trivialSignalLaw action
  simp_rw [htransition]
  have hinner (action :
      (publicQuittingGame table trivialSignalLaw).JointAct) :
      expect activeLaw
          (fun nextState => rootSequenceFinitePotential table roots who
            horizon (time + 1)
            (Fin.snoc history.1 (history.2, action), nextState)) =
        finiteStoppingBest table.zeroNeverReward roots who
          ((time + 1) / 2) (horizon - (time + 1) / 2) := by
    unfold activeLaw
    rw [Math.Probability.expect_map]
    change expect trivialSignalLaw (fun _ =>
      finiteStoppingBest table.zeroNeverReward roots who
        ((time + 1) / 2) (horizon - (time + 1) / 2)) = _
    exact Math.Probability.expect_const _ _
  simp_rw [hinner]
  rw [Math.Probability.expect_const]
  simp only [rootSequenceFinitePotential, hstate]

private theorem rootSequenceFinitePotential_absorbed_harmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (strategy : (publicQuittingGame table trivialSignalLaw).BehaviorProfile)
    {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (quitters : {S : Finset ι // S.Nonempty})
    (hstate : history.2 = .absorbed quitters) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU strategy
        (rootSequenceFinitePotential table roots who horizon) history =
      rootSequenceFinitePotential table roots who horizon time history := by
  rcases history with ⟨past, state⟩
  cases state with
  | draw => simp at hstate
  | active signal => simp at hstate
  | absorbed terminal =>
      have heq : terminal = quitters := by
        injection hstate
      subst terminal
      unfold StochasticGame.historyContinuationEU
      simp only [publicQuittingGame_transition_absorbed,
        Math.Probability.expect_pure, rootSequenceFinitePotential]
      exact Math.Probability.expect_const _ _

private theorem rootSequenceFinitePotential_active_pure
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (signal : Fin 1) (hstate : history.2 = .active signal)
    (action : Bool) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU
        (Function.update (rootSequenceSunspotStrategy table roots) who
          (fun _ _ => PMF.pure action))
        (rootSequenceFinitePotential table roots who horizon) history =
      quittingRootExpectedPayoff table.zeroNeverReward
        (fun _ => finiteStoppingBest table.zeroNeverReward roots who
          (time / 2 + 1) (horizon - (time / 2 + 1)))
      (Function.update (roots (time / 2)) who (PMF.pure action)) who := by
  classical
  rcases history with ⟨past, state⟩
  cases state with
  | draw => simp at hstate
  | absorbed quitters => simp at hstate
  | active observed =>
      have hsignal : observed = signal := Subsingleton.elim _ _
      subst observed
      have hactionDist :
          (publicQuittingGame table trivialSignalLaw).stageActionDist
              (Function.update (rootSequenceSunspotStrategy table roots) who
                (fun _ _ => PMF.pure action)) (past, .active signal) =
            Math.PMFProduct.pmfPi
              (Function.update (roots (time / 2)) who (PMF.pure action)) := by
        unfold StochasticGame.stageActionDist
        congr 1
        funext player
        by_cases hplayer : player = who
        · subst player
          simp only [Function.update_self]
          rfl
        · rw [Function.update_of_ne hplayer]
          simp [rootSequenceSunspotStrategy, rootSequencePublicStrategy,
            Function.update_of_ne hplayer]
      unfold StochasticGame.historyContinuationEU
      rw [hactionDist]
      unfold quittingRootExpectedPayoff
      apply congrArg (expect (Math.PMFProduct.pmfPi
        (Function.update (roots (time / 2)) who (PMF.pure action))))
      funext jointAction
      by_cases hquit : (quittingQuitters jointAction).Nonempty
      · rw [publicQuittingGame_transition_active, dif_pos hquit,
          Math.Probability.expect_pure]
        simp only [rootSequenceFinitePotential, quittingRootPayoff,
          dif_pos hquit]
      · rw [publicQuittingGame_transition_active, dif_neg hquit,
          Math.Probability.expect_pure]
        simp only [rootSequenceFinitePotential, quittingRootPayoff,
          dif_neg hquit]
        rw [show (time + 1 + 1) / 2 = time / 2 + 1 by omega]

private theorem rootSequenceFinitePotential_superharmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (deviation : (publicQuittingGame table
      trivialSignalLaw).BehaviorStrategy who)
    {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (htime : time < 2 * horizon) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU
        (Function.update (rootSequenceSunspotStrategy table roots) who
          deviation)
        (rootSequenceFinitePotential table roots who horizon) history ≤
      rootSequenceFinitePotential table roots who horizon time history := by
  cases hstate : history.2 with
  | draw =>
      exact (rootSequenceFinitePotential_draw_harmonic table roots who
        horizon _ history hstate).le
  | absorbed quitters =>
      exact (rootSequenceFinitePotential_absorbed_harmonic table roots who
        horizon _ history quitters hstate).le
  | active signal =>
      rw [historyContinuationEU_update_eq_expect_pure table
        trivialSignalLaw (rootSequenceSunspotStrategy table roots) who
        deviation (rootSequenceFinitePotential table roots who horizon)
        history]
      calc
        expect (deviation time history) (fun action =>
            (publicQuittingGame table
              trivialSignalLaw).historyContinuationEU
                (Function.update (rootSequenceSunspotStrategy table roots)
                  who (fun _ _ => PMF.pure action))
                (rootSequenceFinitePotential table roots who horizon)
                history) ≤
            expect (deviation time history) (fun _ =>
              rootSequenceFinitePotential table roots who horizon time
                history) := by
          apply Math.Probability.expect_mono
          intro action
          rw [rootSequenceFinitePotential_active_pure table roots who horizon
            history signal hstate action,
            quittingRootExpectedPayoff_update_eq_endpointMix,
            quittingRootQuitPayoff_eq_fixedOpponentsQuitValue,
            quittingRootContinuePayoff_eq_fixedOpponents]
          have hround : time / 2 < horizon := by omega
          have hfuel : horizon - time / 2 =
              horizon - (time / 2 + 1) + 1 := by omega
          rw [show rootSequenceFinitePotential table roots who horizon time
              history = finiteStoppingBest table.zeroNeverReward roots who
                (time / 2) (horizon - time / 2) by
            simp only [rootSequenceFinitePotential, hstate]]
          rw [hfuel, finiteStoppingBest]
          cases action with
          | false =>
              simp only [PMF.pure_apply,
                if_neg (by decide : (true : Bool) ≠ false),
                ENNReal.toReal_zero, if_true, ENNReal.toReal_one,
                zero_mul, one_mul, zero_add]
              exact le_max_right _ _
          | true =>
              simp only [PMF.pure_apply, if_true, ENNReal.toReal_one,
                if_neg (by decide : (false : Bool) ≠ true),
                ENNReal.toReal_zero, one_mul, zero_mul, add_zero]
              exact le_max_left _ _
        _ = rootSequenceFinitePotential table roots who horizon time
              history := Math.Probability.expect_const _ _

private theorem expect_rootSequenceFinitePotential_le_initial
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (deviation : (publicQuittingGame table
      trivialSignalLaw).BehaviorStrategy who) :
    ∀ time, time ≤ 2 * horizon →
      expect ((publicQuittingGame table trivialSignalLaw).histDist
        (Function.update (rootSequenceSunspotStrategy table roots) who
          deviation) .draw time)
        (rootSequenceFinitePotential table roots who horizon time) ≤
      finiteStoppingBest table.zeroNeverReward roots who 0 horizon := by
  intro time htime
  induction time with
  | zero =>
      rw [(publicQuittingGame table trivialSignalLaw).histDist_zero,
        Math.Probability.expect_pure,
        rootSequenceFinitePotential_initial]
  | succ time ih =>
      change (publicQuittingGame table
        trivialSignalLaw).expectedHistoryValue
          (Function.update (rootSequenceSunspotStrategy table roots) who
            deviation) .draw
          (rootSequenceFinitePotential table roots who horizon) (time + 1) ≤
        _
      rw [(publicQuittingGame table
        trivialSignalLaw).expectedHistoryValue_succ]
      apply le_trans (Math.Probability.expect_mono _ _ _ fun history =>
        rootSequenceFinitePotential_superharmonic table roots who horizon
          deviation history (by omega))
      exact ih (by omega)

/-! The finite payoff after subtracting the nontermination payoff. Live
states then have value zero, and an absorbed state has value `r_S-r_∅`. -/
private def publicZeroNeverStateValue
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1))) (who : ι) :
    (publicQuittingGame table signalLaw).State → ℝ :=
  fun state => match state with
    | .absorbed quitters => table.zeroNeverReward quitters who
    | _ => 0

private def publicZeroNeverFinitePayoff
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (time : ℕ) (who : ι) : ℝ :=
  expect ((publicQuittingGame table signalLaw).histDist strategy .draw time)
    (fun history => publicZeroNeverStateValue table signalLaw who history.2)

private theorem publicZeroNeverFinitePayoff_eq_sub
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (time : ℕ) (who : ι) :
    publicZeroNeverFinitePayoff table signalLaw strategy time who =
      publicFinitePayoff table signalLaw strategy time who -
        table.never who := by
  let distribution := (publicQuittingGame table signalLaw).histDist
    strategy .draw time
  let fullObservable : (publicQuittingGame table signalLaw).Hist time → ℝ :=
    fun history => match history.2 with
      | .absorbed quitters => table.terminal quitters who
      | _ => table.never who
  let zeroObservable : (publicQuittingGame table signalLaw).Hist time → ℝ :=
    fun history => publicZeroNeverStateValue table signalLaw who history.2
  have hpoint (history : (publicQuittingGame table signalLaw).Hist time) :
      fullObservable history = zeroObservable history + table.never who := by
    dsimp only [fullObservable, zeroObservable]
    cases history.2 <;>
      simp [publicZeroNeverStateValue,
        QuittingPayoffTable.zeroNeverReward]
  have hexpect : expect distribution fullObservable =
      expect distribution zeroObservable + table.never who := by
    calc
      expect distribution fullObservable =
          expect distribution (fun history =>
            zeroObservable history + table.never who) := by
        apply congrArg (expect distribution)
        funext history
        exact hpoint history
      _ = expect distribution zeroObservable + table.never who := by
        rw [Math.Probability.expect_add, Math.Probability.expect_const]
  change expect distribution zeroObservable =
    expect distribution fullObservable - table.never who
  linarith

private theorem publicZeroNeverFinitePayoff_deviation_le_best
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (deviation : (publicQuittingGame table
      trivialSignalLaw).BehaviorStrategy who)
    (horizon : ℕ) :
    publicZeroNeverFinitePayoff table trivialSignalLaw
        (Function.update (rootSequenceSunspotStrategy table roots) who
          deviation) (2 * horizon) who ≤
      finiteStoppingBest table.zeroNeverReward roots who 0 horizon := by
  let strategy := Function.update (rootSequenceSunspotStrategy table roots)
    who deviation
  let distribution := (publicQuittingGame table trivialSignalLaw).histDist
    strategy .draw (2 * horizon)
  let observable : (publicQuittingGame table
      trivialSignalLaw).Hist (2 * horizon) → ℝ :=
    fun history => publicZeroNeverStateValue table trivialSignalLaw who
      history.2
  let potential : (publicQuittingGame table
      trivialSignalLaw).Hist (2 * horizon) → ℝ :=
    rootSequenceFinitePotential table roots who horizon (2 * horizon)
  have heq : observable = potential := by
    funext history
    change publicZeroNeverStateValue table trivialSignalLaw who history.2 =
      rootSequenceFinitePotential table roots who horizon (2 * horizon)
        history
    rw [rootSequenceFinitePotential_evenHorizon table roots who horizon
      history]
    cases history.2 <;> rfl
  have hbound : expect distribution potential ≤
      finiteStoppingBest table.zeroNeverReward roots who 0 horizon := by
    exact expect_rootSequenceFinitePotential_le_initial table roots who
      horizon deviation (2 * horizon) le_rfl
  have hobservable : expect distribution observable ≤
      finiteStoppingBest table.zeroNeverReward roots who 0 horizon := by
    rw [heq]
    exact hbound
  simpa only [publicZeroNeverFinitePayoff, distribution, strategy,
    observable] using hobservable

private theorem eventually_finiteStoppingBest_le_of_epsilonEquilibrium
    (table : Table ι) {ε : ℝ}
    (profile : (quittingGame table.terminal).BehaviorProfile)
    (hnash : EpsilonEquilibrium table ε profile) (who : ι)
    {δ : ℝ} (hδ : 0 < δ) :
    ∀ᶠ horizon in atTop,
      finiteStoppingBest table.zeroNeverReward
          (quittingProfileLiveRoot table.zeroNeverReward profile) who
          0 horizon ≤
        quittingTerminalPayoff table.zeroNeverReward profile who + ε + δ := by
  let roots := quittingProfileLiveRoot table.zeroNeverReward profile
  let neverValue := quittingRootSequencePureTimeTerminalValue
    table.zeroNeverReward roots who none 0
  have hzeroNash : (quittingGame table.terminal).IsεAsymptoticNash
      (quittingTerminalPayoff table.zeroNeverReward) ε profile := by
    exact (table.isεAsymptoticNash_iff ε profile).1 hnash
  have hneverNash := hzeroNash who
    (quittingPureTimeBehaviorStrategy table.zeroNeverReward who none)
  have hneverEq :=
    quittingTerminalPayoff_update_pureTimeBehaviorStrategy
      table.zeroNeverReward profile who none
  have hneverBound : neverValue ≤
      quittingTerminalPayoff table.zeroNeverReward profile who + ε := by
    dsimp only [neverValue, roots]
    rw [← hneverEq]
    exact hneverNash
  have hneverTendsto : Tendsto (fun horizon =>
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (quittingPureTimeHazard none) 0 horizon) atTop (nhds neverValue) := by
    simpa [neverValue, roots, quittingRootSequencePureTimeTerminalValue,
      quittingRootSequenceHazardTerminalValue,
      quittingRootSequenceTerminalValue] using
      (tendsto_quittingFiniteRootPayoff_terminal table.zeroNeverReward roots
        who (quittingPureTimeHazard none) 0)
  obtain ⟨threshold, hafter⟩ :=
    (Metric.tendsto_atTop.mp hneverTendsto) δ hδ
  filter_upwards [eventually_ge_atTop threshold] with horizon hhorizon
  have hclose := hafter horizon hhorizon
  rw [Real.dist_eq] at hclose
  obtain ⟨choice, hchoice⟩ :=
    exists_quittingFinitePureTimePayoff_eq_finiteStoppingBest
      table.zeroNeverReward roots who 0 horizon
  revert hchoice
  refine Fin.lastCases ?_ (fun earlier => ?_) choice
  · intro hchoice
    rw [quittingFinitePureTimePayoff_last_eq_neverFinite] at hchoice
    dsimp only [roots] at hneverNash hchoice ⊢
    dsimp only [neverValue, roots] at hclose
    dsimp only [neverValue, roots] at hneverBound
    linarith [(abs_lt.mp hclose).2]
  · intro hchoice
    rw [quittingFinitePureTimePayoff_castSucc_eq_terminal] at hchoice
    simp only [Nat.zero_add] at hchoice
    have hpure := hzeroNash who
      (quittingPureTimeBehaviorStrategy table.zeroNeverReward who
        (some earlier.val))
    have hpureEq :=
      quittingTerminalPayoff_update_pureTimeBehaviorStrategy
        table.zeroNeverReward profile who (some earlier.val)
    have hpureBound :
        quittingRootSequencePureTimeTerminalValue table.zeroNeverReward
            (quittingProfileLiveRoot table.zeroNeverReward profile) who
            (some earlier.val) 0 ≤
          quittingTerminalPayoff table.zeroNeverReward profile who + ε := by
      rw [← hpureEq]
      exact hpure
    dsimp only [roots] at hchoice ⊢
    linarith [hpureBound]

/-! Exact finite value of the prescribed root sequence on the doubled public
clock. This differs from `rootSequenceFinitePotential`, which is the
best-response Bellman envelope. -/
private def rootSequencePrescribedFinitePotential (table : Table ι)
    (roots : ℕ → ι → PMF Bool) (who : ι) (horizon : ℕ) :
    (publicQuittingGame table trivialSignalLaw).HistoryPotential :=
  fun time history => match history.2 with
    | .draw => quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) ((time + 1) / 2)
          (horizon - (time + 1) / 2)
    | .active _ => quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) (time / 2) (horizon - time / 2)
    | .absorbed quitters => table.zeroNeverReward quitters who

@[simp] private theorem rootSequencePrescribedFinitePotential_initial
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) :
    rootSequencePrescribedFinitePotential table roots who horizon 0
        ((publicQuittingGame table trivialSignalLaw).emptyHist .draw) =
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) 0 horizon := by
  rfl

@[simp] private theorem rootSequencePrescribedFinitePotential_evenHorizon
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (history : (publicQuittingGame table trivialSignalLaw).Hist
      (2 * horizon)) :
    rootSequencePrescribedFinitePotential table roots who horizon
        (2 * horizon) history =
      match history.2 with
      | .absorbed quitters => table.zeroNeverReward quitters who
      | _ => 0 := by
  rcases history with ⟨past, state⟩
  cases state with
  | draw =>
      change quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) ((2 * horizon + 1) / 2)
          (horizon - (2 * horizon + 1) / 2) = 0
      rw [show (2 * horizon + 1) / 2 = horizon by omega]
      simp [quittingFiniteRootPayoff]
  | active signal =>
      change quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) ((2 * horizon) / 2)
          (horizon - (2 * horizon) / 2) = 0
      simp [quittingFiniteRootPayoff]
  | absorbed quitters => rfl

private theorem rootSequencePrescribedFinitePotential_draw_harmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (strategy : (publicQuittingGame table trivialSignalLaw).BehaviorProfile)
    {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (hstate : history.2 = .draw) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU strategy
        (rootSequencePrescribedFinitePotential table roots who horizon)
        history =
      rootSequencePrescribedFinitePotential table roots who horizon time
        history := by
  unfold StochasticGame.historyContinuationEU
  let activeLaw : PMF
      (publicQuittingGame table trivialSignalLaw).State :=
    trivialSignalLaw.map PublicQuittingState.active
  have htransition (action :
      (publicQuittingGame table trivialSignalLaw).JointAct) :
      (publicQuittingGame table trivialSignalLaw).transition history.2
          action = activeLaw := by
    rw [hstate]
    exact publicQuittingGame_transition_draw table trivialSignalLaw action
  simp_rw [htransition]
  have hinner (action :
      (publicQuittingGame table trivialSignalLaw).JointAct) :
      expect activeLaw (fun nextState =>
          rootSequencePrescribedFinitePotential table roots who horizon
            (time + 1)
            (Fin.snoc history.1 (history.2, action), nextState)) =
        quittingFiniteRootPayoff table.zeroNeverReward roots who
          (fun stage => roots stage who) ((time + 1) / 2)
            (horizon - (time + 1) / 2) := by
    unfold activeLaw
    rw [Math.Probability.expect_map]
    change expect trivialSignalLaw (fun _ =>
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) ((time + 1) / 2)
          (horizon - (time + 1) / 2)) = _
    exact Math.Probability.expect_const _ _
  simp_rw [hinner]
  rw [Math.Probability.expect_const]
  simp only [rootSequencePrescribedFinitePotential, hstate]

private theorem rootSequencePrescribedFinitePotential_absorbed_harmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ)
    (strategy : (publicQuittingGame table trivialSignalLaw).BehaviorProfile)
    {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (quitters : {S : Finset ι // S.Nonempty})
    (hstate : history.2 = .absorbed quitters) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU strategy
        (rootSequencePrescribedFinitePotential table roots who horizon)
        history =
      rootSequencePrescribedFinitePotential table roots who horizon time
        history := by
  rcases history with ⟨past, state⟩
  cases state with
  | draw => simp at hstate
  | active signal => simp at hstate
  | absorbed terminal =>
      have heq : terminal = quitters := by injection hstate
      subst terminal
      unfold StochasticGame.historyContinuationEU
      simp only [publicQuittingGame_transition_absorbed,
        Math.Probability.expect_pure,
        rootSequencePrescribedFinitePotential]
      exact Math.Probability.expect_const _ _

private theorem rootSequencePrescribedFinitePotential_active_harmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (signal : Fin 1) (hstate : history.2 = .active signal)
    (htime : time < 2 * horizon) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU
        (rootSequenceSunspotStrategy table roots)
        (rootSequencePrescribedFinitePotential table roots who horizon)
        history =
      rootSequencePrescribedFinitePotential table roots who horizon time
        history := by
  classical
  have hactionDist :
      (publicQuittingGame table trivialSignalLaw).stageActionDist
          (rootSequenceSunspotStrategy table roots) history =
        Math.PMFProduct.pmfPi (roots (time / 2)) := by
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    simp only [rootSequenceSunspotStrategy, rootSequencePublicStrategy,
      hstate]
  have hround : time / 2 < horizon := by omega
  have hfuel : horizon - time / 2 =
      horizon - (time / 2 + 1) + 1 := by omega
  rw [show rootSequencePrescribedFinitePotential table roots who horizon
      time history = quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) (time / 2)
          (horizon - time / 2) by
    simp only [rootSequencePrescribedFinitePotential, hstate]]
  rw [hfuel, quittingFiniteRootPayoff]
  rw [Function.update_eq_self]
  unfold StochasticGame.historyContinuationEU
  rw [hactionDist]
  unfold quittingRootExpectedPayoff
  apply congrArg (expect (Math.PMFProduct.pmfPi (roots (time / 2))))
  funext jointAction
  by_cases hquit : (quittingQuitters jointAction).Nonempty
  · rw [show (publicQuittingGame table
        trivialSignalLaw).transition history.2 jointAction =
        PMF.pure (.absorbed ⟨quittingQuitters jointAction, hquit⟩) by
      rw [hstate, publicQuittingGame_transition_active, dif_pos hquit]]
    rw [Math.Probability.expect_pure]
    simp only [rootSequencePrescribedFinitePotential, quittingRootPayoff,
      dif_pos hquit]
  · rw [show (publicQuittingGame table
        trivialSignalLaw).transition history.2 jointAction = PMF.pure .draw by
      rw [hstate, publicQuittingGame_transition_active, dif_neg hquit]]
    rw [Math.Probability.expect_pure]
    simp only [rootSequencePrescribedFinitePotential, quittingRootPayoff,
      dif_neg hquit]
    rw [show (time + 1 + 1) / 2 = time / 2 + 1 by omega]

private theorem rootSequencePrescribedFinitePotential_harmonic
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) {time : ℕ}
    (history : (publicQuittingGame table trivialSignalLaw).Hist time)
    (htime : time < 2 * horizon) :
    (publicQuittingGame table trivialSignalLaw).historyContinuationEU
        (rootSequenceSunspotStrategy table roots)
        (rootSequencePrescribedFinitePotential table roots who horizon)
        history =
      rootSequencePrescribedFinitePotential table roots who horizon time
        history := by
  cases hstate : history.2 with
  | draw =>
      exact rootSequencePrescribedFinitePotential_draw_harmonic table roots
        who horizon _ history hstate
  | active signal =>
      exact rootSequencePrescribedFinitePotential_active_harmonic table roots
        who horizon history signal hstate htime
  | absorbed quitters =>
      exact rootSequencePrescribedFinitePotential_absorbed_harmonic table
        roots who horizon _ history quitters hstate

private theorem expect_rootSequencePrescribedFinitePotential_eq_initial
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) :
    ∀ time, time ≤ 2 * horizon →
      expect ((publicQuittingGame table trivialSignalLaw).histDist
        (rootSequenceSunspotStrategy table roots) .draw time)
        (rootSequencePrescribedFinitePotential table roots who horizon time) =
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) 0 horizon := by
  intro time htime
  induction time with
  | zero =>
      rw [(publicQuittingGame table trivialSignalLaw).histDist_zero,
        Math.Probability.expect_pure,
        rootSequencePrescribedFinitePotential_initial]
  | succ time ih =>
      change (publicQuittingGame table
        trivialSignalLaw).expectedHistoryValue
          (rootSequenceSunspotStrategy table roots) .draw
          (rootSequencePrescribedFinitePotential table roots who horizon)
          (time + 1) = _
      rw [(publicQuittingGame table
        trivialSignalLaw).expectedHistoryValue_succ]
      have htimeLt : time < 2 * horizon := by omega
      have hharmonic (history : (publicQuittingGame table
          trivialSignalLaw).Hist time) :=
        rootSequencePrescribedFinitePotential_harmonic table roots who
          horizon history htimeLt
      simp_rw [hharmonic]
      exact ih (by omega)

private theorem publicZeroNeverFinitePayoff_rootSequence_eq
    (table : Table ι) (roots : ℕ → ι → PMF Bool) (who : ι)
    (horizon : ℕ) :
    publicZeroNeverFinitePayoff table trivialSignalLaw
        (rootSequenceSunspotStrategy table roots) (2 * horizon) who =
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) 0 horizon := by
  let distribution := (publicQuittingGame table trivialSignalLaw).histDist
    (rootSequenceSunspotStrategy table roots) .draw (2 * horizon)
  let observable : (publicQuittingGame table
      trivialSignalLaw).Hist (2 * horizon) → ℝ :=
    fun history => publicZeroNeverStateValue table trivialSignalLaw who
      history.2
  let potential : (publicQuittingGame table
      trivialSignalLaw).Hist (2 * horizon) → ℝ :=
    rootSequencePrescribedFinitePotential table roots who horizon
      (2 * horizon)
  have heq : observable = potential := by
    funext history
    change publicZeroNeverStateValue table trivialSignalLaw who history.2 =
      rootSequencePrescribedFinitePotential table roots who horizon
        (2 * horizon) history
    rw [rootSequencePrescribedFinitePotential_evenHorizon table roots who
      horizon history]
    cases history.2 <;> rfl
  have hvalue : expect distribution potential =
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) 0 horizon := by
    exact expect_rootSequencePrescribedFinitePotential_eq_initial table roots
      who horizon (2 * horizon) le_rfl
  have hobservable : expect distribution observable =
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) 0 horizon := by
    rw [heq]
    exact hvalue
  simpa only [publicZeroNeverFinitePayoff, distribution, observable] using
    hobservable

private theorem publicQuittingPayoff_rootSequenceProfile_eq
    (table : Table ι)
    (profile : (quittingGame table.terminal).BehaviorProfile)
    (who : ι) :
    publicQuittingPayoff table trivialSignalLaw
        (rootSequenceSunspotStrategy table
          (quittingProfileLiveRoot table.zeroNeverReward profile)) who =
      table.terminalPayoff profile who := by
  let roots := quittingProfileLiveRoot table.zeroNeverReward profile
  let strategy := rootSequenceSunspotStrategy table roots
  have heven : Tendsto (fun horizon : ℕ => 2 * horizon) atTop atTop := by
    apply tendsto_atTop.2
    intro threshold
    filter_upwards [eventually_ge_atTop threshold] with horizon hhorizon
    omega
  have hpublicFull : Tendsto (fun horizon =>
      publicFinitePayoff table trivialSignalLaw strategy (2 * horizon) who)
      atTop (nhds (publicQuittingPayoff table trivialSignalLaw strategy who)) :=
    (tendsto_publicFinitePayoff table trivialSignalLaw strategy who).comp heven
  have hpublicZero : Tendsto (fun horizon =>
      publicZeroNeverFinitePayoff table trivialSignalLaw strategy
        (2 * horizon) who) atTop
      (nhds (publicQuittingPayoff table trivialSignalLaw strategy who -
        table.never who)) := by
    apply (hpublicFull.sub_const (table.never who)).congr'
    exact Filter.Eventually.of_forall fun horizon =>
      (publicZeroNeverFinitePayoff_eq_sub table trivialSignalLaw strategy
        (2 * horizon) who).symm
  have hroot : Tendsto (fun horizon =>
      quittingFiniteRootPayoff table.zeroNeverReward roots who
        (fun stage => roots stage who) 0 horizon) atTop
      (nhds (quittingRootSequenceTerminalValue table.zeroNeverReward roots
        who 0)) :=
    tendsto_quittingFiniteRootPayoff_self_terminalValue
      table.zeroNeverReward roots who 0
  have hrootAsPublic : Tendsto (fun horizon =>
      publicZeroNeverFinitePayoff table trivialSignalLaw strategy
        (2 * horizon) who) atTop
      (nhds (quittingRootSequenceTerminalValue table.zeroNeverReward roots
        who 0)) := by
    apply hroot.congr'
    exact Filter.Eventually.of_forall fun horizon => by
      exact (publicZeroNeverFinitePayoff_rootSequence_eq table roots who
        horizon).symm
  have hlimit := tendsto_nhds_unique hpublicZero hrootAsPublic
  have hordinary := quittingTerminalPayoff_eq_rootSequence_profileLiveRoot
    table.zeroNeverReward profile who
  rw [table.terminalPayoff_eq_add_never]
  dsimp only [strategy, roots] at hlimit ⊢
  rw [hordinary]
  linarith

private theorem tendsto_publicZeroNeverFinitePayoff_even
    {signalCount : ℕ} (table : Table ι)
    (signalLaw : PMF (Fin (signalCount + 1)))
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (who : ι) :
    Tendsto (fun horizon => publicZeroNeverFinitePayoff table signalLaw
        strategy (2 * horizon) who) atTop
      (nhds (publicQuittingPayoff table signalLaw strategy who -
        table.never who)) := by
  have heven : Tendsto (fun horizon : ℕ => 2 * horizon) atTop atTop := by
    apply tendsto_atTop.2
    intro threshold
    filter_upwards [eventually_ge_atTop threshold] with horizon hhorizon
    omega
  have hfull :=
    (tendsto_publicFinitePayoff table signalLaw strategy who).comp heven
  apply (hfull.sub_const (table.never who)).congr'
  exact Filter.Eventually.of_forall fun horizon =>
    (publicZeroNeverFinitePayoff_eq_sub table signalLaw strategy
      (2 * horizon) who).symm

private theorem publicQuittingPayoff_deviation_le_of_epsilonEquilibrium
    (table : Table ι) {ε : ℝ}
    (profile : (quittingGame table.terminal).BehaviorProfile)
    (hnash : EpsilonEquilibrium table ε profile) (who : ι)
    (deviation : (publicQuittingGame table
      trivialSignalLaw).BehaviorStrategy who) :
    publicQuittingPayoff table trivialSignalLaw
        (Function.update (rootSequenceSunspotStrategy table
          (quittingProfileLiveRoot table.zeroNeverReward profile)) who
          deviation) who ≤
      table.terminalPayoff profile who + ε := by
  let roots := quittingProfileLiveRoot table.zeroNeverReward profile
  let strategy := rootSequenceSunspotStrategy table roots
  let deviated := Function.update strategy who deviation
  apply le_of_forall_pos_le_add
  intro δ hδ
  have hbestEventually :=
    eventually_finiteStoppingBest_le_of_epsilonEquilibrium table profile
      hnash who hδ
  have hfiniteEventually : ∀ᶠ horizon in atTop,
      publicZeroNeverFinitePayoff table trivialSignalLaw deviated
          (2 * horizon) who ≤
        quittingTerminalPayoff table.zeroNeverReward profile who + ε + δ := by
    filter_upwards [hbestEventually] with horizon hbest
    exact (publicZeroNeverFinitePayoff_deviation_le_best table roots who
      deviation horizon).trans hbest
  have hzeroBound :
      publicQuittingPayoff table trivialSignalLaw deviated who -
          table.never who ≤
        quittingTerminalPayoff table.zeroNeverReward profile who + ε + δ := by
    apply le_of_tendsto
      (tendsto_publicZeroNeverFinitePayoff_even table trivialSignalLaw
        deviated who)
    exact hfiniteEventually
  rw [table.terminalPayoff_eq_add_never]
  dsimp only [deviated, strategy, roots] at hzeroBound ⊢
  linarith

/-! An ordinary ε-equilibrium remains an ε-equilibrium after inserting
the action-irrelevant public draw state before every quitting decision. -/
private theorem rootSequenceSunspotProfile_isEpsilonEquilibrium
    (table : Table ι) {ε : ℝ}
    (profile : (quittingGame table.terminal).BehaviorProfile)
    (hnash : EpsilonEquilibrium table ε profile) :
    SunspotEpsilonEquilibrium table ε
      (rootSequenceSunspotProfile table
        (quittingProfileLiveRoot table.zeroNeverReward profile)) := by
  intro who deviation
  have hdeviation :=
    publicQuittingPayoff_deviation_le_of_epsilonEquilibrium table profile
      hnash who deviation
  have hprofile := publicQuittingPayoff_rootSequenceProfile_eq table profile who
  dsimp only [rootSequenceSunspotProfile, SunspotProfile.payoff]
  rw [hprofile]
  linarith

/-! A deviating normal player can only bring the final tail closer by
quitting.  Thus the forced-Continue final-tail potential is superharmonic
under every behavioral deviation by that player. -/
theorem KiloblockConstruction.continueFinalHistoryPotential_superharmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1 deviation)
        (construction.continueFinalHistoryPotential excluded) history ≤
      construction.continueFinalHistoryPotential excluded t history := by
  rw [historyContinuationEU_update_eq_expect_pure table
    construction.profile.signalLaw construction.profile.strategy excluded.1
      deviation (construction.continueFinalHistoryPotential excluded) history]
  calc
    expect (deviation t history) (fun action =>
        (publicQuittingGame table
          construction.profile.signalLaw).historyContinuationEU
            (Function.update construction.profile.strategy excluded.1
              (fun _ _ => PMF.pure action))
            (construction.continueFinalHistoryPotential excluded) history) ≤
        expect (deviation t history) (fun _ =>
          construction.continueFinalHistoryPotential excluded t history) := by
      apply Math.Probability.expect_mono
      intro action
      cases action with
      | false =>
          simpa only [KiloblockConstruction.continueProfile] using
            (construction.continueFinalHistoryPotential_harmonic
              excluded history).le
      | true =>
          exact (construction.continueFinal_pureQuit_le_continue
            excluded history).trans_eq
              (construction.continueFinalHistoryPotential_harmonic
                excluded history)
    _ = construction.continueFinalHistoryPotential excluded t history := by
      exact Math.Probability.expect_const _ _

theorem KiloblockConstruction.expect_continueFinalHistoryPotential_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1) : ∀ t,
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded.1 deviation)
        .draw t)
      (construction.continueFinalHistoryPotential excluded t) ≤
        construction.continueMacroSurvivalProbability excluded
  | 0 => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
          Math.Probability.expect_pure]
      unfold KiloblockConstruction.continueFinalHistoryPotential
      rw [construction.mode_initial]
      exact (construction.continueFinalPotential_initial excluded).le
  | t + 1 => by
      change (publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue
          (Function.update construction.profile.strategy excluded.1 deviation)
          .draw (construction.continueFinalHistoryPotential excluded) (t + 1) ≤ _
      rw [(publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue_succ]
      calc
        expect ((publicQuittingGame table
            construction.profile.signalLaw).histDist
          (Function.update construction.profile.strategy excluded.1 deviation)
            .draw t)
          (fun history => (publicQuittingGame table
            construction.profile.signalLaw).historyContinuationEU
              (Function.update construction.profile.strategy excluded.1 deviation)
              (construction.continueFinalHistoryPotential excluded) history) ≤
            expect ((publicQuittingGame table
                construction.profile.signalLaw).histDist
              (Function.update construction.profile.strategy excluded.1 deviation)
                .draw t)
              (construction.continueFinalHistoryPotential excluded t) := by
          apply Math.Probability.expect_mono
          exact fun history =>
            construction.continueFinalHistoryPotential_superharmonic
              excluded deviation history
        _ ≤ construction.continueMacroSurvivalProbability excluded :=
          construction.expect_continueFinalHistoryPotential_le
            excluded deviation t

theorem KiloblockConstruction.expect_deviationPastFinal_lt_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded.1 deviation)
        .draw t)
      (fun history => construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history))) < ε := by
  calc
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded.1 deviation)
        .draw t)
      (fun history => construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history))) ≤
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        (Function.update construction.profile.strategy excluded.1 deviation)
          .draw t)
        (construction.continueFinalHistoryPotential excluded t) := by
        apply Math.Probability.expect_mono
        intro history
        exact construction.continuePastFinalIndicator_le_finalPotential
          excluded (construction.finiteMode (construction.mode t history))
    _ ≤ construction.continueMacroSurvivalProbability excluded :=
      construction.expect_continueFinalHistoryPotential_le
        excluded deviation t
    _ < ε := construction.continueMacroSurvivalProbability_lt_epsilon excluded

/-! A selected type is supported when its public-lottery atom has positive
mass.  This condition matters only while that type is being executed. -/
def KiloblockConstruction.SelectedChoiceSupported
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockMode table construction.blockCount → Prop
  | .draw (.resume k (some owner) _) =>
      0 < (construction.buildingBlock k).z.singleton owner
  | .active k (some owner) _ =>
      0 < (construction.buildingBlock k).z.singleton owner
  | _ => True

private theorem KiloblockConstruction.selector_weight_pos_of_mem_support
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (signal : Fin (construction.profile.signalCount + 1))
    (hsignal : signal ∈ construction.profile.signalLaw.support) :
    match construction.signalSelector k signal with
    | none => 0 < (construction.buildingBlock k).z.cemetery
    | some owner =>
        0 < (construction.buildingBlock k).z.singleton owner := by
  let choice := construction.signalSelector k signal
  have hchoice : choice ∈
      (construction.profile.signalLaw.map
        (construction.signalSelector k)).support := by
    rw [PMF.mem_support_map_iff]
    exact ⟨signal, hsignal, rfl⟩
  rw [construction.signalSelector_law k] at hchoice
  have hne := (PMF.mem_support_iff _ _).mp hchoice
  have hpos := ENNReal.toReal_pos hne
    (PMF.apply_ne_top (construction.buildingBlock k).z.toPMF choice)
  cases hchoiceEq : choice with
  | none =>
      simpa only [choice, hchoiceEq,
        SimplexWeights.toPMF_none_toReal] using hpos
  | some owner =>
      simpa only [choice, hchoiceEq,
        SimplexWeights.toPMF_some_toReal] using hpos

/-! Public signals remain supported after any unilateral behavioral
deviation, so every selected owner reached by the construction has positive
lottery weight. -/
theorem KiloblockConstruction.selectedChoiceSupported_of_mem_support
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : ι)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy who) :
    ∀ t (history : (publicQuittingGame table
        construction.profile.signalLaw).Hist t),
      history ∈ ((publicQuittingGame table
        construction.profile.signalLaw).histDist
          (Function.update construction.profile.strategy who deviation)
          .draw t).support →
        construction.SelectedChoiceSupported
          (construction.mode t history)
  | 0, history, hhistory => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
          PMF.mem_support_pure_iff] at hhistory
      subst history
      rw [construction.mode_initial]
      trivial
  | t + 1, nextHistory, hnextHistory => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).mem_support_histDist_succ]
        at hnextHistory
      obtain ⟨history, hhistory, action, _haction, nextState,
        hnextState, rfl⟩ := hnextHistory
      have hcurrent := construction.selectedChoiceSupported_of_mem_support
        who deviation t history hhistory
      have hstep := construction.mode_step t history action nextState hnextState
      cases hmode : construction.mode t history with
      | draw phase =>
          cases phase with
          | choose k =>
              have hstate : history.2 = PublicQuittingState.draw := by
                have hmatches := construction.mode_state t history
                cases hs : history.2 <;>
                  simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
              rw [hstate] at hnextState hstep ⊢
              cases nextState with
              | draw =>
                  simp [publicQuittingGame] at hnextState
                  obtain ⟨signal, _, hfalse⟩ := hnextState
                  cases hfalse
              | absorbed quitters =>
                  simp [publicQuittingGame] at hnextState
                  obtain ⟨signal, _, hfalse⟩ := hnextState
                  cases hfalse
              | active signal =>
                  have hsignal : signal ∈
                      construction.profile.signalLaw.support := by
                    change PublicQuittingState.active signal ∈
                      (construction.profile.signalLaw.map
                        PublicQuittingState.active).support at hnextState
                    rw [PMF.mem_support_map_iff] at hnextState
                    obtain ⟨other, hother, heq⟩ := hnextState
                    cases PublicQuittingState.active.inj heq
                    exact hother
                  have hnextMode : construction.mode (t + 1)
                      (Fin.snoc history.1
                        (PublicQuittingState.draw, action),
                          PublicQuittingState.active signal) =
                        .active k (construction.signalSelector k signal)
                          (construction.mesh k) := by
                    simpa [KiloblockModeStep, hmode] using hstep
                  rw [hnextMode]
                  cases hchoice : construction.signalSelector k signal with
                  | none => trivial
                  | some owner =>
                      simpa only [KiloblockConstruction.SelectedChoiceSupported,
                        hchoice] using
                          construction.selector_weight_pos_of_mem_support
                            k signal hsignal
          | resume k choice remaining =>
              cases nextState <;>
                simp [KiloblockModeStep, hmode] at hstep
              rw [hstep]
              cases choice <;>
                simp [KiloblockConstruction.SelectedChoiceSupported,
                  hmode] at hcurrent ⊢
              exact hcurrent
          | final =>
              cases nextState <;>
                simp [KiloblockModeStep, hmode] at hstep
              rw [hstep]
              trivial
      | active k choice remaining =>
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep
          · rw [hstep.2]
            trivial
          · split at hstep
            · rw [hstep.2]
              cases choice with
              | none =>
                  by_cases hk : k.1 = 0 <;>
                    simp [KiloblockConstruction.SelectedChoiceSupported,
                      phaseAfterAttempt, precedingKiloblockPhase, hk]
              | some owner =>
                  cases hcontinuation :
                      (construction.attempt k owner).continuation with
                  | restart =>
                      simp [KiloblockConstruction.SelectedChoiceSupported,
                        phaseAfterAttempt, hcontinuation]
                  | advance =>
                      by_cases hk : k.1 = 0 <;>
                        simp [KiloblockConstruction.SelectedChoiceSupported,
                          phaseAfterAttempt, precedingKiloblockPhase,
                          hcontinuation, hk]
            · rw [hstep.2]
              cases choice <;>
                simp [KiloblockConstruction.SelectedChoiceSupported,
                  hmode] at hcurrent ⊢
              exact hcurrent
      | finalActive =>
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep <;> rw [hstep.2] <;> trivial
      | absorbed origin =>
          cases hsnext : nextState <;>
            simp [KiloblockModeStep, hmode, hsnext] at hstep
          rw [hstep]
          trivial

theorem KiloblockConstruction.selectedValue_lower
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hremaining : remaining ≤ construction.mesh k)
    (who : NormalPlayer table) :
    -ε ≤ construction.selectedValue k choice remaining who := by
  cases choice with
  | none =>
      exact (neg_nonpos.mpr construction.epsilon_pos.le).trans
        ((construction.point_boundary k).1.2 who)
  | some owner =>
      let hazard := quittingMeshHazard
        (construction.attempt k owner).quitWeight (construction.mesh k)
      let survival : ℕ → ℝ := fun stages => (1 - hazard) ^ stages
      let endpoint := match (construction.attempt k owner).continuation with
        | .restart => (construction.buildingBlock k).w who
        | .advance => construction.point k who
      let terminal := NormalMatrix table who owner
      have hhazard0 : 0 ≤ hazard := quittingMeshHazard_nonneg _
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one.le
      have hhazard1 : hazard ≤ 1 := quittingMeshHazard_le_one _
        (construction.attempt k owner).quitWeight_lt_one.le
      have hbase0 : 0 ≤ 1 - hazard := sub_nonneg.mpr hhazard1
      have hbase1 : 1 - hazard ≤ 1 := by linarith
      have hsurvival0 (stages : ℕ) : 0 ≤ survival stages := by
        exact pow_nonneg hbase0 _
      have hsurvival1 (stages : ℕ) : survival stages ≤ 1 := by
        exact pow_le_one₀ hbase0 hbase1
      have hsurvivalOrder :
          survival (construction.mesh k) ≤ survival remaining := by
        exact pow_le_pow_of_le_one hbase0 hbase1 hremaining
      have hendpoint0 : 0 ≤ endpoint := by
        cases hcontinuation : (construction.attempt k owner).continuation with
        | restart =>
            dsimp only [endpoint]
            rw [hcontinuation]
            exact (construction.buildingBlock k).w_boundary.1.2 who
        | advance =>
            dsimp only [endpoint]
            rw [hcontinuation]
            exact (construction.point_boundary k).1.2 who
      have hfull : -ε ≤
          survival (construction.mesh k) * endpoint +
            (1 - survival (construction.mesh k)) * terminal := by
        rw [show survival (construction.mesh k) * endpoint +
            (1 - survival (construction.mesh k)) * terminal =
              (construction.attempt k owner).remainingValue
                (construction.mesh k) (construction.mesh k) who by
          rfl]
        rw [(construction.attempt k owner).remainingValue_mesh
          (construction.mesh k) (construction.mesh_survival k owner) who]
        exact (construction.buildingBlock k).lower owner who
      change -ε ≤ survival remaining * endpoint +
        (1 - survival remaining) * terminal
      by_cases hterminal : 0 ≤ terminal
      · have hleft := mul_nonneg (hsurvival0 remaining) hendpoint0
        have hright := mul_nonneg
          (sub_nonneg.mpr (hsurvival1 remaining)) hterminal
        linarith [construction.epsilon_pos]
      · have hterminalEndpoint : terminal ≤ endpoint := by linarith
        nlinarith

theorem KiloblockConstruction.selectedValue_self_eq_zero
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (owner : NormalPlayer table) (remaining : ℕ)
    (hweight : 0 < (construction.buildingBlock k).z.singleton owner) :
    construction.selectedValue k (some owner) remaining owner = 0 := by
  let endpoint := match (construction.attempt k owner).continuation with
    | .restart => (construction.buildingBlock k).w owner
    | .advance => construction.point k owner
  have hdiagonal : NormalMatrix table owner owner = 0 := by
    exact construction.soloExitNormalized owner.1
  have hfull := (construction.buildingBlock k).complementary owner hweight
  rw [← (construction.attempt k owner).remainingValue_mesh
    (construction.mesh k) (construction.mesh_survival k owner) owner] at hfull
  have hendpoint : endpoint = 0 := by
    unfold BuildingAttempt.remainingValue at hfull
    rw [construction.mesh_survival k owner, hdiagonal] at hfull
    simp only [mul_zero, add_zero] at hfull
    have hsurvival : 0 < 1 - (construction.attempt k owner).quitWeight :=
      sub_pos.mpr (construction.attempt k owner).quitWeight_lt_one
    dsimp only [endpoint]
    exact (mul_eq_zero.mp hfull).resolve_left (ne_of_gt hsurvival)
  change (construction.attempt k owner).remainingValue
    (construction.mesh k) remaining owner = 0
  unfold BuildingAttempt.remainingValue
  change (1 - quittingMeshHazard
      (construction.attempt k owner).quitWeight
        (construction.mesh k)) ^ remaining * endpoint +
      (1 - (1 - quittingMeshHazard
        (construction.attempt k owner).quitWeight
          (construction.mesh k)) ^ remaining) *
        NormalMatrix table owner owner = 0
  rw [hdiagonal]
  rw [hendpoint]
  ring

private theorem KiloblockConstruction.stageActionDist_pureQuit_no_other
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hmode : ∀ k owner remaining,
      construction.mode t history = .active k (some owner) remaining →
        owner = excluded) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true)) history =
      PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded.1 true) := by
  unfold StochasticGame.stageActionDist
  rw [show (fun who => Function.update construction.profile.strategy excluded.1
      (fun _ _ => PMF.pure true) who t history) =
      fun who => PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded.1 true who) by
    funext who
    by_cases hwho : who = excluded.1
    · subst who
      simp [publicSingleQuitAction]
    · rw [Function.update_of_ne hwho, construction.strategy_eq]
      cases hm : construction.mode t history with
      | draw phase => simp [publicSingleQuitAction, hwho]
      | finalActive => simp [publicSingleQuitAction, hwho]
      | absorbed origin => simp [publicSingleQuitAction, hwho]
      | active k choice remaining =>
          cases choice with
          | none => simp [publicSingleQuitAction, hwho]
          | some owner =>
              have howner := hmode k owner remaining hm
              subst owner
              simp [hwho, publicSingleQuitAction]]
  exact Math.PMFProduct.pmfPi_pure _

private theorem KiloblockConstruction.stageActionDist_pureQuit_selected_other
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded owner : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (howner : owner ≠ excluded)
    (hmode : construction.mode t history =
      .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true)) history =
      (quittingMeshHazardCoin (construction.attempt k owner).quitWeight
        (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one).bind
          (fun quits => PMF.pure (Function.update
            (publicSingleQuitAction table construction.profile.signalLaw
              owner.1 quits) excluded.1 true)) := by
  classical
  let coin := quittingMeshHazardCoin
    (construction.attempt k owner).quitWeight (construction.mesh k)
    (construction.attempt k owner).quitWeight_pos.le
    (construction.attempt k owner).quitWeight_lt_one
  let base : ∀ who,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act who) :=
    fun who => if who = excluded.1 then PMF.pure true else PMF.pure false
  have hfamily :
      (fun who => Function.update construction.profile.strategy excluded.1
        (fun _ _ => PMF.pure true) who t history) =
        Function.update base owner.1 coin := by
    funext who
    by_cases hwhoExcluded : who = excluded.1
    · subst who
      have hne : excluded.1 ≠ owner.1 := fun heq =>
        howner (Subtype.ext heq.symm)
      rw [Function.update_self, Function.update_of_ne hne]
      simp [base]
    · rw [Function.update_of_ne hwhoExcluded]
      by_cases hwhoOwner : who = owner.1
      · subst who
        simp [construction.strategy_eq, hmode, coin, base]
      · rw [Function.update_of_ne hwhoOwner, construction.strategy_eq]
        simp [hmode, hwhoOwner, base, hwhoExcluded]
  unfold StochasticGame.stageActionDist
  rw [hfamily, Math.PMFProduct.pmfPi_update_bind]
  apply congrArg (PMF.bind coin)
  funext quits
  rw [show Function.update base owner.1 (PMF.pure quits) =
      fun who => PMF.pure (Function.update
        (publicSingleQuitAction table construction.profile.signalLaw
          owner.1 quits) excluded.1 true who) by
    funext who
    by_cases hwhoOwner : who = owner.1
    · subst who
      have hne : owner.1 ≠ excluded.1 := fun heq =>
        howner (Subtype.ext heq)
      simp [base, publicSingleQuitAction, hne]
    · rw [Function.update_of_ne hwhoOwner]
      by_cases hwhoExcluded : who = excluded.1
      · subst who
        simp [base]
      · simp [base, hwhoExcluded, publicSingleQuitAction, hwhoOwner]]
  exact Math.PMFProduct.pmfPi_pure _

private theorem KiloblockConstruction.pureQuit_nextTerminalPayoff_le_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true))
        (fun _ nextHistory => match nextHistory.2 with
          | .absorbed quitters => table.terminal quitters excluded.1
          | _ => 0) history ≤ ε := by
  classical
  have hstate : ∃ signal, history.2 = PublicQuittingState.active signal := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨signal, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  cases choice with
  | none =>
      have haction := construction.stageActionDist_pureQuit_no_other
        excluded (past, PublicQuittingState.active signal) (by
          intro block owner tail hselected
          simp [hmode] at hselected)
      unfold StochasticGame.historyContinuationEU
      rw [haction, Math.Probability.expect_pure,
        publicQuittingGame_transition_publicSingleQuitAction,
        Math.Probability.expect_pure]
      change table.terminal
        (quittingProjectiveSingletonTerminal excluded.1) excluded.1 ≤ ε
      rw [construction.soloExitNormalized excluded.1]
      exact construction.epsilon_pos.le
  | some owner =>
      by_cases howner : owner = excluded
      · subst owner
        have haction := construction.stageActionDist_pureQuit_no_other
          excluded (past, PublicQuittingState.active signal) (by
            intro block owner tail hselected
            have heq : k = block ∧ excluded = owner ∧
                remaining = tail := by
              simpa [hmode] using hselected
            exact heq.2.1.symm)
        unfold StochasticGame.historyContinuationEU
        rw [haction, Math.Probability.expect_pure,
          publicQuittingGame_transition_publicSingleQuitAction,
          Math.Probability.expect_pure]
        change table.terminal
          (quittingProjectiveSingletonTerminal excluded.1) excluded.1 ≤ ε
        rw [construction.soloExitNormalized excluded.1]
        exact construction.epsilon_pos.le
      · have haction :=
          construction.stageActionDist_pureQuit_selected_other
            excluded owner (past, PublicQuittingState.active signal)
              k remaining howner hmode
        unfold StochasticGame.historyContinuationEU
        rw [haction, Math.Probability.expect_bind]
        simp_rw [Math.Probability.expect_pure]
        let coin := quittingMeshHazardCoin
          (construction.attempt k owner).quitWeight (construction.mesh k)
          (construction.attempt k owner).quitWeight_pos.le
          (construction.attempt k owner).quitWeight_lt_one
        change expect coin (fun quits => expect
            ((publicQuittingGame table
              construction.profile.signalLaw).transition
                (PublicQuittingState.active signal)
                (Function.update (publicSingleQuitAction table
                  construction.profile.signalLaw owner.1 quits)
                  excluded.1 true))
            (fun nextState => match nextState with
              | .absorbed quitters => table.terminal quitters excluded.1
              | _ => 0)) ≤ ε
        calc
          expect coin (fun quits => expect
              ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  (PublicQuittingState.active signal)
                  (Function.update (publicSingleQuitAction table
                    construction.profile.signalLaw owner.1 quits)
                    excluded.1 true))
              (fun nextState => match nextState with
                | .absorbed quitters => table.terminal quitters excluded.1
                | _ => 0)) ≤
              expect coin (fun quits => if quits then 1 else 0) := by
            apply Math.Probability.expect_mono
            intro quits
            cases quits with
            | false =>
                have hactionEq : Function.update
                    (publicSingleQuitAction table
                      construction.profile.signalLaw owner.1 false)
                    excluded.1 true =
                    publicSingleQuitAction table
                      construction.profile.signalLaw excluded.1 true := by
                  funext who
                  by_cases hwho : who = excluded.1
                  · subst who
                    simp [publicSingleQuitAction]
                  · simp [publicSingleQuitAction, hwho]
                rw [hactionEq,
                  publicQuittingGame_transition_publicSingleQuitAction,
                  Math.Probability.expect_pure]
                exact construction.soloExitNormalized excluded.1 |>.le
            | true =>
                simp only [publicQuittingGame]
                split
                · rw [Math.Probability.expect_pure]
                  exact (le_abs_self _).trans
                    (construction.payoffsBounded.1 _ excluded.1)
                · rename_i hfalse
                  exfalso
                  apply hfalse
                  refine ⟨excluded.1, ?_⟩
                  simp
          _ = quittingMeshHazard
              (construction.attempt k owner).quitWeight
                (construction.mesh k) := by
            rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
            simp [coin]
          _ ≤ ε := (construction.mesh_hazard_small k owner).le

/-! The stagewise supermartingale from Lemma 3.10.  Before the final tail it
adds `2ε` to the corrected block value; pre-final absorption keeps the real
terminal payoff and its signed tracking correction; the final tail freezes
at the nonnegative endpoint `y⁰`. -/
def KiloblockConstruction.normalDeviationPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => match construction.mode t history with
    | .draw .final => construction.point ⟨0, by omega⟩ who
    | .finalActive => construction.point ⟨0, by omega⟩ who
    | .absorbed none => construction.point ⟨0, by omega⟩ who
    | .absorbed (some _) =>
        construction.normalEndpointPotential who t history
    | _ => construction.normalEndpointPotential who t history + 2 * ε

private theorem historyContinuationEU_congr_next_profile
    {table : Table ι} {Signal : Type} [Fintype Signal]
    (signalLaw : PMF Signal)
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (left right : (publicQuittingGame table signalLaw).HistoryPotential)
    {t : ℕ}
    (history : (publicQuittingGame table signalLaw).Hist t)
    (heq : ∀ action,
      action ∈ ((publicQuittingGame table signalLaw).stageActionDist
        strategy history).support →
      ∀ nextState,
        nextState ∈ ((publicQuittingGame table signalLaw).transition
          history.2 action).support →
        left (t + 1) (Fin.snoc history.1
            (history.2, action), nextState) =
          right (t + 1) (Fin.snoc history.1
            (history.2, action), nextState)) :
    (publicQuittingGame table signalLaw).historyContinuationEU
        strategy left history =
      (publicQuittingGame table signalLaw).historyContinuationEU
        strategy right history := by
  unfold StochasticGame.historyContinuationEU
  apply Math.ProbabilityMassFunction.expect_congr_on_support
  intro action haction
  apply Math.ProbabilityMassFunction.expect_congr_on_support
  exact heq action haction

private theorem expect_le_expect_of_le_on_support
    {A : Type} [Finite A] (distribution : PMF A)
    (left right : A → ℝ)
    (hle : ∀ value, value ∈ distribution.support →
      left value ≤ right value) :
    expect distribution left ≤ expect distribution right := by
  classical
  let repaired := fun value =>
    if distribution value = 0 then right value else left value
  rw [Math.ProbabilityMassFunction.expect_congr_on_support
    distribution left repaired (by
      intro value hvalue
      have hne := (PMF.mem_support_iff _ value).mp hvalue
      simp [repaired, hne])]
  apply Math.Probability.expect_mono
  intro value
  by_cases hzero : distribution value = 0
  · simp [repaired, hzero]
  · simp only [repaired, hzero, if_false]
    apply hle value
    exact (PMF.mem_support_iff _ value).mpr hzero

private theorem KiloblockConstruction.pureQuit_normalDeviationPotential_eq
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true))
        (construction.normalDeviationPotential excluded) history =
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          (Function.update construction.profile.strategy excluded.1
            (fun _ _ => PMF.pure true))
          (fun _ nextHistory => match nextHistory.2 with
            | .absorbed quitters => table.terminal quitters excluded.1
            | _ => 0) history +
        construction.trackingCorrection k excluded := by
  let strategy := Function.update construction.profile.strategy excluded.1
    (fun _ _ => PMF.pure true)
  let terminal : (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
    fun _ nextHistory => match nextHistory.2 with
      | .absorbed quitters => table.terminal quitters excluded.1
      | _ => 0
  let corrected : (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
    fun nextTime nextHistory => terminal nextTime nextHistory +
      construction.trackingCorrection k excluded
  have hcongr : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU strategy
        (construction.normalDeviationPotential excluded) history =
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU strategy
          corrected history := by
    apply historyContinuationEU_congr_next_profile
    intro action haction nextState hnextState
    have hactionQuit : action excluded.1 = true := by
      unfold strategy StochasticGame.stageActionDist at haction
      let root : ∀ player,
          PMF ((publicQuittingGame table
            construction.profile.signalLaw).Act player) :=
        fun player => construction.profile.strategy player t history
      have hfamily :
          (fun player => Function.update construction.profile.strategy
            excluded.1 (fun _ _ => PMF.pure true) player t history) =
            Function.update root excluded.1 (PMF.pure true) := by
        funext player
        by_cases hplayer : player = excluded.1
        · subst player
          simp [root]
        · simp [root, Function.update_of_ne hplayer]
      rw [hfamily] at haction
      by_contra hne
      have hzero : Math.PMFProduct.pmfPi
          (Function.update root excluded.1 (PMF.pure true)) action = 0 := by
        rw [Math.PMFProduct.pmfPi_apply]
        apply Finset.prod_eq_zero (Finset.mem_univ excluded.1)
        rw [Function.update_self, PMF.pure_apply, if_neg hne]
      exact (PMF.mem_support_iff _ action).mp haction hzero
    change (show Bool from action excluded.1) = true at hactionQuit
    have hstep := construction.mode_step t history action nextState hnextState
    simp only [KiloblockModeStep, hmode] at hstep
    split at hstep
    case isTrue =>
      have hnextMode := hstep.2
      unfold KiloblockConstruction.normalDeviationPotential corrected terminal
      rw [hnextMode, KiloblockConstruction.normalEndpointPotential,
        KiloblockConstruction.normalPotential, hnextMode]
      simp [KiloblockConstruction.trackingCorrectionAtOrigin]
    case isFalse hfalse =>
      exfalso
      apply hfalse
      refine ⟨excluded.1, ?_⟩
      simpa only [Finset.mem_filter, Finset.mem_univ, true_and] using
        hactionQuit
  rw [hcongr]
  change (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU strategy
        corrected history =
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU strategy
        terminal history + construction.trackingCorrection k excluded
  unfold corrected
  have hsplit := historyContinuationEU_add_const_mul
    construction.profile.signalLaw strategy terminal (fun _ _ => 1)
      (construction.trackingCorrection k excluded) history
  have hone : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU strategy
        (fun _ _ => 1) history = 1 := by
    unfold StochasticGame.historyContinuationEU
    simp only [Math.Probability.expect_const]
  rw [hone, mul_one] at hsplit
  simpa only [mul_one] using hsplit

private theorem KiloblockConstruction.normalDeviationPotential_pureQuit_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining)
    (hremaining : remaining ≤ construction.mesh k) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true))
        (construction.normalDeviationPotential excluded) history ≤
      construction.normalDeviationPotential excluded t history := by
  rw [construction.pureQuit_normalDeviationPotential_eq
    excluded history k choice remaining hmode]
  have hterminal := construction.pureQuit_nextTerminalPayoff_le_epsilon
    excluded history k choice remaining hmode
  have hselected := construction.selectedValue_lower
    k choice remaining hremaining excluded
  unfold KiloblockConstruction.normalDeviationPotential
  rw [hmode, KiloblockConstruction.normalEndpointPotential,
    KiloblockConstruction.normalPotential, hmode]
  linarith

private theorem KiloblockConstruction.normalEndpointPotential_continue_self
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (hmode : construction.mode t history =
      .active k (some excluded) remaining)
    (hweight : 0 <
      (construction.buildingBlock k).z.singleton excluded) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.normalEndpointPotential excluded) history =
      construction.normalEndpointPotential excluded t history := by
  classical
  have hstate : ∃ signal, history.2 = PublicQuittingState.active signal := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨signal, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  let quitAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded.1 true
  let continueAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded.1 false
  let continuation := expect ((publicQuittingGame table
      construction.profile.signalLaw).transition
        (PublicQuittingState.active signal) continueAction)
    (fun nextState => construction.normalEndpointPotential excluded (t + 1)
      (Fin.snoc past
        (PublicQuittingState.active signal, continueAction), nextState))
  have hquitTransition : (publicQuittingGame table
      construction.profile.signalLaw).transition
        (PublicQuittingState.active signal) quitAction =
      PMF.pure (.absorbed
        (quittingProjectiveSingletonTerminal excluded.1)) := by
    exact publicQuittingGame_transition_publicSingleQuitAction
      table construction.profile.signalLaw signal excluded.1
  have hquitSupport : PublicQuittingState.absorbed
      (quittingProjectiveSingletonTerminal excluded.1) ∈
      ((publicQuittingGame table
        construction.profile.signalLaw).transition
          (PublicQuittingState.active signal) quitAction).support := by
    rw [hquitTransition]
    exact (PMF.mem_support_pure_iff _ _).2 rfl
  have hquitStep := construction.mode_step t
    (past, PublicQuittingState.active signal) quitAction
      (PublicQuittingState.absorbed
        (quittingProjectiveSingletonTerminal excluded.1)) hquitSupport
  have hquitMode : construction.mode (t + 1)
      (Fin.snoc past (PublicQuittingState.active signal, quitAction),
        PublicQuittingState.absorbed
          (quittingProjectiveSingletonTerminal excluded.1)) =
        .absorbed (some k) := by
    simp [KiloblockModeStep, hmode, quitAction,
      publicSingleQuitAction_apply_eq_raw,
      rawSingleQuitAction_true_quitters] at hquitStep
    exact hquitStep.2
  have hquitValue : construction.normalEndpointPotential excluded (t + 1)
      (Fin.snoc past (PublicQuittingState.active signal, quitAction),
        PublicQuittingState.absorbed
          (quittingProjectiveSingletonTerminal excluded.1)) =
      construction.trackingCorrection k excluded := by
    unfold KiloblockConstruction.normalEndpointPotential
    rw [hquitMode]
    unfold KiloblockConstruction.normalPotential
    rw [hquitMode]
    change table.terminal
      (quittingProjectiveSingletonTerminal excluded.1) excluded.1 +
        construction.trackingCorrection k excluded =
      construction.trackingCorrection k excluded
    rw [construction.soloExitNormalized excluded.1]
    ring
  let coin := quittingMeshHazardCoin
    (construction.attempt k excluded).quitWeight (construction.mesh k)
    (construction.attempt k excluded).quitWeight_pos.le
    (construction.attempt k excluded).quitWeight_lt_one
  let actionLaw : PMF ((publicQuittingGame table
      construction.profile.signalLaw).JointAct) :=
    coin.bind (fun quits => PMF.pure
      (publicSingleQuitAction table construction.profile.signalLaw
        excluded.1 quits))
  have hprofileAction : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        construction.profile.strategy
        (past, PublicQuittingState.active signal) = actionLaw := by
    unfold actionLaw coin
    exact construction.stageActionDist_active_some
      (past, PublicQuittingState.active signal) k excluded remaining hmode
  have hprofile : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        (construction.normalEndpointPotential excluded)
        (past, PublicQuittingState.active signal) =
      quittingMeshHazard (construction.attempt k excluded).quitWeight
          (construction.mesh k) *
            construction.trackingCorrection k excluded +
        (1 - quittingMeshHazard
          (construction.attempt k excluded).quitWeight
            (construction.mesh k)) * continuation := by
    unfold StochasticGame.historyContinuationEU
    rw [hprofileAction]
    unfold actionLaw
    rw [Math.Probability.expect_bind]
    simp_rw [Math.Probability.expect_pure]
    rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
    change (coin true).toReal * expect
        ((publicQuittingGame table
          construction.profile.signalLaw).transition
            (PublicQuittingState.active signal) quitAction)
        (fun nextState => construction.normalEndpointPotential excluded (t + 1)
          (Fin.snoc past
            (PublicQuittingState.active signal, quitAction), nextState)) +
      (coin false).toReal * continuation = _
    rw [hquitTransition, Math.Probability.expect_pure, hquitValue]
    unfold coin
    simp only [quittingMeshHazardCoin_false_toReal,
      quittingMeshHazardCoin_true_toReal]
  have hactionContinue := construction.stageActionDist_continue_selected_self
    excluded (past, PublicQuittingState.active signal) k remaining hmode
  have hcontinueAction : publicAllContinueAction table
      construction.profile.signalLaw = continueAction := by
    funext who
    simp [publicAllContinueAction, continueAction,
      publicSingleQuitAction_apply_eq_raw, rawSingleQuitAction]
  have hcontinue : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.normalEndpointPotential excluded)
        (past, PublicQuittingState.active signal) = continuation := by
    unfold StochasticGame.historyContinuationEU
    rw [hactionContinue, Math.Probability.expect_pure, hcontinueAction]
  have hcurrent : construction.normalEndpointPotential excluded t
      (past, PublicQuittingState.active signal) =
        construction.trackingCorrection k excluded := by
    simp [KiloblockConstruction.normalEndpointPotential,
      KiloblockConstruction.normalPotential, hmode,
      construction.selectedValue_self_eq_zero k excluded remaining hweight]
  have hharmonic := construction.normalEndpointPotential_harmonic excluded
    (past, PublicQuittingState.active signal)
  rw [hprofile, hcurrent] at hharmonic
  rw [hcontinue, hcurrent]
  have hhazard := quittingMeshHazard_lt_one
    (construction.attempt k excluded).quitWeight_lt_one
      (construction.mesh k)
  nlinarith

private theorem KiloblockConstruction.normalDeviationPotential_le_endpoint_add
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hnot : construction.mode t history ≠ .absorbed none) :
    construction.normalDeviationPotential who t history ≤
      construction.normalEndpointPotential who t history + 2 * ε := by
  unfold KiloblockConstruction.normalDeviationPotential
  cases hmode : construction.mode t history with
  | draw phase =>
      cases phase <;> simp [KiloblockConstruction.normalEndpointPotential,
        hmode, construction.epsilon_pos.le]
  | active k choice remaining => exact le_rfl
  | finalActive =>
      simp [KiloblockConstruction.normalEndpointPotential, hmode,
        construction.epsilon_pos.le]
  | absorbed origin =>
      cases origin with
      | none => exact False.elim (hnot hmode)
      | some k =>
          simp [construction.epsilon_pos.le]

private theorem KiloblockConstruction.normalEndpointPotential_continue_active
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining)
    (hsupported : construction.SelectedChoiceSupported
      (construction.mode t history)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.normalEndpointPotential excluded) history =
      construction.normalEndpointPotential excluded t history := by
  cases choice with
  | none =>
      have hcontinue := construction.stageActionDist_continue_all
        excluded history (by
          intro block owner tail hselected
          simp [hmode] at hselected)
      have hprofile := construction.stageActionDist_profile_continue_all
        history (by
          intro block owner tail hselected
          simp [hmode] at hselected)
      have hharmonic :=
        construction.normalEndpointPotential_harmonic excluded history
      unfold StochasticGame.historyContinuationEU at hharmonic ⊢
      rw [hcontinue]
      rw [hprofile] at hharmonic
      exact hharmonic
  | some owner =>
      by_cases howner : owner = excluded
      · subst owner
        apply construction.normalEndpointPotential_continue_self
          excluded history k remaining hmode
        simpa [KiloblockConstruction.SelectedChoiceSupported, hmode] using
          hsupported
      · have haction := construction.stageActionDist_continue_selected_other
          excluded owner history k remaining howner hmode
        have hharmonic :=
          construction.normalEndpointPotential_harmonic excluded history
        unfold StochasticGame.historyContinuationEU at hharmonic ⊢
        rw [haction]
        exact hharmonic

private theorem KiloblockConstruction.normalDeviationPotential_continue_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining)
    (hsupported : construction.SelectedChoiceSupported
      (construction.mode t history)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.normalDeviationPotential excluded) history ≤
      construction.normalDeviationPotential excluded t history := by
  unfold StochasticGame.historyContinuationEU
  calc
    expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist
      (construction.continueProfile excluded) history)
      (fun action => expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 action)
        (fun nextState => construction.normalDeviationPotential excluded
          (t + 1) (Fin.snoc history.1
            (history.2, action), nextState))) ≤
      expect ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
        (construction.continueProfile excluded) history)
        (fun action => expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
          (fun nextState => construction.normalEndpointPotential excluded
            (t + 1) (Fin.snoc history.1
              (history.2, action), nextState) + 2 * ε)) := by
        apply Math.Probability.expect_mono
        intro action
        apply expect_le_expect_of_le_on_support
        intro nextState hsupport
        apply construction.normalDeviationPotential_le_endpoint_add
        intro hnextMode
        have hstep := construction.mode_step t history action nextState hsupport
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep
        · rw [hnextMode] at hstep
          cases hstep.2
        · split at hstep <;> rw [hnextMode] at hstep <;> cases hstep.2
    _ = (publicQuittingGame table
          construction.profile.signalLaw).historyContinuationEU
          (construction.continueProfile excluded)
          (construction.normalEndpointPotential excluded) history +
        2 * ε := by
      rw [show (fun action => expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
          (fun nextState => construction.normalEndpointPotential excluded
            (t + 1) (Fin.snoc history.1
              (history.2, action), nextState) + 2 * ε)) =
          fun action => expect ((publicQuittingGame table
            construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.normalEndpointPotential excluded
              (t + 1) (Fin.snoc history.1
                (history.2, action), nextState)) + 2 * ε by
        funext action
        rw [Math.Probability.expect_add, Math.Probability.expect_const]]
      rw [Math.Probability.expect_add, Math.Probability.expect_const]
      rfl
    _ = construction.normalEndpointPotential excluded t history + 2 * ε := by
      rw [construction.normalEndpointPotential_continue_active
        excluded history k choice remaining hmode hsupported]
    _ = construction.normalDeviationPotential excluded t history := by
      unfold KiloblockConstruction.normalDeviationPotential
      rw [hmode]

private theorem KiloblockConstruction.stageActionDist_pureAction_no_owner
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (action : Bool)
    (hmode : ∀ k owner remaining,
      construction.mode t history ≠ .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action)) history =
      PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded.1 action) := by
  cases action with
  | true =>
      exact construction.stageActionDist_pureQuit_no_other excluded history
        (fun k owner remaining heq => False.elim (hmode k owner remaining heq))
  | false =>
      have hcontinue := construction.stageActionDist_continue_all
        excluded history hmode
      have hprofileEq : Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure false) = construction.continueProfile excluded :=
        rfl
      rw [hprofileEq, hcontinue]
      congr 1
      funext who
      simp [publicAllContinueAction, publicSingleQuitAction_apply_eq_raw,
        rawSingleQuitAction]

private theorem KiloblockConstruction.normalDeviationPotential_draw
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (phase : KiloblockPhase table construction.blockCount)
    (action : Bool)
    (hmode : construction.mode t history = .draw phase) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action))
        (construction.normalDeviationPotential excluded) history =
      construction.normalDeviationPotential excluded t history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  let jointAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded.1 action
  have haction := construction.stageActionDist_pureAction_no_owner
    excluded (past, PublicQuittingState.draw) action (by
      intro k owner remaining hselected
      simp [hmode] at hselected)
  unfold StochasticGame.historyContinuationEU
  rw [haction, Math.Probability.expect_pure]
  change expect (construction.profile.signalLaw.map
      PublicQuittingState.active)
    (fun nextState => construction.normalDeviationPotential excluded (t + 1)
      (Fin.snoc past
        (PublicQuittingState.draw, jointAction), nextState)) = _
  rw [Math.Probability.expect_map]
  cases phase with
  | choose k =>
      calc
        expect construction.profile.signalLaw (fun signal =>
            construction.normalDeviationPotential excluded (t + 1)
              (Fin.snoc past
                (PublicQuittingState.draw, jointAction),
                  PublicQuittingState.active signal)) =
          expect construction.profile.signalLaw (fun signal =>
            construction.selectedValue k
                (construction.signalSelector k signal)
                (construction.mesh k) excluded +
              construction.trackingCorrection k excluded + 2 * ε) := by
            apply Math.ProbabilityMassFunction.expect_congr_on_support
            intro signal hsignal
            have hnextSupport : PublicQuittingState.active signal ∈
                ((publicQuittingGame table
                  construction.profile.signalLaw).transition
                    PublicQuittingState.draw jointAction).support := by
              change PublicQuittingState.active signal ∈
                (construction.profile.signalLaw.map
                  PublicQuittingState.active).support
              rw [PMF.mem_support_map_iff]
              exact ⟨signal, hsignal, rfl⟩
            have hstep := construction.mode_step t
              (past, PublicQuittingState.draw) jointAction
              (PublicQuittingState.active signal) hnextSupport
            have hnextMode : construction.mode (t + 1)
                (Fin.snoc past (PublicQuittingState.draw, jointAction),
                  PublicQuittingState.active signal) =
                .active k (construction.signalSelector k signal)
                  (construction.mesh k) := by
              simpa [KiloblockModeStep, hmode] using hstep
            simp [KiloblockConstruction.normalDeviationPotential,
              KiloblockConstruction.normalEndpointPotential,
              KiloblockConstruction.normalPotential, hnextMode]
        _ = (construction.buildingBlock k).w excluded +
            construction.trackingCorrection k excluded + 2 * ε := by
          rw [Math.Probability.expect_add, Math.Probability.expect_const,
            Math.Probability.expect_add, Math.Probability.expect_const,
            construction.expect_selectedValue_mesh]
        _ = construction.normalDeviationPotential excluded t
            (past, PublicQuittingState.draw) := by
          simp [KiloblockConstruction.normalDeviationPotential,
            KiloblockConstruction.normalEndpointPotential,
            KiloblockConstruction.normalPotential, hmode]
  | resume k choice remaining =>
      calc
        expect construction.profile.signalLaw (fun signal =>
            construction.normalDeviationPotential excluded (t + 1)
              (Fin.snoc past (PublicQuittingState.draw, jointAction),
                PublicQuittingState.active signal)) =
          expect construction.profile.signalLaw (fun _ =>
            construction.selectedValue k choice remaining excluded +
              construction.trackingCorrection k excluded + 2 * ε) := by
            apply Math.ProbabilityMassFunction.expect_congr_on_support
            intro signal hsignal
            have hnextSupport : PublicQuittingState.active signal ∈
                ((publicQuittingGame table
                  construction.profile.signalLaw).transition
                    PublicQuittingState.draw jointAction).support := by
              change PublicQuittingState.active signal ∈
                (construction.profile.signalLaw.map
                  PublicQuittingState.active).support
              rw [PMF.mem_support_map_iff]
              exact ⟨signal, hsignal, rfl⟩
            have hstep := construction.mode_step t
              (past, PublicQuittingState.draw) jointAction
              (PublicQuittingState.active signal) hnextSupport
            have hnextMode : construction.mode (t + 1)
                (Fin.snoc past (PublicQuittingState.draw, jointAction),
                  PublicQuittingState.active signal) =
                  .active k choice remaining := by
              simpa [KiloblockModeStep, hmode] using hstep
            simp [KiloblockConstruction.normalDeviationPotential,
              KiloblockConstruction.normalEndpointPotential,
              KiloblockConstruction.normalPotential, hnextMode]
        _ = construction.selectedValue k choice remaining excluded +
            construction.trackingCorrection k excluded + 2 * ε :=
          Math.Probability.expect_const _ _
        _ = construction.normalDeviationPotential excluded t
            (past, PublicQuittingState.draw) := by
          simp [KiloblockConstruction.normalDeviationPotential,
            KiloblockConstruction.normalEndpointPotential,
            KiloblockConstruction.normalPotential, hmode]
  | final =>
      calc
        expect construction.profile.signalLaw (fun signal =>
            construction.normalDeviationPotential excluded (t + 1)
              (Fin.snoc past (PublicQuittingState.draw, jointAction),
                PublicQuittingState.active signal)) =
          expect construction.profile.signalLaw (fun _ =>
            construction.point ⟨0, by omega⟩ excluded) := by
            apply Math.ProbabilityMassFunction.expect_congr_on_support
            intro signal hsignal
            have hnextSupport : PublicQuittingState.active signal ∈
                ((publicQuittingGame table
                  construction.profile.signalLaw).transition
                    PublicQuittingState.draw jointAction).support := by
              change PublicQuittingState.active signal ∈
                (construction.profile.signalLaw.map
                  PublicQuittingState.active).support
              rw [PMF.mem_support_map_iff]
              exact ⟨signal, hsignal, rfl⟩
            have hstep := construction.mode_step t
              (past, PublicQuittingState.draw) jointAction
              (PublicQuittingState.active signal) hnextSupport
            have hnextMode : construction.mode (t + 1)
                (Fin.snoc past (PublicQuittingState.draw, jointAction),
                  PublicQuittingState.active signal) = .finalActive := by
              simpa [KiloblockModeStep, hmode] using hstep
            simp [KiloblockConstruction.normalDeviationPotential, hnextMode]
        _ = construction.point ⟨0, by omega⟩ excluded :=
          Math.Probability.expect_const _ _
        _ = construction.normalDeviationPotential excluded t
            (past, PublicQuittingState.draw) := by
          simp [KiloblockConstruction.normalDeviationPotential, hmode]

private theorem KiloblockConstruction.normalDeviationPotential_absorbed
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (origin : Option (Fin (construction.blockCount + 1)))
    (action : Bool)
    (hmode : construction.mode t history = .absorbed origin) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action))
        (construction.normalDeviationPotential excluded) history =
      construction.normalDeviationPotential excluded t history := by
  classical
  have hstate : ∃ quitters, history.2 =
      PublicQuittingState.absorbed quitters := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨quitters, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  let absorbedState : (publicQuittingGame table
      construction.profile.signalLaw).State :=
    PublicQuittingState.absorbed quitters
  have hmode' : construction.mode t (past, absorbedState) =
      .absorbed origin := by
    simpa [absorbedState] using hmode
  let jointAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded.1 action
  have haction := construction.stageActionDist_pureAction_no_owner
    excluded (past, absorbedState) action (by
      intro k owner remaining hselected
      simp [hmode'] at hselected)
  have htransition : (publicQuittingGame table
      construction.profile.signalLaw).transition
        absorbedState jointAction = PMF.pure absorbedState := by
    simp [publicQuittingGame, absorbedState]
  have hsupport : absorbedState ∈
      ((publicQuittingGame table
        construction.profile.signalLaw).transition
          absorbedState jointAction).support := by
    rw [htransition]
    exact (PMF.mem_support_pure_iff _ _).2 rfl
  have hstep := construction.mode_step t
    (past, absorbedState) jointAction absorbedState hsupport
  have hnextMode : construction.mode (t + 1)
      (Fin.snoc past (absorbedState, jointAction), absorbedState) =
        .absorbed origin := by
    simpa [KiloblockModeStep, hmode'] using hstep
  dsimp only [jointAction] at hnextMode
  change (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action))
        (construction.normalDeviationPotential excluded)
        (past, absorbedState) =
      construction.normalDeviationPotential excluded t
        (past, absorbedState)
  unfold StochasticGame.historyContinuationEU
  rw [haction, Math.Probability.expect_pure, htransition,
    Math.Probability.expect_pure]
  cases origin with
  | none =>
      simp [KiloblockConstruction.normalDeviationPotential, hmode', hnextMode]
  | some k =>
      simp [KiloblockConstruction.normalDeviationPotential,
        KiloblockConstruction.normalEndpointPotential,
        KiloblockConstruction.normalPotential, hmode', hnextMode,
        KiloblockConstruction.trackingCorrectionAtOrigin]

private theorem KiloblockConstruction.normalDeviationPotential_finalActive
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (action : Bool)
    (hmode : construction.mode t history = .finalActive) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action))
        (construction.normalDeviationPotential excluded) history =
      construction.normalDeviationPotential excluded t history := by
  classical
  have hstate : ∃ signal, history.2 = PublicQuittingState.active signal := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨signal, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  let activeState : (publicQuittingGame table
      construction.profile.signalLaw).State :=
    PublicQuittingState.active signal
  have hmode' : construction.mode t (past, activeState) = .finalActive := by
    simpa [activeState] using hmode
  let jointAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded.1 action
  have haction := construction.stageActionDist_pureAction_no_owner
    excluded (past, activeState) action (by
      intro k owner remaining hselected
      simp [hmode'] at hselected)
  change (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action))
        (construction.normalDeviationPotential excluded)
        (past, activeState) =
      construction.normalDeviationPotential excluded t (past, activeState)
  unfold StochasticGame.historyContinuationEU
  rw [haction, Math.Probability.expect_pure]
  cases action with
  | false =>
      let drawState : (publicQuittingGame table
          construction.profile.signalLaw).State :=
        PublicQuittingState.draw
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
            activeState jointAction = PMF.pure drawState := by
        simp [jointAction, activeState, publicQuittingGame,
          drawState, publicSingleQuitAction_apply_eq_raw,
          rawSingleQuitAction_false]
      have hsupport : drawState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              activeState jointAction).support := by
        rw [htransition]
        exact (PMF.mem_support_pure_iff _ _).2 rfl
      have hstep := construction.mode_step t
        (past, activeState) jointAction drawState hsupport
      have hnextMode : construction.mode (t + 1)
          (Fin.snoc past (activeState, jointAction), drawState) =
            .draw .final := by
        simpa [KiloblockModeStep, hmode', jointAction, activeState, drawState,
          publicSingleQuitAction_apply_eq_raw, rawSingleQuitAction_false]
            using hstep
      dsimp only [jointAction] at hnextMode
      rw [htransition, Math.Probability.expect_pure]
      simp [KiloblockConstruction.normalDeviationPotential, hmode', hnextMode]
  | true =>
      let absorbedState : (publicQuittingGame table
          construction.profile.signalLaw).State :=
        PublicQuittingState.absorbed
          (quittingProjectiveSingletonTerminal excluded.1)
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition
            activeState jointAction = PMF.pure absorbedState := by
        exact publicQuittingGame_transition_publicSingleQuitAction
          table construction.profile.signalLaw signal excluded.1
      have hsupport : absorbedState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              activeState jointAction).support := by
        rw [htransition]
        exact (PMF.mem_support_pure_iff _ _).2 rfl
      have hstep := construction.mode_step t
        (past, activeState) jointAction absorbedState hsupport
      have hnextMode : construction.mode (t + 1)
          (Fin.snoc past (activeState, jointAction), absorbedState) =
            .absorbed none := by
        simp [KiloblockModeStep, hmode', jointAction, activeState,
          absorbedState,
          publicSingleQuitAction_apply_eq_raw,
          rawSingleQuitAction_true_quitters] at hstep
        exact hstep.2
      dsimp only [jointAction] at hnextMode
      rw [htransition, Math.Probability.expect_pure]
      simp [KiloblockConstruction.normalDeviationPotential, hmode', hnextMode]

private theorem KiloblockConstruction.normalDeviationPotential_pureAction_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (action : Bool)
    (hsupported : construction.SelectedChoiceSupported
      (construction.mode t history)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure action))
        (construction.normalDeviationPotential excluded) history ≤
      construction.normalDeviationPotential excluded t history := by
  cases hmode : construction.mode t history with
  | draw phase =>
      exact (construction.normalDeviationPotential_draw
        excluded history phase action hmode).le
  | active k choice remaining =>
      cases action with
      | false =>
          exact construction.normalDeviationPotential_continue_le
            excluded history k choice remaining hmode hsupported
      | true =>
          apply construction.normalDeviationPotential_pureQuit_le
            excluded history k choice remaining hmode
          exact construction.mode_remaining_le_mesh t history
            k choice remaining (Or.inl hmode)
  | finalActive =>
      exact (construction.normalDeviationPotential_finalActive
        excluded history action hmode).le
  | absorbed origin =>
      exact (construction.normalDeviationPotential_absorbed
        excluded history origin action hmode).le

/-! The corrected block value is a supermartingale under every behavioral
deviation by a normal player, along histories that the deviation can reach. -/
theorem KiloblockConstruction.normalDeviationPotential_superharmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hsupported : construction.SelectedChoiceSupported
      (construction.mode t history)) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1 deviation)
        (construction.normalDeviationPotential excluded) history ≤
      construction.normalDeviationPotential excluded t history := by
  rw [historyContinuationEU_update_eq_expect_pure table
    construction.profile.signalLaw construction.profile.strategy excluded.1
      deviation (construction.normalDeviationPotential excluded) history]
  calc
    expect (deviation t history) (fun action =>
        (publicQuittingGame table
          construction.profile.signalLaw).historyContinuationEU
            (Function.update construction.profile.strategy excluded.1
              (fun _ _ => PMF.pure action))
            (construction.normalDeviationPotential excluded) history) ≤
        expect (deviation t history) (fun _ =>
          construction.normalDeviationPotential excluded t history) := by
      apply Math.Probability.expect_mono
      intro action
      exact construction.normalDeviationPotential_pureAction_le
        excluded history action hsupported
    _ = construction.normalDeviationPotential excluded t history :=
      Math.Probability.expect_const _ _

theorem KiloblockConstruction.expect_normalDeviationPotential_le_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1) : ∀ t,
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded.1 deviation)
        .draw t)
      (construction.normalDeviationPotential excluded t) ≤
        construction.normalDeviationPotential excluded 0
          ((publicQuittingGame table
            construction.profile.signalLaw).emptyHist .draw)
  | 0 => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
          Math.Probability.expect_pure]
  | t + 1 => by
      change (publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue
          (Function.update construction.profile.strategy excluded.1 deviation)
          .draw (construction.normalDeviationPotential excluded) (t + 1) ≤ _
      rw [(publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue_succ]
      calc
        expect ((publicQuittingGame table
            construction.profile.signalLaw).histDist
          (Function.update construction.profile.strategy excluded.1 deviation)
            .draw t)
          (fun history => (publicQuittingGame table
            construction.profile.signalLaw).historyContinuationEU
              (Function.update construction.profile.strategy excluded.1 deviation)
              (construction.normalDeviationPotential excluded) history) ≤
            expect ((publicQuittingGame table
                construction.profile.signalLaw).histDist
              (Function.update construction.profile.strategy excluded.1 deviation)
                .draw t)
              (construction.normalDeviationPotential excluded t) := by
          apply expect_le_expect_of_le_on_support
          intro history hhistory
          exact construction.normalDeviationPotential_superharmonic
            excluded deviation history
              (construction.selectedChoiceSupported_of_mem_support
                excluded.1 deviation t history hhistory)
        _ ≤ _ := construction.expect_normalDeviationPotential_le_initial
          excluded deviation t

noncomputable def KiloblockConstruction.continueTransiencePotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    KiloblockFiniteMode construction → ℝ :=
  (Classical.choice
    (construction.continueKernel_transienceCertificate excluded)).potential

theorem KiloblockConstruction.continueTransiencePotential_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (mode : KiloblockFiniteMode construction) :
    0 ≤ construction.continueTransiencePotential excluded mode :=
  (Classical.choice
    (construction.continueKernel_transienceCertificate excluded)).potential_nonneg mode

theorem KiloblockConstruction.continueTransiencePotential_eq_zero_on_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {mode : KiloblockFiniteMode construction}
    (hmode : mode ∈ construction.continueBoundary) :
    construction.continueTransiencePotential excluded mode = 0 :=
  (Classical.choice
    (construction.continueKernel_transienceCertificate excluded)
  ).potential_eq_zero_on_core hmode

theorem KiloblockConstruction.continueTransiencePotential_poisson
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) (mode : KiloblockFiniteMode construction) :
    construction.continueTransiencePotential excluded mode -
        expect (construction.continueFiniteModeKernel excluded mode)
          (construction.continueTransiencePotential excluded) =
      Math.Probability.transientCharge construction.continueBoundary mode :=
  (Classical.choice
    (construction.continueKernel_transienceCertificate excluded)).poisson mode

noncomputable def KiloblockConstruction.continueTransienceHistoryPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => construction.continueTransiencePotential excluded
    (construction.finiteMode (construction.mode t history))

private theorem KiloblockConstruction.continueTransienceHistoryPotential_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.continueTransienceHistoryPotential excluded) history =
      expect (construction.continueFiniteModeKernel excluded
          (construction.finiteMode (construction.mode t history)))
        (construction.continueTransiencePotential excluded) := by
  unfold StochasticGame.historyContinuationEU
    KiloblockConstruction.continueTransienceHistoryPotential
  rw [← construction.finiteHistoryModeStepDist_continue_eq excluded history]
  unfold KiloblockConstruction.historyModeStepDist
  rw [PMF.map_bind, Math.Probability.expect_bind]
  apply congrArg (expect ((publicQuittingGame table
    construction.profile.signalLaw).stageActionDist
      (construction.continueProfile excluded) history))
  funext action
  rw [PMF.map_comp, Math.Probability.expect_map]
  rfl

theorem KiloblockConstruction.continueTransienceHistoryPotential_drift
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (construction.continueProfile excluded)
        (construction.continueTransienceHistoryPotential excluded) history +
      Math.Probability.transientCharge construction.continueBoundary
        (construction.finiteMode (construction.mode t history)) =
      construction.continueTransienceHistoryPotential excluded t history := by
  rw [construction.continueTransienceHistoryPotential_step excluded history]
  unfold KiloblockConstruction.continueTransienceHistoryPotential
  linarith [construction.continueTransiencePotential_poisson excluded
    (construction.finiteMode (construction.mode t history))]

private theorem KiloblockConstruction.transience_pureQuit_le_continue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true))
        (construction.continueTransienceHistoryPotential excluded) history ≤
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          (construction.continueProfile excluded)
          (construction.continueTransienceHistoryPotential excluded) history := by
  classical
  let actions : ∀ player,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act player) :=
    fun player => construction.profile.strategy player t history
  have hquitDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded.1
          (fun _ _ => PMF.pure true)) history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded.1 (PMF.pure true)) := by
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = excluded.1
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  have hcontinueDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        (construction.continueProfile excluded) history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded.1 (PMF.pure false)) := by
    unfold StochasticGame.stageActionDist
      KiloblockConstruction.continueProfile
    congr 1
    funext player
    by_cases hplayer : player = excluded.1
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  unfold StochasticGame.historyContinuationEU
  rw [hquitDist, hcontinueDist,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded.1 true,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded.1 false,
    Math.Probability.expect_bind, Math.Probability.expect_bind]
  simp_rw [Math.Probability.expect_pure]
  apply Math.Probability.expect_mono
  intro action
  let quitAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded.1 true
  let continueAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded.1 false
  cases hmode : construction.mode t history with
  | active k choice remaining =>
      have hquitValue : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.continueTransienceHistoryPotential
            excluded (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        have hnextMode : construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, quitAction), nextState) =
              .absorbed (some k) := by
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep
          case isTrue => exact hstep.2
          case isFalse hfalse =>
            exfalso
            apply hfalse
            refine ⟨excluded.1, ?_⟩
            simp [quitAction]
        unfold KiloblockConstruction.continueTransienceHistoryPotential
        rw [hnextMode]
        exact construction.continueTransiencePotential_eq_zero_on_boundary
          excluded trivial
      rw [hquitValue]
      exact expect_nonneg _ _ fun nextState =>
        construction.continueTransiencePotential_nonneg excluded
          (construction.finiteMode (construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState)))
  | finalActive =>
      have hleft : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.continueTransienceHistoryPotential
            excluded (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        unfold KiloblockConstruction.continueTransienceHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep <;> rw [hstep.2]
        all_goals
          apply construction.continueTransiencePotential_eq_zero_on_boundary
          trivial
      have hright : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 continueAction)
          (fun nextState => construction.continueTransienceHistoryPotential
            excluded (t + 1)
              (Fin.snoc history.1 (history.2, continueAction), nextState)) =
          0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history continueAction
          nextState hnext
        unfold KiloblockConstruction.continueTransienceHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep <;> rw [hstep.2]
        all_goals
          apply construction.continueTransiencePotential_eq_zero_on_boundary
          trivial
      rw [hleft, hright]
  | draw phase =>
      have hstate : history.2 = PublicQuittingState.draw := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
      rw [hstate]
      apply le_of_eq
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro nextState hnext
      have hquitStep := construction.mode_step t history quitAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hcontinueStep := construction.mode_step t history continueAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hm : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState) := by
        cases phase <;> cases hsnext : nextState <;>
          simp [KiloblockModeStep, hmode, hsnext] at hquitStep hcontinueStep
        all_goals rw [hquitStep, hcontinueStep]
      unfold KiloblockConstruction.continueTransienceHistoryPotential
      have hm' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.draw, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1
              (PublicQuittingState.draw, continueAction), nextState) := by
        simpa only [hstate] using hm
      rw [hm']
  | absorbed origin =>
      have hstate : ∃ quitters, history.2 =
          PublicQuittingState.absorbed quitters := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
        exact ⟨_, rfl⟩
      obtain ⟨quitters, hstate⟩ := hstate
      rw [hstate]
      simp only [publicQuittingGame, Math.Probability.expect_pure]
      have hquitStep := construction.mode_step t history quitAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      have hcontinueStep := construction.mode_step t history continueAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      simp only [KiloblockModeStep, hmode] at hquitStep hcontinueStep
      unfold KiloblockConstruction.continueTransienceHistoryPotential
      have hquitStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, quitAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hquitStep
      have hcontinueStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, continueAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hcontinueStep
      dsimp only [quitAction] at hquitStep'
      dsimp only [continueAction] at hcontinueStep'
      exact (congrArg (construction.continueTransiencePotential excluded)
        (congrArg construction.finiteMode
          (hquitStep'.trans hcontinueStep'.symm))).le

theorem KiloblockConstruction.continueTransienceHistoryPotential_deviation_drift
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1 deviation)
        (construction.continueTransienceHistoryPotential excluded) history +
      Math.Probability.transientCharge construction.continueBoundary
        (construction.finiteMode (construction.mode t history)) ≤
      construction.continueTransienceHistoryPotential excluded t history := by
  have hpure : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded.1 deviation)
        (construction.continueTransienceHistoryPotential excluded) history ≤
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          (construction.continueProfile excluded)
          (construction.continueTransienceHistoryPotential excluded) history := by
    rw [historyContinuationEU_update_eq_expect_pure table
      construction.profile.signalLaw construction.profile.strategy excluded.1
        deviation (construction.continueTransienceHistoryPotential excluded)
        history]
    calc
      expect (deviation t history) (fun action =>
          (publicQuittingGame table
            construction.profile.signalLaw).historyContinuationEU
              (Function.update construction.profile.strategy excluded.1
                (fun _ _ => PMF.pure action))
              (construction.continueTransienceHistoryPotential excluded)
              history) ≤
          expect (deviation t history) (fun _ =>
            (publicQuittingGame table
              construction.profile.signalLaw).historyContinuationEU
                (construction.continueProfile excluded)
                (construction.continueTransienceHistoryPotential excluded)
                history) := by
        apply Math.Probability.expect_mono
        intro action
        cases action with
        | false => exact le_rfl
        | true =>
            exact construction.transience_pureQuit_le_continue
              excluded history
      _ = _ := Math.Probability.expect_const _ _
  linarith [construction.continueTransienceHistoryPotential_drift
    excluded history]

def KiloblockConstruction.deviationTransientMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    (t : ℕ) : ℝ :=
  expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist
    (Function.update construction.profile.strategy excluded.1 deviation)
      .draw t)
    (fun history => Math.Probability.transientCharge
      construction.continueBoundary
        (construction.finiteMode (construction.mode t history)))

def KiloblockConstruction.expectedContinueTransiencePotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    (t : ℕ) : ℝ :=
  expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist
    (Function.update construction.profile.strategy excluded.1 deviation)
      .draw t)
    (construction.continueTransienceHistoryPotential excluded t)

theorem KiloblockConstruction.expectedContinueTransiencePotential_drift
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    (t : ℕ) :
    construction.expectedContinueTransiencePotential
        excluded deviation (t + 1) +
      construction.deviationTransientMass excluded deviation t ≤
    construction.expectedContinueTransiencePotential excluded deviation t := by
  unfold KiloblockConstruction.expectedContinueTransiencePotential
    KiloblockConstruction.deviationTransientMass
  change (publicQuittingGame table
      construction.profile.signalLaw).expectedHistoryValue
        (Function.update construction.profile.strategy excluded.1 deviation)
        .draw (construction.continueTransienceHistoryPotential excluded)
          (t + 1) + _ ≤ _
  rw [(publicQuittingGame table
    construction.profile.signalLaw).expectedHistoryValue_succ]
  rw [← Math.Probability.expect_add]
  apply Math.Probability.expect_mono
  intro history
  exact construction.continueTransienceHistoryPotential_deviation_drift
    excluded deviation history

theorem KiloblockConstruction.sum_deviationTransientMass_add_potential_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1) : ∀ steps,
    (∑ t ∈ Finset.range steps,
        construction.deviationTransientMass excluded deviation t) +
      construction.expectedContinueTransiencePotential
        excluded deviation steps ≤
      construction.continueTransienceHistoryPotential excluded 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw)
  | 0 => by
      simp [KiloblockConstruction.expectedContinueTransiencePotential]
  | steps + 1 => by
      rw [Finset.sum_range_succ]
      have hinduction :=
        construction.sum_deviationTransientMass_add_potential_le
          excluded deviation steps
      have hdrift :=
        construction.expectedContinueTransiencePotential_drift
          excluded deviation steps
      linarith

theorem KiloblockConstruction.exists_deviationTransientMass_lt
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    {threshold : ℝ} (hthreshold : 0 < threshold) :
    ∃ t, construction.deviationTransientMass excluded deviation t <
      threshold := by
  let initial := construction.continueTransienceHistoryPotential excluded 0
    ((publicQuittingGame table
      construction.profile.signalLaw).emptyHist .draw)
  obtain ⟨horizon, hhorizon⟩ := exists_nat_gt (initial / threshold)
  have hlarge : initial < (horizon : ℝ) * threshold := by
    exact (div_lt_iff₀ hthreshold).mp hhorizon
  by_contra hnot
  push Not at hnot
  have hlower : (horizon : ℝ) * threshold ≤
      ∑ t ∈ Finset.range horizon,
        construction.deviationTransientMass excluded deviation t := by
    calc
      (horizon : ℝ) * threshold =
          ∑ _t ∈ Finset.range horizon, threshold := by simp
      _ ≤ _ := Finset.sum_le_sum fun t _ => hnot t
  have hpotentialNonneg : 0 ≤
      construction.expectedContinueTransiencePotential
        excluded deviation horizon := by
    unfold KiloblockConstruction.expectedContinueTransiencePotential
    exact expect_nonneg _ _ fun history =>
      construction.continueTransiencePotential_nonneg excluded
        (construction.finiteMode (construction.mode horizon history))
  have hsum := construction.sum_deviationTransientMass_add_potential_le
    excluded deviation horizon
  have hupper : (∑ t ∈ Finset.range horizon,
      construction.deviationTransientMass excluded deviation t) ≤
      initial := by
    dsimp only [initial] at hsum ⊢
    linarith
  exact (not_lt_of_ge (hlower.trans hupper)) hlarge

theorem KiloblockConstruction.deviationTransientMass_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    (t : ℕ) :
    0 ≤ construction.deviationTransientMass excluded deviation t := by
  unfold KiloblockConstruction.deviationTransientMass
  exact expect_nonneg _ _ fun history =>
    Math.Probability.transientCharge_nonneg construction.continueBoundary
      (construction.finiteMode (construction.mode t history))

theorem KiloblockConstruction.exists_deviationTransientMass_lt_after
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    {threshold : ℝ} (hthreshold : 0 < threshold) (start : ℕ) :
    ∃ t, start ≤ t ∧
      construction.deviationTransientMass excluded deviation t <
        threshold := by
  let initial := construction.continueTransienceHistoryPotential excluded 0
    ((publicQuittingGame table
      construction.profile.signalLaw).emptyHist .draw)
  obtain ⟨span, hspan⟩ := exists_nat_gt (initial / threshold)
  have hlarge : initial < (span : ℝ) * threshold := by
    exact (div_lt_iff₀ hthreshold).mp hspan
  by_contra hnot
  push Not at hnot
  have hlower : (span : ℝ) * threshold ≤
      ∑ t ∈ Finset.Ico start (start + span),
        construction.deviationTransientMass excluded deviation t := by
    calc
      (span : ℝ) * threshold =
          ∑ _t ∈ Finset.Ico start (start + span), threshold := by simp
      _ ≤ _ := Finset.sum_le_sum fun t ht =>
        hnot t (Finset.mem_Ico.mp ht).1
  have hsubset : Finset.Ico start (start + span) ⊆
      Finset.range (start + span) := by
    intro t ht
    exact Finset.mem_range.mpr (Finset.mem_Ico.mp ht).2
  have hsumSubset : (∑ t ∈ Finset.Ico start (start + span),
      construction.deviationTransientMass excluded deviation t) ≤
      ∑ t ∈ Finset.range (start + span),
        construction.deviationTransientMass excluded deviation t := by
    apply Finset.sum_le_sum_of_subset_of_nonneg hsubset
    intro t _ _
    exact construction.deviationTransientMass_nonneg excluded deviation t
  have hpotentialNonneg : 0 ≤
      construction.expectedContinueTransiencePotential
        excluded deviation (start + span) := by
    unfold KiloblockConstruction.expectedContinueTransiencePotential
    exact expect_nonneg _ _ fun history =>
      construction.continueTransiencePotential_nonneg excluded
        (construction.finiteMode
          (construction.mode (start + span) history))
  have hsum := construction.sum_deviationTransientMass_add_potential_le
    excluded deviation (start + span)
  have hupper : (∑ t ∈ Finset.range (start + span),
      construction.deviationTransientMass excluded deviation t) ≤
      initial := by
    dsimp only [initial] at hsum ⊢
    linarith
  exact (not_lt_of_ge (hlower.trans (hsumSubset.trans hupper))) hlarge

private theorem KiloblockConstruction.normalDeviationPotential_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hnotAbsorbed : ∀ k, construction.mode t history ≠
      .absorbed (some k)) :
    0 ≤ construction.normalDeviationPotential excluded t history := by
  have hcorrection (k : Fin (construction.blockCount + 1)) :
      -ε < construction.trackingCorrection k excluded := by
    exact (abs_lt.mp
      (construction.abs_trackingCorrection_lt k excluded)).1
  have hpoint : 0 ≤ construction.point ⟨0, by omega⟩ excluded :=
    (construction.point_boundary ⟨0, by omega⟩).1.2 excluded
  cases hmode : construction.mode t history with
  | draw phase =>
      cases phase with
      | choose k =>
          have hw : 0 ≤ (construction.buildingBlock k).w excluded :=
            (construction.buildingBlock k).w_boundary.1.2 excluded
          simp only [KiloblockConstruction.normalDeviationPotential,
            KiloblockConstruction.normalEndpointPotential,
            KiloblockConstruction.normalPotential, hmode]
          linarith [construction.epsilon_pos, hcorrection k]
      | resume k choice remaining =>
          have hremaining := construction.mode_remaining_le_mesh t history
            k choice remaining (Or.inr hmode)
          have hselected := construction.selectedValue_lower
            k choice remaining hremaining excluded
          simp only [KiloblockConstruction.normalDeviationPotential,
            KiloblockConstruction.normalEndpointPotential,
            KiloblockConstruction.normalPotential, hmode]
          linarith [construction.epsilon_pos, hcorrection k]
      | final =>
          simpa only [KiloblockConstruction.normalDeviationPotential, hmode]
            using hpoint
  | active k choice remaining =>
      have hremaining := construction.mode_remaining_le_mesh t history
        k choice remaining (Or.inl hmode)
      have hselected := construction.selectedValue_lower
        k choice remaining hremaining excluded
      simp only [KiloblockConstruction.normalDeviationPotential,
        KiloblockConstruction.normalEndpointPotential,
        KiloblockConstruction.normalPotential, hmode]
      linarith [construction.epsilon_pos, hcorrection k]
  | finalActive =>
      simpa only [KiloblockConstruction.normalDeviationPotential, hmode]
        using hpoint
  | absorbed origin =>
      cases origin with
      | none =>
          simpa only [KiloblockConstruction.normalDeviationPotential, hmode]
            using hpoint
      | some k => exact False.elim (hnotAbsorbed k hmode)

private theorem KiloblockConstruction.publicHistoryPayoff_le_deviationPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (match history.2 with
      | .absorbed quitters => table.terminal quitters excluded.1
      | _ => table.never excluded.1) ≤
      construction.normalDeviationPotential excluded t history + ε +
        construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history)) +
        Math.Probability.transientCharge construction.continueBoundary
          (construction.finiteMode (construction.mode t history)) := by
  by_cases habsorbed : ∃ k, construction.mode t history =
      .absorbed (some k)
  · obtain ⟨k, hmode⟩ := habsorbed
    have hstate : ∃ quitters, history.2 =
        PublicQuittingState.absorbed quitters := by
      have hmatches := construction.mode_state t history
      cases hs : history.2 <;>
        simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
      exact ⟨_, rfl⟩
    obtain ⟨quitters, hstate⟩ := hstate
    have hcorrection := construction.abs_trackingCorrection_lt k excluded
    have hboundary : (.absorbed (some k) :
        KiloblockFiniteMode construction) ∈ construction.continueBoundary :=
      trivial
    have hpotential :
        construction.normalDeviationPotential excluded t history =
          table.terminal quitters excluded.1 +
            construction.trackingCorrection k excluded := by
      simp only [KiloblockConstruction.normalDeviationPotential, hmode,
        KiloblockConstruction.normalEndpointPotential,
        KiloblockConstruction.normalPotential, hstate,
        KiloblockConstruction.trackingCorrectionAtOrigin,
        Option.elim_some]
    have hpast : construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history)) = 0 := by
      simp [hmode, KiloblockConstruction.finiteMode,
        KiloblockConstruction.continuePastFinalIndicator]
    have htransient : Math.Probability.transientCharge
        construction.continueBoundary
          (construction.finiteMode (construction.mode t history)) = 0 := by
      rw [hmode]
      simpa only [KiloblockConstruction.finiteMode] using
        Math.Probability.transientCharge_of_mem hboundary
    rw [hstate, hpotential, hpast, htransient]
    linarith [(abs_lt.mp hcorrection).1]
  · have hvalue := construction.normalDeviationPotential_nonneg
      excluded history (by
        intro k hmode
        exact habsorbed ⟨k, hmode⟩)
    have hactual : (match history.2 with
        | .absorbed quitters => table.terminal quitters excluded.1
        | _ => table.never excluded.1) ≤ 1 := by
      cases history.2 with
      | draw =>
          exact (le_abs_self _).trans
            (construction.payoffsBounded.2 excluded.1)
      | active signal =>
          exact (le_abs_self _).trans
            (construction.payoffsBounded.2 excluded.1)
      | absorbed quitters =>
          exact (le_abs_self _).trans
            (construction.payoffsBounded.1 quitters excluded.1)
    have habsorbedZero : construction.continueAbsorbedIndicator
        (construction.finiteMode (construction.mode t history)) = 0 := by
      cases hmode : construction.mode t history with
      | draw phase =>
          cases phase <;> rfl
      | active k choice remaining => rfl
      | finalActive => rfl
      | absorbed origin =>
          cases origin with
          | none => rfl
          | some k => exact False.elim (habsorbed ⟨k, hmode⟩)
    have hpartition := construction.continueIndicators_partition
      (construction.finiteMode (construction.mode t history))
    rw [habsorbedZero, zero_add] at hpartition
    linarith [construction.epsilon_pos]

theorem KiloblockConstruction.publicFinitePayoff_deviation_le_potential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1)
    (t : ℕ) :
    publicFinitePayoff table construction.profile.signalLaw
        (Function.update construction.profile.strategy excluded.1 deviation)
        t excluded.1 ≤
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        (Function.update construction.profile.strategy excluded.1 deviation)
          .draw t)
        (construction.normalDeviationPotential excluded t) + ε +
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        (Function.update construction.profile.strategy excluded.1 deviation)
          .draw t)
        (fun history => construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history))) +
      construction.deviationTransientMass excluded deviation t := by
  rw [← expect_publicHistoryPayoff_eq_publicFinitePayoff table
    construction.profile.signalLaw
    (Function.update construction.profile.strategy excluded.1 deviation)
    t excluded.1]
  let distribution := (publicQuittingGame table
    construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded.1 deviation)
        .draw t
  have hmono : expect distribution (fun history => match history.2 with
        | .absorbed quitters => table.terminal quitters excluded.1
        | _ => table.never excluded.1) ≤
      expect distribution (fun history =>
        construction.normalDeviationPotential excluded t history + ε +
          construction.continuePastFinalIndicator
            (construction.finiteMode (construction.mode t history)) +
          Math.Probability.transientCharge construction.continueBoundary
            (construction.finiteMode (construction.mode t history))) := by
    apply Math.Probability.expect_mono
    exact fun history =>
      construction.publicHistoryPayoff_le_deviationPotential excluded history
  rw [Math.Probability.expect_add,
    Math.Probability.expect_add,
    Math.Probability.expect_add,
    Math.Probability.expect_const] at hmono
  unfold KiloblockConstruction.deviationTransientMass
  dsimp only [distribution] at hmono
  convert hmono using 1
  congr 1
  funext history
  cases history.2 <;> rfl

theorem KiloblockConstruction.normalDeviationPotential_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table) :
    construction.normalDeviationPotential excluded 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw) =
      construction.normalTarget excluded +
        construction.trackingCorrection
          (Fin.last construction.blockCount) excluded + 2 * ε := by
  simp only [KiloblockConstruction.normalDeviationPotential,
    construction.mode_initial,
    KiloblockConstruction.normalEndpointPotential,
    KiloblockConstruction.normalPotential,
    KiloblockConstruction.normalTarget]

theorem KiloblockConstruction.normalDeviationPayoff_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : NormalPlayer table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded.1) :
    publicQuittingPayoff table construction.profile.signalLaw
        (Function.update construction.profile.strategy excluded.1 deviation)
        excluded.1 ≤
      construction.normalTarget excluded + 5 * ε := by
  apply le_of_forall_pos_le_add
  intro δ hδ
  let strategy :=
    Function.update construction.profile.strategy excluded.1 deviation
  let payoff := publicQuittingPayoff table construction.profile.signalLaw
    strategy excluded.1
  let finitePayoff := fun t => publicFinitePayoff table
    construction.profile.signalLaw strategy t excluded.1
  have hpayEventually : ∀ᶠ t in atTop,
      |finitePayoff t - payoff| < δ / 2 := by
    have htendsto : Tendsto finitePayoff atTop (nhds payoff) := by
      dsimp only [finitePayoff, payoff, strategy]
      exact tendsto_publicFinitePayoff table construction.profile.signalLaw
        (Function.update construction.profile.strategy excluded.1 deviation)
        excluded.1
    have hevent := htendsto.eventually
      (Metric.ball_mem_nhds _ (half_pos hδ))
    filter_upwards [hevent] with t ht
    simpa only [Metric.mem_ball, Real.dist_eq] using ht
  obtain ⟨start, hstart⟩ := eventually_atTop.1 hpayEventually
  obtain ⟨t, ht, htransient⟩ :=
    construction.exists_deviationTransientMass_lt_after
      excluded deviation (half_pos hδ) start
  have hpayClose := hstart t ht
  have hfiniteBound :=
    construction.publicFinitePayoff_deviation_le_potential
      excluded deviation t
  have hpotential :=
    construction.expect_normalDeviationPotential_le_initial
      excluded deviation t
  rw [construction.normalDeviationPotential_initial excluded] at hpotential
  have hcorrection := construction.abs_trackingCorrection_lt
    (Fin.last construction.blockCount) excluded
  have hpast := construction.expect_deviationPastFinal_lt_epsilon
    excluded deviation t
  dsimp only [strategy, finitePayoff, payoff] at hpayClose ⊢
  linarith [(abs_lt.mp hpayClose).1, (abs_lt.mp hcorrection).2]

/-! The abnormal-player paragraph truncates at the final kiloblock.  The
positive part is harmless on prescribed pre-final absorption (Lemma 2.6
makes that payoff positive) and gives a globally nonnegative continuation
value for the stopping comparison. -/
def KiloblockConstruction.positiveTruncatedPayoff
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) (who : ι) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => match construction.mode t history with
    | .absorbed (some _) => match history.2 with
        | .absorbed quitters => max (table.terminal quitters who) 0
        | _ => 0
    | _ => 0

def KiloblockConstruction.finiteTruncatedContinuation
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (who : ι) : ℕ →
      (publicQuittingGame table
        construction.profile.signalLaw).HistoryPotential
  | 0 => construction.positiveTruncatedPayoff who
  | fuel + 1 => fun _ history =>
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU strategy
          (construction.finiteTruncatedContinuation strategy who fuel) history

private theorem kiloblockModeStep_next_unique
    {table : Table ι} {Signal : Type} [Fintype Signal] {lastIndex : ℕ}
    (mesh : Fin (lastIndex + 1) → ℕ)
    (selector : Fin (lastIndex + 1) → Signal →
      Option (NormalPlayer table))
    (continuation : Fin (lastIndex + 1) → NormalPlayer table →
      AttemptContinuation)
    (mode : KiloblockMode table lastIndex) (action : ι → Bool)
    (nextState : PublicQuittingState ι Signal)
    {left right : KiloblockMode table lastIndex}
    (hleft : KiloblockModeStep mesh selector continuation
      mode action nextState left)
    (hright : KiloblockModeStep mesh selector continuation
      mode action nextState right) :
    left = right := by
  classical
  cases mode with
  | draw phase =>
      cases phase <;> cases nextState <;>
        simp [KiloblockModeStep] at hleft hright ⊢ <;>
        exact hleft.trans hright.symm
  | active k choice remaining =>
      by_cases hquit : ({who | action who = true} : Finset ι).Nonempty <;>
        by_cases hremaining : remaining ≤ 1 <;>
        cases nextState <;>
        simp [KiloblockModeStep, hquit, hremaining] at hleft hright ⊢ <;>
        first
        | exact hleft.trans hright.symm
        | exact hleft.2.trans hright.2.symm
  | finalActive =>
      by_cases hquit : ({who | action who = true} : Finset ι).Nonempty <;>
        cases nextState <;>
        simp [KiloblockModeStep, hquit] at hleft hright ⊢ <;>
        first
        | exact hleft.trans hright.symm
        | exact hleft.2.trans hright.2.symm
  | absorbed origin =>
      cases nextState <;> simp [KiloblockModeStep] at hleft hright ⊢
      all_goals exact hleft.trans hright.symm

theorem KiloblockConstruction.finiteTruncatedContinuation_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (who : ι) : ∀ fuel t
      (history : (publicQuittingGame table
        construction.profile.signalLaw).Hist t),
      0 ≤ construction.finiteTruncatedContinuation
        strategy who fuel t history
  | 0, t, history => by
      unfold KiloblockConstruction.finiteTruncatedContinuation
        KiloblockConstruction.positiveTruncatedPayoff
      split
      · split <;> positivity
      · exact le_rfl
  | fuel + 1, t, history => by
      unfold KiloblockConstruction.finiteTruncatedContinuation
        StochasticGame.historyContinuationEU
      apply expect_nonneg
      intro action
      apply expect_nonneg
      intro nextState
      exact construction.finiteTruncatedContinuation_nonneg
        strategy who fuel (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)

theorem KiloblockConstruction.finiteTruncatedContinuation_absorbed
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (who : ι) : ∀ fuel t
      (history : (publicQuittingGame table
        construction.profile.signalLaw).Hist t)
      (origin : Option (Fin (construction.blockCount + 1)))
      (quitters : {S : Finset ι // S.Nonempty}),
      construction.mode t history = .absorbed origin →
      history.2 = PublicQuittingState.absorbed quitters →
      construction.finiteTruncatedContinuation
          strategy who fuel t history =
        origin.elim 0 (fun _ => max (table.terminal quitters who) 0)
  | 0, t, history, origin, quitters, hmode, hstate => by
      cases origin <;>
        simp [KiloblockConstruction.finiteTruncatedContinuation,
          KiloblockConstruction.positiveTruncatedPayoff, hmode, hstate]
  | fuel + 1, t, history, origin, quitters, hmode, hstate => by
      let absorbedState : (publicQuittingGame table
          construction.profile.signalLaw).State :=
        PublicQuittingState.absorbed quitters
      have hstate' : history.2 = absorbedState := by
        simpa [absorbedState] using hstate
      unfold KiloblockConstruction.finiteTruncatedContinuation
        StochasticGame.historyContinuationEU
      calc
        expect ((publicQuittingGame table
            construction.profile.signalLaw).stageActionDist strategy history)
          (fun action => expect ((publicQuittingGame table
              construction.profile.signalLaw).transition history.2 action)
            (fun nextState => construction.finiteTruncatedContinuation
              strategy who fuel (t + 1)
                (Fin.snoc history.1 (history.2, action), nextState))) =
          expect ((publicQuittingGame table
              construction.profile.signalLaw).stageActionDist strategy history)
            (fun _ => origin.elim 0
              (fun _ => max (table.terminal quitters who) 0)) := by
          apply Math.ProbabilityMassFunction.expect_congr_on_support
          intro action _haction
          have htransition : (publicQuittingGame table
              construction.profile.signalLaw).transition history.2 action =
            PMF.pure absorbedState := by
            rw [hstate']
            simp [publicQuittingGame, absorbedState]
          rw [htransition, Math.Probability.expect_pure]
          have hsupport : absorbedState ∈
              ((publicQuittingGame table
                construction.profile.signalLaw).transition
                  history.2 action).support := by
            rw [htransition]
            exact (PMF.mem_support_pure_iff _ _).2 rfl
          have hstep := construction.mode_step t history action
            absorbedState hsupport
          have hnextMode : construction.mode (t + 1)
              (Fin.snoc history.1 (history.2, action),
                absorbedState) = .absorbed origin := by
            simpa [KiloblockModeStep, hmode] using hstep
          exact construction.finiteTruncatedContinuation_absorbed
            strategy who fuel (t + 1)
              (Fin.snoc history.1 (history.2, action),
                absorbedState)
              origin quitters hnextMode (by simp [absorbedState])
        _ = _ := Math.Probability.expect_const _ _

private theorem KiloblockConstruction.profileTruncatedContinuation_congr
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : ι) : ∀ fuel t
      (left right : (publicQuittingGame table
        construction.profile.signalLaw).Hist t),
      construction.mode t left = construction.mode t right →
      left.2 = right.2 →
      construction.finiteTruncatedContinuation
          construction.profile.strategy who fuel t left =
        construction.finiteTruncatedContinuation
          construction.profile.strategy who fuel t right
  | 0, t, left, right, hmode, hstate => by
      unfold KiloblockConstruction.finiteTruncatedContinuation
        KiloblockConstruction.positiveTruncatedPayoff
      rw [hmode, hstate]
  | fuel + 1, t, left, right, hmode, hstate => by
      unfold KiloblockConstruction.finiteTruncatedContinuation
        StochasticGame.historyContinuationEU
      have haction : (publicQuittingGame table
          construction.profile.signalLaw).stageActionDist
            construction.profile.strategy left =
          (publicQuittingGame table
            construction.profile.signalLaw).stageActionDist
              construction.profile.strategy right := by
        unfold StochasticGame.stageActionDist
        congr 1
        funext player
        rw [construction.strategy_eq, construction.strategy_eq, hmode]
      rw [haction]
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro action _haction
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition left.2 action =
        (publicQuittingGame table
          construction.profile.signalLaw).transition right.2 action := by
        rw [hstate]
      rw [htransition]
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro nextState hnextState
      have hleftSupport : nextState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              left.2 action).support := by
        rwa [htransition]
      have hleftStep := construction.mode_step t left action
        nextState hleftSupport
      have hrightStep := construction.mode_step t right action
        nextState hnextState
      rw [hmode] at hleftStep
      have hnextMode := kiloblockModeStep_next_unique
        construction.mesh construction.signalSelector
          (fun block owner => (construction.attempt block owner).continuation)
          (construction.mode t right) action nextState
          hleftStep hrightStep
      exact construction.profileTruncatedContinuation_congr who fuel (t + 1)
        (Fin.snoc left.1 (left.2, action), nextState)
        (Fin.snoc right.1 (right.2, action), nextState)
        hnextMode rfl

theorem KiloblockConstruction.strategy_abnormal_continue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : ι) (habnormal : who ∉ NormalPlayers table)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    construction.profile.strategy who t history = PMF.pure false := by
  rw [construction.strategy_eq]
  cases hmode : construction.mode t history with
  | draw phase => with_unfolding_all rfl
  | finalActive => with_unfolding_all rfl
  | absorbed origin => with_unfolding_all rfl
  | active k choice remaining =>
      cases choice with
      | none => with_unfolding_all rfl
      | some owner =>
          have hne : who ≠ owner.1 := by
            intro heq
            subst who
            exact habnormal owner.2
          with_unfolding_all simp [hne]
          with_unfolding_all rfl

theorem KiloblockConstruction.update_abnormal_continue_eq_profile
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (who : ι) (habnormal : who ∉ NormalPlayers table) :
    Function.update construction.profile.strategy who
        (fun _ _ => PMF.pure false) =
      construction.profile.strategy := by
  funext player t history
  by_cases hplayer : player = who
  · subst player
    rw [Function.update_self]
    exact (construction.strategy_abnormal_continue who habnormal history).symm
  · rw [Function.update_of_ne hplayer]

private theorem KiloblockConstruction.stageActionDist_abnormalPureQuit_none
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k none remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true)) history =
      PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded true) := by
  unfold StochasticGame.stageActionDist
  rw [show (fun who => Function.update construction.profile.strategy excluded
      (fun _ _ => PMF.pure true) who t history) =
      fun who => PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded true who) by
    funext who
    by_cases hwho : who = excluded
    · subst who
      simp [publicSingleQuitAction]
    · rw [Function.update_of_ne hwho, construction.strategy_eq]
      simp [hmode, publicSingleQuitAction, hwho]]
  exact Math.PMFProduct.pmfPi_pure _

private theorem KiloblockConstruction.stageActionDist_abnormalPure_draw
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (phase : KiloblockPhase table construction.blockCount) (action : Bool)
    (hmode : construction.mode t history = .draw phase) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure action)) history =
      PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded action) := by
  unfold StochasticGame.stageActionDist
  rw [show (fun who => Function.update construction.profile.strategy excluded
      (fun _ _ => PMF.pure action) who t history) =
      fun who => PMF.pure (publicSingleQuitAction table
        construction.profile.signalLaw excluded action who) by
    funext who
    by_cases hwho : who = excluded
    · subst who
      simp [publicSingleQuitAction]
    · rw [Function.update_of_ne hwho, construction.strategy_eq]
      simp [hmode, publicSingleQuitAction, hwho]]
  exact Math.PMFProduct.pmfPi_pure _

private theorem KiloblockConstruction.profileTruncated_draw_pureQuit_eq_continue
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (fuel : ℕ) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (phase : KiloblockPhase table construction.blockCount)
    (hmode : construction.mode t history = .draw phase) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true))
        (construction.finiteTruncatedContinuation
          construction.profile.strategy excluded fuel) history =
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          construction.profile.strategy
          (construction.finiteTruncatedContinuation
            construction.profile.strategy excluded fuel) history := by
  classical
  have hstate : history.2 = PublicQuittingState.draw := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
  have hquit := construction.stageActionDist_abnormalPure_draw
    excluded history phase true hmode
  have hcontinue := construction.stageActionDist_abnormalPure_draw
    excluded history phase false hmode
  rw [construction.update_abnormal_continue_eq_profile
    excluded habnormal] at hcontinue
  unfold StochasticGame.historyContinuationEU
  rw [hquit, hcontinue, Math.Probability.expect_pure,
    Math.Probability.expect_pure]
  let quitAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded true
  let continueAction := publicSingleQuitAction table
    construction.profile.signalLaw excluded false
  rw [hstate]
  change expect (construction.profile.signalLaw.map
      PublicQuittingState.active)
        (fun nextState => construction.finiteTruncatedContinuation
          construction.profile.strategy excluded fuel (t + 1)
            (Fin.snoc history.1
              (PublicQuittingState.draw, quitAction), nextState)) =
    expect (construction.profile.signalLaw.map PublicQuittingState.active)
      (fun nextState => construction.finiteTruncatedContinuation
        construction.profile.strategy excluded fuel (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.draw, continueAction), nextState))
  rw [Math.Probability.expect_map, Math.Probability.expect_map]
  apply Math.ProbabilityMassFunction.expect_congr_on_support
  intro signal hsignal
  have hquitSupport : PublicQuittingState.active signal ∈
      ((publicQuittingGame table
        construction.profile.signalLaw).transition
          PublicQuittingState.draw quitAction).support := by
    change PublicQuittingState.active signal ∈
      (construction.profile.signalLaw.map PublicQuittingState.active).support
    rw [PMF.mem_support_map_iff]
    exact ⟨signal, hsignal, rfl⟩
  have hcontinueSupport : PublicQuittingState.active signal ∈
      ((publicQuittingGame table
        construction.profile.signalLaw).transition
          PublicQuittingState.draw continueAction).support := by
    change PublicQuittingState.active signal ∈
      (construction.profile.signalLaw.map PublicQuittingState.active).support
    rw [PMF.mem_support_map_iff]
    exact ⟨signal, hsignal, rfl⟩
  have hquitSupport' := hquitSupport
  have hcontinueSupport' := hcontinueSupport
  rw [← hstate] at hquitSupport' hcontinueSupport'
  have hquitStep := construction.mode_step t history quitAction
    (PublicQuittingState.active signal) hquitSupport'
  have hcontinueStep := construction.mode_step t history continueAction
    (PublicQuittingState.active signal) hcontinueSupport'
  have hquitStep' : construction.mode (t + 1)
      (Fin.snoc history.1
        (PublicQuittingState.draw, quitAction),
          PublicQuittingState.active signal) =
      construction.mode (t + 1)
        (Fin.snoc history.1
          (history.2, quitAction), PublicQuittingState.active signal) := by
    rw [hstate]
  have hcontinueStep' : construction.mode (t + 1)
      (Fin.snoc history.1
        (PublicQuittingState.draw, continueAction),
          PublicQuittingState.active signal) =
      construction.mode (t + 1)
        (Fin.snoc history.1
          (history.2, continueAction), PublicQuittingState.active signal) := by
    rw [hstate]
  have hnextMode : construction.mode (t + 1)
      (Fin.snoc history.1
        (PublicQuittingState.draw, quitAction),
          PublicQuittingState.active signal) =
    construction.mode (t + 1)
      (Fin.snoc history.1
        (PublicQuittingState.draw, continueAction),
          PublicQuittingState.active signal) := by
    rw [hquitStep', hcontinueStep']
    cases phase <;>
      simp [KiloblockModeStep, hmode] at hquitStep hcontinueStep
    all_goals rw [hquitStep, hcontinueStep]
  exact construction.profileTruncatedContinuation_congr excluded fuel (t + 1)
    (Fin.snoc history.1
      (PublicQuittingState.draw, quitAction),
        PublicQuittingState.active signal)
    (Fin.snoc history.1
      (PublicQuittingState.draw, continueAction),
        PublicQuittingState.active signal)
    hnextMode rfl

private theorem KiloblockConstruction.stageActionDist_abnormalPureQuit_some
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (owner : NormalPlayer table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1)) (remaining : ℕ)
    (howner : owner.1 ≠ excluded)
    (hmode : construction.mode t history =
      .active k (some owner) remaining) :
    (publicQuittingGame table construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true)) history =
      (quittingMeshHazardCoin (construction.attempt k owner).quitWeight
        (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one).bind
          (fun quits => PMF.pure (Function.update
            (publicSingleQuitAction table construction.profile.signalLaw
              owner.1 quits) excluded true)) := by
  classical
  let coin := quittingMeshHazardCoin
    (construction.attempt k owner).quitWeight (construction.mesh k)
    (construction.attempt k owner).quitWeight_pos.le
    (construction.attempt k owner).quitWeight_lt_one
  let base : ∀ who,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act who) :=
    fun who => if who = excluded then PMF.pure true else PMF.pure false
  have hfamily :
      (fun who => Function.update construction.profile.strategy excluded
        (fun _ _ => PMF.pure true) who t history) =
        Function.update base owner.1 coin := by
    funext who
    by_cases hwhoExcluded : who = excluded
    · subst who
      rw [Function.update_self, Function.update_of_ne howner.symm]
      simp [base]
    · rw [Function.update_of_ne hwhoExcluded]
      by_cases hwhoOwner : who = owner.1
      · subst who
        simp [construction.strategy_eq, hmode, coin, base]
      · rw [Function.update_of_ne hwhoOwner, construction.strategy_eq]
        simp [hmode, hwhoOwner, base, hwhoExcluded]
  unfold StochasticGame.stageActionDist
  rw [hfamily, Math.PMFProduct.pmfPi_update_bind]
  apply congrArg (PMF.bind coin)
  funext quits
  rw [show Function.update base owner.1 (PMF.pure quits) =
      fun who => PMF.pure (Function.update
        (publicSingleQuitAction table construction.profile.signalLaw
          owner.1 quits) excluded true who) by
    funext who
    by_cases hwhoOwner : who = owner.1
    · subst who
      simp [base, publicSingleQuitAction, howner]
    · rw [Function.update_of_ne hwhoOwner]
      by_cases hwhoExcluded : who = excluded
      · subst who
        simp [base]
      · simp [base, hwhoExcluded, publicSingleQuitAction, hwhoOwner]]
  exact Math.PMFProduct.pmfPi_pure _

private theorem KiloblockConstruction.abnormal_pureQuit_positivePayoff_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true))
        (fun _ nextHistory => match nextHistory.2 with
          | .absorbed quitters => max (table.terminal quitters excluded) 0
          | _ => 0) history ≤ ε := by
  classical
  have hstate : ∃ signal, history.2 = PublicQuittingState.active signal := by
    have hmatches := construction.mode_state t history
    cases hs : history.2 <;>
      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨signal, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  cases choice with
  | none =>
      have haction := construction.stageActionDist_abnormalPureQuit_none
        excluded (past, PublicQuittingState.active signal)
          k remaining hmode
      unfold StochasticGame.historyContinuationEU
      rw [haction, Math.Probability.expect_pure,
        publicQuittingGame_transition_publicSingleQuitAction,
        Math.Probability.expect_pure]
      change max (table.terminal
        (quittingProjectiveSingletonTerminal excluded) excluded) 0 ≤ ε
      rw [construction.soloExitNormalized excluded]
      simp [construction.epsilon_pos.le]
  | some owner =>
      have howner : owner.1 ≠ excluded := by
        intro heq
        subst excluded
        exact habnormal owner.2
      have haction := construction.stageActionDist_abnormalPureQuit_some
        excluded owner (past, PublicQuittingState.active signal)
          k remaining howner hmode
      unfold StochasticGame.historyContinuationEU
      rw [haction, Math.Probability.expect_bind]
      simp_rw [Math.Probability.expect_pure]
      let coin := quittingMeshHazardCoin
        (construction.attempt k owner).quitWeight (construction.mesh k)
        (construction.attempt k owner).quitWeight_pos.le
        (construction.attempt k owner).quitWeight_lt_one
      change expect coin (fun quits => expect
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              (PublicQuittingState.active signal)
              (Function.update (publicSingleQuitAction table
                construction.profile.signalLaw owner.1 quits)
                excluded true))
          (fun nextState => match nextState with
            | .absorbed quitters => max
                (table.terminal quitters excluded) 0
            | _ => 0)) ≤ ε
      calc
        expect coin (fun quits => expect
            ((publicQuittingGame table
              construction.profile.signalLaw).transition
                (PublicQuittingState.active signal)
                (Function.update (publicSingleQuitAction table
                  construction.profile.signalLaw owner.1 quits)
                  excluded true))
            (fun nextState => match nextState with
              | .absorbed quitters => max
                  (table.terminal quitters excluded) 0
              | _ => 0)) ≤
            expect coin (fun quits => if quits then 1 else 0) := by
          apply Math.Probability.expect_mono
          intro quits
          cases quits with
          | false =>
              have hactionEq : Function.update
                  (publicSingleQuitAction table
                    construction.profile.signalLaw owner.1 false)
                  excluded true =
                  publicSingleQuitAction table
                    construction.profile.signalLaw excluded true := by
                funext who
                by_cases hwho : who = excluded
                · subst who
                  simp [publicSingleQuitAction]
                · simp [publicSingleQuitAction, hwho]
              rw [hactionEq,
                publicQuittingGame_transition_publicSingleQuitAction,
                Math.Probability.expect_pure]
              with_unfolding_all
                change max (table.terminal
                  (quittingProjectiveSingletonTerminal excluded) excluded) 0 ≤ 0
              rw [construction.soloExitNormalized excluded]
              simp
          | true =>
              simp only [publicQuittingGame]
              split
              · rw [Math.Probability.expect_pure]
                apply max_le
                · exact (le_abs_self _).trans
                    (construction.payoffsBounded.1 _ excluded)
                · exact zero_le_one
              · rename_i hfalse
                exfalso
                apply hfalse
                refine ⟨excluded, ?_⟩
                simp
        _ = quittingMeshHazard
            (construction.attempt k owner).quitWeight
              (construction.mesh k) := by
          rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
          simp [coin]
        _ ≤ ε := (construction.mesh_hazard_small k owner).le

private theorem KiloblockConstruction.abnormal_pureQuit_continuation_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (continuationStrategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (fuel : ℕ) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    (hmode : construction.mode t history = .active k choice remaining) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true))
        (construction.finiteTruncatedContinuation
          continuationStrategy excluded fuel) history ≤ ε := by
  classical
  let quitStrategy := Function.update construction.profile.strategy excluded
    (fun _ _ => PMF.pure true)
  let terminal : (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
    fun _ nextHistory => match nextHistory.2 with
      | .absorbed quitters => max (table.terminal quitters excluded) 0
      | _ => 0
  have hcongr : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU quitStrategy
        (construction.finiteTruncatedContinuation
          continuationStrategy excluded fuel) history =
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU quitStrategy
          terminal history := by
    apply historyContinuationEU_congr_next_profile
    intro action haction nextState hnextState
    have hactionQuit : action excluded = true := by
      unfold quitStrategy StochasticGame.stageActionDist at haction
      let root : ∀ player,
          PMF ((publicQuittingGame table
            construction.profile.signalLaw).Act player) :=
        fun player => construction.profile.strategy player t history
      have hfamily :
          (fun player => Function.update construction.profile.strategy
            excluded (fun _ _ => PMF.pure true) player t history) =
            Function.update root excluded (PMF.pure true) := by
        funext player
        by_cases hplayer : player = excluded
        · subst player
          simp [root]
        · simp [root, Function.update_of_ne hplayer]
      rw [hfamily] at haction
      by_contra hne
      have hzero : Math.PMFProduct.pmfPi
          (Function.update root excluded (PMF.pure true)) action = 0 := by
        rw [Math.PMFProduct.pmfPi_apply]
        apply Finset.prod_eq_zero (Finset.mem_univ excluded)
        rw [Function.update_self, PMF.pure_apply, if_neg hne]
      exact (PMF.mem_support_iff _ action).mp haction hzero
    change (show Bool from action excluded) = true at hactionQuit
    have hstep := construction.mode_step t history action nextState hnextState
    simp only [KiloblockModeStep, hmode] at hstep
    split at hstep
    case isTrue hquit =>
      have hnextMode := hstep.2
      have hnextState := hstep.1
      cases hs : nextState with
      | draw => simp [hs] at hnextState
      | active signal => simp [hs] at hnextState
      | absorbed quitters =>
          have hnextMode' : construction.mode (t + 1)
              (Fin.snoc history.1 (history.2, action),
                PublicQuittingState.absorbed quitters) =
              .absorbed (some k) := by
            simpa only [hs] using hnextMode
          rw [construction.finiteTruncatedContinuation_absorbed
            continuationStrategy excluded fuel (t + 1)
              (Fin.snoc history.1
                (history.2, action), PublicQuittingState.absorbed quitters)
              (some k) quitters hnextMode' rfl]
          rfl
    case isFalse hfalse =>
      exfalso
      apply hfalse
      refine ⟨excluded, ?_⟩
      simpa only [Finset.mem_filter, Finset.mem_univ, true_and] using
        hactionQuit
  rw [hcongr]
  exact construction.abnormal_pureQuit_positivePayoff_le
    excluded habnormal history k choice remaining hmode

private theorem historyContinuationEU_le_add_const
    {table : Table ι} {Signal : Type} [Fintype Signal]
    (signalLaw : PMF Signal)
    (strategy : (publicQuittingGame table signalLaw).BehaviorProfile)
    (left right : (publicQuittingGame table signalLaw).HistoryPotential)
    (constant : ℝ) {t : ℕ}
    (history : (publicQuittingGame table signalLaw).Hist t)
    (hle : ∀ action nextState,
      left (t + 1) (Fin.snoc history.1
          (history.2, action), nextState) ≤
        right (t + 1) (Fin.snoc history.1
          (history.2, action), nextState) + constant) :
    (publicQuittingGame table signalLaw).historyContinuationEU
        strategy left history ≤
      (publicQuittingGame table signalLaw).historyContinuationEU
        strategy right history + constant := by
  unfold StochasticGame.historyContinuationEU
  calc
    expect ((publicQuittingGame table signalLaw).stageActionDist
        strategy history) (fun action =>
      expect ((publicQuittingGame table signalLaw).transition
          history.2 action) (fun nextState =>
        left (t + 1) (Fin.snoc history.1
          (history.2, action), nextState))) ≤
      expect ((publicQuittingGame table signalLaw).stageActionDist
        strategy history) (fun action =>
      expect ((publicQuittingGame table signalLaw).transition
          history.2 action) (fun nextState =>
        right (t + 1) (Fin.snoc history.1
          (history.2, action), nextState) + constant)) := by
      apply Math.Probability.expect_mono
      intro action
      apply Math.Probability.expect_mono
      exact hle action
    _ = expect ((publicQuittingGame table signalLaw).stageActionDist
          strategy history) (fun action =>
        expect ((publicQuittingGame table signalLaw).transition
          history.2 action) (fun nextState =>
            right (t + 1) (Fin.snoc history.1
              (history.2, action), nextState)) + constant) := by
      apply congrArg (expect ((publicQuittingGame table
        signalLaw).stageActionDist strategy history))
      funext action
      rw [Math.Probability.expect_add, Math.Probability.expect_const]
    _ = _ := by
      rw [Math.Probability.expect_add, Math.Probability.expect_const]

private theorem KiloblockConstruction.finiteTruncated_absorbed_step
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (currentStrategy tailStrategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (who : ι) (fuel : ℕ) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (origin : Option (Fin (construction.blockCount + 1)))
    (quitters : {S : Finset ι // S.Nonempty})
    (hmode : construction.mode t history = .absorbed origin)
    (hstate : history.2 = PublicQuittingState.absorbed quitters) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU currentStrategy
        (construction.finiteTruncatedContinuation tailStrategy who fuel)
        history =
      origin.elim 0 (fun _ => max (table.terminal quitters who) 0) := by
  unfold StochasticGame.historyContinuationEU
  calc
    expect ((publicQuittingGame table
        construction.profile.signalLaw).stageActionDist currentStrategy history)
      (fun action => expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 action)
        (fun nextState => construction.finiteTruncatedContinuation
          tailStrategy who fuel (t + 1)
            (Fin.snoc history.1 (history.2, action), nextState))) =
      expect ((publicQuittingGame table
          construction.profile.signalLaw).stageActionDist currentStrategy history)
        (fun _ => origin.elim 0
          (fun _ => max (table.terminal quitters who) 0)) := by
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro action _haction
      let absorbedState : (publicQuittingGame table
          construction.profile.signalLaw).State :=
        PublicQuittingState.absorbed quitters
      have hstate' : history.2 = absorbedState := by
        simpa [absorbedState] using hstate
      have htransition : (publicQuittingGame table
          construction.profile.signalLaw).transition history.2 action =
        PMF.pure absorbedState := by
        rw [hstate']
        simp [publicQuittingGame, absorbedState]
      rw [htransition, Math.Probability.expect_pure]
      have hsupport : absorbedState ∈
          ((publicQuittingGame table
            construction.profile.signalLaw).transition
              history.2 action).support := by
        rw [htransition]
        exact (PMF.mem_support_pure_iff _ _).2 rfl
      have hstep := construction.mode_step t history action
        absorbedState hsupport
      have hnextMode : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, action), absorbedState) =
          .absorbed origin := by
        simpa [KiloblockModeStep, hmode] using hstep
      exact construction.finiteTruncatedContinuation_absorbed
        tailStrategy who fuel (t + 1)
          (Fin.snoc history.1 (history.2, action), absorbedState)
          origin quitters hnextMode (by simp [absorbedState])
    _ = _ := Math.Probability.expect_const _ _

theorem KiloblockConstruction.finiteTruncatedDeviation_le_profile_add
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded) : ∀ fuel t
      (history : (publicQuittingGame table
        construction.profile.signalLaw).Hist t),
      construction.finiteTruncatedContinuation
          (Function.update construction.profile.strategy excluded deviation)
          excluded fuel t history ≤
        construction.finiteTruncatedContinuation
          construction.profile.strategy excluded fuel t history + ε
  | 0, t, history => by
      unfold KiloblockConstruction.finiteTruncatedContinuation
      linarith [construction.epsilon_pos]
  | fuel + 1, t, history => by
      let deviationProfile :=
        Function.update construction.profile.strategy excluded deviation
      let deviationTail := construction.finiteTruncatedContinuation
        deviationProfile excluded fuel
      let profileTail := construction.finiteTruncatedContinuation
        construction.profile.strategy excluded fuel
      unfold KiloblockConstruction.finiteTruncatedContinuation
      rw [historyContinuationEU_update_eq_expect_pure table
        construction.profile.signalLaw construction.profile.strategy excluded
          deviation deviationTail history]
      calc
        expect (deviation t history) (fun action =>
            (publicQuittingGame table
              construction.profile.signalLaw).historyContinuationEU
                (Function.update construction.profile.strategy excluded
                  (fun _ _ => PMF.pure action))
                deviationTail history) ≤
          expect (deviation t history) (fun _ =>
            (publicQuittingGame table
              construction.profile.signalLaw).historyContinuationEU
                construction.profile.strategy profileTail history + ε) := by
          apply Math.Probability.expect_mono
          intro action
          cases action with
          | false =>
              rw [construction.update_abnormal_continue_eq_profile
                excluded habnormal]
              apply historyContinuationEU_le_add_const
                construction.profile.signalLaw construction.profile.strategy
                  deviationTail profileTail ε history
              intro jointAction nextState
              exact construction.finiteTruncatedDeviation_le_profile_add
                excluded habnormal deviation fuel (t + 1)
                  (Fin.snoc history.1
                    (history.2, jointAction), nextState)
          | true =>
              cases hmode : construction.mode t history with
              | active k choice remaining =>
                  have hquit :=
                    construction.abnormal_pureQuit_continuation_le
                      excluded habnormal deviationProfile fuel history
                        k choice remaining hmode
                  have hprofileNonneg :=
                    construction.finiteTruncatedContinuation_nonneg
                      construction.profile.strategy excluded (fuel + 1)
                        t history
                  dsimp only [profileTail, deviationTail, deviationProfile]
                    at hquit
                  have hprofileNonneg' : 0 ≤ (publicQuittingGame table
                      construction.profile.signalLaw).historyContinuationEU
                        construction.profile.strategy
                        (construction.finiteTruncatedContinuation
                          construction.profile.strategy excluded fuel)
                        history := by
                    simpa only [KiloblockConstruction.finiteTruncatedContinuation]
                      using hprofileNonneg
                  dsimp only [profileTail, deviationTail, deviationProfile]
                  linarith
              | draw phase =>
                  have htail := historyContinuationEU_le_add_const
                    construction.profile.signalLaw
                    (Function.update construction.profile.strategy excluded
                      (fun _ _ => PMF.pure true))
                    deviationTail profileTail ε history (by
                      intro jointAction nextState
                      exact construction.finiteTruncatedDeviation_le_profile_add
                        excluded habnormal deviation fuel (t + 1)
                          (Fin.snoc history.1
                            (history.2, jointAction), nextState))
                  have hbaseline :=
                    construction.profileTruncated_draw_pureQuit_eq_continue
                      excluded habnormal fuel history phase hmode
                  dsimp only [profileTail, deviationTail, deviationProfile]
                    at htail hbaseline
                  dsimp only [profileTail, deviationTail, deviationProfile]
                  rw [hbaseline] at htail
                  exact htail
              | absorbed origin =>
                  have hstate : ∃ quitters, history.2 =
                      PublicQuittingState.absorbed quitters := by
                    have hmatches := construction.mode_state t history
                    cases hs : history.2 <;>
                      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
                    exact ⟨_, rfl⟩
                  obtain ⟨quitters, hstate⟩ := hstate
                  have hleft := construction.finiteTruncated_absorbed_step
                    (Function.update construction.profile.strategy excluded
                      (fun _ _ => PMF.pure true))
                    deviationProfile excluded fuel history origin quitters
                      hmode hstate
                  have hright :=
                    construction.finiteTruncatedContinuation_absorbed
                      construction.profile.strategy excluded (fuel + 1) t
                        history origin quitters hmode hstate
                  have hright' : (publicQuittingGame table
                      construction.profile.signalLaw).historyContinuationEU
                        construction.profile.strategy profileTail history =
                      origin.elim 0
                        (fun _ => max (table.terminal quitters excluded) 0) := by
                    dsimp only [profileTail]
                    simpa only [KiloblockConstruction.finiteTruncatedContinuation]
                      using hright
                  dsimp only [profileTail, deviationTail, deviationProfile]
                    at hleft ⊢
                  rw [hleft, hright']
                  linarith [construction.epsilon_pos]
              | finalActive =>
                  have hstate : ∃ signal, history.2 =
                      PublicQuittingState.active signal := by
                    have hmatches := construction.mode_state t history
                    cases hs : history.2 <;>
                      simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
                    exact ⟨_, rfl⟩
                  obtain ⟨signal, hstate⟩ := hstate
                  have hzero : (publicQuittingGame table
                      construction.profile.signalLaw).historyContinuationEU
                        (Function.update construction.profile.strategy excluded
                          (fun _ _ => PMF.pure true))
                        deviationTail history = 0 := by
                    let jointAction := publicSingleQuitAction table
                      construction.profile.signalLaw excluded true
                    let absorbedState : (publicQuittingGame table
                        construction.profile.signalLaw).State :=
                      PublicQuittingState.absorbed
                        (quittingProjectiveSingletonTerminal excluded)
                    have haction : (publicQuittingGame table
                        construction.profile.signalLaw).stageActionDist
                          (Function.update construction.profile.strategy
                            excluded (fun _ _ => PMF.pure true)) history =
                        PMF.pure jointAction := by
                      unfold StochasticGame.stageActionDist
                      rw [show (fun who => Function.update
                          construction.profile.strategy excluded
                            (fun _ _ => PMF.pure true) who t history) =
                          fun who => PMF.pure (jointAction who) by
                        funext who
                        by_cases hwho : who = excluded
                        · subst who
                          simp [jointAction, publicSingleQuitAction]
                        · rw [Function.update_of_ne hwho,
                            construction.strategy_eq]
                          simp [hmode, jointAction,
                            publicSingleQuitAction, hwho]]
                      exact Math.PMFProduct.pmfPi_pure _
                    have htransition : (publicQuittingGame table
                        construction.profile.signalLaw).transition
                          history.2 jointAction = PMF.pure absorbedState := by
                      rw [hstate]
                      exact publicQuittingGame_transition_publicSingleQuitAction
                        table construction.profile.signalLaw signal excluded
                    have hsupport : absorbedState ∈
                        ((publicQuittingGame table
                          construction.profile.signalLaw).transition
                            history.2 jointAction).support := by
                      rw [htransition]
                      exact (PMF.mem_support_pure_iff _ _).2 rfl
                    have hstep := construction.mode_step t history jointAction
                      absorbedState hsupport
                    have hnextMode : construction.mode (t + 1)
                        (Fin.snoc history.1
                          (history.2, jointAction), absorbedState) =
                        .absorbed none := by
                      simp [KiloblockModeStep, hmode, jointAction,
                        absorbedState, publicSingleQuitAction_apply_eq_raw,
                        rawSingleQuitAction_true_quitters] at hstep
                      exact hstep.2
                    unfold StochasticGame.historyContinuationEU
                    rw [haction, Math.Probability.expect_pure,
                      htransition, Math.Probability.expect_pure]
                    dsimp only [deviationTail]
                    exact construction.finiteTruncatedContinuation_absorbed
                      deviationProfile excluded fuel (t + 1)
                        (Fin.snoc history.1
                          (history.2, jointAction), absorbedState)
                        none (quittingProjectiveSingletonTerminal excluded)
                        hnextMode (by simp [absorbedState])
                    -- At `finalActive`, forcing Quit absorbs on the
                    -- post-kiloblock side, whose truncated value is zero.
                  have hprofileNonneg :=
                    construction.finiteTruncatedContinuation_nonneg
                      construction.profile.strategy excluded (fuel + 1)
                        t history
                  have hprofileNonneg' : 0 ≤ (publicQuittingGame table
                      construction.profile.signalLaw).historyContinuationEU
                        construction.profile.strategy
                        (construction.finiteTruncatedContinuation
                          construction.profile.strategy excluded fuel)
                        history := by
                    simpa only [KiloblockConstruction.finiteTruncatedContinuation]
                      using hprofileNonneg
                  dsimp only [profileTail, deviationTail, deviationProfile]
                    at hzero
                  dsimp only [profileTail, deviationTail, deviationProfile]
                  linarith [construction.epsilon_pos]
        _ = _ := by
          rw [Math.Probability.expect_const]

theorem KiloblockConstruction.expect_finiteTruncatedContinuation_eq_future
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (who : ι) : ∀ fuel t,
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist strategy .draw t)
      (construction.finiteTruncatedContinuation strategy who fuel t) =
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist strategy .draw (t + fuel))
      (construction.positiveTruncatedPayoff who (t + fuel))
  | 0, t => rfl
  | fuel + 1, t => by
      change expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist strategy .draw t)
        (fun history => (publicQuittingGame table
          construction.profile.signalLaw).historyContinuationEU strategy
            (construction.finiteTruncatedContinuation strategy who fuel)
              history) = _
      rw [← (publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue_succ]
      unfold StochasticGame.expectedHistoryValue
      have htime : t + 1 + fuel = t + (fuel + 1) := by omega
      rw [← htime]
      exact construction.expect_finiteTruncatedContinuation_eq_future
        strategy who fuel (t + 1)

theorem KiloblockConstruction.finiteTruncatedContinuation_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (strategy : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorProfile)
    (who : ι) (fuel : ℕ) :
    construction.finiteTruncatedContinuation strategy who fuel 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw) =
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist strategy .draw fuel)
        (construction.positiveTruncatedPayoff who fuel) := by
  have hfuture := construction.expect_finiteTruncatedContinuation_eq_future
    strategy who fuel 0
  rw [(publicQuittingGame table
    construction.profile.signalLaw).histDist_zero,
      Math.Probability.expect_pure, zero_add] at hfuture
  exact hfuture

noncomputable def KiloblockConstruction.profileTransiencePotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode construction → ℝ :=
  (Classical.choice construction.profileKernel_transienceCertificate).potential

theorem KiloblockConstruction.profileTransiencePotential_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    0 ≤ construction.profileTransiencePotential mode :=
  (Classical.choice
    construction.profileKernel_transienceCertificate).potential_nonneg mode

theorem KiloblockConstruction.profileTransiencePotential_eq_zero_on_boundary
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    {mode : KiloblockFiniteMode construction}
    (hmode : mode ∈ construction.continueBoundary) :
    construction.profileTransiencePotential mode = 0 :=
  (Classical.choice construction.profileKernel_transienceCertificate
  ).potential_eq_zero_on_core hmode

theorem KiloblockConstruction.profileTransiencePotential_poisson
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    construction.profileTransiencePotential mode -
        expect (construction.profileFiniteModeKernel mode)
          construction.profileTransiencePotential =
      Math.Probability.transientCharge construction.continueBoundary mode :=
  (Classical.choice
    construction.profileKernel_transienceCertificate).poisson mode

noncomputable def KiloblockConstruction.profileTransienceHistoryPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => construction.profileTransiencePotential
    (construction.finiteMode (construction.mode t history))

theorem KiloblockConstruction.profileTransienceHistoryPotential_drift
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        construction.profileTransienceHistoryPotential history +
      Math.Probability.transientCharge construction.continueBoundary
        (construction.finiteMode (construction.mode t history)) =
      construction.profileTransienceHistoryPotential t history := by
  have hstep : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        construction.profileTransienceHistoryPotential history =
      expect (construction.profileFiniteModeKernel
          (construction.finiteMode (construction.mode t history)))
        construction.profileTransiencePotential := by
    unfold StochasticGame.historyContinuationEU
      KiloblockConstruction.profileTransienceHistoryPotential
    rw [← construction.finiteHistoryModeStepDist_profile_eq history]
    unfold KiloblockConstruction.historyModeStepDist
    rw [PMF.map_bind, Math.Probability.expect_bind]
    apply congrArg (expect ((publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history))
    funext action
    rw [PMF.map_comp, Math.Probability.expect_map]
    rfl
  rw [hstep]
  unfold KiloblockConstruction.profileTransienceHistoryPotential
  linarith [construction.profileTransiencePotential_poisson
    (construction.finiteMode (construction.mode t history))]

private theorem KiloblockConstruction.profileTransience_pureQuit_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true))
        construction.profileTransienceHistoryPotential history ≤
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          construction.profile.strategy
          construction.profileTransienceHistoryPotential history := by
  classical
  let actions : ∀ player,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act player) :=
    fun player => construction.profile.strategy player t history
  have hquitDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true)) history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded (PMF.pure true)) := by
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = excluded
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  have hcontinueDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded (PMF.pure false)) := by
    rw [← construction.update_abnormal_continue_eq_profile
      excluded habnormal]
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = excluded
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  unfold StochasticGame.historyContinuationEU
  rw [hquitDist, hcontinueDist,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded true,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded false,
    Math.Probability.expect_bind, Math.Probability.expect_bind]
  simp_rw [Math.Probability.expect_pure]
  apply Math.Probability.expect_mono
  intro action
  let quitAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded true
  let continueAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded false
  cases hmode : construction.mode t history with
  | active k choice remaining =>
      have hquitValue : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.profileTransienceHistoryPotential
            (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        have hnextMode : construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, quitAction), nextState) =
              .absorbed (some k) := by
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep
          case isTrue => exact hstep.2
          case isFalse hfalse =>
            exfalso
            apply hfalse
            refine ⟨excluded, ?_⟩
            simp [quitAction]
        unfold KiloblockConstruction.profileTransienceHistoryPotential
        rw [hnextMode]
        exact construction.profileTransiencePotential_eq_zero_on_boundary
          trivial
      rw [hquitValue]
      exact expect_nonneg _ _ fun nextState =>
        construction.profileTransiencePotential_nonneg
          (construction.finiteMode (construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState)))
  | finalActive =>
      have hleft : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.profileTransienceHistoryPotential
            (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        unfold KiloblockConstruction.profileTransienceHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep <;> rw [hstep.2]
        all_goals
          apply construction.profileTransiencePotential_eq_zero_on_boundary
          trivial
      have hright : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 continueAction)
          (fun nextState => construction.profileTransienceHistoryPotential
            (t + 1)
              (Fin.snoc history.1 (history.2, continueAction), nextState)) =
          0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history continueAction
          nextState hnext
        unfold KiloblockConstruction.profileTransienceHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep <;> rw [hstep.2]
        all_goals
          apply construction.profileTransiencePotential_eq_zero_on_boundary
          trivial
      rw [hleft, hright]
  | draw phase =>
      have hstate : history.2 = PublicQuittingState.draw := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
      rw [hstate]
      apply le_of_eq
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro nextState hnext
      have hquitStep := construction.mode_step t history quitAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hcontinueStep := construction.mode_step t history continueAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hm : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState) := by
        cases phase <;> cases hsnext : nextState <;>
          simp [KiloblockModeStep, hmode, hsnext] at hquitStep hcontinueStep
        all_goals rw [hquitStep, hcontinueStep]
      unfold KiloblockConstruction.profileTransienceHistoryPotential
      have hm' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.draw, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1
              (PublicQuittingState.draw, continueAction), nextState) := by
        simpa only [hstate] using hm
      rw [hm']
  | absorbed origin =>
      have hstate : ∃ quitters, history.2 =
          PublicQuittingState.absorbed quitters := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
        exact ⟨_, rfl⟩
      obtain ⟨quitters, hstate⟩ := hstate
      rw [hstate]
      simp only [publicQuittingGame, Math.Probability.expect_pure]
      have hquitStep := construction.mode_step t history quitAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      have hcontinueStep := construction.mode_step t history continueAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      simp only [KiloblockModeStep, hmode] at hquitStep hcontinueStep
      unfold KiloblockConstruction.profileTransienceHistoryPotential
      have hquitStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, quitAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hquitStep
      have hcontinueStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, continueAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hcontinueStep
      dsimp only [quitAction] at hquitStep'
      dsimp only [continueAction] at hcontinueStep'
      exact (congrArg construction.profileTransiencePotential
        (congrArg construction.finiteMode
          (hquitStep'.trans hcontinueStep'.symm))).le

theorem KiloblockConstruction.profileTransienceHistoryPotential_deviation_drift
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded deviation)
        construction.profileTransienceHistoryPotential history +
      Math.Probability.transientCharge construction.continueBoundary
        (construction.finiteMode (construction.mode t history)) ≤
      construction.profileTransienceHistoryPotential t history := by
  have hpure : (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded deviation)
        construction.profileTransienceHistoryPotential history ≤
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          construction.profile.strategy
          construction.profileTransienceHistoryPotential history := by
    rw [historyContinuationEU_update_eq_expect_pure table
      construction.profile.signalLaw construction.profile.strategy excluded
        deviation construction.profileTransienceHistoryPotential history]
    calc
      expect (deviation t history) (fun action =>
          (publicQuittingGame table
            construction.profile.signalLaw).historyContinuationEU
              (Function.update construction.profile.strategy excluded
                (fun _ _ => PMF.pure action))
              construction.profileTransienceHistoryPotential history) ≤
          expect (deviation t history) (fun _ =>
            (publicQuittingGame table
              construction.profile.signalLaw).historyContinuationEU
                construction.profile.strategy
                construction.profileTransienceHistoryPotential history) := by
        apply Math.Probability.expect_mono
        intro action
        cases action with
        | false =>
            rw [construction.update_abnormal_continue_eq_profile
              excluded habnormal]
        | true =>
            exact construction.profileTransience_pureQuit_le
              excluded habnormal history
      _ = _ := Math.Probability.expect_const _ _
  linarith [construction.profileTransienceHistoryPotential_drift history]

def KiloblockConstruction.abnormalDeviationTransientMass
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    (t : ℕ) : ℝ :=
  expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist
    (Function.update construction.profile.strategy excluded deviation)
      .draw t)
    (fun history => Math.Probability.transientCharge
      construction.continueBoundary
        (construction.finiteMode (construction.mode t history)))

def KiloblockConstruction.expectedProfileTransiencePotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    (t : ℕ) : ℝ :=
  expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist
    (Function.update construction.profile.strategy excluded deviation)
      .draw t)
    (construction.profileTransienceHistoryPotential t)

theorem KiloblockConstruction.expectedProfileTransiencePotential_drift
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    (t : ℕ) :
    construction.expectedProfileTransiencePotential excluded deviation (t + 1) +
      construction.abnormalDeviationTransientMass excluded deviation t ≤
    construction.expectedProfileTransiencePotential excluded deviation t := by
  unfold KiloblockConstruction.expectedProfileTransiencePotential
    KiloblockConstruction.abnormalDeviationTransientMass
  change (publicQuittingGame table
      construction.profile.signalLaw).expectedHistoryValue
        (Function.update construction.profile.strategy excluded deviation)
        .draw construction.profileTransienceHistoryPotential (t + 1) + _ ≤ _
  rw [(publicQuittingGame table
    construction.profile.signalLaw).expectedHistoryValue_succ]
  rw [← Math.Probability.expect_add]
  apply Math.Probability.expect_mono
  intro history
  exact construction.profileTransienceHistoryPotential_deviation_drift
    excluded habnormal deviation history

theorem KiloblockConstruction.sum_abnormalDeviationTransientMass_add_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded) : ∀ steps,
    (∑ t ∈ Finset.range steps,
        construction.abnormalDeviationTransientMass excluded deviation t) +
      construction.expectedProfileTransiencePotential
        excluded deviation steps ≤
      construction.profileTransienceHistoryPotential 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw)
  | 0 => by
      simp [KiloblockConstruction.expectedProfileTransiencePotential]
  | steps + 1 => by
      rw [Finset.sum_range_succ]
      have hinduction :=
        construction.sum_abnormalDeviationTransientMass_add_le
          excluded habnormal deviation steps
      have hdrift :=
        construction.expectedProfileTransiencePotential_drift
          excluded habnormal deviation steps
      linarith

theorem KiloblockConstruction.abnormalDeviationTransientMass_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    (t : ℕ) :
    0 ≤ construction.abnormalDeviationTransientMass excluded deviation t := by
  unfold KiloblockConstruction.abnormalDeviationTransientMass
  exact expect_nonneg _ _ fun history =>
    Math.Probability.transientCharge_nonneg construction.continueBoundary
      (construction.finiteMode (construction.mode t history))

theorem KiloblockConstruction.exists_abnormalDeviationTransientMass_lt_after
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    {threshold : ℝ} (hthreshold : 0 < threshold) (start : ℕ) :
    ∃ t, start ≤ t ∧
      construction.abnormalDeviationTransientMass excluded deviation t <
        threshold := by
  let initial := construction.profileTransienceHistoryPotential 0
    ((publicQuittingGame table
      construction.profile.signalLaw).emptyHist .draw)
  obtain ⟨span, hspan⟩ := exists_nat_gt (initial / threshold)
  have hlarge : initial < (span : ℝ) * threshold := by
    exact (div_lt_iff₀ hthreshold).mp hspan
  by_contra hnot
  push Not at hnot
  have hlower : (span : ℝ) * threshold ≤
      ∑ t ∈ Finset.Ico start (start + span),
        construction.abnormalDeviationTransientMass excluded deviation t := by
    calc
      (span : ℝ) * threshold =
          ∑ _t ∈ Finset.Ico start (start + span), threshold := by simp
      _ ≤ _ := Finset.sum_le_sum fun t ht =>
        hnot t (Finset.mem_Ico.mp ht).1
  have hsubset : Finset.Ico start (start + span) ⊆
      Finset.range (start + span) := by
    intro t ht
    exact Finset.mem_range.mpr (Finset.mem_Ico.mp ht).2
  have hsumSubset : (∑ t ∈ Finset.Ico start (start + span),
      construction.abnormalDeviationTransientMass excluded deviation t) ≤
      ∑ t ∈ Finset.range (start + span),
        construction.abnormalDeviationTransientMass excluded deviation t := by
    apply Finset.sum_le_sum_of_subset_of_nonneg hsubset
    intro t _ _
    exact construction.abnormalDeviationTransientMass_nonneg
      excluded deviation t
  have hpotentialNonneg : 0 ≤
      construction.expectedProfileTransiencePotential
        excluded deviation (start + span) := by
    unfold KiloblockConstruction.expectedProfileTransiencePotential
    exact expect_nonneg _ _ fun history =>
      construction.profileTransiencePotential_nonneg
        (construction.finiteMode
          (construction.mode (start + span) history))
  have hsum := construction.sum_abnormalDeviationTransientMass_add_le
    excluded habnormal deviation (start + span)
  have hupper : (∑ t ∈ Finset.range (start + span),
      construction.abnormalDeviationTransientMass excluded deviation t) ≤
      initial := by
    dsimp only [initial] at hsum ⊢
    linarith
  exact (not_lt_of_ge (hlower.trans (hsumSubset.trans hupper))) hlarge

/-! The final-tail hitting potential counts a deviation which quits after the
final kiloblock as having reached that tail.  The extra indicator is invisible
under the prescribed profile, which never enters `.absorbed none`. -/
def KiloblockConstruction.postFinalAbsorptionIndicator
    {table : Table ι} {ε : ℝ}
    (_construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode _construction → ℝ
  | .absorbed none => 1
  | _ => 0

private theorem KiloblockConstruction.profileModeKernel_absorbed_none_iff
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockMode table construction.blockCount) :
    KiloblockMode.absorbed none ∈
        (construction.profileModeKernel mode).support ↔
      mode = .absorbed none := by
  cases mode with
  | draw phase =>
      cases phase <;>
        simp [KiloblockConstruction.profileModeKernel]
  | active k choice remaining =>
      cases choice with
      | none =>
          simp only [KiloblockConstruction.profileModeKernel]
          constructor
          · intro h
            have heq := (PMF.mem_support_pure_iff _ _).mp h
            split at heq <;> cases heq
          · intro h
            cases h
      | some owner =>
          simp only [KiloblockConstruction.profileModeKernel]
          constructor
          · intro h
            rw [PMF.mem_support_bind_iff] at h
            obtain ⟨quits, _hquits, hnext⟩ := h
            cases quits with
            | false =>
                have heq := (PMF.mem_support_pure_iff _ _).mp hnext
                split at heq <;> cases heq
            | true =>
                have heq := (PMF.mem_support_pure_iff _ _).mp hnext
                cases heq
          · intro h
            cases h
  | finalActive =>
      simp [KiloblockConstruction.profileModeKernel]
  | absorbed origin =>
      cases origin <;> simp [KiloblockConstruction.profileModeKernel]

private theorem KiloblockConstruction.finiteMode_eq_absorbed_none_iff
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockMode table construction.blockCount) :
    construction.finiteMode mode = .absorbed none ↔
      mode = .absorbed none := by
  cases mode with
  | draw phase => cases phase <;> simp [KiloblockConstruction.finiteMode]
  | active k choice remaining =>
      simp [KiloblockConstruction.finiteMode]
  | finalActive => simp [KiloblockConstruction.finiteMode]
  | absorbed origin =>
      cases origin <;> simp [KiloblockConstruction.finiteMode]

theorem KiloblockConstruction.postFinalAbsorptionIndicator_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    expect (construction.profileFiniteModeKernel mode)
        construction.postFinalAbsorptionIndicator =
      construction.postFinalAbsorptionIndicator mode := by
  by_cases hmode : mode = .absorbed none
  · subst mode
    unfold KiloblockConstruction.profileFiniteModeKernel
    simp only [KiloblockConstruction.modeOfFinite,
      KiloblockConstruction.profileModeKernel]
    rw [PMF.pure_map, Math.Probability.expect_pure]
    rfl
  · have hzero : construction.postFinalAbsorptionIndicator mode = 0 := by
      cases mode <;> try rfl
      rename_i origin
      cases origin with
      | none => exact False.elim (hmode rfl)
      | some k => rfl
    rw [hzero, ← Math.Probability.expect_const _ 0]
    apply Math.ProbabilityMassFunction.expect_congr_on_support
    intro destination hdestination
    have hnot : destination ≠
        (FiniteKiloblockMode.absorbed none) := by
      intro hdestinationNone
      unfold KiloblockConstruction.profileFiniteModeKernel at hdestination
      rw [PMF.mem_support_map_iff] at hdestination
      obtain ⟨raw, hraw, hrawFinite⟩ := hdestination
      have hrawNone : raw = KiloblockMode.absorbed none :=
        (construction.finiteMode_eq_absorbed_none_iff raw).mp
          (hrawFinite.trans hdestinationNone)
      have hsourceNone :=
        (construction.profileModeKernel_absorbed_none_iff
          (construction.modeOfFinite mode)).mp (hrawNone ▸ hraw)
      apply hmode
      rw [← construction.finiteMode_modeOfFinite mode, hsourceNone]
      rfl
    cases destination <;> try rfl
    rename_i origin
    cases origin with
    | none => exact False.elim (hnot rfl)
    | some k => rfl

def KiloblockConstruction.profilePastFinalPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    KiloblockFiniteMode construction → ℝ :=
  fun mode => construction.profileFinalPotential mode +
    construction.postFinalAbsorptionIndicator mode

theorem KiloblockConstruction.profilePastFinalPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    expect (construction.profileFiniteModeKernel mode)
        construction.profilePastFinalPotential =
      construction.profilePastFinalPotential mode := by
  unfold KiloblockConstruction.profilePastFinalPotential
  rw [Math.Probability.expect_add,
    construction.profileFinalPotential_harmonic,
    construction.postFinalAbsorptionIndicator_harmonic]

theorem KiloblockConstruction.profileSelectedSurvival_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (k : Fin (construction.blockCount + 1))
    (choice : Option (NormalPlayer table)) (remaining : ℕ)
    {downstream : ℝ} (hdownstream : 0 ≤ downstream) :
    0 ≤ construction.profileSelectedSurvival
      k choice remaining downstream := by
  cases choice with
  | none =>
      simpa [KiloblockConstruction.profileSelectedSurvival] using hdownstream
  | some owner =>
      have hendpoint : 0 ≤
          match (construction.attempt k owner).continuation with
          | .restart =>
              construction.macroAdvanceProbability k * downstream
          | .advance => downstream := by
        cases (construction.attempt k owner).continuation
        · exact mul_nonneg
            (construction.macroAdvanceProbability_nonneg k) hdownstream
        · exact hdownstream
      simp only [KiloblockConstruction.profileSelectedSurvival]
      apply mul_nonneg (pow_nonneg _ _) hendpoint
      have hhazard := quittingMeshHazard_le_one
        (construction.mesh k)
        (construction.attempt k owner).quitWeight_lt_one.le
      linarith

theorem KiloblockConstruction.profilePastFinalPotential_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    0 ≤ construction.profilePastFinalPotential mode := by
  have hfinal : 0 ≤ construction.profileFinalPotential mode := by
    cases mode with
    | drawChoose k =>
        exact construction.macroSurvivalFuel_nonneg (k.1 + 1)
    | drawResume k choice remaining =>
        exact construction.profileSelectedSurvival_nonneg
          k (choice.map construction.ownerOfCode) remaining.1
          (construction.macroSurvivalFuel_nonneg k.1)
    | drawFinal => exact zero_le_one
    | active k choice remaining =>
        exact construction.profileSelectedSurvival_nonneg
          k (choice.map construction.ownerOfCode) remaining.1
          (construction.macroSurvivalFuel_nonneg k.1)
    | finalActive => exact zero_le_one
    | absorbed origin => exact le_rfl
  unfold KiloblockConstruction.profilePastFinalPotential
  apply add_nonneg hfinal
  cases mode with
  | drawChoose k => exact le_rfl
  | drawResume k choice remaining => exact le_rfl
  | drawFinal => exact le_rfl
  | active k choice remaining => exact le_rfl
  | finalActive => exact le_rfl
  | absorbed origin =>
      cases origin with
      | none => exact zero_le_one
      | some k => exact le_rfl

theorem KiloblockConstruction.continuePastFinalIndicator_le_profilePotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (mode : KiloblockFiniteMode construction) :
    construction.continuePastFinalIndicator mode ≤
      construction.profilePastFinalPotential mode := by
  cases mode with
  | drawChoose k =>
      exact construction.profilePastFinalPotential_nonneg _
  | drawResume k choice remaining =>
      exact construction.profilePastFinalPotential_nonneg _
  | active k choice remaining =>
      exact construction.profilePastFinalPotential_nonneg _
  | drawFinal =>
      simp [KiloblockConstruction.continuePastFinalIndicator,
        KiloblockConstruction.profilePastFinalPotential,
        KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.postFinalAbsorptionIndicator]
  | finalActive =>
      simp [KiloblockConstruction.continuePastFinalIndicator,
        KiloblockConstruction.profilePastFinalPotential,
        KiloblockConstruction.profileFinalPotential,
        KiloblockConstruction.postFinalAbsorptionIndicator]
  | absorbed origin =>
      cases origin <;>
        simp [KiloblockConstruction.continuePastFinalIndicator,
          KiloblockConstruction.profilePastFinalPotential,
          KiloblockConstruction.profileFinalPotential,
          KiloblockConstruction.postFinalAbsorptionIndicator]

noncomputable def KiloblockConstruction.profilePastFinalHistoryPotential
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    (publicQuittingGame table
      construction.profile.signalLaw).HistoryPotential :=
  fun t history => construction.profilePastFinalPotential
    (construction.finiteMode (construction.mode t history))

theorem KiloblockConstruction.profilePastFinalHistoryPotential_harmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        construction.profile.strategy
        construction.profilePastFinalHistoryPotential history =
      construction.profilePastFinalHistoryPotential t history := by
  unfold StochasticGame.historyContinuationEU
    KiloblockConstruction.profilePastFinalHistoryPotential
  rw [← construction.profilePastFinalPotential_harmonic
    (construction.finiteMode (construction.mode t history))]
  rw [← construction.finiteHistoryModeStepDist_profile_eq history]
  unfold KiloblockConstruction.historyModeStepDist
  rw [PMF.map_bind, Math.Probability.expect_bind]
  apply congrArg (expect ((publicQuittingGame table
    construction.profile.signalLaw).stageActionDist
      construction.profile.strategy history))
  funext action
  rw [PMF.map_comp, Math.Probability.expect_map]
  rfl

theorem KiloblockConstruction.profilePastFinalHistoryPotential_initial
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε) :
    construction.profilePastFinalHistoryPotential 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw) =
      construction.macroSurvivalProbability := by
  unfold KiloblockConstruction.profilePastFinalHistoryPotential
    KiloblockConstruction.profilePastFinalPotential
  rw [construction.mode_initial]
  change construction.profileFinalPotential
      (.drawChoose (Fin.last construction.blockCount)) + 0 = _
  rw [
    construction.profileFinalPotential_initial]
  ring

private theorem KiloblockConstruction.profilePastFinal_pureQuit_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true))
        construction.profilePastFinalHistoryPotential history ≤
      (publicQuittingGame table
        construction.profile.signalLaw).historyContinuationEU
          construction.profile.strategy
          construction.profilePastFinalHistoryPotential history := by
  classical
  let actions : ∀ player,
      PMF ((publicQuittingGame table
        construction.profile.signalLaw).Act player) :=
    fun player => construction.profile.strategy player t history
  have hquitDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        (Function.update construction.profile.strategy excluded
          (fun _ _ => PMF.pure true)) history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded (PMF.pure true)) := by
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = excluded
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  have hcontinueDist : (publicQuittingGame table
      construction.profile.signalLaw).stageActionDist
        construction.profile.strategy history =
      Math.PMFProduct.pmfPi
        (Function.update actions excluded (PMF.pure false)) := by
    rw [← construction.update_abnormal_continue_eq_profile
      excluded habnormal]
    unfold StochasticGame.stageActionDist
    congr 1
    funext player
    by_cases hplayer : player = excluded
    · subst player
      simp [actions]
    · simp [actions, Function.update_of_ne hplayer]
  unfold StochasticGame.historyContinuationEU
  rw [hquitDist, hcontinueDist,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded true,
    ← Math.PMFProduct.pmfPi_bind_update_pure actions excluded false,
    Math.Probability.expect_bind, Math.Probability.expect_bind]
  simp_rw [Math.Probability.expect_pure]
  apply Math.Probability.expect_mono
  intro action
  let quitAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded true
  let continueAction : (publicQuittingGame table
      construction.profile.signalLaw).JointAct :=
    Function.update action excluded false
  cases hmode : construction.mode t history with
  | active k choice remaining =>
      have hquitValue : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.profilePastFinalHistoryPotential
            (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 0 := by
        rw [← Math.Probability.expect_const _ 0]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        have hnextMode : construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, quitAction), nextState) =
              .absorbed (some k) := by
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep
          case isTrue => exact hstep.2
          case isFalse hfalse =>
            exfalso
            apply hfalse
            refine ⟨excluded, ?_⟩
            simp [quitAction]
        unfold KiloblockConstruction.profilePastFinalHistoryPotential
        rw [hnextMode]
        simp [KiloblockConstruction.finiteMode,
          KiloblockConstruction.profilePastFinalPotential,
          KiloblockConstruction.profileFinalPotential,
          KiloblockConstruction.postFinalAbsorptionIndicator]
      rw [hquitValue]
      exact expect_nonneg _ _ fun nextState =>
        construction.profilePastFinalPotential_nonneg
          (construction.finiteMode (construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState)))
  | finalActive =>
      have hleft : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 quitAction)
          (fun nextState => construction.profilePastFinalHistoryPotential
            (t + 1)
              (Fin.snoc history.1 (history.2, quitAction), nextState)) = 1 := by
        rw [← Math.Probability.expect_const _ 1]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history quitAction
          nextState hnext
        unfold KiloblockConstruction.profilePastFinalHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep <;> rw [hstep.2]
        all_goals
          simp [KiloblockConstruction.finiteMode,
            KiloblockConstruction.profilePastFinalPotential,
            KiloblockConstruction.profileFinalPotential,
            KiloblockConstruction.postFinalAbsorptionIndicator]
      have hright : expect ((publicQuittingGame table
          construction.profile.signalLaw).transition history.2 continueAction)
          (fun nextState => construction.profilePastFinalHistoryPotential
            (t + 1)
              (Fin.snoc history.1 (history.2, continueAction), nextState)) =
          1 := by
        rw [← Math.Probability.expect_const _ 1]
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        have hstep := construction.mode_step t history continueAction
          nextState hnext
        unfold KiloblockConstruction.profilePastFinalHistoryPotential
        simp only [KiloblockModeStep, hmode] at hstep
        split at hstep <;> rw [hstep.2]
        all_goals
          simp [KiloblockConstruction.finiteMode,
            KiloblockConstruction.profilePastFinalPotential,
            KiloblockConstruction.profileFinalPotential,
            KiloblockConstruction.postFinalAbsorptionIndicator]
      rw [hleft, hright]
  | draw phase =>
      have hstate : history.2 = PublicQuittingState.draw := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches ⊢
      rw [hstate]
      apply le_of_eq
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro nextState hnext
      have hquitStep := construction.mode_step t history quitAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hcontinueStep := construction.mode_step t history continueAction
        nextState (by simpa [hstate, publicQuittingGame] using hnext)
      have hm : construction.mode (t + 1)
          (Fin.snoc history.1 (history.2, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1 (history.2, continueAction), nextState) := by
        cases phase <;> cases hsnext : nextState <;>
          simp [KiloblockModeStep, hmode, hsnext] at hquitStep hcontinueStep
        all_goals rw [hquitStep, hcontinueStep]
      unfold KiloblockConstruction.profilePastFinalHistoryPotential
      have hm' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.draw, quitAction), nextState) =
          construction.mode (t + 1)
            (Fin.snoc history.1
              (PublicQuittingState.draw, continueAction), nextState) := by
        simpa only [hstate] using hm
      rw [hm']
  | absorbed origin =>
      have hstate : ∃ quitters, history.2 =
          PublicQuittingState.absorbed quitters := by
        have hmatches := construction.mode_state t history
        cases hs : history.2 <;>
          simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
        exact ⟨_, rfl⟩
      obtain ⟨quitters, hstate⟩ := hstate
      rw [hstate]
      simp only [publicQuittingGame, Math.Probability.expect_pure]
      have hquitStep := construction.mode_step t history quitAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      have hcontinueStep := construction.mode_step t history continueAction
        (PublicQuittingState.absorbed quitters) (by
          simp only [hstate, publicQuittingGame]
          exact (PMF.mem_support_pure_iff _ _).2 rfl)
      simp only [KiloblockModeStep, hmode] at hquitStep hcontinueStep
      unfold KiloblockConstruction.profilePastFinalHistoryPotential
      have hquitStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, quitAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hquitStep
      have hcontinueStep' : construction.mode (t + 1)
          (Fin.snoc history.1
            (PublicQuittingState.absorbed quitters, continueAction),
              PublicQuittingState.absorbed quitters) = .absorbed origin := by
        simpa only [hstate] using hcontinueStep
      dsimp only [quitAction] at hquitStep'
      dsimp only [continueAction] at hcontinueStep'
      exact (congrArg construction.profilePastFinalPotential
        (congrArg construction.finiteMode
          (hquitStep'.trans hcontinueStep'.symm))).le

theorem KiloblockConstruction.profilePastFinalHistoryPotential_superharmonic
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (publicQuittingGame table
      construction.profile.signalLaw).historyContinuationEU
        (Function.update construction.profile.strategy excluded deviation)
        construction.profilePastFinalHistoryPotential history ≤
      construction.profilePastFinalHistoryPotential t history := by
  rw [historyContinuationEU_update_eq_expect_pure table
    construction.profile.signalLaw construction.profile.strategy excluded
      deviation construction.profilePastFinalHistoryPotential history]
  calc
    expect (deviation t history) (fun action =>
        (publicQuittingGame table
          construction.profile.signalLaw).historyContinuationEU
            (Function.update construction.profile.strategy excluded
              (fun _ _ => PMF.pure action))
            construction.profilePastFinalHistoryPotential history) ≤
        expect (deviation t history) (fun _ =>
          construction.profilePastFinalHistoryPotential t history) := by
      apply Math.Probability.expect_mono
      intro action
      cases action with
      | false =>
          rw [construction.update_abnormal_continue_eq_profile
            excluded habnormal]
          exact (construction.profilePastFinalHistoryPotential_harmonic
            history).le
      | true =>
          exact (construction.profilePastFinal_pureQuit_le
            excluded habnormal history).trans_eq
              (construction.profilePastFinalHistoryPotential_harmonic history)
    _ = construction.profilePastFinalHistoryPotential t history := by
      exact Math.Probability.expect_const _ _

theorem KiloblockConstruction.expect_abnormalPastFinalPotential_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded) : ∀ t,
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded deviation)
        .draw t)
      (construction.profilePastFinalHistoryPotential t) ≤
        construction.macroSurvivalProbability
  | 0 => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
          Math.Probability.expect_pure]
      exact construction.profilePastFinalHistoryPotential_initial.le
  | t + 1 => by
      change (publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue
          (Function.update construction.profile.strategy excluded deviation)
          .draw construction.profilePastFinalHistoryPotential (t + 1) ≤ _
      rw [(publicQuittingGame table
        construction.profile.signalLaw).expectedHistoryValue_succ]
      calc
        expect ((publicQuittingGame table
            construction.profile.signalLaw).histDist
          (Function.update construction.profile.strategy excluded deviation)
            .draw t)
          (fun history => (publicQuittingGame table
            construction.profile.signalLaw).historyContinuationEU
              (Function.update construction.profile.strategy excluded deviation)
              construction.profilePastFinalHistoryPotential history) ≤
            expect ((publicQuittingGame table
                construction.profile.signalLaw).histDist
              (Function.update construction.profile.strategy excluded deviation)
                .draw t)
              (construction.profilePastFinalHistoryPotential t) := by
          apply Math.Probability.expect_mono
          exact fun history =>
            construction.profilePastFinalHistoryPotential_superharmonic
              excluded habnormal deviation history
        _ ≤ construction.macroSurvivalProbability :=
          construction.expect_abnormalPastFinalPotential_le
            excluded habnormal deviation t

theorem KiloblockConstruction.expect_abnormalDeviationPastFinal_lt_epsilon
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded deviation)
        .draw t)
      (fun history => construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history))) < ε := by
  calc
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded deviation)
        .draw t)
      (fun history => construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history))) ≤
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        (Function.update construction.profile.strategy excluded deviation)
          .draw t)
        (construction.profilePastFinalHistoryPotential t) := by
        apply Math.Probability.expect_mono
        intro history
        exact construction.continuePastFinalIndicator_le_profilePotential
          (construction.finiteMode (construction.mode t history))
    _ ≤ construction.macroSurvivalProbability :=
      construction.expect_abnormalPastFinalPotential_le
        excluded habnormal deviation t
    _ < ε := construction.macroSurvivalProbability_lt_epsilon

/-! Under the prescribed profile, every pre-final absorption is a singleton
quit by the selected normal player.  Lemma 2.6 therefore makes its payoff
strictly positive for every abnormal player. -/
theorem KiloblockConstruction.profile_preFinal_payoff_nonneg
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table) :
    ∀ t (history : (publicQuittingGame table
        construction.profile.signalLaw).Hist t),
      history ∈ ((publicQuittingGame table
        construction.profile.signalLaw).histDist
          construction.profile.strategy .draw t).support →
      ∀ k, construction.mode t history = .absorbed (some k) →
        0 ≤ match history.2 with
          | .absorbed quitters => table.terminal quitters excluded
          | _ => 0
  | 0, history, hhistory, k, hmode => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).histDist_zero,
          PMF.mem_support_pure_iff] at hhistory
      subst history
      rw [construction.mode_initial] at hmode
      cases hmode
  | t + 1, nextHistory, hnextHistory, targetBlock, htargetMode => by
      rw [(publicQuittingGame table
        construction.profile.signalLaw).mem_support_histDist_succ]
        at hnextHistory
      obtain ⟨history, hhistory, action, haction, nextState,
        hnextState, rfl⟩ := hnextHistory
      have hstep := construction.mode_step t history action
        nextState hnextState
      cases hmode : construction.mode t history with
      | draw phase =>
          cases phase <;> cases nextState <;>
            simp [KiloblockModeStep, hmode] at hstep
          all_goals rw [hstep] at htargetMode
      | active block choice remaining =>
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep
          case isFalse hnoQuit =>
            split at hstep <;> rw [hstep.2] at htargetMode <;>
              cases htargetMode
          case isTrue hquit =>
            cases choice with
            | none =>
                have hdist := construction.stageActionDist_profile_continue_all
                  history (by
                    intro other owner rest hselected
                    simp [hmode] at hselected)
                rw [hdist, PMF.mem_support_pure_iff] at haction
                subst action
                exfalso
                obtain ⟨who, hwho⟩ := hquit
                simp [publicAllContinueAction] at hwho
            | some owner =>
                have hdist := construction.stageActionDist_active_some_core
                  history block owner remaining hmode
                rw [hdist, PMF.mem_support_bind_iff] at haction
                obtain ⟨quits, _hquits, haction⟩ := haction
                rw [PMF.mem_support_pure_iff] at haction
                subst action
                cases quits with
                | false =>
                    exfalso
                    obtain ⟨who, hwho⟩ := hquit
                    simp [publicSingleQuitAction_apply_eq_raw,
                      rawSingleQuitAction] at hwho
                | true =>
                    have hstate : ∃ signal, history.2 =
                        PublicQuittingState.active signal := by
                      have hmatches := construction.mode_state t history
                      cases hs : history.2 <;>
                        simp [KiloblockMode.MatchesState, hmode, hs]
                          at hmatches
                      exact ⟨_, rfl⟩
                    obtain ⟨signal, hstate⟩ := hstate
                    have htransition :=
                      publicQuittingGame_transition_publicSingleQuitAction
                        table construction.profile.signalLaw signal owner.1
                    rw [hstate, htransition, PMF.mem_support_pure_iff]
                      at hnextState
                    subst nextState
                    have hne : owner.1 ≠ excluded := by
                      intro heq
                      subst excluded
                      exact habnormal owner.2
                    exact (lemma2_6 table construction.soloExitNormalized
                      construction.payoffsBounded
                      (by simpa only [mem_normalPlayers_iff] using habnormal)
                      hne).le
      | finalActive =>
          simp only [KiloblockModeStep, hmode] at hstep
          split at hstep <;> rw [hstep.2] at htargetMode <;>
            cases htargetMode
      | absorbed origin =>
          have hstate : ∃ quitters, history.2 =
              PublicQuittingState.absorbed quitters := by
            have hmatches := construction.mode_state t history
            cases hs : history.2 <;>
              simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
            exact ⟨_, rfl⟩
          obtain ⟨quitters, hstate⟩ := hstate
          have htransition : (publicQuittingGame table
              construction.profile.signalLaw).transition history.2 action =
              PMF.pure (PublicQuittingState.absorbed quitters :
                (publicQuittingGame table
                  construction.profile.signalLaw).State) := by
            rw [hstate]
            simp [publicQuittingGame]
          have hnextState' : nextState ∈ (PMF.pure
              (PublicQuittingState.absorbed quitters :
                (publicQuittingGame table
                  construction.profile.signalLaw).State)).support := by
            rw [← htransition]
            exact hnextState
          have hnextEq := (PMF.mem_support_pure_iff _ _).mp hnextState'
          subst nextState
          have hnextOrigin : construction.mode (t + 1)
              (Fin.snoc history.1
                (history.2, action), PublicQuittingState.absorbed quitters) =
              .absorbed origin := by
            simpa [KiloblockModeStep, hmode] using hstep
          have horigin : origin = some targetBlock := by
            exact KiloblockMode.absorbed.inj
              (hnextOrigin.symm.trans htargetMode)
          subst origin
          simpa only [hstate] using
            construction.profile_preFinal_payoff_nonneg
              excluded habnormal t history hhistory targetBlock hmode

private theorem KiloblockConstruction.publicHistoryPayoff_le_positiveTail
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t) :
    (match history.2 with
      | .absorbed quitters => table.terminal quitters excluded
      | _ => table.never excluded) ≤
      construction.positiveTruncatedPayoff excluded t history +
        construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history)) +
        Math.Probability.transientCharge construction.continueBoundary
          (construction.finiteMode (construction.mode t history)) := by
  by_cases habsorbed : ∃ k, construction.mode t history =
      .absorbed (some k)
  · obtain ⟨k, hmode⟩ := habsorbed
    have hstate : ∃ quitters, history.2 =
        PublicQuittingState.absorbed quitters := by
      have hmatches := construction.mode_state t history
      cases hs : history.2 <;>
        simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
      exact ⟨_, rfl⟩
    obtain ⟨quitters, hstate⟩ := hstate
    have hboundary : (.absorbed (some k) :
        KiloblockFiniteMode construction) ∈ construction.continueBoundary :=
      trivial
    have hpast : construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history)) = 0 := by
      simp [hmode, KiloblockConstruction.finiteMode,
        KiloblockConstruction.continuePastFinalIndicator]
    have htransient : Math.Probability.transientCharge
        construction.continueBoundary
          (construction.finiteMode (construction.mode t history)) = 0 := by
      rw [hmode]
      simpa only [KiloblockConstruction.finiteMode] using
        Math.Probability.transientCharge_of_mem hboundary
    rw [hmode] at hpast htransient
    simp only [KiloblockConstruction.positiveTruncatedPayoff, hmode, hstate]
    rw [hpast, htransient]
    linarith [le_max_left (table.terminal quitters excluded) 0]
  · have hactual : (match history.2 with
        | .absorbed quitters => table.terminal quitters excluded
        | _ => table.never excluded) ≤ 1 := by
      cases history.2 with
      | draw =>
          exact (le_abs_self _).trans
            (construction.payoffsBounded.2 excluded)
      | active signal =>
          exact (le_abs_self _).trans
            (construction.payoffsBounded.2 excluded)
      | absorbed quitters =>
          exact (le_abs_self _).trans
            (construction.payoffsBounded.1 quitters excluded)
    have hpositive : construction.positiveTruncatedPayoff
        excluded t history = 0 := by
      unfold KiloblockConstruction.positiveTruncatedPayoff
      cases hmode : construction.mode t history with
      | draw phase => rfl
      | active k choice remaining => rfl
      | finalActive => rfl
      | absorbed origin =>
          cases origin with
          | none => rfl
          | some k => exact False.elim (habsorbed ⟨k, hmode⟩)
    have habsorbedZero : construction.continueAbsorbedIndicator
        (construction.finiteMode (construction.mode t history)) = 0 := by
      cases hmode : construction.mode t history with
      | draw phase => cases phase <;> rfl
      | active k choice remaining => rfl
      | finalActive => rfl
      | absorbed origin =>
          cases origin with
          | none => rfl
          | some k => exact False.elim (habsorbed ⟨k, hmode⟩)
    have hpartition := construction.continueIndicators_partition
      (construction.finiteMode (construction.mode t history))
    rw [habsorbedZero, zero_add] at hpartition
    rw [hpositive]
    linarith

private theorem KiloblockConstruction.positiveTail_le_publicHistoryPayoff
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    {t : ℕ}
    (history : (publicQuittingGame table
      construction.profile.signalLaw).Hist t)
    (hhistory : history ∈ ((publicQuittingGame table
      construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t).support) :
    construction.positiveTruncatedPayoff excluded t history ≤
      (match history.2 with
        | .absorbed quitters => table.terminal quitters excluded
        | _ => table.never excluded) +
        construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history)) +
        Math.Probability.transientCharge construction.continueBoundary
          (construction.finiteMode (construction.mode t history)) := by
  by_cases habsorbed : ∃ k, construction.mode t history =
      .absorbed (some k)
  · obtain ⟨k, hmode⟩ := habsorbed
    have hstate : ∃ quitters, history.2 =
        PublicQuittingState.absorbed quitters := by
      have hmatches := construction.mode_state t history
      cases hs : history.2 <;>
        simp [KiloblockMode.MatchesState, hmode, hs] at hmatches
      exact ⟨_, rfl⟩
    obtain ⟨quitters, hstate⟩ := hstate
    have hpayoff := construction.profile_preFinal_payoff_nonneg
      excluded habnormal t history hhistory k hmode
    have hboundary : (.absorbed (some k) :
        KiloblockFiniteMode construction) ∈ construction.continueBoundary :=
      trivial
    have hpast : construction.continuePastFinalIndicator
        (construction.finiteMode (.absorbed (some k))) = 0 := by
      simp [KiloblockConstruction.finiteMode,
        KiloblockConstruction.continuePastFinalIndicator]
    have htransient : Math.Probability.transientCharge
        construction.continueBoundary
          (construction.finiteMode (.absorbed (some k))) = 0 := by
      simpa only [KiloblockConstruction.finiteMode] using
        Math.Probability.transientCharge_of_mem hboundary
    simp only [KiloblockConstruction.positiveTruncatedPayoff, hmode, hstate]
    have hpayoff' : 0 ≤ table.terminal quitters excluded := by
      simpa only [hstate] using hpayoff
    rw [max_eq_left hpayoff', hpast, htransient]
    linarith
  · have hactual : -1 ≤ (match history.2 with
        | .absorbed quitters => table.terminal quitters excluded
        | _ => table.never excluded) := by
      cases history.2 with
      | draw =>
          exact neg_le_of_abs_le
            (construction.payoffsBounded.2 excluded)
      | active signal =>
          exact neg_le_of_abs_le
            (construction.payoffsBounded.2 excluded)
      | absorbed quitters =>
          exact neg_le_of_abs_le
            (construction.payoffsBounded.1 quitters excluded)
    have hpositive : construction.positiveTruncatedPayoff
        excluded t history = 0 := by
      unfold KiloblockConstruction.positiveTruncatedPayoff
      cases hmode : construction.mode t history with
      | draw phase => rfl
      | active k choice remaining => rfl
      | finalActive => rfl
      | absorbed origin =>
          cases origin with
          | none => rfl
          | some k => exact False.elim (habsorbed ⟨k, hmode⟩)
    have habsorbedZero : construction.continueAbsorbedIndicator
        (construction.finiteMode (construction.mode t history)) = 0 := by
      cases hmode : construction.mode t history with
      | draw phase => cases phase <;> rfl
      | active k choice remaining => rfl
      | finalActive => rfl
      | absorbed origin =>
          cases origin with
          | none => rfl
          | some k => exact False.elim (habsorbed ⟨k, hmode⟩)
    have hpartition := construction.continueIndicators_partition
      (construction.finiteMode (construction.mode t history))
    rw [habsorbedZero, zero_add] at hpartition
    rw [hpositive]
    linarith

theorem KiloblockConstruction.publicFinitePayoff_abnormalDeviation_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded)
    (t : ℕ) :
    publicFinitePayoff table construction.profile.signalLaw
        (Function.update construction.profile.strategy excluded deviation)
        t excluded ≤
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        (Function.update construction.profile.strategy excluded deviation)
          .draw t)
        (construction.positiveTruncatedPayoff excluded t) +
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        (Function.update construction.profile.strategy excluded deviation)
          .draw t)
        (fun history => construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history))) +
      construction.abnormalDeviationTransientMass excluded deviation t := by
  rw [← expect_publicHistoryPayoff_eq_publicFinitePayoff table
    construction.profile.signalLaw
    (Function.update construction.profile.strategy excluded deviation)
    t excluded]
  let distribution := (publicQuittingGame table
    construction.profile.signalLaw).histDist
      (Function.update construction.profile.strategy excluded deviation)
        .draw t
  have hmono : expect distribution (fun history => match history.2 with
        | .absorbed quitters => table.terminal quitters excluded
        | _ => table.never excluded) ≤
      expect distribution (fun history =>
        construction.positiveTruncatedPayoff excluded t history +
          construction.continuePastFinalIndicator
            (construction.finiteMode (construction.mode t history)) +
          Math.Probability.transientCharge construction.continueBoundary
            (construction.finiteMode (construction.mode t history))) := by
    apply Math.Probability.expect_mono
    exact fun history =>
      construction.publicHistoryPayoff_le_positiveTail excluded history
  rw [Math.Probability.expect_add, Math.Probability.expect_add] at hmono
  unfold KiloblockConstruction.abnormalDeviationTransientMass
  dsimp only [distribution] at hmono
  convert hmono using 1
  congr 1
  funext history
  cases history.2 <;> rfl

theorem KiloblockConstruction.profilePositiveTail_le_publicFinitePayoff
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (t : ℕ) :
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (construction.positiveTruncatedPayoff excluded t) ≤
      publicFinitePayoff table construction.profile.signalLaw
        construction.profile.strategy t excluded +
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t)
        (fun history => construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history))) +
      expect ((publicQuittingGame table
          construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t)
        (fun history => Math.Probability.transientCharge
          construction.continueBoundary
            (construction.finiteMode (construction.mode t history))) := by
  rw [← expect_publicHistoryPayoff_eq_publicFinitePayoff table
    construction.profile.signalLaw construction.profile.strategy t excluded]
  let distribution := (publicQuittingGame table
    construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t
  have hmono : expect distribution
      (construction.positiveTruncatedPayoff excluded t) ≤
      expect distribution (fun history =>
        (match history.2 with
          | .absorbed quitters => table.terminal quitters excluded
          | _ => table.never excluded) +
        construction.continuePastFinalIndicator
          (construction.finiteMode (construction.mode t history)) +
        Math.Probability.transientCharge construction.continueBoundary
          (construction.finiteMode (construction.mode t history))) := by
    apply expect_le_expect_of_le_on_support
    intro history hhistory
    exact construction.positiveTail_le_publicHistoryPayoff
      excluded habnormal history hhistory
  rw [Math.Probability.expect_add, Math.Probability.expect_add] at hmono
  dsimp only [distribution] at hmono
  convert hmono using 1
  congr 1
  congr 1
  congr 1
  funext history
  cases history.2 <;> rfl

theorem KiloblockConstruction.abnormalDeviationPayoff_le
    {table : Table ι} {ε : ℝ}
    (construction : KiloblockConstruction table ε)
    (excluded : ι) (habnormal : excluded ∉ NormalPlayers table)
    (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy excluded) :
    publicQuittingPayoff table construction.profile.signalLaw
        (Function.update construction.profile.strategy excluded deviation)
        excluded ≤
      construction.profile.payoff excluded + 5 * ε := by
  apply le_of_forall_pos_le_add
  intro δ hδ
  let deviationProfile :=
    Function.update construction.profile.strategy excluded deviation
  let deviationPayoff := publicQuittingPayoff table
    construction.profile.signalLaw deviationProfile excluded
  let profilePayoff := publicQuittingPayoff table
    construction.profile.signalLaw construction.profile.strategy excluded
  let deviationFinite := fun t => publicFinitePayoff table
    construction.profile.signalLaw deviationProfile t excluded
  let profileFinite := fun t => publicFinitePayoff table
    construction.profile.signalLaw construction.profile.strategy t excluded
  let threshold := δ / 4
  have hthreshold : 0 < threshold := by
    dsimp only [threshold]
    positivity
  have hdeviationEventually : ∀ᶠ t in atTop,
      |deviationFinite t - deviationPayoff| < threshold := by
    have htendsto : Tendsto deviationFinite atTop (nhds deviationPayoff) := by
      dsimp only [deviationFinite, deviationPayoff, deviationProfile]
      exact tendsto_publicFinitePayoff table construction.profile.signalLaw
        (Function.update construction.profile.strategy excluded deviation)
        excluded
    have hevent := htendsto.eventually
      (Metric.ball_mem_nhds _ hthreshold)
    filter_upwards [hevent] with t ht
    simpa only [Metric.mem_ball, Real.dist_eq] using ht
  have hprofileEventually : ∀ᶠ t in atTop,
      |profileFinite t - profilePayoff| < threshold := by
    have htendsto : Tendsto profileFinite atTop (nhds profilePayoff) := by
      dsimp only [profileFinite, profilePayoff]
      exact tendsto_publicFinitePayoff table construction.profile.signalLaw
        construction.profile.strategy excluded
    have hevent := htendsto.eventually
      (Metric.ball_mem_nhds _ hthreshold)
    filter_upwards [hevent] with t ht
    simpa only [Metric.mem_ball, Real.dist_eq] using ht
  let initial : KiloblockFiniteMode construction :=
    .drawChoose (Fin.last construction.blockCount)
  let transient := Math.Probability.transientCharge
    construction.continueBoundary
  obtain ⟨steps, hstepsTransient⟩ :=
    construction.exists_iter_profile_transientCharge_lt
      hthreshold initial
  have hstepsEventually : ∀ᶠ t in atTop, steps ≤ t :=
    eventually_ge_atTop steps
  have hall : ∀ᶠ t in atTop,
      |deviationFinite t - deviationPayoff| < threshold ∧
      |profileFinite t - profilePayoff| < threshold ∧ steps ≤ t :=
    hdeviationEventually.and (hprofileEventually.and hstepsEventually)
  obtain ⟨start, hstart⟩ := eventually_atTop.1 hall
  obtain ⟨t, ht, hdeviationTransient⟩ :=
    construction.exists_abnormalDeviationTransientMass_lt_after
      excluded habnormal deviation hthreshold start
  obtain ⟨hdeviationClose, hprofileClose, hsteps⟩ := hstart t ht
  have hpad := Math.Probability.expect_iter_add_transientCharge_le
    construction.profileBoundary_closed steps (t - steps) initial
  rw [Nat.add_sub_of_le hsteps] at hpad
  have hprofileTransientIter : expect
      (Math.PMFIter.iter construction.profileFiniteModeKernel t initial)
      transient < threshold := hpad.trans_lt hstepsTransient
  have hprofileTransient : expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t)
      (fun history => transient
        (construction.finiteMode (construction.mode t history))) <
      threshold := by
    have hmodeExpect : expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
          construction.profile.strategy .draw t)
        (fun history => transient
          (construction.finiteMode (construction.mode t history))) =
        expect (construction.modeDist
          construction.profile.strategy t) transient := by
      unfold KiloblockConstruction.modeDist
      rw [Math.Probability.expect_map]
    rw [hmodeExpect]
    rw [construction.modeDist_profile_eq_iter t]
    exact hprofileTransientIter
  have hpastDeviation :=
    construction.expect_abnormalDeviationPastFinal_lt_epsilon
      excluded habnormal deviation t
  have hpastProfileRaw :=
    construction.expect_abnormalDeviationPastFinal_lt_epsilon
      excluded habnormal (construction.profile.strategy excluded) t
  have hpastProfile : expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist
        construction.profile.strategy .draw t)
      (fun history => construction.continuePastFinalIndicator
        (construction.finiteMode (construction.mode t history))) < ε := by
    simpa only [Function.update_eq_self] using hpastProfileRaw
  have htruncated :=
    construction.finiteTruncatedDeviation_le_profile_add
      excluded habnormal deviation t 0
        ((publicQuittingGame table
          construction.profile.signalLaw).emptyHist .draw)
  rw [construction.finiteTruncatedContinuation_initial
      deviationProfile excluded t,
    construction.finiteTruncatedContinuation_initial
      construction.profile.strategy excluded t] at htruncated
  have hdeviationFinite :=
    construction.publicFinitePayoff_abnormalDeviation_le
      excluded deviation t
  have hprofilePositive :=
    construction.profilePositiveTail_le_publicFinitePayoff
      excluded habnormal t
  have hfinite : deviationFinite t ≤
      profileFinite t + 3 * ε + 2 * threshold := by
    dsimp only [deviationFinite, profileFinite, deviationProfile]
      at hdeviationFinite ⊢
    dsimp only [deviationProfile] at htruncated
    linarith
  unfold SunspotProfile.payoff
  dsimp only [deviationFinite, profileFinite, deviationPayoff,
    profilePayoff, deviationProfile] at hdeviationClose hprofileClose
  have hε : 0 < ε := construction.epsilon_pos
  dsimp only [threshold] at hfinite hdeviationClose hprofileClose ⊢
  linarith [(abs_lt.mp hdeviationClose).1,
    (abs_lt.mp hprofileClose).2]

/-! Lemma 3.8: for normal `i`, `|γ_i(ξ*)-w_i(yᴷ)|<2ε`. -/
theorem lemma3_8
    (table : Table ι) {ε : ℝ} (_hε : 0 < ε)
    (construction : KiloblockConstruction table ε) :
    ∀ i : NormalPlayer table,
      |construction.profile.payoff i.1 - construction.normalTarget i| <
      2 * ε := by
  intro who
  with_unfolding_all
    change |publicQuittingPayoff table construction.profile.signalLaw
      construction.profile.strategy who.1 - construction.normalTarget who| <
        2 * ε
  let initial : KiloblockFiniteMode construction :=
    .drawChoose (Fin.last construction.blockCount)
  let transient := Math.Probability.transientCharge
    construction.continueBoundary
  let residual : KiloblockFiniteMode construction → ℝ :=
    construction.profileAccountingResidual who
  let bound := ‖residual‖
  let lastCorrection := construction.trackingCorrection
    (Fin.last construction.blockCount) who
  let survivalCorrection := construction.macroSurvivalProbability *
    (lastCorrection + table.never who.1 -
      construction.point ⟨0, by omega⟩ who)
  let finitePayoff : ℕ → ℝ := fun t =>
    expect ((publicQuittingGame table
        construction.profile.signalLaw).histDist
      construction.profile.strategy .draw t)
      (fun history => match history.2 with
        | .absorbed quitters => table.terminal quitters who.1
        | _ => table.never who.1)
  let targetPayoff := publicQuittingPayoff table
    construction.profile.signalLaw construction.profile.strategy who.1
  have hmain : |construction.normalTarget who -
      targetPayoff| ≤
      KiloblockTracking construction.point construction.buildingBlock +
        |survivalCorrection| := by
    apply le_of_forall_pos_le_add
    intro δ hδ
    let threshold := δ / (2 * (bound + 1))
    have hbound : 0 ≤ bound := norm_nonneg _
    have hthreshold : 0 < threshold := by
      dsimp only [threshold]
      positivity
    obtain ⟨steps, hstepsTransient⟩ :=
      construction.exists_iter_profile_transientCharge_lt
        hthreshold initial
    have hfiniteToMass (t : ℕ) : finitePayoff t =
        publicFinitePayoffByMass table construction.profile.signalLaw
          construction.profile.strategy t who.1 := by
      dsimp only [finitePayoff]
      classical
      let distribution := (publicQuittingGame table
        construction.profile.signalLaw).histDist
          construction.profile.strategy .draw t
      have hpoint : ∀ history : (publicQuittingGame table
          construction.profile.signalLaw).Hist t,
          (match history.2 with
            | .absorbed quitters => table.terminal quitters who.1
            | _ => table.never who.1) =
          table.never who.1 + ∑ quitters,
            publicAbsorbedIndicator quitters history.2 *
              (table.terminal quitters who.1 - table.never who.1) := by
        intro history
        rcases history with ⟨past, state⟩
        cases state <;> simp [publicAbsorbedIndicator]
      calc
        expect distribution
            (fun history => match history.2 with
              | .absorbed quitters => table.terminal quitters who.1
              | _ => table.never who.1) =
            expect distribution (fun history => table.never who.1 +
              ∑ quitters, publicAbsorbedIndicator quitters history.2 *
                (table.terminal quitters who.1 -
                  table.never who.1)) := by
          apply Math.ProbabilityMassFunction.expect_congr_on_support
          intro history _
          exact hpoint history
        _ = table.never who.1 + ∑ quitters,
            expect distribution (fun history =>
              publicAbsorbedIndicator quitters history.2 *
                (table.terminal quitters who.1 -
                  table.never who.1)) := by
          rw [Math.Probability.expect_add, Math.Probability.expect_const]
          congr 1
          exact (Math.Probability.expect_sum_comm distribution
            (fun quitters history =>
              publicAbsorbedIndicator quitters history.2 *
                (table.terminal quitters who.1 -
                  table.never who.1))).symm
        _ = publicFinitePayoffByMass table construction.profile.signalLaw
              construction.profile.strategy t who.1 := by
          unfold publicFinitePayoffByMass publicAbsorbedMass
            StochasticGame.expectedStateValue
          congr 1
          apply Finset.sum_congr rfl
          intro quitters _
          let indicator : (publicQuittingGame table
              construction.profile.signalLaw).Hist t → ℝ :=
            fun history => publicAbsorbedIndicator quitters history.2
          change expect distribution (fun history => indicator history *
              (table.terminal quitters who.1 - table.never who.1)) =
            expect distribution indicator *
              (table.terminal quitters who.1 - table.never who.1)
          rw [show (fun history => indicator history *
              (table.terminal quitters who.1 - table.never who.1)) =
            fun history =>
              (table.terminal quitters who.1 - table.never who.1) *
                indicator history by
            funext history
            ring]
          rw [Math.Probability.expect_const_mul]
          ring
    have hpayEventually : ∀ᶠ t in atTop,
        |finitePayoff t - targetPayoff| < δ / 2 := by
      have hmass : Tendsto (fun t =>
          publicFinitePayoffByMass table construction.profile.signalLaw
            construction.profile.strategy t who.1) atTop
          (nhds (publicQuittingPayoff table
            construction.profile.signalLaw
            construction.profile.strategy who.1)) := by
        unfold publicFinitePayoffByMass publicQuittingPayoff
        apply Tendsto.add tendsto_const_nhds
        apply tendsto_finsetSum
        intro quitters _
        exact (tendsto_publicAbsorbedMass table
          construction.profile.signalLaw construction.profile.strategy
          quitters).mul_const
          (table.terminal quitters who.1 - table.never who.1)
      have htendsto : Tendsto finitePayoff atTop (nhds targetPayoff) := by
        apply hmass.congr'
        exact Filter.Eventually.of_forall fun t => (hfiniteToMass t).symm
      have hevent := htendsto.eventually
        (Metric.ball_mem_nhds _ (half_pos hδ))
      filter_upwards [hevent] with t ht
      simpa only [Metric.mem_ball, Real.dist_eq] using ht
    have hafterSteps : ∀ᶠ t in atTop, steps ≤ t :=
      eventually_ge_atTop steps
    obtain ⟨t, hpay, hsteps⟩ :=
      (hpayEventually.and hafterSteps).exists
    have hpad := Math.Probability.expect_iter_add_transientCharge_le
      construction.profileBoundary_closed steps (t - steps) initial
    rw [Nat.add_sub_of_le hsteps] at hpad
    have htransientIter : expect
        (Math.PMFIter.iter construction.profileFiniteModeKernel t initial)
        transient < threshold := hpad.trans_lt hstepsTransient
    let distribution := construction.modeDist
      construction.profile.strategy t
    have htransient : expect distribution transient < threshold := by
      dsimp only [distribution]
      rw [construction.modeDist_profile_eq_iter t]
      exact htransientIter
    have hresidualBound :=
      construction.abs_expect_profileAccountingResidual_le
        who distribution
    have hresidual : |expect distribution residual| < δ / 2 := by
      by_cases hboundZero : bound = 0
      · rw [show ‖construction.profileAccountingResidual who‖ = 0 by
            exact hboundZero] at hresidualBound
        have hhalf : 0 < δ / 2 := half_pos hδ
        linarith
      · have hboundPos : 0 < bound := lt_of_le_of_ne hbound
            (Ne.symm hboundZero)
        calc
          |expect distribution residual| ≤
              bound * expect distribution transient := hresidualBound
          _ < bound * threshold :=
            mul_lt_mul_of_pos_left htransient hboundPos
          _ = (δ / 2) * (bound / (bound + 1)) := by
            dsimp only [threshold]
            field_simp [ne_of_gt (show 0 < bound + 1 by positivity)]
          _ < (δ / 2) * 1 := by
            apply mul_lt_mul_of_pos_left _ (half_pos hδ)
            exact (div_lt_one (show 0 < bound + 1 by positivity)).2
              (lt_add_one bound)
          _ = δ / 2 := by ring
    have hcorrection :=
      construction.abs_expect_trackingBoundaryCorrection_sub_last_le
        who distribution
    have haccount := construction.expect_normalPayoffPotential_account who t
    have hinvariant :=
      construction.expect_normalPayoffPotential_eq_initial who t
    have hinitial := construction.normalPayoffPotential_initial who
    have hexact : construction.normalTarget who -
        targetPayoff =
        (finitePayoff t -
          targetPayoff) +
        (expect distribution
            (construction.trackingBoundaryCorrection who) -
          lastCorrection) +
        expect distribution residual - survivalCorrection := by
      dsimp only [lastCorrection, survivalCorrection]
      rw [hinvariant, hinitial] at haccount
      dsimp only [finitePayoff]
      linarith
    rw [hexact]
    calc
      |(finitePayoff t -
          targetPayoff) +
        (expect distribution
            (construction.trackingBoundaryCorrection who) -
          lastCorrection) +
        expect distribution residual - survivalCorrection| ≤
          |finitePayoff t -
            targetPayoff| +
          |expect distribution
              (construction.trackingBoundaryCorrection who) -
            lastCorrection| +
          |expect distribution residual| + |survivalCorrection| := by
        calc
          |_ - survivalCorrection| ≤ |_| + |survivalCorrection| :=
            abs_sub _ _
          _ ≤ (|finitePayoff t - targetPayoff| +
                |expect distribution
                    (construction.trackingBoundaryCorrection who) -
                  lastCorrection| +
                |expect distribution residual|) +
              |survivalCorrection| := by
            gcongr
            linarith [abs_add_le
              (finitePayoff t -
                targetPayoff)
              (expect distribution
                  (construction.trackingBoundaryCorrection who) -
                lastCorrection),
              abs_add_le
                ((finitePayoff t -
                  targetPayoff) +
                  (expect distribution
                      (construction.trackingBoundaryCorrection who) -
                    lastCorrection))
                (expect distribution residual)]
          _ = _ := by ring
      _ ≤ δ / 2 +
          KiloblockTracking construction.point construction.buildingBlock +
          δ / 2 + |survivalCorrection| := by
        gcongr
      _ = KiloblockTracking construction.point construction.buildingBlock +
          |survivalCorrection| + δ := by ring
  calc
    |targetPayoff - construction.normalTarget who| =
        |construction.normalTarget who -
          targetPayoff| := abs_sub_comm _ _
    _ ≤ KiloblockTracking construction.point construction.buildingBlock +
        |survivalCorrection| := hmain
    _ < ε + ε := add_lt_add construction.tracking_small
      (by
        dsimp only [survivalCorrection, lastCorrection]
        exact construction.abs_survival_endpointCorrection_lt_epsilon who)
    _ = 2 * ε := by ring

/-! Lemma 3.9: continuing by a normal player still terminates with probability
at least `1-ε` before the last kiloblock. -/
theorem lemma3_9
    (table : Table ι) {ε : ℝ} (_hε : 0 < ε)
    (construction : KiloblockConstruction table ε) :
    ∀ i : NormalPlayer table,
      continueTerminationBeforeFinal construction i.1 ≥ 1 - ε := by
  intro excluded
  let strategy := construction.continueProfile excluded
  let mass : ℕ → ℝ := fun t =>
    expect ((publicQuittingGame table
      construction.profile.signalLaw).histDist strategy .draw t)
      (absorbedBeforeFinalIndicator construction t)
  obtain ⟨steps, hfinite⟩ :=
    construction.exists_iter_continueAbsorbedIndicator_gt excluded
  have hbridge :=
    construction.expect_absorbedBeforeFinalIndicator_eq strategy steps
  rw [construction.modeDist_continue_eq_iter excluded steps] at hbridge
  have hmass : 1 - ε < mass steps := by
    dsimp only [mass]
    rw [hbridge]
    exact hfinite
  have hbdd : BddAbove (Set.range mass) := by
    refine ⟨1, ?_⟩
    rintro value ⟨t, rfl⟩
    exact construction.expect_absorbedBeforeFinalIndicator_le_one strategy t
  change 1 - ε ≤ ⨆ t, mass t
  exact le_ciSup_of_le hbdd steps hmass.le

/-! Lemma 3.10: a normal player's deviation is capped by `wᵢ(yᴷ)+5ε`;
an abnormal player's gain over the prescribed profile is at most `5ε`.
The printed abnormal-player paragraph writes `wᵢ(yᴷ)`, although `w`
has only normal coordinates; its actual argument is the direct gain bound
recorded by the second branch below. -/
theorem lemma3_10
    {table : Table ι} {ε : ℝ} (_hε : 0 < ε)
    (construction : KiloblockConstruction table ε) :
    ∀ i (deviation : (publicQuittingGame table
      construction.profile.signalLaw).BehaviorStrategy i),
      publicQuittingPayoff table construction.profile.signalLaw
          (Function.update construction.profile.strategy i deviation) i ≤
        if hi : i ∈ NormalPlayers table then
          construction.normalTarget ⟨i, hi⟩ + 5 * ε
        else construction.profile.payoff i + 5 * ε := by
  intro i deviation
  by_cases hi : i ∈ NormalPlayers table
  · rw [dif_pos hi]
    exact construction.normalDeviationPayoff_le ⟨i, hi⟩ deviation
  · rw [dif_neg hi]
    exact construction.abnormalDeviationPayoff_le i hi deviation

/-! Section 3.4 concludes from Lemmas 3.8--3.10 that the constructed profile
is a sunspot `7ε`-equilibrium. -/

/-! Section 3.4: the constructed profile is a sunspot `7ε`-equilibrium. -/
theorem section3_4
    (table : Table ι) (profile : SunspotProfile table)
    (target : NormalPlayer table → ℝ) (ε : ℝ) (hε : 0 ≤ ε)
    (hpayoff : ∀ who : NormalPlayer table,
      |profile.payoff who.1 - target who| < 2 * ε)
    (hdeviation : ∀ who
      (deviation : (publicQuittingGame table profile.signalLaw).BehaviorStrategy who),
      publicQuittingPayoff table profile.signalLaw
        (Function.update profile.strategy who deviation) who ≤
          if hwho : who ∈ NormalPlayers table then
            target ⟨who, hwho⟩ + 5 * ε
          else profile.payoff who + 5 * ε) :
    SunspotEpsilonEquilibrium table (7 * ε) profile := by
  intro who deviation
  have hupper := hdeviation who deviation
  by_cases hwho : who ∈ NormalPlayers table
  · rw [dif_pos hwho] at hupper
    have hlower := (abs_lt.mp (hpayoff ⟨who, hwho⟩)).1
    dsimp only [SunspotProfile.payoff] at hlower
    linarith
  · rw [dif_neg hwho] at hupper
    dsimp only [SunspotProfile.payoff] at hupper
    linarith

/-! Theorem 2.13(2): the Q branch yields a unilateral-quitting sunspot
`ε`-equilibrium for every `ε>0`. The declaration occurs here because its
proof consumes the literal Section 3 compiler and Lemmas 3.8--3.10. -/
theorem theorem2_13_sunspot
    (table : Table ι)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (hnormal : HasNormalPlayers table)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution
      (NormalMatrix table))
    (hQ : QMatrix (NormalMatrix table)) :
    ∀ requested : ℝ, 0 < requested → ∃ profile : SunspotProfile table,
      SunspotEpsilonEquilibrium table requested profile ∧
      AtMostOneQuitter profile := by
  intro requested hrequested
  obtain ⟨ε, hscale, ⟨construction⟩⟩ := exists_kiloblockConstruction
    table hnormalized hbounded hnormal hzero hQ hrequested
  refine ⟨construction.profile, ?_, construction.atMostOneQuitter⟩
  have hseven := section3_4 table construction.profile
    construction.normalTarget ε construction.epsilon_pos.le
    (lemma3_8 table construction.epsilon_pos construction)
    (lemma3_10 construction.epsilon_pos construction)
  intro who deviation
  have hbound := hseven who deviation
  linarith

/-! **Theorem 2.4 (paper).** Every normalized quitting game has a sunspot
`ε`-equilibrium for every `ε>0`. The proof combines the four LCP branches
above; ordinary equilibria are transported through the checked trivial-signal
clock-dilation argument. -/
theorem theorem2_4 (table : Table ι)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table) :
    ∀ ε : ℝ, 0 < ε → ∃ profile : SunspotProfile table,
      SunspotEpsilonEquilibrium table ε profile := by
  intro ε hε
  by_cases habnormal : AllPlayersAbnormal table
  · obtain ⟨root, hnash⟩ := lemma2_7 table hnormalized hbounded
      habnormal ε hε
    let ordinary := quittingStationaryProfile table.terminal root
    refine ⟨rootSequenceSunspotProfile table
      (quittingProfileLiveRoot table.zeroNeverReward ordinary), ?_⟩
    exact rootSequenceSunspotProfile_isEpsilonEquilibrium table ordinary
      hnash
  · have hnormal : HasNormalPlayers table := by
      exact Finset.nonempty_iff_ne_empty.mpr habnormal
    by_cases hzero : HasNontrivialZeroProjectiveLCPSolution
        (NormalMatrix table)
    · obtain ⟨ordinary, hnash⟩ :=
        epsilonEquilibria_of_nontrivialZeroProjectiveLCPSolution table
          hnormalized hbounded hzero ε hε
      refine ⟨rootSequenceSunspotProfile table
        (quittingProfileLiveRoot table.zeroNeverReward ordinary), ?_⟩
      exact rootSequenceSunspotProfile_isEpsilonEquilibrium table ordinary
        hnash
    · by_cases hQ : QMatrix (NormalMatrix table)
      · obtain ⟨profile, hequilibrium, _⟩ := theorem2_13_sunspot table
          hnormalized hbounded hnormal hzero hQ ε hε
        exact ⟨profile, hequilibrium⟩
      · obtain ⟨ordinary, hnash⟩ := theorem2_13_nonQ table
          hnormalized hbounded hnormal hzero hQ ε hε
        refine ⟨rootSequenceSunspotProfile table
          (quittingProfileLiveRoot table.zeroNeverReward ordinary), ?_⟩
        exact rootSequenceSunspotProfile_isEpsilonEquilibrium table ordinary
          hnash

/-! ## Section 4 — sunspot payoff characterization -/

/-! **Definition (paper, Section 4).** A vector is a sunspot equilibrium
payoff if it is the limit of payoffs of sunspot ε-equilibria as ε↓0. -/

/-! A sunspot equilibrium payoff is a limit of payoffs of sunspot
ε-equilibria as `ε↓0`. -/
def SunspotEquilibriumPayoff
    (table : Table ι) (value : Payoff ι) : Prop :=
  ∃ profiles : ℕ → SunspotProfile table,
    (∀ n : ℕ, SunspotEpsilonEquilibrium table
      ((n + 1 : ℝ)⁻¹) (profiles n)) ∧
    ∀ who, Tendsto (fun n => (profiles n).payoff who) atTop (𝓝 (value who))

/-! **M-matrix convention (paper).** Since the diagonal of `R̂` is zero, the
paper calls a Q-matrix an M-matrix when every row and every column has exactly
one positive entry. -/

/-! The paper's M-matrix convention: textbook Q, with exactly one positive
entry in each row and column.  The textbook qualifier is essential here:
the proof invokes inverse positivity, whereas the paper's normalized
projective Q predicate also admits the homogeneous branch. -/
def MMatrix (M : ι → ι → ℝ) : Prop :=
  IsStandardQMatrix M ∧
    (∀ who, (Finset.filter (fun owner => 0 < M who owner) Finset.univ).card = 1) ∧
    (∀ owner, (Finset.filter (fun who => 0 < M who owner) Finset.univ).card = 1)

namespace MMatrix

/-! The unique positive column in a row. -/
noncomputable def positiveOwner {M : ι → ι → ℝ}
    (hM : MMatrix M) (who : ι) : ι :=
  Classical.choose (Finset.card_eq_one.mp (hM.2.1 who))

theorem positiveOwner_filter {M : ι → ι → ℝ}
    (hM : MMatrix M) (who : ι) :
    Finset.filter (fun owner => 0 < M who owner) Finset.univ =
      {hM.positiveOwner who} :=
  Classical.choose_spec (Finset.card_eq_one.mp (hM.2.1 who))

theorem positive_positiveOwner {M : ι → ι → ℝ}
    (hM : MMatrix M) (who : ι) :
    0 < M who (hM.positiveOwner who) := by
  have hmem : hM.positiveOwner who ∈
      Finset.filter (fun owner => 0 < M who owner) Finset.univ := by
    rw [hM.positiveOwner_filter who]
    simp
  exact (Finset.mem_filter.mp hmem).2

theorem owner_eq_positiveOwner_of_pos {M : ι → ι → ℝ}
    (hM : MMatrix M) {who owner : ι} (hpos : 0 < M who owner) :
    owner = hM.positiveOwner who := by
  have hmem : owner ∈
      Finset.filter (fun candidate => 0 < M who candidate) Finset.univ := by
    exact Finset.mem_filter.mpr ⟨Finset.mem_univ _, hpos⟩
  rw [hM.positiveOwner_filter who] at hmem
  simpa using hmem

theorem nonpos_of_ne_positiveOwner {M : ι → ι → ℝ}
    (hM : MMatrix M) {who owner : ι}
    (hne : owner ≠ hM.positiveOwner who) :
    M who owner ≤ 0 := by
  exact le_of_not_gt fun hpos => hne (hM.owner_eq_positiveOwner_of_pos hpos)

/-! The unique positive row in a column. -/
noncomputable def positiveRow {M : ι → ι → ℝ}
    (hM : MMatrix M) (owner : ι) : ι :=
  Classical.choose (Finset.card_eq_one.mp (hM.2.2 owner))

theorem positiveRow_filter {M : ι → ι → ℝ}
    (hM : MMatrix M) (owner : ι) :
    Finset.filter (fun who => 0 < M who owner) Finset.univ =
      {hM.positiveRow owner} :=
  Classical.choose_spec (Finset.card_eq_one.mp (hM.2.2 owner))

theorem positive_positiveRow {M : ι → ι → ℝ}
    (hM : MMatrix M) (owner : ι) :
    0 < M (hM.positiveRow owner) owner := by
  have hmem : hM.positiveRow owner ∈
      Finset.filter (fun who => 0 < M who owner) Finset.univ := by
    rw [hM.positiveRow_filter owner]
    simp
  exact (Finset.mem_filter.mp hmem).2

theorem positiveOwner_positiveRow {M : ι → ι → ℝ}
    (hM : MMatrix M) (owner : ι) :
    hM.positiveOwner (hM.positiveRow owner) = owner := by
  exact (hM.owner_eq_positiveOwner_of_pos
    (hM.positive_positiveRow owner)).symm

theorem positiveRow_positiveOwner {M : ι → ι → ℝ}
    (hM : MMatrix M) (who : ι) :
    hM.positiveRow (hM.positiveOwner who) = who := by
  have hpos := hM.positive_positiveOwner who
  have hmem : who ∈ Finset.filter
      (fun candidate => 0 < M candidate (hM.positiveOwner who))
      Finset.univ := Finset.mem_filter.mpr ⟨Finset.mem_univ _, hpos⟩
  rw [hM.positiveRow_filter (hM.positiveOwner who)] at hmem
  exact (Finset.mem_singleton.mp hmem).symm

/-! The positive-entry pattern is a permutation between rows and columns. -/
noncomputable def positiveOwnerEquiv {M : ι → ι → ℝ}
    (hM : MMatrix M) : ι ≃ ι where
  toFun := hM.positiveOwner
  invFun := hM.positiveRow
  left_inv := hM.positiveRow_positiveOwner
  right_inv := hM.positiveOwner_positiveRow

/-! Column multiplication in the paper's `Σⱼ zⱼ rⱼ` convention. -/
def columnAction (M : ι → ι → ℝ) (weight : ι → ℝ) : ι → ℝ :=
  fun who => ∑ owner, weight owner * M who owner

/-! The coordinate vector `eᵢ`. -/
def unitVector (target : ι) : ι → ℝ :=
  fun who => if who = target then 1 else 0

/-! A nonnegative solution of `Mx=eᵢ`, the inverse-positive ray used in
Section 4. -/
structure UnitColumnSolution (M : ι → ι → ℝ) (target : ι) where
  weight : ι → ℝ
  weight_nonneg : ∀ owner, 0 ≤ weight owner
  equation : columnAction M weight = unitVector target

private theorem standardSolution_weight_pos_of_negative_direction
    {M : ι → ι → ℝ} (hM : MMatrix M) {q : ι → ℝ}
    (hq : ∀ who, q who < 0) (solution : StandardLCPSolution M q) :
    ∀ owner, 0 < solution.weight owner := by
  intro owner
  have hnonzero : solution.weight owner ≠ 0 := by
    intro hzero
    let row := hM.positiveRow owner
    have hrowOwner : hM.positiveOwner row = owner := by
      exact hM.positiveOwner_positiveRow owner
    have hsum : (∑ candidate,
        solution.weight candidate * M row candidate) ≤ 0 := by
      apply Finset.sum_nonpos
      intro candidate _
      by_cases hcandidate : candidate = owner
      · subst candidate
        simp [hzero]
      · exact mul_nonpos_of_nonneg_of_nonpos
          (solution.weight_nonneg candidate)
          (hM.nonpos_of_ne_positiveOwner (by simpa [hrowOwner]))
    linarith [solution.residual_nonneg row, hq row]
  exact lt_of_le_of_ne (solution.weight_nonneg owner) (Ne.symm hnonzero)

private theorem standardSolution_columnAction_eq_neg_of_negative_direction
    {M : ι → ι → ℝ} (hM : MMatrix M) {q : ι → ℝ}
    (hq : ∀ who, q who < 0) (solution : StandardLCPSolution M q) :
    columnAction M solution.weight = -q := by
  funext who
  have hweight := standardSolution_weight_pos_of_negative_direction
    hM hq solution who
  have hzero := solution.complementary who
  have hresidual : q who + ∑ owner,
      solution.weight owner * M who owner = 0 :=
    (mul_eq_zero.mp hzero).resolve_left (ne_of_gt hweight)
  change (∑ owner, solution.weight owner * M who owner) = -q who
  linarith

/-! Textbook Q plus the permutation sign pattern supplies a strictly positive
calibration `p` with `Mp=1`. -/
noncomputable def positiveCalibration {M : ι → ι → ℝ}
    (hM : MMatrix M) : ι → ℝ :=
  (Classical.choice (hM.1 (fun _ => -1))).weight

theorem positiveCalibration_pos {M : ι → ι → ℝ}
    (hM : MMatrix M) : ∀ owner, 0 < hM.positiveCalibration owner := by
  let solution := Classical.choice (hM.1 (fun _ => -1))
  exact standardSolution_weight_pos_of_negative_direction hM
    (fun _ => by norm_num) solution

theorem columnAction_positiveCalibration {M : ι → ι → ℝ}
    (hM : MMatrix M) :
    columnAction M hM.positiveCalibration = fun _ => 1 := by
  let solution := Classical.choice (hM.1 (fun _ => -1))
  have heq := standardSolution_columnAction_eq_neg_of_negative_direction hM
    (fun _ => by norm_num) solution
  funext who
  have hcoordinate := congrFun heq who
  simpa [positiveCalibration, solution] using hcoordinate

/-! A permutation M-matrix is inverse monotone: `Mx≥0` implies `x≥0`.
This is the finite minimum-ratio proof, avoiding any determinant API. -/
theorem columnAction_nonneg_imp_nonneg [Nonempty ι]
    {M : ι → ι → ℝ} (hM : MMatrix M) {x : ι → ℝ}
    (hx : ∀ who, 0 ≤ columnAction M x who) :
    ∀ owner, 0 ≤ x owner := by
  by_contra hnot
  push Not at hnot
  obtain ⟨owner, _, hminimum⟩ := Finset.exists_min_image
    Finset.univ (fun candidate =>
      x candidate / hM.positiveCalibration candidate) Finset.univ_nonempty
  have hownerNeg : x owner / hM.positiveCalibration owner < 0 := by
    obtain ⟨negativeOwner, hnegativeOwner⟩ := hnot
    have hcalibrationPos := hM.positiveCalibration_pos negativeOwner
    exact (hminimum negativeOwner (Finset.mem_univ _)).trans_lt
      (div_neg_of_neg_of_pos hnegativeOwner hcalibrationPos)
  let ratio := x owner / hM.positiveCalibration owner
  let row := hM.positiveRow owner
  have hrowOwner : hM.positiveOwner row = owner := by
    exact hM.positiveOwner_positiveRow owner
  have hterm : ∀ candidate : ι,
      x candidate * M row candidate ≤
        ratio * hM.positiveCalibration candidate * M row candidate := by
    intro candidate
    by_cases hcandidate : candidate = owner
    · subst candidate
      have hcalibrationPos := hM.positiveCalibration_pos owner
      dsimp only [ratio]
      rw [div_mul_cancel₀ _ (ne_of_gt hcalibrationPos)]
    · have hratio : ratio * hM.positiveCalibration candidate ≤
          x candidate := by
        apply (le_div_iff₀ (hM.positiveCalibration_pos candidate)).mp
        dsimp only [ratio]
        exact hminimum candidate (Finset.mem_univ _)
      exact mul_le_mul_of_nonpos_right hratio
        (hM.nonpos_of_ne_positiveOwner (by simpa [hrowOwner]))
  have hsum : columnAction M x row ≤ ratio := by
    calc
      columnAction M x row =
          ∑ candidate, x candidate * M row candidate := rfl
      _ ≤ ∑ candidate,
          ratio * hM.positiveCalibration candidate * M row candidate :=
        Finset.sum_le_sum fun candidate _ => hterm candidate
      _ = ratio * columnAction M hM.positiveCalibration row := by
        simp only [columnAction]
        rw [Finset.mul_sum]
        apply Finset.sum_congr rfl
        intro candidate _
        ring
      _ = ratio := by
        rw [hM.columnAction_positiveCalibration]
        simp
  linarith [hx row]

theorem columnAction_injective [Nonempty ι]
    {M : ι → ι → ℝ} (hM : MMatrix M) :
    Function.Injective (columnAction M) := by
  intro x y hxy
  have hsub : columnAction M (x - y) = 0 := by
    funext who
    have hcoordinate := congrFun hxy who
    change (∑ owner, (x owner - y owner) * M who owner) = 0
    rw [show (∑ owner, (x owner - y owner) * M who owner) =
        (∑ owner, x owner * M who owner) -
          ∑ owner, y owner * M who owner by
      simpa only [sub_mul] using
        (Finset.sum_sub_distrib (s := Finset.univ)
          (fun owner : ι => x owner * M who owner)
          (fun owner : ι => y owner * M who owner))]
    exact sub_eq_zero.mpr hcoordinate
  have hsubNonneg : ∀ owner, 0 ≤ (x - y) owner := by
    apply hM.columnAction_nonneg_imp_nonneg
    intro who
    have hzero := congrFun hsub who
    change columnAction M (x - y) who = (0 : ℝ) at hzero
    linarith
  have hnegSubNonneg : ∀ owner, 0 ≤ -((x - y) owner) := by
    apply hM.columnAction_nonneg_imp_nonneg
    intro who
    have hneg : columnAction M (fun owner => -((x - y) owner)) = 0 := by
      funext row
      rw [show columnAction M (fun owner => -((x - y) owner)) row =
          -columnAction M (x - y) row by
        simp only [columnAction]
        simpa only [neg_mul] using
          (Finset.sum_neg_distrib (s := Finset.univ)
            (fun owner : ι => (x - y) owner * M row owner)),
        hsub]
      simp
    have hzero := congrFun hneg who
    change columnAction M (fun owner => -((x - y) owner)) who =
      (0 : ℝ) at hzero
    linarith
  funext owner
  have hle := hsubNonneg owner
  have hge := hnegSubNonneg owner
  simp only [Pi.sub_apply] at hle hge
  linarith

/-! The inverse-positive conclusion used in Eq. (20): for each `i` there is
a nonnegative `x` with `Mx=eᵢ`. -/
theorem exists_unitColumnSolution [Nonempty ι]
    {M : ι → ι → ℝ} (hM : MMatrix M) (target : ι) :
    Nonempty (UnitColumnSolution M target) := by
  let qOne : ι → ℝ := fun who => -unitVector target who - 1
  obtain ⟨oneSolution⟩ := hM.1 qOne
  have hqOne : ∀ who, qOne who < 0 := by
    intro who
    dsimp only [qOne, unitVector]
    split <;> norm_num
  have honeEquation :=
    standardSolution_columnAction_eq_neg_of_negative_direction
      hM hqOne oneSolution
  let x : ι → ℝ := fun owner =>
    oneSolution.weight owner - hM.positiveCalibration owner
  have hxEquation : columnAction M x = unitVector target := by
    funext who
    have hone := congrFun honeEquation who
    have hcalibration := congrFun hM.columnAction_positiveCalibration who
    dsimp only [qOne] at hone
    simp only [Pi.neg_apply] at hone
    dsimp only [x, columnAction]
    rw [show (∑ owner,
        (oneSolution.weight owner - hM.positiveCalibration owner) *
          M who owner) =
        (∑ owner, oneSolution.weight owner * M who owner) -
          ∑ owner, hM.positiveCalibration owner * M who owner by
      simpa only [sub_mul] using
        (Finset.sum_sub_distrib (s := Finset.univ)
          (fun owner : ι => oneSolution.weight owner * M who owner)
          (fun owner : ι =>
            hM.positiveCalibration owner * M who owner))]
    change (∑ owner, oneSolution.weight owner * M who owner) =
      -(-unitVector target who - 1) at hone
    change (∑ owner, hM.positiveCalibration owner * M who owner) = 1
      at hcalibration
    linarith
  have hxNonneg : ∀ owner, 0 ≤ x owner := by
    intro owner
    by_contra hnegative
    have hxNegative : x owner < 0 := lt_of_not_ge hnegative
    let δ := -x owner / (2 * hM.positiveCalibration owner)
    have hδ : 0 < δ := by
      dsimp only [δ]
      exact div_pos (neg_pos.mpr hxNegative)
        (mul_pos (by norm_num) (hM.positiveCalibration_pos owner))
    let qδ : ι → ℝ := fun who => -unitVector target who - δ
    obtain ⟨δSolution⟩ := hM.1 qδ
    have hqδ : ∀ who, qδ who < 0 := by
      intro who
      dsimp only [qδ, unitVector]
      split <;> linarith
    have hδEquation :=
      standardSolution_columnAction_eq_neg_of_negative_direction
        hM hqδ δSolution
    have haffine : δSolution.weight = fun candidate =>
        x candidate + δ * hM.positiveCalibration candidate := by
      apply hM.columnAction_injective
      funext who
      have hleft := congrFun hδEquation who
      have hx := congrFun hxEquation who
      have hp := congrFun hM.columnAction_positiveCalibration who
      dsimp only [qδ] at hleft
      simp only [Pi.neg_apply] at hleft
      change (∑ candidate, δSolution.weight candidate * M who candidate) =
        ∑ candidate,
          (x candidate + δ * hM.positiveCalibration candidate) *
            M who candidate
      rw [show (∑ candidate,
          (x candidate + δ * hM.positiveCalibration candidate) *
            M who candidate) =
          (∑ candidate, x candidate * M who candidate) +
            ∑ candidate,
              (δ * hM.positiveCalibration candidate) * M who candidate by
        simp_rw [add_mul]
        rw [Finset.sum_add_distrib]]
      rw [show (∑ candidate,
          (δ * hM.positiveCalibration candidate) * M who candidate) =
          δ * (∑ candidate,
            hM.positiveCalibration candidate * M who candidate) by
        rw [Finset.mul_sum]
        apply Finset.sum_congr rfl
        intro candidate _
        ring]
      change (∑ candidate, δSolution.weight candidate * M who candidate) =
        -(-unitVector target who - δ) at hleft
      change (∑ candidate, x candidate * M who candidate) =
        unitVector target who at hx
      change (∑ candidate,
        hM.positiveCalibration candidate * M who candidate) = 1 at hp
      linear_combination hleft - hx - δ * hp
    have hcoordinate := congrFun haffine owner
    have hcalibrationPos := hM.positiveCalibration_pos owner
    have hδNonneg := δSolution.weight_nonneg owner
    dsimp only [δ] at hcoordinate
    rw [hcoordinate] at hδNonneg
    field_simp [ne_of_gt hcalibrationPos] at hδNonneg
    nlinarith
  exact ⟨{
    weight := x
    weight_nonneg := hxNonneg
    equation := hxEquation
  }⟩

end MMatrix

/-! `conv{rⁱ : i∈I*}` in the full payoff space of all players. -/
def NormalSingletonHull (table : Table ι) : Set (Payoff ι) :=
  convexHull ℝ (Set.range fun owner : NormalPlayer table =>
    fun who => table.terminal (quittingProjectiveSingletonTerminal owner) who)

/-! `D̃ = conv{rⁱ : i∈I*} ∩ ℝ^N_{≥0}` from Section 4. -/
def TildeD (table : Table ι) : Set (Payoff ι) :=
  NormalSingletonHull table ∩ {value | ∀ who, 0 ≤ value who}

/-! ## Section 4.1 — inverse-positive axis geometry -/

namespace MMatrix

noncomputable def axisSolution [Nonempty ι] {M : ι → ι → ℝ}
    (hM : MMatrix M) (target : ι) : UnitColumnSolution M target :=
  Classical.choice (hM.exists_unitColumnSolution target)

def axisMass [Nonempty ι] {M : ι → ι → ℝ}
    (hM : MMatrix M) (target : ι) : ℝ :=
  ∑ owner, (hM.axisSolution target).weight owner

theorem axisMass_pos [Nonempty ι] {M : ι → ι → ℝ}
    (hM : MMatrix M) (target : ι) : 0 < hM.axisMass target := by
  have hnonneg : 0 ≤ hM.axisMass target :=
    Finset.sum_nonneg fun owner _ =>
      (hM.axisSolution target).weight_nonneg owner
  by_contra hnot
  have hsumZero : hM.axisMass target = 0 :=
    le_antisymm (le_of_not_gt hnot) hnonneg
  have hweightZero : ∀ owner,
      (hM.axisSolution target).weight owner = 0 := by
    intro owner
    exact (Finset.sum_eq_zero_iff_of_nonneg
      (fun candidate _ =>
        (hM.axisSolution target).weight_nonneg candidate)).mp
      hsumZero owner (Finset.mem_univ owner)
  have hequation := congrFun (hM.axisSolution target).equation target
  simp [columnAction, hweightZero, unitVector] at hequation

/-! The normalized inverse column `λ̂ᵢ` in Eq. (20). -/
noncomputable def axisWeight [Nonempty ι] {M : ι → ι → ℝ}
    (hM : MMatrix M) (target : ι) : stdSimplex ℝ ι := by
  let mass := hM.axisMass target
  refine ⟨fun owner => (hM.axisSolution target).weight owner / mass,
    fun owner => div_nonneg ((hM.axisSolution target).weight_nonneg owner)
      (hM.axisMass_pos target).le, ?_⟩
  rw [← Finset.sum_div]
  exact div_self (ne_of_gt (hM.axisMass_pos target))

theorem axisWeight_apply [Nonempty ι] {M : ι → ι → ℝ}
    (hM : MMatrix M) (target owner : ι) :
    hM.axisWeight target owner =
      (hM.axisSolution target).weight owner / hM.axisMass target := rfl

theorem columnAction_axisWeight [Nonempty ι]
    {M : ι → ι → ℝ} (hM : MMatrix M) (target : ι) :
    columnAction M (hM.axisWeight target) = fun who =>
      (hM.axisMass target)⁻¹ * unitVector target who := by
  funext who
  have hequation := congrFun (hM.axisSolution target).equation who
  calc
    columnAction M (hM.axisWeight target) who =
        (hM.axisMass target)⁻¹ *
          columnAction M (hM.axisSolution target).weight who := by
      simp only [columnAction, axisWeight_apply]
      rw [Finset.mul_sum]
      apply Finset.sum_congr rfl
      intro owner _
      field_simp [ne_of_gt (hM.axisMass_pos target)]
    _ = (hM.axisMass target)⁻¹ * unitVector target who := by
      rw [hequation]

theorem axisWeight_designated_pos [Nonempty ι]
    {M : ι → ι → ℝ} (hM : MMatrix M) (target : ι) :
    0 < hM.axisWeight target (hM.positiveOwner target) := by
  have hequation := congrFun (hM.axisSolution target).equation target
  have hnonpos : ∀ owner, owner ≠ hM.positiveOwner target →
      (hM.axisSolution target).weight owner * M target owner ≤ 0 := by
    intro owner howner
    exact mul_nonpos_of_nonneg_of_nonpos
      ((hM.axisSolution target).weight_nonneg owner)
      (hM.nonpos_of_ne_positiveOwner howner)
  have hpositiveWeight : 0 <
      (hM.axisSolution target).weight (hM.positiveOwner target) := by
    by_contra hnot
    have hzero : (hM.axisSolution target).weight
        (hM.positiveOwner target) = 0 := le_antisymm
      (le_of_not_gt hnot)
      ((hM.axisSolution target).weight_nonneg _)
    have hsum : columnAction M (hM.axisSolution target).weight target ≤ 0 := by
      simp only [columnAction]
      apply Finset.sum_nonpos
      intro owner _
      by_cases howner : owner = hM.positiveOwner target
      · subst owner
        simp [hzero]
      · exact hnonpos owner howner
    rw [hequation] at hsum
    norm_num [unitVector] at hsum
  rw [axisWeight_apply]
  exact div_pos hpositiveWeight (hM.axisMass_pos target)

end MMatrix

section Section4Geometry

variable (table : Table ι) [Nonempty (NormalPlayer table)]
  (hM : MMatrix (NormalMatrix table))

/-! The full-payoff lift of the normal-coordinate axis `wᵢ`. -/
noncomputable def mAxisPayoff
    (target : NormalPlayer table) : Payoff ι :=
  fun who => ∑ owner,
    hM.axisWeight target owner *
      table.terminal (quittingProjectiveSingletonTerminal owner.1) who

theorem mAxisPayoff_normal
    (target who : NormalPlayer table) :
    mAxisPayoff table hM target who.1 =
      (hM.axisMass target)⁻¹ * MMatrix.unitVector target who := by
  have heq := congrFun (hM.columnAction_axisWeight target) who
  exact heq

theorem mAxisPayoff_target_pos (target : NormalPlayer table) :
    0 < mAxisPayoff table hM target target.1 := by
  rw [mAxisPayoff_normal]
  simp [MMatrix.unitVector]
  exact hM.axisMass_pos target

theorem mAxisPayoff_other_normal_zero
    (target who : NormalPlayer table) (hne : who ≠ target) :
    mAxisPayoff table hM target who.1 = 0 := by
  rw [mAxisPayoff_normal]
  simp [MMatrix.unitVector, hne]

theorem mAxisPayoff_mem_normalSingletonHull
    (target : NormalPlayer table) :
    mAxisPayoff table hM target ∈ NormalSingletonHull table := by
  apply mem_convexHull_of_exists_fintype
    (hM.axisWeight target)
    (fun owner : NormalPlayer table => fun who =>
      table.terminal (quittingProjectiveSingletonTerminal owner.1) who)
  · exact fun owner => (hM.axisWeight target).property.1 owner
  · exact (hM.axisWeight target).property.2
  · exact fun owner => ⟨owner, rfl⟩
  · funext who
    simp [mAxisPayoff, Pi.smul_apply, smul_eq_mul]

theorem mAxisPayoff_nonneg
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (target : NormalPlayer table) :
    ∀ who, 0 ≤ mAxisPayoff table hM target who := by
  intro who
  by_cases hnormal : who ∈ NormalPlayers table
  · let normalWho : NormalPlayer table := ⟨who, hnormal⟩
    by_cases hsame : normalWho = target
    · have hwho : who = target.1 := congrArg Subtype.val hsame
      rw [hwho]
      exact (mAxisPayoff_target_pos table hM target).le
    · rw [show who = normalWho.1 by rfl,
        mAxisPayoff_other_normal_zero table hM target normalWho hsame]
  · unfold mAxisPayoff
    apply Finset.sum_nonneg
    intro owner _
    apply mul_nonneg ((hM.axisWeight target).property.1 owner)
    exact (lemma2_6 table hnormalized hbounded
      (by simpa [mem_normalPlayers_iff] using hnormal)
      (by
        intro hsame
        subst who
        exact hnormal owner.2)).le

theorem mAxisPayoff_mem_tildeD
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (target : NormalPlayer table) :
    mAxisPayoff table hM target ∈ TildeD table :=
  ⟨mAxisPayoff_mem_normalSingletonHull table hM target,
    mAxisPayoff_nonneg table hM hnormalized hbounded target⟩

/-! A simplex representation of a full payoff by normal singleton columns. -/
structure NormalSingletonRepresentation (value : Payoff ι) where
  weight : stdSimplex ℝ (NormalPlayer table)
  equation : ∀ who, value who = ∑ owner,
    weight owner *
      table.terminal (quittingProjectiveSingletonTerminal owner.1) who

theorem exists_normalSingletonRepresentation
    {value : Payoff ι} (hvalue : value ∈ NormalSingletonHull table) :
    Nonempty (NormalSingletonRepresentation table value) := by
  rw [NormalSingletonHull,
    convexHull_range_eq_exists_affineCombination] at hvalue
  obtain ⟨support, weight, hweightNonneg, hweightTotal, hcombination⟩ := hvalue
  let fullWeight : NormalPlayer table → ℝ := fun owner =>
    if owner ∈ support then weight owner else 0
  have hfullNonneg : ∀ owner, 0 ≤ fullWeight owner := by
    intro owner
    dsimp only [fullWeight]
    split
    · exact hweightNonneg owner (by assumption)
    · exact le_rfl
  have hfullTotal : ∑ owner, fullWeight owner = 1 := by
    have hinter : (NormalPlayers table).attach ∩ support = support :=
      Finset.inter_eq_right.mpr fun owner _ =>
        by simpa using owner.property
    simpa [fullWeight, hinter] using hweightTotal
  let simplex : stdSimplex ℝ (NormalPlayer table) :=
    ⟨fullWeight, hfullNonneg, hfullTotal⟩
  refine ⟨{ weight := simplex, equation := ?_ }⟩
  intro who
  rw [Finset.affineCombination_eq_linear_combination _ _ _ hweightTotal]
      at hcombination
  have hcoordinate := congrFun hcombination who
  simp only [Finset.sum_apply, Pi.smul_apply, smul_eq_mul] at hcoordinate
  change value who = ∑ owner, fullWeight owner *
    table.terminal (quittingProjectiveSingletonTerminal owner.1) who
  rw [← hcoordinate]
  have hinter : (NormalPlayers table).attach ∩ support = support :=
    Finset.inter_eq_right.mpr fun owner _ =>
      by simpa using owner.property
  simp [fullWeight, hinter]

/-! Coefficients of a nonnegative normal value in the axis basis. -/
noncomputable def mAxisCoefficient
    (normalValue : NormalPlayer table → ℝ)
    (target : NormalPlayer table) : ℝ :=
  normalValue target * hM.axisMass target

/-! Expanding axis coefficients back into singleton-column weights. -/
noncomputable def mAxisExpandedWeight
    (normalValue : NormalPlayer table → ℝ)
    (owner : NormalPlayer table) : ℝ :=
  ∑ target, mAxisCoefficient table hM normalValue target *
    hM.axisWeight target owner

theorem mAxisExpandedWeight_eq_of_columnAction
    (normalValue : NormalPlayer table → ℝ)
    (weight : NormalPlayer table → ℝ)
    (hequation : MMatrix.columnAction (NormalMatrix table) weight =
      normalValue) :
    mAxisExpandedWeight table hM normalValue = weight := by
  apply hM.columnAction_injective
  funext who
  rw [hequation]
  simp only [MMatrix.columnAction, mAxisExpandedWeight,
    mAxisCoefficient]
  simp_rw [Finset.sum_mul]
  rw [Finset.sum_comm]
  calc
    _ = ∑ target, normalValue target * hM.axisMass target *
          MMatrix.columnAction (NormalMatrix table)
            (hM.axisWeight target) who := by
      apply Finset.sum_congr rfl
      intro target _
      simp only [MMatrix.columnAction]
      rw [Finset.mul_sum]
      apply Finset.sum_congr rfl
      intro owner _
      ring
    _ = ∑ target, normalValue target * hM.axisMass target *
          ((hM.axisMass target)⁻¹ *
            MMatrix.unitVector target who) := by
      apply Finset.sum_congr rfl
      intro target _
      rw [hM.columnAction_axisWeight]
    _ = normalValue who := by
      rw [Finset.sum_eq_single who]
      · simp only [MMatrix.unitVector, if_pos]
        field_simp [ne_of_gt (hM.axisMass_pos who)]
      · intro target _ hne
        simp [MMatrix.unitVector, Ne.symm hne]
      · simp

theorem mAxisCoefficient_nonneg
    {normalValue : NormalPlayer table → ℝ}
    (hnonneg : ∀ who, 0 ≤ normalValue who) :
    ∀ target, 0 ≤ mAxisCoefficient table hM normalValue target := by
  intro target
  exact mul_nonneg (hnonneg target) (hM.axisMass_pos target).le

theorem sum_mAxisCoefficient_eq_one
    {value : Payoff ι}
    (representation : NormalSingletonRepresentation table value) :
    ∑ target, mAxisCoefficient table hM
      (fun who => value who.1) target = 1 := by
  have hnormalEquation : MMatrix.columnAction (NormalMatrix table)
      representation.weight = fun who => value who.1 := by
    funext who
    have heq := representation.equation who.1
    exact heq.symm
  have hexpanded := mAxisExpandedWeight_eq_of_columnAction table hM
    (fun who => value who.1) representation.weight hnormalEquation
  calc
    ∑ target, mAxisCoefficient table hM
        (fun who => value who.1) target =
        ∑ owner, mAxisExpandedWeight table hM
          (fun who => value who.1) owner := by
      simp only [mAxisExpandedWeight]
      rw [Finset.sum_comm]
      apply Finset.sum_congr rfl
      intro target _
      rw [← Finset.mul_sum]
      have hsum := (hM.axisWeight target).property.2
      change (∑ owner, hM.axisWeight target owner) = 1 at hsum
      rw [hsum, mul_one]
    _ = ∑ owner, representation.weight owner := by rw [hexpanded]
    _ = 1 := representation.weight.property.2

theorem mAxisPayoff_combination_eq
    {value : Payoff ι}
    (representation : NormalSingletonRepresentation table value) :
    (∑ target, mAxisCoefficient table hM
      (fun who => value who.1) target • mAxisPayoff table hM target) =
      value := by
  have hnormalEquation : MMatrix.columnAction (NormalMatrix table)
      representation.weight = fun who => value who.1 := by
    funext who
    exact (representation.equation who.1).symm
  have hexpanded := mAxisExpandedWeight_eq_of_columnAction table hM
    (fun who => value who.1) representation.weight hnormalEquation
  funext who
  simp only [Finset.sum_apply, Pi.smul_apply, smul_eq_mul,
    mAxisPayoff]
  simp_rw [Finset.mul_sum]
  rw [Finset.sum_comm]
  calc
    _ = ∑ owner, mAxisExpandedWeight table hM
          (fun normal => value normal.1) owner *
            table.terminal
              (quittingProjectiveSingletonTerminal owner.1) who := by
      apply Finset.sum_congr rfl
      intro owner _
      simp only [mAxisExpandedWeight]
      rw [Finset.sum_mul]
      apply Finset.sum_congr rfl
      intro target _
      ring
    _ = ∑ owner, representation.weight owner *
          table.terminal
            (quittingProjectiveSingletonTerminal owner.1) who := by
      rw [hexpanded]
    _ = value who := (representation.equation who).symm

/-! A strictly positive part of the designated owner's axis weight.  The
second bound is the paper's requirement that removing this exit branch leave
the target coordinate nonnegative. -/
noncomputable def mExitProbability
    (target : NormalPlayer table) : ℝ :=
  min (hM.axisWeight target (hM.positiveOwner target) / 2)
    (mAxisPayoff table hM target target.1 /
      (2 * NormalMatrix table target (hM.positiveOwner target)))

theorem mExitProbability_pos (target : NormalPlayer table) :
    0 < mExitProbability table hM target := by
  unfold mExitProbability
  apply lt_min (half_pos (hM.axisWeight_designated_pos target))
  exact div_pos (mAxisPayoff_target_pos table hM target)
    (mul_pos zero_lt_two (hM.positive_positiveOwner target))

theorem mExitProbability_lt_one (target : NormalPlayer table) :
    mExitProbability table hM target < 1 := by
  have hcoordinateLe : hM.axisWeight target
      (hM.positiveOwner target) ≤ 1 := by
    calc
      hM.axisWeight target (hM.positiveOwner target) ≤
          ∑ owner, hM.axisWeight target owner :=
        Finset.single_le_sum
          (fun owner _ => (hM.axisWeight target).property.1 owner)
          (Finset.mem_univ _)
      _ = 1 := (hM.axisWeight target).property.2
  unfold mExitProbability
  have hhalf : hM.axisWeight target (hM.positiveOwner target) / 2 < 1 := by
    linarith [hM.axisWeight_designated_pos target]
  exact (min_le_left _ _).trans_lt hhalf

theorem mExitProbability_mul_designated_le_axis
    (target : NormalPlayer table) :
    mExitProbability table hM target *
        NormalMatrix table target (hM.positiveOwner target) ≤
      mAxisPayoff table hM target target.1 := by
  have hcolumnPos := hM.positive_positiveOwner target
  have hle := min_le_right
    (hM.axisWeight target (hM.positiveOwner target) / 2)
    (mAxisPayoff table hM target target.1 /
      (2 * NormalMatrix table target (hM.positiveOwner target)))
  change mExitProbability table hM target ≤
    mAxisPayoff table hM target target.1 /
      (2 * NormalMatrix table target (hM.positiveOwner target)) at hle
  have hratio :
      (mAxisPayoff table hM target target.1 /
          (2 * NormalMatrix table target (hM.positiveOwner target))) *
          NormalMatrix table target (hM.positiveOwner target) =
        mAxisPayoff table hM target target.1 / 2 := by
    field_simp [ne_of_gt hcolumnPos]
  have haxisPos := mAxisPayoff_target_pos table hM target
  nlinarith

/-! Singleton-column weights of the continuation `y^[i]` after the
designated owner survives the block. -/
noncomputable def mContinuationOwnerWeight
    (target owner : NormalPlayer table) : ℝ :=
  (hM.axisWeight target owner -
      if owner = hM.positiveOwner target then
        mExitProbability table hM target else 0) /
    (1 - mExitProbability table hM target)

theorem mContinuationOwnerWeight_nonneg
    (target owner : NormalPlayer table) :
    0 ≤ mContinuationOwnerWeight table hM target owner := by
  apply div_nonneg
  · by_cases howner : owner = hM.positiveOwner target
    · rw [if_pos howner]
      subst owner
      have hle : mExitProbability table hM target ≤
          hM.axisWeight target (hM.positiveOwner target) / 2 := by
        unfold mExitProbability
        exact min_le_left _ _
      linarith [hM.axisWeight_designated_pos target]
    · rw [if_neg howner, sub_zero]
      exact (hM.axisWeight target).property.1 owner
  · linarith [mExitProbability_lt_one table hM target]

theorem sum_mContinuationOwnerWeight (target : NormalPlayer table) :
    ∑ owner, mContinuationOwnerWeight table hM target owner = 1 := by
  let designated := hM.positiveOwner target
  have hdenominator : 1 - mExitProbability table hM target ≠ 0 :=
    ne_of_gt (sub_pos.mpr (mExitProbability_lt_one table hM target))
  simp only [mContinuationOwnerWeight]
  rw [← Finset.sum_div]
  have hindicator : (∑ owner,
      if owner = designated then mExitProbability table hM target else 0) =
      mExitProbability table hM target := by
    rw [Finset.sum_eq_single designated]
    · simp
    · intro owner _ hne
      simp [hne]
    · simp
  have hsum := (hM.axisWeight target).property.2
  change (∑ owner, hM.axisWeight target owner) = 1 at hsum
  rw [Finset.sum_sub_distrib, hsum, hindicator]
  exact div_self hdenominator

noncomputable def mContinuationOwnerSimplex
    (target : NormalPlayer table) :
    stdSimplex ℝ (NormalPlayer table) :=
  ⟨mContinuationOwnerWeight table hM target,
    mContinuationOwnerWeight_nonneg table hM target,
    sum_mContinuationOwnerWeight table hM target⟩

/-! The full continuation payoff `y^[i]`. -/
noncomputable def mContinuationPayoff
    (target : NormalPlayer table) : Payoff ι :=
  fun who => ∑ owner,
    mContinuationOwnerWeight table hM target owner *
      table.terminal (quittingProjectiveSingletonTerminal owner.1) who

noncomputable def mContinuationRepresentation
    (target : NormalPlayer table) :
    NormalSingletonRepresentation table
      (mContinuationPayoff table hM target) where
  weight := mContinuationOwnerSimplex table hM target
  equation := fun _ => rfl

theorem mAxisPayoff_exit_balance
    (target : NormalPlayer table) :
    mAxisPayoff table hM target =
      mExitProbability table hM target •
          (fun who => table.terminal
            (quittingProjectiveSingletonTerminal
              (hM.positiveOwner target).1) who) +
        (1 - mExitProbability table hM target) •
          mContinuationPayoff table hM target := by
  funext who
  simp only [mAxisPayoff, mContinuationPayoff, Pi.add_apply,
    Pi.smul_apply, smul_eq_mul]
  let designated := hM.positiveOwner target
  have hdenominator : 1 - mExitProbability table hM target ≠ 0 :=
    ne_of_gt (sub_pos.mpr (mExitProbability_lt_one table hM target))
  calc
    ∑ owner, hM.axisWeight target owner *
          table.terminal (quittingProjectiveSingletonTerminal owner.1) who =
        ∑ owner,
          (if owner = designated then
              mExitProbability table hM target else 0) *
            table.terminal
              (quittingProjectiveSingletonTerminal owner.1) who +
          ∑ owner,
            (hM.axisWeight target owner -
              if owner = designated then
                mExitProbability table hM target else 0) *
              table.terminal
                (quittingProjectiveSingletonTerminal owner.1) who := by
      rw [← Finset.sum_add_distrib]
      apply Finset.sum_congr rfl
      intro owner _
      ring
    _ = mExitProbability table hM target *
          table.terminal
            (quittingProjectiveSingletonTerminal designated.1) who +
        ∑ owner,
          (hM.axisWeight target owner -
            if owner = designated then
              mExitProbability table hM target else 0) *
            table.terminal
              (quittingProjectiveSingletonTerminal owner.1) who := by
      congr 1
      rw [Finset.sum_eq_single designated]
      · simp
      · intro owner _ hne
        simp [hne]
      · simp
    _ = mExitProbability table hM target *
          table.terminal
            (quittingProjectiveSingletonTerminal designated.1) who +
        (1 - mExitProbability table hM target) *
          ∑ owner, mContinuationOwnerWeight table hM target owner *
            table.terminal
              (quittingProjectiveSingletonTerminal owner.1) who := by
      dsimp only [designated]
      congr 1
      rw [Finset.mul_sum]
      apply Finset.sum_congr rfl
      intro owner _
      simp only [mContinuationOwnerWeight]
      field_simp [hdenominator]

theorem mContinuationPayoff_normal_nonneg
    (target who : NormalPlayer table) :
    0 ≤ mContinuationPayoff table hM target who.1 := by
  have hbalance := congrFun (mAxisPayoff_exit_balance table hM target) who.1
  simp only [Pi.add_apply, Pi.smul_apply, smul_eq_mul] at hbalance
  have hdenominator : 0 < 1 - mExitProbability table hM target :=
    sub_pos.mpr (mExitProbability_lt_one table hM target)
  by_cases hsame : who = target
  · subst who
    change mAxisPayoff table hM target target.1 =
      mExitProbability table hM target *
          NormalMatrix table target (hM.positiveOwner target) +
        (1 - mExitProbability table hM target) *
          mContinuationPayoff table hM target target.1 at hbalance
    have hcoefficient :=
      mExitProbability_mul_designated_le_axis table hM target
    nlinarith
  · have haxisZero := mAxisPayoff_other_normal_zero
      table hM target who hsame
    change mAxisPayoff table hM target who.1 =
      mExitProbability table hM target *
          NormalMatrix table who (hM.positiveOwner target) +
        (1 - mExitProbability table hM target) *
          mContinuationPayoff table hM target who.1 at hbalance
    have hcolumnNonpos : NormalMatrix table who
        (hM.positiveOwner target) ≤ 0 := by
      have hrowNe : who ≠ target := hsame
      have hpositiveRow := hM.positiveRow_positiveOwner target
      apply le_of_not_gt
      intro hpos
      have hrow := hM.positiveRow_filter (hM.positiveOwner target)
      have hmem : who ∈ Finset.filter
          (fun candidate => 0 < NormalMatrix table candidate
            (hM.positiveOwner target)) Finset.univ :=
        Finset.mem_filter.mpr ⟨Finset.mem_univ _, hpos⟩
      rw [hrow] at hmem
      exact hrowNe (by simpa [hpositiveRow] using hmem)
    rw [haxisZero] at hbalance
    nlinarith [mExitProbability_pos table hM target]

theorem mContinuationPayoff_nonneg
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (target : NormalPlayer table) :
    ∀ who, 0 ≤ mContinuationPayoff table hM target who := by
  intro who
  by_cases hnormal : who ∈ NormalPlayers table
  · exact mContinuationPayoff_normal_nonneg table hM target ⟨who, hnormal⟩
  · unfold mContinuationPayoff
    apply Finset.sum_nonneg
    intro owner _
    apply mul_nonneg
      (mContinuationOwnerWeight_nonneg table hM target owner)
    exact (lemma2_6 table hnormalized hbounded
      (by simpa [mem_normalPlayers_iff] using hnormal)
      (by
        intro hsame
        subst who
        exact hnormal owner.2)).le

/-! The paper's transition distribution `βᵢ`: it expands `y^[i]` in the
axis basis. -/
noncomputable def mTransitionWeight
    (target : NormalPlayer table)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table) :
    stdSimplex ℝ (NormalPlayer table) := by
  let normalValue : NormalPlayer table → ℝ := fun who =>
    mContinuationPayoff table hM target who.1
  refine ⟨mAxisCoefficient table hM normalValue,
    mAxisCoefficient_nonneg table hM
      (fun who => mContinuationPayoff_normal_nonneg table hM target who), ?_⟩
  have hsum := sum_mAxisCoefficient_eq_one table hM
    (mContinuationRepresentation table hM target)
  exact hsum

theorem mTransitionWeight_axis_balance
    (target : NormalPlayer table)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table) :
    (∑ next, mTransitionWeight table hM target hnormalized hbounded next •
      mAxisPayoff table hM next) =
      mContinuationPayoff table hM target := by
  exact mAxisPayoff_combination_eq table hM
    (mContinuationRepresentation table hM target)

theorem mDesignatedOwner_ne_target
    (hnormalized : SoloExitNormalized table)
    (target : NormalPlayer table) :
    hM.positiveOwner target ≠ target := by
  intro heq
  have hpositive := hM.positive_positiveOwner target
  rw [heq, normalMatrix_diagonal_zero table hnormalized target] at hpositive
  exact (lt_irrefl 0) hpositive

theorem mAxisPayoff_designatedOwner_zero
    (hnormalized : SoloExitNormalized table)
    (target : NormalPlayer table) :
    mAxisPayoff table hM target (hM.positiveOwner target).1 = 0 :=
  mAxisPayoff_other_normal_zero table hM target
    (hM.positiveOwner target)
    (mDesignatedOwner_ne_target table hM hnormalized target)

/-! For an `η`-equilibrium the same axis decomposition may use a smaller
one-stage exit probability.  This replaces a long fine-mesh block by its
equivalent small total hazard and makes collision error at most `η/2`. -/
noncomputable def mFineExitProbability
    (target : NormalPlayer table) (accuracy : ℝ) : ℝ :=
  min (mExitProbability table hM target) (accuracy / 2)

theorem mFineExitProbability_pos
    (target : NormalPlayer table) {accuracy : ℝ} (haccuracy : 0 < accuracy) :
    0 < mFineExitProbability table hM target accuracy := by
  exact lt_min (mExitProbability_pos table hM target) (half_pos haccuracy)

theorem mFineExitProbability_lt_one
    (target : NormalPlayer table) {accuracy : ℝ} (haccuracy : 0 < accuracy) :
    mFineExitProbability table hM target accuracy < 1 :=
  (min_le_left _ _).trans_lt (mExitProbability_lt_one table hM target)

theorem mFineExitProbability_lt_accuracy
    (target : NormalPlayer table) {accuracy : ℝ} (haccuracy : 0 < accuracy) :
    mFineExitProbability table hM target accuracy < accuracy := by
  have hle : mFineExitProbability table hM target accuracy ≤ accuracy / 2 :=
    min_le_right _ _
  linarith

noncomputable def mFineContinuationOwnerWeight
    (target owner : NormalPlayer table) (accuracy : ℝ) : ℝ :=
  (hM.axisWeight target owner -
      if owner = hM.positiveOwner target then
        mFineExitProbability table hM target accuracy else 0) /
    (1 - mFineExitProbability table hM target accuracy)

theorem mFineContinuationOwnerWeight_nonneg
    (target owner : NormalPlayer table) {accuracy : ℝ}
    (haccuracy : 0 < accuracy) :
    0 ≤ mFineContinuationOwnerWeight table hM target owner accuracy := by
  apply div_nonneg
  · by_cases howner : owner = hM.positiveOwner target
    · rw [if_pos howner]
      subst owner
      have hfineLe : mFineExitProbability table hM target accuracy ≤
          mExitProbability table hM target := min_le_left _ _
      have hexitLe : mExitProbability table hM target ≤
          hM.axisWeight target (hM.positiveOwner target) / 2 := by
        unfold mExitProbability
        exact min_le_left _ _
      linarith [hM.axisWeight_designated_pos target]
    · rw [if_neg howner, sub_zero]
      exact (hM.axisWeight target).property.1 owner
  · linarith [mFineExitProbability_lt_one table hM target haccuracy]

theorem sum_mFineContinuationOwnerWeight
    (target : NormalPlayer table) {accuracy : ℝ}
    (haccuracy : 0 < accuracy) :
    ∑ owner, mFineContinuationOwnerWeight table hM target owner accuracy = 1 := by
  let probability := mFineExitProbability table hM target accuracy
  let designated := hM.positiveOwner target
  have hdenominator : 1 - probability ≠ 0 :=
    ne_of_gt (sub_pos.mpr
      (mFineExitProbability_lt_one table hM target haccuracy))
  simp only [mFineContinuationOwnerWeight]
  rw [← Finset.sum_div]
  have hindicator : (∑ owner,
      if owner = designated then probability else 0) = probability := by
    rw [Finset.sum_eq_single designated]
    · simp
    · intro owner _ hne
      simp [hne]
    · simp
  have hsum := (hM.axisWeight target).property.2
  change (∑ owner, hM.axisWeight target owner) = 1 at hsum
  rw [Finset.sum_sub_distrib, hsum, hindicator]
  exact div_self hdenominator

noncomputable def mFineContinuationOwnerSimplex
    (target : NormalPlayer table) (accuracy : ℝ) (haccuracy : 0 < accuracy) :
    stdSimplex ℝ (NormalPlayer table) :=
  ⟨fun owner => mFineContinuationOwnerWeight table hM target owner accuracy,
    fun owner => mFineContinuationOwnerWeight_nonneg
      table hM target owner haccuracy,
    sum_mFineContinuationOwnerWeight table hM target haccuracy⟩

noncomputable def mFineContinuationPayoff
    (target : NormalPlayer table) (accuracy : ℝ) : Payoff ι :=
  fun who => ∑ owner,
    mFineContinuationOwnerWeight table hM target owner accuracy *
      table.terminal (quittingProjectiveSingletonTerminal owner.1) who

noncomputable def mFineContinuationRepresentation
    (target : NormalPlayer table) (accuracy : ℝ) (haccuracy : 0 < accuracy) :
    NormalSingletonRepresentation table
      (mFineContinuationPayoff table hM target accuracy) where
  weight := mFineContinuationOwnerSimplex table hM target accuracy haccuracy
  equation := fun _ => rfl

theorem mAxisPayoff_fine_exit_balance
    (target : NormalPlayer table) {accuracy : ℝ}
    (haccuracy : 0 < accuracy) :
    mAxisPayoff table hM target =
      mFineExitProbability table hM target accuracy •
          (fun who => table.terminal
            (quittingProjectiveSingletonTerminal
              (hM.positiveOwner target).1) who) +
        (1 - mFineExitProbability table hM target accuracy) •
          mFineContinuationPayoff table hM target accuracy := by
  funext who
  simp only [mAxisPayoff, mFineContinuationPayoff, Pi.add_apply,
    Pi.smul_apply, smul_eq_mul]
  let probability := mFineExitProbability table hM target accuracy
  let designated := hM.positiveOwner target
  have hdenominator : 1 - probability ≠ 0 :=
    ne_of_gt (sub_pos.mpr
      (mFineExitProbability_lt_one table hM target haccuracy))
  calc
    ∑ owner, hM.axisWeight target owner *
          table.terminal (quittingProjectiveSingletonTerminal owner.1) who =
        ∑ owner,
          (if owner = designated then probability else 0) *
            table.terminal
              (quittingProjectiveSingletonTerminal owner.1) who +
          ∑ owner,
            (hM.axisWeight target owner -
              if owner = designated then probability else 0) *
              table.terminal
                (quittingProjectiveSingletonTerminal owner.1) who := by
      rw [← Finset.sum_add_distrib]
      apply Finset.sum_congr rfl
      intro owner _
      ring
    _ = probability * table.terminal
          (quittingProjectiveSingletonTerminal designated.1) who +
        ∑ owner,
          (hM.axisWeight target owner -
            if owner = designated then probability else 0) *
            table.terminal
              (quittingProjectiveSingletonTerminal owner.1) who := by
      congr 1
      rw [Finset.sum_eq_single designated]
      · simp
      · intro owner _ hne
        simp [hne]
      · simp
    _ = probability * table.terminal
          (quittingProjectiveSingletonTerminal designated.1) who +
        (1 - probability) *
          ∑ owner, mFineContinuationOwnerWeight
            table hM target owner accuracy *
              table.terminal
                (quittingProjectiveSingletonTerminal owner.1) who := by
      dsimp only [probability, designated]
      congr 1
      rw [Finset.mul_sum]
      apply Finset.sum_congr rfl
      intro owner _
      simp only [mFineContinuationOwnerWeight]
      have hdenominator' :
          1 - mFineExitProbability table hM target accuracy ≠ 0 :=
        hdenominator
      field_simp [hdenominator']

theorem mFineContinuationPayoff_normal_nonneg
    (target who : NormalPlayer table) {accuracy : ℝ}
    (haccuracy : 0 < accuracy) :
    0 ≤ mFineContinuationPayoff table hM target accuracy who.1 := by
  have hbalance := congrFun
    (mAxisPayoff_fine_exit_balance table hM target haccuracy) who.1
  simp only [Pi.add_apply, Pi.smul_apply, smul_eq_mul] at hbalance
  have hdenominator : 0 <
      1 - mFineExitProbability table hM target accuracy :=
    sub_pos.mpr (mFineExitProbability_lt_one table hM target haccuracy)
  by_cases hsame : who = target
  · subst who
    change mAxisPayoff table hM target target.1 =
      mFineExitProbability table hM target accuracy *
          NormalMatrix table target (hM.positiveOwner target) +
        (1 - mFineExitProbability table hM target accuracy) *
          mFineContinuationPayoff table hM target accuracy target.1 at hbalance
    have hfineLe : mFineExitProbability table hM target accuracy ≤
        mExitProbability table hM target := min_le_left _ _
    have hcolumnPos := hM.positive_positiveOwner target
    have hcoefficient :=
      mExitProbability_mul_designated_le_axis table hM target
    nlinarith
  · have haxisZero := mAxisPayoff_other_normal_zero
      table hM target who hsame
    change mAxisPayoff table hM target who.1 =
      mFineExitProbability table hM target accuracy *
          NormalMatrix table who (hM.positiveOwner target) +
        (1 - mFineExitProbability table hM target accuracy) *
          mFineContinuationPayoff table hM target accuracy who.1 at hbalance
    have hcolumnNonpos : NormalMatrix table who
        (hM.positiveOwner target) ≤ 0 := by
      apply le_of_not_gt
      intro hpos
      have hrow := hM.positiveRow_filter (hM.positiveOwner target)
      have hmem : who ∈ Finset.filter
          (fun candidate => 0 < NormalMatrix table candidate
            (hM.positiveOwner target)) Finset.univ :=
        Finset.mem_filter.mpr ⟨Finset.mem_univ _, hpos⟩
      rw [hrow] at hmem
      exact hsame (by
        simpa [hM.positiveRow_positiveOwner target] using hmem)
    rw [haxisZero] at hbalance
    nlinarith [mFineExitProbability_pos table hM target haccuracy]

theorem mFineContinuationPayoff_nonneg
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (target : NormalPlayer table) {accuracy : ℝ}
    (haccuracy : 0 < accuracy) :
    ∀ who, 0 ≤ mFineContinuationPayoff table hM target accuracy who := by
  intro who
  by_cases hnormal : who ∈ NormalPlayers table
  · exact mFineContinuationPayoff_normal_nonneg
      table hM target ⟨who, hnormal⟩ haccuracy
  · unfold mFineContinuationPayoff
    apply Finset.sum_nonneg
    intro owner _
    apply mul_nonneg
      (mFineContinuationOwnerWeight_nonneg
        table hM target owner haccuracy)
    exact (lemma2_6 table hnormalized hbounded
      (by simpa [mem_normalPlayers_iff] using hnormal)
      (by
        intro hsame
        subst who
        exact hnormal owner.2)).le

/-! At the designated owner's coordinate both the axis value and its
continuation are zero.  This is the local indifference that lets that owner
replace the prescribed rare quit by Continue. -/
theorem mFineContinuationPayoff_designatedOwner_zero
    (hnormalized : SoloExitNormalized table)
    (target : NormalPlayer table) {accuracy : ℝ}
    (haccuracy : 0 < accuracy) :
    mFineContinuationPayoff table hM target accuracy
        (hM.positiveOwner target).1 = 0 := by
  have hbalance := congrFun
    (mAxisPayoff_fine_exit_balance table hM target haccuracy)
      (hM.positiveOwner target).1
  have haxis := mAxisPayoff_designatedOwner_zero
    table hM hnormalized target
  have hdiagonal : NormalMatrix table (hM.positiveOwner target)
      (hM.positiveOwner target) = 0 :=
    normalMatrix_diagonal_zero table hnormalized (hM.positiveOwner target)
  simp only [Pi.add_apply, Pi.smul_apply, smul_eq_mul] at hbalance
  change mAxisPayoff table hM target (hM.positiveOwner target).1 =
      mFineExitProbability table hM target accuracy *
          NormalMatrix table (hM.positiveOwner target)
            (hM.positiveOwner target) +
        (1 - mFineExitProbability table hM target accuracy) *
          mFineContinuationPayoff table hM target accuracy
            (hM.positiveOwner target).1 at hbalance
  rw [haxis, hdiagonal, mul_zero, zero_add] at hbalance
  exact (mul_eq_zero.mp hbalance.symm).resolve_left
    (ne_of_gt (sub_pos.mpr
      (mFineExitProbability_lt_one table hM target haccuracy)))

noncomputable def mFineTransitionWeight
    (target : NormalPlayer table) (accuracy : ℝ) (haccuracy : 0 < accuracy) :
    stdSimplex ℝ (NormalPlayer table) := by
  let normalValue : NormalPlayer table → ℝ := fun who =>
    mFineContinuationPayoff table hM target accuracy who.1
  refine ⟨mAxisCoefficient table hM normalValue,
    mAxisCoefficient_nonneg table hM
      (fun who => mFineContinuationPayoff_normal_nonneg
        table hM target who haccuracy), ?_⟩
  exact sum_mAxisCoefficient_eq_one table hM
    (mFineContinuationRepresentation table hM target accuracy haccuracy)

theorem mFineTransitionWeight_axis_balance
    (target : NormalPlayer table) (accuracy : ℝ) (haccuracy : 0 < accuracy) :
    (∑ next, mFineTransitionWeight table hM target accuracy haccuracy next •
      mAxisPayoff table hM next) =
      mFineContinuationPayoff table hM target accuracy := by
  exact mAxisPayoff_combination_eq table hM
    (mFineContinuationRepresentation table hM target accuracy haccuracy)

end Section4Geometry

/-! ## Section 4.2 — finite public axis compiler -/

structure MMatrixPayoffBlueprint
    (table : Table ι) [Nonempty (NormalPlayer table)]
    (hM : MMatrix (NormalMatrix table))
    (value : Payoff ι) (accuracy : ℝ) where
  soloExitNormalized : SoloExitNormalized table
  payoffsBounded : TablePayoffsBounded table
  representation : NormalSingletonRepresentation table value
  value_nonneg : ∀ who, 0 ≤ value who
  accuracy_pos : 0 < accuracy

namespace MMatrixPayoffBlueprint

variable {table : Table ι} [Nonempty (NormalPlayer table)]
  {hM : MMatrix (NormalMatrix table)} {value : Payoff ι} {accuracy : ℝ}

noncomputable def initialWeight
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    stdSimplex ℝ (NormalPlayer table) :=
  ⟨mAxisCoefficient table hM (fun who => value who.1),
    mAxisCoefficient_nonneg table hM (fun who => blueprint.value_nonneg who.1),
    sum_mAxisCoefficient_eq_one table hM blueprint.representation⟩

theorem initialWeight_axis_balance
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    (∑ target, blueprint.initialWeight target •
      mAxisPayoff table hM target) = value :=
  mAxisPayoff_combination_eq table hM blueprint.representation

noncomputable def signalMarginal
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    Option (NormalPlayer table) → PMF (NormalPlayer table)
  | none => Math.ProbabilityMassFunction.ofVector
      blueprint.initialWeight blueprint.initialWeight.property
  | some target => Math.ProbabilityMassFunction.ofVector
      (mFineTransitionWeight table hM target accuracy blueprint.accuracy_pos)
      (mFineTransitionWeight table hM target accuracy
        blueprint.accuracy_pos).property

noncomputable def signalData
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    Math.PMFProduct.CommonFiniteSignal
      (Option (NormalPlayer table)) (NormalPlayer table)
      blueprint.signalMarginal :=
  Math.PMFProduct.commonFiniteSignal _ _ blueprint.signalMarginal

@[simp] theorem signalMarginal_none_toReal
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (target : NormalPlayer table) :
    (blueprint.signalMarginal none target).toReal =
      blueprint.initialWeight target := by
  exact Math.ProbabilityMassFunction.ofVector_toReal
    blueprint.initialWeight.property target

@[simp] theorem signalMarginal_some_toReal
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (source target : NormalPlayer table) :
    (blueprint.signalMarginal (some source) target).toReal =
      mFineTransitionWeight table hM source accuracy
        blueprint.accuracy_pos target := by
  exact Math.ProbabilityMassFunction.ofVector_toReal
    (mFineTransitionWeight table hM source accuracy
      blueprint.accuracy_pos).property target

end MMatrixPayoffBlueprint

inductive MMatrixPayoffMode (table : Table ι)
  | draw (source : Option (NormalPlayer table))
  | active (target : NormalPlayer table)
  | absorbed

namespace MMatrixPayoffMode

def finiteCodeEquiv (table : Table ι) :
    MMatrixPayoffMode table ≃
      Option (NormalPlayer table) ⊕ (NormalPlayer table ⊕ Unit) where
  toFun
    | .draw source => Sum.inl source
    | .active target => Sum.inr (Sum.inl target)
    | .absorbed => Sum.inr (Sum.inr ())
  invFun
    | Sum.inl source => .draw source
    | Sum.inr (Sum.inl target) => .active target
    | Sum.inr (Sum.inr ()) => .absorbed
  left_inv mode := by cases mode <;> rfl
  right_inv code := by
    rcases code with source | target
    · rfl
    · rcases target with target | terminal
      · rfl
      · rcases terminal with ⟨⟩
        rfl

noncomputable instance (table : Table ι) :
    Fintype (MMatrixPayoffMode table) :=
  Fintype.ofEquiv
    (Option (NormalPlayer table) ⊕ (NormalPlayer table ⊕ Unit))
    (finiteCodeEquiv table).symm

end MMatrixPayoffMode

def MMatrixPayoffMode.MatchesState
    {table : Table ι} {Signal : Type}
    (mode : MMatrixPayoffMode table)
    (state : PublicQuittingState ι Signal) : Prop :=
  match mode, state with
  | .draw _, .draw => True
  | .active _, .active _ => True
  | .absorbed, .absorbed _ => True
  | _, _ => False

def nextMMatrixPayoffMode
    {table : Table ι} {Signal : Type}
    (selector : Option (NormalPlayer table) → Signal → NormalPlayer table)
    (mode : MMatrixPayoffMode table) (_action : ι → Bool)
    (nextState : PublicQuittingState ι Signal) :
    MMatrixPayoffMode table :=
  match nextState with
  | .draw =>
      match mode with
      | .active target => .draw (some target)
      | .draw source => .draw source
      | .absorbed => .draw none
  | .active signal =>
      match mode with
      | .draw source => .active (selector source signal)
      | _ => .active (selector none signal)
  | .absorbed _ => .absorbed

def compiledMMatrixPayoffMode
    {table : Table ι} {signalCount : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (selector : Option (NormalPlayer table) →
      Fin (signalCount + 1) → NormalPlayer table) :
    ∀ t, (publicQuittingGame table signalLaw).Hist t →
      MMatrixPayoffMode table
  | 0, history =>
      match history.2 with
      | .draw => .draw none
      | .active signal => .active (selector none signal)
      | .absorbed _ => .absorbed
  | t + 1, history =>
      nextMMatrixPayoffMode selector
        (compiledMMatrixPayoffMode signalLaw selector t
          (publicHistoryInit signalLaw history))
        (history.1 (Fin.last t)).2 history.2

theorem nextMMatrixPayoffMode_matchesState
    {table : Table ι} {Signal : Type}
    (selector : Option (NormalPlayer table) → Signal → NormalPlayer table)
    (mode : MMatrixPayoffMode table)
    (action : ι → Bool) (nextState : PublicQuittingState ι Signal) :
    (nextMMatrixPayoffMode selector mode action nextState).MatchesState
      nextState := by
  cases nextState <;> cases mode <;>
    simp [nextMMatrixPayoffMode, MMatrixPayoffMode.MatchesState]

theorem compiledMMatrixPayoffMode_matchesState
    {table : Table ι} {signalCount : ℕ}
    (signalLaw : PMF (Fin (signalCount + 1)))
    (selector : Option (NormalPlayer table) →
      Fin (signalCount + 1) → NormalPlayer table) :
    ∀ t (history : (publicQuittingGame table signalLaw).Hist t),
      (compiledMMatrixPayoffMode signalLaw selector t history).MatchesState
        history.2 := by
  intro t
  cases t with
  | zero =>
      intro history
      rcases history with ⟨past, state⟩
      cases state <;> simp [compiledMMatrixPayoffMode,
        MMatrixPayoffMode.MatchesState]
  | succ t =>
      intro history
      exact nextMMatrixPayoffMode_matchesState selector
        (compiledMMatrixPayoffMode signalLaw selector t
          (publicHistoryInit signalLaw history))
        (history.1 (Fin.last t)).2 history.2

namespace MMatrixPayoffBlueprint

variable {table : Table ι} [Nonempty (NormalPlayer table)]
  {hM : MMatrix (NormalMatrix table)} {value : Payoff ι} {accuracy : ℝ}

noncomputable def mode
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    ∀ t,
      (publicQuittingGame table blueprint.signalData.law).Hist t →
        MMatrixPayoffMode table :=
  compiledMMatrixPayoffMode blueprint.signalData.law
    blueprint.signalData.selector

noncomputable def strategy
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    (publicQuittingGame table blueprint.signalData.law).BehaviorProfile :=
  fun who t history =>
    match blueprint.mode t history with
    | .active target =>
        if _hwho : who = (hM.positiveOwner target).1 then
          quittingHazardCoin
            (mFineExitProbability table hM target accuracy)
            (mFineExitProbability_pos table hM target
              blueprint.accuracy_pos).le
            (mFineExitProbability_lt_one table hM target
              blueprint.accuracy_pos).le
        else PMF.pure false
    | _ => PMF.pure false

noncomputable def profile
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    SunspotProfile table :=
  { signalCount := blueprint.signalData.signalCount
    signalLaw := blueprint.signalData.law
    strategy := blueprint.strategy }

theorem mode_initial
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    blueprint.mode 0
      ((publicQuittingGame table blueprint.signalData.law).emptyHist .draw) =
      .draw none := rfl

theorem mode_state
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (t : ℕ)
    (history : (publicQuittingGame table blueprint.signalData.law).Hist t) :
    (blueprint.mode t history).MatchesState history.2 :=
  compiledMMatrixPayoffMode_matchesState
    blueprint.signalData.law blueprint.signalData.selector t history

@[simp] theorem mode_snoc
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (action : ι → Bool)
    (nextState : PublicQuittingState ι
      (Fin (blueprint.signalData.signalCount + 1))) :
    blueprint.mode (t + 1)
        (Fin.snoc history.1 (history.2, action), nextState) =
      nextMMatrixPayoffMode blueprint.signalData.selector
        (blueprint.mode t history) action nextState := by
  simp [MMatrixPayoffBlueprint.mode, compiledMMatrixPayoffMode,
    publicHistoryInit_snoc]

/-! The finite control-state kernel induced by the prescribed profile. -/
noncomputable def modeKernel
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    MMatrixPayoffMode table → PMF (MMatrixPayoffMode table)
  | .draw source => blueprint.signalData.law.map fun signal =>
      .active (blueprint.signalData.selector source signal)
  | .active target =>
      (quittingHazardCoin
          (mFineExitProbability table hM target accuracy)
          (mFineExitProbability_pos table hM target blueprint.accuracy_pos).le
          (mFineExitProbability_lt_one table hM target
            blueprint.accuracy_pos).le).bind fun quits =>
        if quits then PMF.pure .absorbed
        else PMF.pure (.draw (some target))
  | .absorbed => PMF.pure .absorbed

/-! The conditional law of the next control state at an actual history. -/
noncomputable def historyModeStepDist
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (strategy : (publicQuittingGame table
      blueprint.signalData.law).BehaviorProfile)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    PMF (MMatrixPayoffMode table) :=
  ((publicQuittingGame table
      blueprint.signalData.law).stageActionDist strategy history).bind
    fun action =>
      ((publicQuittingGame table blueprint.signalData.law).transition
          history.2 action).map fun nextState =>
        blueprint.mode (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)

private theorem stageActionDist_continue
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (hmode : ∀ target, blueprint.mode t history ≠ .active target) :
    (publicQuittingGame table blueprint.signalData.law).stageActionDist
        blueprint.strategy history =
      PMF.pure (publicAllContinueAction table blueprint.signalData.law) := by
  unfold StochasticGame.stageActionDist
  rw [show (fun who => blueprint.strategy who t history) =
      fun _ => PMF.pure false by
    funext who
    unfold strategy
    split
    next target heq => exact False.elim (hmode target heq)
    next => rfl]
  exact Math.PMFProduct.pmfPi_pure _

private theorem stageActionDist_active
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target) :
    (publicQuittingGame table blueprint.signalData.law).stageActionDist
        blueprint.strategy history =
      (quittingHazardCoin
          (mFineExitProbability table hM target accuracy)
          (mFineExitProbability_pos table hM target blueprint.accuracy_pos).le
          (mFineExitProbability_lt_one table hM target
            blueprint.accuracy_pos).le).bind fun quits =>
        PMF.pure (publicSingleQuitAction table blueprint.signalData.law
          (hM.positiveOwner target).1 quits) := by
  classical
  let coin := quittingHazardCoin
    (mFineExitProbability table hM target accuracy)
    (mFineExitProbability_pos table hM target blueprint.accuracy_pos).le
    (mFineExitProbability_lt_one table hM target
      blueprint.accuracy_pos).le
  let base : ∀ who,
      PMF ((publicQuittingGame table
        blueprint.signalData.law).Act who) :=
    fun _ => PMF.pure false
  let owner := (hM.positiveOwner target).1
  have hfamily :
      (fun who => blueprint.strategy who t history) =
        Function.update base owner coin := by
    funext who
    by_cases hwho : who = owner
    · subst who
      simp [strategy, hmode, coin, base, owner]
    · simp [strategy, hmode, base, owner, hwho]
  unfold StochasticGame.stageActionDist
  rw [hfamily]
  calc
    Math.PMFProduct.pmfPi (Function.update base owner coin) =
        coin.bind (fun quits => Math.PMFProduct.pmfPi
          (Function.update base owner (PMF.pure quits))) :=
      Math.PMFProduct.pmfPi_update_bind base owner coin
    _ = coin.bind (fun quits => PMF.pure
        (publicSingleQuitAction table blueprint.signalData.law
          owner quits)) := by
      apply congrArg (PMF.bind coin)
      funext quits
      rw [show Function.update base owner (PMF.pure quits) =
          fun who => PMF.pure
            (publicSingleQuitAction table blueprint.signalData.law
              owner quits who) by
        funext who
        by_cases hwho : who = owner
        · subst who
          simp [publicSingleQuitAction]
        · simp [base, publicSingleQuitAction_apply_eq_raw,
            rawSingleQuitAction, hwho]]
      exact Math.PMFProduct.pmfPi_pure _

private theorem stageActionDist_pureContinue_active
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target) :
    (publicQuittingGame table blueprint.signalData.law).stageActionDist
        (Function.update blueprint.strategy who
          (fun _ _ => PMF.pure false)) history =
      if who = (hM.positiveOwner target).1 then
        PMF.pure (publicAllContinueAction table blueprint.signalData.law)
      else
        (publicQuittingGame table blueprint.signalData.law).stageActionDist
          blueprint.strategy history := by
  classical
  split_ifs with hwho
  · subst who
    unfold StochasticGame.stageActionDist
    rw [show (fun player => Function.update blueprint.strategy
        (hM.positiveOwner target).1 (fun _ _ => PMF.pure false)
          player t history) = fun _ => PMF.pure false by
      funext player
      by_cases hplayer : player = (hM.positiveOwner target).1
      · subst player
        simp
        rfl
      · rw [Function.update_of_ne hplayer]
        simp [strategy, hmode, hplayer]
        rfl]
    exact Math.PMFProduct.pmfPi_pure _
  · apply congrArg Math.PMFProduct.pmfPi
    funext player
    by_cases hplayer : player = who
    · subst player
      rw [Function.update_self]
      simp [strategy, hmode, hwho]
    · rw [Function.update_of_ne hplayer]

private theorem stageActionDist_pureQuit_active
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target) :
    (publicQuittingGame table blueprint.signalData.law).stageActionDist
        (Function.update blueprint.strategy who
          (fun _ _ => PMF.pure true)) history =
      if who = (hM.positiveOwner target).1 then
        PMF.pure (publicSingleQuitAction table blueprint.signalData.law
          who true)
      else
        (quittingHazardCoin
            (mFineExitProbability table hM target accuracy)
            (mFineExitProbability_pos table hM target
              blueprint.accuracy_pos).le
            (mFineExitProbability_lt_one table hM target
              blueprint.accuracy_pos).le).bind fun quits =>
          PMF.pure (Function.update
            (publicSingleQuitAction table blueprint.signalData.law
              (hM.positiveOwner target).1 quits) who true) := by
  classical
  split_ifs with hwho
  · subst who
    unfold StochasticGame.stageActionDist
    rw [show (fun player => Function.update blueprint.strategy
        (hM.positiveOwner target).1 (fun _ _ => PMF.pure true)
          player t history) = fun player => PMF.pure
            (publicSingleQuitAction table blueprint.signalData.law
              (hM.positiveOwner target).1 true player) by
      funext player
      by_cases hplayer : player = (hM.positiveOwner target).1
      · subst player
        simp [publicSingleQuitAction]
      · rw [Function.update_of_ne hplayer]
        simp [strategy, hmode, publicSingleQuitAction_apply_eq_raw,
          rawSingleQuitAction, hplayer]]
    exact Math.PMFProduct.pmfPi_pure _
  · let coin := quittingHazardCoin
      (mFineExitProbability table hM target accuracy)
      (mFineExitProbability_pos table hM target
        blueprint.accuracy_pos).le
      (mFineExitProbability_lt_one table hM target
        blueprint.accuracy_pos).le
    let base : ∀ player,
        PMF ((publicQuittingGame table
          blueprint.signalData.law).Act player) :=
      fun player => if player = who then PMF.pure true else PMF.pure false
    have hfamily :
        (fun player => Function.update blueprint.strategy who
          (fun _ _ => PMF.pure true) player t history) =
          Function.update base (hM.positiveOwner target).1 coin := by
      funext player
      by_cases hplayerWho : player = who
      · subst player
        have hne : who ≠ (hM.positiveOwner target).1 := hwho
        rw [Function.update_self, Function.update_of_ne hne]
        simp [base]
      · rw [Function.update_of_ne hplayerWho]
        by_cases hplayerOwner :
            player = (hM.positiveOwner target).1
        · subst player
          simp [strategy, hmode, coin, base]
        · rw [Function.update_of_ne hplayerOwner]
          simp [strategy, hmode, base, hplayerWho, hplayerOwner]
    unfold StochasticGame.stageActionDist
    rw [hfamily, Math.PMFProduct.pmfPi_update_bind]
    apply congrArg (PMF.bind coin)
    funext quits
    rw [show Function.update base (hM.positiveOwner target).1
        (PMF.pure quits) = fun player => PMF.pure
          (Function.update
            (publicSingleQuitAction table blueprint.signalData.law
              (hM.positiveOwner target).1 quits) who true player) by
      funext player
      by_cases hplayerOwner :
          player = (hM.positiveOwner target).1
      · subst player
        have hne : (hM.positiveOwner target).1 ≠ who := Ne.symm hwho
        simp [base, publicSingleQuitAction, hne]
      · rw [Function.update_of_ne hplayerOwner]
        by_cases hplayerWho : player = who
        · subst player
          simp [base]
        · simp [base, hplayerWho, publicSingleQuitAction_apply_eq_raw,
            rawSingleQuitAction, hplayerOwner]]
    exact Math.PMFProduct.pmfPi_pure _

theorem historyModeStepDist_profile_eq
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    blueprint.historyModeStepDist blueprint.strategy history =
      blueprint.modeKernel (blueprint.mode t history) := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := blueprint.mode_state t (past, state)
  cases hmode : blueprint.mode t (past, state) with
  | draw source =>
      cases state with
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | draw =>
          have haction := blueprint.stageActionDist_continue
            (past, PublicQuittingState.draw) (by
              intro target hactive
              simp [hmode] at hactive)
          have hnext : ∀ signal,
              blueprint.mode (t + 1)
                  (Fin.snoc past
                    (PublicQuittingState.draw,
                      publicAllContinueAction table blueprint.signalData.law),
                    PublicQuittingState.active signal) =
                .active (blueprint.signalData.selector source signal) := by
            intro signal
            exact (blueprint.mode_snoc
              (past, PublicQuittingState.draw)
              (publicAllContinueAction table blueprint.signalData.law)
              (PublicQuittingState.active signal)).trans (by
                simp [hmode, nextMMatrixPayoffMode])
          unfold historyModeStepDist
          rw [haction, PMF.pure_bind]
          rw [publicQuittingGame_transition_draw]
          unfold modeKernel
          calc
            PMF.map _
                (PMF.map PublicQuittingState.active
                  blueprint.signalData.law) = PMF.map
                ((fun nextState => blueprint.mode (t + 1)
                    (Fin.snoc past
                      (PublicQuittingState.draw,
                        publicAllContinueAction table
                          blueprint.signalData.law), nextState)) ∘
                  PublicQuittingState.active)
                blueprint.signalData.law := PMF.map_comp _ _ _
            _ = PMF.map (fun signal => MMatrixPayoffMode.active
                (blueprint.signalData.selector source signal))
                blueprint.signalData.law := by
              apply congrArg
                (fun f => PMF.map f blueprint.signalData.law)
              funext signal
              exact hnext signal
  | active target =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          have haction := blueprint.stageActionDist_active
            (past, PublicQuittingState.active signal) target hmode
          unfold historyModeStepDist
          rw [haction, PMF.bind_bind]
          unfold modeKernel
          apply Math.ProbabilityMassFunction.bind_congr_on_support
          intro quits _
          cases quits with
          | false =>
              rw [PMF.pure_bind]
              rw [publicQuittingGame_transition_publicSingleQuitAction_false
                table blueprint.signalData.law signal
                (hM.positiveOwner target).1]
              rw [PMF.pure_map]
              congr 1
              rw [show blueprint.mode (t + 1)
                  (Fin.snoc past
                    (PublicQuittingState.active signal,
                      publicSingleQuitAction table blueprint.signalData.law
                        (hM.positiveOwner target).1 false),
                    PublicQuittingState.draw) =
                  nextMMatrixPayoffMode blueprint.signalData.selector
                    (blueprint.mode t
                      (past, PublicQuittingState.active signal))
                    (publicSingleQuitAction table blueprint.signalData.law
                      (hM.positiveOwner target).1 false) .draw by
                simpa using blueprint.mode_snoc
                  (past, PublicQuittingState.active signal)
                  (publicSingleQuitAction table blueprint.signalData.law
                    (hM.positiveOwner target).1 false) .draw]
              simp [hmode, nextMMatrixPayoffMode]
          | true =>
              rw [PMF.pure_bind]
              rw [publicQuittingGame_transition_publicSingleQuitAction
                table blueprint.signalData.law signal
                (hM.positiveOwner target).1]
              rw [PMF.pure_map]
              congr 1
  | absorbed =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          have haction := blueprint.stageActionDist_continue
            (past, PublicQuittingState.absorbed quitters) (by
              intro target hactive
              simp [hmode] at hactive)
          unfold historyModeStepDist
          rw [haction, PMF.pure_bind]
          simp only [publicQuittingGame, PMF.pure_map]
          unfold modeKernel
          congr 1

/-! The time-`t` marginal of the actual prescribed control mode. -/
noncomputable def modeDist
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (t : ℕ) : PMF (MMatrixPayoffMode table) :=
  ((publicQuittingGame table blueprint.signalData.law).histDist
      blueprint.strategy .draw t).map fun history =>
    blueprint.mode t history

theorem modeDist_succ
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (t : ℕ) :
    blueprint.modeDist (t + 1) =
      (blueprint.modeDist t).bind blueprint.modeKernel := by
  unfold modeDist
  rw [(publicQuittingGame table
    blueprint.signalData.law).histDist_succ,
    PMF.map_bind, PMF.bind_map]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro history _
  simp only [Function.comp_apply]
  rw [← blueprint.historyModeStepDist_profile_eq history]
  unfold historyModeStepDist
  rw [PMF.map_bind]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro action _
  rw [PMF.map_bind]
  simp_rw [PMF.pure_map]
  rw [← PMF.bind_pure_comp]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro nextState _
  rfl

theorem modeDist_eq_iter
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (t : ℕ) :
    blueprint.modeDist t =
      Math.PMFIter.iter blueprint.modeKernel t (.draw none) := by
  induction t with
  | zero =>
      unfold modeDist
      rw [(publicQuittingGame table
        blueprint.signalData.law).histDist_zero,
        PMF.pure_map, Math.PMFIter.iter_zero]
      simp [blueprint.mode_initial]
  | succ t ih =>
      rw [blueprint.modeDist_succ t, ih, Math.PMFIter.iter_succ']

def absorbedCore : Set (MMatrixPayoffMode table)
  | .absorbed => True
  | _ => False

theorem absorbedCore_closed
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    Math.Probability.IsClosedCore blueprint.modeKernel absorbedCore := by
  intro source hsource destination hdestination
  cases source with
  | draw source => contradiction
  | active target => contradiction
  | absorbed =>
      unfold modeKernel at hdestination
      have heq : destination = .absorbed := by
        simpa using hdestination
      subst destination
      trivial

private theorem hazardCoin_true_mem_support
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (target : NormalPlayer table) :
    true ∈ (quittingHazardCoin
      (mFineExitProbability table hM target accuracy)
      (mFineExitProbability_pos table hM target
        blueprint.accuracy_pos).le
      (mFineExitProbability_lt_one table hM target
        blueprint.accuracy_pos).le).support := by
  rw [PMF.mem_support_iff]
  intro hzero
  have hreal : ((quittingHazardCoin
      (mFineExitProbability table hM target accuracy)
      (mFineExitProbability_pos table hM target
        blueprint.accuracy_pos).le
      (mFineExitProbability_lt_one table hM target
        blueprint.accuracy_pos).le) true).toReal = 0 := by
    simp [hzero]
  rw [quittingHazardCoin_true_toReal] at hreal
  linarith [mFineExitProbability_pos table hM target
    blueprint.accuracy_pos]

private theorem draw_support_step
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (source : Option (NormalPlayer table))
    (signal : Fin (blueprint.signalData.signalCount + 1))
    (hsignal : signal ∈ blueprint.signalData.law.support) :
    Math.Probability.PMFSupportStep blueprint.modeKernel (.draw source)
      (.active (blueprint.signalData.selector source signal)) := by
  rw [Math.Probability.PMFSupportStep, ← PMF.mem_support_iff]
  unfold modeKernel
  rw [PMF.mem_support_map_iff]
  exact ⟨signal, hsignal, rfl⟩

private theorem active_absorbed_support_step
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (target : NormalPlayer table) :
    Math.Probability.PMFSupportStep blueprint.modeKernel (.active target)
      .absorbed := by
  rw [Math.Probability.PMFSupportStep, ← PMF.mem_support_iff]
  unfold modeKernel
  rw [PMF.mem_support_bind_iff]
  refine ⟨true, blueprint.hazardCoin_true_mem_support target, ?_⟩
  exact (PMF.mem_support_pure_iff _ _).2 rfl

theorem modeKernel_reaches_absorbed
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    ∀ source, Math.Probability.PMFReachable blueprint.modeKernel source
      .absorbed := by
  intro source
  cases source with
  | absorbed => exact Relation.ReflTransGen.refl
  | active target =>
      exact Relation.ReflTransGen.single
        (blueprint.active_absorbed_support_step target)
  | draw source =>
      obtain ⟨signal, hsignal⟩ := blueprint.signalData.law.support_nonempty
      exact Relation.ReflTransGen.head
        (blueprint.draw_support_step source signal hsignal)
        (Relation.ReflTransGen.single
          (blueprint.active_absorbed_support_step
            (blueprint.signalData.selector source signal)))

theorem modeKernel_transienceCertificate
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy) :
    Nonempty (Math.Probability.ClosedCoreTransienceCertificate
      blueprint.modeKernel absorbedCore) := by
  letI : Nonempty (MMatrixPayoffMode table) := ⟨.absorbed⟩
  exact Math.Probability.exists_closedCoreTransienceCertificate
    blueprint.modeKernel absorbedCore blueprint.absorbedCore_closed
    fun source => ⟨.absorbed, trivial,
      blueprint.modeKernel_reaches_absorbed source⟩

/-! ## Section 4.3 — the forced-Continue recurrent core

The infinite Markov implementation above is exact on path, but by itself it
does not handle a designated player who suppresses every prescribed exit.
The published proof's instruction to repeat Section 3 requires a finite
cutoff followed by a punishment tail.  The finite-chain fact below isolates
the required cutoff: after player `who` is forced to Continue, every recurrent
live active mode is designated to `who` itself. -/

noncomputable def continueModeKernel
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    MMatrixPayoffMode table → PMF (MMatrixPayoffMode table)
  | .draw source => blueprint.signalData.law.map fun signal =>
      .active (blueprint.signalData.selector source signal)
  | .active target =>
      if who = (hM.positiveOwner target).1 then
        PMF.pure (.draw (some target))
      else blueprint.modeKernel (.active target)
  | .absorbed => PMF.pure .absorbed

noncomputable def continueProfile
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    (publicQuittingGame table
      blueprint.signalData.law).BehaviorProfile :=
  Function.update blueprint.strategy who (fun _ _ => PMF.pure false)

private theorem stageActionDist_continueProfile
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    (publicQuittingGame table blueprint.signalData.law).stageActionDist
        (blueprint.continueProfile who) history =
      match blueprint.mode t history with
      | .active target =>
          if who = (hM.positiveOwner target).1 then
            PMF.pure
              (publicAllContinueAction table blueprint.signalData.law)
          else
            (publicQuittingGame table
              blueprint.signalData.law).stageActionDist
                blueprint.strategy history
      | _ => PMF.pure
          (publicAllContinueAction table blueprint.signalData.law) := by
  classical
  cases hmode : blueprint.mode t history with
  | active target =>
      exact blueprint.stageActionDist_pureContinue_active
        who history target hmode
  | draw source =>
      unfold continueProfile StochasticGame.stageActionDist
      rw [show (fun player => Function.update blueprint.strategy who
          (fun _ _ => PMF.pure false) player t history) =
          fun _ => PMF.pure false by
        funext player
        by_cases hplayer : player = who
        · subst player
          rw [Function.update_self]
          rfl
        · rw [Function.update_of_ne hplayer]
          simp [strategy, hmode]
          rfl]
      exact Math.PMFProduct.pmfPi_pure _
  | absorbed =>
      unfold continueProfile StochasticGame.stageActionDist
      rw [show (fun player => Function.update blueprint.strategy who
          (fun _ _ => PMF.pure false) player t history) =
          fun _ => PMF.pure false by
        funext player
        by_cases hplayer : player = who
        · subst player
          rw [Function.update_self]
          rfl
        · rw [Function.update_of_ne hplayer]
          simp [strategy, hmode]
          rfl]
      exact Math.PMFProduct.pmfPi_pure _

theorem historyModeStepDist_continueProfile_eq
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    blueprint.historyModeStepDist (blueprint.continueProfile who) history =
      blueprint.continueModeKernel who (blueprint.mode t history) := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := blueprint.mode_state t (past, state)
  cases hmode : blueprint.mode t (past, state) with
  | draw source =>
      cases state with
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | draw =>
          have haction := blueprint.stageActionDist_continueProfile
            who (past, PublicQuittingState.draw)
          rw [hmode] at haction
          have hnext : ∀ signal,
              blueprint.mode (t + 1)
                  (Fin.snoc past
                    (PublicQuittingState.draw,
                      publicAllContinueAction table blueprint.signalData.law),
                    PublicQuittingState.active signal) =
                .active (blueprint.signalData.selector source signal) := by
            intro signal
            exact (blueprint.mode_snoc
              (past, PublicQuittingState.draw)
              (publicAllContinueAction table blueprint.signalData.law)
              (PublicQuittingState.active signal)).trans (by
                simp [hmode, nextMMatrixPayoffMode])
          unfold historyModeStepDist continueModeKernel
          rw [haction, PMF.pure_bind,
            publicQuittingGame_transition_draw]
          calc
            PMF.map _
                (PMF.map PublicQuittingState.active
                  blueprint.signalData.law) =
                PMF.map
                  ((fun nextState => blueprint.mode (t + 1)
                    (Fin.snoc past
                      (PublicQuittingState.draw,
                        publicAllContinueAction table
                          blueprint.signalData.law), nextState)) ∘
                    PublicQuittingState.active)
                  blueprint.signalData.law := PMF.map_comp _ _ _
            _ = PMF.map (fun signal => MMatrixPayoffMode.active
                  (blueprint.signalData.selector source signal))
                blueprint.signalData.law := by
              apply congrArg
                (fun f => PMF.map f blueprint.signalData.law)
              funext signal
              exact hnext signal
  | active target =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          have haction := blueprint.stageActionDist_continueProfile
            who (past, PublicQuittingState.active signal)
          simp only [hmode] at haction
          by_cases hwho : who = (hM.positiveOwner target).1
          · rw [if_pos hwho] at haction
            unfold historyModeStepDist
            rw [haction, PMF.pure_bind]
            have hall :
                publicAllContinueAction table blueprint.signalData.law =
                  publicSingleQuitAction table blueprint.signalData.law
                    who false := by
              funext player
              simp [publicAllContinueAction,
                publicSingleQuitAction_apply_eq_raw,
                rawSingleQuitAction]
            rw [hall,
              publicQuittingGame_transition_publicSingleQuitAction_false]
            rw [PMF.pure_map]
            rw [blueprint.mode_snoc]
            simp [continueModeKernel, hmode, hwho,
              nextMMatrixPayoffMode]
          · rw [if_neg hwho] at haction
            unfold historyModeStepDist
            rw [haction]
            simpa [historyModeStepDist, hmode, continueModeKernel, hwho] using
              blueprint.historyModeStepDist_profile_eq
                (past, PublicQuittingState.active signal)
  | absorbed =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          have haction := blueprint.stageActionDist_continueProfile
            who (past, PublicQuittingState.absorbed quitters)
          rw [hmode] at haction
          unfold historyModeStepDist continueModeKernel
          rw [haction, PMF.pure_bind]
          simp only [publicQuittingGame, PMF.pure_map]
          congr 1

noncomputable def continueModeDist
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (t : ℕ) : PMF (MMatrixPayoffMode table) :=
  ((publicQuittingGame table blueprint.signalData.law).histDist
      (blueprint.continueProfile who) .draw t).map fun history =>
    blueprint.mode t history

theorem continueModeDist_succ
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (t : ℕ) :
    blueprint.continueModeDist who (t + 1) =
      (blueprint.continueModeDist who t).bind
        (blueprint.continueModeKernel who) := by
  unfold continueModeDist
  rw [(publicQuittingGame table
    blueprint.signalData.law).histDist_succ,
    PMF.map_bind, PMF.bind_map]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro history _
  simp only [Function.comp_apply]
  rw [← blueprint.historyModeStepDist_continueProfile_eq who history]
  unfold historyModeStepDist
  rw [PMF.map_bind]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro action _
  rw [PMF.map_bind]
  simp_rw [PMF.pure_map]
  rw [← PMF.bind_pure_comp]
  apply Math.ProbabilityMassFunction.bind_congr_on_support
  intro nextState _
  rfl

theorem continueModeDist_eq_iter
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (t : ℕ) :
    blueprint.continueModeDist who t =
      Math.PMFIter.iter (blueprint.continueModeKernel who) t (.draw none) := by
  induction t with
  | zero =>
      unfold continueModeDist
      rw [(publicQuittingGame table
        blueprint.signalData.law).histDist_zero,
        PMF.pure_map, Math.PMFIter.iter_zero]
      simp [blueprint.mode_initial]
  | succ t ih =>
      rw [blueprint.continueModeDist_succ who t, ih,
        Math.PMFIter.iter_succ']

/-! The canonical closed core for forced Continue: absorption together with
every mode from which absorption is no longer support-reachable. -/
def continueCore
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) : Set (MMatrixPayoffMode table) :=
  fun mode => mode = .absorbed ∨
    ¬Math.Probability.PMFReachable
      (blueprint.continueModeKernel who) mode .absorbed

theorem continueCore_closed
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    Math.Probability.IsClosedCore
      (blueprint.continueModeKernel who) (blueprint.continueCore who) := by
  intro source hsource destination hstep
  rcases hsource with rfl | hunreachable
  · unfold continueModeKernel at hstep
    have heq : destination = .absorbed := by simpa using hstep
    exact Or.inl heq
  · right
    intro hreach
    exact hunreachable
      (Relation.ReflTransGen.head hstep hreach)

theorem continueModeKernel_reaches_core
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (source : MMatrixPayoffMode table) :
    ∃ target, target ∈ blueprint.continueCore who ∧
      Math.Probability.PMFReachable
        (blueprint.continueModeKernel who) source target := by
  by_cases hreach : Math.Probability.PMFReachable
      (blueprint.continueModeKernel who) source .absorbed
  · exact ⟨.absorbed, Or.inl rfl, hreach⟩
  · exact ⟨source, Or.inr hreach, Relation.ReflTransGen.refl⟩

theorem continueModeKernel_transienceCertificate
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    Nonempty (Math.Probability.ClosedCoreTransienceCertificate
      (blueprint.continueModeKernel who) (blueprint.continueCore who)) := by
  letI : Nonempty (MMatrixPayoffMode table) := ⟨.absorbed⟩
  exact Math.Probability.exists_closedCoreTransienceCertificate
    (blueprint.continueModeKernel who) (blueprint.continueCore who)
    (blueprint.continueCore_closed who)
    (blueprint.continueModeKernel_reaches_core who)

theorem positiveOwner_eq_of_active_mem_continueCore
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (target : NormalPlayer table)
    (hcore : MMatrixPayoffMode.active target ∈
      blueprint.continueCore who) :
    (hM.positiveOwner target).1 = who := by
  rcases hcore with hfalse | hunreachable
  · simp at hfalse
  · by_contra hne
    apply hunreachable
    apply Relation.ReflTransGen.single
    have hkernel : blueprint.continueModeKernel who (.active target) =
        blueprint.modeKernel (.active target) := by
      simp only [continueModeKernel, if_neg (Ne.symm hne)]
    rw [Math.Probability.PMFSupportStep, hkernel]
    simpa [Math.Probability.PMFSupportStep] using
      blueprint.active_absorbed_support_step target

/-! At an odd cutoff only active live modes matter.  This charge records the
event that the selected punishment owner would not be the deviator. -/
def wrongOwnerActiveCharge
    (_blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) : MMatrixPayoffMode table → ℝ
  | .active target =>
      if (hM.positiveOwner target).1 = who then 0 else 1
  | _ => 0

theorem wrongOwnerActiveCharge_le_transientCharge
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (mode : MMatrixPayoffMode table) :
    blueprint.wrongOwnerActiveCharge who mode ≤
      Math.Probability.transientCharge
        (blueprint.continueCore who) mode := by
  by_cases hcore : mode ∈ blueprint.continueCore who
  · rw [Math.Probability.transientCharge_of_mem hcore]
    cases mode with
    | draw source => rfl
    | absorbed => rfl
    | active target =>
        simp [wrongOwnerActiveCharge,
          blueprint.positiveOwner_eq_of_active_mem_continueCore
            who target hcore]
  · rw [Math.Probability.transientCharge_of_not_mem hcore]
    cases mode with
    | draw source => simp [wrongOwnerActiveCharge]
    | absorbed => simp [wrongOwnerActiveCharge]
    | active target =>
        simp only [wrongOwnerActiveCharge]
        split <;> norm_num

/-! The continuation value attached to a live control mode.  At an absorbed
mode the actual terminal payoff is read from the public state instead. -/
noncomputable def modeContinuationValue
    (_blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) : MMatrixPayoffMode table → ℝ
  | .draw none => value who
  | .draw (some source) =>
      mFineContinuationPayoff table hM source accuracy who
  | .active target => mAxisPayoff table hM target who
  | .absorbed => 0

/-! The actual-history payoff martingale used in the proof of Theorem 4.3. -/
noncomputable def payoffPotential
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    (publicQuittingGame table
      blueprint.signalData.law).HistoryPotential :=
  fun t history => match blueprint.mode t history with
    | .absorbed => match history.2 with
        | .absorbed quitters => table.terminal quitters who
        | _ => 0
    | mode => blueprint.modeContinuationValue who mode

/-! The one-stage collision budget used against a unilateral deviation.
It is present exactly while the public game is live. -/
noncomputable def deviationPotential
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    (publicQuittingGame table
      blueprint.signalData.law).HistoryPotential :=
  fun t history => blueprint.payoffPotential who t history +
    match blueprint.mode t history with
    | .absorbed => 0
    | _ => accuracy

theorem deviationPotential_initial
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    blueprint.deviationPotential who 0
        ((publicQuittingGame table
          blueprint.signalData.law).emptyHist .draw) =
      value who + accuracy := by
  simp [deviationPotential, payoffPotential, blueprint.mode_initial,
    modeContinuationValue]

private theorem pureQuit_nextTerminalPayoff_le_accuracy
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU
        (Function.update blueprint.strategy who
          (fun _ _ => PMF.pure true))
        (fun _ nextHistory => match nextHistory.2 with
          | .absorbed quitters => table.terminal quitters who
          | _ => 0) history ≤ accuracy := by
  classical
  have hstate : ∃ signal, history.2 = PublicQuittingState.active signal := by
    have hmatches := blueprint.mode_state t history
    cases hs : history.2 <;>
      simp [MMatrixPayoffMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨signal, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  have haction := blueprint.stageActionDist_pureQuit_active who
    (past, PublicQuittingState.active signal) target hmode
  by_cases hwho : who = (hM.positiveOwner target).1
  · rw [if_pos hwho] at haction
    unfold StochasticGame.historyContinuationEU
    rw [haction, Math.Probability.expect_pure,
      publicQuittingGame_transition_publicSingleQuitAction,
      Math.Probability.expect_pure]
    subst who
    change table.terminal
      (quittingProjectiveSingletonTerminal
        (hM.positiveOwner target).1)
        (hM.positiveOwner target).1 ≤ accuracy
    rw [blueprint.soloExitNormalized]
    exact blueprint.accuracy_pos.le
  · rw [if_neg hwho] at haction
    unfold StochasticGame.historyContinuationEU
    rw [haction, Math.Probability.expect_bind]
    simp_rw [Math.Probability.expect_pure]
    let coin := quittingHazardCoin
      (mFineExitProbability table hM target accuracy)
      (mFineExitProbability_pos table hM target
        blueprint.accuracy_pos).le
      (mFineExitProbability_lt_one table hM target
        blueprint.accuracy_pos).le
    change expect coin (fun quits => expect
        ((publicQuittingGame table blueprint.signalData.law).transition
          (PublicQuittingState.active signal)
          (Function.update (publicSingleQuitAction table
            blueprint.signalData.law (hM.positiveOwner target).1 quits)
            who true))
        (fun nextState => match nextState with
          | .absorbed quitters => table.terminal quitters who
          | _ => 0)) ≤ accuracy
    calc
      expect coin (fun quits => expect
          ((publicQuittingGame table blueprint.signalData.law).transition
            (PublicQuittingState.active signal)
            (Function.update (publicSingleQuitAction table
              blueprint.signalData.law (hM.positiveOwner target).1 quits)
              who true))
          (fun nextState => match nextState with
            | .absorbed quitters => table.terminal quitters who
            | _ => 0)) ≤ expect coin (fun quits => if quits then 1 else 0) := by
        apply Math.Probability.expect_mono
        intro quits
        cases quits with
        | false =>
            have hactionEq : Function.update
                (publicSingleQuitAction table blueprint.signalData.law
                  (hM.positiveOwner target).1 false) who true =
                publicSingleQuitAction table blueprint.signalData.law
                  who true := by
              funext player
              by_cases hplayer : player = who
              · subst player
                simp [publicSingleQuitAction]
              · simp [publicSingleQuitAction_apply_eq_raw,
                  rawSingleQuitAction, hplayer]
            rw [hactionEq,
              publicQuittingGame_transition_publicSingleQuitAction,
              Math.Probability.expect_pure]
            exact blueprint.soloExitNormalized who |>.le
        | true =>
            simp only [publicQuittingGame]
            split
            · rw [Math.Probability.expect_pure]
              exact (le_abs_self _).trans
                (blueprint.payoffsBounded.1 _ who)
            · rename_i hfalse
              exfalso
              apply hfalse
              refine ⟨who, ?_⟩
              simp
      _ = mFineExitProbability table hM target accuracy := by
        rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
        simp [coin]
      _ ≤ accuracy :=
        (mFineExitProbability_lt_accuracy table hM target
          blueprint.accuracy_pos).le

theorem expect_axisPayoff_signal
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (source : Option (NormalPlayer table)) (who : ι) :
    expect blueprint.signalData.law (fun signal =>
      mAxisPayoff table hM
        (blueprint.signalData.selector source signal) who) =
      match source with
      | none => value who
      | some target =>
          mFineContinuationPayoff table hM target accuracy who := by
  calc
    expect blueprint.signalData.law (fun signal =>
        mAxisPayoff table hM
          (blueprint.signalData.selector source signal) who) =
        expect (blueprint.signalData.law.map
          (blueprint.signalData.selector source))
          (fun target => mAxisPayoff table hM target who) := by
      rw [Math.Probability.expect_map]
    _ = expect (blueprint.signalMarginal source)
        (fun target => mAxisPayoff table hM target who) := by
      rw [blueprint.signalData.selector_law source]
    _ = _ := by
      cases source with
      | none =>
          rw [Math.Probability.expect_eq_sum]
          simp_rw [blueprint.signalMarginal_none_toReal]
          have hbalance := congrFun blueprint.initialWeight_axis_balance who
          simpa only [Finset.sum_apply, Pi.smul_apply, smul_eq_mul]
            using hbalance
      | some source =>
          rw [Math.Probability.expect_eq_sum]
          simp_rw [blueprint.signalMarginal_some_toReal]
          have hbalance := congrFun
            (mFineTransitionWeight_axis_balance table hM source accuracy
              blueprint.accuracy_pos) who
          simpa only [Finset.sum_apply, Pi.smul_apply, smul_eq_mul]
            using hbalance

private theorem expect_hazard_terminal_continuation
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (target : NormalPlayer table) (who : ι) :
    expect (quittingHazardCoin
        (mFineExitProbability table hM target accuracy)
        (mFineExitProbability_pos table hM target
          blueprint.accuracy_pos).le
        (mFineExitProbability_lt_one table hM target
          blueprint.accuracy_pos).le)
      (fun quits => if quits then
        table.terminal (quittingProjectiveSingletonTerminal
          (hM.positiveOwner target).1) who
      else mFineContinuationPayoff table hM target accuracy who) =
    mAxisPayoff table hM target who := by
  rw [Math.Probability.expect_eq_sum, Fintype.sum_bool]
  simp only [quittingHazardCoin_false_toReal,
    quittingHazardCoin_true_toReal, Bool.false_eq_true, ↓reduceIte]
  have hbalance := congrFun
    (mAxisPayoff_fine_exit_balance table hM target
      blueprint.accuracy_pos) who
  simpa only [Pi.add_apply, Pi.smul_apply, smul_eq_mul] using hbalance.symm

private theorem payoffPotential_snoc_active_of_draw
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (source : Option (NormalPlayer table))
    (hmode : blueprint.mode t history = .draw source)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct)
    (signal : Fin (blueprint.signalData.signalCount + 1)) :
    blueprint.payoffPotential who (t + 1)
        (Fin.snoc history.1 (history.2, action), .active signal) =
      mAxisPayoff table hM
        (blueprint.signalData.selector source signal) who := by
  unfold payoffPotential
  rw [blueprint.mode_snoc]
  simp [hmode, nextMMatrixPayoffMode, modeContinuationValue]

private theorem payoffPotential_snoc_draw_of_active
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct) :
    blueprint.payoffPotential who (t + 1)
        (Fin.snoc history.1 (history.2, action), .draw) =
      mFineContinuationPayoff table hM target accuracy who := by
  unfold payoffPotential
  rw [blueprint.mode_snoc]
  simp [hmode, nextMMatrixPayoffMode, modeContinuationValue]

private theorem payoffPotential_snoc_singleton_of_active
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (_hmode : blueprint.mode t history = .active target)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct) :
    blueprint.payoffPotential who (t + 1)
        (Fin.snoc history.1 (history.2, action),
          .absorbed (quittingProjectiveSingletonTerminal
            (hM.positiveOwner target).1)) =
      table.terminal (quittingProjectiveSingletonTerminal
        (hM.positiveOwner target).1) who := by
  unfold payoffPotential
  rw [blueprint.mode_snoc]
  simp [nextMMatrixPayoffMode]

private theorem payoffPotential_snoc_absorbed
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct)
    (quitters : {S : Finset ι // S.Nonempty}) :
    blueprint.payoffPotential who (t + 1)
        (Fin.snoc history.1 (history.2, action), .absorbed quitters) =
      table.terminal quitters who := by
  unfold payoffPotential
  rw [blueprint.mode_snoc]
  simp [nextMMatrixPayoffMode]

private theorem payoffPotential_draw_step
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (source : Option (NormalPlayer table))
    (hmode : blueprint.mode t history = .draw source)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct) :
    expect (blueprint.signalData.law.map PublicQuittingState.active)
        (fun nextState => blueprint.payoffPotential who (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)) =
      blueprint.payoffPotential who t history := by
  calc
    expect (blueprint.signalData.law.map PublicQuittingState.active)
        (fun nextState => blueprint.payoffPotential who (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)) =
        expect blueprint.signalData.law (fun signal =>
          blueprint.payoffPotential who (t + 1)
            (Fin.snoc history.1 (history.2, action),
              PublicQuittingState.active signal)) := by
      rw [Math.Probability.expect_map]
    _ = expect blueprint.signalData.law (fun signal =>
        mAxisPayoff table hM
          (blueprint.signalData.selector source signal) who) := by
      apply Math.ProbabilityMassFunction.expect_congr_on_support
      intro signal _
      exact blueprint.payoffPotential_snoc_active_of_draw who history
        source hmode action signal
    _ = blueprint.modeContinuationValue who (.draw source) := by
      cases source with
      | none =>
          simpa [modeContinuationValue] using
            blueprint.expect_axisPayoff_signal none who
      | some target =>
          simpa [modeContinuationValue] using
            blueprint.expect_axisPayoff_signal (some target) who
    _ = blueprint.payoffPotential who t history := by
      cases source <;>
        simp [payoffPotential, hmode, modeContinuationValue]

theorem payoffPotential_harmonic
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU
        blueprint.strategy (blueprint.payoffPotential who) history =
      blueprint.payoffPotential who t history := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := blueprint.mode_state t (past, state)
  cases hmode : blueprint.mode t (past, state) with
  | draw source =>
      cases state with
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | draw =>
          have haction := blueprint.stageActionDist_continue
            (past, PublicQuittingState.draw) (by
              intro target hactive
              simp [hmode] at hactive)
          unfold StochasticGame.historyContinuationEU
          rw [haction, Math.Probability.expect_pure,
            publicQuittingGame_transition_draw]
          exact blueprint.payoffPotential_draw_step who
            (past, PublicQuittingState.draw) source hmode
            (publicAllContinueAction table blueprint.signalData.law)
  | active target =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          have haction := blueprint.stageActionDist_active
            (past, PublicQuittingState.active signal) target hmode
          unfold StochasticGame.historyContinuationEU
          rw [haction, Math.Probability.expect_bind]
          simp_rw [Math.Probability.expect_pure]
          calc
            expect (quittingHazardCoin
                (mFineExitProbability table hM target accuracy)
                (mFineExitProbability_pos table hM target
                  blueprint.accuracy_pos).le
                (mFineExitProbability_lt_one table hM target
                  blueprint.accuracy_pos).le)
                (fun quits => expect
                  ((publicQuittingGame table
                    blueprint.signalData.law).transition
                      (PublicQuittingState.active signal)
                      (publicSingleQuitAction table
                        blueprint.signalData.law
                        (hM.positiveOwner target).1 quits))
                  (fun nextState => blueprint.payoffPotential who (t + 1)
                    (Fin.snoc past
                      (PublicQuittingState.active signal,
                        publicSingleQuitAction table
                          blueprint.signalData.law
                          (hM.positiveOwner target).1 quits),
                      nextState))) =
                expect (quittingHazardCoin
                    (mFineExitProbability table hM target accuracy)
                    (mFineExitProbability_pos table hM target
                      blueprint.accuracy_pos).le
                    (mFineExitProbability_lt_one table hM target
                      blueprint.accuracy_pos).le)
                  (fun quits => if quits then
                    table.terminal (quittingProjectiveSingletonTerminal
                      (hM.positiveOwner target).1) who
                  else mFineContinuationPayoff table hM target accuracy who) := by
              apply Math.ProbabilityMassFunction.expect_congr_on_support
              intro quits _
              cases quits with
              | false =>
                  rw [publicQuittingGame_transition_publicSingleQuitAction_false,
                    Math.Probability.expect_pure]
                  exact blueprint.payoffPotential_snoc_draw_of_active who
                    (past, PublicQuittingState.active signal) target hmode _
              | true =>
                  rw [publicQuittingGame_transition_publicSingleQuitAction,
                    Math.Probability.expect_pure]
                  exact blueprint.payoffPotential_snoc_singleton_of_active who
                    (past, PublicQuittingState.active signal) target hmode _
            _ = mAxisPayoff table hM target who :=
              blueprint.expect_hazard_terminal_continuation target who
            _ = blueprint.payoffPotential who t
                (past, PublicQuittingState.active signal) := by
              simp [payoffPotential, hmode, modeContinuationValue]
  | absorbed =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          have haction := blueprint.stageActionDist_continue
            (past, PublicQuittingState.absorbed quitters) (by
              intro target hactive
              simp [hmode] at hactive)
          unfold StochasticGame.historyContinuationEU
          rw [haction, Math.Probability.expect_pure,
            publicQuittingGame_transition_absorbed,
            Math.Probability.expect_pure]
          rw [blueprint.payoffPotential_snoc_absorbed who
            (past, PublicQuittingState.absorbed quitters)]
          simp [payoffPotential, hmode]

private theorem historyContinuationEU_deviationPotential_le_add_accuracy
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι)
    (strategy : (publicQuittingGame table
      blueprint.signalData.law).BehaviorProfile)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy
        (blueprint.deviationPotential who) history ≤
      (publicQuittingGame table
        blueprint.signalData.law).historyContinuationEU strategy
          (blueprint.payoffPotential who) history + accuracy := by
  let lifted : (publicQuittingGame table
      blueprint.signalData.law).HistoryPotential :=
    fun nextTime nextHistory =>
      blueprint.payoffPotential who nextTime nextHistory + accuracy * 1
  have hmono : (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy
        (blueprint.deviationPotential who) history ≤
      (publicQuittingGame table
        blueprint.signalData.law).historyContinuationEU strategy
          lifted history := by
    unfold StochasticGame.historyContinuationEU
    apply Math.Probability.expect_mono
    intro action
    apply Math.Probability.expect_mono
    intro nextState
    unfold deviationPotential lifted
    split <;> linarith [blueprint.accuracy_pos]
  have hsplit := historyContinuationEU_add_const_mul
    blueprint.signalData.law strategy (blueprint.payoffPotential who)
      (fun _ _ => 1) accuracy history
  have hone : (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy
        (fun _ _ => 1) history = 1 := by
    unfold StochasticGame.historyContinuationEU
    simp only [Math.Probability.expect_const]
  rw [hone, mul_one] at hsplit
  exact hmono.trans_eq (by simpa [lifted] using hsplit)

private theorem pureQuit_deviationPotential_le
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU
        (Function.update blueprint.strategy who
          (fun _ _ => PMF.pure true))
        (blueprint.deviationPotential who) history ≤
      blueprint.deviationPotential who t history := by
  classical
  have hstate : ∃ signal,
      history.2 = PublicQuittingState.active signal := by
    have hmatches := blueprint.mode_state t history
    cases hs : history.2 <;>
      simp [MMatrixPayoffMode.MatchesState, hmode, hs] at hmatches
    exact ⟨_, rfl⟩
  obtain ⟨signal, hstate⟩ := hstate
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  let strategy := Function.update blueprint.strategy who
    (fun _ _ => PMF.pure true)
  let terminal : (publicQuittingGame table
      blueprint.signalData.law).HistoryPotential :=
    fun _ nextHistory => match nextHistory.2 with
      | .absorbed quitters => table.terminal quitters who
      | _ => 0
  have hcongr : (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy
        (blueprint.deviationPotential who)
          (past, PublicQuittingState.active signal) =
      (publicQuittingGame table
        blueprint.signalData.law).historyContinuationEU strategy
          terminal (past, PublicQuittingState.active signal) := by
    apply historyContinuationEU_congr_next_profile
    intro action haction nextState hnextState
    have hactionQuit : action who = true := by
      unfold strategy StochasticGame.stageActionDist at haction
      let root : ∀ player,
          PMF ((publicQuittingGame table
            blueprint.signalData.law).Act player) :=
        fun player => blueprint.strategy player t
          (past, PublicQuittingState.active signal)
      have hfamily :
          (fun player => Function.update blueprint.strategy who
            (fun _ _ => PMF.pure true) player t
              (past, PublicQuittingState.active signal)) =
            Function.update root who (PMF.pure true) := by
        funext player
        by_cases hplayer : player = who
        · subst player
          simp [root]
        · simp [root, Function.update_of_ne hplayer]
      rw [hfamily] at haction
      by_contra hne
      have hzero : Math.PMFProduct.pmfPi
          (Function.update root who (PMF.pure true)) action = 0 := by
        rw [Math.PMFProduct.pmfPi_apply]
        apply Finset.prod_eq_zero (Finset.mem_univ who)
        rw [Function.update_self, PMF.pure_apply, if_neg hne]
      exact (PMF.mem_support_iff _ action).mp haction hzero
    simp only [publicQuittingGame] at hnextState
    split at hnextState
    · rename_i hquitters
      have hnext := (PMF.mem_support_pure_iff _ _).mp hnextState
      subst nextState
      unfold deviationPotential payoffPotential terminal
      rw [blueprint.mode_snoc]
      simp [nextMMatrixPayoffMode]
    · rename_i himpossible
      exfalso
      apply himpossible
      refine ⟨who, ?_⟩
      simp only [Finset.mem_filter, Finset.mem_univ, true_and]
      change (show Bool from action who) = true at hactionQuit
      exact hactionQuit
  rw [hcongr]
  have hterminal := blueprint.pureQuit_nextTerminalPayoff_le_accuracy
    who (past, PublicQuittingState.active signal) target hmode
  change (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy terminal
        (past, PublicQuittingState.active signal) ≤ _
  have hcurrent : 0 ≤ mAxisPayoff table hM target who :=
    mAxisPayoff_nonneg table hM blueprint.soloExitNormalized
      blueprint.payoffsBounded target who
  unfold deviationPotential payoffPotential
  rw [hmode]
  change (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy terminal
        (past, PublicQuittingState.active signal) ≤
      mAxisPayoff table hM target who + accuracy
  exact hterminal.trans (le_add_of_nonneg_left hcurrent)

private theorem pureContinue_deviationPotential_le
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (target : NormalPlayer table)
    (hmode : blueprint.mode t history = .active target) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU
        (Function.update blueprint.strategy who
          (fun _ _ => PMF.pure false))
        (blueprint.deviationPotential who) history ≤
      blueprint.deviationPotential who t history := by
  classical
  by_cases hwho : who = (hM.positiveOwner target).1
  · have hstate : ∃ signal,
        history.2 = PublicQuittingState.active signal := by
      have hmatches := blueprint.mode_state t history
      cases hs : history.2 <;>
        simp [MMatrixPayoffMode.MatchesState, hmode, hs] at hmatches
      exact ⟨_, rfl⟩
    obtain ⟨signal, hstate⟩ := hstate
    rcases history with ⟨past, state⟩
    simp only at hstate
    subst state
    have haction := blueprint.stageActionDist_pureContinue_active who
      (past, PublicQuittingState.active signal) target hmode
    rw [if_pos hwho] at haction
    unfold StochasticGame.historyContinuationEU
    rw [haction, Math.Probability.expect_pure]
    have hall : publicAllContinueAction table blueprint.signalData.law =
        publicSingleQuitAction table blueprint.signalData.law who false := by
      funext player
      simp [publicAllContinueAction, publicSingleQuitAction_apply_eq_raw,
        rawSingleQuitAction]
    rw [hall,
      publicQuittingGame_transition_publicSingleQuitAction_false,
      Math.Probability.expect_pure]
    unfold deviationPotential payoffPotential
    rw [blueprint.mode_snoc]
    simp [hmode, nextMMatrixPayoffMode, modeContinuationValue]
    subst who
    rw [mAxisPayoff_designatedOwner_zero table hM
        blueprint.soloExitNormalized target,
      mFineContinuationPayoff_designatedOwner_zero table hM
        blueprint.soloExitNormalized target blueprint.accuracy_pos]
  · let strategy := Function.update blueprint.strategy who
        (fun _ _ => PMF.pure false)
    have hupper := blueprint.historyContinuationEU_deviationPotential_le_add_accuracy
      who strategy history
    have haction := blueprint.stageActionDist_pureContinue_active who
      history target hmode
    rw [if_neg hwho] at haction
    dsimp only [strategy] at hupper
    unfold StochasticGame.historyContinuationEU at hupper
    rw [haction] at hupper
    have hharmonic := blueprint.payoffPotential_harmonic who history
    unfold StochasticGame.historyContinuationEU at hharmonic
    rw [hharmonic] at hupper
    unfold StochasticGame.historyContinuationEU
    rw [haction]
    unfold deviationPotential
    rw [hmode]
    exact hupper

private theorem deviationPotential_draw_step
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (source : Option (NormalPlayer table))
    (hmode : blueprint.mode t history = .draw source)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct) :
    expect (blueprint.signalData.law.map PublicQuittingState.active)
        (fun nextState => blueprint.deviationPotential who (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)) =
      blueprint.deviationPotential who t history := by
  calc
    expect (blueprint.signalData.law.map PublicQuittingState.active)
        (fun nextState => blueprint.deviationPotential who (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState)) =
      expect (blueprint.signalData.law.map PublicQuittingState.active)
        (fun nextState => blueprint.payoffPotential who (t + 1)
          (Fin.snoc history.1 (history.2, action), nextState) +
            accuracy) := by
        apply Math.ProbabilityMassFunction.expect_congr_on_support
        intro nextState hnext
        rw [PMF.mem_support_map_iff] at hnext
        obtain ⟨signal, _, rfl⟩ := hnext
        unfold deviationPotential
        rw [blueprint.mode_snoc]
        simp [hmode, nextMMatrixPayoffMode]
    _ = expect (blueprint.signalData.law.map PublicQuittingState.active)
          (fun nextState => blueprint.payoffPotential who (t + 1)
            (Fin.snoc history.1 (history.2, action), nextState)) +
        accuracy := by
      rw [Math.Probability.expect_add, Math.Probability.expect_const]
    _ = blueprint.payoffPotential who t history + accuracy := by
      rw [blueprint.payoffPotential_draw_step who history source hmode action]
    _ = blueprint.deviationPotential who t history := by
      simp [deviationPotential, hmode]

private theorem deviationPotential_absorbed_step
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct)
    (quitters : {S : Finset ι // S.Nonempty}) :
    blueprint.deviationPotential who (t + 1)
        (Fin.snoc history.1 (history.2, action), .absorbed quitters) =
      table.terminal quitters who := by
  unfold deviationPotential payoffPotential
  rw [blueprint.mode_snoc]
  simp [nextMMatrixPayoffMode]

private def pureDeviation
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (action : Bool) :
    (publicQuittingGame table
      blueprint.signalData.law).BehaviorStrategy who :=
  fun _ _ => PMF.pure action

private theorem deviationPotential_harmonic_draw
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι)
    (strategy : (publicQuittingGame table
      blueprint.signalData.law).BehaviorProfile)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (source : Option (NormalPlayer table))
    (hmode : blueprint.mode t history = .draw source)
    (hstate : history.2 = .draw) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy
        (blueprint.deviationPotential who) history =
      blueprint.deviationPotential who t history := by
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  have hinner (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct) :
      expect ((publicQuittingGame table
          blueprint.signalData.law).transition .draw action)
        (fun nextState => blueprint.deviationPotential who (t + 1)
          (Fin.snoc past (.draw, action), nextState)) =
        blueprint.deviationPotential who t (past, .draw) := by
    rw [publicQuittingGame_transition_draw]
    exact blueprint.deviationPotential_draw_step who
      (past, PublicQuittingState.draw) source hmode action
  unfold StochasticGame.historyContinuationEU
  simp_rw [hinner]
  exact Math.Probability.expect_const _ _

private theorem deviationPotential_harmonic_absorbed
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι)
    (strategy : (publicQuittingGame table
      blueprint.signalData.law).BehaviorProfile)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t)
    (hmode : blueprint.mode t history = .absorbed)
    (quitters : {S : Finset ι // S.Nonempty})
    (hstate : history.2 = .absorbed quitters) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU strategy
        (blueprint.deviationPotential who) history =
      blueprint.deviationPotential who t history := by
  rcases history with ⟨past, state⟩
  simp only at hstate
  subst state
  have hinner (action : (publicQuittingGame table
      blueprint.signalData.law).JointAct) :
      expect ((publicQuittingGame table
          blueprint.signalData.law).transition (.absorbed quitters) action)
        (fun nextState => blueprint.deviationPotential who (t + 1)
          (Fin.snoc past (.absorbed quitters, action), nextState)) =
        blueprint.deviationPotential who t (past, .absorbed quitters) := by
    rw [publicQuittingGame_transition_absorbed,
      Math.Probability.expect_pure]
    rw [blueprint.deviationPotential_absorbed_step who
      (past, PublicQuittingState.absorbed quitters)]
    simp [deviationPotential, payoffPotential, hmode]
  unfold StochasticGame.historyContinuationEU
  simp_rw [hinner]
  exact Math.Probability.expect_const _ _

private theorem pureAction_deviationPotential_le
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (action : Bool) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU
        (Function.update blueprint.strategy who
          (blueprint.pureDeviation who action))
        (blueprint.deviationPotential who) history ≤
      blueprint.deviationPotential who t history := by
  classical
  rcases history with ⟨past, state⟩
  have hmatches := blueprint.mode_state t (past, state)
  cases hmode : blueprint.mode t (past, state) with
  | active target =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          cases action with
          | false =>
              change (publicQuittingGame table
                  blueprint.signalData.law).historyContinuationEU
                    (Function.update blueprint.strategy who
                      (fun _ _ => PMF.pure false))
                    (blueprint.deviationPotential who)
                    (past, PublicQuittingState.active signal) ≤ _
              exact blueprint.pureContinue_deviationPotential_le who
                (past, PublicQuittingState.active signal) target hmode
          | true =>
              change (publicQuittingGame table
                  blueprint.signalData.law).historyContinuationEU
                    (Function.update blueprint.strategy who
                      (fun _ _ => PMF.pure true))
                    (blueprint.deviationPotential who)
                    (past, PublicQuittingState.active signal) ≤ _
              exact blueprint.pureQuit_deviationPotential_le who
                (past, PublicQuittingState.active signal) target hmode
  | draw source =>
      cases state with
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | draw =>
          exact (blueprint.deviationPotential_harmonic_draw who _
            (past, PublicQuittingState.draw) source hmode rfl).le
  | absorbed =>
      cases state with
      | draw =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | active signal =>
          simp [MMatrixPayoffMode.MatchesState, hmode] at hmatches
      | absorbed quitters =>
          exact (blueprint.deviationPotential_harmonic_absorbed who _
            (past, PublicQuittingState.absorbed quitters) hmode quitters rfl).le

theorem deviationPotential_superharmonic
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι)
    (deviation : (publicQuittingGame table
      blueprint.signalData.law).BehaviorStrategy who)
    {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    (publicQuittingGame table
      blueprint.signalData.law).historyContinuationEU
        (Function.update blueprint.strategy who deviation)
        (blueprint.deviationPotential who) history ≤
      blueprint.deviationPotential who t history := by
  rw [historyContinuationEU_update_eq_expect_pure table
    blueprint.signalData.law blueprint.strategy who deviation
      (blueprint.deviationPotential who) history]
  calc
    expect (deviation t history) (fun action =>
        (publicQuittingGame table
          blueprint.signalData.law).historyContinuationEU
            (Function.update blueprint.strategy who
              (fun _ _ => PMF.pure action))
            (blueprint.deviationPotential who) history) ≤
      expect (deviation t history) (fun _ =>
        blueprint.deviationPotential who t history) := by
      apply Math.Probability.expect_mono
      intro action
      have hpure := blueprint.pureAction_deviationPotential_le
        who action history
      unfold pureDeviation at hpure
      exact hpure
    _ = blueprint.deviationPotential who t history :=
      Math.Probability.expect_const _ _

theorem expect_deviationPotential_le_initial
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι)
    (deviation : (publicQuittingGame table
      blueprint.signalData.law).BehaviorStrategy who) : ∀ t,
    expect ((publicQuittingGame table
        blueprint.signalData.law).histDist
      (Function.update blueprint.strategy who deviation) .draw t)
      (blueprint.deviationPotential who t) ≤ value who + accuracy
  | 0 => by
      rw [(publicQuittingGame table
        blueprint.signalData.law).histDist_zero,
        Math.Probability.expect_pure,
        blueprint.deviationPotential_initial who]
  | t + 1 => by
      change (publicQuittingGame table
        blueprint.signalData.law).expectedHistoryValue
          (Function.update blueprint.strategy who deviation) .draw
          (blueprint.deviationPotential who) (t + 1) ≤ _
      rw [(publicQuittingGame table
        blueprint.signalData.law).expectedHistoryValue_succ]
      calc
        expect ((publicQuittingGame table
            blueprint.signalData.law).histDist
          (Function.update blueprint.strategy who deviation) .draw t)
          (fun history => (publicQuittingGame table
            blueprint.signalData.law).historyContinuationEU
              (Function.update blueprint.strategy who deviation)
              (blueprint.deviationPotential who) history) ≤
          expect ((publicQuittingGame table
              blueprint.signalData.law).histDist
            (Function.update blueprint.strategy who deviation) .draw t)
            (blueprint.deviationPotential who t) := by
          apply Math.Probability.expect_mono
          exact fun history => blueprint.deviationPotential_superharmonic
            who deviation history
        _ ≤ value who + accuracy :=
          blueprint.expect_deviationPotential_le_initial who deviation t

theorem expect_payoffPotential_eq_value
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) : ∀ t,
    expect ((publicQuittingGame table
        blueprint.signalData.law).histDist
      blueprint.strategy .draw t) (blueprint.payoffPotential who t) =
      value who
  | 0 => by
      rw [(publicQuittingGame table
        blueprint.signalData.law).histDist_zero,
        Math.Probability.expect_pure]
      simp [payoffPotential, blueprint.mode_initial, modeContinuationValue]
  | t + 1 => by
      change (publicQuittingGame table
        blueprint.signalData.law).expectedHistoryValue
          blueprint.strategy .draw (blueprint.payoffPotential who) (t + 1) = _
      rw [(publicQuittingGame table
        blueprint.signalData.law).expectedHistoryValue_succ]
      simp_rw [blueprint.payoffPotential_harmonic who]
      exact blueprint.expect_payoffPotential_eq_value who t

noncomputable def payoffResidual
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) : MMatrixPayoffMode table → ℝ
  | .absorbed => 0
  | mode => table.never who - blueprint.modeContinuationValue who mode

private theorem historyPayoff_eq_potential_add_residual
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) {t : ℕ}
    (history : (publicQuittingGame table
      blueprint.signalData.law).Hist t) :
    (match history.2 with
      | .absorbed quitters => table.terminal quitters who
      | _ => table.never who) =
      blueprint.payoffPotential who t history +
        blueprint.payoffResidual who (blueprint.mode t history) := by
  have hmatches := blueprint.mode_state t history
  cases hmode : blueprint.mode t history with
  | draw source =>
      cases hstate : history.2 <;>
        simp [MMatrixPayoffMode.MatchesState, hmode, hstate] at hmatches ⊢
      · cases source <;>
          simp [payoffPotential, payoffResidual, modeContinuationValue,
            hmode]
  | active target =>
      cases hstate : history.2 <;>
        simp [MMatrixPayoffMode.MatchesState, hmode, hstate] at hmatches ⊢
      · simp [payoffPotential, payoffResidual, modeContinuationValue,
          hmode]
  | absorbed =>
      cases hstate : history.2 <;>
        simp [MMatrixPayoffMode.MatchesState, hmode, hstate] at hmatches ⊢
      · unfold payoffPotential payoffResidual
        rw [hmode, hstate]
        simp

theorem publicFinitePayoff_sub_value_eq_expect_residual
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (t : ℕ) :
    publicFinitePayoff table blueprint.signalData.law blueprint.strategy
        t who - value who =
      expect (blueprint.modeDist t) (blueprint.payoffResidual who) := by
  have hreplace : publicFinitePayoff table blueprint.signalData.law
      blueprint.strategy t who =
      expect ((publicQuittingGame table
          blueprint.signalData.law).histDist
        blueprint.strategy .draw t) (fun history =>
          blueprint.payoffPotential who t history +
            blueprint.payoffResidual who (blueprint.mode t history)) := by
    unfold publicFinitePayoff
    apply Math.ProbabilityMassFunction.expect_congr_on_support
    intro history _
    have hmatches := blueprint.mode_state t history
    cases hmode : blueprint.mode t history with
    | draw source =>
        cases hstate : history.2 <;>
          simp [MMatrixPayoffMode.MatchesState, hmode, hstate]
            at hmatches ⊢
        · cases source <;>
            simp [payoffPotential, payoffResidual, modeContinuationValue,
              hmode]
    | active target =>
        cases hstate : history.2 <;>
          simp [MMatrixPayoffMode.MatchesState, hmode, hstate]
            at hmatches ⊢
        · simp [payoffPotential, payoffResidual, modeContinuationValue,
            hmode]
    | absorbed =>
        cases hstate : history.2 <;>
          simp [MMatrixPayoffMode.MatchesState, hmode, hstate]
            at hmatches ⊢
        · unfold payoffPotential payoffResidual
          rw [hmode, hstate]
          simp
  have hmodeExpect : expect (blueprint.modeDist t)
      (blueprint.payoffResidual who) =
      expect ((publicQuittingGame table
          blueprint.signalData.law).histDist
        blueprint.strategy .draw t) (fun history =>
          blueprint.payoffResidual who (blueprint.mode t history)) := by
    unfold modeDist
    rw [Math.Probability.expect_map]
  rw [hreplace, Math.Probability.expect_add,
    blueprint.expect_payoffPotential_eq_value who t, hmodeExpect]
  ring

theorem abs_expect_payoffResidual_le
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) (distribution : PMF (MMatrixPayoffMode table)) :
    |expect distribution (blueprint.payoffResidual who)| ≤
      ‖blueprint.payoffResidual who‖ *
        expect distribution (Math.Probability.transientCharge
          (absorbedCore (table := table))) := by
  let residual := blueprint.payoffResidual who
  let transient := Math.Probability.transientCharge
    (absorbedCore (table := table))
  let bound := ‖residual‖
  have hpoint : ∀ mode, |residual mode| ≤ bound * transient mode := by
    intro mode
    cases mode with
    | absorbed =>
        have hmem : (.absorbed : MMatrixPayoffMode table) ∈
            absorbedCore := trivial
        rw [show transient (.absorbed : MMatrixPayoffMode table) = 0 by
          exact Math.Probability.transientCharge_of_mem hmem]
        simp [residual, payoffResidual]
    | draw source =>
        rw [show transient (.draw source) = 1 by
          exact Math.Probability.transientCharge_of_not_mem (by
            intro hmem
            exact hmem)]
        rw [mul_one]
        exact norm_le_pi_norm residual (.draw source)
    | active target =>
        rw [show transient (.active target) = 1 by
          exact Math.Probability.transientCharge_of_not_mem (by
            intro hmem
            exact hmem)]
        rw [mul_one]
        exact norm_le_pi_norm residual (.active target)
  have hupper := Math.Probability.expect_mono distribution residual
    (fun mode => bound * transient mode)
    (fun mode => (le_abs_self _).trans (hpoint mode))
  have hlower := Math.Probability.expect_mono distribution
    (fun mode => -(bound * transient mode)) residual
    (fun mode => neg_le_of_abs_le (hpoint mode))
  have hscale : expect distribution (fun mode => bound * transient mode) =
      bound * expect distribution transient :=
    Math.Probability.expect_const_mul distribution bound transient
  have hnegScale : expect distribution
      (fun mode => -(bound * transient mode)) =
      -(bound * expect distribution transient) := by
    rw [Math.Probability.expect_neg, hscale]
  rw [hscale] at hupper
  rw [hnegScale] at hlower
  exact abs_le.mpr ⟨hlower, hupper⟩

theorem payoff_eq_value
    (blueprint : MMatrixPayoffBlueprint table hM value accuracy)
    (who : ι) :
    publicQuittingPayoff table blueprint.signalData.law
      blueprint.strategy who = value who := by
  let payoff := publicQuittingPayoff table blueprint.signalData.law
    blueprint.strategy who
  let finitePayoff := fun t => publicFinitePayoff table
    blueprint.signalData.law blueprint.strategy t who
  let residual := blueprint.payoffResidual who
  let bound := ‖residual‖
  have hclose : ∀ δ : ℝ, 0 < δ → |payoff - value who| < δ := by
    intro δ hδ
    let threshold := δ / (2 * (bound + 1))
    have hbound : 0 ≤ bound := norm_nonneg _
    have hthreshold : 0 < threshold := by
      dsimp only [threshold]
      positivity
    let certificate := Classical.choice blueprint.modeKernel_transienceCertificate
    obtain ⟨steps, hsteps⟩ :=
      certificate.exists_iter_transientCharge_lt hthreshold (.draw none)
    have heventually : ∀ᶠ t in atTop, |finitePayoff t - payoff| < δ / 2 := by
      have htendsto : Tendsto finitePayoff atTop (nhds payoff) := by
        dsimp only [finitePayoff, payoff]
        exact tendsto_publicFinitePayoff table blueprint.signalData.law
          blueprint.strategy who
      have hevent := htendsto.eventually
        (Metric.ball_mem_nhds _ (half_pos hδ))
      filter_upwards [hevent] with t ht
      simpa only [Metric.mem_ball, Real.dist_eq] using ht
    have hafter : ∀ᶠ t in atTop, steps ≤ t := eventually_ge_atTop steps
    obtain ⟨t, hpayoff, hstepsLe⟩ := (heventually.and hafter).exists
    have hpad := Math.Probability.expect_iter_add_transientCharge_le
      blueprint.absorbedCore_closed steps (t - steps) (.draw none)
    rw [Nat.add_sub_of_le hstepsLe] at hpad
    have htransientIter : expect
        (Math.PMFIter.iter blueprint.modeKernel t (.draw none))
          (Math.Probability.transientCharge
            (absorbedCore (table := table))) < threshold :=
      hpad.trans_lt hsteps
    have htransient : expect (blueprint.modeDist t)
        (Math.Probability.transientCharge
          (absorbedCore (table := table))) < threshold := by
      rw [blueprint.modeDist_eq_iter]
      exact htransientIter
    have hresidualBound := blueprint.abs_expect_payoffResidual_le
      who (blueprint.modeDist t)
    have hscaled : bound * expect (blueprint.modeDist t)
        (Math.Probability.transientCharge
          (absorbedCore (table := table))) < δ / 2 := by
      have htransientNonneg : 0 ≤ expect (blueprint.modeDist t)
          (Math.Probability.transientCharge
            (absorbedCore (table := table))) := by
        apply Math.Probability.expect_nonneg
        exact Math.Probability.transientCharge_nonneg
          (absorbedCore (table := table))
      calc
        bound * expect (blueprint.modeDist t)
              (Math.Probability.transientCharge
                (absorbedCore (table := table))) ≤
            (bound + 1) * expect (blueprint.modeDist t)
              (Math.Probability.transientCharge
                (absorbedCore (table := table))) := by
          exact mul_le_mul_of_nonneg_right (by linarith) htransientNonneg
        _ < (bound + 1) * threshold :=
          mul_lt_mul_of_pos_left htransient (by positivity)
        _ = δ / 2 := by
          dsimp only [threshold]
          field_simp [ne_of_gt (show 0 < bound + 1 by positivity)]
    have hfinite : |finitePayoff t - value who| < δ / 2 := by
      have heq := blueprint.publicFinitePayoff_sub_value_eq_expect_residual
        who t
      dsimp only [finitePayoff]
      rw [heq]
      exact hresidualBound.trans_lt hscaled
    calc
      |payoff - value who| =
          |(payoff - finitePayoff t) + (finitePayoff t - value who)| := by
        congr 1
        ring
      _ ≤ |payoff - finitePayoff t| + |finitePayoff t - value who| :=
        abs_add_le _ _
      _ < δ / 2 + δ / 2 := add_lt_add
        (by simpa [abs_sub_comm] using hpayoff) hfinite
      _ = δ := by ring
  have habs : |payoff - value who| = 0 := by
    apply le_antisymm
    · exact le_of_forall_pos_le_add fun δ hδ => by
        simpa using (hclose δ hδ).le
    · exact abs_nonneg _
  dsimp only [payoff] at habs ⊢
  exact sub_eq_zero.mp (abs_eq_zero.mp habs)

end MMatrixPayoffBlueprint

/-! **Theorem 4.3 (paper).** If `R̂` is an M-matrix, every vector in
`D~ = conv(r¹,…,rⁿ)∩ℝⁿ_≥0` is a sunspot equilibrium payoff. -/

/-! Theorem 4.3: an M-matrix makes every `v∈D~` a sunspot equilibrium payoff. -/
theorem theorem4_3
    (table : Table ι)
    (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table)
    (hM : MMatrix (NormalMatrix table)) :
    ∀ value ∈ TildeD table, SunspotEquilibriumPayoff table value := by
  sorry

/-! ## Section 5 — discussion and open problems -/

/-! **Section 5.1 (paper).** The alpha players are the intersection of the
decreasing recursion
`I₀=I`,
`Iₗ₊₁={i∈Iₗ | ∃j∈Iₗ\{i}, rʲ_i≤0}`.
This is different from the min--max normal-player set in Section 2.3. Under
Assumption 2.1 it is exactly the production `normalCore` below. -/

abbrev AlphaPlayers (table : Table ι) :=
  normalCore (normalizedSoloMatrix table.zeroNeverReward)

abbrev AlphaMatrix (table : Table ι) :=
  normalizedNormalPlayerMatrix table.zeroNeverReward

def HasAlphaPlayers (table : Table ι) : Prop :=
  (AlphaPlayers table).Nonempty

/-! Every alpha player is normal, as the paper notes after equation (19). -/
theorem alphaPlayer_isNormal
    (table : Table ι) (hnormalized : SoloExitNormalized table)
    (hbounded : TablePayoffsBounded table) (who : AlphaPlayers table) :
    IsNormalPlayer table who.1 := by
  by_contra habnormal
  obtain ⟨owner, hne, hnonpositive⟩ :=
    exists_blocker_of_mem_normalCore
      (normalizedSoloMatrix table.zeroNeverReward) who.2
  have hmatrix := congr_fun
    (congr_fun
      (normalizedSoloMatrix_zeroNeverReward_eq_singletonMatrix
        table hnormalized) who.1) owner
  rw [hmatrix] at hnonpositive
  have hpositive := lemma2_6 table hnormalized hbounded habnormal hne
  exact (not_lt_of_ge hnonpositive) hpositive

/-! **Theorem 5.1(1) (paper).** If the alpha-player matrix has no
nontrivial homogeneous solution and is not Q, stationary approximate
equilibria exist. The checked production theorem proves the stronger claim
that these equilibria approach one fixed payoff. -/
theorem theorem5_1_nonQ
    (table : Table ι)
    (hnormal : HasAlphaPlayers table)
    (hzero : ¬HasNontrivialZeroProjectiveLCPSolution (AlphaMatrix table))
    (hnotQ : ¬QMatrix (AlphaMatrix table)) :
    StationaryEpsilonEquilibria table := by
  rw [hasNontrivialZeroProjectiveLCPSolution_iff_homogeneous] at hzero
  have hnotStandard : ¬IsStandardQMatrix (AlphaMatrix table) := by
    rw [← isProjectiveQMatrix_iff_standard_of_noHomogeneous
      (AlphaMatrix table) hzero]
    exact hnotQ
  let branch : OrdinaryNonQMatrixBranch table.zeroNeverReward :=
    { normal_nonempty := hnormal
      no_homogeneous := hzero
      normal_not_standardQ := hnotStandard }
  obtain ⟨value, hvalue⟩ :=
    exists_stationaryUniformEquilibriumPayoff_of_ordinaryNonQMatrixBranch
      table.zeroNeverReward branch
  change table.StationaryεEquilibriumExistence
  rw [QuittingPayoffTable.stationaryεEquilibriumExistence_iff]
  exact hvalue.hasApproximateEquilibria

/-! Theorem 5.1(2) repeats the Q-side public-signal conclusion of Theorem
2.13 with alpha players. Its semantic proof is the same kiloblock compiler
isolated in Sections 3.3--3.4 above. -/

/-! The paper discusses stopping games, absorbing games, the need for a
uniform lower bound on quitting probabilities, finite-range recursions, the
inverse-positivity question for zero-diagonal Q-matrices, and the question
whether a column with strictly negative off-diagonal entries forces Q. These
are open-problem prose, not Lean theorem declarations. -/

end
end Literature.SolanAndSolan2020
