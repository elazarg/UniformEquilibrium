# Research

This directory contains compileable Lean whose interface, relevance, or
generality is not settled. Files follow the repository trust policy even when
they are orphaned from the integrated umbrella.

Promote accepted game-semantic results to `UniformEquilibrium` and accepted
game-independent results to `MathUE` or GameTheory's shared `Math` library.
