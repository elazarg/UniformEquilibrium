import UniformEquilibrium.Diagnostics.Quitting.CounterexampleRegimeCoalitionLocks

/-!
# Axiom audit for promoted pure coalition locks

This isolated experiment keeps `#print axioms` output out of the production
diagnostics import graph while checking the two main promoted conclusions.
-/
